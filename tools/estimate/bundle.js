/* 空調王 本体 202609122100 — この中身は build.mjs が作っています。手で直さないこと */
window.__KUCHOO_BUNDLE__({"v":"202609122100","html":"\n<!-- ====================== アプリ画面（印刷時は非表示） ====================== -->\n<div id=\"app\">\n\n  <header class=\"topbar\">\n    <div class=\"topbar-inner\">\n      <div class=\"brand\">\n        <span class=\"brand-mark\" id=\"brand-mark\">空調</span>\n        <div>\n          <strong>空調王</strong>\n          <small id=\"brand-company\">自社情報が未設定です</small>\n        </div>\n      </div>\n      <nav class=\"tabs\">\n        <button class=\"tab is-active\" data-view=\"edit\">見積作成</button>\n        <button class=\"tab\" data-view=\"list\">現場</button>\n        <button class=\"tab\" data-view=\"master\">単価マスタ</button>\n        <button class=\"tab\" data-view=\"settings\">自社情報</button>\n      </nav>\n    </div>\n  </header>\n\n  <div class=\"bk-warn\" id=\"bk-warn\" style=\"display:none\">\n    <span id=\"bk-warn-text\"></span>\n    <button class=\"btn btn-primary btn-sm\" id=\"btn-bk-warn-save\">いますぐ保存</button>\n    <button class=\"btn btn-ghost btn-sm\" id=\"btn-bk-warn-hide\">あとで</button>\n  </div>\n\n  <div class=\"bk-warn sync-warn\" id=\"sync-warn\" style=\"display:none\">\n    <span>もう一方の端末の変更を受け取りました。</span>\n    <button class=\"btn btn-primary btn-sm\" id=\"btn-sync-reload\">画面を新しくする</button>\n  </div>\n\n  <div class=\"bk-warn a2hs-warn\" id=\"a2hs-warn\" style=\"display:none\">\n    <span><b>iPhoneをお使いの方へ。</b>Safariは<b>7日間開かないと、入れた内容を消してしまいます。</b>\n      ホーム画面に追加すると消えなくなります。</span>\n    <button class=\"btn btn-primary btn-sm\" id=\"btn-a2hs-how\">やり方を見る</button>\n    <button class=\"btn btn-ghost btn-sm\" id=\"btn-a2hs-hide\">あとで</button>\n  </div>\n\n  <div class=\"bk-warn new-warn\" id=\"new-warn\" style=\"display:none\">\n    <span>空調王の<b>新しい版</b>が出ています。押すと入れ替わります（入力した内容は消えません）。</span>\n    <button class=\"btn btn-primary btn-sm\" id=\"btn-new-reload\">新しくする</button>\n  </div>\n\n  <main class=\"wrap\">\n\n    <!-- ============ 見積作成 ============ -->\n    <section class=\"view is-active\" id=\"view-edit\">\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">基本情報</h2>\n        <div class=\"grid\">\n          <label class=\"f\"><span>見積番号</span><input type=\"text\" id=\"m-no\"></label>\n          <label class=\"f\"><span>発行日</span><input type=\"date\" id=\"m-date\"></label>\n          <label class=\"f f-wide\"><span>宛名（お客様名）</span>\n            <div class=\"f-row\">\n              <input type=\"text\" id=\"m-customer\" placeholder=\"株式会社〇〇\">\n              <select id=\"m-honorific\">\n                <option>御中</option>\n                <option>様</option>\n                <option>（なし）</option>\n              </select>\n            </div>\n          </label>\n          <label class=\"f f-wide\"><span>件名</span><input type=\"text\" id=\"m-subject\" placeholder=\"事務所 業務用エアコン更新工事\"></label>\n          <label class=\"f f-wide\"><span>工事場所</span><input type=\"text\" id=\"m-site\" placeholder=\"山形県〇〇市〇〇 1-2-3\"></label>\n          <label class=\"f\"><span>有効期限（日）</span><input type=\"number\" id=\"m-valid\" min=\"0\" step=\"1\"></label>\n          <label class=\"f\"><span>工期・納期</span><input type=\"text\" id=\"m-delivery\"></label>\n          <label class=\"f f-wide\"><span>お支払条件</span><input type=\"text\" id=\"m-payment\"></label>\n        </div>\n      </div>\n\n      <div class=\"card\">\n        <div class=\"card-head\">\n          <h2 class=\"card-title\">明細</h2>\n          <div class=\"card-actions\">\n            <label class=\"cost-toggle\" title=\"原価と粗利を画面に出します。見積書には印刷されません\">\n              <input type=\"checkbox\" id=\"chk-cost\"><span>原価・粗利を見る</span>\n            </label>\n            <button class=\"btn btn-ghost\" id=\"btn-add-blank\">＋ 空の行</button>\n            <button class=\"btn btn-ghost\" id=\"btn-strip-spec\">仕様を品番だけに</button>\n            <button class=\"btn btn-ghost\" id=\"btn-clear-lines\">明細をクリア</button>\n          </div>\n        </div>\n\n        <!-- 単価表から追加 -->\n        <div class=\"picker\">\n          <div class=\"picker-head\">\n            <span class=\"picker-label\">単価表から追加</span>\n            <input type=\"search\" id=\"picker-search\" placeholder=\"品名で検索（例：配管、天カセ、取外し）\">\n          </div>\n          <div class=\"picker-cats\" id=\"picker-cats\"></div>\n          <div class=\"picker-items\" id=\"picker-items\"></div>\n        </div>\n\n        <!-- 機器を選ぶ（メーカーの機種データから絞り込んで明細に入れる） -->\n        <div class=\"chooser\" id=\"chooser\">\n          <div class=\"picker-head\">\n            <span class=\"picker-label\">機器を選ぶ</span>\n            <span class=\"chooser-src\" id=\"chooser-src\"></span>\n            <button class=\"btn btn-ghost btn-sm\" id=\"btn-chooser-reset\" type=\"button\">最初から</button>\n          </div>\n          <div id=\"chooser-body\"></div>\n          <!-- 選んだ機種に付けられる別売品 -->\n          <div id=\"chooser-options\"></div>\n        </div>\n\n        <!-- 明細テーブル -->\n        <div class=\"lines-wrap\">\n          <table class=\"lines\" id=\"lines-table\">\n            <thead>\n              <tr>\n                <th class=\"c-move\"></th>\n                <th class=\"c-name\">品名・仕様</th>\n                <th class=\"c-qty\">数量</th>\n                <th class=\"c-unit\">単位</th>\n                <th class=\"c-rate\">掛率</th>\n                <th class=\"c-price\">単価</th>\n                <th class=\"c-amount\">金額</th>\n                <th class=\"c-cost\" title=\"その行ぶんの原価（数量を掛けた額）を入れてください。1つあたりの額は下に小さく出ます\">原価</th>\n                <th class=\"c-margin\">粗利率</th>\n                <th class=\"c-del\"></th>\n              </tr>\n            </thead>\n            <tbody id=\"lines-body\"></tbody>\n          </table>\n          <p class=\"empty-note\" id=\"lines-empty\">上の「単価表から追加」から項目をタップして明細を作ります。</p>\n        </div>\n      </div>\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">金額の調整</h2>\n        <div class=\"grid\">\n          <label class=\"f\"><span>諸経費（小計の%）</span><input type=\"number\" id=\"m-overhead\" min=\"0\" step=\"0.1\"></label>\n          <label class=\"f\"><span>値引き（円・プラス数値で入力）</span><input type=\"number\" id=\"m-discount\" min=\"0\" step=\"1\"></label>\n          <label class=\"f\"><span>消費税率（%）</span><input type=\"number\" id=\"m-tax\" min=\"0\" step=\"0.1\"></label>\n          <label class=\"f\"><span>1人工の金額（円）</span><input type=\"number\" id=\"m-manday\" min=\"0\" step=\"1000\"></label>\n          <label class=\"f\"><span>単価の端数（行ごと）</span>\n            <select id=\"m-unit-round\">\n              <option value=\"0\">そのまま</option>\n              <option value=\"10\">10円未満は繰り上げ</option>\n              <option value=\"100\">100円未満は繰り上げ</option>\n            </select>\n          </label>\n          <label class=\"f\"><span>端数処理（諸経費・消費税）</span>\n            <select id=\"m-rounding\">\n              <option value=\"floor\">切り捨て</option>\n              <option value=\"round\">四捨五入</option>\n              <option value=\"ceil\">切り上げ</option>\n            </select>\n          </label>\n        </div>\n\n        <div class=\"totals\" id=\"totals\"></div>\n      </div>\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">備考</h2>\n        <textarea id=\"m-note\" rows=\"4\" placeholder=\"現地状況により変更となる場合があります など\"></textarea>\n      </div>\n\n      <div class=\"actionbar\">\n        <button class=\"btn btn-ghost\" id=\"btn-new\">新規作成</button>\n        <button class=\"btn btn-ghost\" id=\"btn-save\">保存</button>\n        <button class=\"btn btn-ghost\" id=\"btn-preview\">プレビュー</button>\n        <button class=\"btn btn-pdf\" id=\"btn-pdf\">PDFにする</button>\n        <button class=\"btn btn-primary\" id=\"btn-print\">印刷 / PDF</button>\n      </div>\n    </section>\n\n    <!-- ============ 保存済み ============ -->\n    <section class=\"view\" id=\"view-list\">\n      <!-- 現場の一覧 -->\n      <div class=\"card\" id=\"site-list-card\">\n        <div class=\"card-head\">\n          <h2 class=\"card-title\">現場</h2>\n          <div class=\"card-actions\">\n            <button class=\"btn btn-primary\" id=\"btn-new-site\">＋ 現場を追加</button>\n            <button class=\"btn btn-ghost\" id=\"btn-paste-survey\">現調シートを貼り付け</button>\n            <button class=\"btn btn-ghost\" id=\"btn-export-estimates\">バックアップを書き出す</button>\n            <label class=\"btn btn-ghost file-btn\">読み込む<input type=\"file\" id=\"file-import-estimates\" accept=\"application/json\"></label>\n          </div>\n        </div>\n        <input type=\"search\" id=\"site-search\" placeholder=\"現場名・お客様名で検索\">\n        <div id=\"site-list\"></div>\n      </div>\n\n      <!-- 現場の中身（見積・請求書・現調シート） -->\n      <div class=\"card\" id=\"site-detail-card\" style=\"display:none\">\n        <div class=\"card-head\">\n          <button class=\"btn btn-ghost btn-sm\" id=\"btn-site-back\">← 現場一覧</button>\n          <div class=\"card-actions\">\n            <button class=\"btn btn-ghost\" id=\"btn-site-edit\">現場情報を編集</button>\n            <button class=\"btn btn-ghost btn-danger\" id=\"btn-site-del\">現場を削除</button>\n          </div>\n        </div>\n        <div id=\"site-detail\"></div>\n      </div>\n    </section>\n\n    <!-- ============ 単価マスタ ============ -->\n    <section class=\"view\" id=\"view-master\">\n      <div class=\"card\">\n        <div class=\"card-head\">\n          <h2 class=\"card-title\">単価マスタ</h2>\n          <div class=\"card-actions\">\n            <label class=\"cost-toggle\" title=\"原価の列を出します。見積書には印刷されません\">\n              <input type=\"checkbox\" id=\"chk-cost-m\"><span>原価を見る</span>\n            </label>\n            <button class=\"btn btn-ghost\" id=\"btn-export-pb\">バックアップを書き出す</button>\n            <label class=\"btn btn-ghost file-btn\">読み込む<input type=\"file\" id=\"file-import-pb\" accept=\"application/json\"></label>\n            <button class=\"btn btn-ghost btn-danger\" id=\"btn-reset-pb\">初期値に戻す</button>\n          </div>\n        </div>\n        <p class=\"hint\">\n          ここで書き換えた金額は<strong>この端末のブラウザの中だけ</strong>に保存されます。GitHubやサイトには出ません。<br>\n          スマホとパソコンの両方で使うときは、［バックアップを書き出す］でファイルを作り、もう一方の端末で［読み込む］してください。\n        </p>\n\n        <div class=\"csv-box\">\n          <div class=\"csv-head\">\n            <strong>CSVでまとめて取り込む</strong>\n            <span>因幡電工などメーカーの価格表を、Excelで CSV 形式にして読み込めます。</span>\n          </div>\n          <div class=\"csv-row\">\n            <label class=\"f\">\n              <span>カテゴリ列が無いときの入れ先</span>\n              <select id=\"csv-target\"></select>\n            </label>\n            <div class=\"csv-btns\">\n              <label class=\"btn btn-primary file-btn\">CSVを選ぶ<input type=\"file\" id=\"file-csv\" accept=\".csv,.txt,text/csv,text/plain\"></label>\n              <button class=\"btn btn-ghost\" id=\"btn-csv-template\">テンプレートCSVを取得</button>\n            </div>\n          </div>\n          <label class=\"check\">\n            <input type=\"checkbox\" id=\"csv-replace\">\n            <span>取り込む前に、そのカテゴリの中身をいったん空にする（入れ直したいとき用）</span>\n          </label>\n          <label class=\"csv-check\">\n            <input type=\"checkbox\" id=\"csv-costonly\">\n            <span><b>仕入先の見積を読ませる（原価だけ入れる・行は増やさない）</b><br>\n              <small>品番でいまの項目を探して、原価だけを書き込みます。定価も入っていれば掛率を計算して、\n              ［自社情報］の仕入掛率に入れるかどうかを聞きます。</small></span>\n          </label>\n          <p class=\"csv-note\">\n            見出し行は <code>カテゴリ / 品番 / 品名 / 規格 / 単位 / 定価</code>。並び順は自由で、無い列はとばせます。<br>\n            「型番」「希望小売価格」など、よくある別の言い方でも認識します。Excel から出した Shift_JIS のCSVもそのまま読めます。\n          </p>\n\n          <div class=\"paste-box\">\n            <label class=\"f\">\n              <span>Excel やメモ帳からそのまま貼り付ける（ファイルにしなくてOK）</span>\n              <textarea id=\"paste-area\" rows=\"4\" placeholder=\"Excelで範囲を選んでコピー → ここに貼り付け&#10;LD-70\tスリムダクト LD ダクト\t本\t1200&#10;HPC-2320\tペアコイル 2分3分\t巻\t18000\"></textarea>\n            </label>\n            <div class=\"csv-btns\">\n              <button class=\"btn btn-primary\" id=\"btn-paste-import\">貼り付けた内容を取り込む</button>\n              <button class=\"btn btn-ghost\" id=\"btn-paste-clear\">消す</button>\n            </div>\n          </div>\n        </div>\n\n        <div class=\"csv-box\">\n          <div class=\"csv-head\">\n            <strong>機器の機種データを読み込む（本体価格）</strong>\n            <span>メーカーの機種データを入れると、見積作成画面の「機器を選ぶ」でシリーズ・馬力・電源などから絞り込めるようになります。<br>\n              ファイルは<b>まとめて選べます</b>。スマホ⇄PCの連動を使っていれば、片方で入れればもう片方にも入るので、\n              スマホでファイルを探す必要はありません。</span>\n          </div>\n          <div class=\"csv-row\">\n            <div class=\"csv-btns\">\n              <label class=\"btn btn-primary file-btn\">機種データを選ぶ<input type=\"file\" id=\"file-models\" accept=\"application/json,.json\" multiple></label>\n              <button class=\"btn btn-ghost btn-danger\" id=\"btn-models-clear\">機種データを削除</button>\n            </div>\n          </div>\n          <div class=\"csv-note ng file-hint\" id=\"models-hint\" hidden></div>\n          <div class=\"csv-note\" id=\"models-status\">まだ読み込まれていません。</div>\n        </div>\n\n        <div class=\"csv-box\">\n          <div class=\"csv-head\">\n            <strong>カタログPDFから機種データを作る</strong>\n            <span>メーカーのカタログPDFを入れると、<b>この端末の中だけで</b>読み取って機種データを作ります。\n              PDFも、読み取った中身も、どこにも送りません。<br>\n              <b>パソコンでやってください。</b>カタログが大きいので、スマホでは重すぎます。</span>\n          </div>\n          <div class=\"csv-row\">\n            <div class=\"csv-btns\">\n              <select id=\"catalog-maker\" class=\"catalog-maker\"></select>\n              <label class=\"btn btn-primary file-btn\">カタログのファイルを選ぶ<input type=\"file\" id=\"file-catalog\" accept=\".pdf,.json,application/pdf,application/json\"></label>\n            </div>\n          </div>\n          <div class=\"csv-note\" id=\"catalog-howto\"></div>\n          <div class=\"csv-note\" id=\"catalog-status\">カタログPDFを選ぶと読み取りが始まります。</div>\n          <div class=\"csv-row\">\n            <div class=\"csv-btns\">\n              <label class=\"btn btn-ghost file-btn\">別売品データを選ぶ<input type=\"file\" id=\"file-options\" accept=\"application/json,.json\" multiple></label>\n              <button class=\"btn btn-ghost\" id=\"btn-options-save\">別売品データを書き出す</button>\n            </div>\n          </div>\n          <div class=\"csv-note\">別売品（パネル・リモコンなど）は、上のメーカー選びで「（別売品）」と付いたものを読むと入ります。1度読んだら書き出しておくと、次の端末では選ぶだけで済みます。</div>\n          <div class=\"csv-note ng file-hint\" id=\"options-hint\" hidden></div>\n          <div class=\"csv-note\" id=\"options-status\">まだ読み込まれていません。</div>\n        </div>\n\n        <div id=\"master-editor\"></div>\n        <div class=\"card-actions\" style=\"margin-top:16px\">\n          <button class=\"btn btn-ghost\" id=\"btn-add-cat\">＋ カテゴリを追加</button>\n          <button class=\"btn btn-ghost\" id=\"btn-autocolor\">色がついていない項目に色をつける</button>\n        </div>\n      </div>\n    </section>\n\n    <!-- ============ 自社情報 ============ -->\n    <section class=\"view\" id=\"view-settings\">\n      <div class=\"card\">\n        <div class=\"card-head\">\n          <h2 class=\"card-title\">自社情報（見積書に印刷されます）</h2>\n          <div class=\"card-actions\" id=\"preset-actions\"></div>\n        </div>\n        <p class=\"hint\">\n          ここで入れた内容は<strong>この端末のブラウザの中</strong>に保存されます（連動を設定した場合は、暗号にしてクラウドにも預けます）。<br>\n          同じURLを別の会社の方が開いても、それぞれ自分の会社の情報が入るので、お互いの情報は見えません。\n        </p>\n        <div class=\"grid\">\n          <label class=\"f f-wide\"><span>会社名</span><input type=\"text\" id=\"c-name\"></label>\n          <label class=\"f\"><span>代表者・担当者</span><input type=\"text\" id=\"c-owner\"></label>\n          <label class=\"f\"><span>郵便番号</span><input type=\"text\" id=\"c-zip\"></label>\n          <label class=\"f f-wide\"><span>住所</span><input type=\"text\" id=\"c-address\"></label>\n          <label class=\"f\"><span>電話番号</span><input type=\"text\" id=\"c-tel\"></label>\n          <label class=\"f\"><span>メール</span><input type=\"text\" id=\"c-email\"></label>\n          <label class=\"f\"><span>ウェブサイト</span><input type=\"text\" id=\"c-web\"></label>\n          <label class=\"f\"><span>登録番号（インボイス）</span><input type=\"text\" id=\"c-invoice\"></label>\n          <label class=\"f f-wide\"><span>振込先</span><input type=\"text\" id=\"c-bank\" placeholder=\"〇〇銀行 〇〇支店 普通 1234567 カ）〇〇〇〇\"></label>\n          <label class=\"f\"><span>1人工の金額（円・新しい見積の初期値）</span><input type=\"number\" id=\"c-manday\" min=\"0\" step=\"1000\"></label>\n          <label class=\"f\"><span>原価の1人工（円・社内用）</span><input type=\"number\" id=\"c-manday-cost\" min=\"0\" step=\"500\"></label>\n          <label class=\"f\"><span>材料の仕入掛率（下の表に当たらないとき・定価の%）</span><input type=\"number\" id=\"c-material-cost\" min=\"0\" max=\"100\" step=\"1\"></label>\n          <div class=\"f f-wide\">\n            <span>メーカー・シリーズごとの仕入掛率（社内用）</span>\n            <small class=\"rate-note\">商社はメーカーごと、寒冷地モデルかどうかで掛率を変えます。シリーズを空にすると、そのメーカー全部に当たります。</small>\n            <div class=\"rate-head\"><b>メーカー</b><b>シリーズ</b><b>掛率%</b><i></i></div>\n            <div id=\"cost-rates\"></div>\n            <button class=\"btn btn-ghost btn-sm\" id=\"btn-rate-add\" type=\"button\">＋ 行を足す</button>\n          </div>\n          <label class=\"f\"><span>機種に乗せる利益（見積に出す金額 ＝ 原価 ÷ この数）</span>\n            <input type=\"number\" id=\"c-model-div\" min=\"0\" max=\"1\" step=\"0.01\" placeholder=\"0.65\">\n            <small class=\"rate-note\" id=\"c-model-div-note\"></small>\n          </label>\n          <label class=\"f\"><span>単価の端数（新しい見積の初期値）</span>\n            <select id=\"c-unit-round\">\n              <option value=\"0\">そのまま</option>\n              <option value=\"10\">10円未満は繰り上げ</option>\n              <option value=\"100\">100円未満は繰り上げ</option>\n            </select>\n          </label>\n          <label class=\"f f-wide\"><span>見積書の下部に入れる注意書き</span><textarea id=\"c-footer\" rows=\"3\"></textarea></label>\n        </div>\n        <div class=\"card-actions\" style=\"margin-top:16px\">\n          <button class=\"btn btn-primary\" id=\"btn-save-company\">保存</button>\n          <a class=\"btn btn-ghost\" href=\"manual.html\" target=\"_blank\" rel=\"noopener\">使い方（説明書）</a>\n          <p class=\"app-ver\" id=\"app-ver\"></p>\n        </div>\n      </div>\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">端末を移すとき（全データのバックアップ）</h2>\n        <p class=\"hint\">\n          <strong>スマホとパソコンの両方で使うときは、これ1つで全部運べます。</strong><br>\n          自社情報・ロゴ・社判・単価マスタ・機種データ・現場・現調シート・見積・請求書を、\n          まとめて1つのファイルに書き出します。<br>\n          <small>※読み込むと、いまの内容はすべて置き換わります。</small>\n        </p>\n        <div class=\"csv-btns\">\n          <button class=\"btn btn-primary\" id=\"btn-export-all\">全部まとめて書き出す</button>\n          <label class=\"btn btn-ghost file-btn\">まとめて読み込む<input type=\"file\" id=\"file-import-all\" accept=\"application/json\"></label>\n        </div>\n\n        <p class=\"hint\" style=\"margin-top:14px\">\n          <strong>現場写真は、上のファイルには入りません。</strong><br>\n          写真は1枚が大きく、まとめて入れるとバックアップが重くなるので、別のファイルにしています。\n          端末を替えるときは、こちらも書き出してください。<br>\n          <small>※読み込むときは、先に「まとめて読み込む」で現場を入れてから写真を入れてください。</small>\n        </p>\n        <div class=\"csv-btns\">\n          <button class=\"btn btn-ghost\" id=\"btn-export-photos\">写真を書き出す</button>\n          <label class=\"btn btn-ghost file-btn\">写真を読み込む<input type=\"file\" id=\"file-import-photos\" accept=\"application/json\"></label>\n        </div>\n\n        <div class=\"bk-box\" id=\"bk-box\">\n          <div class=\"bk-head\">\n            <strong>かんたんバックアップ</strong>\n            <span>保存先を1回決めておけば、次からはボタン1つで上書き保存できます。\n              <b>OneDrive の中に置くと、そのままクラウドにも残ります。</b></span>\n          </div>\n          <div class=\"bk-row\">\n            <div class=\"bk-info\">\n              <div>保存先：<span id=\"bk-target\">まだ決めていません</span></div>\n              <div>最終バックアップ：<span id=\"bk-last\">まだ</span></div>\n            </div>\n            <div class=\"csv-btns\">\n              <button class=\"btn btn-primary\" id=\"btn-bk-save\">いますぐ保存</button>\n              <button class=\"btn btn-ghost\" id=\"btn-bk-pick\">保存先を決める</button>\n            </div>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"card\" id=\"sync-card\">\n        <div class=\"card-head\">\n          <h2 class=\"card-title\">スマホとパソコンの連動</h2>\n          <div class=\"card-actions\">\n            <span class=\"sync-badge is-off\" id=\"sync-state\">連動していません</span>\n          </div>\n        </div>\n        <p class=\"hint\">\n          <strong>現場のスマホで入れた内容が、事務所のパソコンにも自動で入ります。</strong>（その逆も）<br>\n          データは<strong>この端末の中で暗号にしてから</strong>預けるので、\n          クラウド側には意味の分からない文字列しか残りません。中身を開けるのは\n          <strong>あいことばを入れた端末だけ</strong>です。<br>\n          <small>※あいことばを無くすと、預けた分は誰にも開けません。「全部まとめて書き出す」バックアップは今までどおり続けてください。</small>\n        </p>\n\n        <p class=\"hint\" id=\"sync-setup-note\" style=\"display:none\">\n          <strong>まだクラウドの設定が入っていません。</strong>（firebase-config.js）\n        </p>\n\n        <div class=\"bk-row\">\n          <div class=\"bk-info\">\n            <div>状態：<span id=\"sync-last-label\">最終同期</span> <span id=\"sync-last\">まだ</span> <span id=\"sync-err\" class=\"sync-err\"></span></div>\n            <div>預けている写真：<span id=\"sync-photos\">—</span></div>\n            <div>あいことば：<code id=\"sync-code-view\"></code></div>\n          </div>\n          <div class=\"csv-btns\" id=\"sync-off-btns\">\n            <button class=\"btn btn-primary\" id=\"btn-sync-start\">この端末で連動を始める</button>\n            <button class=\"btn btn-ghost\" id=\"btn-sync-join\">あいことばを入れる</button>\n          </div>\n          <div class=\"csv-btns\" id=\"sync-on-btns\" style=\"display:none\">\n            <button class=\"btn btn-primary\" id=\"btn-sync-now\">いま同期する</button>\n            <button class=\"btn btn-ghost\" id=\"btn-sync-show\">あいことばを表示</button>\n            <button class=\"btn btn-ghost btn-danger\" id=\"btn-sync-stop\">連動をやめる</button>\n          </div>\n        </div>\n\n        <div class=\"sync-code-box\" id=\"sync-code-box\" style=\"display:none\">\n          <p id=\"sync-code-msg\"></p>\n          <div class=\"sync-code-big\" id=\"sync-code-big\"></div>\n          <div class=\"csv-btns\">\n            <button class=\"btn btn-ghost\" id=\"btn-sync-copy\">コピーする</button>\n          </div>\n          <small>\n            <strong>紙に控えてください。</strong>忘れても、連動中の端末のこのボタンでいつでも確認できますが、\n            全部の端末が使えなくなると、預けたデータは誰にも開けません。<br>\n            <strong>人には教えないでください。</strong>あいことばを入れた端末は、中身をすべて開けます。\n          </small>\n        </div>\n      </div>\n\n      <!-- データを守る手立てのまとめ。ばらばらに置いてあると誰も全部は読まないので、\n           「消える経路」と「その塞ぎ方」を1か所に並べる -->\n      <div class=\"card\" id=\"safety-card\">\n        <h2 class=\"card-title\">入れた内容を失わないために</h2>\n        <p class=\"hint\">\n          空調王は<strong>お使いの端末の中</strong>に内容を保存します。速くて、電波が無くても動く代わりに、\n          <strong>端末の側の都合で消えることがあります。</strong>消える道は3つです。\n        </p>\n\n        <div class=\"safety-grid\">\n          <div class=\"safety-row\">\n            <b>1. ブラウザの閲覧データを消したとき</b>\n            <span>「履歴とWebサイトデータを消去」を押すと、入れた単価も見積も一緒に消えます。</span>\n            <em>塞ぎ方 → 下の連動を入れる／バックアップを取る</em>\n          </div>\n          <div class=\"safety-row\" id=\"safety-ios\">\n            <b>2. iPhoneで7日間開かなかったとき</b>\n            <span>SafariはAppleの決まりで、<strong>7日そのサイトを開かないと保存した中身を消します。</strong>\n              現場が続いて2週間ぶりに開いたら空だった、が起こり得ます。</span>\n            <em>塞ぎ方 → <strong>ホーム画面に追加する</strong>（追加したものはこの決まりの外になります）</em>\n          </div>\n          <div class=\"safety-row\">\n            <b>3. 端末が壊れた・失くした・買い替えた</b>\n            <span>その端末しか持っていない内容は、一緒に無くなります。</span>\n            <em>塞ぎ方 → 連動を入れる（もう一方の端末とクラウドに残ります）</em>\n          </div>\n        </div>\n\n        <p class=\"hint\" style=\"margin-top:14px\">\n          <strong>いちばん確実なのは、連動を入れてホーム画面に追加することです。</strong>\n          この2つをやっておけば、3つの道すべてが塞がります。\n        </p>\n        <div class=\"csv-btns\">\n          <button class=\"btn btn-primary\" id=\"btn-a2hs-how2\">ホーム画面への追加のしかた</button>\n        </div>\n\n        <div class=\"terms-box\">\n          <b class=\"terms-title\">ご利用にあたって</b>\n          <ul>\n            <li>空調王は<strong>現状のまま</strong>提供します。動作や計算結果について保証はいたしません。</li>\n            <li><strong>見積の金額・選んだ機種・必要な能力は、必ずご自身で確かめてください。</strong>\n              この道具が出す数字は目安であり、最終の判断はお使いになる方の責任で行ってください。</li>\n            <li>端末やクラウドに保存した内容が失われた場合について、責任を負いかねます。\n              上に書いた手立てをとってください。</li>\n            <li><strong>メーカーの機種データは、お使いになる方ご自身が取得したもの</strong>です。\n              各メーカーの利用条件に従ってお使いください。空調王はそれを配布しません。</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">見積書の見た目</h2>\n        <p class=\"hint\">\n          見積書・請求書の紙の形を選べます。<strong>中身（金額・明細）はどれを選んでも同じ</strong>で、\n          線の引き方と見出しの出し方だけが変わります。<br>\n          押すとすぐ切り替わり、次に出す見積書から反映されます。\n          <small>※［プレビュー］で実物を確かめられます。</small>\n        </p>\n        <div class=\"skin-pick\" id=\"skin-pick\"></div>\n\n        <h3 class=\"sub-title\">色</h3>\n        <div class=\"color-pick\" id=\"color-pick\"></div>\n        <p class=\"hint\" id=\"color-note\"></p>\n      </div>\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">ロゴ（見積書の右上に入ります）</h2>\n        <p class=\"hint\">\n          会社のロゴを入れると、見積書の会社名の上に印字されます。<strong>入れなくても大丈夫です。</strong><br>\n          対応形式：<strong>PNG / JPEG / SVG / WebP / GIF / BMP</strong>。\n          背景が透明なPNG・SVGなら、そのまま自然に乗ります。<br>\n          <small>※社判とちがい、白い背景を抜く加工はしません。背景を消したい場合は、透過PNGで用意してください。</small>\n        </p>\n\n        <div class=\"seal-setup\">\n          <div class=\"logo-preview\" id=\"logo-preview\">\n            <span class=\"seal-placeholder\">ロゴなし<br><small>会社名だけ印字</small></span>\n          </div>\n\n          <div class=\"seal-controls\">\n            <div class=\"seal-btns\">\n              <label class=\"btn btn-primary file-btn\">ロゴの画像・データを選ぶ<input type=\"file\" id=\"file-logo\" accept=\"image/png,image/jpeg,image/svg+xml,image/webp,image/gif,image/bmp,image/*\"></label>\n              <button class=\"btn btn-ghost btn-danger\" id=\"btn-logo-clear\">ロゴを削除</button>\n            </div>\n\n            <label class=\"f seal-size-field\">\n              <span>ロゴの高さ（mm）　※10〜16mm くらいが見積書になじみます（「ロゴ大きく左」では、この2倍の大きさで左上に出ます）</span>\n              <input type=\"number\" id=\"logo-size\" min=\"5\" max=\"40\" step=\"1\">\n            </label>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"card\">\n        <h2 class=\"card-title\">社判（角印・ゴム印）</h2>\n        <p class=\"hint\">\n          <strong>電子印鑑のデータ（PNG・JPEG・SVG）も、紙に押して撮った写真も、どちらでも使えます。</strong><br>\n          ・すでに背景が透明なデータ → そのまま取り込みます（加工しません）<br>\n          ・写真やスキャン → 「白い背景を透明にする」で、紙の白い部分が消えて印影だけが残ります<br>\n          写真から取り込むときは、<strong>真上から・明るいところで</strong>撮ってください。<br>\n          <small>※PDFやWord内の印影は読み込めません。PNGで書き出してからお使いください。</small>\n        </p>\n\n        <div class=\"seal-setup\">\n          <div class=\"seal-preview\" id=\"seal-preview\">\n            <span class=\"seal-placeholder\">社判なし<br><small>㊞ と印字</small></span>\n          </div>\n\n          <div class=\"seal-controls\">\n            <label class=\"check\">\n              <input type=\"checkbox\" id=\"seal-transparent\" checked>\n              <span>白い背景を透明にする（写真・スキャンのとき用。透過データには影響しません）</span>\n            </label>\n\n            <div class=\"seal-btns\">\n              <label class=\"btn btn-primary file-btn\">社判の画像・データを選ぶ<input type=\"file\" id=\"file-seal\" accept=\"image/png,image/jpeg,image/svg+xml,image/webp,image/gif,image/bmp,image/*\"></label>\n              <button class=\"btn btn-ghost btn-danger\" id=\"btn-seal-clear\">社判を削除</button>\n            </div>\n\n            <label class=\"f seal-size-field\">\n              <span>社判の大きさ（mm）　※角印は 18〜24mm くらいが実物大です</span>\n              <input type=\"number\" id=\"seal-size\" min=\"8\" max=\"45\" step=\"1\">\n            </label>\n          </div>\n        </div>\n      </div>\n    </section>\n\n  </main>\n\n  <div class=\"toast\" id=\"toast\"></div>\n</div>\n\n<!-- ====================== 見積書（プレビューと印刷で共用） ======================\n     画面で見ているものと印刷されるものが必ず同じになるよう、\n     #sheet は1つだけ作って、プレビューでも印刷でもこれを使う。\n     ========================================================================== -->\n<div class=\"preview\" id=\"preview\" hidden>\n  <div class=\"preview-bar\">\n    <b class=\"preview-title\" id=\"pv-title\">プレビュー</b>\n    <small class=\"preview-hint\">紙に出したときの見た目です。<b>1枚ずつに分けて出しています</b></small>\n    <button class=\"btn btn-sm btn-pdf\" type=\"button\" id=\"pv-pdf\">PDFにする</button>\n    <button class=\"btn btn-sm\" type=\"button\" id=\"pv-print\">印刷 / PDF</button>\n    <button class=\"btn btn-sm btn-ghost\" type=\"button\" id=\"pv-close\">閉じる</button>\n  </div>\n  <div class=\"preview-scroll\" id=\"pv-scroll\">\n    <div class=\"preview-fit\" id=\"pv-fit\">\n      <div class=\"preview-stage\" id=\"pv-stage\"><div id=\"sheet\"></div></div>\n    </div>\n  </div>\n</div>\n","css":"/* ==========================================================================\n   50Airtec 見積作成ツール（社内用）\n   ========================================================================== */\n\n*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }\n\n:root {\n  --bg: #f4f5f7;\n  --surface: #ffffff;\n  --text: #1a1d21;\n  --text-sub: #5a6068;\n  --text-light: #8a8f96;\n  --border: #e2e5e9;\n  --dark: #1a1d21;\n  --accent: #0b6bcb;\n  --accent-soft: #e8f1fb;\n  --danger: #c0392b;\n  --ok: #1e7a46;\n  --radius: 10px;\n  --font: \"Noto Sans JP\", \"Hiragino Sans\", \"Yu Gothic\", \"Meiryo\", sans-serif;\n}\n\nhtml { -webkit-text-size-adjust: 100%; }\n\nbody {\n  font-family: var(--font);\n  font-size: 15px;\n  line-height: 1.7;\n  color: var(--text);\n  background: var(--bg);\n  -webkit-font-smoothing: antialiased;\n}\n\ninput, select, textarea, button { font-family: inherit; font-size: inherit; color: inherit; }\n\n/* ---------- ヘッダー ---------- */\n.topbar {\n  position: sticky; top: 0; z-index: 50;\n  background: var(--dark);\n  color: #fff;\n}\n.topbar-inner {\n  max-width: 1100px; margin: 0 auto; padding: 10px 16px;\n  display: flex; align-items: center; gap: 20px; flex-wrap: wrap;\n}\n.brand { display: flex; align-items: center; gap: 10px; }\n.brand-mark {\n  display: grid; place-items: center;\n  width: 34px; height: 34px; border-radius: 8px;\n  background: #fff; color: var(--dark);\n  font-weight: 800; font-size: 15px; letter-spacing: -0.5px;\n}\n.brand strong { display: block; font-size: 15px; line-height: 1.3; }\n.brand small { display: block; font-size: 11px; color: #a8adb4; line-height: 1.3; }\n\n.tabs { display: flex; gap: 4px; margin-left: auto; flex-wrap: wrap; }\n.tab {\n  border: 0; background: transparent; color: #c3c8ce;\n  padding: 7px 13px; border-radius: 7px; cursor: pointer;\n  font-size: 14px; white-space: nowrap;\n}\n.tab:hover { background: rgba(255,255,255,.1); color: #fff; }\n.tab.is-active { background: #fff; color: var(--dark); font-weight: 600; }\n\n/* ---------- レイアウト ---------- */\n.wrap { max-width: 1100px; margin: 0 auto; padding: 20px 16px 120px; }\n.view { display: none; }\n.view.is-active { display: block; }\n\n.card {\n  background: var(--surface);\n  border: 1px solid var(--border);\n  border-radius: var(--radius);\n  padding: 18px;\n  margin-bottom: 16px;\n}\n.card-head { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; margin-bottom: 14px; }\n.card-title { font-size: 15px; font-weight: 700; letter-spacing: .02em; }\n.card-head .card-title { margin: 0; }\n.card > .card-title { margin-bottom: 14px; }\n.card-actions { display: flex; gap: 8px; margin-left: auto; flex-wrap: wrap; }\n\n.hint {\n  font-size: 13px; color: var(--text-sub);\n  background: #fbfbfc; border: 1px solid var(--border);\n  border-left: 3px solid var(--accent);\n  border-radius: 6px; padding: 10px 12px; margin-bottom: 16px;\n}\n\n/* ---------- フォーム ---------- */\n.grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }\n.f { display: flex; flex-direction: column; gap: 5px; grid-column: span 1; min-width: 0; }\n.f-wide { grid-column: span 2; }\n.f > span { font-size: 12px; color: var(--text-sub); font-weight: 600; }\n.f-row { display: flex; gap: 6px; }\n.f-row input { flex: 1 1 auto; min-width: 0; }\n.f-row select { flex: 0 0 auto; width: auto; }\n\ninput[type=text], input[type=date], input[type=number], input[type=search], select, textarea {\n  width: 100%;\n  border: 1px solid var(--border);\n  border-radius: 7px;\n  padding: 8px 10px;\n  background: #fff;\n  min-width: 0;\n}\ntextarea { resize: vertical; line-height: 1.6; }\ninput:focus, select:focus, textarea:focus {\n  outline: 2px solid var(--accent-soft);\n  border-color: var(--accent);\n}\n\n/* ---------- ボタン ---------- */\n.btn {\n  border: 1px solid var(--border);\n  background: #fff;\n  border-radius: 7px;\n  padding: 8px 14px;\n  cursor: pointer;\n  font-size: 14px;\n  white-space: nowrap;\n  transition: background .15s, border-color .15s;\n}\n.btn:hover { background: #f2f4f6; }\n.btn-primary { background: var(--dark); color: #fff; border-color: var(--dark); font-weight: 600; }\n.btn-primary:hover { background: #2f353c; }\n.btn-danger { color: var(--danger); border-color: #f0d4d0; }\n.btn-danger:hover { background: #fdf2f0; }\n.file-btn { position: relative; overflow: hidden; display: inline-block; }\n.file-btn input { position: absolute; inset: 0; opacity: 0; cursor: pointer; }\n\n/* ---------- 単価ピッカー ---------- */\n.picker {\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fafbfc;\n  padding: 12px;\n  margin-bottom: 16px;\n}\n.picker-head { display: flex; align-items: center; gap: 12px; margin-bottom: 10px; flex-wrap: wrap; }\n.picker-label { font-size: 12px; font-weight: 700; color: var(--text-sub); }\n.picker-head input { flex: 1; min-width: 180px; background: #fff; }\n\n.picker-cats { display: flex; gap: 6px; flex-wrap: wrap; margin-bottom: 10px; }\n.cat-btn {\n  border: 1px solid var(--border); background: #fff;\n  border-radius: 999px; padding: 5px 13px; font-size: 13px; cursor: pointer;\n}\n.cat-btn:hover { border-color: var(--accent); color: var(--accent); }\n.cat-btn.is-active { background: var(--dark); color: #fff; border-color: var(--dark); }\n.cat-freq { border-color: #e8c66a; color: #8a6d1f; background: #fffaf0; font-weight: 600; }\n.cat-freq:hover { border-color: #d9ab3c; color: #7a5f14; }\n.cat-freq.is-active { background: var(--dark); color: #fff; border-color: var(--dark); }\n\n.picker-items { display: grid; grid-template-columns: repeat(auto-fill, minmax(230px, 1fr)); gap: 6px; max-height: 300px; overflow-y: auto; }\n.item-btn {\n  text-align: left; border: 1px solid var(--border); background: #fff;\n  border-radius: 7px; padding: 8px 10px; cursor: pointer; line-height: 1.45;\n}\n.item-btn:hover { border-color: var(--accent); background: var(--accent-soft); }\n.item-btn b { display: block; font-size: 13px; font-weight: 600; }\n.item-btn em { display: block; font-style: normal; font-size: 11px; color: var(--text-light); }\n.item-btn span { display: block; font-size: 12px; color: var(--accent); font-weight: 600; margin-top: 2px; }\n/* 単価の一覧：ボタンの右上に製品ページへのリンクを重ねる */\n.item-cell { position: relative; display: flex; }\n.item-cell .item-btn { flex: 1 1 auto; min-width: 0; }\n.item-btn.has-ref { padding-right: 30px; }\n.item-ref { position: absolute; top: 4px; right: 4px; margin: 0; }\n\n.picker-empty { grid-column: 1 / -1; font-size: 13px; color: var(--text-light); padding: 10px; }\n\n/* ---------- 明細テーブル ---------- */\n.lines-wrap { overflow-x: auto; }\ntable.lines { width: 100%; border-collapse: collapse; min-width: 720px; }\ntable.lines th {\n  font-size: 11px; color: var(--text-sub); text-align: left;\n  padding: 6px 8px; border-bottom: 2px solid var(--border); font-weight: 700;\n}\ntable.lines td { padding: 4px; border-bottom: 1px solid #f0f2f4; vertical-align: middle; }\ntable.lines input { border-color: transparent; background: transparent; padding: 6px 8px; }\ntable.lines input:hover { border-color: var(--border); background: #fff; }\ntable.lines input:focus { background: #fff; }\n.c-move { width: 48px; }\n.c-qty { width: 84px; }\n.c-unit { width: 70px; }\n.c-rate { width: 78px; }\n.c-price { width: 116px; }\n.rate-sel { width: 100%; padding: 6px 4px; font-size: 13px; border: 1px solid var(--border); border-radius: 6px; background: #fff; }\ntd.c-rate.is-off .rate-sel { border-color: var(--accent); color: var(--accent); font-weight: 600; background: var(--accent-soft); }\n.c-amount { width: 120px; text-align: right; }\n.c-del { width: 140px; }\n/* 原価・粗利（社内用。見積書には出ない） */\n.c-cost { width: 110px; }\n.c-margin { width: 74px; text-align: right; }\ntd.c-cost input { text-align: right; font-variant-numeric: tabular-nums; }\ntd.c-margin { text-align: right; font-size: 12px; font-variant-numeric: tabular-nums; color: var(--text-sub); }\ntd.c-margin.is-thin { color: var(--danger); font-weight: 700; }\nth.c-cost, th.c-margin, td.c-cost, td.c-margin { background: var(--accent-soft); }\n.cost-toggle {\n  display: inline-flex; align-items: center; gap: 5px;\n  font-size: 13px; color: var(--text-sub); cursor: pointer;\n  padding: 6px 10px; border: 1px dashed var(--border); border-radius: 6px;\n}\n.cost-toggle input { margin: 0; }\n#totals dt.row-cost, #totals dd.row-cost { color: var(--accent); }\n/* 原価の列は、チェックを入れたときだけ出す */\nbody:not(.show-cost) .c-cost,\nbody:not(.show-cost) .c-margin,\nbody:not(.show-cost) .mcol-cost { display: none; }\n.mrow input.m-cost { text-align: right; font-variant-numeric: tabular-nums; }\n/* 色分けを選ぶ欄 */\n.m-color { width: 100%; padding: 5px 2px; font-size: 12px; font-weight: 700;\n  border: 1px solid var(--border); border-radius: 6px; background: #fff; }\ntd.c-amount { text-align: right; font-variant-numeric: tabular-nums; font-weight: 600; padding-right: 10px; }\ntd.c-qty input, td.c-price input { text-align: right; font-variant-numeric: tabular-nums; }\n.line-name { display: flex; flex-direction: column; gap: 2px; }\n.line-name input.spec { font-size: 12px; color: var(--text-sub); }\n.icon-btn {\n  border: 0; background: transparent; cursor: pointer;\n  color: var(--text-light); padding: 4px 6px; border-radius: 5px; line-height: 1;\n}\n.icon-btn:hover { background: #f0f2f4; color: var(--danger); }\n.icon-reg { font-size: 11px; border: 1px solid var(--border); margin-right: 4px; padding: 3px 5px; }\n.icon-reg:hover { color: var(--accent); border-color: var(--accent); background: var(--accent-soft); }\n/* 作業費に数えるかどうかの切り替え（消耗品雑費・諸経費のもと） */\n.icon-work { font-size: 11px; border: 1px solid var(--border); margin-right: 4px; padding: 3px 6px; }\n.icon-work:hover { color: var(--accent); border-color: var(--accent); background: var(--accent-soft); }\n.icon-work.is-on { color: var(--accent); border-color: var(--accent); background: var(--accent-soft); font-weight: 700; }\n/* 製品ページを見るボタン */\n.icon-ref { font-size: 13px; margin-right: 2px; }\n.icon-ref:hover { color: var(--accent); background: var(--accent-soft); }\n.m-ref { display: flex; justify-content: center; }\n\n/* Excel貼り付け欄 */\n.paste-box { margin-top: 14px; padding-top: 14px; border-top: 1px dashed var(--border); }\n.paste-box textarea {\n  background: #fff; font-size: 13px;\n  font-family: var(--font-mono, ui-monospace, \"SFMono-Regular\", Consolas, monospace);\n}\n.paste-box .csv-btns { margin-top: 8px; }\n/* ---------- 並び替え（つかんで動かす） ---------- */\n.move-wrap { display: flex; align-items: center; gap: 1px; }\n.row-grip {\n  cursor: grab; touch-action: none; user-select: none; -webkit-user-select: none;\n  color: var(--text-light); font-size: 15px; line-height: 1; letter-spacing: -1px;\n  padding: 7px 3px; border-radius: 5px;\n}\n.row-grip:hover { background: var(--accent-soft); color: var(--accent); }\n.row-grip:focus-visible { outline: 2px solid var(--accent); outline-offset: 1px; }\n.row-grip:active, tbody.is-reordering .row-grip { cursor: grabbing; }\ntbody.is-reordering { cursor: grabbing; }\ntr.is-dragging {\n  background: var(--accent-soft); opacity: .8;\n  box-shadow: 0 2px 10px rgba(0, 0, 0, .16);\n}\n.move-btns { display: flex; flex-direction: column; }\n.move-btns .icon-btn { padding: 0 4px; font-size: 11px; }\n.move-btns .icon-btn:hover { color: var(--accent); }\n.empty-note { font-size: 13px; color: var(--text-light); padding: 18px 4px; text-align: center; }\n\n/* ---------- 合計 ---------- */\n.totals { margin-top: 16px; margin-left: auto; max-width: 380px; }\n.totals dl { display: grid; grid-template-columns: 1fr auto; gap: 6px 16px; }\n.totals dt { font-size: 13px; color: var(--text-sub); }\n.totals dd { text-align: right; font-variant-numeric: tabular-nums; font-size: 14px; }\n.totals dt.row-total, .totals dd.row-total {\n  border-top: 2px solid var(--dark); padding-top: 8px; margin-top: 4px;\n  font-size: 18px; font-weight: 700; color: var(--text);\n}\n\n/* ---------- 下部アクションバー ---------- */\n.actionbar {\n  position: fixed; left: 0; right: 0; bottom: 0; z-index: 40;\n  background: rgba(255,255,255,.96);\n  border-top: 1px solid var(--border);\n  padding: 10px 16px;\n  display: flex; gap: 8px; justify-content: flex-end;\n  backdrop-filter: blur(6px);\n}\n\n/* ---------- 保存済み一覧 ---------- */\n.est-row {\n  display: flex; align-items: center; gap: 14px;\n  padding: 12px 4px; border-bottom: 1px solid var(--border); flex-wrap: wrap;\n}\n.est-row:last-child { border-bottom: 0; }\n.est-main { flex: 1; min-width: 200px; }\n.est-main b { display: block; font-size: 14px; }\n.est-main small { color: var(--text-light); font-size: 12px; }\n.est-amount { font-weight: 700; font-variant-numeric: tabular-nums; }\n\n/* ---------- 単価マスタ編集 ---------- */\n.cat-block { border: 1px solid var(--border); border-radius: 8px; margin-bottom: 14px; overflow: hidden; }\n.cat-block > summary {\n  padding: 10px 14px; background: #f7f8fa; cursor: pointer;\n  font-weight: 700; font-size: 14px; display: flex; align-items: center; gap: 10px;\n}\n.cat-block > summary::-webkit-details-marker { display: none; }\n.cat-block > summary::before { content: \"▶\"; font-size: 10px; color: var(--text-light); transition: transform .15s; }\n.cat-block[open] > summary::before { transform: rotate(90deg); }\n.cat-body { padding: 12px 14px; }\n.cat-count { color: var(--text-light); font-weight: 400; font-size: 12px; }\n.cat-filter { margin-bottom: 10px; }\n.cat-filter input { max-width: 320px; background: #fff; }\n.mrow { display: grid; grid-template-columns: 110px 1.6fr 1.2fr 62px 62px 104px 58px 32px 32px; gap: 6px; margin-bottom: 6px; align-items: center; }\n/* 原価の列を出しているときは1列ぶん広げる */\nbody.show-cost .mrow { grid-template-columns: 110px 1.6fr 1.2fr 62px 62px 90px 104px 58px 32px 32px; }\n.mrow input { padding: 6px 8px; font-size: 13px; }\n.mrow-head { font-size: 11px; color: var(--text-sub); font-weight: 700; }\n.mrow input.m-price { text-align: right; font-variant-numeric: tabular-nums; }\n.mrow input.m-code { font-family: var(--font-mono, ui-monospace, \"SFMono-Regular\", Consolas, monospace); font-size: 12px; }\n\n/* 「作業費の◯%」で単価が決まる項目（消耗品雑費など） */\n.m-auto { display: flex; align-items: center; gap: 4px; min-width: 0; }\n.m-auto input { flex: 1; min-width: 0; }\n.m-auto i { font-style: normal; font-size: 11px; color: var(--text-sub); white-space: nowrap; }\n/* 人工（にんく）。ここに数字を入れると単価は［人工 × 1人工の金額］で決まる */\n.m-manday { display: flex; align-items: center; gap: 3px; min-width: 0; }\n.m-manday input { flex: 1; min-width: 0; text-align: right; }\n.m-manday i { font-style: normal; font-size: 11px; color: var(--text-light); }\n.mrow input.m-price[readonly] { background: var(--bg); color: var(--text-sub); }\ntr.line-auto td.c-price input { background: var(--bg); color: var(--text-sub); }\ntr.line-auto td.c-rate .rate-sel { opacity: .4; }\n\n/* ---------- CSV取り込み ---------- */\n.csv-box {\n  border: 1px solid var(--border); border-radius: 8px;\n  background: #fafbfc; padding: 14px; margin-bottom: 18px;\n}\n.csv-head { margin-bottom: 12px; }\n.csv-head strong { display: block; font-size: 14px; }\n.csv-head span { display: block; font-size: 12px; color: var(--text-sub); }\n.csv-row { display: flex; gap: 14px; align-items: flex-end; flex-wrap: wrap; margin-bottom: 10px; }\n.csv-row .f { min-width: 220px; }\n.csv-row .f select { background: #fff; }\n.csv-btns { display: flex; gap: 8px; flex-wrap: wrap; }\n.csv-note { font-size: 12px; color: var(--text-light); line-height: 1.7; margin-top: 8px; }\n.csv-note code {\n  background: #eef0f3; border-radius: 4px; padding: 1px 5px;\n  font-size: 11.5px; color: var(--text);\n}\n\n/* ---------- カタログPDFから機種データを作る ---------- */\n/* 高さはとなりのボタンに合わせる（select は放っておくと伸びない） */\n.catalog-maker {\n  height: 42px; padding: 0 12px; border: 1px solid var(--border); border-radius: 8px;\n  background: #fff; color: var(--text); font-size: 13px; min-width: 200px;\n}\nol.catalog-howto { margin: 2px 0 8px; padding-left: 20px; }\nol.catalog-howto li { margin: 2px 0; }\n.catalog-link { color: var(--accent); text-decoration: underline; word-break: break-all; }\n.catalog-size { margin-top: 6px; }\n.csv-note.ok { color: var(--ok); font-weight: 600; }\n.csv-note.ng { color: var(--danger); }\n/* ボタンを取り違えたときの案内。1ファイル1行で出すので改行を活かす */\n.csv-note.file-hint { white-space: pre-line; font-weight: 600; }\n\n/* 単価ピッカーの品番表示 */\n.item-btn .item-code {\n  display: block; font-style: normal; font-size: 11px; color: var(--text-light);\n  font-family: var(--font-mono, ui-monospace, \"SFMono-Regular\", Consolas, monospace);\n}\n\n/* ---------- メーカーごとの仕入掛率 ---------- */\n.rate-note { display: block; font-size: 11px; color: var(--text-sub); line-height: 1.6; margin: 2px 0 8px; }\n.rate-head, .rate-row { display: grid; grid-template-columns: 1fr 1.3fr 66px 30px; gap: 6px; align-items: center; }\n.rate-head { font-size: 11px; color: var(--text-sub); margin-bottom: 4px; }\n.rate-head i { display: block; }\n.rate-row { margin-bottom: 6px; }\n.rate-row input { width: 100%; padding: 8px 9px; font-size: 14px; border: 1px solid var(--border); border-radius: 8px; background: var(--surface-2); }\n.rate-row input:focus { background: #fff; outline: 2px solid var(--accent); outline-offset: -1px; }\n\n/* ---------- 社判の設定 ---------- */\n.seal-setup { display: flex; gap: 20px; align-items: flex-start; flex-wrap: wrap; }\n.seal-preview {\n  flex: none;\n  width: 132px; height: 132px;\n  border: 1px dashed var(--border);\n  border-radius: 8px;\n  display: grid; place-items: center;\n  background:\n    linear-gradient(45deg, #f0f1f3 25%, transparent 25%, transparent 75%, #f0f1f3 75%),\n    linear-gradient(45deg, #f0f1f3 25%, #fff 25%, #fff 75%, #f0f1f3 75%);\n  background-size: 14px 14px;\n  background-position: 0 0, 7px 7px;\n  text-align: center;\n  padding: 8px;\n}\n.seal-preview.has-seal { border-style: solid; }\n.seal-placeholder { font-size: 12px; color: var(--text-light); line-height: 1.6; }\n.seal-placeholder small { font-size: 11px; }\n.seal-size-field input { max-width: 110px; }\n.seal-img-preview { max-width: 100%; max-height: 116px; object-fit: contain; }\n\n/* ロゴ（社判と同じ枠を使い、白いロゴでも見えるよう市松模様の上に置く） */\n.logo-preview {\n  flex: none;\n  width: 200px; height: 132px;\n  border: 1px dashed var(--border);\n  border-radius: 8px;\n  display: grid; place-items: center;\n  background:\n    linear-gradient(45deg, #f0f1f3 25%, transparent 25%, transparent 75%, #f0f1f3 75%),\n    linear-gradient(45deg, #f0f1f3 25%, #fff 25%, #fff 75%, #f0f1f3 75%);\n  background-size: 14px 14px;\n  background-position: 0 0, 7px 7px;\n  text-align: center;\n  padding: 8px;\n}\n.logo-preview.has-logo { border-style: solid; }\n.logo-img-preview { max-width: 100%; max-height: 116px; object-fit: contain; }\n\n.seal-controls { flex: 1; min-width: 240px; display: flex; flex-direction: column; gap: 12px; }\n.seal-btns { display: flex; gap: 8px; flex-wrap: wrap; }\n.check { display: flex; align-items: flex-start; gap: 8px; font-size: 13px; color: var(--text-sub); cursor: pointer; }\n.check input { width: auto; margin-top: 4px; flex: none; }\n.seal-controls .f > span { font-weight: 400; }\n\n/* ---------- トースト ---------- */\n.toast {\n  position: fixed; left: 50%; bottom: 78px; transform: translateX(-50%) translateY(14px);\n  background: var(--dark); color: #fff; padding: 10px 20px; border-radius: 8px;\n  font-size: 14px; opacity: 0; pointer-events: none; transition: opacity .2s, transform .2s; z-index: 100;\n}\n.toast.is-show { opacity: 1; transform: translateX(-50%) translateY(0); }\n\n/* ---------- スマホ ---------- */\n@media (max-width: 780px) {\n  .grid { grid-template-columns: repeat(2, 1fr); }\n  .f-wide { grid-column: span 2; }\n  .topbar-inner { gap: 10px; }\n  .tabs { margin-left: 0; width: 100%; overflow-x: auto; }\n  .mrow { grid-template-columns: 1fr 1fr; }\n  .mrow-head { display: none; }\n  .csv-row { flex-direction: column; align-items: stretch; }\n  .csv-row .f { min-width: 0; }\n  .sizer-form { grid-template-columns: repeat(2, 1fr); }\n  .wrap { padding-bottom: 140px; }\n  .totals { max-width: none; }\n}\n@media (max-width: 480px) {\n  .grid { grid-template-columns: 1fr; }\n  .f-wide { grid-column: span 1; }\n  .sizer-form { grid-template-columns: 1fr; }\n  .actionbar { justify-content: stretch; }\n  .actionbar .btn { flex: 1; padding: 10px 6px; }\n}\n\n/* ==========================================================================\n   印刷用 見積書（A4縦）\n   ========================================================================== */\n#sheet { display: block; color: #000; font-family: var(--font); }\n\n/* ---------- プレビュー（紙に出す前に画面で見た目を確かめる） ----------\n   印刷用の #sheet をそのまま画面にかぶせて出すだけなので、\n   ここで見えているものと印刷されるものは必ず同じになる。 */\n.preview {\n  position: fixed; inset: 0; z-index: 200;\n  display: flex; flex-direction: column;\n  background: #6b7178;\n}\n.preview[hidden] { display: none; }\n\n.preview-bar {\n  flex: none; display: flex; align-items: center; gap: 10px; flex-wrap: wrap;\n  padding: 10px 14px; background: var(--dark); color: #fff;\n}\n.preview-title { font-size: 14px; }\n/* バーの文字は白なので、ボタンだけは元の文字色に戻す。\n   「印刷 / PDF」は白ベタで目立たせ、「閉じる」は枠だけにして区別する。 */\n.preview-bar .btn { color: var(--text); font-weight: 600; }\n.preview-bar .btn-ghost {\n  background: transparent; color: #fff; border-color: #5a6068; font-weight: 400;\n}\n.preview-bar .btn-ghost:hover { background: #2f353c; }\n.preview-hint { color: #a8adb4; font-size: 12px; margin-right: auto; }\n\n/* PDFのボタン。プレビューの帯は黒いので、黒いボタンを置くと見えなくなる。\n   （2026-09-02、実機で気づいた）緑で塗って、どちらの場所でもはっきり見えるようにする。 */\n.btn-pdf,\n.preview-bar .btn-pdf {\n  background: #16a34a; border-color: #16a34a; color: #fff; font-weight: 600;\n}\n.btn-pdf:hover, .preview-bar .btn-pdf:hover { background: #12833b; border-color: #12833b; }\n.btn-pdf:disabled { background: #6b7280; border-color: #6b7280; color: #fff; }\n\n.preview-scroll { flex: 1; overflow: auto; padding: 12px; -webkit-overflow-scrolling: touch; }\n.preview-fit { margin: 0 auto; }\n/* A4の幅そのままで組んでおき、画面が狭いときは JS が縮小する */\n.preview-stage { width: 210mm; transform-origin: top left; }\n\n.preview .sheet-page {\n  background: #fff;\n  padding: 14mm 14mm 12mm;\n  min-height: 297mm;\n  box-shadow: 0 2px 14px rgba(0,0,0,.35);\n  margin-bottom: 12px;\n}\n\n@media print {\n  @page { size: A4 portrait; margin: 14mm 14mm 12mm; }\n\n  body { background: #fff; font-size: 10.5pt; }\n  /* プレビュー中は body のスクロールを止めているが、\n     そのまま印刷すると1枚目しか出ないブラウザがあるので必ず戻す */\n  html, body { overflow: visible !important; height: auto !important; }\n  #app { display: none !important; }\n\n  /* プレビューを開いたまま印刷しても、閉じた状態で印刷しても、\n     紙には見積書だけが出るようにする */\n  .preview, .preview[hidden] { display: block !important; position: static; background: none; }\n  .preview-bar { display: none !important; }\n  .preview-scroll { overflow: visible; padding: 0; }\n  .preview-fit { width: auto !important; height: auto !important; }\n  .preview-stage { width: auto; transform: none !important; }\n\n  /* ------------------------------------------------------------------\n     iPhone は印刷のとき、紙の幅ではなく「画面の幅」で組み立ててから、\n     紙に合わせて拡大する。そのせいで文字も行も大きくなり、\n     こちらで測って割った1枚ぶんが入りきらず、ブラウザがもう一度切る。\n     （2026-09-02、実機のPDFで確認。同じ見積がPCで3枚、iPhoneで4枚になった）\n\n     幅をmmで決め打ちして、画面の幅を見に行かせない。\n     210mm から左右の余白14mmずつを引いた 182mm が、紙に印刷できる幅。\n     ------------------------------------------------------------------ */\n  html, body { width: 182mm; }\n  .preview, .preview-scroll, .preview-fit, .preview-stage,\n  .preview .sheet-page { width: 182mm !important; max-width: 182mm; }\n  .preview .sheet-page {\n    background: none; background-image: none; padding: 0; min-height: 0;\n    box-shadow: none; margin-bottom: 0;\n  }\n\n  .sheet-page { page-break-after: always; }\n  .sheet-page:last-child { page-break-after: auto; }\n\n  /* ------------------------------------------------------------------\n     どこで紙を変えるか。\n     指定しないとブラウザは高さだけを見て切るので、\n     「小計と消費税は2枚目、合計だけ3枚目」のような紙ができてしまう。\n     ------------------------------------------------------------------ */\n\n  /* 明細が次の紙に続くときは、見出し（No 品名 数量…）も一緒に出す */\n  table.sheet-lines thead { display: table-header-group; }\n\n  /* 1行の途中で切らない。品名と金額が別の紙に離れないように */\n  table.sheet-lines tr { page-break-inside: avoid; break-inside: avoid; }\n\n  /* 宛名・金額・工期の固まりと、備考・合計の固まりは、それぞれ丸ごと動かす */\n  .sheet-head,\n  .sheet-foot,\n  .sheet-remarks,\n  table.sheet-sum { page-break-inside: avoid; break-inside: avoid; }\n\n}\n\n.sheet-title {\n  text-align: center; font-size: 20pt; font-weight: 700;\n  letter-spacing: .8em; text-indent: .8em; margin-bottom: 6mm;\n}\n.sheet-meta { text-align: right; font-size: 9pt; line-height: 1.6; margin-bottom: 4mm; }\n\n.sheet-head { display: flex; gap: 8mm; align-items: flex-start; margin-bottom: 6mm; }\n.sheet-head-left { flex: 1; min-width: 0; }\n.sheet-head-right { width: 66mm; font-size: 9pt; line-height: 1.65; }\n\n.sheet-to { font-size: 14pt; font-weight: 700; border-bottom: 1.2pt solid #000; padding-bottom: 2mm; margin-bottom: 4mm; }\n.sheet-lead { font-size: 10pt; margin-bottom: 5mm; }\n\n.sheet-total-box { border: 1.2pt solid #000; padding: 3mm 4mm; margin-bottom: 5mm; display: flex; align-items: baseline; gap: 5mm; }\n.sheet-total-box .label { font-size: 10pt; font-weight: 700; letter-spacing: .3em; }\n.sheet-total-box .value { font-size: 17pt; font-weight: 700; font-variant-numeric: tabular-nums; }\n.sheet-total-box .tax-note { font-size: 8.5pt; margin-left: auto; }\n\n.sheet-terms { font-size: 9pt; line-height: 1.7; }\n.sheet-terms div { display: flex; gap: 3mm; }\n.sheet-terms dt-label { }\n.sheet-terms .k { width: 22mm; flex: none; color: #333; }\n.sheet-terms .v { flex: 1; border-bottom: .4pt dotted #999; }\n\n.sheet-company .sheet-logo { display: block; width: auto; max-width: 52mm; margin-bottom: 1.5mm; object-fit: contain; }\n.sheet-company b { font-size: 11pt; }\n.sheet-company .seal-area {\n  margin-top: 2mm;\n  position: relative;\n  display: flex; align-items: center;\n  padding-right: 4mm;\n}\n.sheet-company .seal-area .owner { flex: 1; }\n.sheet-company .seal-img {\n  flex: none;\n  height: auto;\n  /* 代表者名に少し重なる位置に置く（実際の押印に近い見え方） */\n  margin-left: -3mm;\n  mix-blend-mode: multiply;\n  -webkit-print-color-adjust: exact;\n  print-color-adjust: exact;\n}\n.sheet-company .seal-fallback { flex: none; margin-left: 4mm; }\n\ntable.sheet-lines { width: 100%; border-collapse: collapse; margin-bottom: 4mm; font-size: 9.5pt; }\ntable.sheet-lines th {\n  background: #f0f0f0; border: .6pt solid #666; padding: 1.6mm 2mm;\n  font-weight: 700; font-size: 9pt;\n}\ntable.sheet-lines td { border: .6pt solid #999; padding: 1.6mm 2mm; vertical-align: top; }\ntable.sheet-lines .t-no { width: 9mm; text-align: center; }\ntable.sheet-lines .t-qty { width: 15mm; text-align: right; }\ntable.sheet-lines .t-unit { width: 12mm; text-align: center; }\ntable.sheet-lines .t-price { width: 24mm; text-align: right; }\ntable.sheet-lines .t-amount { width: 27mm; text-align: right; }\ntable.sheet-lines td.t-qty, table.sheet-lines td.t-price, table.sheet-lines td.t-amount { font-variant-numeric: tabular-nums; }\ntable.sheet-lines .l-spec { display: block; font-size: 8.5pt; color: #444; }\n/* 「定価 ¥○○ → ¥△△」の一行。仕様より少し大きく・濃くして、お客様の目に留まるようにする */\ntable.sheet-lines .l-list   { display: block; font-size: 9.5pt; color: #111; margin-top: 0.8mm; }\ntable.sheet-lines .l-list s { color: #777; }\ntable.sheet-lines .l-list b { font-size: 10.5pt; }\n\n.sheet-foot { display: flex; gap: 6mm; align-items: flex-start; }\n.sheet-remarks { flex: 1; font-size: 9pt; white-space: pre-wrap; border: .6pt solid #999; padding: 2.5mm 3mm; min-height: 24mm; }\n.sheet-remarks .rk { font-weight: 700; font-size: 8.5pt; display: block; margin-bottom: 1mm; }\n\ntable.sheet-sum { width: 62mm; flex: none; border-collapse: collapse; font-size: 9.5pt; }\ntable.sheet-sum th, table.sheet-sum td { border: .6pt solid #999; padding: 1.6mm 2.5mm; }\ntable.sheet-sum th { background: #f6f6f6; text-align: left; font-weight: 500; width: 26mm; }\ntable.sheet-sum td { text-align: right; font-variant-numeric: tabular-nums; }\ntable.sheet-sum tr.grand th, table.sheet-sum tr.grand td { background: #e8e8e8; font-weight: 700; font-size: 11pt; }\n\n/* ===== 機器を選ぶ（機種データからの絞り込み） ===== */\n.chooser {\n  border: 1px solid var(--border);\n  border-radius: 8px;\n  background: #fafbfc;\n  padding: 12px;\n  margin-bottom: 16px;\n}\n.chooser-src { font-size: 11px; color: var(--text-light); }\n.btn-sm { padding: 4px 10px; font-size: 12px; }\n.chooser .picker-head { margin-bottom: 8px; }\n.chooser .picker-head .btn-sm { margin-left: auto; }\n\n.chooser-step { padding: 6px 0; border-top: 1px dashed var(--border); }\n.chooser-step:first-child { border-top: 0; }\n.chooser-step-head { display: flex; align-items: center; gap: 8px; }\n.chooser-step-label { font-size: 12px; font-weight: 700; color: var(--text-sub); }\n.chooser-clear { font-size: 11px; border: 1px solid var(--border); padding: 2px 7px; }\n.chooser-clear:hover { color: var(--accent); border-color: var(--accent); background: var(--accent-soft); }\n.chooser-chosen { font-size: 14px; font-weight: 600; padding: 2px 0 4px; }\n\n.chooser-opts { display: grid; grid-template-columns: repeat(auto-fill, minmax(170px, 1fr)); gap: 6px; margin-top: 6px; }\n.chooser-opt {\n  text-align: left; cursor: pointer;\n  border: 1px solid var(--border); border-radius: 7px;\n  background: #fff; padding: 8px 10px;\n  display: flex; align-items: baseline; gap: 8px;\n}\n.chooser-opt:hover { border-color: var(--accent); background: var(--accent-soft); }\n.chooser-opt b { font-size: 13px; font-weight: 600; flex: 1; line-height: 1.35; }\n.chooser-opt span { font-size: 11px; color: var(--text-light); flex: none; }\n\n.chooser-result { margin-top: 10px; border-top: 1px solid var(--border); padding-top: 10px; }\n.chooser-result .chooser-step-label { display: block; margin-bottom: 8px; }\n.model-btn {\n  display: block; width: 100%; text-align: left; cursor: pointer;\n  border: 1px solid var(--border); border-radius: 7px;\n  background: #fff; padding: 9px 11px; margin-bottom: 6px;\n}\n.model-btn:hover { border-color: var(--accent); background: var(--accent-soft); }\n.model-btn-top { display: flex; align-items: baseline; gap: 10px; }\n.model-btn-top .item-code { flex: 1; font-style: normal; font-weight: 700; font-size: 13px; }\n.model-price { flex: none; font-weight: 700; font-variant-numeric: tabular-nums; }\n.model-btn em { display: block; font-style: normal; font-size: 12px; color: var(--accent); margin-top: 3px; }\n.model-btn small { display: block; font-size: 11px; color: var(--text-light); margin-top: 3px; line-height: 1.5; }\n\n@media (max-width: 600px) {\n  .chooser-opts { grid-template-columns: 1fr 1fr; }\n}\n\n/* ===== 現場（案件） ===== */\n#site-search { width: 100%; margin-bottom: 12px; }\n.site-row {\n  display: flex; align-items: center; gap: 12px; width: 100%;\n  text-align: left; cursor: pointer;\n  border: 1px solid var(--border); border-radius: 8px;\n  background: #fff; padding: 12px 14px; margin-bottom: 8px;\n}\n.site-row:hover { border-color: var(--accent); background: var(--accent-soft); }\n.site-row .est-main { flex: 1; min-width: 0; }\n.site-row .est-main b { display: block; font-size: 15px; }\n.site-row .est-main small { display: block; font-size: 12px; color: var(--text-light); margin-top: 2px; }\n.site-right { flex: none; text-align: right; }\n.site-right small { display: block; font-size: 11px; color: var(--text-light); }\n.site-head { margin-bottom: 12px; }\n.site-head .card-title { margin-bottom: 4px; }\n.site-acts { margin-bottom: 16px; }\n.site-sec-label { font-size: 12px; font-weight: 700; color: var(--text-sub); margin: 16px 0 8px; }\n\n/* ===== 現地調査チェックシート（画面） ===== */\n.survey-block { margin-bottom: 20px; }\n.survey-head { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; margin: 16px 0 8px; }\n.survey-head .site-sec-label { margin: 0; }\n.survey-count { font-size: 11px; color: var(--text-light); }\n.survey-head .btn-sm { margin-left: auto; }\n.survey-head .btn-sm + .btn-sm { margin-left: 0; }\n\n.survey-sec { border: 1px solid var(--border); border-radius: 8px; margin-bottom: 6px; background: #fff; }\n.survey-sec > summary {\n  cursor: pointer; padding: 10px 14px; font-size: 13px; font-weight: 700;\n  color: var(--text-sub); list-style: none; user-select: none;\n}\n.survey-sec > summary::-webkit-details-marker { display: none; }\n.survey-sec > summary::before { content: '▸ '; color: var(--text-light); }\n.survey-sec[open] > summary::before { content: '▾ '; }\n.survey-sec[open] > summary { border-bottom: 1px solid var(--border); }\n\n.survey-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px 16px; padding: 12px 14px; }\n.survey-f { display: flex; flex-direction: column; gap: 4px; min-width: 0; }\n.survey-f-wide { grid-column: 1 / -1; }\n.survey-k { font-size: 11px; font-weight: 700; color: var(--text-sub); }\n.survey-f input[type=text], .survey-f input[type=date], .survey-f textarea {\n  width: 100%; padding: 6px 8px; font-size: 13px;\n  border: 1px solid var(--border); border-radius: 6px; background: #fff;\n}\n.survey-sub { margin-top: 4px; }\n.survey-opts { display: flex; flex-wrap: wrap; gap: 4px 10px; }\n.survey-chk { display: inline-flex; align-items: center; gap: 4px; font-size: 13px; cursor: pointer; }\n.survey-chk input { width: 16px; height: 16px; }\n\n@media (max-width: 700px) {\n  .survey-grid { grid-template-columns: 1fr; }\n}\n\n/* ===== 現地調査チェックシート（印刷） ===== */\n.sv-page { font-size: 8.5pt; }\n.sv-title {\n  text-align: center; font-size: 13pt; font-weight: 700; letter-spacing: .1em;\n  background: #dde3ea; padding: 2.5mm; margin-bottom: 3mm;\n}\n.sv-sec {\n  background: #2b5578; color: #fff; font-weight: 700; font-size: 9pt;\n  padding: 1.4mm 2.5mm; margin-top: 2.5mm;\n}\n.sv-tbl { width: 100%; border-collapse: collapse; table-layout: fixed; }\n.sv-tbl th, .sv-tbl td { border: .4pt solid #9aa5b1; padding: 1.3mm 2mm; vertical-align: top; }\n.sv-tbl .sv-k { width: 24mm; background: #dfe5ec; font-weight: 600; text-align: left; font-size: 8pt; }\n.sv-tbl .sv-v { width: 62mm; background: #f4f2ee; }\n.sv-head .sv-k { background: #d5dce4; }\n.sv-head .sv-v { background: #e9edf2; }\n.sv-o { display: inline-block; margin-right: 2.5mm; white-space: nowrap; }\n.sv-sub { display: block; margin-top: .8mm; color: #333; }\n.sv-ph { color: #999; }\n\n/* ===== 現場写真（画面） ===== */\n.photo-block { margin-bottom: 20px; }\n.photo-pick { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 10px; }\n.photo-warn {\n  background: #fff4e5; border: 1px solid #f0c48a; border-radius: 8px;\n  padding: 9px 12px; margin: 0 0 10px; font-size: 12px; line-height: 1.8; color: #7a4b00;\n}\n.photo-note { margin-top: 8px; line-height: 1.8; }\n\n.photo-grid {\n  display: grid; gap: 10px;\n  /* 46% と書いておくと、スマホの細い画面でも必ず2枚ずつ並ぶ */\n  grid-template-columns: repeat(auto-fill, minmax(min(150px, 46%), 1fr));\n}\n.photo-cell {\n  border: 1px solid var(--border); border-radius: 8px; background: #fff;\n  padding: 6px; display: flex; flex-direction: column; gap: 6px;\n}\n.photo-cell img {\n  width: 100%; height: 110px; object-fit: cover;\n  border-radius: 6px; background: #eee; cursor: zoom-in; display: block;\n}\n.photo-bar { display: flex; gap: 4px; align-items: center; }\n.photo-kind {\n  flex: 1; min-width: 0; padding: 3px 4px; font-size: 12px;\n  border: 1px solid var(--border); border-radius: 6px; background: #fff;\n}\n.photo-bar .btn-sm { padding: 3px 7px; font-size: 11px; }\n.photo-memo {\n  width: 100%; padding: 4px 6px; font-size: 12px;\n  border: 1px solid var(--border); border-radius: 6px; background: #fff;\n}\n.photo-grid .empty-note { grid-column: 1 / -1; }\n\n/* 写真を大きく見る */\n.photo-view {\n  position: fixed; inset: 0; z-index: 90; background: rgba(0,0,0,.88);\n  display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 10px;\n  padding: 16px;\n}\n.photo-view img { max-width: 100%; max-height: calc(100vh - 110px); object-fit: contain; }\n.photo-view-cap { color: #fff; font-size: 13px; text-align: center; }\n.photo-nav {\n  position: absolute; top: 50%; transform: translateY(-50%);\n  width: 44px; height: 64px; border: 0; border-radius: 8px;\n  background: rgba(255,255,255,.18); color: #fff; font-size: 30px; line-height: 1; cursor: pointer;\n}\n.photo-prev { left: 10px; }\n.photo-next { right: 10px; }\n.photo-close {\n  position: absolute; top: 12px; right: 12px;\n  padding: 6px 14px; border: 0; border-radius: 8px;\n  background: rgba(255,255,255,.9); font-size: 13px; cursor: pointer;\n}\n\n/* ===== 現場写真（印刷） ===== */\n.ph-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4mm; margin-top: 3mm; }\n.ph-cell { margin: 0; page-break-inside: avoid; break-inside: avoid; }\n.ph-cell img {\n  width: 100%; height: 62mm; object-fit: contain;\n  background: #f2f2f2; border: .4pt solid #9aa5b1;\n}\n.ph-cell figcaption { font-size: 8pt; padding: 1mm 0 0; word-break: break-all; }\n\n/* 請求書：税率ごとの内訳（インボイス） */\n.sheet-sum tr.tax-break th, .sheet-sum tr.tax-break td { background: #fafafa; font-size: 8.5pt; color: #333; }\n.inv-block { margin-top: 8px; }\n\n/* ===== かんたんバックアップ ===== */\n.bk-box { border: 1px solid var(--border); border-radius: 8px; background: #fafbfc; padding: 12px 14px; margin-top: 14px; }\n.bk-head strong { display: block; font-size: 13px; margin-bottom: 4px; }\n.bk-head span { display: block; font-size: 12px; color: var(--text-sub); line-height: 1.7; margin-bottom: 10px; }\n.bk-row { display: flex; align-items: center; gap: 14px; flex-wrap: wrap; }\n.bk-info { flex: 1; min-width: 200px; font-size: 12px; color: var(--text-sub); line-height: 1.9; }\n.bk-info span { color: var(--text); font-weight: 600; }\n\n.bk-warn {\n  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;\n  background: #fff4e5; border-bottom: 1px solid #f0c48a;\n  padding: 10px 16px; font-size: 13px; color: #7a4b00;\n}\n.bk-warn span { flex: 1; min-width: 200px; }\n/* ===== 人工の出どころ・目安の札 ===== */\n.m-pricecell { display: inline-flex; flex-direction: column; gap: 2px; min-width: 0; }\n.m-mdnote, .m-estnote, .line-mdnote {\n  font-size: 10px; line-height: 1.3; white-space: nowrap;\n}\n.m-mdnote, .line-mdnote { color: var(--accent); }\n.m-estnote { color: #8a6d1f; font-weight: 700; }\n.m-estnote:not(:empty) {\n  background: #fffaf0; border: 1px solid #e8c66a; border-radius: 4px;\n  padding: 0 4px; align-self: flex-start;\n}\n.c-price { position: relative; }\n.line-mdnote { display: block; margin-top: 2px; }\n@media print { .m-mdnote, .m-estnote, .line-mdnote { display: none !important; } }\n\n@media print { .bk-warn { display: none !important; } }\n\n/* ===== 入れた内容を失わないために ===== */\n.a2hs-warn { background: #e8f1fb; border-bottom-color: #a8c8ea; color: #14406e; }\n.safety-grid { display: grid; gap: 10px; margin-top: 12px; }\n.safety-row {\n  border: 1px solid var(--border); border-left: 3px solid #c9a227;\n  border-radius: 8px; background: #fffdf6; padding: 10px 14px;\n}\n.safety-row b { display: block; font-size: 13px; margin-bottom: 4px; }\n.safety-row span { display: block; font-size: 12px; color: var(--text-sub); line-height: 1.8; }\n.safety-row em {\n  display: block; margin-top: 6px; font-style: normal;\n  font-size: 12px; color: #14406e;\n}\n/* ご利用にあたって（免責）。bk-box を借りると strong が block になって\n   文の途中で改行してしまうので、専用にする */\n.terms-box {\n  border: 1px solid var(--border); border-radius: 8px;\n  background: #fafbfc; padding: 12px 14px; margin-top: 16px;\n}\n.terms-title { display: block; font-size: 13px; margin-bottom: 6px; }\n.terms-box ul { margin: 0; padding-left: 1.2em; }\n.terms-box li {\n  font-size: 12px; color: var(--text-sub); line-height: 1.9; margin-bottom: 4px;\n}\n.terms-box li strong { color: var(--text); }\n\n@media print { #safety-card { display: none !important; } }\n/* 写真を開いたまま印刷しても、紙には出さない */\n@media print { .photo-view { display: none !important; } }\n\n/* ===== スマホ⇄パソコンの連動 ===== */\n.sync-warn { background: var(--accent-soft); border-bottom: 1px solid #b9d4ef; color: #0b4f96; }\n.new-warn  { background: #e9f7ee; border-bottom: 1px solid #a9d8ba; color: #14603a; }\n.app-ver { margin-top: 10px; font-size: 11px; color: var(--text-light); }\n\n.sync-badge {\n  display: inline-block; font-size: 12px; font-weight: 700;\n  padding: 3px 10px; border-radius: 999px; border: 1px solid var(--border);\n}\n.sync-badge.is-off { background: #f2f3f5; color: var(--text-sub); }\n.sync-badge.is-on  { background: #e7f5ed; color: var(--ok); border-color: #b8e0c8; }\n.sync-badge.is-err { background: #fdecea; color: var(--danger); border-color: #f2b8b1; }\n.sync-err { color: var(--danger); font-weight: 600; }\n\n#sync-code-view { font-family: ui-monospace, \"SFMono-Regular\", Menlo, Consolas, monospace; letter-spacing: .04em; }\n\n.sync-code-box {\n  margin-top: 14px; padding: 14px; border-radius: 8px;\n  background: var(--accent-soft); border: 1px solid #b9d4ef;\n}\n.sync-code-box p { font-size: 13px; line-height: 1.8; white-space: pre-line; margin-bottom: 8px; }\n.sync-code-box small { display: block; margin-top: 8px; font-size: 11px; color: var(--text-sub); line-height: 1.7; }\n.sync-code-big {\n  font-family: ui-monospace, \"SFMono-Regular\", Menlo, Consolas, monospace;\n  font-size: clamp(18px, 5vw, 26px); font-weight: 700; letter-spacing: .08em;\n  background: #fff; border: 1px solid var(--border); border-radius: 8px;\n  padding: 12px 14px; margin-bottom: 10px; word-break: break-all; user-select: all;\n}\n\n/* 単価表の色分け：入力欄も行の色を受け継ぐ */\n.mrow input { color: inherit; }\n.item-btn b, .item-btn .item-code { color: inherit; }\n\n/* ---------- 読み込んだ機種データ（メーカーごと） ---------- */\n.models-row {\n  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;\n  padding: 6px 0; border-bottom: 1px dashed var(--border);\n}\n.models-row:last-child { border-bottom: 0; }\n.models-row b { font-size: 13px; color: var(--text); }\n.models-num { font-weight: 600; color: var(--accent); }\n.models-note { color: var(--text-light); }\n.models-row .icon-btn { margin-left: auto; }\n\n/* 明細画面だけに出る小さな案内。見積書には印刷されない */\n.line-list-hint:empty { display: none; }\n.line-list-hint {\n  margin-top: 3px;\n  font-size: 11px;\n  color: #0a7d3c;\n}\n.line-list-hint.is-warn { color: #a35200; }\n\n/* ---------- 選んだ機種に付けられる別売品 ---------- */\n#chooser-options { margin-top: 10px; }\n#chooser-options:empty { display: none; }\n.opt-head {\n  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;\n  padding: 8px 10px; margin-bottom: 8px;\n  background: var(--accent-soft); border-radius: 8px; font-size: 13px;\n}\n.opt-head .models-num { color: var(--text-sub); font-size: 12px; }\n.opt-head .icon-btn { margin-left: auto; }\n\n/* ---------- 必要な能力を出す ---------- */\n.sizer { border-color: #d7e3f0; background: #f7fafd; }\n.sizer-note { font-size: 11px; color: var(--text-light); }\n.picker-head .sizer-note { margin-left: 4px; }\n.sizer-form { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 10px; }\n.sizer-form .f-row { display: flex; gap: 6px; }\n.sizer-form .f-row input { flex: 1 1 auto; min-width: 0; }\n.sizer-form .f-row select { flex: 0 0 auto; width: auto; background: #fff; }\n.sizer-form select, .sizer-form input { background: #fff; }\n.sizer-checks { display: flex; gap: 14px; flex-wrap: wrap; margin-bottom: 10px; }\n.sz-check { display: flex; align-items: center; gap: 5px; font-size: 13px; cursor: pointer; }\n.sz-check input { width: auto; }\n.sizer-result { border-top: 1px solid var(--border); padding-top: 10px; }\n.sz-empty { font-size: 13px; color: var(--text-light); }\n.sz-head { font-size: 16px; margin-bottom: 4px; }\n.sz-pick { font-size: 14px; color: var(--accent); margin-bottom: 6px; }\n.sz-pick span { color: var(--text-sub); font-size: 12px; font-weight: normal; }\n.sz-why { font-size: 12px; color: var(--text-sub); line-height: 1.8; margin-bottom: 8px; }\n.sz-note { font-size: 11.5px; color: var(--text-light); line-height: 1.7; margin-top: 8px; }\n\n\n.sz-range { font-size: 12px; color: var(--text-sub); font-weight: normal; margin-left: 8px; }\n\n/* ---------- 現調シートの中の機種選定 ---------- */\n.sz-sec > summary { font-weight: 700; color: var(--accent); }\n.sz-body { padding: 4px 2px 2px; }\n.sz-body .sizer-form { margin-bottom: 10px; }\n.sz-body .sizer-checks { margin-bottom: 8px; }\n.sz-body .sizer-result { margin-top: 10px; }\n.sz-body .btn { margin-top: 2px; }\n\n/* ---------- 寸法から広さを出す ---------- */\n.sz-meas {\n  border: 1px solid #d7e3f0; background: #f7fafd; border-radius: 8px;\n  padding: 10px; margin-bottom: 10px;\n}\n.sz-meas-head { display: flex; align-items: center; flex-wrap: wrap; gap: 8px; margin-bottom: 8px; }\n.sz-meas-head b { font-size: 13.5px; }\n.sz-meas-head .sz-note { margin-top: 0; flex: 1 1 auto; }\n.sz-unit { width: auto; flex: 0 0 auto; background: #fff; font-size: 12.5px; padding: 4px 6px; }\n.sz-rows { display: flex; flex-direction: column; gap: 6px; }\n.sz-row {\n  display: flex; align-items: center; flex-wrap: wrap; gap: 6px;\n  background: #fff; border: 1px solid var(--border); border-radius: 6px; padding: 6px 8px;\n}\n.sz-row-minus { background: #fdf6f6; border-color: #e6cfcf; }\n.sz-row input.sz-nm { flex: 1 1 120px; min-width: 90px; font-size: 13px; }\n.sz-row input.sz-d { flex: 0 0 82px; width: 82px; font-size: 14px; text-align: right; }\n.sz-lab { font-size: 12px; color: var(--text-light); }\n.sz-x { font-size: 14px; color: var(--text-sub); }\n.sz-eq { font-size: 13.5px; font-weight: 700; margin-left: 4px; white-space: nowrap; }\n.sz-row-minus .sz-eq::before { content: '−'; color: #b45; margin-right: 2px; }\n.sz-pm { flex: 0 0 auto; margin-top: 0 !important; padding: 4px 8px; font-size: 12px; }\n.sz-row-minus .sz-pm { color: #b45; border-color: #e6cfcf; }\n.sz-del { flex: 0 0 auto; margin-top: 0 !important; padding: 4px 8px; color: var(--text-light); }\n.sz-meas-foot { display: flex; align-items: center; flex-wrap: wrap; gap: 10px; margin-top: 8px; }\n.sz-meas-total { font-size: 14.5px; font-weight: 700; color: var(--accent); flex: 1 1 auto; }\n@media (max-width: 480px) {\n  .sz-row input.sz-nm { flex: 1 1 100%; }\n  .sz-row input.sz-d { flex: 1 1 0; width: auto; min-width: 0; }\n  .sz-eq { flex: 1 1 100%; text-align: right; }\n  .sz-meas-foot .btn { flex: 1 1 auto; }\n}\n.sz-meas-total.sz-warn { color: #b45; font-size: 13px; }\n\n/* 別売品のうち、すでにセット価格に入っているもの（リモコン・化粧パネル）。\n   押すと二重取りになるので、見た目でも分かるようにする（2026-09-09） */\n.item-btn.is-included { border-color: #e0a800; background: #fffbe9; }\n.item-btn .item-dup {\n  display: inline-block; margin-top: 3px; padding: 1px 6px;\n  font-size: 11px; font-weight: 600; color: #8a6100;\n  background: #ffe9a8; border-radius: 999px;\n}\n\n/* 原価の欄に入れるのは1つあたり。数量を掛けた額も小さく出す（2026-09-09） */\n.c-cost .cost-sum {\n  font-size: 11px; color: var(--text-light); text-align: right;\n  margin-top: 1px; white-space: nowrap;\n}\n\n/* ======================================================================\n   見積書の見た目（着せ替え）\n   ----------------------------------------------------------------------\n   組み立てるHTMLは1つだけ。#sheet に付くクラスで線と色だけを変える。\n   だから紙に出る金額・明細はどれを選んでも同じ。\n   ・skin-standard … これまでの形（下に何も書かない＝素のまま）\n   ・skin-frame    … 枠と罫線を濃く。白黒コピー向け\n   ・skin-plain    … 横線だけ・縦罫なし。余白広め\n   ・skin-band     … 表題と見出しを色のベタに\n   ・skin-logo     … ロゴを左上に大きく（便箋のような形）\n\n   色は CSS変数 --sheet-accent。buildSheet が ［自社情報］の設定から入れる。\n   「標準」だけは色を使わない（白黒のままが正解なので）。\n\n   高さが変わると紙の割り方も変わるが、buildSheet が着せてから\n   実際の高さを測って割るので、どれを選んでもずれない。\n   ====================================================================== */\n\n/* ---------- きっちり枠 ---------- */\n#sheet.skin-frame .sheet-title {\n  border: 1.4pt solid var(--sheet-accent, #1A1D21);\n  color: var(--sheet-accent, #1A1D21); padding: 2.5mm 0 2mm;\n  letter-spacing: .6em; text-indent: .6em; margin-bottom: 5mm;\n}\n#sheet.skin-frame .sheet-to { border-bottom-width: 1.6pt; }\n#sheet.skin-frame .sheet-total-box { border-width: 1.6pt; }\n#sheet.skin-frame table.sheet-lines { border: 1.2pt solid var(--sheet-accent, #1A1D21); }\n#sheet.skin-frame table.sheet-lines th,\n#sheet.skin-frame table.sheet-lines td { border: .8pt solid #000; }\n#sheet.skin-frame table.sheet-lines th { background: #e6e6e6; font-weight: 700; }\n#sheet.skin-frame .sheet-remarks { border: 1pt solid #000; }\n#sheet.skin-frame table.sheet-sum { border: 1.2pt solid var(--sheet-accent, #1A1D21); }\n#sheet.skin-frame table.sheet-sum th,\n#sheet.skin-frame table.sheet-sum td { border: .8pt solid #000; }\n#sheet.skin-frame table.sheet-sum th { background: #ededed; font-weight: 700; }\n#sheet.skin-frame table.sheet-sum tr.grand th,\n#sheet.skin-frame table.sheet-sum tr.grand td {\n  background: #dcdcdc; border-top: 1.6pt double var(--sheet-accent, #1A1D21);\n}\n\n/* ---------- すっきり（横線だけ） ---------- */\n#sheet.skin-plain .sheet-title {\n  text-align: left; font-size: 17pt; font-weight: 600;\n  letter-spacing: .25em; text-indent: 0;\n  border-bottom: .8pt solid var(--sheet-accent, #1A1D21); padding-bottom: 2.5mm; margin-bottom: 6mm;\n  color: var(--sheet-accent, #1A1D21);\n}\n#sheet.skin-plain .sheet-meta { margin-top: -12mm; }\n#sheet.skin-plain .sheet-to { border-bottom: .5pt solid #bbb; }\n#sheet.skin-plain .sheet-total-box {\n  border: 0; border-bottom: 1.4pt solid var(--sheet-accent, #1A1D21); padding: 1mm 0 2mm;\n}\n#sheet.skin-plain table.sheet-lines { font-size: 9.5pt; }\n#sheet.skin-plain table.sheet-lines th {\n  background: none; border: 0; border-bottom: .9pt solid var(--sheet-accent, #1A1D21);\n  padding: 1.4mm 2mm 2mm; color: #333; font-weight: 600;\n}\n#sheet.skin-plain table.sheet-lines td {\n  border: 0; border-bottom: .4pt solid #d5d5d5; padding: 2.2mm 2mm;\n}\n#sheet.skin-plain .sheet-remarks {\n  border: 0; border-top: .5pt solid #d5d5d5; padding: 2.5mm 0 0;\n}\n#sheet.skin-plain table.sheet-sum th,\n#sheet.skin-plain table.sheet-sum td {\n  border: 0; border-bottom: .4pt solid #d5d5d5; padding: 1.8mm 1mm;\n}\n#sheet.skin-plain table.sheet-sum th { background: none; color: #333; }\n#sheet.skin-plain table.sheet-sum tr.grand th,\n#sheet.skin-plain table.sheet-sum tr.grand td {\n  background: none; border-top: .9pt solid var(--sheet-accent, #1A1D21); border-bottom: 0; padding-top: 2.2mm;\n}\n\n/* ---------- 色帯（名刺と同じ炭色） ---------- */\n#sheet.skin-band .sheet-title {\n  background: var(--sheet-accent, #1A1D21); color: #fff; padding: 3mm 0 2.6mm;\n  letter-spacing: .6em; text-indent: .6em; font-size: 18pt; margin-bottom: 5mm;\n}\n#sheet.skin-band .sheet-to { border-bottom: 1.2pt solid var(--sheet-accent, #1A1D21); }\n#sheet.skin-band .sheet-total-box {\n  border: 0; border-left: 3.5pt solid var(--sheet-accent, #1A1D21); background: #f2f3f4;\n  padding: 3mm 4mm;\n}\n#sheet.skin-band table.sheet-lines th {\n  background: var(--sheet-accent, #1A1D21); color: #fff; border-color: var(--sheet-accent, #1A1D21); font-weight: 600;\n}\n#sheet.skin-band table.sheet-lines td { border-color: #c9ccd0; }\n#sheet.skin-band .sheet-remarks { border-color: #c9ccd0; background: #fafafa; }\n#sheet.skin-band table.sheet-sum th,\n#sheet.skin-band table.sheet-sum td { border-color: #c9ccd0; }\n#sheet.skin-band table.sheet-sum th { background: #f2f3f4; color: var(--sheet-accent, #1A1D21); }\n#sheet.skin-band table.sheet-sum tr.grand th,\n#sheet.skin-band table.sheet-sum tr.grand td {\n  background: var(--sheet-accent, #1A1D21); color: #fff; border-color: var(--sheet-accent, #1A1D21);\n}\n/* 色ベタは「背景を印刷しない」設定だと消えてしまうので、必ず刷るよう頼む */\n#sheet.skin-band .sheet-title,\n#sheet.skin-band table.sheet-lines th,\n#sheet.skin-band table.sheet-sum tr.grand th,\n#sheet.skin-band table.sheet-sum tr.grand td,\n#sheet.skin-band .sheet-total-box {\n  -webkit-print-color-adjust: exact; print-color-adjust: exact;\n}\n\n/* ======================================================================\n   ［自社情報］の「見積書の見た目」を選ぶ欄\n   小さな紙の絵を並べて、印刷しなくても違いが見えるようにする\n   ====================================================================== */\n.skin-pick { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 12px; }\n.skin-card {\n  display: flex; flex-direction: column; gap: 6px; align-items: stretch;\n  padding: 10px; border: 2px solid var(--line); border-radius: 10px;\n  background: #fff; cursor: pointer; text-align: left; font: inherit;\n}\n.skin-card:hover { border-color: #9aa1a9; }\n.skin-card.is-on { border-color: var(--dark); box-shadow: 0 0 0 3px rgba(26,29,33,.12); }\n.skin-name { font-weight: 700; font-size: 14px; }\n.skin-card.is-on .skin-name::after { content: '　選択中'; font-size: 11px; font-weight: 500; color: var(--dark); }\n.skin-hint { font-size: 11.5px; color: #5A6068; line-height: 1.5; }\n\n.skin-demo {\n  display: block; background: #fff; border: 1px solid #e3e5e8; border-radius: 4px;\n  padding: 8px 8px 10px; margin-bottom: 2px;\n}\n.skin-demo .sd-title {\n  display: block; text-align: center; font-size: 9px; letter-spacing: .25em;\n  font-weight: 700; color: #111; margin-bottom: 6px;\n}\n.skin-demo .sd-table { display: block; margin-bottom: 6px; }\n.skin-demo .sd-row { display: flex; gap: 3px; padding: 2px 0; }\n.skin-demo .sd-row > span { display: block; height: 4px; background: #d8dade; border-radius: 1px; }\n.skin-demo .sd-c1 { flex: 3; }\n.skin-demo .sd-c2 { flex: 1; }\n.skin-demo .sd-c3 { flex: 1.2; }\n.skin-demo .sd-head > span { background: #b9bdc3; }\n.skin-demo .sd-sum { display: block; height: 6px; width: 45%; margin-left: auto; background: #c6c9ce; border-radius: 1px; }\n\n/* 標準 */\n.skin-demo-standard .sd-row { border: 1px solid #d0d3d7; border-bottom: 0; padding: 2px; }\n.skin-demo-standard .sd-table { border-bottom: 1px solid #d0d3d7; }\n.skin-demo-standard .sd-head { background: #f0f1f3; }\n\n/* きっちり枠 */\n.skin-demo-frame .sd-title { border: 1.5px solid #111; padding: 3px 0; }\n.skin-demo-frame .sd-table { border: 1.5px solid #111; }\n.skin-demo-frame .sd-row { border-bottom: 1px solid #111; padding: 2px; }\n.skin-demo-frame .sd-row:last-child { border-bottom: 0; }\n.skin-demo-frame .sd-head { background: #dedede; }\n.skin-demo-frame .sd-sum { border: 1.5px solid #111; height: 8px; background: #dedede; }\n\n/* すっきり */\n.skin-demo-plain .sd-title { text-align: left; letter-spacing: .12em; border-bottom: 1.5px solid #111; padding-bottom: 3px; }\n.skin-demo-plain .sd-row { border-bottom: 1px solid #e6e7ea; padding: 3px 0; }\n.skin-demo-plain .sd-head { border-bottom: 1.5px solid #111; }\n.skin-demo-plain .sd-head > span { background: #cfd2d6; }\n.skin-demo-plain .sd-sum { background: none; border-top: 1.5px solid #111; height: 4px; }\n\n/* 色帯 */\n.skin-demo-band .sd-title { background: var(--sheet-accent, #1A1D21); color: #fff; padding: 3px 0; }\n.skin-demo-band .sd-head { background: var(--sheet-accent, #1A1D21); }\n.skin-demo-band .sd-head > span { background: #6b7076; }\n.skin-demo-band .sd-row { padding: 2px 0; }\n.skin-demo-band .sd-sum { background: var(--sheet-accent, #1A1D21); height: 8px; }\n\n/* ---------- ロゴを置く帯（ふつうは隠れている） ----------\n   buildSheet はいつも書き出しておき、「ロゴ大きく左」のときだけ見せる。\n   こうすると見た目の切り替えはCSSだけで済み、組み立てる関数は1つで足りる。 */\n.sheet-brand { display: none; }\n\n/* ---------- ロゴ大きく左（便箋のような形） ---------- */\n#sheet.skin-logo .sheet-brand {\n  display: block;\n  border-bottom: 2.2pt solid var(--sheet-accent, #1A1D21);\n  padding-bottom: 3mm; margin-bottom: 4mm;\n}\n#sheet.skin-logo .sheet-brand .brand-logo {\n  display: block; width: auto; max-width: 95mm; object-fit: contain;   /* 高さは buildSheet が入れる */\n}\n/* ロゴが無い会社は会社名を大きく出す（空白の帯にしない） */\n#sheet.skin-logo .sheet-brand .brand-name {\n  display: block; font-size: 16pt; font-weight: 700; letter-spacing: .04em;\n  color: var(--sheet-accent, #1A1D21);\n}\n/* 右の会社欄の小さいロゴは、左上と二重になるので隠す */\n#sheet.skin-logo .sheet-company .sheet-logo { display: none; }\n\n#sheet.skin-logo .sheet-title {\n  text-align: left; font-size: 16pt; font-weight: 700;\n  letter-spacing: .5em; text-indent: 0;\n  color: var(--sheet-accent, #1A1D21); margin-bottom: 1mm;\n}\n#sheet.skin-logo .sheet-meta { margin-top: -8mm; margin-bottom: 5mm; }\n#sheet.skin-logo .sheet-to { border-bottom: .8pt solid var(--sheet-accent, #1A1D21); }\n#sheet.skin-logo .sheet-total-box {\n  border: 0; border-top: .8pt solid var(--sheet-accent, #1A1D21);\n  border-bottom: 2.2pt solid var(--sheet-accent, #1A1D21); padding: 2.5mm 1mm;\n}\n#sheet.skin-logo table.sheet-lines th {\n  background: none; color: var(--sheet-accent, #1A1D21); font-weight: 700;\n  border: 0; border-top: .8pt solid var(--sheet-accent, #1A1D21);\n  border-bottom: .8pt solid var(--sheet-accent, #1A1D21);\n}\n#sheet.skin-logo table.sheet-lines td { border: 0; border-bottom: .4pt solid #d5d5d5; padding: 2.2mm 2mm; }\n#sheet.skin-logo .sheet-remarks { border: 0; border-top: .5pt solid #d5d5d5; padding: 2.5mm 0 0; }\n#sheet.skin-logo table.sheet-sum th,\n#sheet.skin-logo table.sheet-sum td { border: 0; border-bottom: .4pt solid #d5d5d5; padding: 1.8mm 1mm; }\n#sheet.skin-logo table.sheet-sum th { background: none; color: #333; }\n#sheet.skin-logo table.sheet-sum tr.grand th,\n#sheet.skin-logo table.sheet-sum tr.grand td {\n  background: none; color: var(--sheet-accent, #1A1D21);\n  border-top: .9pt solid var(--sheet-accent, #1A1D21); border-bottom: 0; padding-top: 2.2mm;\n}\n#sheet.skin-logo .sheet-brand,\n#sheet.skin-logo .sheet-title,\n#sheet.skin-logo table.sheet-lines th {\n  -webkit-print-color-adjust: exact; print-color-adjust: exact;\n}\n\n/* ---------- 見本の紙の絵：ロゴ大きく左 ---------- */\n.skin-demo-logo .sd-title { text-align: left; letter-spacing: .12em; color: var(--sheet-accent, #1A1D21); }\n.skin-demo-logo::before {\n  content: ''; display: block; width: 46%; height: 11px; margin-bottom: 5px;\n  background: var(--sheet-accent, #1A1D21); border-radius: 1px;\n}\n.skin-demo-logo .sd-table { border-top: 1.5px solid var(--sheet-accent, #1A1D21); }\n.skin-demo-logo .sd-head { border-bottom: 1.5px solid var(--sheet-accent, #1A1D21); }\n.skin-demo-logo .sd-head > span { background: #cfd2d6; }\n.skin-demo-logo .sd-row { border-bottom: 1px solid #e6e7ea; padding: 3px 0; }\n.skin-demo-logo .sd-sum { background: none; border-top: 1.5px solid var(--sheet-accent, #1A1D21); height: 4px; }\n\n/* 見本の絵も選んだ色で（きっちり枠・すっきりは線だけ） */\n.skin-demo-frame .sd-title { border-color: var(--sheet-accent, #1A1D21); color: var(--sheet-accent, #1A1D21); }\n.skin-demo-frame .sd-table,\n.skin-demo-frame .sd-sum { border-color: var(--sheet-accent, #1A1D21); }\n.skin-demo-plain .sd-title { border-bottom-color: var(--sheet-accent, #1A1D21); color: var(--sheet-accent, #1A1D21); }\n.skin-demo-plain .sd-head { border-bottom-color: var(--sheet-accent, #1A1D21); }\n.skin-demo-plain .sd-sum { border-top-color: var(--sheet-accent, #1A1D21); }\n\n/* ======================================================================\n   色を選ぶ欄\n   ====================================================================== */\n.sub-title { font-size: 14px; font-weight: 700; margin: 18px 0 8px; }\n.color-pick { display: flex; flex-wrap: wrap; gap: 10px; }\n.color-card {\n  display: flex; align-items: center; gap: 8px;\n  padding: 7px 12px 7px 8px; border: 2px solid var(--line); border-radius: 999px;\n  background: #fff; cursor: pointer; font: inherit;\n}\n.color-card:hover { border-color: #9aa1a9; }\n.color-card.is-on { border-color: var(--dark); box-shadow: 0 0 0 3px rgba(26,29,33,.12); }\n.color-swatch { display: block; width: 20px; height: 20px; border-radius: 50%; flex: none; }\n.color-name { font-size: 13px; }\n.color-card.is-on .color-name { font-weight: 700; }\n\n/* ロゴが無い会社は左上に会社名が大きく出るので、右の会社欄の会社名は隠す（2回出さない） */\n#sheet.skin-logo.no-logo .sheet-company b { display: none; }\n","js":[["pricebook.js","/* ==========================================================================\n   空調王（空調工事 見積作成ツール） - 初期単価マスタ（仮の目安金額）\n   --------------------------------------------------------------------------\n   ここに書いてある金額は「とりあえずの目安」です。GitHub上に公開されます。\n   実際の御社の単価は、ツールの［単価マスタ］画面で書き換えてください。\n   書き換えた金額はブラウザ内（localStorage）にだけ保存され、\n   このファイルやGitHubには一切書き込まれません。\n\n   会社名・住所・社判も同じく、［自社情報］画面で設定した内容が\n   その端末のブラウザにだけ保存されます。\n   同じURLを別の会社の方が開いても、お互いの情報は見えません。\n   ========================================================================== */\n\nconst DEFAULT_PRICEBOOK = {\n  version: 7,\n\n  /* ---------- 自社情報（見積書に印刷される / ［自社情報］画面で設定） ---------- */\n  company: {\n    name: '',\n    owner: '',\n    zip: '',\n    address: '',\n    tel: '',\n    email: '',\n    web: '',\n    bank: '',\n    invoiceNo: '',\n    sealImage: '',     // 社判の画像（アップロードするとここに入る）\n    sealSizeMm: 18,    // 社判の大きさ（mm）\n    logoImage: '',     // ロゴの画像（アップロードするとここに入る）\n    logoHeightMm: 12,  // ロゴの高さ（mm）\n    sheetSkin: 'standard',  // 見積書の見た目（standard / frame / plain / band / logo）\n    sheetColor: 'charcoal', // 見積書の色（charcoal / navy / forest / wine / brown）\n  },\n\n  /* ---------- ワンタップ入力用のひな形（任意・追加してOK） ---------- */\n  companyPresets: [\n    {\n      label: '50Airtec',\n      name: '50Airtec（ゴーマルエアテック）',\n      owner: '代表　五十嵐　透',\n      zip: '〒991-0065',\n      address: '山形県寒河江市中郷683-9',\n      tel: '070-8969-7724',\n      email: 'info@50airtec.com',\n      web: 'https://50airtec.com/',\n    },\n  ],\n\n  /* ---------- 見積書の既定文言 ---------- */\n  defaults: {\n    validDays: 30,\n    taxRatePercent: 10,\n    // 行の単価の端数。0＝そのまま／10＝10円未満は繰り上げ／100＝100円未満は繰り上げ\n    unitRoundYen: 0,\n    /* 1人工（1人が1日働く分）の金額。公共工事の考え方に合わせて、\n       作業の項目には「人工」を持たせ、単価は［人工 × この金額］で出す。\n       配管工でおおむね3万〜4万と言われる。実際の金額は［自社情報］で設定する。 */\n    manDayYen: 30000,\n    /* 原価の1人工。賃金＋法定福利費など、こちらが実際に出ていく金額。\n       見積の1人工（manDayYen）との差が、そのまま作業の粗利になる。 */\n    manDayCostYen: 0,\n    /* 材料の仕入掛率（定価の何%で仕入れているか）。0なら未設定あつかい。\n       項目ごとに原価を入れてあれば、そちらが優先される。 */\n    materialCostPercent: 0,\n    /* メーカー・シリーズごとの仕入掛率。\n       商社は「メーカーごと、寒冷地モデルかどうか」で掛率を変えているので、\n       材料ぜんぶを1つの率でまとめられない。\n       { maker:'日立', series:'寒さ知らず', percent:24 } のように書く。\n       series を空にすると、そのメーカーの機器ぜんぶに当たる。\n       ここに当たらないものは、上の materialCostPercent を使う。 */\n    costRates: [],\n    /* 機種の見積金額の出し方。\n         定価 × 仕入掛率 ＝ 原価　→　原価 ÷ この数 ＝ 見積に出す金額\n       0.65 なら粗利率35%。0 や空なら効かせない（定価そのまま）。\n       会社によって乗せる利益が違うので、［自社情報］で変えられる。 */\n    modelSellDivisor: 0.65,\n    overheadPercent: 0,\n    paymentTerms: '工事完了後、月末締め翌月末払い',\n    deliveryTerms: 'ご発注後、別途打合せ',\n    footerNote: '※本見積は現地状況により変更となる場合があります。\\n※記載のない工事・部材は含まれておりません。',\n  },\n\n  /* ---------- 単価マスタ ---------- */\n  categories: [\n    {\n      id: 'unit',\n      name: '機器本体',\n      items: [\n        { name: '機器本体（型番を入力）', spec: '', unit: '台', price: 0 },\n        { name: '室内機', spec: '', unit: '台', price: 0 },\n        { name: '室外機', spec: '', unit: '台', price: 0 },\n        { name: 'リモコン（別売）', spec: '', unit: '個', price: 0 }\n      ]\n    },\n    {\n      id: 'home',\n      name: '家庭用｜取付・交換',\n      work: true,          // ここから入れた行は「作業費」として数える（消耗品雑費の計算のもと）\n      items: [\n        /* 取付・入替。まずここから拾う */\n        { name: '標準取付工事', spec: '〜4.0kW（6〜14畳）', unit: '台', price: 16000, color: '緑' , est: 1 },\n        { name: '標準取付工事', spec: '4.0〜6.3kW（14〜20畳）', unit: '台', price: 22000, color: '緑' , est: 1 },\n        { name: '標準取付工事', spec: '6.3kW超（20畳以上・単相200V）', unit: '台', price: 28000, color: '緑' , est: 1 },\n        { name: '入替工事（取外し＋取付 同日）', spec: '', unit: '台', price: 24000, color: '緑' , est: 1 },\n        { name: '既存機 取外し', spec: '', unit: '台', price: 8000, color: '灰' , est: 1 },\n        /* 配管まわり。長さ、化粧、そして壁の穴 */\n        { name: '配管延長', spec: '標準4mを超える分', unit: 'm', price: 3000, color: '水' , est: 1 },\n        { name: '配管化粧カバー（屋外）', spec: '', unit: 'm', price: 3000, color: '水' , est: 1 },\n        { name: '配管化粧カバー（屋内）', spec: '', unit: 'm', price: 4000, color: '水' , est: 1 },\n        { name: '化粧カバー 曲がり・ジョイント', spec: '', unit: '箇所', price: 1500, color: '水' , est: 1 },\n        { name: '壁貫通穴あけ（木造・モルタル）', spec: '', unit: '箇所', price: 5000, color: '茶' , est: 1 },\n        { name: '壁貫通穴あけ（ALC・コンクリート）', spec: '', unit: '箇所', price: 12000, color: '茶' , est: 1 },\n        /* 電気まわり */\n        { name: '電圧切替（100V ⇔ 200V）', spec: '', unit: '式', price: 5000, color: '桃' , est: 1 },\n        { name: '専用コンセント新設', spec: '分電盤からの配線含む', unit: '式', price: 16000, color: '桃' , est: 1 },\n        { name: 'コンセント交換（形状変更）', spec: '', unit: '箇所', price: 4000, color: '桃' , est: 1 },\n        /* 室外機を、どこに、どう据えるか */\n        { name: '室外機 立ち下ろし（1階置き）', spec: '', unit: '台', price: 5000, color: '紫' , est: 1 },\n        { name: '室外機 壁面金具設置', spec: '', unit: '台', price: 12000, color: '紫' , est: 1 },\n        { name: '室外機 屋根置き設置', spec: '', unit: '台', price: 15000, color: '紫' , est: 1 },\n        { name: '室外機 二段置き設置', spec: '', unit: '台', price: 14000, color: '紫' , est: 1 },\n        { name: '室外機 天吊り設置', spec: '', unit: '台', price: 18000, color: '紫' , est: 1 },\n        { name: '防振ゴム・据付ブロック', spec: '', unit: '台', price: 3000, color: '紫' , est: 1 },\n        /* 現場の条件で足すもの */\n        { name: 'ドレンポンプ設置', spec: '', unit: '台', price: 18000, color: '水' , est: 1 },\n        { name: '真空引き・ガスチャージ追加', spec: '', unit: '式', price: 6000 , est: 1 },\n        { name: '高所作業（2階以上・脚立不可）', spec: '', unit: '式', price: 10000, color: '紫' , est: 1 },\n        { name: '出張費', spec: '寒河江市から車で1時間圏外', unit: '式', price: 5000 , est: 1 },\n        /* 自動で入る分 */\n        { name: '消耗品雑費', spec: '', unit: '式', price: 0, autoPercent: 5, autoBase: 'work' },\n      ]\n    },\n    {\n      id: 'biz',\n      name: '業務用｜天カセ・パッケージ',\n      work: true,          // ここから入れた行は「作業費」として数える（消耗品雑費の計算のもと）\n      items: [\n        /* 機器の取付。室内機の形ごとに色を分けてある */\n        { name: '天井カセット4方向 取付', spec: '1.5馬力', unit: '台', price: 90000, color: '青' , est: 1 },\n        { name: '天井カセット4方向 取付', spec: '2.0馬力', unit: '台', price: 100000, color: '青' , est: 1 },\n        { name: '天井カセット4方向 取付', spec: '2.5馬力', unit: '台', price: 110000, color: '青' , est: 1 },\n        { name: '天井カセット4方向 取付', spec: '3馬力', unit: '台', price: 120000, color: '青' , est: 1 },\n        { name: '天井カセット4方向 取付', spec: '4馬力', unit: '台', price: 140000, color: '青' , est: 1 },\n        { name: '天井カセット4方向 取付', spec: '5馬力', unit: '台', price: 160000, color: '青' , est: 1 },\n        { name: '天井カセット4方向 取付', spec: '6馬力', unit: '台', price: 180000, color: '青' , est: 1 },\n        { name: '天井吊形 取付', spec: '2〜3馬力', unit: '台', price: 100000, color: '赤' , est: 1 },\n        { name: '天井吊形 取付', spec: '4〜6馬力', unit: '台', price: 140000, color: '赤' , est: 1 },\n        { name: '床置形 取付', spec: '2〜3馬力', unit: '台', price: 90000, color: '緑' , est: 1 },\n        { name: '壁掛形（業務用）取付', spec: '1.5〜2.5馬力', unit: '台', price: 70000, color: '橙' , est: 1 },\n        /* 機器に付けるもの。天カセにも天吊形にも付く */\n        { name: 'オートグリル 組み込み', spec: '', unit: '台', price: 20000, color: '青' , est: 1 },\n        /* 建物側の仕込み。機器を吊る前に要る */\n        { name: '下地補強（吊ボルト・アンカー）', spec: '', unit: '台', price: 12000, color: '茶' , est: 1 },\n        { name: '点検口 新設', spec: '450角', unit: '箇所', price: 15000, color: '茶' , est: 1 },\n        { name: '天井開口・復旧', spec: '', unit: '箇所', price: 20000, color: '茶' , est: 1 },\n        /* 配管まわり */\n        { name: '冷媒配管 新設', spec: '被覆銅管・保温込み', unit: 'm', price: 6000, color: '水' , est: 1 },\n        { name: '冷媒配管 既設流用（洗浄・フラッシング）', spec: '', unit: '式', price: 25000, color: '水' , est: 1 },\n        { name: 'ドレン配管 新設', spec: '', unit: 'm', price: 3000, color: '水' , est: 1 },\n        { name: 'ドレンアップポンプ', spec: '', unit: '台', price: 25000, color: '水' , est: 1 },\n        { name: '配管化粧カバー（屋外）', spec: '', unit: 'm', price: 4000, color: '水' , est: 1 },\n        /* 電気まわり */\n        { name: '電源工事（単相200V）', spec: 'ブレーカー増設含む', unit: '式', price: 30000, color: '桃' , est: 1 },\n        { name: '電源工事（三相200V）', spec: 'ブレーカー増設含む', unit: '式', price: 45000, color: '桃' , est: 1 },\n        /* 足場・重機。日いくらで数える */\n        { name: '高所作業車 使用', spec: '', unit: '日', price: 40000, color: '紫' , est: 1 },\n        { name: 'ユニック・クレーン 使用', spec: '', unit: '日', price: 50000, color: '紫' , est: 1 },\n        { name: '足場設置・解体', spec: '', unit: '式', price: 60000, color: '紫' , est: 1 },\n        /* 引き渡しまで */\n        { name: '試運転・調整・取扱説明', spec: '', unit: '式', price: 10000 , est: 1 },\n        { name: '養生・残材処分', spec: '', unit: '式', price: 15000 , est: 1 },\n        /* 自動で入る分 */\n        { name: '消耗品雑費', spec: '', unit: '式', price: 0, autoPercent: 5, autoBase: 'work' },\n      ]\n    },\n    {\n      id: 'move',\n      name: '移設・取外し・処分',\n      work: true,          // ここから入れた行は「作業費」として数える（消耗品雑費の計算のもと）\n      items: [\n        /* 家庭用。外して、運んで、捨てるまで */\n        { name: '家庭用 取外しのみ', spec: '', unit: '台', price: 8000, color: '灰' , est: 1 },\n        { name: '家庭用 移設（同一建物内）', spec: '取外し＋再取付', unit: '台', price: 30000, color: '緑' , est: 1 },\n        { name: '家庭用 移設（別建物・運搬含む）', spec: '', unit: '台', price: 38000, color: '緑' , est: 1 },\n        { name: '家電リサイクル料（エアコン）', spec: '', unit: '台', price: 2000, color: '灰' , est: 1 },\n        { name: '収集運搬料', spec: '', unit: '台', price: 3000, color: '灰' , est: 1 },\n        /* 業務用。フロンの回収と行程管理票が要る */\n        { name: '業務用 取外しのみ', spec: '天カセ・パッケージ', unit: '台', price: 40000, color: '灰' , est: 1 },\n        { name: '業務用 移設', spec: '取外し＋再取付', unit: '台', price: 120000, color: '緑' , est: 1 },\n        { name: '業務用 廃棄処分', spec: 'フロン回収含む', unit: '台', price: 25000, color: '灰' , est: 1 },\n        { name: 'フロン回収・行程管理票発行', spec: '', unit: '台', price: 8000, color: '灰' , est: 1 },\n        /* 外したあとの後始末 */\n        { name: '残置配管 撤去', spec: '', unit: '式', price: 8000, color: '灰' , est: 1 },\n        { name: '壁穴 補修（パテ・キャップ）', spec: '', unit: '箇所', price: 3000, color: '茶' , est: 1 },\n        { name: '壁穴 補修（造作・下地復旧）', spec: '', unit: '箇所', price: 12000, color: '茶' , est: 1 },\n      ]\n    },\n    /* メーカーの材料（因幡電工・キヤッチャーなど）は、\n       ［単価マスタ］→「CSVでまとめて取り込む」で読み込むと、\n       CSVのカテゴリ列に書かれた名前のカテゴリが自動で作られる。\n       そのため、ここに空の置き場をあらかじめ用意しておく必要はない。 */\n    {\n      id: 'other',\n      name: 'その他・値引き',\n      items: [\n        /* 作業費 ＋ 消耗品雑費 の合計の15%を、ツールが自動で計算して入れる。\n           この行を使うときは、見積画面の「諸経費（小計の%）」は0のままにしておく（二重取りになるため）。 */\n        /* 消耗品雑費は家庭用・業務用にも置いてあるが、諸経費と並べて選べたほうが早いので\n           ここにも置く。どこから入れても金額は同じ（作業費の合計の5%）。 */\n        { name: '消耗品雑費', spec: '', unit: '式', price: 0, autoPercent: 5, autoBase: 'work' },\n        { name: '諸経費（現場管理費）', spec: '', unit: '式', price: 0, autoPercent: 15, autoBase: 'work+auto' },\n        { name: '駐車場代・通行料', spec: '', unit: '式', price: 0 },\n        { name: '出精値引き', spec: '', unit: '式', price: 0 }\n      ]\n    }\n  ]\n};\n"],["survey.js","/* ==========================================================================\n   現地調査チェックシートの項目定義\n   --------------------------------------------------------------------------\n   紙の「店舗・事務所・工場用 空調設備現場調査確認表」をそのまま写したもの。\n   画面も印刷も、この定義から自動で組み立てる。\n   項目を足したり消したりしたいときは、ここだけ直せば両方に反映される。\n\n   t（種類）\n     check     … チェックボックス（複数選べる）\n     text      … 自由記入\n     memo      … 自由記入（複数行）\n     checktext … チェックボックス ＋ 自由記入欄\n\n   pairs は紙と同じ「左の項目・右の項目」の並び。片方だけの行は1つだけ書く。\n   ========================================================================== */\n\nconst SURVEY_HEAD = [\n  { k: '案件名',   t: 'text' },\n  { k: '調査日',   t: 'date' },\n  { k: '担当者',   t: 'text' },\n  { k: 'お客様名', t: 'text' },\n  { k: '連絡先',   t: 'text' },\n  { k: '現場住所', t: 'text' },\n  { k: '建物用途', t: 'text' },\n  { k: '見積期限', t: 'text' },\n  { k: '工事希望日', t: 'text' },\n];\n\nconst SURVEY = [\n  {\n    sec: '1. 建物・使用条件',\n    pairs: [\n      [{ k: '用途', t: 'check', o: ['店舗', '事務所', '工場', '倉庫', '施設', 'その他'] },\n       { k: '対象面積・天井高', t: 'text', ph: '例：120㎡ / 天井高2.7m（8.2×6 でもOK）' }],\n      [{ k: '構造', t: 'check', o: ['木造', '軽量鉄骨', 'S造', 'RC', 'その他'] },\n       { k: '階数・築年数', t: 'text', ph: '例：2階 / 築15年' }],\n      [{ k: '使用人数', t: 'text', ph: '通常  人 / 最大  人' },\n       { k: '営業時間', t: 'text', ph: '例：9:00〜18:00' }],\n      [{ k: '発熱負荷', t: 'check', o: ['厨房', '窯', '機械', '照明', 'PC', '冷蔵庫', 'その他'] },\n       { k: '外気・日射', t: 'check', o: ['出入口多い', '換気量多い', '西日', '吹抜け'] }],\n      [{ k: '空調要望', t: 'check', o: ['冷房主体', '暖房主体', '年間運転', 'ゾーン別', '省エネ'] },\n       { k: '能力判定', t: 'check', o: ['不足', '適正', '要計算'] }],\n    ]\n  },\n  {\n    sec: '2. 既存設備',\n    pairs: [\n      [{ k: '室内機', t: 'text', ph: 'メーカー / 型式 / 台数' },\n       { k: '室外機', t: 'text', ph: 'メーカー / 型式' }],\n      [{ k: '能力・冷媒', t: 'checktext', o: ['R22', 'R407C', 'R410A', 'R32'], ph: '例：14kW / 5馬力' },\n       { k: '製造年・電源', t: 'checktext', o: ['単相200V', '三相200V'], ph: '例：2008年' }],\n      [{ k: '方式', t: 'check', o: ['シングル', '同時ツイン', '個別マルチ', 'ビルマル'] },\n       { k: '状態', t: 'check', o: ['正常', '故障', '能力不足', '異音', '水漏れ', 'エラー'] }],\n      [{ k: '更新方針', t: 'check', o: ['全更新', '部分更新', '修理', '増設'] },\n       { k: '配管・配線再利用', t: 'check', o: ['可', '不可', '洗浄必要', '要確認'] }],\n    ]\n  },\n  {\n    sec: '3. 室内機・天井内条件',\n    pairs: [\n      [{ k: '室内機形式', t: 'check', o: ['4方向', '2方向', '1方向', '天吊', '壁掛', '床置', 'ダクト'] },\n       { k: '設置高さ', t: 'text', ph: '例：2.6m' }],\n      [{ k: '天井・点検口', t: 'check', o: ['ボード', 'ジプトン', 'スケルトン', '点検口 有', '点検口 無', '点検口 新設'] },\n       { k: '天井内寸法', t: 'checktext', o: ['梁', 'ダクト', '配管'], ph: '高さ  mm / 障害物' }],\n      [{ k: '吊り・補強', t: 'check', o: ['既存利用', '新設', '補強必要'] },\n       { k: '開口', t: 'check', o: ['既存利用', '拡張', '新規'] }],\n      [{ k: 'ドレン', t: 'check', o: ['自然勾配', 'ドレンアップ', '既存利用'] },\n       { k: '排水先', t: 'check', o: ['排水管', '屋外', '雨樋', 'その他'] }],\n      [{ k: 'リモコン', t: 'check', o: ['ワイヤード', 'ワイヤレス', '集中管理'] },\n       { k: '位置・配線', t: 'check', o: ['既存利用', '移設', '新設'] }],\n    ]\n  },\n  {\n    sec: '4. 配管・室外機・搬入',\n    pairs: [\n      [{ k: '配管長・高低差', t: 'text', ph: '合計  m / 高低差  m / 液・ガス' },\n       { k: '分岐管', t: 'check', o: ['既存利用', '交換', '新設'] }],\n      [{ k: '配管経路', t: 'check', o: ['天井内', '露出', 'PS', '屋上', '外壁'] },\n       { k: '貫通・仕上げ', t: 'checktext', o: ['ラッキング', 'ダクト'], ph: '  箇所' }],\n      [{ k: '室外機設置', t: 'check', o: ['地上', '屋上', '壁面', '架台', '機械室'] },\n       { k: '重量・台数', t: 'text', ph: '約  kg /  台' }],\n      [{ k: '搬入・揚重', t: 'check', o: ['手運び', '階段', 'EV', 'クレーン', 'ユニック'] },\n       { k: '揚重条件', t: 'check', o: ['道路使用', '警備員', '夜間', '要申請'] }],\n      [{ k: '架台・基礎', t: 'check', o: ['既存利用', '交換', '新設', '防振'] },\n       { k: '雪・風対策', t: 'check', o: ['高置台', '防雪フード', '転倒防止'] }],\n      [{ k: '周辺条件', t: 'check', o: ['塩害', '高温', '油煙', '粉塵', '腐食'] },\n       { k: 'サービススペース', t: 'check', o: ['確保', '不足', '移設必要'] }],\n    ]\n  },\n  {\n    sec: '5. 電源・付帯工事・安全',\n    pairs: [\n      [{ k: '電源', t: 'checktext', o: ['単相200V', '三相200V'], ph: 'ブレーカー  A' },\n       { k: '電線・アース', t: 'checktext', o: ['アース 有', 'アース 無', 'アース 新設'], ph: '  sq' }],\n      [{ k: '電源工事', t: 'check', o: ['既存利用', '容量変更', '新設', '盤改造'] },\n       { k: '他業者', t: 'check', o: ['電気', '建築', '消防', '足場', 'クレーン'] }],\n      [{ k: '付帯工事', t: 'check', o: ['足場', '高所作業車', 'クレーン', 'コア抜き', '天井補修'] },\n       { k: '石綿調査', t: 'check', o: ['必要', '不要', '要確認'] }],\n      [{ k: '作業制限', t: 'check', o: ['営業時間外', '夜間', '休日', '騒音制限', '養生厳重'] },\n       { k: '駐車・搬入', t: 'check', o: ['良好', '制限あり', '要申請'] }],\n      [{ k: '危険要因', t: 'check', o: ['高所', '重量物', '火気', '狭所', '第三者', '商品・設備'] },\n       { k: '写真', t: 'check', o: ['全景', '銘板', '配管', '電源', '搬入経路'] }],\n    ]\n  },\n  {\n    sec: '6. 提案・見積条件',\n    pairs: [\n      [{ k: '提案内容', t: 'memo' },\n       { k: '概算予算', t: 'text', ph: '¥' }],\n      [{ k: '工事区分', t: 'check', o: ['標準', '追加あり', '別途工事', '再調査必要'] },\n       { k: '支払条件', t: 'check', o: ['前払', '出来高', '完了後', '要相談'] }],\n      [{ k: '補助金・リース', t: 'check', o: ['なし', '補助金', 'リース', '割賦', '要確認'] },\n       { k: '見積期限', t: 'text' }],\n      [{ k: 'お客様要望', t: 'memo' },\n       { k: '未確認事項', t: 'memo' }],\n      [{ k: '調査者所見', t: 'memo' }],\n    ]\n  },\n];\n"],["firebase-config.js","/* ==========================================================================\n   空調王：連動に使うクラウドの住所\n   --------------------------------------------------------------------------\n   ここに入れる apiKey は「秘密のカギ」ではなく、\n   Googleのどのプロジェクトかを示す“住所”です（公開されていて問題ありません）。\n   実際に中身を守っているのは\n     ・当てられない64桁の置き場所の名前\n     ・端末の中で掛ける暗号（あいことば）\n     ・Firestore のルール（ログインした人だけ・決まった形の名前だけ）\n   の3つです。\n\n   ★ 使い方\n   Firebase コンソール →（歯車）プロジェクトの設定 → マイアプリ（ウェブ）\n   に出てくる firebaseConfig の中から、apiKey と projectId をここに写します。\n   ========================================================================== */\nwindow.AIRTEC_FIREBASE = {\n  apiKey: 'AIzaSyC2OqPA7QwRfi9IMEOg3dD0TY2E-oN_SAo',\n  projectId: 'airtec-sync'\n};\n"],["catalog.js","/* ======================================================================\n   空調王｜カタログPDFから機種データを作る\n   ----------------------------------------------------------------------\n   メーカーのデジタルカタログからPDFを保存して、この画面に入れると、\n   その端末の中だけで読み取って「機種データ」を作る。\n\n   ・よそのサイトを叩かない（社内のPCでも、電波が悪いところでも動く）\n   ・PDFも、読み取った中身も、どこにも送らない\n   ・だから、メーカーのデータを人に渡すことにはならない\n\n   カタログの作りが変わって読めなくなったら、直すのは MAKERS の中の\n   build（どの文字を手がかりにするか）だけ。ほかは触らなくてよい。\n\n   【いちばん大事な約束】\n   読み取りは「0件でも黙って通す」ことを絶対にしない。\n   件数が下限を割ったら失敗にして、何を疑えばいいかを画面に出す。\n   （2026-09-03、金額の「¥」1文字を見落として1社まるごと落としかけた反省）\n   ====================================================================== */\n(function () {\n  'use strict';\n\n  /* --------------------------------------------------------------------\n     金額の読み方\n     社によって「49,000」「49,000円」「¥49,000」「￥49,000」と書き方が違う。\n     1つ通し忘れるとその社が丸ごと落ちるので、ここで全部まとめて受ける。\n     -------------------------------------------------------------------- */\n  function yen(s) {\n    if (s == null) return 0;\n    var t = String(s)\n      .replace(/[０-９]/g, function (c) { return String.fromCharCode(c.charCodeAt(0) - 0xFEE0); })\n      .replace(/[￥¥,、\\s円]/g, '');\n    var n = Number(t);\n    return isFinite(n) ? n : 0;\n  }\n\n  /* --------------------------------------------------------------------\n     日本キヤリア（旧東芝）\n     店舗・オフィス用カスタムエアコン（デジタルカタログ・1本のPDF）\n\n     形名の読み方（カタログ p.16〜21「形名の見方」より）\n       G［タイプ］［シリーズ］［構成］［容量3桁］…［末尾＝リモコン］\n     C＝天井吊形 と W＝天カセ2方向 は記号表の並びが紛らわしいので、\n     カタログ本文（p.97、p.60）で裏を取ってある。\n     -------------------------------------------------------------------- */\n  var CARRIER_TYPE = {\n    U: '天井カセット形4方向', W: '天井カセット形2方向', S: '天井カセット形1方向',\n    C: '天井吊形', K: '壁掛形', B: 'ビルトイン', D: 'ダクト',\n    F: '床置形（スタンド）', P: '厨房用天井吊形'\n  };\n  var CARRIER_SERIES = {\n    X: 'ウルトラパワーエコ', S: 'スーパーパワーエコ ゴールド',\n    E: 'スマートエコ neo', H: '暖太郎（寒冷地向け）'\n  };\n  var CARRIER_CONF = { A: 'シングル', B: '同時ツイン', C: '同時トリプル', F: '同時ダブルツイン' };\n  var HP = {\n    40: 1.5, 45: 1.8, 50: 2, 56: 2.3, 63: 2.5, 71: 2.8, 80: 3, 90: 3.2, 112: 4,\n    140: 5, 160: 6, 180: 7, 224: 8, 280: 10, 335: 12, 400: 14, 450: 16, 500: 18, 560: 20\n  };\n\n  // 価格ページ＝「セット ¥○○」が2つ以上あるページ\n  function carrierIsPricePage(t) {\n    return ((t || '').match(/セット\\s*[¥￥]/g) || []).length >= 2;\n  }\n\n  /** 別売品の読み取り用。「セッ ト ¥」のように割れて来てもよいように、すき間を無視して数える */\n  function carrierIsPricePageLoose(t) {\n    return ((t || '').replace(/\\s+/g, '').match(/セット[¥￥]/g) || []).length >= 2;\n  }\n\n  function buildCarrier(pages) {\n    var nums = Object.keys(pages).map(Number).sort(function (a, b) { return a - b; });\n    var pricePages = nums.filter(function (n) { return carrierIsPricePage(pages[n]); });\n\n    var seen = {}, rows = [];\n    pricePages.forEach(function (n) {\n      var t = pages[n];\n      var re = /セット\\s*[¥￥]\\s*([\\d,]+)/g, m;\n      while ((m = re.exec(t)) !== null) {\n        var before = t.slice(Math.max(0, m.index - 400), m.index);\n        var parts = before.split(/セット\\s*[¥￥]\\s*[\\d,]+|ワイヤレス|ワイヤード|受信部組込|送信部/);\n        var tail = parts[parts.length - 1];\n        var after = t.slice(m.index, m.index + 900);   // 構成品はセットの後ろに書かれている\n\n        var im = (after.match(/室\\s*内\\s*([A-Z]{2,4}-[A-Z0-9]+)/) || [])[1] || '';\n        var pm = (after.match(/パネル\\s*([A-Z]{2,4}-[A-Z0-9]+)/) || [])[1] || '';\n        // 室外機は単相ぶんと三相ぶんが続けて書いてある（末尾Jが単相）。電源に合う方を選ぶ\n        var omBlock = (after.match(/室\\s*外([\\s\\S]{0,140})/) || [])[1] || '';\n        var omCands = [];\n        var re2 = /\\b([A-Z]{2,4}-[A-Z0-9]+)\\b/g, m2;\n        while ((m2 = re2.exec(omBlock)) !== null) omCands.push(m2[1]);\n\n        var re3 = /\\b([A-Z]{4}\\d{5}[A-Z0-9]*)\\b\\s*(○単)?/g, g;\n        while ((g = re3.exec(tail)) !== null) {\n          var name = g[1];\n          if (seen[name]) continue;\n          seen[name] = 1;\n          var cap = Number(name.slice(4, 7));\n          var hp = HP[cap] || 0;\n          var pw = g[2] ? '単相' : '三相';\n          var om = '';\n          for (var i = 0; i < omCands.length; i++) {\n            var isJ = /J$/.test(omCands[i]);\n            if ((pw === '単相') === isJ) { om = omCands[i]; break; }\n          }\n          if (!om) om = omCands[0] || '';\n          rows.push({\n            m: name, hp: hp, y: yen(m[1]), u: String(n),\n            s: CARRIER_SERIES[name[2]] || 'その他',\n            i: CARRIER_TYPE[name[1]] || 'その他',\n            ab: cap ? 'P' + cap + '形（' + (hp || '?') + '馬力相当）' : '',\n            pw: pw,\n            rc: /XU$/.test(name) ? 'ワイヤレス' : (/BU$/.test(name) ? '内蔵リモコン' : 'ワイヤード'),\n            tp: CARRIER_CONF[name[3]] || '',\n            // 形名に P1 が入るものはプラズマ空清つき（同じ容量でも十数万円高い）\n            opt: /\\d{5}J?P1/.test(name) ? 'プラズマ空清つき' : '',\n            om: om, im: im, pm: pm, rm: ''\n          });\n        }\n      }\n    });\n\n    return {\n      rows: rows,\n      pricePages: pricePages.length,\n      head: {\n        maker: '日本キヤリア（旧東芝）',\n        brand: '店舗・オフィス用カスタムエアコン',\n        source: '店舗・オフィス用カスタムエアコン（デジタルカタログ）',\n        note: '希望小売価格・税抜。消費税/配送費/配管パイプ・据付部材/電気・据付工事費/試運転調整費は含まず。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['ウルトラパワーエコ', 'スーパーパワーエコ ゴールド', 'スマートエコ neo', '暖太郎（寒冷地向け）', 'その他'],\n        typeOrder: ['シングル', '同時ツイン', '同時トリプル', '同時ダブルツイン'],\n        urlBase: 'https://cjc.icata.net/iportal/oc.do?v=CJC00001&d=CJCD01&c=090_90_9999_1&p='\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     日立\n     店舗・オフィス用パッケージエアコン総合カタログ\n\n     価格は「セット価格」として、室内機・室外機・化粧パネル・分岐管・\n     リモコンの内訳つきで載っている。それをそのまま1機種にする。\n\n     ・「RAS-GP80RGHJ2・GP80RGH2」のように、単相ぶんと三相ぶんが\n       1行にまとめて書かれている。分けて2件にする。\n     ・日立の価格は「事業者向けの積算見積価格」。ほかの社の希望小売価格\n       とは意味が少し違う（note に書いてある）。\n     -------------------------------------------------------------------- */\n  var HITACHI_TYPE = [\n    ['RCIC', 'てんかせJr.'], ['RCID', 'てんかせ2方向'], ['RCIS', 'てんかせ1方向'], ['RCI', 'てんかせ4方向'],\n    ['RCB', 'ビルトイン'], ['RPI', 'てんうめ'], ['RPCK', '厨房用てんつり'], ['RPC', 'てんつり'],\n    ['RPK', 'かべかけ'], ['RPFI', 'ゆかおき（埋込形）'], ['RPF', 'ゆかおき'],\n    /* RPV は外気処理ではなく**ゆかおき（床置形）**。\n       ここを外気処理と読んでいたので、60機種が別の型として並び、\n       ゆかおきの別売品が1つも出なかった（2026-09-09、NotebookLMで紙面を確認）。\n       外気処理エアコンの室内機は RPI-GP…KAF で、セット価格の表には載っていない */\n    ['RPV', 'ゆかおき']\n  ];\n  var HITACHI_TP = { 1: 'シングル', 2: 'ツイン', 3: 'トリプル', 4: 'フォー' };\n\n  function hitachiType(m) {\n    for (var i = 0; i < HITACHI_TYPE.length; i++) {\n      if (m.indexOf(HITACHI_TYPE[i][0]) === 0) return HITACHI_TYPE[i][1];\n    }\n    return 'その他';\n  }\n  function hitachiSeries(om) {\n    if (/RGH/.test(om)) return '省エネの達人プレミアム';\n    if (/RSH/.test(om)) return '省エネの達人';\n    if (/RHN/.test(om)) return '寒さ知らず（寒冷地向け）';\n    return 'その他';\n  }\n  function hitachiRc(rm) {\n    if (!rm) return 'リモコン別売';\n    return /AWR/.test(rm) ? 'ワイヤレス' : 'ワイヤード';\n  }\n\n  function hitachiIsPricePage(t) {\n    return (t || '').indexOf('セット価格') >= 0;\n  }\n\n  function buildHitachi(pages) {\n    var nums = Object.keys(pages).map(Number).sort(function (a, b) { return a - b; });\n    var pricePages = nums.filter(function (n) { return hitachiIsPricePage(pages[n]); });\n\n    var rows = [], blocks = 0;\n    pricePages.forEach(function (n) {\n      var t = pages[n] || '';\n      var re = /室内\\s*([A-Z]{2,4}-[A-Z0-9]+)\\s*(?:×\\s*(\\d+))?\\s*([\\d,]+)\\s*円([\\s\\S]*?)セット価格\\s*([\\d,]+)\\s*円/g;\n      var m;\n      while ((m = re.exec(t)) !== null) {\n        blocks++;\n        var im = m[1], imN = Number(m[2] || 1), mid = m[4], setYen = m[5];\n        var before = t.slice(Math.max(0, m.index - 300), m.index);\n        var hpAll = before.match(/（([\\d.]+)馬力相当）/g) || [];\n        var hp = hpAll.length ? Number(hpAll[hpAll.length - 1].replace(/[^\\d.]/g, '')) : 0;\n        var omRaw = (mid.match(/室外\\s*([A-Z0-9\\-]+(?:\\s*・\\s*[A-Z0-9\\-]+)*)/) || [])[1] || '';\n        var pm = (mid.match(/化粧パネル\\s*([A-Z0-9\\-]+)/) || [])[1] || '';\n        var rm = (mid.match(/リモコン\\s*([A-Z0-9\\-]+)/) || [])[1] || '';\n        var br = (mid.match(/分岐管\\s*([A-Z0-9\\-]+)/) || [])[1] || '';\n        var type = hitachiType(im);\n        var imLabel = im + (imN > 1 ? '×' + imN : '');\n\n        // 単相ぶんと三相ぶんを分ける（「・」でつないで書かれている）\n        var heads = omRaw.replace(/\\s/g, '').split('・');\n        heads.forEach(function (h, i) {\n          if (!h) return;\n          var om = (i === 0 || h.indexOf('RAS-') === 0) ? h : 'RAS-' + h;\n          var pw = /J\\d*$/.test(om) ? '単相' : '三相';\n          var form = Number((om.match(/(\\d{2,3})[A-Z]/) || [])[1]) || 0;\n          rows.push({\n            m: om + '／' + imLabel,\n            hp: hp, y: yen(setYen), u: String(n),\n            s: hitachiSeries(om),\n            i: type,\n            ab: form ? form + '型（' + hp + '馬力相当）' : hp + '馬力相当',\n            pw: pw, rc: hitachiRc(rm), tp: HITACHI_TP[imN] || '',\n            opt: br ? '分岐管 ' + br : '',\n            om: om, im: imLabel, pm: pm, rm: rm\n          });\n        });\n      }\n    });\n\n    return {\n      rows: rows,\n      pricePages: pricePages.length,\n      blocks: blocks,          // 読み取れた「セット価格」の塊の数（点検で紙面と突き合わせる）\n      head: {\n        maker: '日立',\n        brand: '店舗・オフィス用パッケージエアコン',\n        source: '店舗・オフィス用パッケージエアコン総合カタログ（デジタルカタログ）',\n        note: '事業者向けの積算見積価格・税抜。消費税/配送費/試運転調整費/配管セット/工事費は含まず。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['省エネの達人プレミアム', '省エネの達人', '寒さ知らず（寒冷地向け）', 'その他'],\n        typeOrder: ['シングル', 'ツイン', 'トリプル', 'フォー'],\n        urlBase: 'https://www.hitachi-gls.co.jp/catalog/office/book/index.html#target/page_no='\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     パナソニック\n     オフィス・店舗用エアコン総合カタログ\n\n     **このメーカーだけ、文字を「位置つき」で読む。**\n     紙面が3段組で、素直につなげると隣の段の部材が混ざるため。\n     「セット価格」「合計希望小売価格」の x で段を割り出し、段ごとに読む。\n\n     踏んだ落とし穴（2026-09-03 に踏んだもの）\n     ・段の境目は「隣との真ん中」ではなく「次の段の左端」。真ん中で切ると金額が落ちる\n     ・行にまとめる y の許容は3pt。広いと隣の行と混ざり、狭いと同じ行が割れる\n     ・塊の終わりは2通り（「セット価格」＝分岐管あり／「合計希望小売価格」＝分岐管なし）\n     ・部材の合計＝合計価格 で検算する。合わない塊は捨てる（金額なので疑わしきは通さない）\n     -------------------------------------------------------------------- */\n  var PANA_TOL = 3;\n  // 金額は「3桁ずつカンマで区切った形」だけを受ける。\n  // 紙面には「室内CS-P56FE7C・7CL × 2946,000円」のように、\n  // 台数の「×2」と金額がくっついて出てくる行がある。\n  // [\\d,]{5,} で拾うと「2946,000」＝2,946,000円と読み、200万円ずれて塊ごと落ちる。\n  // カンマの位置まで見れば「946,000」だけが取れ、ついでに台数の2も m[3] 側に残る。\n  var PANA_MONEY = '((?:\\\\d{1,3},)+\\\\d{3})';\n  // 「円」は付いていないことがある。紙面でラベルと品番が別の行に割れると\n  //   「エパ ネ ル円（税抜）」／「CZ-02HPF3 × 2･････66,000」\n  // のように、金額だけが「円」に置いていかれる。円を必須にすると部材が1つ落ち、\n  // 検算がずれて塊ごと捨ててしまう。金額はカンマの形で見分けているので円は要らない。\n  var PANA_PART = new RegExp('^(.{0,12}?)([A-Z][A-Z0-9\\\\-]{3,})(.*?)' + PANA_MONEY + '\\\\s*円?');\n  var PANA_END = new RegExp('(セット価格|合計希望小売価格)（工事費別）\\\\D*' + PANA_MONEY);\n\n  // 室内機のタイプ（品番 PA-P［容量］［ここ］7… の記号）。長い記号から先に見る\n  var PANA_TYPE = [\n    ['BD', '床置形（ダクト形）'], ['DM', '1方向天井カセット形'], ['FE', 'ビルトインオールダクト形'],\n    ['VK', '高温吸込み天吊形厨房用エアコン'],\n    ['B', '床置形'], ['D', '高天井用1方向カセット形'], ['E', '天井埋込形'],\n    ['F', '天井ビルトインカセット形'], ['K', '壁掛形'], ['L', '2方向天井カセット形'],\n    ['T', '天井吊形'], ['U', '4方向天井カセット形'], ['V', '天吊形厨房用エアコン']\n  ];\n  var PANA_TP = { 1: 'シングル', 2: 'ツイン', 3: 'トリプル', 4: 'ダブルツイン' };\n\n  function panaCols(items) {\n    var xs = [];\n    items.forEach(function (i) {\n      if (/セット価格|合計希望小売価格/.test(i.s)) xs.push(Math.round(i.x));\n    });\n    if (!xs.length) return null;\n    xs.sort(function (a, b) { return a - b; });\n    var s = [];\n    xs.forEach(function (x) { if (!s.length || x - s[s.length - 1] > 40) s.push(x); });\n    return s;\n  }\n\n  /** 段の中を行にまとめて、行ごとの文字列にする */\n  function panaLines(items, lo, hi) {\n    var a = items.filter(function (i) { return i.s.trim() && i.x >= lo && i.x < hi; })\n      .sort(function (p, q) { return q.y - p.y || p.x - q.x; });\n    var rows = [], cur = null;\n    a.forEach(function (it) {\n      if (!cur || Math.abs(cur.y - it.y) > PANA_TOL) { cur = { y: it.y, a: [] }; rows.push(cur); }\n      cur.a.push(it);\n    });\n    return rows.map(function (r) {\n      return r.a.sort(function (p, q) { return p.x - q.x; })\n        .map(function (o) { return o.s; }).join('');\n    });\n  }\n\n  /** 1つの段を読んで、機種の塊を取り出す */\n  function panaColumn(lines, page, carry) {\n    var out = [], start = 0;\n\n    // 「合計希望小売価格（工事費別）････円（税抜）」の行に金額が無く、\n    // 金額だけが次の行に落ちていることがある（p.121 の床置形など）。\n    // 塊の終わりを見つけられずに丸ごと落ちるので、先につないでおく。\n    for (var q = 0; q < lines.length - 1; q++) {\n      if (!/(セット価格|合計希望小売価格)（工事費別）/.test(lines[q])) continue;\n      if (PANA_END.test(lines[q])) continue;\n      var nx = lines[q + 1].match(/^\\s*([1-9]\\d{0,2}(?:,\\d{3})+)\\s*$/);\n      if (nx) { lines[q] = lines[q] + nx[1]; lines[q + 1] = ''; }\n    }\n\n    lines.forEach(function (L, idx) {\n      var ab = L.match(/P(\\d+)\\s*形（([\\d.]+)\\s*馬力相当）/);\n      if (ab) { carry.form = Number(ab[1]); carry.hp = Number(ab[2]); }\n\n      var endM = L.match(PANA_END);\n      if (!endM) return;\n      var blk = lines.slice(start, idx + 1);\n      start = idx + 1;\n      var setYen = yen(endM[2]);\n      var isSet = endM[1] === 'セット価格';\n\n      // 品番は通し番号とセットで書かれていることが多いが、\n      //   「標準（ワイヤレス）500○単」／「PA-P45T7SGNCX」\n      // のように番号と品番が別の行に割れることがある。番号は当てにしない。\n      // 品番は必ず PA-P で始まるので、部材（CS-・CU-・CZ-）と取り違えることはない。\n      var mds = [];\n      blk.forEach(function (x) {\n        var re = /(PA-P[A-Z0-9]{4,})\\s*[○●]?\\s*([単三])?/g, m;\n        while ((m = re.exec(x)) !== null) mds.push({ m: m[1], pw: m[2] || '' });\n      });\n      if (!mds.length) return;\n\n      var listYen = 0, brYen = 0, afterSum = false, parts = [], brs = [];\n      blk.forEach(function (L2) {\n        if (PANA_END.test(L2)) { if (!isSet) { listYen = setYen; afterSum = true; } return; }\n        var s2 = L2.match(new RegExp('合計希望小売価格\\\\D*' + PANA_MONEY));\n        if (s2) { listYen = yen(s2[1]); afterSum = true; return; }\n        // 台数と金額がくっついて出てくる（「×31,386,000円」＝×3で1,386,000円）。\n        // 「31,386,000」はカンマの形として正しいので、金額の形だけでは見分けられない。\n        // 「×」の直後の数字は台数、と決めて先に切り離す。\n        // 台数はできるだけ短く取る（× 2946,000 の台数は 29 ではなく 2）。\n        // 金額の先頭は0にならないので、それを手がかりに境目を決める。\n        L2 = L2.replace(/×\\s*(\\d{1,2}?)([1-9]\\d{0,2}(?:,\\d{3})+)/g, '×$1 $2');\n\n        var m = L2.match(PANA_PART);\n        if (!m) return;\n        var n = (m[3].match(/×\\s*(\\d+)/) || [])[1];\n        var rec = {\n          label: m[1].replace(/[^ぁ-ヿ一-鿿]/g, ''),\n          code: m[2], n: n ? Number(n) : 1, yen: yen(m[4])\n        };\n        if (afterSum) { brs.push(rec); brYen += rec.yen; return; }\n        parts.push(rec);\n      });\n      if (!listYen) listYen = setYen;\n\n      // 検算。合わない塊は捨てる（読み違えた金額を見積に出すほうが怖い）\n      var sum = 0;\n      parts.forEach(function (p) { sum += p.yen; });\n      if (sum !== listYen) return;\n      if (listYen + brYen !== setYen) return;\n\n      var find = function (re) {\n        for (var i = 0; i < parts.length; i++) if (re.test(parts[i].label)) return parts[i];\n        return null;\n      };\n      var im = find(/内/), om = find(/外/), pm = find(/パネル/);\n      var rms = parts.filter(function (p) { return /リモコン/.test(p.label); });\n\n      mds.forEach(function (x) {\n        out.push({\n          page: page, m: x.m,\n          pw: x.pw === '単' ? '単相' : (x.pw === '三' ? '三相' : ''),\n          im: im ? im.code : '', imN: im ? im.n : 1,\n          om: om ? om.code : '', pm: pm ? pm.code : '',\n          rm: rms.map(function (r) { return r.code; }).join('・'),\n          br: brs.map(function (b) { return b.code; }).join('・'),\n          form: carry.form, hp: carry.hp, y: setYen\n        });\n      });\n    });\n    return out;\n  }\n\n  /** 1ページぶんの「位置つきの文字」から機種の塊を取り出す */\n  function panaReadPage(items, page) {\n    var st = panaCols(items);\n    if (!st) return [];\n    var out = [], carry = {};\n    st.forEach(function (s, i) {\n      var lo = s - 12;\n      var hi = (i === st.length - 1) ? s + 200 : st[i + 1] - 12;\n      out.push.apply(out, panaColumn(panaLines(items, lo, hi), page, carry));\n    });\n    return out;\n  }\n\n  /** 品番の記号から室内機のタイプを読む */\n  function panaType(m) {\n    var sym = (m.match(/^PA-P\\d+([A-Z]+)/) || [])[1] || '';\n    for (var i = 0; i < PANA_TYPE.length; i++) {\n      if (sym.indexOf(PANA_TYPE[i][0]) === 0) return PANA_TYPE[i][1];\n    }\n    return 'その他';\n  }\n\n  /** 品番の記号からシリーズを読む\n      PA-P［容量］［タイプ］7［S＝単相］［ここ］…\n      G＝Premium／H＝Eco（HZ なら沖縄向け）／K＝寒冷地／M＝中温用 */\n  function panaSeries(m) {\n    var tail = m.replace(/^PA-P\\d+[A-Z]*/, '').replace(/^\\d/, '').replace(/^S/, '');\n    var c = tail.charAt(0);\n    if (c === 'G') return 'XEPHY Premium';\n    if (c === 'H') return tail.charAt(1) === 'Z' ? '沖縄向け' : 'XEPHY Eco';\n    if (c === 'K') return '寒冷地向け';\n    if (c === 'M') return '中温用';\n    return 'その他';\n  }\n\n  /** 集めた塊を、空調王の行にそろえる */\n  function panaFinish(sets) {\n    var rows = [], seen = {}, pages = {};\n    sets.forEach(function (x) {\n      pages[x.page] = 1;\n      if (seen[x.m]) return;\n      seen[x.m] = 1;\n      var hp = x.hp || 0;\n      rows.push({\n        m: x.m, hp: hp, y: x.y, u: String(x.page),\n        s: panaSeries(x.m),\n        i: panaType(x.m),\n        ab: x.form ? x.form + '形（' + (hp || '?') + '馬力相当）' : '',\n        pw: x.pw || (/^PA-P\\d+[A-Z]*\\d?S/.test(x.m) ? '単相' : '三相'),\n        rc: !x.rm ? 'リモコン別売' : (/CZ-\\d*RW/.test(x.rm) ? 'ワイヤレス' : 'ワイヤード'),\n        tp: PANA_TP[x.imN] || 'シングル',\n        opt: x.br ? '別売分岐管 ' + x.br : '',\n        om: x.om, im: x.im + (x.imN > 1 ? '×' + x.imN : ''), pm: x.pm, rm: x.rm\n      });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: 'パナソニック',\n        brand: 'オフィス・店舗用エアコン',\n        source: 'オフィス・店舗用エアコン総合カタログ（デジタルカタログ）',\n        note: '希望小売価格・税抜。配管/据付工事費は含まず。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['XEPHY Premium', 'XEPHY Eco', '寒冷地向け', '中温用', '沖縄向け', 'その他'],\n        typeOrder: ['シングル', 'ツイン', 'トリプル', 'ダブルツイン'],\n        urlBase: 'https://panasonic.icata.net/iportal/CatalogSearch.do?method=catalogSearchByAnyCategories&volumeID=PEWJ0001&categoryID=353090000#'\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     ダイキン\n     店舗・オフィスエアコン（スカイエア）\n\n     **パナソニックと同じく位置つきで読む。ただし行の文字をつなげない。**\n     PDFの中では［別売リモコンBRC1G4］［46,000］［円］と部品が分かれている。\n     つなげると「BRC1G4＋46,000」か「BRC1G＋446,000」か分からなくなる\n     （400,000円ずれる）。部品のまま「数字だけの部品＝金額」と読む。\n\n     ・紙面は4段組。「合計価格／セット価格」の x で段を割る\n     ・塊の終わりは「合計価格」か「セット価格」の2通り\n     ・ワイヤレス版は差分しか書いていない（リモコンだけ差し替え）。\n       直前の塊の部材を引き継ぐ。引き継ぎ違いは検算で落ちるので危なくない\n     -------------------------------------------------------------------- */\n  var DAIKIN_TOL = 3;\n  var DAIKIN_MONEY_CELL = /^((?:[1-9]\\d{0,2},)?\\d{1,3},\\d{3})$/;\n  // 品番は必ず英字で終わる（D-SEARCHの1,063件すべてで確認）。\n  // 末尾に数字を許すと、紙面で品番のうしろに続く通し番号まで飲み込み、\n  // 「SZRUC40CV401SZRUC40CT403」のような幻の品番ができる。\n  var DAIKIN_CODE_RE = /(S[DSZ]R[A-Z]{1,3}\\d{2,3}[A-Z]*)/g;\n\n  // タイプ記号（S?R のあとの英字）。長い記号から先に見る\n  var DAIKIN_TYPE = [\n    ['JMM', '天井埋込ダクト形'], ['HU', 'スタイリッシュフロー'], ['JH', '天井吊形'],\n    ['JM', '天井埋込ダクト形'], ['MH', '天井埋込ダクト形'], ['MM', '天井埋込ダクト形'],\n    ['UC', '天井埋込カセット形 Ｓ－ラウンドフロー'],\n    ['A', '壁掛形'], ['B', '天井埋込カセット形 ビルトインＨｉ'],\n    ['C', '天井埋込カセット形 Ｓ－ラウンドフロー'], ['G', '天井埋込カセット形 エコ・ダブルフロー'],\n    ['H', '天井吊形'], ['K', '天井埋込カセット形 シングルフロー'],\n    ['M', '天井埋込ダクト形'], ['N', '天井埋込カセット形 マルチフロー（ショーカセ）'],\n    ['T', '厨房用エアコン'], ['U', '天吊自在形ワンダ風流'], ['V', '床置形']\n  ];\n  var DAIKIN_SERIES = { SDR: 'スゴ暖ＺＥＡＳ', SSR: 'ＦＩＶＥ ＳＴＡＲ ＺＥＡＳ', SZR: 'ＥＣＯ ＺＥＡＳ' };\n  var DAIKIN_TP = { 1: 'シングル', 2: 'ツイン', 3: 'トリプル', 4: 'ダブルツイン' };\n\n  function daikinCols(items) {\n    var xs = [];\n    items.forEach(function (i) { if (/合計価格|セット価格/.test(i.s)) xs.push(Math.round(i.x)); });\n    if (!xs.length) return null;\n    xs.sort(function (a, b) { return a - b; });\n    var s = [];\n    xs.forEach(function (x) { if (!s.length || x - s[s.length - 1] > 40) s.push(x); });\n    return s;\n  }\n\n  /** 段の中を行にまとめる。文字はつなげず、部品のまま持つ */\n  function daikinRows(items, lo, hi) {\n    var a = items.filter(function (i) { return i.s.trim() && i.x >= lo && i.x < hi; })\n      .sort(function (p, q) { return q.y - p.y || p.x - q.x; });\n    var rows = [], cur = null;\n    a.forEach(function (o) {\n      if (!cur || Math.abs(cur.y - o.y) > DAIKIN_TOL) { cur = { y: o.y, c: [] }; rows.push(cur); }\n      cur.c.push(o);\n    });\n    return rows.map(function (r) {\n      var c = r.c.sort(function (p, q) { return p.x - q.x; });\n      return { cells: c, text: c.map(function (o) { return o.s; }).join('') };\n    });\n  }\n\n  /** 1行から「ラベル・品番・金額」を取る。数字だけの部品を金額とみなす */\n  function daikinPart(row) {\n    var mi = -1;\n    row.cells.forEach(function (c, i) { if (DAIKIN_MONEY_CELL.test(c.s.trim())) mi = i; });\n    if (mi < 0) return null;\n    var head = row.cells.slice(0, mi).map(function (o) { return o.s; }).join('');\n    // 品番のうしろに「×2」「×3」が付くことがある（ツイン・トリプル）\n    var cm = head.match(/([A-Z][A-Z0-9\\-]*(?:・[A-Z0-9\\-]+)*)\\s*(?:×\\s*(\\d+))?\\s*$/);\n    if (!cm) return null;\n    return {\n      label: head.slice(0, head.length - cm[0].length).replace(/[^ぁ-ヿ一-鿿]/g, ''),\n      code: cm[1], n: cm[2] ? Number(cm[2]) : 1, yen: yen(row.cells[mi].s)\n    };\n  }\n\n  function daikinEnd(row) {\n    if (!/合計価格|セット価格/.test(row.text)) return null;\n    var v = 0;\n    row.cells.forEach(function (c) { if (DAIKIN_MONEY_CELL.test(c.s.trim())) v = yen(c.s); });\n    if (!v) return null;\n    return { kind: /セット価格/.test(row.text) ? 'set' : 'sum', yen: v };\n  }\n\n  function daikinColumn(rows, page, out) {\n    var start = 0, carry = null, form = 0, hp = 0;\n    rows.forEach(function (row, idx) {\n      var ab = row.text.match(/(\\d{2,3})\\s*形\\s*（\\s*([\\d.]+)\\s*馬力相当/);\n      if (ab) { form = Number(ab[1]); hp = Number(ab[2]); }\n\n      var e = daikinEnd(row);\n      if (!e) return;\n      var blk = rows.slice(start, idx + 1);\n      start = idx + 1;\n\n      var mds = [];\n      blk.forEach(function (r) {\n        var re = new RegExp(DAIKIN_CODE_RE.source, 'g'), m;\n        while ((m = re.exec(r.text.replace(/\\s/g, ''))) !== null) mds.push(m[1]);\n      });\n      if (!mds.length) return;\n\n      var sumYen = 0, afterSum = false, brYen = 0, parts = [], brs = [];\n      blk.forEach(function (r) {\n        var ee = daikinEnd(r);\n        if (ee) { if (ee.kind === 'sum') { sumYen = ee.yen; afterSum = true; } return; }\n        var rec = daikinPart(r);\n        if (!rec) return;\n        if (afterSum) { brs.push(rec); brYen += rec.yen; return; }\n        parts.push(rec);\n      });\n      if (!parts.length) return;\n      if (!sumYen) sumYen = e.yen;\n\n      // 室内機が書かれていない塊は、上の塊から引き継ぐ（リモコンだけ差し替える）\n      var use = parts;\n      var hasIm = parts.some(function (p) { return /室内/.test(p.label); });\n      if (!hasIm && carry) {\n        var kinds = {};\n        parts.forEach(function (p) { kinds[p.label] = 1; });\n        use = carry.filter(function (p) { return !kinds[p.label]; }).concat(parts);\n      }\n\n      var calc = 0;\n      use.forEach(function (p) { calc += p.yen; });\n      if (calc !== sumYen) return;                                   // 検算。合わない塊は捨てる\n      if (e.kind === 'set' && sumYen + brYen !== e.yen) return;\n      if (use.some(function (p) { return /室内/.test(p.label); })) carry = use;\n\n      var find = function (re) {\n        for (var i = 0; i < use.length; i++) if (re.test(use[i].label)) return use[i];\n        return null;\n      };\n      var im = find(/室内/), om = find(/室外/), pm = find(/パネル/), rm = find(/リモコン/);\n      mds.forEach(function (code) {\n        out.push({\n          page: page, m: code, form: form, hp: hp,\n          im: im ? im.code : '', imN: im ? im.n : 1,\n          om: om ? om.code : '', pm: pm ? pm.code : '', rm: rm ? rm.code : '',\n          br: brs.map(function (b) { return b.code; }).join('・'),\n          y: e.kind === 'set' ? e.yen : sumYen\n        });\n      });\n    });\n  }\n\n  /* 品番が横一列に並び、そのすぐ下に価格が横一列に並ぶ形の表。\n     （p.72 の「ツイン・トリプル同時マルチ」など）\n     段組みでもなく、塊の終わりを示す「合計価格」の行も無いので、\n     ふつうの読み方では1件も取れない。x の近さで品番と価格を結ぶ。\n\n     「SSRJH63DT(V)」は1つのマスに2機種ぶん書いてある書き方。\n     紙面に「（V）は単相200V電源機種です。その他は全て3相200V」とあるので、\n     三相の SSRJH63DT と単相の SSRJH63DTV の2つに分ける。\n\n     この表は部材の内訳が無いので、**検算ができない**。\n     代わりに「品番と金額の x が60pt以内で並んでいる」ことだけを頼りにする。 */\n  function daikinWideTable(items, page) {\n    var rows = daikinRows(items, -1e9, 1e9);\n    var out = [];\n    for (var i = 0; i < rows.length - 1; i++) {\n      var codes = rows[i].cells.filter(function (c) {\n        return /^S[DSZ]R[A-Z]{1,3}\\d{2,3}[A-Z]*(\\(V\\))?$/.test(c.s.replace(/\\s/g, ''));\n      });\n      if (codes.length < 2) continue;\n      var money = rows[i + 1].cells.filter(function (c) { return DAIKIN_MONEY_CELL.test(c.s.trim()); });\n      if (money.length < codes.length) continue;\n\n      codes.forEach(function (c) {\n        var best = null, bd = 1e9;\n        money.forEach(function (m) { var d = Math.abs(m.x - c.x); if (d < bd) { bd = d; best = m; } });\n        if (!best || bd > 60) return;\n        var raw = c.s.replace(/\\s/g, '');\n        var base = raw.replace(/\\(V\\)$/, '');\n        var list = /\\(V\\)$/.test(raw) ? [base, base + 'V'] : [base];\n        list.forEach(function (code) {\n          out.push({ page: page, m: code, form: 0, hp: 0, im: '', imN: 1,\n                     om: '', pm: '', rm: '', br: '', y: yen(best.s) });\n        });\n      });\n    }\n    return out;\n  }\n\n  function daikinReadPage(items, page) {\n    var out = [];\n    var st = daikinCols(items);\n    if (st) {\n      st.forEach(function (s, i) {\n        var lo = s - 12;\n        var hi = (i === st.length - 1) ? s + 200 : st[i + 1] - 12;\n        daikinColumn(daikinRows(items, lo, hi), page, out);\n      });\n    }\n    // ふつうの読み方で1件も取れなかったページだけ、横並びの表として読み直す\n    if (!out.length) out = daikinWideTable(items, page);\n    return out;\n  }\n\n  function daikinType(m) {\n    var sym = (m.match(/^S[DSZ]R([A-Z]{1,3})\\d/) || [])[1] || '';\n    for (var i = 0; i < DAIKIN_TYPE.length; i++) {\n      if (sym.indexOf(DAIKIN_TYPE[i][0]) === 0) return DAIKIN_TYPE[i][1];\n    }\n    return 'その他';\n  }\n\n  /* セットに除菌ユニットが入っているかどうか。\n\n     SSRH112D と SSRJH112D は、室外機も室内機もリモコンも同じで\n     値段だけ17万円ちがう。SSRJH のほうに\n     ストリーマ除菌ユニット BAEF50A160（¥170,000）が入っているためで、\n     1,536,000＋170,000＝1,706,000 でぴったり合う。\n\n     形名の頭で見分けられる（D-SEARCHの1,063機種ぜんぶで確かめた。混ざりは0）\n\n       …RJH / …RUC  → ストリーマ除菌ユニット      91機種\n       …RJM         → ダクト接続式除菌ユニット    44機種\n\n     印を出さないと、見た目が同じ機種が2つ並んで、\n     安いほうと高いほうを取り違える（2026-09-09、BIGBOSSの指摘） */\n  function daikinKit(m) {\n    if (/^S[DSZ]R(JH|UC)/.test(m)) return 'ストリーマ除菌ユニットつき';\n    if (/^S[DSZ]RJM/.test(m)) return 'ダクト接続式除菌ユニットつき';\n    return '';\n  }\n\n  function daikinFinish(sets) {\n    var rows = [], seen = {}, pages = {};\n    sets.forEach(function (x) {\n      pages[x.page] = 1;\n      if (seen[x.m]) return;\n      seen[x.m] = 1;\n      var cap = Number((x.m.match(/^S[DSZ]R[A-Z]{1,3}(\\d{2,3})/) || [])[1]) || 0;\n      var hp = x.hp || HP[cap] || 0;\n      // 容量より後ろに V があれば単相（CV・CNV・CVD）。無ければ三相\n      var tail = x.m.replace(/^S[DSZ]R[A-Z]{1,3}\\d{2,3}/, '');\n      rows.push({\n        m: x.m, hp: hp, y: x.y, u: String(x.page),\n        s: DAIKIN_SERIES[x.m.slice(0, 3)] || 'その他',\n        i: daikinType(x.m),\n        ab: cap ? cap + '形（' + (hp || '?') + '馬力相当）' : '',\n        pw: /V/.test(tail) ? '単相' : '三相',\n        rc: !x.rm ? 'リモコン別売' : (/^BRC1/.test(x.rm) ? 'ワイヤード' : 'ワイヤレス'),\n        tp: DAIKIN_TP[x.imN] || 'シングル',\n        opt: [daikinKit(x.m), x.br ? '別売分岐管 ' + x.br : ''].filter(Boolean).join('　'),\n        om: x.om, im: x.im + (x.imN > 1 ? '×' + x.imN : ''), pm: x.pm, rm: x.rm\n      });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: 'ダイキン',\n        brand: 'スカイエア（店舗・オフィスエアコン）',\n        source: '店舗・オフィスエアコン スカイエア（公開デジタルカタログ）',\n        note: '希望小売価格・税抜。配管/据付工事費は含まず。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['ＦＩＶＥ ＳＴＡＲ ＺＥＡＳ', 'ＥＣＯ ＺＥＡＳ', 'スゴ暖ＺＥＡＳ', 'その他'],\n        typeOrder: ['シングル', 'ツイン', 'トリプル', 'ダブルツイン'],\n        urlBase: 'https://ec.daikinaircon.com/ecatalog/index.html#'\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     ダイキン ルームエアコン（住宅設備用カタログ）……まずは壁掛形\n     --------------------------------------------------------------------\n     家電の店に並ぶモデルはオープン価格だが、工事店向けの住宅設備用カタログには、\n     スタンダード（Eシリーズ）以外に希望小売価格が載っている\n     （2026-09-11、BIGBOSSの「オープンなのはスタンダードモデルだけ」で調べ直した。\n       私は確かめずに「ルームエアコンはオープン価格が多い」と言っていた）。\n\n     1台ぶんは、3列に並んだ小さな表\n       S286ATRS-W(-C)\n       価格 605,000円（税抜き 550,000円）          ← 品番と同じ行か、すぐ下の行\n       室内 F286ATRS-W(-C)／質量16kg   室内電源 単 100 V 20A\n       室外 R286ARS／質量46kg\n       （品番の上に「おもに 10 畳程度」）\n     品番の形：S ＋ 能力の数字3けた（28＝2.8kW）＋ AT（壁掛形）＋ シリーズの字 ＋ 電源の字\n       電源の字は S＝単相100V、P・V＝単相200V（Vは室外から電源をとる形）\n\n     ・カタログのはじめの一覧表にも品番と値段が並ぶが、室内機・室外機の品番が無い。\n       品番と値段だけ拾うと別の列の値段を掴むので、室内機と室外機がそろったものだけ採る\n     ・天井埋込などのハウジングエアコンは、パネル別売の「合計価格」があって形が違うので、まだ読まない\n     -------------------------------------------------------------------- */\n  var DK_ROOM_SERIES = {\n    R: 'RXシリーズ', A: 'AXシリーズ', S: 'SXシリーズ', G: 'GXシリーズ', C: 'CXシリーズ',\n    D: 'DXシリーズ', H: 'HXシリーズ', K: 'KXシリーズ', E: 'Eシリーズ'\n  };\n  /* ハウジングエアコンは、能力の数字のあとの記号でシリーズと形が決まる（38〜52ページの見出しで確かめた）\n       S28ZCRV   CRシリーズ   天井埋込カセット形 シングルフロー\n       S284ACDV  CDシリーズ   天井埋込カセット形 シングルフロー（スゴ暖）\n       S28ZCV    Cシリーズ    天井埋込カセット形 シングルフロー\n       S40ZGV    ダブルフロー 天井埋込カセット形 ダブルフロー\n       S285AVRV  VRシリーズ   床置形／S285AVDV VDシリーズ／S285AVV Vシリーズ\n       S28ZMV    壁埋込形\n       S283ALV   アメニティビルトイン／S286ALDV フリービルトイン */\n  var DK_ROOM_KIND = {\n    ZCR: ['CRシリーズ', '天井埋込カセット形（シングルフロー）'],\n    ACD: ['CDシリーズ', '天井埋込カセット形（シングルフロー）'],\n    ZC: ['Cシリーズ', '天井埋込カセット形（シングルフロー）'],\n    ZG: ['ダブルフロー', '天井埋込カセット形（ダブルフロー）'],\n    AVR: ['VRシリーズ', '床置形'],\n    AVD: ['VDシリーズ', '床置形'],\n    AV: ['Vシリーズ', '床置形'],\n    ZM: ['壁埋込形', '壁埋込形'],\n    AL: ['アメニティビルトイン', 'ビルトイン形'],\n    ALD: ['フリービルトイン', 'ビルトイン形']\n  };\n  // 能力（kW）ごとの「おもに○畳」。紙面の「○畳程度」が読めなかったときだけ使う\n  var ROOM_TATAMI = { 22: 6, 25: 8, 28: 10, 36: 12, 40: 14, 45: 15, 50: 16, 56: 18, 63: 20, 71: 23, 80: 26, 90: 29 };\n  // S ＋ 能力2けた ＋（年式の数字）＋ 形の記号 ＋ 電源の字（S＝単相100V、P・V＝単相200V）\n  var DK_ROOM_CODE = /^(S(\\d{2})\\d?([A-Z]{2,3})([SPV]))(?=-|\\s|$)/;\n  // 別売のパネル・グリル・据付枠の品番\n  var DK_ROOM_PARTS = /(BCF\\d{3}[A-Z](?:-[A-Z])?|BC\\d{2}[A-Z]*(?:-[A-Z]+)?|BG\\d{2}[A-Z]*(?:-[A-Z]+)?|KDG\\d{3}[A-Z]\\d*|KKF\\d{3}[A-Z]\\d*[A-Z]?)/g;\n\n  function dkRoomKind(sym) {\n    if (/^AT/.test(sym)) return DK_ROOM_SERIES[sym.charAt(2)] ? [DK_ROOM_SERIES[sym.charAt(2)], '壁掛形'] : null;\n    return DK_ROOM_KIND[sym] || null;\n  }\n\n  function dkRoomReadPage(items, page) {\n    var out = [];\n    var codes = [];\n    items.forEach(function (c) {\n      var m = c.s.trim().match(DK_ROOM_CODE);\n      if (m && dkRoomKind(m[3])) codes.push({ c: c, m: m });\n    });\n    codes.forEach(function (k) {\n      var c = k.c, m = k.m, kind = dkRoomKind(m[3]);\n      // その品番の列（3列並びの1列ぶん）の文字だけを見る\n      var band = items.filter(function (o) { return o.x >= c.x - 6 && o.x < c.x + 150; });\n      function sel(dyLo, dyHi) {\n        return band.filter(function (o) { var dy = o.y - c.y; return dy >= dyLo && dy <= dyHi; })\n          .sort(function (a, b) { return b.y - a.y || a.x - b.x; });\n      }\n      // HXシリーズのページだけ、すき間が空白ではなく制御文字（U+0007）で来る。\n      // 「（税抜き␇390,000円）」「単␇100 V」を読めず、HXが1台も入らなかった\n      function clean(t) { return t.replace(/[\\u0000-\\u001f]/g, ' '); }\n      function text(dyLo, dyHi) {\n        return sel(dyLo, dyHi).map(function (o) { return clean(o.s); }).join(' ');\n      }\n      var head = text(-14, 2);\n      var price = head.match(/税抜き\\s*([\\d,]+)\\s*円/);\n      // Eシリーズ（スタンダード）は「オープン価格」。機種は入れて、値段は0（仕入先の見積で入れる）\n      var open = !price && /オープン価格/.test(head);\n      var im = text(-32, -4).match(/(F\\d{2,3}[A-Z]{2,5})(?=[-／\\s(]|$)/);\n      // 室外機の品番のうしろは「／質量46kg」のことも「 297,000円」のこともある（SXシリーズ）\n      var om = text(-48, -8).match(/(R\\d{2,3}[A-Z]{2,5})(?=[-／\\s(]|$)/);\n      // カタログのはじめの一覧表は品番と値段だけで、室内機・室外機が無い。そろったものだけ採る\n      if ((!price && !open) || !im || !om) return;\n      var pw = text(-36, -14).match(/単\\s*(100|200)\\s*V/);\n      var tat = text(4, 26).match(/(\\d{1,2})\\s*畳程度/);\n      // SXシリーズ（risora）はパネル込みのセット価格。パネルの品番も覚えておく\n      var inPanel = text(-34, -20).match(/パネル\\s*：?\\s*(BC[A-Z0-9]+)/);\n      var cap = Number(m[2]);\n      var base = {\n        page: page, m: m[1], y: price ? yen(price[1]) : 0, im: im[1], om: om[1],\n        pm: inPanel ? inPanel[1] : '',\n        kw: cap / 10,\n        tat: tat ? Number(tat[1]) : (ROOM_TATAMI[cap] || 0),\n        pw: '単相' + (pw ? pw[1] : (m[4] === 'S' ? '100' : '200')) + 'V',\n        s: kind[0], i: kind[1],\n        opt: open ? 'オープン価格（値段は仕入先の見積で）' : ''\n      };\n\n      /* 天井埋込カセット・壁埋込形は、パネルやグリルが別売。\n         セットの価格の下に「フラットパネル（別売）BC40JF-WF 価格…」「採用時 合計価格 693,000円（税抜き 630,000円）」\n         が組み合わせの数だけ並ぶ。セットだけの値段では見積にならないので、合計価格ごとに1台として出す。\n         下を見るのは、同じ列の次の品番の少し上まで */\n      var lo = -110;\n      codes.forEach(function (k2) {\n        if (k2 === k || Math.abs(k2.c.x - c.x) > 20 || k2.c.y >= c.y) return;\n        lo = Math.max(lo, k2.c.y - c.y + 25);\n      });\n      var rows = [];\n      sel(lo, -30).forEach(function (o) {\n        var r = null;\n        rows.forEach(function (x) { if (Math.abs(x.y - o.y) <= 1.5) r = x; });\n        if (!r) { r = { y: o.y, s: '' }; rows.push(r); }\n        r.s += clean(o.s) + ' ';\n      });\n      rows.sort(function (a, b) { return b.y - a.y; });\n      var parts = [], names = [], variants = [];\n      rows.forEach(function (r) {\n        var flat = r.s.replace(/\\s+/g, '');\n        (r.s.match(DK_ROOM_PARTS) || []).forEach(function (x) { if (parts.indexOf(x) < 0) parts.push(x); });\n        (flat.match(/フラットパネル|標準パネル|別売パネル|和風グリル|据付枠/g) || []).forEach(function (x) {\n          if (names.indexOf(x) < 0) names.push(x);\n        });\n        var tot = /合計価格/.test(flat) && r.s.match(/税抜き\\s*([\\d,]+)\\s*円/);\n        if (tot) {\n          variants.push({ y: yen(tot[1]), pm: parts.join('・'), label: names.join('＋') });\n          parts = []; names = [];\n        }\n      });\n      if (!variants.length) { out.push(base); return; }\n      variants.forEach(function (v) {\n        var x = {};\n        for (var key in base) x[key] = base[key];\n        x.y = v.y; x.pm = v.pm;\n        x.opt = (v.label || 'パネル') + '込み';\n        out.push(x);\n      });\n    });\n    return out.concat(dkMultiReadPage(items, page));\n  }\n\n  /* --------------------------------------------------------------------\n     ダイキン マルチエアコン（住宅設備用カタログ 53〜59ページ）\n     --------------------------------------------------------------------\n     室外機1台に室内機を何台かつなぐ形なので、セットの品番が無い（マルチパックだけはある）。\n     機器を選ぶで1台ずつ明細に足していけるように、室外機と室内機を別々の1台として入れる。\n       マルチパック   PAC-403AV  室内 C22RTV×2 ＋ 室外 MP403AV（パック価格）\n       ココタス室外機 2M30YCV・2M403ACV（品番が「2M」「30YCV」と2つに割れて来る）\n       システムマルチ室外機 2M455AV〜5M1005AV（頭の数字が何室用か）\n       室内機         C28ZCV・C285AVV-W・C223ATSVW …… パネルが別売のものは「合計価格」ごとに1台\n     室内機の一覧は、パネル違いの「合計価格」が同じ行に2つ並ぶ（フラットパネル・標準パネル）。\n     だから合計価格は、文字の x の位置ごとに見る。\n     壁埋込形の「別売前面グリル・据付枠合計価格」はグリルと据付枠だけの合計なので、本体の値段に足す\n     -------------------------------------------------------------------- */\n  var DK_MULTI_IN = {\n    ZC: '天井埋込カセット形（シングルフロー）', ZG: '天井埋込カセット形（ダブルフロー）', AV: '床置形',\n    ZM: '壁埋込形', YCC: '小空間マルチ（ココタス）', AL: 'ビルトイン形',\n    RT: '壁掛形', VTCC: '壁掛形', AT: '壁掛形', ATC: '壁掛形', ATCS: '壁掛形', ATS: '壁掛形'\n  };\n\n  function dkCapLabel(cap) {\n    var t = ROOM_TATAMI[cap];\n    return (cap / 10).toFixed(1) + 'kW' + (t ? '（おもに' + t + '畳）' : '');\n  }\n\n  function dkPanelName(code) {\n    if (/^BCF\\d/.test(code)) return '別売パネル';\n    if (/^BC\\d{2}JF/.test(code)) return 'フラットパネル';\n    if (/^BC\\d{2}J-/.test(code)) return '標準パネル';\n    return '';\n  }\n\n  function dkMultiReadPage(items, page) {\n    function clean(t) { return t.replace(/[\\u0000-\\u001f]/g, ' '); }\n    var all = clean(items.map(function (o) { return o.s; }).join(' '));\n    // 別売品の一覧のページにも室内機の品番は出るが、そこは読まない。\n    // 「マルチ」の字では絞らない（59ページの室内機の一覧には「マルチ」の字が無く、丸ごと落ちていた）\n    if (/別売品の種類/.test(all)) return [];\n\n    // 「2M」「30YCV」のように割れて来る室外機の品番をつなぐ\n    var its = items.slice();\n    items.forEach(function (o) {\n      if (!/^\\dM$/.test(o.s.trim())) return;\n      var nx = null;\n      items.forEach(function (q) {\n        if (q !== o && Math.abs(q.y - o.y) < 1 && q.x > o.x && q.x - o.x < 20 && (!nx || q.x < nx.x)) nx = q;\n      });\n      if (nx) its.push({ s: o.s.trim() + nx.s.trim(), x: o.x, y: o.y, w: 0 });\n    });\n\n    var codes = [];\n    its.forEach(function (o) {\n      var t = clean(o.s).trim(), m;\n      if ((m = t.match(/^PAC-(\\d{3})AV$/))) {\n        codes.push({ o: o, kind: 'pack', m: 'PAC-' + m[1] + 'AV' });\n      } else if ((m = t.match(/^(\\d)M(\\d{2,4})([A-Z]{1,3})V$/))) {\n        var d = m[2];\n        codes.push({ o: o, kind: 'out', m: t, rooms: Number(m[1]), cap: Number(d.length >= 4 ? d.slice(0, 3) : d.slice(0, 2)), coco: /YC|AC/.test(m[3]) });\n      } else if ((m = t.match(/^C(\\d{2})(\\d?)([A-Z]{2,4}?)V([WK]?)(?=$|[-(（／\\s])/)) && DK_MULTI_IN[m[3]]) {\n        codes.push({ o: o, kind: 'in', m: 'C' + m[1] + m[2] + m[3] + 'V' + m[4], cap: Number(m[1]), type: DK_MULTI_IN[m[3]] });\n      }\n    });\n\n    var out = [];\n    codes.forEach(function (k) {\n      var o = k.o;\n      // その品番の列：右どなりの品番（上下120ポイント以内）の手前まで\n      var bx1 = o.x + 150;\n      codes.forEach(function (k2) {\n        if (k2 !== k && k2.o.x > o.x + 20 && Math.abs(k2.o.y - o.y) < 120) bx1 = Math.min(bx1, k2.o.x - 6);\n      });\n      var band = its.filter(function (q) { return q.x >= o.x - 6 && q.x < bx1; });\n      function sel(dyLo, dyHi) {\n        return band.filter(function (q) { var dy = q.y - o.y; return dy >= dyLo && dy <= dyHi; })\n          .sort(function (a, b) { return b.y - a.y || a.x - b.x; });\n      }\n      function text(dyLo, dyHi) { return sel(dyLo, dyHi).map(function (q) { return clean(q.s); }).join(' '); }\n\n      // 値段は「税抜き」と4けた以上の数。「（税抜き 249,000」「円）」と別の行に割れることがある（58ページ C40ZGV）\n      var TAX = /税抜き\\s*([\\d,]{4,})/;\n      // ビルトイン形（59ページ C283ALV）は値段の行が品番の18〜25ポイント下にある。上から順に見るので、自分の値段が先に当たる\n      var price = text(-26, 2).match(TAX);\n      if (!price) return;\n      var y = yen(price[1]);\n      var pw = text(-20, 2).match(/単\\s*(100|200)\\s*V/);\n\n      if (k.kind === 'pack') {\n        var ins = text(-40, -8).match(/C\\d{2}RTV/g) || [];\n        var om = text(-50, -10).match(/MP\\d{3}AV/);\n        if (!ins.length || !om) return;\n        var caps = ins.map(function (c) { return (Number(c.slice(1, 3)) / 10).toFixed(1) + 'kW'; });\n        out.push({\n          page: page, m: k.m, y: y, om: om[0],\n          im: ins.every(function (c) { return c === ins[0]; }) ? ins[0] + '×' + ins.length : ins.join('＋'),\n          pm: '', cap: caps.join('＋') + '（' + ins.length + '室）',\n          s: 'マルチパック', i: '壁掛形（' + ins.length + '室パック）', tp: 'マルチパック（' + ins.length + '室）',\n          pw: '単相' + (pw ? pw[1] : '200') + 'V', rc: 'ワイヤレス', opt: ''\n        });\n        return;\n      }\n      if (k.kind === 'out') {\n        out.push({\n          page: page, m: k.m, y: y, om: k.m, im: '', pm: '',\n          cap: (k.cap / 10).toFixed(1) + 'kW（' + k.rooms + '室用）',\n          s: k.coco ? 'ココタス（室外機）' : 'システムマルチ（室外機）', i: 'マルチ室外機', tp: k.rooms + '室用',\n          pw: '単相' + (pw ? pw[1] : '200') + 'V', rc: '', opt: ''\n        });\n        return;\n      }\n\n      // 室内機。パネル・グリルが別売なら「合計価格」ごとに1台\n      var base = {\n        page: page, m: k.m, y: y, om: '', im: k.m, pm: '', cap: dkCapLabel(k.cap),\n        s: 'マルチ用室内機', i: k.type, tp: 'マルチ用室内機', pw: '室外機から', rc: 'ワイヤレス', opt: ''\n      };\n      var lo = -100;\n      codes.forEach(function (k2) {\n        if (k2 === k || k2.o.y >= o.y || k2.o.x < o.x - 6 || k2.o.x >= bx1) return;\n        // 色違いの兄弟（最後の W・K だけ違う）は、同じ1組なのでさえぎらない\n        if (k2.kind === 'in' && k2.m.replace(/[WK]$/, '') === k.m.replace(/[WK]$/, '') && o.y - k2.o.y < 15) return;\n        lo = Math.max(lo, k2.o.y - o.y + 12);\n      });\n      // 「別売グリル・据付枠別」とある室内機（ビルトイン形）は、グリルと据付枠が別に要る\n      if (/グリル・?据付枠別/.test(text(-12, 2).replace(/\\s+/g, ''))) base.opt = '別売グリル・据付枠が別に必要';\n      var tots = sel(lo, -5).filter(function (q) { return /合計価格/.test(clean(q.s)); });\n      var used = [], prevByCol = [], variants = [];\n      tots.forEach(function (T) {\n        function near(q, dyLo, dyHi, xLo, xHi) {\n          return q.y - T.y >= dyLo && q.y - T.y <= dyHi && q.x >= T.x + xLo && q.x <= T.x + xHi;\n        }\n        var cand = band.filter(function (q) { return near(q, -3, 3, -5, 120) && TAX.test(clean(q.s)); });\n        if (!cand.length) cand = band.filter(function (q) { return near(q, -9, -3, -30, 120) && TAX.test(clean(q.s)); });\n        cand = cand.filter(function (q) { return used.indexOf(q) < 0; });\n        if (!cand.length) return;\n        cand.sort(function (a, b) { return Math.abs(a.x - T.x) - Math.abs(b.x - T.x); });\n        used.push(cand[0]);\n        var tot = yen(clean(cand[0].s).match(TAX)[1]);\n        // この合計に入る部品：同じ縦の列で、ひとつ上の合計（無ければ本体の値段の行）との間\n        var upper = o.y - 12;\n        prevByCol.forEach(function (P) { if (Math.abs(P.x - T.x) < 40 && P.y > T.y) upper = Math.min(upper, P.y); });\n        prevByCol.push(T);\n        var parts = [], names = [];\n        band.forEach(function (q) {\n          if (!(q.y > T.y && q.y < upper && Math.abs(q.x - T.x) < 60)) return;\n          (clean(q.s).match(DK_ROOM_PARTS) || []).forEach(function (x) { if (parts.indexOf(x) < 0) parts.push(x); });\n        });\n        parts.forEach(function (x) { var nm = dkPanelName(x); if (nm && names.indexOf(nm) < 0) names.push(nm); });\n        if (/グリル|据付枠/.test(clean(T.s) + text(T.y - o.y - 2, T.y - o.y + 8))) {\n          if (names.indexOf('前面グリル・据付枠') < 0) names.push('前面グリル・据付枠');\n        }\n        // 「別売前面グリル・据付枠合計価格」はグリルと据付枠だけの合計。本体の値段に足す\n        var full = /^合計価格/.test(clean(T.s).trim());\n        var onlyPanels = parts.length > 1 && parts.every(function (x) { return /^B[CG]/.test(x); });\n        variants.push({ y: full ? tot : y + tot, pm: parts.join(onlyPanels ? '／' : '・'), label: names.join('＋') });\n      });\n      if (!variants.length) { out.push(base); return; }\n      variants.forEach(function (v) {\n        var x = {};\n        for (var key in base) x[key] = base[key];\n        x.y = v.y; x.pm = v.pm;\n        x.opt = (v.label || 'パネル') + '込み';\n        out.push(x);\n      });\n    });\n    return out;\n  }\n\n\n  /* --------------------------------------------------------------------\n     ダイキン ルームエアコンの別売品（住宅設備用カタログ 69〜77ページ）\n     --------------------------------------------------------------------\n     紙面の表は3通り\n       ・●の表：縦に別売品（品番・税込価格）、横に機種の列。列の見出しは「【RX】S22～406ATRS」のような品番の範囲。\n         1つの列の見出しに、上の段（セパレート）と下の段（システムマルチ）の範囲が縦に積まれている。\n         見出しの文字の左はしと●の位置はずれるので、●の列のあいだのまん中を境目にして区画に分け、\n         見出しのまん中がどの区画に入るかで列を決める（69ページは1列目に見出しが2つ〔RX・AX と DX〕ある）\n       ・一覧の表（70ページ）：「機種名 システムマルチ C28～56ZCV」の下に品目が並ぶ。●は無い。表の中の範囲全部に付く\n       ・配管の太さの表（71ページ、ビルトイン形のダクト）：列が φ100～φ200。見出しの形の名前で付ける\n     **値段は税込**（「本ページに掲載の別売品は税込価格を表記しています」）。1.1で割って税抜きにする。\n       パネル BC40JF-WF は税込136,400円 → 124,000円で、機種のページの税抜きと合う\n     -------------------------------------------------------------------- */\n  // 別売品の品番（頭がKかBで、数字を含む。うしろの（WW）（W）（T）は色）\n  // 数字の無い品番もある（71ページ「K-FDSKD」断熱材・「K-FDSKDP」ダクトテープ・「K-FDBPA」バンド本体・「K-FDBSPA」サドルバンド）\n  var DKO_CODE = /^((?:K-?[A-Z]{1,6}|B[A-Z]{1,3})[A-Z0-9-]*\\d[A-Z0-9-]*|K-[A-Z]{4,8})((?:\\([A-Z]+\\))*)$/;\n  var DKO_MONEY = /([\\d,]{2,})\\s*円/;\n\n  // 見出しの品番の範囲 → 付く機種の形\n  function dkoRangeFit(txt) {\n    var t = String(txt || '').replace(/[\\u0000-\\u001f\\s]/g, '');\n    var m = t.match(/^(MP|\\dM|[SC])(\\d{2})([～~〜・])(\\d{2})([0-9]?[A-Z][A-Z0-9]*)$/);\n    if (m) return { rm: { p: m[1], t: m[5], r: m[3] !== '・', c: [Number(m[2]), Number(m[4])] } };\n    m = t.match(/^(MP|\\dM|[SC])(\\d{2,4}[A-Z][A-Z0-9]*)$/);\n    if (m) return { mc: m[1] + m[2] };\n    return null;\n  }\n\n  function dkRoomOptReadPage(items, page) {\n    function clean(t) { return String(t || '').replace(/[\\u0000-\\u001f]/g, ' '); }\n    var all = clean(items.map(function (o) { return o.s; }).join(''));\n    // 別売品のページ（69〜78）には必ず「本ページに掲載の別売品は税込価格を表記しています」とある。\n    // 機種のページ（43〜48）の「パネル（別売）価格…」まで読んでいたので、これで絞る\n    if (!/税込価格を表記/.test(all.replace(/\\s/g, ''))) return [];\n    var its = items.filter(function (o) { return clean(o.s).trim(); });\n\n    // 「S40」「・566ATRP」のように割れて来る見出しをつなぐ\n    var merged = its.slice();\n    its.forEach(function (o) {\n      var t0 = clean(o.s).trim();\n      if (!/^(MP|\\dM|[SC])\\d{2}/.test(t0) || dkoRangeFit(t0)) return;\n      var txt = t0, end = o.x + (o.w || 0), grew = false;\n      for (var guard = 0; guard < 4; guard++) {\n        var nx = null;\n        its.forEach(function (q) {\n          var tq = clean(q.s).trim();\n          if (q !== o && Math.abs(q.y - o.y) < 1 && q.x >= end - 2 && q.x - end < 4 &&\n              /^[・～~〜0-9A-Z]+$/.test(tq) && (!nx || q.x < nx.x)) nx = q;\n        });\n        if (!nx) break;\n        txt += clean(nx.s).trim(); end = nx.x + (nx.w || 0); grew = true;\n      }\n      if (grew && dkoRangeFit(txt)) merged.push({ s: txt, x: o.x, y: o.y, w: end - o.x });\n    });\n\n    // 表の見出し（「…別売品」）で区切る。左右2段のページは左右に分ける\n    /* 表の見出しは「…用別売品」「…関連別売品」「室外機用別売品（…」で終わる行。\n       ページの下の注記（「…他のHA端子S21を使用する別売品との併用は…」）にも「別売品」の字があり、\n       それを右の段の見出しと取り違えて、ページを左右2段に分け、右半分の●の列を捨てていた（73ページ） */\n    var titles = its.filter(function (o) {\n      var t = clean(o.s).replace(/\\s+/g, '');\n      return /(用|関連|配管)別売品/.test(t) && !/。|注|別売品名|別売品の|別売品は|別売品に|別売品と|別売品を|別売品が/.test(t);\n    });\n    var twoCol = titles.some(function (o) { return o.x >= 300; });\n    var out = [];\n    // 78ページのスカイダクト（配管化粧ダクト）も見出しに「別売品」が無い。どの機種にも使う部材\n    if (!titles.length) { readDkoTable(merged, /スカイダクト/.test(all) ? 'スカイダクト' : '', page, out, clean); return out; }\n    titles.forEach(function (T) {\n      var right = twoCol && T.x >= 300;\n      var xLo = right ? 305 : 0, xHi = (twoCol && !right) ? 310 : 1e9;\n      // 表の下の端。70ページのいちばん下の行（KDG99C41-X）は高さ40にあり、40より上としていて落ちていた\n      var yLo = 20;\n      titles.forEach(function (U) {\n        if (U === T || U.y >= T.y - 2) return;\n        if ((twoCol && U.x >= 300) !== right) return;\n        yLo = Math.max(yLo, U.y + 2);\n      });\n      var reg = merged.filter(function (o) { return o.x >= xLo && o.x < xHi && o.y < T.y + 2 && o.y > yLo; });\n      var title = its.filter(function (o) { return Math.abs(o.y - T.y) < 2 && o.x <= T.x + 1 && o.x > T.x - 200; })\n        .sort(function (a, b) { return a.x - b.x; }).map(function (o) { return clean(o.s); }).join('').replace(/\\s+/g, '');\n      readDkoTable(reg, title, page, out, clean);\n    });\n    return out;\n  }\n\n  /* 別売品の表の品名（2026-09-11）\n     左の欄は2段のことが多い（74ページ）。\n       左の段：大きなまとまり「樹脂製日除け屋根」「防雪屋根」「防雪フード」（何行ぶんものマスのまん中）\n       右の段：その中の区分「塗装」「ステンレス」「（加湿部）」「（吸込側面）」（これも何行ぶんものまん中）\n     いちばん近い名前を採ると、となりのまとまりの区分（「ステンレス」）を取ってしまう。\n     日立の別売品と同じく、「分けた行のまん中」と「名前の高さ」がいちばん合う分け方で割り当てる。\n     区分は、大きなまとまりの中だけで分ける */\n  function dkoAssign(rowYs, labels, U, E, glue) {\n    var n = rowYs.length, k = labels.length, INF = 1e12, i, j, p, t;\n    var f = [], from = [];\n    for (i = 0; i <= n; i++) { f.push([]); from.push([]); for (j = 0; j <= k; j++) { f[i].push(INF); from[i].push(null); } }\n    f[0][0] = 0;\n    for (i = 0; i <= n; i++) {\n      for (j = 0; j <= k; j++) {\n        if (!i && !j) continue;\n        var best = INF, how = null;\n        if (i > 0 && f[i - 1][j] + U < best) { best = f[i - 1][j] + U; how = ['skip']; }\n        if (j > 0 && f[i][j - 1] + E < best) { best = f[i][j - 1] + E; how = ['empty']; }\n        if (j > 0) {\n          for (p = 0; p < i; p++) {\n            if (f[p][j - 1] >= INF) continue;\n            // glue[t]：t-1行目とt行目は同じマス。その境目でまとまりを切らない\n            if (glue && ((p > 0 && glue[p]) || (i < n && glue[i]))) continue;\n            var sum = 0;\n            for (t = p; t < i; t++) sum += rowYs[t];\n            var c = f[p][j - 1] + Math.abs(sum / (i - p) - labels[j - 1].y);\n            if (c < best) { best = c; how = ['block', p]; }\n          }\n        }\n        f[i][j] = best; from[i][j] = how;\n      }\n    }\n    var asg = [];\n    for (t = 0; t < n; t++) asg.push(-1);\n    i = n; j = k;\n    while (i > 0 || j > 0) {\n      var h = from[i][j];\n      if (!h) break;\n      if (h[0] === 'skip') i--;\n      else if (h[0] === 'empty') j--;\n      else { for (t = h[1]; t < i; t++) asg[t] = j - 1; i = h[1]; j--; }\n    }\n    return asg;\n  }\n\n  function dkoNames(reg, codes, top, clean) {\n    function isNameBit(o) {\n      var tt = clean(o.s).trim();\n      if (!tt || /^注\\s*[\\d,\\s]*$/.test(tt) || /^[●―□★※]+$/.test(tt)) return false;\n      if (DKO_CODE.test(tt) || /^[SCRF]\\d{2}/.test(tt) || DKO_MONEY.test(tt) || /^[\\d,]+$/.test(tt)) return false;\n      if (/。/.test(tt)) return false;                        // 表の下の注記の文\n      return true;\n    }\n    function tidy(t) {\n      // 全角の英字と数字は半角にそろえる（「Ｐ板」「高さ １２０」）\n      t = t.replace(/[Ａ-Ｚａ-ｚ０-９]/g, function (ch) { return String.fromCharCode(ch.charCodeAt(0) - 0xFEE0); });\n      t = t.replace(/注\\s*[\\d,]+/g, ' ').replace(/★\\s*\\d?|□|※/g, ' ').replace(/別売品名/g, ' ')\n        .replace(/([ぁ-んァ-ヶー一-龥（）])\\s+(?=[ぁ-んァ-ヶー一-龥（）])/g, '$1')\n        .replace(/\\s+/g, ' ').trim();\n      // 1文字ずつあいた英数字（73ページ「L A N」「H E M S」、70ページ「N 3 . 3」、77ページ「高さ ３ ０ ０」）をつなぐ\n      // かっこを離してから数える（「３０ ０）」の「０）」も1けたの数字として見る）\n      var tok = t.replace(/（/g, '（ ').replace(/）/g, ' ）').replace(/\\s+/g, ' ').trim().split(' '), outT = [], prevOne = false;\n      tok.forEach(function (w) {\n        var one1 = /^[A-Za-z0-9.０-９Ａ-Ｚ．]$/.test(w);\n        // 「３０」「０」のように数字が2つに割れて来ることもある（77ページ「高さ 300」）\n        var digAfterDigits = /^[0-9]$/.test(w) && /^[0-9.]+$/.test(outT[outT.length - 1] || '');\n        if (one1 && (prevOne || digAfterDigits)) outT[outT.length - 1] += w; else outT.push(w);\n        prevOne = one1;\n      });\n      return outT.join(' ').replace(/（\\s+/g, '（').replace(/\\s+）/g, '）');\n    }\n    // 品番の行\n    var rows = [];\n    codes.forEach(function (c) { if (!rows.some(function (y) { return Math.abs(y - c.y) < 2.5; })) rows.push(c.y); });\n    rows.sort(function (a, b) { return b - a; });\n    var xsC = codes.map(function (c) { return c.x; }).sort(function (a, b) { return a - b; });\n    /* 名前の欄の端にする品番の列は、左から見て「いちばん多い列の3分の1以上（3個以上）の品番がある列」。\n       70ページ左下は上の表（x167 に17個）と下のワイドグリルの表（x123 に4個）が1つの区切りに入り、\n       x123 を端にして、上の表の名前の右半分（「壁用」「間用」「空清フィルタ」の「ー」）を捨てていた */\n    var colN = xsC.map(function (x0) { return xsC.filter(function (x) { return Math.abs(x - x0) < 6; }).length; });\n    var maxN = Math.max.apply(null, colN);\n    var codeX = xsC[0];\n    for (var q = 0; q < xsC.length; q++) {\n      if (colN[q] >= 3 && colN[q] * 3 >= maxN) { codeX = xsC[q]; break; }\n    }\n    var lo = Math.min.apply(null, rows) - 12;\n    /* 名前の欄の左の端。名前の欄の左に、同じ高さで別の小さな表（78ページ「バンドホルダー K-TH7A 99 円」）があると、\n       その表の名前がまとまりの名前になり、TM シリーズの35品目が「バンドホルダー」になっていた。\n       名前の列の品番と同じ高さの範囲で、名前の列より左にある品番・値段の右はしより右だけを名前にする */\n    var colYs = codes.filter(function (c) { return Math.abs(c.x - codeX) < 6; }).map(function (c) { return c.y; });\n    var yHi = Math.max.apply(null, colYs) + 4, yLo = Math.min.apply(null, colYs) - 4;\n    var xLeft = -1e9;\n    reg.forEach(function (o) {\n      if (o.x >= codeX - 20 || o.y > yHi || o.y < yLo) return;\n      var tt = clean(o.s).trim();\n      if ((DKO_CODE.test(tt) && !/^[SCRF]\\d/.test(tt)) || DKO_MONEY.test(tt)) xLeft = Math.max(xLeft, o.x + (o.w || 0));\n    });\n    var bits = reg.filter(function (o) { return o.x < codeX - 2 && o.x > xLeft + 2 && o.y <= top + 4 && o.y >= lo && isNameBit(o); })\n      .sort(function (a, b) { return b.y - a.y || a.x - b.x; });\n    /* 縦書きの名前（70ページ「前」「面」「グ」「リ」「ル※」が x57 に1文字ずつ縦に並ぶ）は、\n       となりの横書きの名前（「和 風 （ 白 木 ）」）とくっついて「前和 風（白」になっていた。\n       同じ x に1文字の字が4つ以上、4〜9ポイントの間で縦に続き、その多くが右どなりに1文字の字を持たない\n       （字間をあけた横書き「簡　単」「気　密」ではない）ものは、上から読んで1つの名前にする */\n    var single = bits.filter(function (o) { return /^[ぁ-んァ-ヶー一-龥]※?$/.test(clean(o.s).trim()); });\n    var used = [], vert = [];\n    single.forEach(function (o) {\n      if (used.indexOf(o) >= 0) return;\n      var ch = [o];\n      for (;;) {\n        var a0 = ch[ch.length - 1];\n        var nx = single.filter(function (p) {\n          return used.indexOf(p) < 0 && ch.indexOf(p) < 0 && Math.abs(p.x - a0.x) <= 1.5 && a0.y - p.y >= 4 && a0.y - p.y <= 9;\n        })[0];\n        if (!nx) break;\n        ch.push(nx);\n      }\n      if (ch.length < 4) return;\n      var spread = ch.filter(function (c) {\n        // 右か左に、同じ高さの1文字の字があれば字間をあけた横書き（「ミ ン ト グ リ ー ン」の右はしの「ン」）\n        return single.some(function (p) { return p !== c && Math.abs(p.y - c.y) < 0.6 && Math.abs(p.x - c.x) > 4 && Math.abs(p.x - c.x) < 60; });\n      }).length;\n      if (spread * 2 >= ch.length) return;\n      ch.forEach(function (c) { used.push(c); });\n      vert.push({ x: o.x, hi: ch[0].y, lo: ch[ch.length - 1].y, t: ch.map(function (c) { return clean(c.s).trim(); }).join('') });\n    });\n    bits = bits.filter(function (o) { return used.indexOf(o) < 0; });\n    // 同じ高さで続く文字を1行に（「ペ」「ア」「コ」「イ」「ル」）\n    var lines = [];\n    function one(o) { return /^[ぁ-んァ-ヶー一-龥（）・]$/.test(clean(o.s).trim()); }\n    bits.forEach(function (o) {\n      var L = null;\n      lines.forEach(function (x) {\n        if (Math.abs(x.y - o.y) >= 2.5 || o.x < x.x - 1) return;\n        // 1文字ずつ間をあけて組んだ名前（39ページ「パ　ネ　ル」「気　密　枠」）は、字の間が広くても1行\n        // 70ページ「気　　密　　枠」は字の間が47ある（同じ高さぴったりのときだけ60まで）\n        if (o.x - x.end < 14 || (x.one && one(o) && Math.abs(x.y - o.y) < 1.2 &&\n            (o.x - x.end < 36 || (Math.abs(x.y - o.y) < 0.6 && o.x - x.end < 60)))) L = x;\n      });\n      if (!L) { L = { x: o.x, end: o.x + (o.w || 0), y: o.y, t: '', src: [] }; lines.push(L); }\n      L.t += ' ' + clean(o.s); L.end = Math.max(L.end, o.x + (o.w || 0));\n      L.src.push(o);\n      L.one = one(o) || /\\s[ぁ-んァ-ヶー一-龥]$/.test(clean(o.s).trim());\n    });\n    /* 区分の列をまたいだ札を切る（2026-09-12）\n       70ページのパネルの欄は、区分がどれも x=130.4 から始まる\n       （「フレッシュホワイト」「ブラウン」「木目」「枠付」「枠なし」）。\n       外側のまとまり「パ ネ ル」は x=53.9 から字間27で組んであり、\n       「ル」の終わり124.7 と「ブ」の始まり130.4 の間は 5.7 しかないので1行につながって\n       「パネルブラウン」になり、BG50N-WF（白）と BG50F-T（茶）が同じ名前になっていた。\n       間の広さでは見分けられない（「空 清 フ ィ ル タ ー」の字間も5.4）。\n       札を組んだあとで「3つ以上の札が同じxから始まる」ところを列の境目とし、\n       そこをまたぐ札だけを切る。字ひとつずつのxで決めると、よその行の字と\n       たまたま揃ったところで切ってしまう（「パネル」が「パ」になった）*/\n    (function () {\n      var cl = [];\n      lines.forEach(function (L) {\n        var c = null;\n        cl.forEach(function (g) { if (Math.abs(g.x - L.x) <= 1.5) c = g; });\n        if (!c) { c = { x: L.x, ys: [] }; cl.push(c); }\n        if (!c.ys.some(function (y) { return Math.abs(y - L.y) < 2.5; })) c.ys.push(L.y);\n      });\n      var cols = cl.filter(function (g) { return g.ys.length >= 3; }).map(function (g) { return g.x; })\n        .sort(function (a, b) { return a - b; });\n      if (!cols.length) return;\n      var add = [];\n      lines.forEach(function (L) {\n        if (!L.src || L.src.length < 2) return;\n        /* その境目を、この札の近くの行が本当に使っているか。\n           ページ全体で見ると「揚程」102.0 や「単相100V」135.2 も境目に見えるが、\n           それはずっと下のドレンの欄の列。近くの行（上下30ポイント）で\n           2つ以上の札がそこから始まっているものだけを境目として使う */\n        var cut = cols.filter(function (cx) {\n          if (!(cx > L.x + 1.5 && cx <= L.end + 1.5)) return false;\n          var near = lines.filter(function (M) {\n            return M !== L && Math.abs(M.x - cx) <= 1.5 && Math.abs(M.y - L.y) <= 30;\n          });\n          return near.length >= 2;\n        });\n        if (!cut.length) return;\n        var src = L.src.slice().sort(function (a, b) { return a.x - b.x; });\n        // 字と字の間（前の字の終わりから次の字の始まりまで）\n        var gaps = [0];\n        for (var q1 = 1; q1 < src.length; q1++) gaps.push(src[q1].x - (src[q1 - 1].x + (src[q1 - 1].w || 0)));\n        function med(lo2, hi2) {\n          var v = [];\n          for (var q2 = lo2; q2 <= hi2; q2++) if (q2 >= 1 && q2 < gaps.length) v.push(gaps[q2]);\n          if (!v.length) return -1;\n          v.sort(function (a, b) { return a - b; });\n          return v[v.length >> 1];\n        }\n        // 列の境目に当たる字の場所\n        var cand = [];\n        for (var q3 = 1; q3 < src.length; q3++) {\n          if (cut.some(function (cx) { return src[q3].x >= cx - 1.5 && src[q3 - 1].x < cx - 1.5; })) cand.push(q3);\n        }\n        if (!cand.length) return;\n        /* 札の切れ目は「字間が変わるところ」（2026-09-12）\n           70ページ「据 付 枠 | 塗 壁 用」……字間 7.3 → 26.5 で変わる。ここが切れ目\n             （2文字目の「付」も列の境目に当たるが、字間が7.3のまま続くので切らない）\n           77ページ「屋 根 直 角 置 台 | 塗 装」……7.0 → 17.3\n           70ページ「パ ネ ル | ブ ラ ウ ン」……27.1 → 4.5\n           右が1文字だけのときは、切れ目の間の広さを左の字間とくらべる\n             （71ページ「キ ッ ト | 3m」……字間20.8に対し切れ目11.6）*/\n        var acc = [], start = 0;\n        cand.forEach(function (ci, k1) {\n          var before = med(start + 1, ci - 1);\n          var nextC = k1 + 1 < cand.length ? cand[k1 + 1] : src.length;\n          var after = med(ci + 1, nextC - 1);\n          var ok;\n          /* 字間の話は「1文字ずつ間をあけて組んだ札」だけの話。\n             ふつうの語どうし（「TLシリーズ」＋「ウォールカバー丸フランジ」）は\n             間が狭いのがふつうなので、列の境目をそのまま信じる */\n          if (!(one(src[ci - 1]) && one(src[ci]))) ok = true;\n          else if (before < 0) ok = /^[A-Za-z]$/.test(clean(src[0].s).trim());   // 左が1文字（英字1文字だけ切ってよい）\n          else if (after >= 0) ok = Math.abs(after - before) > Math.max(after, before) * 0.35;\n          else ok = Math.abs(gaps[ci] - before) > before * 0.35;\n          if (ok) { acc.push(ci); start = ci; }\n        });\n        if (!acc.length) return;\n        var groups = [[]];\n        src.forEach(function (o, oi) { if (acc.indexOf(oi) >= 0) groups.push([]); groups[groups.length - 1].push(o); });\n        if (groups.length < 2) return;\n        var first = groups.shift();\n        L.t = first.map(function (o) { return ' ' + clean(o.s); }).join('');\n        L.x = first[0].x; L.end = first[first.length - 1].x + (first[first.length - 1].w || 0);\n        groups.forEach(function (g) {\n          if (!g.length) return;\n          add.push({ x: g[0].x, end: g[g.length - 1].x + (g[g.length - 1].w || 0), y: L.y,\n                     t: g.map(function (o) { return ' ' + clean(o.s); }).join(''), src: g });\n        });\n      });\n      lines.push.apply(lines, add);\n    })();\n    lines.forEach(function (L) { L.t = tidy(L.t); });\n    // 縦書きの名前は、まん中の高さの1行として足す\n    vert.forEach(function (v) { lines.push({ x: v.x, end: v.x + 6, y: (v.hi + v.lo) / 2, t: tidy(v.t) }); });\n    // 1文字の英字の行（78ページ「Ｔ」）は、すぐ下の英字で始まる行（「M シリーズ」）の頭につなぐ（「TM シリーズ」）\n    lines.forEach(function (L) {\n      if (!/^[A-Za-z]$/.test(L.t.trim())) return;\n      var below = lines.filter(function (M) {\n        return M !== L && /^[A-Za-z]/.test(M.t) && L.y - M.y > 2 && L.y - M.y <= 20 && Math.abs(M.x - L.x) <= 6;\n      }).sort(function (a, b) { return b.y - a.y; })[0];\n      if (below) { below.t = L.t.trim() + below.t; L.t = ''; }\n    });\n    // 表の下の注記（「注 1. 防雪フード（加湿部）…」は「注 1」を抜くと「. 防雪フード…」になる）は名前にしない\n    lines = lines.filter(function (L) { return L.t && !/^[.．、,注※]/.test(L.t) && !/[、。]/.test(L.t) && /[ぁ-んァ-ヶー一-龥\\dａ-ｚA-Za-z]/.test(L.t); });\n    // 句点が別の文字で来る注記の文（77ページ「…のいずれかを使用してくださ」「い。」）も名前にしない\n    lines = lines.filter(function (L) { return !/くださ|ます|です|ません|を使用する|のいずれか|お願い|納品姿|ご注文|販売単位/.test(L.t) && !/^[\\d\\s.]+$/.test(L.t); });\n    // 表の見出しの言葉（78ページ「梱包入数」「外観」「室内用」「ダクト外寸（mm）…」）と、英字1文字だけの行は名前にしない\n    lines = lines.filter(function (L) { return !/^(梱包入数|外観|品番|価格|機種名|適用銅管|室内用|ダクト外寸.*|外形寸法.*|[Ａ-ＺA-Z]|[\\d,]+ ?個)$/.test(L.t.trim()); });\n    /* 区切りの見出し（73ページ「■遠隔制御Ｐ板による遠隔制御」「■ＬＡＮ接続」）は区切りの頭に書いてある。\n       まん中に書く名前と同じに扱うと、上の区切りの品物にまで下の見出しを付けていた。\n       名前には使わず、見出しから次の見出しまでを1つの区切りにする */\n    var heads = lines.filter(function (L) { return /^■/.test(L.t); }).map(function (L) { return L.y; });\n    lines = lines.filter(function (L) { return !/^■/.test(L.t); });\n    function sectionOf(y) {\n      var best = null;\n      heads.forEach(function (h) { if (h > y && (best == null || h < best)) best = h; });\n      return best == null ? 1e9 : best;\n    }\n    if (!lines.length) return { rows: rows, names: [], codeX: codeX };\n    // 値段のある品番の行ぜんぶで見る（70ページ KAF968B41「枠付」は2列目の品番の行）\n    var rowsX = rows;\n    // 一覧の名前は行の高さぴったり（0.3 以内）。69ページ「埋込配管用／シングルコイル／組合せ」は行から1.0ずれた3行の名前\n    function onRow(y) { return y != null && rowsX.some(function (r) { return Math.abs(r - y) <= 0.8; }); }\n    function len(t) { return t.replace(/\\s+/g, '').length; }\n    /* 段：いちばん左に始まる行が「まとまり」、それより右が「区分」。\n       区分は1列と決めつけていたが、2列ある表がある（2026-09-12）。\n       71ページの断熱フレキシブルダクトは\n         「断熱フレキシブルダクト関連」（x52.7）／「キット」（x65.2）／「1m」「2m」「3m」（x134.9）\n       の3段。1列しか採らないと「3m」が落ちて、1mも2mも3mも「キット」になる。\n       札の左端でまとめて、左から順に列とし、列ごとに1つずつ札を割り当ててつなぐ */\n    var minX = Math.min.apply(null, lines.map(function (L) { return L.x; }));\n    var colXs = (function () {\n      var xs = lines.map(function (L) { return L.x; }).sort(function (a, b) { return a - b; }), cs = [];\n      xs.forEach(function (x) { if (!cs.length || x - cs[cs.length - 1] >= 6) cs.push(x); });\n      return cs;\n    })();\n    function colOfX(x) {\n      var k = 0;\n      for (var t = 0; t < colXs.length; t++) if (colXs[t] <= x + 3) k = t;\n      return k;\n    }\n    function labelsOf(col) {\n      var ls = lines.filter(function (L) { return colOfX(L.x) === col; })\n        .sort(function (a, b) { return b.y - a.y; });\n      // 縦に続く行（「防雪」「フード」）は1つの名前\n      var out = [];\n      ls.forEach(function (L) {\n        var last = out[out.length - 1];\n        /* 1行に1つ名前がある一覧の表（45ページ「集塵・脱臭フィルター枠付」「抗ウイルスフィルター枠付」「木台」…）は、\n           行の高さに名前が1つずつある。縦に続く1つの名前（「防雪三点」「セット」）と思ってつなげると、\n           表の名前が全部1つにつながった（39・45・70ページの34品目）。行の高さにある長い名前どうしはつなげない */\n        // 長い名前（9文字以上）のあとだけ止める。73ページ「無線 L A N 接」「続アダプター」は1つの名前の途中で割れている\n        var lt = last ? (last.lt || last.t) : '';\n        var paren = /^[（(]/.test(L.t);\n        var own = col === 0 && last && !paren && (\n          (onRow(last.lo) && onRow(L.y) && len(lt) >= 5 && len(L.t) >= 5) ||   // 行の高さの名前どうし（「防振フレーム」「ブラケット」）\n          (onRow(last.lo) && len(L.t) > 5 && len(lt) > 8) ||                    // 39ページ「ワイドパネル（470…）」のあとの「ワイドパネル（670…）」\n          (onRow(L.y) && len(lt) > 8));                                         // 「ワイドパネル（670…）」のあとの「気密枠」\n        // かっこで始まる行どうし（74ページ「（吸込側面）」「（吸込背面）」、77ページ「（標準）」「（戸袋用）」）は別々の区分\n        var sib = last && paren && /^[（(]/.test(lt) &&\n          (lt.match(/[（(]/g) || []).length <= (lt.match(/[）)]/g) || []).length;   // 前の行のかっこが閉じていなければ続き\n        /* 縦に3行以上、行の高さに1つずつ名前が並ぶ欄は一覧（39ページの色「フレッシュホワイト」「ホワイト」「ブラウン」「木目」）。\n           2行だけ（「防雪三点」「セット」、「配管スペーサー」「付き」）は1つの名前の続き */\n        var run = last && onRow(last.lo) && onRow(L.y) && lines.some(function (M) {\n          return M !== L && Math.abs(M.x - L.x) < 8 && onRow(M.y) &&\n            ((M.y < L.y - 2 && M.y > L.y - 10) || (M.y > last.hi + 2 && M.y < last.hi + 10));\n        });\n        /* かっこで始まる続きの行（69ページ「前面パネル」…「（交換用）」）は、マスの上と下に分けて書いてある。\n           間にほかの名前が無ければ、離れていてもつなぐ */\n        var near = last && (last.lo - L.y <= 7 ||\n          (last.lo - L.y <= 30 && /^[（(]/.test(L.t) && !ls.some(function (M) { return M.y < last.lo - 1 && M.y > L.y + 1; })));\n        if (last && !own && !run && !sib && near && Math.abs(last.x - L.x) < 8) { last.t += L.t; last.lo = L.y; last.ys.push(L.y); last.lt = L.t; }\n        else out.push({ x: L.x, hi: L.y, lo: L.y, t: L.t, ys: [L.y], lt: L.t });\n      });\n      out.forEach(function (x) { x.y = (x.hi + x.lo) / 2; });\n      return out;\n    }\n    var g = labelsOf(0), subCols = [];\n    for (var ci0 = 1; ci0 < colXs.length; ci0++) { var lv = labelsOf(ci0); if (lv.length) subCols.push(lv); }\n    var sub = subCols.length ? subCols[0] : [];\n    var names = rows.map(function () { return ''; });\n    /* 2つの行のまん中に名前の文字があれば、その2行は同じマスの中。\n       71ページのドレンアップキットは4行（K-KDU573MS・MV・NS・NV）で、名前は上寄りに書いてある。\n       まん中の高さだけで分けると3行と1行に分け、NV を下の「ドレンポンプキット」にしていた。\n       NS と NV のまん中には「ハーフサイズ」があるので、ここは切れない */\n    var glue = rows.map(function (y, t) {\n      if (!t) return false;\n      var a = rows[t - 1], gap = a - y, mid = (a + y) / 2;\n      if (gap > 9) return false;\n      return lines.some(function (L) { return Math.abs(L.y - mid) < gap / 4; });\n    });\n    // まとまりは区切りごとに割り当てる（区切りの中の名前だけを使う）\n    var ga = rows.map(function () { return -1; });\n    var sec = rows.map(function (y) { return sectionOf(y); });\n    var s0 = 0;\n    while (s0 < rows.length) {\n      var s1 = s0;\n      // 品番の行が30以上あいたら別の表（77ページの下の「防雪三点セットの組み合わせ」）\n      while (s1 + 1 < rows.length && sec[s1 + 1] === sec[s0] && rows[s1] - rows[s1 + 1] <= 30) s1++;\n      var top0 = (s0 > 0 && sec[s0 - 1] === sec[s0]) ? rows[s0 - 1] : sec[s0], bottom0 = s1 + 1 < rows.length ? rows[s1 + 1] : -1e9;\n      var gIn = g.filter(function (x) { return x.y < top0 && x.y > bottom0 && sectionOf(x.y) === sec[s0]; });\n      var part = dkoAssign(rows.slice(s0, s1 + 1), gIn, 40, 9, glue.slice(s0, s1 + 1).map(function (v, t) { return t > 0 && v; }));\n      for (var t0 = s0; t0 <= s1; t0++) ga[t0] = part[t0 - s0] >= 0 ? g.indexOf(gIn[part[t0 - s0]]) : -1;\n      s0 = s1 + 1;\n    }\n    // まとまりごとに、区分を割り当てる\n    var leaf = rows.map(function () { return false; });\n    function hasOn(lab, y) { return lab.ys.some(function (v) { return Math.abs(v - y) <= 1.2; }); }\n    var i = 0;\n    while (i < rows.length) {\n      var j = i;\n      while (j + 1 < rows.length && ga[j + 1] === ga[i] && sec[j + 1] === sec[i]) j++;\n      var blockRows = rows.slice(i, j + 1);\n      var hi = blockRows[0] + 4, lw = blockRows[blockRows.length - 1] - 4;\n      // 区分の列ごとに割り当てる（左から順に名前をつなぐ）\n      var picks = subCols.map(function (lv, pi0) {\n        var subs = lv.filter(function (x) { return x.y <= hi && x.y >= lw; });\n        /* 2列目より右の区分は、表の一部の行にしか札が無いのがふつう\n           （72ページのリモコンコードの長さ「3m」「8m」「12m」「20m」は15行のうち4行だけ）。\n           行を飛ばす代金を9にすると、札を大きなかたまりに付けてしまい、\n           そのあと「行の高さに無い」で捨てられて長さが消える（2026-09-12）。\n           5にすると、長さは自分の行に付き、ほかの名前は動かなかった\n           （0.5まで下げると別のものが壊れる。617品目で確かめた値が5）*/\n        return { subs: subs, sa: dkoAssign(blockRows, subs, pi0 ? 5 : 9, 9) };\n      });\n      for (var t = i; t <= j; t++) {\n        var parts = [], k0 = t - i, leafSub = false;\n        if (ga[t] >= 0) parts.push(g[ga[t]].t);\n        picks.forEach(function (P, pi) {\n          var sl = P.sa[k0] >= 0 ? P.subs[P.sa[k0]] : null;\n          if (!sl) return;\n          /* 2列目より右の区分は、品番の行の高さに書いてある札（「1m」「2m」…）が多い。\n             それは「その行だけの札」なので、よその行に付けてはいけない。\n             （付けると「TM シリーズ 端末カバー 2m」のように関係ない長さが混ざった）\n             行の間に書いてある札（何行ぶんかのマスのまん中＝「キット」）はそのまま使う */\n          if (pi >= 1 && sl.ys.every(onRow) && !hasOn(sl, rows[t])) return;\n          // すでに入っている言葉は足さない（「高さ 500 アルミ」のあとの「アルミ」）\n          var joined = parts.join(' ');\n          if (parts.indexOf(sl.t) < 0 && joined.replace(/\\s/g, '').indexOf(sl.t.replace(/\\s/g, '')) < 0) parts.push(sl.t);\n          /* 品番の行の高さに書いてあり、その行だけに付いた名前なら「その品物だけの名前」。\n             同じ品番が何ページにもあるとき（KRP413BB1S は70・71・73ページ）は、これを使う */\n          if (P.sa.filter(function (v) { return v === P.sa[k0]; }).length === 1 && hasOn(sl, rows[t])) leafSub = true;\n        });\n        names[t] = parts.join(' ').trim();\n        leaf[t] = leafSub ||\n                  (ga[t] >= 0 && ga.filter(function (v) { return v === ga[t]; }).length === 1 && hasOn(g[ga[t]], rows[t]));\n      }\n      i = j + 1;\n    }\n    return { rows: rows, names: names, codeX: codeX, leaf: leaf };\n  }\n\n  /* 1つの見出しの下に、列の間隔の違う表が上下に並ぶことがある（72ページのリモコン用別売品）。\n     2つの表の●の列を混ぜると、見出しの当たらない列ができて品目が落ちる。\n     品番の範囲の見出しが縦に続くかたまりごとに、表を分けて読む */\n  function readDkoTable(reg, title, page, out, clean) {\n    var hs = reg.filter(function (o) { return dkoRangeFit(clean(o.s)); })\n      .map(function (o) { return o.y; }).sort(function (a, b) { return b - a; });\n    var blocks = [];\n    hs.forEach(function (y) {\n      var last = blocks[blocks.length - 1];\n      if (last && last.lo - y <= 20) { last.lo = y; last.n++; }\n      else blocks.push({ hi: y, lo: y, n: 1 });\n    });\n    /* 表を分けるのは、本物の見出しのかたまり（品番の範囲が3つ以上）で、\n       その上の見出しとの間に品番の行が2つ以上あるときだけ。\n       表の途中にある範囲の文字1つ（注記の中など）で分けると、74・75ページの品目がごっそり落ちた */\n    var codeRows = [];\n    reg.forEach(function (o) {\n      var t = clean(o.s).trim();\n      if (DKO_CODE.test(t) && !/^[SCRF]\\d/.test(t) && !codeRows.some(function (y) { return Math.abs(y - o.y) < 2.5; })) codeRows.push(o.y);\n    });\n    var real = [];\n    blocks.forEach(function (b) {\n      if (!real.length) { real.push({ hi: b.hi, lo: b.lo }); return; }\n      var prev = real[real.length - 1];\n      var between = codeRows.filter(function (y) { return y < prev.lo && y > b.hi; }).length;\n      if (b.n >= 3 && between >= 2) real.push({ hi: b.hi, lo: b.lo });\n    });\n    if (real.length <= 1) { readDkoTable1(reg, title, page, out, clean); return; }\n    real.forEach(function (b, i) {\n      var bottom = i + 1 < real.length ? real[i + 1].hi + 12 : -1e9;\n      // 下の表は、その見出しの12上（【RX】などの札）から。上の表の品番の行は含めない\n      var sub = reg.filter(function (o) { return o.y > bottom && (i === 0 || o.y <= b.hi + 12); });\n      readDkoTable1(sub, title, page, out, clean);\n    });\n  }\n\n  function readDkoTable1(reg, title, page, out, clean) {\n    var codes = reg.filter(function (o) {\n      var t = clean(o.s).trim();\n      return DKO_CODE.test(t) && !/^[SCRF]\\d/.test(t) && !/^K[A-Z]$/.test(t);\n    });\n    if (!codes.length) return;\n    /* 表の上の端は、品番が縦に3つ以上ならぶ列の品番で決める。\n       71ページのドレンの表は、左の写真の説明（「● ドレンアップキット K-KDU573NS（NV）」）の品番が\n       見出しの高さにあり、それを表の上の端にして、見出しの範囲を31のうち7つしか数えなかった */\n    var inCol = codes.filter(function (c) {\n      return codes.filter(function (d) { return Math.abs(d.x - c.x) < 6; }).length >= 3;\n    });\n    if (inCol.length) {\n      var topCol = Math.max.apply(null, inCol.map(function (o) { return o.y; }));\n      codes = codes.filter(function (o) { return inCol.indexOf(o) >= 0 || o.y <= topCol + 2; });\n    }\n    var top = Math.max.apply(null, codes.map(function (o) { return o.y; }));\n    var heads = reg.filter(function (o) { return o.y > top + 2 && dkoRangeFit(clean(o.s)); });\n    // 列は ● だけで決める。一覧の表の「―」（その色は無い）を列と思い込み、表ごと捨てていた（70ページ）\n    var marks = reg.filter(function (o) { return /^●/.test(clean(o.s).trim()) && o.y <= top + 2; });\n\n    // ●の列：まん中の位置をまとめる\n    var mcols = [];\n    marks.forEach(function (o) {\n      var cx = o.x + 3, c = null;\n      mcols.forEach(function (k) { if (!c && Math.abs(k.x - cx) < 10) c = k; });\n      if (!c) { c = { x: cx, n: 0, fits: [] }; mcols.push(c); }\n      c.n++;\n    });\n    /* ●が1つしかない列も列と見る（72ページ下の表の KRC944A2 は、その列の●がこれ1つ）。\n       見出しの範囲が当たらない列は、下の「一覧の表として読む」で捨てられる */\n    mcols = mcols.sort(function (a, b) { return a.x - b.x; });\n    // 見出しを、●の列のあいだのまん中を境目にした区画に割り当てる\n    heads.forEach(function (h) {\n      var cx = h.x + (h.w || 0) / 2, best = -1;\n      for (var i = 0; i < mcols.length; i++) {\n        var lo = i ? (mcols[i - 1].x + mcols[i].x) / 2 : -1e9;\n        var hi = i < mcols.length - 1 ? (mcols[i].x + mcols[i + 1].x) / 2 : 1e9;\n        if (cx >= lo && cx < hi) best = i;\n      }\n      if (best >= 0) mcols[best].fits.push(dkoRangeFit(clean(h.s)));\n    });\n    /* ●の列に見出しの範囲が1つも当たらないなら、●の表ではない（注記の「●」を拾っただけ）。\n       71ページのビルトイン形のダクトの表を●の表と思い込み、●の無い行を120品目ほど捨てていた */\n    if (!mcols.some(function (k) { return k.fits.length; })) mcols = [];\n    var allFits = heads.map(function (h) { return dkoRangeFit(clean(h.s)); });\n    // 品名は表ごとにまとめて作る（何行ぶんもあるマスのまん中に書いた名前を、行に割り当てる）\n    /* 品名の行は、値段のある品番の行だけ。77ページは上の注記の中にも品番（「K-KW5G」など）があり、\n       その行まで名前の割り当てに入って、いちばん下の「つかみ金具」が40行に付いた */\n    var priced = codes.filter(function (c) {\n      return reg.some(function (o) {\n        if (Math.abs(o.y - c.y) >= 2.5 || o.x <= c.x) return false;\n        var tt = clean(o.s).trim();\n        return DKO_MONEY.test(tt) || /オープン価格/.test(tt) || /^[\\d,]{4,}$/.test(tt);\n      });\n    });\n    /* 品名は、値段のある品番の行を「行が20以上あいたところ」で分けた帯ごとに作る。\n       78ページは1つの区切りに D シリーズ・TL シリーズ・TM シリーズの表が縦に並び、品番の列の位置が表ごとに違う（x225 と x382）。\n       1つとして読むと、TM シリーズの品名を x225 より左の文字（となりの「バンドホルダー」の表）から付けていた */\n    var src = priced.length ? priced : codes;\n    var bys = [];\n    src.forEach(function (c) { if (!bys.some(function (y) { return Math.abs(y - c.y) < 2.5; })) bys.push(c.y); });\n    bys.sort(function (a, b) { return b - a; });\n    var bands = [];\n    bys.forEach(function (y) { var b = bands[bands.length - 1]; if (b && b.lo - y <= 20) b.lo = y; else bands.push({ hi: y, lo: y }); });\n    var nameRows = { rows: [], names: [], leaf: [], codeXs: [] };\n    bands.forEach(function (b, k) {\n      var up = k ? bands[k - 1].lo - 2 : 1e9, down = k + 1 < bands.length ? bands[k + 1].hi + 2 : -1e9;\n      var subReg = reg.filter(function (o) { return o.y < up && o.y > down; });\n      var subCodes = src.filter(function (c) { return c.y <= b.hi + 2.5 && c.y >= b.lo - 2.5; });\n      // 帯の上の端は最初の行より10上まで（73ページ「無線 LAN 接」、70ページ「ワイドグリル※」は最初の行より上に書いてある）\n      var r = dkoNames(subReg, subCodes, k ? Math.min(b.hi + 10, up - 1) : top, clean);\n      r.rows.forEach(function (y, t) {\n        nameRows.rows.push(y); nameRows.names.push(r.names[t] || '');\n        nameRows.leaf.push(!!(r.leaf && r.leaf[t])); nameRows.codeXs.push(r.codeX);\n      });\n    });\n    var names = nameRows.names;\n    /* 組み合わせの表（77ページ「防雪三点セットの組み合わせについて」）は、品番が横に何列も並び、\n       列の頭に「置台」「防雪屋根」「防雪パネル」と書いてある。左の欄の名前ではなく列の頭で呼ぶ。\n       品番が3つ以上ならぶ列が3列以上ある表だけ（2列の表は、機種ちがいの同じ品物） */\n    var cols = [];\n    codes.forEach(function (c) {\n      if (cols.some(function (x) { return Math.abs(x - c.x) < 6; })) return;\n      if (codes.filter(function (d) { return Math.abs(d.x - c.x) < 6; }).length >= 3) cols.push(c.x);\n    });\n    function stem(o) { var m = clean(o.s).trim().match(/^[A-Z]+(?:-[A-Z]+)?/); return m ? m[0] : ''; }\n    function colHead(c) {\n      if (cols.length < 3 || Math.abs(c.x - nameRows.codeXs[rowOf(c.y)]) < 20) return '';\n      /* 同じ行の品番が同じ頭（78ページ K-TD6A・K-TD8A・K-TD10A）なら、大きさ違いの同じ品物の表。\n         列の頭は太さ（「（φ6.35×φ9.52）」）なので名前にしない */\n      var mates = codes.filter(function (d) { return d !== c && Math.abs(d.y - c.y) < 2.5; });\n      if (!mates.length || mates.some(function (d) { return stem(d) === stem(c); })) return '';\n      // 列の上の端は、c から続いている品番だけで決める（77ページは上の注記の中にも同じ x の品番がある）\n      var col = codes.filter(function (d) { return Math.abs(d.x - c.x) < 6; });\n      var colTop = c.y, colBot = c.y;\n      col.slice().sort(function (a, b) { return a.y - b.y; }).forEach(function (d) { if (d.y > colTop && d.y - colTop <= 12) colTop = d.y; });\n      col.slice().sort(function (a, b) { return b.y - a.y; }).forEach(function (d) { if (d.y < colBot && colBot - d.y <= 12) colBot = d.y; });\n      if (col.filter(function (d) { return d.y <= colTop && d.y >= colBot; }).length < 3) return '';\n      var hs = reg.filter(function (o) {\n        var tt = clean(o.s).trim();\n        return o.y > colTop + 2 && o.y <= colTop + 12 && o.x >= c.x - 8 && o.x <= c.x + 32 &&\n          tt && !DKO_CODE.test(tt) && !DKO_MONEY.test(tt) && !dkoRangeFit(tt) && !/^[●―【】]/.test(tt);\n      }).sort(function (a, b) { return Math.abs(a.x - c.x) - Math.abs(b.x - c.x); });\n      if (!hs.length) return '';\n      var h = hs[0], ht = clean(h.s).replace(/\\s+/g, '');\n      // 表の題（「防雪三点セットの組み合わせについて」）を頭に付ける\n      var cap = reg.filter(function (o) { return o.y > h.y + 4 && o.y <= h.y + 20 && /について/.test(clean(o.s)); })[0];\n      var pre = '';\n      if (cap) pre = reg.filter(function (o) { return Math.abs(o.y - cap.y) < 1.5 && o.x <= cap.x + 1 && o.x > cap.x - 120; })\n        .sort(function (a, b) { return a.x - b.x; }).map(function (o) { return clean(o.s); }).join('')\n        .replace(/\\s+/g, '').replace(/の組み?合わせについて$/, '');\n      return (pre + ' ' + (/品番/.test(ht) ? '' : ht)).trim();\n    }\n    function rowOf(y) {\n      var best = 0, bd = 1e9;\n      nameRows.rows.forEach(function (ry, i) { var d = Math.abs(ry - y); if (d < bd) { bd = d; best = i; } });\n      return best;\n    }\n    // 配管の太さの表（ビルトイン形のダクト）は見出しに品番が無い。表の名前の形で付ける\n    if (!allFits.length && /ビルトイン/.test(title)) allFits = [{ type: 'ビルトイン形' }];\n    // スカイダクト（配管化粧ダクト）は、ルームエアコンのどの機種にも使う部材。\n    // ルームエアコンの別売品は、ルームエアコンの機種にしか出ない（app.js の showOptionsFor）\n    if (!allFits.length && /スカイダクト/.test(title)) allFits = [{ all: true }];\n\n    codes.forEach(function (c) {\n      var t = clean(c.s).trim(), cm = t.match(DKO_CODE);\n      var code = cm[1];\n      // 値段：同じ行の右（次の品番の手前まで）\n      var nextX = 1e9;\n      codes.forEach(function (d) { if (d !== c && Math.abs(d.y - c.y) < 2.5 && d.x > c.x && d.x < nextX) nextX = d.x; });\n      var row = reg.filter(function (o) { return Math.abs(o.y - c.y) < 2.5; });\n      var priceItem = null;\n      row.forEach(function (o) {\n        if (o.x <= c.x || o.x >= nextX) return;\n        var tt = clean(o.s);\n        if ((DKO_MONEY.test(tt) || /オープン価格/.test(tt)) && (!priceItem || o.x < priceItem.x)) priceItem = o;\n      });\n      // 値段が「108,240」「円」と2つに割れて来ることがある（76ページ K-AWS8H）\n      var splitYen = null;\n      if (!priceItem) {\n        row.forEach(function (o) {\n          if (o.x <= c.x || o.x >= nextX || !/^[\\d,]{4,}$/.test(clean(o.s).trim())) return;\n          var en = row.some(function (q) { return /^円/.test(clean(q.s).trim()) && q.x > o.x && q.x - (o.x + (o.w || 0)) < 12; });\n          if (en && (!priceItem || o.x < priceItem.x)) { priceItem = o; splitYen = clean(o.s).trim(); }\n        });\n      }\n      if (!priceItem) return;\n      var open = /オープン価格/.test(clean(priceItem.s));\n      var y = open ? 0 : Math.round(yen(splitYen || clean(priceItem.s).match(DKO_MONEY)[1]) / 1.1);\n\n      // 付く機種：●の列の見出し。●の無い表は表の中の範囲全部\n      var fits = [], mates = [];\n      if (mcols.length) {\n        /* ●は品番の行より少し上に組まれていることがある（72ページ KRC944A1 は2.6上）。3.5まで同じ行と見る。\n           それでも無ければ、すぐ上の「品番の無い行」の●（74ページ「上下吹出 KPW937F4 ＋ KPW081A41（アタッチメント）」は\n           ●が上の行にある） */\n        var dots = reg.filter(function (o) { return /^●/.test(clean(o.s).trim()) && Math.abs(o.y - c.y) < 3.5 && o.x > priceItem.x; });\n        if (!dots.length) {\n          var up = reg.filter(function (o) { return /^●/.test(clean(o.s).trim()) && o.y - c.y >= 3.5 && o.y - c.y <= 7.5 && o.x > priceItem.x; });\n          var upHasCode = up.length && codes.some(function (d) { return d !== c && Math.abs(d.y - up[0].y) < 2.5; });\n          if (!upHasCode) dots = up;\n        }\n        dots.forEach(function (o) {\n          var cx = o.x + 3, best = null, bd = 1e9;\n          mcols.forEach(function (k) { var d = Math.abs(k.x - cx); if (d < bd) { bd = d; best = k; } });\n          if (best && bd < 20) best.fits.forEach(function (f) { if (f) fits.push(f); });\n        });\n        /* ●の無いセットの表（77ページ「防雪三点セット」の防雪パネル K-APC6HL）。\n           同じ行のほかの品番（K-AH63HL・K-KP6H）の付く機種を、あとで借りる */\n        if (!fits.length) {\n          codes.forEach(function (d) { if (d !== c && Math.abs(d.y - c.y) < 2.5) mates.push(clean(d.s).trim().match(DKO_CODE)[1]); });\n          if (!mates.length) return;\n        }\n      } else {\n        fits = allFits.slice();\n      }\n      if (!fits.length && !mates.length) return;\n\n      // 品名：同じ行で品番より左の文字。短い（「3m」「枠付」）ときは、左の欄のいちばん近い名前を頭に付ける\n      var head = colHead(c);\n      var name = head || names[rowOf(c.y)] || '';\n      /* 名前の欄より左にある品番（78ページ「バンドホルダー K-TH7A 99 円」は TM シリーズの表の左の小さな表）は、\n         その行の左の文字で呼ぶ。名前の欄の行の名前（「ひねりエルボ」）を付けていた */\n      if (!head && c.x < nameRows.codeXs[rowOf(c.y)] - 20) {\n        var leftBits = reg.filter(function (o) {\n          var tt = clean(o.s).trim();\n          return o.x < c.x - 2 && o.x > c.x - 160 && Math.abs(o.y - c.y) <= 4 && tt &&\n            !DKO_CODE.test(tt) && !DKO_MONEY.test(tt) && !/^注|^[●―□★※]+$|^[\\d,]+ ?個?$|。/.test(tt);\n        }).sort(function (a, b) { return b.y - a.y || a.x - b.x; });\n        var lt0 = leftBits.map(function (o) { return clean(o.s).trim(); }).join(' ')\n          .replace(/([ぁ-んァ-ヶー一-龥（）])\\s+(?=[ぁ-んァ-ヶー一-龥（）])/g, '$1').replace(/\\s+/g, ' ').trim();\n        if (lt0) name = lt0;\n      }\n      if (!name) name = title.replace(/用?別売品.*$/, '') + '用 別売品';\n      if (open) name += '（オープン価格）';\n      if (cm[2]) name += '　色 ' + cm[2].replace(/[()]/g, ' ').trim().split(/\\s+/).join('・');\n      out.push({ page: page, code: code, name: name, y: y, fits: fits, mates: mates, weak: !!head,\n                 leaf: !head && !!(nameRows.leaf && nameRows.leaf[rowOf(c.y)]) });\n    });\n  }\n\n  /** ●の無いセットの表の品目に、同じ行のほかの品番の付く機種を貸す。借りられなかったものは入れない */\n  function dkRoomOptFinish(list) {\n    var byCode = {}, strong = {};\n    /* 同じ品番が何ページにもある（KRP413BB1S は70・71・73ページ）。\n       品番の行の高さに、その品物だけの名前が書いてある表（73ページ「遠隔制御用Ｐ板セット」）の名前がいちばん確か。\n       そういう名前がいくつかあれば短いほう（70ページの「ドレンポンプキット エアコンと離して」より「変換コネクタ」）。\n       無ければ最初に読んだ名前。「いちばん短い名前」だけで選ぶと、途中で切れた名前（「ドレンパイプ用」）を選んだ */\n    // そういう名前が表ごとに違うときは、いちばん多い名前（同じ数なら短いほう）\n    var votes = {};\n    function n0(t) { return t.replace(/\\s+/g, '').length; }\n    list.forEach(function (o) {\n      if (o.weak || !o.name) return;\n      if (!strong[o.code]) strong[o.code] = o.name;\n      if (!o.leaf || n0(o.name) < 4) return;\n      var v = votes[o.code] = votes[o.code] || {};\n      v[o.name] = (v[o.name] || 0) + 1;\n    });\n    Object.keys(votes).forEach(function (k) {\n      var best = null;\n      Object.keys(votes[k]).forEach(function (nm) {\n        var c = votes[k][nm];\n        if (!best || c > votes[k][best] || (c === votes[k][best] && n0(nm) < n0(best))) best = nm;\n      });\n      strong[k] = best;\n    });\n    list.forEach(function (o) {\n      if (!o.fits.length) return;\n      var v = byCode[o.code] = byCode[o.code] || [];\n      o.fits.forEach(function (f) { v.push(f); });\n    });\n    var ok = [];\n    list.forEach(function (o) {\n      if (!o.fits.length) {\n        (o.mates || []).forEach(function (m) { (byCode[m] || []).forEach(function (f) { o.fits.push(f); }); });\n      }\n      if (o.fits.length) ok.push({ page: o.page, code: o.code, name: strong[o.code] || o.name, y: o.y, fits: o.fits });\n    });\n    return optResult(ok, 'ダイキン', 'ルームエアコン（住宅設備用） 別売品');\n  }\n\n  function dkRoomFinish(sets) {\n    var rows = [], seen = {}, pages = {};\n    sets.forEach(function (x) {\n      pages[x.page] = 1;\n      // パネル違いは別の1台（同じ品番でも合計価格が違う）\n      var key = x.m + '｜' + x.pm;\n      if (seen[key]) return;\n      seen[key] = 1;\n      // 「馬力」の手順には、ルームエアコンでは能力の文字を入れる（画面はそのまま出す）\n      var cap = x.cap || (x.kw.toFixed(1) + 'kW（おもに' + x.tat + '畳）');\n      rows.push({\n        m: x.m, hp: cap, y: x.y, u: String(x.page),\n        s: x.s, i: x.i, ab: cap, pw: x.pw,\n        rc: x.rc != null ? x.rc : 'ワイヤレス', tp: x.tp || 'シングル', opt: x.opt,\n        om: x.om, im: x.im, pm: x.pm, rm: ''\n      });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: 'ダイキン',\n        brand: 'ルームエアコン（住宅設備用）',\n        source: '住宅設備用カタログ（公開デジタルカタログ）',\n        note: '希望小売価格・税抜。配管/据付工事費は含まず。Eシリーズはオープン価格（値段0）。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['RXシリーズ', 'AXシリーズ', 'SXシリーズ', 'GXシリーズ', 'CXシリーズ', 'DXシリーズ', 'HXシリーズ', 'KXシリーズ', 'Eシリーズ',\n                      'CRシリーズ', 'CDシリーズ', 'Cシリーズ', 'ダブルフロー', 'VRシリーズ', 'VDシリーズ', 'Vシリーズ',\n                      '壁埋込形', 'アメニティビルトイン', 'フリービルトイン',\n                      'マルチパック', 'システムマルチ（室外機）', 'ココタス（室外機）', 'マルチ用室内機'],\n        typeOrder: ['シングル', 'マルチパック（2室）', '2室用', '3室用', '4室用', '5室用', 'マルチ用室内機'],\n        urlBase: 'https://ec.daikinaircon.com/ecatalog/DKCA001/index.html#'\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     パナソニック ルームエアコン（住宅設備エアコン総合カタログ 26夏号・128ページ）\n\n     1台ぶんは列のかたまり（1行に4〜5台）：\n       「冷暖房時おもに 6 畳用」「単相 100V」／室内機の形名「CS-226DHX」／「（室外）CU-226DHX」／セット品番\n       ／「本体希望小売価格 484,000 円（税抜440,000円）」／その下に「室内：」「室外：」の内訳\n     天井・壁ビルトインは、その下に「合計希望小売価格（別販化粧グリル…を使用した場合）」→ 合計で入れる。\n     値段は税込で書いてあり、（ ）内が税抜 → 税抜を入れる。F・TX・UY・Y・三相モデルはオープン価格（値段0）。\n     マルチ：室内機（CS-ME・MJ・MB・M…）は室外機の形名が無い。室外機（CU-M450D2・CU-3M680D2…）は別の値段。\n     耐塩害仕様（118ページ）は表：形名の末尾E（CS-226DHE／CS-286DH2E）。中身は元の機種（CS-226DHX／CS-286DHX2）を写す。\n     2〜6ページの一覧表（室外機の形名が無い）は読まない（物差しに使う）\n     -------------------------------------------------------------------- */\n  var PANA_CS = /^CS-([A-Z]{0,2})(\\d{2})(\\d)([A-Z]{1,3})(\\d?)(E?)(?=$|-)/;\n  function panaRoomKind(code) {\n    var m = code.match(PANA_CS);\n    if (!m) return null;\n    var pre = m[1], lt = m[4], dg = m[5];\n    var salt = !!m[6] || (lt.length === 3 && /^D[A-Z]E$/.test(lt));\n    if (salt && !m[6]) lt = lt.slice(0, 2);\n    var multi = /^M/.test(pre);\n    var i = '壁掛形';\n    if (/^(B|UB|MB)$/.test(pre)) {\n      if (lt === 'DC') i = '天井ビルトイン形（1方向）';\n      else if (lt === 'DW' || lt === 'CW') i = '天井ビルトイン形（2方向）';\n      else if (lt === 'CK' || lt === 'DK') i = '壁ビルトイン形';\n      else if (lt === 'CA' || lt === 'DA') i = 'フリービルトイン形';\n    }\n    if (/Y$/.test(lt)) i = '床置形';\n    var s;\n    if (multi) s = 'マルチ用室内機';\n    else if (pre === 'TX' || pre === 'K' || pre === 'UB') s = pre + 'シリーズ';\n    else if (pre === 'B') s = ({ DC: 'BC', DW: 'BW', CW: 'BW', CK: 'BK', CA: 'BA' }[lt] || 'B' + lt) + 'シリーズ';\n    else if (lt === 'DU' && dg === '3') s = '三相電源対応モデル';\n    else s = ({ DFL: 'F', CY: 'Y' }[lt] || lt.replace(/^D/, '')) + 'シリーズ';\n    return { kw: Number(m[2]) / 10, pre: pre, lt: lt, dg: dg, salt: salt, multi: multi, i: i, s: s,\n             cold: pre === 'TX' || pre === 'K' || pre === 'UB' || lt === 'DUX' || lt === 'DUY' };\n  }\n\n  /** 合計の注「（別販化粧グリルCZ-BT10-W・WS・Tを使用した場合）」「（CZ-BKG22A、CZ-BKF2を使用した場合）」から品番を出す。\n      main＝値段に入っているもの（「、」で分かれた数だけ）、all＝同じ値段で選べる色ちがい（WS・T）も入れたもの */\n  function panaUsedCodes(t) {\n    var main = [], all = [];\n    (String(t).match(/CZ-[A-Z0-9]+(?:-[A-Z]+)?(?:[・･][A-Z]+)*/g) || []).forEach(function (s) {\n      var parts = s.split(/[・･]/), head = parts[0], base = head.replace(/-[A-Z]+$/, '-');\n      main.push(head);\n      all.push(head);\n      parts.slice(1).forEach(function (p) { all.push(/-$/.test(base) ? base + p : head + p); });\n    });\n    return { main: main, all: all };\n  }\n\n  function panaRoomReadPage(items, page) {\n    function clean(t) { return String(t).replace(/[\\u0000-\\u001f]/g, ' ').replace(/\\s+/g, ' ').trim(); }\n    function nos(t) { return String(t).replace(/\\s+/g, ''); }\n    var its = [], seenIt = {};\n    items.forEach(function (o) {\n      var t = clean(o.s);\n      if (!t) return;\n      var key = t + '|' + o.x.toFixed(1) + '|' + o.y.toFixed(1);\n      if (seenIt[key]) return;\n      seenIt[key] = 1;\n      its.push({ s: t, x: o.x, y: o.y, w: o.w || 0 });\n    });\n    var all = nos(its.map(function (o) { return o.s; }).join(' '));\n    if (!/税抜|オープン価格/.test(all)) return [];\n    function endX(o) { return o.x + (o.w || nos(o.s).length * 5); }\n    // 割れた形名をつなぐ（「CS-256D」「EX」、「CS-406D」「EX」「2」、「CU-」「B404DC2」）\n    its.forEach(function (o) {\n      var t = nos(o.s);\n      if (!/^(CS|CU)-([A-Z]{0,2}\\d{3}[A-Z]?)?$/.test(t)) return;\n      var px = endX(o);\n      its.filter(function (q) { return q !== o && q.s && Math.abs(q.y - o.y) < 1.5 && q.x > o.x && q.x - o.x < 90; })\n        .sort(function (a, b) { return a.x - b.x; })\n        .forEach(function (q) {\n          if (px === null) return;\n          var qt = nos(q.s);\n          if (q.x - px > 10 || !/^([A-Z]{1,3}\\d?|\\d|[A-Z0-9]{4,})$/.test(qt)) { px = null; return; }\n          o.s = nos(o.s) + qt; q.s = ''; px = endX(q);\n        });\n    });\n    its = its.filter(function (o) { return o.s; });\n    var out = [];\n\n    // 値段（税抜・合計・オープン価格）。「室内：」「室外：」の内訳と、フィルターなどの「希望小売価格」は採らない\n    var prices = [];\n    its.forEach(function (o) {\n      var t = nos(o.s), m;\n      if (/税抜/.test(t)) {\n        if (!/円/.test(t)) {\n          var nx = its.filter(function (q) { return Math.abs(q.y - o.y) < 1.5 && q.x > o.x && q.x - o.x < 40; }).sort(function (a, b) { return a.x - b.x; })[0];\n          if (nx) t += nos(nx.s);\n        }\n        m = t.match(/税抜([\\d,]+)円/);\n        if (!m) return;\n        var left = nos(its.filter(function (q) { return Math.abs(q.y - o.y) < 1.5 && q.x < o.x && o.x - q.x < 70; }).map(function (q) { return q.s; }).join(''));\n        if (/室内|室外/.test(left)) return;\n        if (/希望小売価格/.test(left) && !/本体|合計/.test(left)) return;\n        var tot = its.some(function (q) { return /^合計/.test(nos(q.s)) && q.y - o.y >= -1.5 && q.y - o.y <= 20 && q.x <= o.x && o.x - q.x <= 95; });   // 76ページのマルチは見出しが18上\n        prices.push({ x: o.x, y: o.y, v: yen(m[1]), tot: tot });\n      } else if (/^オープン価格/.test(t)) {\n        prices.push({ x: o.x, y: o.y, v: 0, open: true });\n      }\n    });\n\n    // 形名（室内機と、マルチの室外機）。46・71ページの「●室内機」「●室外機」のプラン例は読まない\n    var codes = [];\n    its.forEach(function (o) {\n      var t = nos(o.s), m = t.match(PANA_CS), mu = !m && t.match(/^CU-(\\d?)M(E?)(\\d{2})\\d[A-Z]\\d/);\n      if (!m && !mu) return;\n      if (its.some(function (q) { return /^●室(内|外)機/.test(nos(q.s)) && Math.abs(q.y - o.y) <= 3 && q.x < o.x && o.x - q.x < 40; })) return;\n      if (mu && its.some(function (q) { return /（室外）/.test(q.s) && Math.abs(q.y - o.y) < 1.5 && q.x < o.x && o.x - q.x < 25; })) return;\n      codes.push({ o: o, code: m ? m[0] : t.match(/^CU-[A-Z0-9]+/)[0], x: o.x, y: o.y, mu: mu });\n    });\n    // 近くの字をどの形名のものにするか（下 dy は形名から見て下に正）\n    function owner(a, dyMin, dyMax, dxMax) {\n      var best = null;\n      codes.forEach(function (c) {\n        var dy = c.y - a.y, dx = a.x - c.x;\n        if (dy < dyMin || dy > dyMax || dx < -15 || dx > dxMax) return;\n        var d = Math.abs(dy) + 0.5 * Math.abs(dx);\n        if (!best || d < best.d) best = { d: d, c: c };\n      });\n      return best ? best.c : null;\n    }\n    prices.forEach(function (a) { var c = owner(a, -2, 80, 150); if (c) (c.ps = c.ps || []).push(a); });\n    // 室外機の形名（「（室外）」「CU-226DHX」、耐塩害の表は同じ行の右）\n    its.forEach(function (o) {\n      var t = nos(o.s), m = t.match(/^CU-[A-Z0-9]{5,}/);\n      if (!m) return;\n      var lab = its.some(function (q) { return /（室外）/.test(q.s) && Math.abs(q.y - o.y) < 1.5 && q.x < o.x && o.x - q.x < 25; });\n      var c = owner({ x: o.x, y: o.y }, -2, 25, 70);\n      if (c && (lab || c.y === o.y || Math.abs(c.y - o.y) < 1.5)) c.om = c.om || m[0];\n    });\n    // 電源（「単相」「100V」、「単相 200V」）\n    its.forEach(function (o) {\n      var t = nos(o.s);\n      if (!/^(単相|三相)/.test(t)) return;\n      if (!/V/.test(t)) {\n        var nx = its.filter(function (q) { return Math.abs(q.y - o.y) < 1.5 && q.x > o.x && q.x - o.x < 25; }).sort(function (a, b) { return a.x - b.x; })[0];\n        if (nx) t += nos(nx.s);\n      }\n      var m = t.match(/^(単相|三相)(100|200)V/);\n      if (!m) return;\n      var c = owner({ x: o.x, y: o.y }, -15, 12, 110);\n      if (c && !c.pw) c.pw = m[1] + m[2] + 'V';\n    });\n    // 畳数（「冷暖房時おもに」「6」「畳用」）\n    its.forEach(function (h) {\n      if (nos(h.s) !== '畳用') return;\n      var dg = its.filter(function (q) { return /^\\d{1,2}$/.test(nos(q.s)) && Math.abs(q.y - h.y) <= 8 && q.x < h.x && h.x - q.x < 45; })\n        .sort(function (a, b) { return a.x - b.x; }).map(function (q) { return nos(q.s); }).join('');\n      if (!dg) return;\n      var c = owner({ x: h.x - 55, y: h.y }, -30, 2, 110);\n      if (c && !c.tat) c.tat = Number(dg);\n    });\n\n    // 合計に入っている別売品（「（別販化粧グリルCZ-BT10-W・WS・Tを使用した場合）」）→ 付いてくるもの（pm）。\n    // 化粧グリルを別に選んだとき、二重取りにせず入れ替えにするため（2026-09-12）\n    its.forEach(function (o) {\n      var t = nos(o.s);\n      if (!/^（(別販|CZ-)/.test(t)) return;\n      var rest = its.filter(function (q) { return q !== o && Math.abs(q.y - o.y) < 1.5 && q.x > o.x && q.x - o.x < 120; })\n        .sort(function (a, b) { return a.x - b.x; });\n      for (var i = 0; i < rest.length && !/）/.test(t); i++) t += nos(rest[i].s);\n      if (!/を使用した場合/.test(t)) return;\n      var c = owner(o, -2, 80, 150);\n      if (c && !c.pm) c.pm = panaUsedCodes(t).main.join('・');\n    });\n\n    var saltPage = /耐塩害/.test(all), threePhase = /室外三相/.test(all);\n    codes.forEach(function (c) {\n      var ps = c.ps || [];\n      function nearest(list) { return list.sort(function (a, b) { return (c.y - a.y) - (c.y - b.y); })[0]; }\n      var tot = nearest(ps.filter(function (p) { return p.tot; }));\n      var body = nearest(ps.filter(function (p) { return !p.tot && !p.open; }));\n      var open = nearest(ps.filter(function (p) { return p.open; }));\n      var pr = tot || body || open;\n      if (!pr) return;\n      if (c.mu) {\n        var n = Number(c.mu[1] || 2);\n        out.push({ page: page, m: c.code, kw: Number(c.mu[3]) / 10, s: c.mu[2] ? 'MEシリーズ（室外機）' : 'フリーマルチ（室外機）',\n                   i: 'マルチ室外機', pw: '単相200V', tp: n + '室用', y: pr.v, open: !!pr.open, tot: false, om: c.code, im: '' });\n        return;\n      }\n      var k = panaRoomKind(c.code);\n      if (!k) return;\n      if (k.salt && !saltPage) return;\n      if (!k.multi && !c.om) return;          // 室外機の形名が無い＝一覧表（2〜6ページ）\n      out.push({ page: page, m: c.code, kw: k.kw, s: k.s, i: k.i, salt: k.salt, multi: k.multi, cold: k.cold,\n                 pw: k.multi ? '室外機から' : (c.pw || (threePhase ? '三相200V' : '')), tat: c.tat || 0,\n                 tp: k.multi ? 'マルチ用室内機' : 'シングル', y: pr.v, open: !!pr.open, tot: !!pr.tot, om: c.om || '', im: c.code,\n                 pm: pr.tot ? (c.pm || '') : '' });\n    });\n    return out;\n  }\n\n  function panaRoomFinish(list) {\n    // 同じ形名が何か所にもあるときは、合計（化粧グリル込み）→ 室外機の形名つき → あとのページ の順に採る\n    var by = {}, order = [];\n    list.forEach(function (x) {\n      var v = by[x.m];\n      if (!v) { by[x.m] = x; order.push(x.m); return; }\n      // 合計は、値段に入っている別売品が書いてあるときだけ採る。6ページの一覧表のフリービルトイン（マルチ用 MB…DA2）は\n      // 「合計」の欄にあっても本体だけの値段で、「化粧グリル等込み」と出てしまっていた（2026-09-12）\n      var score = function (e) { return (e.tot && e.pm ? 4 : 0) + (e.om ? 2 : 0) + (e.tat ? 1 : 0); };\n      if (score(x) >= score(v)) by[x.m] = x;\n    });\n    var rows = [], pages = {};\n    order.forEach(function (m) {\n      var x = by[m];\n      if (x.salt) {\n        // 元の機種：CS-226DHE → CS-226DH…（末尾の数字も同じ）\n        var k = PANA_CS.exec(m) ? m.match(PANA_CS) : null;\n        var sk = panaRoomKind(m);\n        var re = new RegExp('^CS-' + k[1] + k[2] + k[3] + sk.lt + '[A-Z]*' + (sk.dg || '') + '$');\n        var b = order.map(function (c) { return by[c]; }).filter(function (e) { return !e.salt && re.test(e.m); })[0];\n        if (!b) return;\n        x = { page: x.page, m: m, kw: b.kw, s: b.s, i: b.i, cold: b.cold, pw: b.pw, tat: b.tat, tp: b.tp, y: x.y, open: x.open,\n              tot: false, om: x.om, im: m, saltOf: b.m };\n      }\n      pages[x.page] = 1;\n      var cap = x.kw.toFixed(1) + 'kW' + (x.tat ? '（おもに' + x.tat + '畳）' : '');\n      var opt = [x.saltOf ? '耐塩害仕様（受注生産品）' : '', x.open ? 'オープン価格' : '', x.tot ? '化粧グリル等込み（合計希望小売価格）' : '',\n                 x.cold ? '寒冷地向け' : ''].filter(Boolean).join('／');\n      rows.push({\n        m: m, hp: x.i === 'マルチ室外機' ? x.kw.toFixed(1) + 'kW' : cap, y: x.y, u: String(x.page),\n        s: x.s, i: x.i, ab: cap, pw: x.pw || '単相200V', rc: x.i === 'マルチ室外機' ? '' : 'ワイヤレス', tp: x.tp,\n        opt: opt, om: x.om, im: x.im, pm: x.pm || '', rm: ''\n      });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: 'パナソニック',\n        brand: 'ルームエアコン（住宅設備用）',\n        source: '住宅設備エアコン総合カタログ 26夏号（公開Webカタログ）',\n        note: '希望小売価格（事業者向け・積算見積価格）の税抜。配管/据付工事費は含まず。F・TX・UY・Y・三相モデルはオープン価格（値段0）。天井・壁ビルトインは別売の化粧グリル等を含む合計。耐塩害仕様は形名の末尾E。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['HXシリーズ', 'EXシリーズ', 'GXシリーズ', 'Jシリーズ', 'Fシリーズ', 'ELシリーズ', 'Nシリーズ', 'Cシリーズ',\n                      'UXシリーズ', 'TXシリーズ', 'Kシリーズ', 'UBシリーズ', 'UYシリーズ', 'LVシリーズ', '三相電源対応モデル', 'Yシリーズ',\n                      'BCシリーズ', 'BWシリーズ', 'BKシリーズ', 'BAシリーズ', 'MEシリーズ（室外機）', 'フリーマルチ（室外機）', 'マルチ用室内機'],\n        typeOrder: ['シングル', '2室用', '3室用', '4室用', 'マルチ用室内機']\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     パナソニック ルームエアコンの別売品（住宅設備エアコン総合カタログ 26夏号・紙面114〜118ページ＝PDFの115〜119）\n\n     114：フィルター部品 適用一覧。行＝室内機品番（「□□6DHX（2）」）、列＝フィルターの種類。\n          マスが縦につながっていて、1つの品番が何行ぶんにも付く。品番の字はつながったマスのまん中にある\n          → 行の分け方を、マスの字のまん中と受け持つ行のまん中がいちばん合うように決める（splitRows）。\n          紙面の罫線で確かめた：K の行だけ背が高く、カビストッパーは TX には付かず K だけに付く（2026-09-12）\n     115：配管化粧カバー「スッキリダクト」。品物ごとのカード（＜題＞・＜60型＞・品番・値段）。品番の■は色の記号\n     116：室外機アクセサリー 適用一覧表。上の表＝行がシリーズ・列が室外機の大きさ・マスに能力（「（100V）2.2～4.0kW」）。\n          下の置台・金具は列ごとの●。日よけ屋根と吹出グリルは列ごとに品番。転倒防止金具は注＊4の文の中に品番と値段\n     118：リモコンホルダー（「適用リモコン」の文）・室外機背面金網（116と同じ形の表）・かんたんリモコン・\n          RACローカルアダプター・有線式集中コントロール\n     値段は税込で書いてあり、（ ）内が税抜 → 税抜を入れる\n     -------------------------------------------------------------------- */\n  /** 行の見出し（「HX」「BK・BA」「J・F」「三相電源」「2室用マルチ」）を、機種データのシリーズの呼び方にする */\n  function panaRowSeries(t) {\n    t = String(t || '').replace(/\\s+/g, '');\n    if (/2室用マルチ/.test(t)) return ['MEシリーズ（室外機）'];\n    if (/フリーマルチ/.test(t)) return ['フリーマルチ（室外機）'];\n    if (/三相電源/.test(t)) return ['三相電源対応モデル'];\n    return (t.match(/[A-Z]{1,2}/g) || []).map(function (s) { return s + 'シリーズ'; });\n  }\n\n  /** マスの字（「（100V）2.2～4.0kW」「2.2～4.0kW/2.8・4.0kW」「2.8～5.6kW＊2026年モデルのみ」）を当て方にする。\n      「/」で分かれていたら行の見出しの順（BK・BA）。年式は形名の3けため（CS-286DY2＝2026年、CS-289CY2＝2019年） */\n  function panaKwFits(series, t) {\n    t = String(t || '').replace(/\\s+/g, '');\n    var v = t.match(/[（(](100|200)V[）)]/), yr = t.match(/(20\\d\\d)年モデル/);\n    var parts = t.replace(/[（(]\\d+V[）)]/g, '').replace(/＊.*$/, '').split('/');\n    var out = [];\n    parts.forEach(function (p, i) {\n      p = p.replace(/kW/gi, '');\n      if (!/\\d/.test(p)) return;\n      var ss = parts.length > 1 && series.length === parts.length ? [series[i]] : series;\n      ss.forEach(function (s) {\n        var f = { s: s };\n        if (/[～~〜]/.test(p)) f.kr = p.split(/[～~〜]/).map(Number).slice(0, 2);\n        else f.k = p.split('・').filter(Boolean).map(Number);\n        if (v) f.v = v[1] + 'V';\n        if (yr) f.re = '^CS-[A-Z]*\\\\d{2}' + (Number(yr[1]) % 10);\n        out.push({ rs: f });\n      });\n    });\n    return out;\n  }\n\n  /** 縦につながったマスの分け方。行（rows＝上から順の y）を、マス（cells＝上から順の字のまん中）の数だけ\n      上から切れ目なく分ける。どのマスも、受け持つ行のまん中にいちばん近くなる分け方（DP）。返すのはマスごとの行の番号 */\n  function splitRows(rows, cells) {\n    var n = rows.length, m = cells.length;\n    if (!m || m > n) return null;\n    var pre = [0];\n    rows.forEach(function (y, i) { pre.push(pre[i] + y); });\n    var D = [], B = [], c, i, j;\n    for (c = 0; c <= m; c++) { D.push([]); B.push([]); for (j = 0; j <= n; j++) { D[c].push(Infinity); B[c].push(-1); } }\n    D[0][0] = 0;\n    for (c = 1; c <= m; c++) {\n      for (j = c; j <= n; j++) {\n        for (i = c - 1; i < j; i++) {\n          var v = D[c - 1][i] + Math.abs((pre[j] - pre[i]) / (j - i) - cells[c - 1]);\n          if (v < D[c][j]) { D[c][j] = v; B[c][j] = i; }\n        }\n      }\n    }\n    var groups = [];\n    for (c = m, j = n; c >= 1; c--) {\n      var g = [];\n      for (i = B[c][j]; i < j; i++) g.push(i);\n      groups.unshift(g);\n      j = B[c][j];\n    }\n    return groups;\n  }\n\n  function panaRoomOptReadPage(items, page) {\n    function clean(t) { return String(t).replace(/[\\u0000-\\u001f]/g, ' ').replace(/\\s+/g, ' ').trim(); }\n    function z2h(t) { return String(t).replace(/[０-９Ａ-Ｚａ-ｚ]/g, function (c) { return String.fromCharCode(c.charCodeAt(0) - 0xFEE0); }); }\n    var its = [], seenIt = {};\n    items.forEach(function (o) {\n      var s = clean(o.s);\n      if (!s) return;\n      var key = s + '|' + o.x.toFixed(1) + '|' + o.y.toFixed(1);\n      if (seenIt[key]) return;\n      seenIt[key] = 1;\n      // t＝すき間を詰めた字、s＝すき間を残した字（「CZ-BWG9 37,400」のように品番と値段が1つの字で来る）\n      its.push({ t: z2h(s.replace(/\\s+/g, '')), s: z2h(s), x: o.x, y: o.y, w: o.w || 0 });\n    });\n    var all = its.map(function (o) { return o.t; }).join('');\n    if (!/希望小売価格/.test(all)) return [];\n    var out = [], done = false;\n    // 行にまとめる。行の高さは入った字の平均で見る（最初の字だけで見ると、1.5ずれた字が上の行に取られ、\n    // 「＜コーナーカバーロングＰ」672.29 と「タイプ＞」672.16 が別の行に割れた）\n    function mkLines(list) {\n      var L = [];\n      list.slice().sort(function (a, b) { return b.y - a.y || a.x - b.x; }).forEach(function (o) {\n        for (var i = 0; i < L.length; i++) {\n          if (Math.abs(L[i].y - o.y) <= 1.5) { L[i].its.push(o); L[i].sy += o.y; L[i].y = L[i].sy / L[i].its.length; return; }\n        }\n        L.push({ y: o.y, sy: o.y, its: [o] });\n      });\n      L.forEach(function (l) { l.its.sort(function (a, b) { return a.x - b.x; }); l.t = l.its.map(function (o) { return o.t; }).join(''); });\n      return L;\n    }\n    var lines = mkLines(its);\n    function taxOf(o) { var m = o.t.match(/税抜([\\d,]+)円/); return m ? yen(m[1]) : 0; }\n    function sameLine(o, f) {\n      return its.filter(function (q) { return q !== o && Math.abs(q.y - o.y) <= 1.5 && f(q); }).sort(function (a, b) { return a.x - b.x; });\n    }\n    /** 品番 c の下（dy は上から下に正。同じ行も入れる）で、x が c から dxMin〜dxMax にある税抜の値段。いちばん近い行のもの */\n    function priceBelow(c, dyMax, dxMin, dxMax) {\n      var best = null;\n      its.forEach(function (o) {\n        var v = taxOf(o), dy = c.y - o.y, dx = o.x - c.x;\n        if (!v || dy < -1.5 || dy > dyMax || dx < dxMin || dx > dxMax) return;\n        if (!best || dy < best.dy - 1.5 || (Math.abs(dy - best.dy) <= 1.5 && dx < best.dx)) best = { v: v, dy: dy, dx: dx, o: o };\n      });\n      return best;\n    }\n    function nearestCol(cols, x) {\n      var ci = 0;\n      cols.forEach(function (c, i) { if (Math.abs(c - x) < Math.abs(cols[ci] - x)) ci = i; });\n      return ci;\n    }\n    function avg(a) { return a.reduce(function (s, v) { return s + v; }, 0) / a.length; }\n\n    /** シリーズ×能力の表（116・118ページ）。cols＝列の x。見出しは列より左、マスは列の中。行は yTop と yBot の間。\n        返すのは列ごとの当て方の並び */\n    function kwTable(cols, yTop, yBot) {\n      var x0 = cols[0] - 20, x9 = cols[cols.length - 1] + 45, rows = [];\n      mkLines(its.filter(function (o) { return o.y < yTop && o.y > yBot && o.x < x0 && o.x > x0 - 110; })).forEach(function (L) {\n        var t = L.its.filter(function (o) { return /^[A-Z]{1,2}$/.test(o.t) || /三相電源|2室用マルチ|フリーマルチ/.test(o.t); })\n          .map(function (o) { return o.t; }).join('・');\n        var ss = t ? panaRowSeries(t) : [];\n        if (ss.length) rows.push({ y: L.y, ss: ss });\n      });\n      var colFits = cols.map(function () { return []; });\n      mkLines(its.filter(function (o) { return o.y < yTop && o.y > yBot && o.x >= x0 && o.x < x9; })).forEach(function (L) {\n        var segs = [], cur = null, prev = '';\n        L.its.forEach(function (o) {\n          var volt = /^[（(]\\d+V[）)]$/.test(o.t), num = /^\\d/.test(o.t);\n          // 「（100V）」「2.2～4.0」「kW」「（200V）」…。電源か、前が「・」「～」「/」「（…V）」でない数字から次のマス\n          if (!cur || volt || (num && !/[・～~〜\\/]$/.test(prev) && !/V[）)]$/.test(prev)) || o.x - cur.x2 > 40) {\n            cur = { t: '', x: null, x2: o.x };\n            segs.push(cur);\n          }\n          cur.t += o.t;\n          cur.x2 = o.x + (o.w || o.t.length * 4);\n          if (cur.x === null && /\\d\\.\\d/.test(o.t)) cur.x = o.x;\n          prev = o.t;\n        });\n        segs.forEach(function (g) {\n          if (g.x === null) return;\n          var r = rows.filter(function (q) { return Math.abs(q.y - L.y) <= 6; })\n            .sort(function (a, b) { return Math.abs(a.y - L.y) - Math.abs(b.y - L.y); })[0];\n          if (!r) return;\n          var ci = nearestCol(cols, g.x);\n          colFits[ci] = colFits[ci].concat(panaKwFits(r.ss, g.t));\n        });\n      });\n      return colFits;\n    }\n\n    /* ---- 114ページ：フィルター部品 適用一覧 ---- */\n    var fh = its.filter(function (o) { return /^エアコン機種名/.test(o.t); })[0];\n    if (fh && /フィルター部品適用一覧/.test(all)) {\n      done = true;\n      var fcols = its.filter(function (o) { return Math.abs(o.y - fh.y) <= 8 && /^(空気清浄|除菌|脱臭|カビス)/.test(o.t); })\n        .sort(function (a, b) { return a.x - b.x; }).map(function (o) { return o.x; });\n      var fTop = fh.y - 4;\n      var fBot = Math.max.apply(null, [0].concat(its.filter(function (o) {\n        return /^★カビストッパー|^フィルター部品$/.test(o.t) && o.y < fh.y;\n      }).map(function (o) { return o.y; })));\n      if (fcols.length >= 2) {\n        var fLeft = fcols[0] - (fcols[1] - fcols[0]) / 2;\n        var fcol = function (x) {\n          if (x < fLeft) return -1;\n          for (var i = 0; i < fcols.length - 1; i++) if (x < (fcols[i] + fcols[i + 1]) / 2) return i;\n          return fcols.length - 1;\n        };\n        // 行＝室内機品番（「□□6DHX」「（2）」、「MB160DC2」）\n        var frows = its.filter(function (o) { return o.x < fLeft && o.y < fTop && o.y > fBot && (/□/.test(o.t) || /^MB\\d{3}DC2$/.test(o.t)); })\n          .sort(function (a, b) { return b.y - a.y; })\n          .map(function (o) {\n            var p2 = sameLine(o, function (q) { return /^[（(]2[）)]$/.test(q.t) && q.x > o.x && q.x - o.x < 45; }).length > 0;\n            var k = panaRoomKind('CS-' + o.t.replace('□□', '22'));\n            return { y: o.y, pat: o.t, p2: p2, s: k ? k.s : '', multi: !!(k && k.multi) };\n          })\n          .filter(function (r) { return r.s; });\n        // シリーズが1行だけならシリーズで当てる（耐塩害の CS-226DHE も同じシリーズなので当たる）。\n        // 同じシリーズが2行（BW の 6DW2 と在庫限定の 9CW2、Y の 6DY2 と 9CY2）と、マルチ用室内機は品番の形で当てる\n        frows.forEach(function (r) {\n          var n = frows.filter(function (q) { return q.s === r.s; }).length;\n          r.fit = { rs: { s: r.s } };\n          if (r.multi || n > 1) r.fit.rs.re = '^CS-' + r.pat.replace('□□', '\\\\d{2}') + (r.p2 ? '2?' : '') + '$';\n        });\n        // 品名は下の「フィルター部品」の欄から（〈空気清浄フィルター〉 CZ-SAF16、〈わさび防カビパック〉（カビストッパー） CZ-SW5AK）\n        var fname = {};\n        its.forEach(function (c) {\n          if (c.y >= fBot || !/^CZ-[A-Z0-9]+$/.test(c.t)) return;\n          var lab = its.filter(function (o) { return /^〈/.test(o.t) && o.y > c.y && o.y - c.y <= 20 && Math.abs(o.x - c.x) <= 10; })\n            .sort(function (a, b) { return a.y - b.y; })[0];\n          if (!lab) return;\n          fname[c.t] = (lab.t + sameLine(lab, function (q) { return q.x > lab.x && q.x - lab.x < 110 && /^（|^[^（]*）$/.test(q.t); })\n            .map(function (q) { return q.t; }).join('')).replace(/[〈〉]/g, '');\n        });\n        var fcells = its.filter(function (o) { return o.y < fTop && o.y > fBot && fcol(o.x) >= 0; });\n        fcols.forEach(function (cx, ci) {\n          var inCol = fcells.filter(function (o) { return fcol(o.x) === ci; });\n          var blocks = [];\n          inCol.forEach(function (o) {\n            if (/^[—―ー－-]$/.test(o.t)) { blocks.push({ y: o.y }); return; }\n            if (!/^CZ-[A-Z0-9]+$/.test(o.t)) return;\n            // マスの字は3行（品番・値段・交換めやす）。まん中はその平均\n            var sub = inCol.filter(function (q) { return /税抜|めやす/.test(q.t) && o.y - q.y > 0 && o.y - q.y <= 16; });\n            var pr = sub.filter(function (q) { return taxOf(q); })[0];\n            var me = sub.map(function (q) { return (q.t.match(/約\\d+年/) || [''])[0]; }).filter(Boolean)[0];\n            blocks.push({ y: avg([o.y].concat(sub.map(function (q) { return q.y; }))), code: o.t, v: pr ? taxOf(pr) : 0, me: me });\n          });\n          blocks.sort(function (a, b) { return b.y - a.y; });\n          var groups = splitRows(frows.map(function (r) { return r.y; }), blocks.map(function (b) { return b.y; }));\n          if (!groups) return;\n          blocks.forEach(function (b, bi) {\n            if (!b.code || !b.v) return;\n            out.push({ page: page, code: b.code, y: b.v,\n                       name: (fname[b.code] || b.code) + '（交換用1枚' + (b.me ? '・交換のめやす' + b.me : '') + '）',\n                       fits: groups[bi].map(function (ri) { return frows[ri].fit; }) });\n          });\n        });\n      }\n    }\n\n    /* ---- 115ページ：配管化粧カバー「スッキリダクト」 ---- */\n    if (/配管化粧カバー「スッキリダクト/.test(all)) {\n      done = true;\n      // カードの題（＜壁面取出しカバー＞）と、大きさ・向き先（＜60型＞・＜排じん&換気機能付きエアコン用＞）\n      var labs = [];\n      its.forEach(function (o) {\n        if (!/^[＜<]/.test(o.t)) return;\n        // 「＜」から右へ「＞」まで。行ではなく、この字と同じ高さの字でつなぐ\n        var seq = [o].concat(sameLine(o, function (q) { return q.x > o.x && q.x - o.x < 150; }));\n        var t = '', k = 0;\n        for (; k < seq.length; k++) { t += seq[k].t; if (/[＞>]/.test(seq[k].t)) break; }\n        var m = t.match(/^[＜<]([^＞>]+)[＞>]/);\n        if (!m) return;\n        var nx = seq[k + 1];\n        labs.push({ x: o.x, y: o.y, t: m[1], small: /型$|用$/.test(m[1]),\n                    tail: nx && /^（/.test(nx.t) && nx.x - o.x < 150 ? nx.t : '' });\n      });\n      // 「≪室内専用≫配管化粧カバー「スッキリダクトRシリーズ」」「≪室外・室内兼用≫配管化粧カバー「スッキリダクト」」\n      var secs = lines.map(function (L) {\n        var m = L.t.match(/≪([^≫]+)≫配管化粧カバー「([^」]+)」/);\n        return m ? { y: L.y, t: m[2] + '（' + m[1] + '）' } : null;\n      }).filter(Boolean);\n      // カードの並びの上の帯「「フィルターお掃除ロボット」（自動排出方式）搭載エアコン対応」（ページ上の「…アクセサリー」は案内なので別）\n      var bans = [];\n      lines.forEach(function (L) {\n        L.its.forEach(function (o, i) {\n          if (!/^「フィルターお掃除ロボット」$/.test(o.t)) return;\n          var t = L.its.slice(i).map(function (q) { return q.t; }).join('');\n          if (/搭載エアコン対応/.test(t) && !/アクセサリー/.test(t)) {\n            bans.push({ x: o.x, y: L.y, t: t.replace(/対応.*$/, '').replace(/[「」]/g, '').replace(/（([^）]+)）/, '〈$1〉') + '用' });\n          }\n        });\n      });\n      var lvMin = Number((all.match(/LVシリーズには(\\d+)型以上/) || [])[1] || 0);\n      its.forEach(function (o) {\n        var m = o.t.match(/(DA[RS]\\d{3,6}S?)(■?)$/);\n        if (!m) return;\n        var color = !!m[2] || sameLine(o, function (q) { return q.t === '■' && q.x > o.x && q.x - o.x < 45; }).length > 0;\n        // 「＜60型」「＞DAS2604」のように、大きさと品番が1つの行に並ぶカードがある。カードの左はしで見る\n        var lead = sameLine(o, function (q) { return /^[＜<]/.test(q.t) && q.x < o.x && o.x - q.x < 25; })[0];\n        var cx = lead ? lead.x : o.x;\n        var pr = priceBelow({ x: cx, y: o.y }, 22, -5, 70);\n        if (!pr) return;\n        var above = labs.filter(function (l) { return Math.abs(l.x - cx) <= 12 && l.y >= o.y - 1.5; }).sort(function (a, b) { return a.y - b.y; });\n        var ttl = above.filter(function (l) { return !l.small; })[0];\n        if (!ttl) return;\n        var sz = above[0] !== ttl ? above[0] : null;\n        var se = secs.filter(function (s) { return s.y > o.y; }).sort(function (a, b) { return a.y - b.y; })[0];\n        var bn = bans.filter(function (b) { return ttl.y < b.y && b.y - ttl.y <= 16 && ttl.x >= b.x - 70 && ttl.x < b.x + 260; })[0];\n        var notes = [];\n        // 値段の行の「※LVシリーズは適用外です。」、題の行の「※一部、適用外の機種があります。」（次の行に続く）\n        // 値段の行はとなりのカードまで続くので、同じカードの中（値段のすぐ右）だけ見る\n        sameLine(pr.o, function (q) { return /^※/.test(q.t) && q.x > pr.o.x && q.x - pr.o.x < 80; }).forEach(function (q) { notes.push(q.t); });\n        var tn = its.filter(function (q) { return /^※/.test(q.t) && Math.abs(q.y - ttl.y) <= 1.5 && q.x > ttl.x && q.x - ttl.x < 110; })[0];\n        if (tn) {\n          notes.push(its.filter(function (q) { return (Math.abs(q.y - tn.y) <= 1.5 && q.x >= tn.x) || (tn.y - q.y > 1.5 && tn.y - q.y <= 9 && q.x >= tn.x - 10 && q.x < tn.x + 60); })\n            .sort(function (a, b) { return b.y - a.y || a.x - b.x; }).map(function (q) { return q.t; }).join(''));\n        }\n        if (lvMin && sz && /^\\d+型$/.test(sz.t) && parseInt(sz.t, 10) < lvMin) notes.push('LVシリーズは' + lvMin + '型以上');\n        var tail = sameLine(o, function (q) { return q.x > o.x && q.x - o.x < 60 && /^（/.test(q.t); }).map(function (q) { return q.t; }).join('');\n        out.push({ page: page, code: m[1] + (color ? '■' : ''), y: pr.v, fits: [{ all: true }],\n                   name: '配管化粧カバー ' + (se ? se.t + ' ' : '') + ttl.t + ttl.tail + (sz ? ' ' + sz.t : '') + tail +\n                         (bn ? '（' + bn.t + '）' : '') + notes.map(function (t) { return '（' + t.replace(/^※|。$/g, '') + '）'; }).join('') +\n                         (color ? '（品番の■は色の記号）' : '') });\n      });\n    }\n\n    /* ---- 116ページ：室外機アクセサリー 適用一覧表 ---- */\n    var oa = its.filter(function (o) { return /^室外機アクセサリー適用一覧表/.test(o.t); })[0];\n    var hl = oa && lines.filter(function (L) { return L.y < oa.y && L.its.some(function (o) { return /（高さ）/.test(o.t); }); })[0];\n    if (oa && hl) {\n      done = true;\n      var ocols =hl.its.filter(function (o) { return /^\\d{3,4}$/.test(o.t); }).map(function (o) { return o.x; });\n      var ofits = kwTable(ocols, oa.y, hl.y + 3);\n      var OCODE = /^(CZ-[A-Z0-9]+(-[A-Z])?|DAG\\d{4}[A-Z]{0,2}|AD-[A-Z0-9]+)$/;\n      var ocodes = its.filter(function (o) { return OCODE.test(o.t) && o.y < hl.y; });\n      var rowCodes = ocodes.filter(function (o) { return o.x < ocols[0] - 20; }).sort(function (a, b) { return b.y - a.y; });\n      var colCodes = ocodes.filter(function (o) { return o.x >= ocols[0] - 20; });\n      var codeX = rowCodes.length ? Math.min.apply(null, rowCodes.map(function (o) { return o.x; })) : ocols[0] - 75;\n      var yLow = rowCodes.length ? Math.min.apply(null, rowCodes.map(function (o) { return o.y; })) - 10 : 0;\n      // 左はしの＜樹脂置台＞＜平地置台＞…＜日よけ屋根＞＜吹出グリル＞\n      var cats = [];\n      lines.forEach(function (L) {\n        var i = -1;\n        L.its.forEach(function (o, k) { if (i < 0 && /^＜/.test(o.t) && o.x < codeX - 40) i = k; });\n        if (i < 0) return;\n        var t = '';\n        for (var k = i; k < L.its.length; k++) { t += L.its[k].t; if (/＞/.test(L.its[k].t)) break; }\n        var m = t.match(/^＜([^＞]+)＞/);\n        if (m) cats.push({ y: L.y, t: m[1] });\n      });\n      // 品番の左の大きさ（L360・H300・横置き・「H587」「～887」「内560」「（～860）」）。縦につながったマスのまん中にある\n      var subs = [];\n      mkLines(its.filter(function (o) { return o.x > codeX - 25 && o.x < codeX - 1 && o.y < hl.y && o.y > yLow; })).forEach(function (L) {\n        var last = subs[subs.length - 1];\n        if (last && /^[～〜内（(]/.test(L.t) && last.ys[last.ys.length - 1] - L.y <= 10) { last.t += L.t; last.ys.push(L.y); }\n        else subs.push({ t: L.t, ys: [L.y] });\n      });\n      subs.forEach(function (s) {\n        s.y = avg(s.ys);\n        var t = s.t.replace(/[（）()]/g, '');\n        s.t = /内/.test(t) ? t.replace('内', '（内') + '）' : t;\n      });\n      (splitRows(rowCodes.map(function (o) { return o.y; }), subs.map(function (s) { return s.y; })) || []).forEach(function (g, si) {\n        g.forEach(function (ci) { rowCodes[ci].sub = subs[si].t; });\n      });\n      // 置台・金具の行：付くのは●の列\n      var perOne = /2個必要/.test(all);\n      rowCodes.forEach(function (c) {\n        var pr = priceBelow(c, 9, 10, 60);\n        if (!pr) return;\n        var fits = [];\n        its.forEach(function (o) {\n          if (/^●/.test(o.t) && o.x >= ocols[0] - 20 && c.y - o.y >= -1 && c.y - o.y <= 5) fits = fits.concat(ofits[nearestCol(ocols, o.x)]);\n        });\n        if (!fits.length) return;\n        var cat = cats.filter(function (k) { return k.y >= c.y - 4; }).sort(function (a, b) { return a.y - b.y; })[0];\n        var ann = sameLine(c, function (q) { return q.x > c.x && q.x - c.x < 60 && /^（/.test(q.t); }).map(function (q) { return q.t; }).join('');\n        var one = perOne && sameLine(pr.o, function (q) { return /^（1個）$/.test(q.t); }).length > 0;\n        out.push({ page: page, code: c.t, y: pr.v, fits: fits,\n                   name: '室外機用 ' + [cat ? cat.t : '', c.sub || ''].filter(Boolean).join(' ') + ann + (one ? '（1個の値段・室外機1台に2個）' : '') });\n      });\n      // 日よけ屋根・吹出グリル：列ごとに品番。区分は品番と値段のまん中にいちばん近い＜…＞\n      colCodes.forEach(function (c) {\n        var pr = priceBelow(c, 18, 0, 45);\n        if (!pr) return;\n        var mid = (c.y + pr.o.y) / 2;\n        var cat = cats.slice().sort(function (a, b) { return Math.abs(a.y - mid) - Math.abs(b.y - mid); })[0];\n        var ann = sameLine(c, function (q) { return q.x > c.x && q.x - c.x < 45 && /^（/.test(q.t); }).map(function (q) { return q.t; }).join('');\n        var tag = its.filter(function (o) { return /^〈[^〉]+〉$/.test(o.t) && c.y - o.y > 0 && c.y - o.y <= 7 && Math.abs(o.x - c.x) <= 10; })\n          .map(function (o) { return ' ' + o.t.replace(/[〈〉]/g, ''); })[0] || '';\n        var dir = (!ann && !tag) ? its.filter(function (o) { return /方向吹出$/.test(o.t) && o.x < ocols[0] && Math.abs(o.y - mid) <= 10; })\n          .sort(function (a, b) { return Math.abs(a.y - mid) - Math.abs(b.y - mid); }).map(function (o) { return '（' + o.t + '）'; })[0] : '';\n        out.push({ page: page, code: c.t, y: pr.v, fits: ofits[nearestCol(ocols, c.x)],\n                   name: '室外機用 ' + (cat ? cat.t : '') + tag + (ann || dir || '') });\n      });\n      // 注＊4 の転倒防止金具（品番と値段が注の文の中）。付くのは＊4 の印がある列\n      its.forEach(function (o) {\n        var m = o.t.match(/^（?(DAG\\d{4}[A-Z]{0,2})希望小売価格/);\n        if (!m) return;\n        var pr = sameLine(o, function (q) { return taxOf(q) && q.x > o.x && q.x - o.x < 120; })[0];\n        var nm = sameLine(o, function (q) { return q.x < o.x && /金具$/.test(q.t); }).map(function (q) { return q.t.replace(/^.*は/, ''); })[0];\n        if (!pr || !nm) return;\n        var cis = [];\n        its.forEach(function (q) {\n          if (/＊4/.test(q.t) && q.y > yLow && q.y < hl.y) { var ci = nearestCol(ocols, q.x); if (cis.indexOf(ci) < 0) cis.push(ci); }\n        });\n        if (!cis.length) return;\n        var head = lines.filter(function (L) { return /＊4：/.test(L.t); })[0];\n        var why = head ? (head.t.split('＊4：')[1] || '').replace(/＊\\d.*$/, '') : '';\n        out.push({ page: page, code: m[1], y: taxOf(pr),\n                   fits: [].concat.apply([], cis.map(function (ci) { return ofits[ci]; })),\n                   name: '室外機用 ' + nm + (why ? '（' + why.replace(/を$/, 'を付けるとき') + '）' : '') });\n      });\n    }\n\n    /* ---- 118ページ：リモコンホルダー・室外機背面金網・かんたんリモコン・RACローカルアダプター・有線式集中コントロール ---- */\n    if (/リモコンホルダー/.test(all) && /室外機背面金網/.test(all)) {\n      done = true;\n      var TTL =/^(リモコンホルダー|室外機背面金網|かんたんリモコン|RACローカルアダプター|有線式集中コントロールシステム)$/;\n      var ttls = its.filter(function (o) { return TTL.test(o.t); });\n      var PC = /^(CF|CZ|VL)-[A-Z0-9]+$/;\n      // 室外機背面金網：列＝品番（CF-UA8…）、行＝シリーズ、マス＝能力\n      var ua = its.filter(function (o) { return /^CF-UA\\d+$/.test(o.t); }).sort(function (a, b) { return a.x - b.x; });\n      var uaFits = [];\n      if (ua.length) {\n        var szY = its.filter(function (o) { return /^H\\d+×W\\d+$/.test(o.t); }).map(function (o) { return o.y; });\n        var uBot = its.filter(function (o) { return /^有線式集中/.test(o.t); }).map(function (o) { return o.y; })[0] || 0;\n        uaFits = kwTable(ua.map(function (o) { return o.x; }), (szY.length ? Math.min.apply(null, szY) : ua[0].y - 30) - 2, uBot);\n      }\n      // リモコンホルダー：品番の下の「適用リモコン」の文（「2026年モデル EX・GX・C・TXシリーズ、2025年モデル N・Kシリーズ、2024年以降モデル Jシリーズ」）\n      var rh = its.filter(function (o) { return /^CF-RH\\d+$/.test(o.t); }).sort(function (a, b) { return a.x - b.x; });\n      var rhFits = function (c) {\n        var i = rh.indexOf(c), x1 = i < rh.length - 1 ? rh[i + 1].x - 10 : 1e9;\n        var hd = its.filter(function (o) { return o.t === '適用リモコン' && o.y < c.y && o.x >= c.x - 10 && o.x < x1; })[0];\n        if (!hd) return [];\n        var t = mkLines(its.filter(function (o) { return o.x >= c.x - 10 && o.x < x1 && o.y < hd.y - 3 && o.y > hd.y - 30; }))\n          .map(function (L) { return L.t; }).join('');\n        var fits = [], re = /(20\\d\\d)年(以降)?モデル(.*?)シリーズ/g, m;\n        while ((m = re.exec(t))) {\n          var yr = m[1], since = !!m[2];\n          (m[3].replace(/住宅設備用/g, '').match(/[A-Z]{1,2}/g) || []).forEach(function (L) {\n            var f = { s: L + 'シリーズ' };\n            if (!since) f.re = '^CS-[A-Z]*\\\\d{2}' + (Number(yr) % 10);\n            fits.push({ rs: f });\n          });\n        }\n        return fits;\n      };\n      its.forEach(function (c) {\n        if (!PC.test(c.t)) return;\n        var pr = priceBelow(c, 30, -10, 120);\n        var open = !pr && sameLine(c, function (q) { return /^オープン価格/.test(q.t) && q.x > c.x && q.x - c.x < 60; }).length > 0;\n        if (!pr && !open) return;\n        var ttl = ttls.filter(function (o) { return o.y > c.y && o.x <= c.x + 10; }).sort(function (a, b) { return (a.y - b.y) || (b.x - a.x); })[0];\n        var nm = ttl ? ttl.t + sameLine(ttl, function (q) { return q.x > ttl.x && q.x - ttl.x < 120 && !TTL.test(q.t); })\n          .map(function (q) { return '（' + q.t + '）'; }).join('') : '';\n        // 品番の左（「■JEM-Aアダプタ（別売）」）か、すぐ上の行（「コントロールユニット（10室用）」）\n        var sub = sameLine(c, function (q) { return q.x < c.x && c.x - q.x < 90 && !PC.test(q.t) && /[ぁ-んァ-ヶ一-龥]/.test(q.t); })\n          .map(function (q) { return q.t; }).join('');\n        if (!sub) {\n          sub = lines.filter(function (L) { return L.y - c.y > 3 && L.y - c.y <= 12; }).map(function (L) {\n            return L.its.filter(function (q) { return q.x >= c.x - 10 && q.x < c.x + 90 && /[ぁ-んァ-ヶ一-龥]/.test(q.t); }).map(function (q) { return q.t; }).join('');\n          }).filter(Boolean)[0] || '';\n        }\n        var size = its.filter(function (q) { return /^H\\d+×W\\d+$/.test(q.t) && Math.abs(q.x - c.x) <= 10 && c.y - q.y > 0 && c.y - q.y <= 40; })\n          .map(function (q) { return q.t; })[0];\n        // 注（「＊接続機種に制限があります。」）。値段の行の、すぐ右だけ（行はページの右はしまで続く）。\n        // オープン価格の品は値段の行が無いので、品番の少し下・右（「＊室内機1台につき1個必要です。」）\n        var notes = (pr ? sameLine(pr.o, function (q) { return /^＊/.test(q.t) && q.x > pr.o.x && q.x - pr.o.x < 100; })\n                        : its.filter(function (q) { return /^＊/.test(q.t) && c.y - q.y > 0 && c.y - q.y <= 35 && q.x >= c.x && q.x - c.x <= 150; }))\n          .map(function (q) { return '（' + q.t.replace(/^＊|。$/g, '') + '）'; }).join('');\n        var fits = /^CF-RH/.test(c.t) ? rhFits(c) : /^CF-UA/.test(c.t) ? uaFits[ua.indexOf(c)] || [] : [{ all: true }];\n        if (!fits.length) return;\n        out.push({ page: page, code: c.t, y: pr ? pr.v : 0, fits: fits,\n                   name: [nm, sub.replace(/^■/, ''), size].filter(Boolean).join(' ') + notes + (open ? '（オープン価格）' : '') });\n      });\n    }\n\n    /* ---- 機種のページの別売品（ほか） ----\n       ビルトインの化粧グリル・天井スペーサー・前面グリル・据付枠、フリービルトインの部材、LVの給気ホース、\n       お掃除ロボットの排気ホース、凍結防止ヒーター、置台、54ページの防雪部材の表とドレンソケット。\n       付く機種は、合計の注「（別販化粧グリルCZ-BT10-W・WS・Tを使用した場合）」（ページをまたいで同じグリルを使う機種）、\n       「（おもな適用機種：CS-B289/409CA2…）」、「対象機種」の文、同じページ（見開きの左のページ）の機種の順に、\n       panaRoomOptFinish で決める */\n    if (!done) {\n      var pms = panaRoomReadPage(items, page).filter(function (r) { return !r.salt; });\n      if (pms.length) out.push({ page: page, models: pms.map(function (r) { return { m: r.m, pm: r.pm || '' }; }) });\n      var apm = all.match(/おもな適用機種：?((?:CS-[A-Z]*\\d{3}(?:\\/\\d{3})*[A-Z]+\\d?[、,]?)+)/);\n      if (apm) {\n        var aps = [];\n        apm[1].replace(/CS-([A-Z]*)(\\d{3}(?:\\/\\d{3})*)([A-Z]+\\d?)/g, function (w, p, caps, suf) {\n          caps.split('/').forEach(function (cp) { aps.push('CS-' + p + cp + suf); });\n          return w;\n        });\n        out.push({ page: page, applies: aps });\n      }\n      var design = /設計・施工編/.test(all);\n      /** 「UX・TX・Kシリーズ、UB・UYシリーズ」→ シリーズごとの当て方 */\n      var seriesFits = function (t) {\n        var fits = [], re = /((?:[A-Z]{1,2}・)*[A-Z]{1,2})シリーズ/g, m;\n        while ((m = re.exec(t))) m[1].split('・').forEach(function (L) { fits.push({ rs: { s: L + 'シリーズ' } }); });\n        return fits;\n      };\n\n      /* 54ページ：防雪部材 適用機種一覧表。行＝シリーズ（そばに能力）、列＝部材（一般地用・塩害地用）。値段は税込だけ → 1.1で割る */\n      var snowH = its.filter(function (o) { return /防雪部材適用機種一覧表/.test(o.t); })[0];\n      if (snowH) {\n        var SNOW = /^(DAG\\d{4}[A-Z]{0,2}|AD-[A-Z0-9-]+|CZ-[A-Z0-9-]+)$/;\n        var sc = its.filter(function (o) { return SNOW.test(o.t) && o.y < snowH.y; });\n        var sParts = its.filter(function (o) { return /^[❶-❻]/.test(o.t) && o.y < snowH.y && snowH.y - o.y < 40; });\n        var sSubs = its.filter(function (o) { return /^（(一般地用|塩害地用|塩害地兼用)）$/.test(o.t) && o.y < snowH.y && snowH.y - o.y < 50; });\n        if (sc.length && sParts.length) {\n          var sx0 = Math.min.apply(null, sc.map(function (o) { return o.x; }));\n          var sHdY = Math.min.apply(null, sSubs.map(function (o) { return o.y; }).concat([snowH.y - 30]));\n          // 行の見出し（HX・UX・UB・UY…）と、そのそばの能力（「（100V）」「2.2～4.0」）\n          var sLab = mkLines(its.filter(function (o) { return o.y < sHdY - 3 && o.x < sx0 - 60 && /^[A-Z]{1,2}$/.test(o.t); }))\n            .map(function (L) { return { y: L.y, ss: panaRowSeries(L.its.map(function (o) { return o.t; }).join('・')), specs: [] }; });\n          mkLines(its.filter(function (o) { return o.y < sHdY - 3 && o.x >= sx0 - 65 && o.x < sx0 - 25 && !/^[HWD]\\d/.test(o.t); })).forEach(function (L) {\n            if (!/\\d\\.\\d/.test(L.t)) return;\n            var r = sLab.slice().sort(function (a, b) { return Math.abs(a.y - L.y) - Math.abs(b.y - L.y); })[0];\n            if (r && Math.abs(r.y - L.y) <= 6) r.specs.push(L.t);\n          });\n          // 品番の行（行の中で品番の高さが3ずれることがある）。まん中は品番と値段の間\n          var sRows = [];\n          sc.slice().sort(function (a, b) { return b.y - a.y; }).forEach(function (o) {\n            var r = sRows.filter(function (q) { return Math.abs(q.y - o.y) <= 5; })[0];\n            if (r) r.its.push(o); else sRows.push({ y: o.y, its: [o] });\n          });\n          var sPrice = function (c) {\n            var p = its.filter(function (q) { return /^\\d{1,3}(,\\d{3})+$/.test(q.t) && Math.abs(q.x - c.x) <= 6 && c.y - q.y > 0 && c.y - q.y <= 12; })[0];\n            if (!p) return 0;\n            var tax = sameLine(p, function (q) { return /税込/.test(q.t) && q.x > p.x && q.x - p.x < 50; }).length > 0;\n            return tax ? Math.round(yen(p.t) / 1.1) : yen(p.t);\n          };\n          sRows.forEach(function (r) {\n            var pys = r.its.map(function (c) {\n              var p = its.filter(function (q) { return /^\\d{1,3}(,\\d{3})+$/.test(q.t) && Math.abs(q.x - c.x) <= 6 && c.y - q.y > 0 && c.y - q.y <= 12; })[0];\n              return p ? (c.y + p.y) / 2 : c.y;\n            });\n            r.mid = avg(pys);\n          });\n          var sGroups = splitRows(sLab.map(function (l) { return l.y; }), sRows.map(function (r) { return r.mid; })) || [];\n          var fnText = lines.filter(function (L) { return /注\\d：/.test(L.t) || /のセット/.test(L.t); }).map(function (L) { return L.t; }).join('');\n          var fnote = {};\n          fnText.replace(/注(\\d)：(.*?)(?=注\\d：|$)/g, function (w, n, t) { fnote[n] = t.replace(/。$/, ''); return w; });\n          sRows.forEach(function (r, ri) {\n            var fits = [];\n            (sGroups[ri] || []).forEach(function (li) {\n              var l = sLab[li];\n              if (l.specs.length) l.specs.forEach(function (sp) { fits = fits.concat(panaKwFits(l.ss, sp)); });\n              else l.ss.forEach(function (s) { fits.push({ rs: { s: s } }); });\n            });\n            if (!fits.length) return;\n            r.its.forEach(function (c) {\n              var v = sPrice(c);\n              if (!v) return;\n              var p = sParts.filter(function (h) { return h.x <= c.x + 25; }).sort(function (a, b) { return b.x - a.x; })[0];\n              var pt = p ? p.t + sameLine(p, function (q) { return q.x > p.x && q.x - p.x < 45 && /^（/.test(q.t); }).map(function (q) { return q.t; }).join('') : '';\n              var sb = sSubs.filter(function (h) { return Math.abs(h.x - c.x) <= 20; }).sort(function (a, b) { return Math.abs(a.x - c.x) - Math.abs(b.x - c.x); })[0];\n              var nn = its.filter(function (q) { return /^注\\d$/.test(q.t) && q.x >= c.x && q.x - c.x <= 45 && q.y - c.y >= 0 && q.y - c.y <= 4; })\n                .map(function (q) { return fnote[q.t.slice(1)] || ''; }).filter(function (t) { return /のセット/.test(t); });\n              out.push({ page: page, code: c.t, y: v, fits: fits,\n                         name: '室外機用 防雪部材 ' + pt.replace(/^[❶-❻]/, '') + (sb ? sb.t : '') + nn.map(function (t) { return '（' + t + '）'; }).join('') });\n            });\n          });\n        }\n      }\n\n      // 排気ホース適用一覧（83ページ）：いまの機種（2026年〜）の行に出てくる品番と、そのジョイント（-1）だけ\n      var hvHd = its.filter(function (o) { return o.t === '発売年度'; })[0];\n      var hvNow = null;\n      if (hvHd && /排気ホース/.test(all)) {\n        var hvRow = lines.filter(function (L) { return L.y < hvHd.y - 3 && L.y > hvHd.y - 25 && L.its.some(function (q) { return /^(CF|CZ)-/.test(q.t); }); })\n          .sort(function (a, b) { return b.y - a.y; })[0];\n        hvNow = hvRow ? hvRow.its.filter(function (q) { return /^(CF|CZ)-/.test(q.t); }).map(function (q) { return q.t; }) : [];\n      }\n\n      var SIDE = /^★?((?:CZ|CF|HS)-[A-Z0-9]+(?:-[A-Z0-9]+)?)$/;\n      var sideCodes = its.filter(function (o) { return SIDE.test(o.s.split(' ')[0]); });\n      /** 品番の近くの税抜の値段。下（同じ行も）を、下へのずれ＋横のずれ×0.3 で近い順に。\n          同じ行でも右の列の値段（CZ-BDR12 の右の CZ-BDG4-W）は採らない。色の行をはさんで32下のこともある（CF-BT20S） */\n      var priceNear = function (c) {\n        var best = null;\n        its.forEach(function (q) {\n          var v = taxOf(q), dy = c.y - q.y, dx = q.x - c.x;\n          if (!v || dy < -1.5 || dy > 34 || dx < -30 || dx > 105) return;\n          var d = Math.max(dy, 0) + 0.3 * Math.abs(dx);\n          if (!best || d < best.d) best = { v: v, d: d, o: q };\n        });\n        return best;\n      };\n      // 枠の見出し（「■化粧グリル〈別売品〉」「化粧グリル（別売品）（1.6kW用）幅745・奥行440mm」「■」「天井スペーサー」「〈別売品〉」）\n      var HEAD = /^■?[^\\d（〈■]{2,14}[（〈]別売品[）〉]/;\n      var heads = [];\n      lines.forEach(function (L) {\n        L.its.forEach(function (h, i) {\n          if (i && h.x - L.its[i - 1].x < 40 && !/^■/.test(h.t)) return;     // 文の途中から始まるものは見出しにしない\n          var ht = '';\n          for (var k = i; k < L.its.length && L.its[k].x - h.x < 180; k++) { if (k > i && /^■/.test(L.its[k].t)) break; ht += L.its[k].t; }\n          // 「…ご用意しています（別売品）。詳しくは…」のような文は見出しにしない\n          if (HEAD.test(ht) && !/ます|です|。/.test(ht.split(/[（〈]別売品/)[0])) heads.push({ x: h.x, y: L.y, t: ht });\n        });\n      });\n      // 「■対象機種」「対象品番」の表の形名（80ページ 電源切替ボックス「CS-UB284/404/564DC2」「CS-B224/254/284/364/404DC2」…）。\n      // その表のそば（上下80まで）の品にだけ使う\n      var pageTargets = [], tgY = [];\n      if (its.some(function (q) { return /対象品番|対象機種/.test(q.t); })) {\n        its.forEach(function (q) {\n          q.t.replace(/CS-([A-Z]*)(\\d{3}(?:\\/\\d{3})+)([A-Z]+\\d?)/g, function (w, p, caps, suf) {\n            caps.split('/').forEach(function (cp) { pageTargets.push('CS-' + p + cp + suf); });\n            tgY.push(q.y);\n            return w;\n          });\n        });\n      }\n      var tgMid = tgY.length ? avg(tgY) : 0;\n      var SKIP = /マンセル|GY|YR|^[\\d.\\s\\/）：]+$|^(幅|高さ|厚み)[\\d,]/;\n      var STOP = /円|税抜|希望小売価格|別売品|^■|^[●［※]|^[A-Z]{2}-|モデル|機種|。|から|買い換え|について|ください|場合/;\n      var sideItems = [];\n      sideCodes.forEach(function (o) {\n        var code = o.s.split(' ')[0].match(SIDE)[1];\n        if (snowH && o.y < snowH.y) return;                    // 防雪部材の表の中は上で読んだ\n        if (/^CZ-(SAF|SAD|SW\\d)/.test(code)) return;            // フィルターは114ページの適用一覧で読む\n        var sfx = sameLine(o, function (q) { return /^-[A-Z]{1,2}$/.test(q.t) && q.x > o.x && q.x - o.x < 55; })[0];\n        if (sfx && !/-[A-Z]{1,2}$/.test(code)) code += sfx.t;\n        if (hvNow && !(hvNow.indexOf(code) >= 0 || hvNow.indexOf(code.replace(/-1$/, '')) >= 0)) return;\n        var pr = priceNear(o);\n        if (!pr) return;\n        // 見出し：同じ枠のいちばん近い上。2列の枠では右の列から見て120ほど左にある\n        var hd = heads.filter(function (h) { return h.y > o.y + 1.5 && h.y - o.y <= 260 && h.x <= o.x + 14 && o.x - h.x <= 200; })\n          .sort(function (a, b) { return a.y - b.y; })[0];\n        var head = hd ? hd.t.replace(/^■/, '').replace(/[（〈]別売品[）〉]/g, '').replace(/(幅|高さ)[\\d,].*$/, '') : '';\n        // 品番の左にくっついた字（表の行「吸込・吹出グリル CZ-BWG8」、「■ 置台 CF-SD8C」、「コンパク」「トタイプ」HS-BT18-W）\n        // 左の字が80をこえて続くのは、となりの列の品物の名前（69ページ下の CZ-BDG4-W の左は ⑬ の「フレキシブルダクト用）」）\n        var left = '', lx = o.x, cut = false;\n        sameLine(o, function (q) { return q.x < o.x; }).reverse().some(function (q) {\n          if (lx - (q.x + (q.w || q.t.length * 6)) > 12 || !(/[ぁ-んァ-ヶ一-龥]/.test(q.t) || /^[・･]$/.test(q.t)) || /円|税抜/.test(q.t)) return true;\n          if (o.x - q.x > 80) { cut = true; return true; }\n          left = q.t + left; lx = q.x;\n          return false;\n        });\n        if (cut) { left = ''; lx = o.x; }\n        left = left.replace(/^■/, '');\n        // 品番の上の名前（「ホワイト」「① 吹出グリル」、3行の「② 吹出グリル用」「チャンバー（オーバル」「フレキシブルダクト用）」）。\n        // マンセル番号や大きさの行はとばす。値段・見出し・文の行でやめる。右の列の品番より右は見ない\n        var rightEdge = o.x + 95;\n        sideCodes.forEach(function (q) { if (q !== o && q.x > o.x + 20 && Math.abs(q.y - o.y) <= 20) rightEdge = Math.min(rightEdge, q.x - 4); });\n        var up = [], yy = null;\n        lines.filter(function (L) { return L.y > o.y + 1.5 && L.y - o.y <= 45; }).sort(function (a, b) { return a.y - b.y; }).some(function (L) {\n          var t = L.its.filter(function (q) { return q.x >= o.x - 6 && q.x < rightEdge; }).map(function (q) { return q.t; }).join('');\n          // 「〈別売品〉」の行（見出し）でやめる。少し左にあっても見る（58ページ「〈別売品〉専用部材」は品番の11左）\n          if (L.its.some(function (q) { return q.x >= o.x - 40 && q.x < rightEdge && /別売品/.test(q.t); })) return true;\n          // 表の上の行（すぐ右に品番がある：69ページ CZ-BWF1 の上の「吹出ダクト CZ-BWD3」）\n          if (L.its.some(function (q) { return q.x >= o.x - 40 && q.x < o.x + 45 && SIDE.test(q.s.split(' ')[0]); })) return true;\n          if (!t) return false;\n          if (STOP.test(t)) return true;\n          // マンセル番号や大きさの行はとばす。とばした行をはさむときは17まであいてよい\n          // （67ページ「見切り代寸法ワイドタイプ（土壁などに）」「高さ363・幅784mm」「和風グリル」CZ-BKG26A）\n          var noise = SKIP.test(t) || !/[ぁ-んァ-ヶ一-龥]/.test(t);\n          if (yy !== null && L.y - yy > (noise ? 17 : 10)) return true;\n          if (noise) { if (yy !== null) yy = L.y; return false; }\n          up.unshift(t);\n          yy = L.y;\n          return up.length >= 3;\n        });\n        var label = (left || up.join('')).replace(/^[①-⑳]/, '').replace(/＜([^＞]*)＞/g, '$1').replace(/^NEW/, '').replace(/＊/g, '・').replace(/・$/, '');\n        var tag = '';\n        if (/在庫限定/.test(label)) { label = label.replace(/在庫限定/, ''); tag = '（在庫限定）'; }\n        if (its.some(function (q) { return /在庫限定/.test(q.t) && Math.abs(q.y - o.y) <= 2.5 && q.x > o.x && q.x - o.x < 70; })) tag = '（在庫限定）';\n        // 表の行は、上の行の品物の説明（「（総檜製）本体811mm幅（縁幅含む825mm）」）と、左の区分（「左右風向変更羽根」「固定式」）\n        if (left) {\n          var desc = lines.filter(function (L) { return L.y > o.y + 1.5 && L.y - o.y <= 10; }).map(function (L) {\n            return L.its.filter(function (q) { return q.x >= lx - 10 && q.x < o.x + 60; }).map(function (q) { return q.t; }).join('');\n          }).filter(function (t) { return /^（/.test(t); })[0];\n          if (desc) label += desc;\n          if (/グリル/.test(left)) {\n            // 区分は2行ぶんのマスのまん中に書いてある（「左右風向変更羽根」「固定式」が CZ-BWG3 と CZ-BWG3A の間）。\n            // いちばん近い字から、10以内で続く字をまとめて1つの区分にする\n            var gc = its.filter(function (q) { return q.x >= o.x - 125 && q.x < lx - 10 && Math.abs(q.y - o.y) <= 20 && /[ぁ-んァ-ヶ一-龥]/.test(q.t); });\n            var g0 = gc.slice().sort(function (a, b) { return Math.abs(a.y - o.y) - Math.abs(b.y - o.y); })[0];\n            if (g0 && Math.abs(g0.y - o.y) <= 12) {\n              var blk = [g0], grow = true;\n              while (grow) {\n                grow = false;\n                gc.forEach(function (q) {\n                  if (blk.indexOf(q) < 0 && blk.some(function (b) { return Math.abs(b.y - q.y) <= 10; })) { blk.push(q); grow = true; }\n                });\n              }\n              label += '（' + blk.sort(function (a, b) { return b.y - a.y; }).map(function (q) { return q.t; }).join('') + '）';\n            }\n          }\n        }\n        // 品番の右（「（1m）」「木製」）と、下の行の色（「-W（ホワイト）、」「-WS（スーパーホワイト）、-M（白木）」）\n        var right = its.filter(function (q) { return q !== o && Math.abs(q.y - o.y) <= 2 && q.x > o.x && q.x - o.x < 60 && !/在庫限定/.test(q.t); })\n          .sort(function (a, b) { return a.x - b.x; });\n        var tail = '';\n        if (right[0] && /^（/.test(right[0].t)) { for (var k = 0; k < right.length; k++) { tail += right[k].t; if (/）/.test(right[k].t)) break; } }\n        else if (right[0] && /^[ぁ-んァ-ヶ一-龥]+＊?$/.test(right[0].t) && !/希望小売価格/.test(right[0].t)) tail = '（' + right[0].t.replace(/＊$/, '') + '）';\n        var below = lines.filter(function (L) { return L.y < o.y - 1.5 && o.y - L.y <= 18; }).sort(function (a, b) { return b.y - a.y; }).map(function (L) {\n          return L.its.filter(function (q) { return q.x >= o.x - 5 && q.x < o.x + 150; }).map(function (q) { return q.t; }).join('');\n        }).filter(function (t) { return /^-[A-Z]/.test(t); }).join('').replace(/、$/, '');\n        // 付く機種が文で書いてあるもの：「対象機種 … UX・TX・Kシリーズ、UB・UYシリーズ」、凍結防止ヒーターの「4.0kW～9.0kW用(200V)」\n        var fits, kwName = '';\n        var tg = its.filter(function (q) { return /^(対象機種|適用機種)$/.test(q.t) && q.x > o.x && q.x - o.x < 160 && Math.abs(q.y - o.y) < 40; })[0];\n        if (tg) {\n          fits = seriesFits(its.filter(function (q) { return q.x >= tg.x - 5 && q.x < tg.x + 80 && q.y <= tg.y && q.y > tg.y - 50; })\n            .sort(function (a, b) { return b.y - a.y || a.x - b.x; }).map(function (q) { return q.t; }).join(''));\n        }\n        var kwT = lines.filter(function (L) { return L.y < pr.o.y + 1.5 && pr.o.y - L.y <= 12; }).map(function (L) {\n          return L.its.filter(function (q) { return q.x >= o.x - 5 && q.x < o.x + 150; }).map(function (q) { return q.t; }).join('');\n        }).filter(function (t) { return /kW.*用[（(]\\d+V/.test(t); })[0];\n        if (kwT) {\n          var ser = its.filter(function (q) { return /^[A-Z]{1,2}シリーズ/.test(q.t) && q.y > o.y && q.y - o.y < 150; })\n            .sort(function (a, b) { return a.y - b.y; }).map(function (q) { return q.t.match(/^([A-Z]{1,2})シリーズ/)[1] + 'シリーズ'; })[0];\n          var vv = kwT.match(/[（(](\\d+)V/)[1] + 'V';\n          var km = kwT.match(/([\\d.]+)kW[～~〜]([\\d.]+)kW用/);\n          var ks = km ? null : (kwT.match(/([\\d.]+)kW/g) || []).map(function (s) { return parseFloat(s); });\n          if (ser) {\n            fits = [{ rs: km ? { s: ser, kr: [Number(km[1]), Number(km[2])], v: vv } : { s: ser, k: ks, v: vv } }];\n            kwName = '（' + ser + ' ' + kwT.replace(/[（(](\\d+V)[）)]/, '・$1') + '）';\n          }\n        }\n        if (hvNow) {\n          // 付属品一覧の「HX・EX・EL シリーズ」「UX・TX シリーズ」（字が分かれているので行でつなぐ）\n          fits = seriesFits(lines.map(function (L) { return L.its.filter(function (q) { return q.x < 150; }).map(function (q) { return q.t; }).join(''); })\n            .filter(function (t) { return /^(?:[A-Z]{1,2}・)*[A-Z]{1,2}シリーズ/.test(t); }).join('、'));\n        }\n        if (!fits && pageTargets.length && Math.abs(o.y - tgMid) <= 80) {\n          fits = pageTargets.map(function (m) { return { mc: m }; });\n          head = '';                                            // 上の別の枠（天井スペーサー）の見出しを借りない\n        }\n        if (!fits && design) return;                           // 設計・施工編のページは、機種から決めない\n        var name = [head, label && label !== head ? label : ''].filter(Boolean).join(' ') + tail + tag + kwName + (below ? '（' + below + '）' : '');\n        sideItems.push({ page: page, code: code, y: pr.v, x: o.x, yy: o.y, name: name, label: label, tail: tail, fits: fits,\n                         seg: hd ? page + ':' + Math.round(hd.y) + ':' + Math.round(hd.x) : null });\n      });\n      // 名前が無い品番は、同じ列のすぐ上の品番の名前を借りる（「CZ-BDD1（1m）」の下の「CZ-BDD2（2m）」）。\n      // それも無ければ、左の区分の字（「縁付きグリル」「取付用木枠」）\n      sideItems.forEach(function (it) {\n        if (it.label) return;\n        var pv = sideItems.filter(function (q) { return q !== it && q.label && Math.abs(q.x - it.x) <= 8 && q.yy > it.yy && q.yy - it.yy <= 40; })\n          .sort(function (a, b) { return a.yy - b.yy; })[0];\n        if (pv) { it.name = pv.name.replace(pv.tail, '') + it.tail; return; }\n        if (it.name && it.name !== it.tail) return;           // 見出しで名前が付いているもの（据付枠）はそのまま\n        var lc = its.filter(function (q) {\n          // 区分の欄だけ（もっと左は「※木枠をご使用になると、より美しく…」の文）\n          return q.x >= it.x - 75 && q.x < it.x - 12 && Math.abs(q.y - it.yy) <= 9 && (/[ぁ-んァ-ヶ一-龥]/.test(q.t) || /^[・･]$/.test(q.t)) && !/円|税抜/.test(q.t);\n        }).sort(function (a, b) { return b.y - a.y || a.x - b.x; }).map(function (q) { return q.t; }).join('');\n        if (lc) it.name = lc + it.tail;\n      });\n      sideItems.forEach(function (it) {\n        if (!it.name) return;\n        out.push({ page: it.page, code: it.code, y: it.y, name: it.name, fits: it.fits, seg: it.seg });\n      });\n    }\n    return out;\n  }\n\n  /* 付く機種が紙面に書いていない品（機種のページの化粧グリル・部材）は、ここで決める。\n     ①同じ見出しの中の品番を、合計の注で「使用した場合」と書いてある機種（ページをまたぐ：BCとUBとマルチは同じ CZ-BT10）\n     ②「おもな適用機種」（フリービルトイン。部材の右のページには無いので、1つ前のページも見る）\n     ③合計の注のある機種が同じページ（か1つ前のページ）にあればその機種 ④同じページの機種 */\n  function panaRoomOptFinish(list) {\n    var usesBy = {}, usesPage = {}, models = {}, applies = {};\n    list.forEach(function (o) {\n      if (o.models) {\n        models[o.page] = (models[o.page] || []).concat(o.models.map(function (r) { return r.m; }));\n        o.models.forEach(function (r) {\n          if (!r.pm) return;\n          (usesPage[o.page] = usesPage[o.page] || []).push(r.m);\n          r.pm.split('・').forEach(function (c) { (usesBy[c] = usesBy[c] || []).push(r.m); });\n        });\n      }\n      if (o.applies) applies[o.page] = o.applies;\n    });\n    var segCodes = {};\n    list.forEach(function (o) { if (o.code && o.seg) (segCodes[o.seg] = segCodes[o.seg] || []).push(o.code); });\n    var items = [];\n    list.forEach(function (o) {\n      if (!o.code) return;\n      if (!o.fits) {\n        var ms = [];\n        (o.seg ? segCodes[o.seg] : [o.code]).forEach(function (c) { ms = ms.concat(usesBy[c] || []); });\n        // 同じページの機種を、1つ前のページ（見開きの左）より先に見る（54ページの置台は UY の機種。1つ前は UB）\n        if (!ms.length) ms = applies[o.page] || applies[o.page - 1] || usesPage[o.page] || models[o.page] || usesPage[o.page - 1] || models[o.page - 1] || [];\n        var seen = {};\n        ms = ms.filter(function (m) { if (seen[m]) return false; seen[m] = 1; return true; });\n        if (!ms.length) return;\n        o = { page: o.page, code: o.code, name: o.name, y: o.y, fits: ms.map(function (m) { return { mc: m }; }) };\n      }\n      if (!o.fits.length) return;\n      items.push(o);\n    });\n    // 同じ品番が何ページにもあるときは、別売品のページ（PDFの115〜119）の品名を採る\n    function rank(o) { return o.page >= 115 && o.page <= 119 ? 0 : 1; }\n    items.sort(function (a, b) { return rank(a) - rank(b) || a.page - b.page; });\n    return optResult(items, 'パナソニック', 'ルームエアコン（住宅設備用） 別売品');\n  }\n\n  /* --------------------------------------------------------------------\n     ゼネラル ルームエアコン（住宅設備用カタログ 2026・No.A674A・20ページ。1ページが見開き2面）\n\n     1台ぶんは列のかたまり（1面に3列×2〜3段）：\n       「冷暖房時おもに」「14」「畳用」／「AS-X406T2」「(W)」「単相200V」／「AO-X406T2」（室外機）／\n       「希望小売価格(税別)」「330,000円」または「オープン価格※」\n     値段は税別なので、そのまま入れる。X・Z・W・L・ZN・DN はオープン価格（値段0）。\n     ZN・DN は寒冷地仕様（暖房強化型）で2025年モデル（形名の3けため 5）。\n     多くの字は化けて読めないが、形名・値段・「単相200V」「畳用」は pdf.js で読める（2026-09-12）。\n     3ページの一覧表・仕様表・注の中の形名は、室外機の形名と値段（オープン価格）がそばに無いので読まない\n     -------------------------------------------------------------------- */\n  var GEN_AS = /^AS-([A-Z]{1,2})(\\d{2})(\\d)([A-Z])(\\d?)$/;\n\n  function genRoomReadPage(items, page) {\n    // 見えない字（制御文字）はすき間にしてから詰める\n    function tidy(t) {\n      return String(t).split('').map(function (ch) { return ch.charCodeAt(0) < 32 ? ' ' : ch; }).join('').replace(/\\s+/g, '');\n    }\n    var its = [];\n    items.forEach(function (o) {\n      var t = tidy(o.s);\n      if (t) its.push({ t: t, x: o.x, y: o.y, w: o.w || 0 });\n    });\n    var out = [];\n    its.forEach(function (c) {\n      var k = c.t.match(GEN_AS);\n      if (!k) return;\n      /** 形名 c のまわりで、re に合う字。dy は上から下に正（形名より下が＋） */\n      function near(re, dyMin, dyMax, dxMin, dxMax) {\n        return its.filter(function (o) {\n          var dy = c.y - o.y, dx = o.x - c.x;\n          return o !== c && re.test(o.t) && dy >= dyMin && dy <= dyMax && dx >= dxMin && dx <= dxMax;\n        }).sort(function (a, b) {\n          return (Math.abs(c.y - a.y) + 0.3 * Math.abs(a.x - c.x)) - (Math.abs(c.y - b.y) + 0.3 * Math.abs(b.x - c.x));\n        })[0];\n      }\n      var om = near(/AO-[A-Z0-9]+/, 2, 14, -5, 45);\n      var pw = near(/^単相(100|200)V/, -6, 4, 30, 110);\n      var pr = near(/^[\\d,]+円$/, 6, 24, 40, 140);\n      var op = near(/オープン価格/, 2, 24, 20, 150);\n      if (!om || !(pr || op)) return;                // 一覧表・仕様表・注の中の形名\n      var tat = 0, h = near(/^畳用$/, -24, -6, 10, 100);\n      if (h) {\n        var dg = its.filter(function (q) { return /^\\d{1,2}$/.test(q.t) && Math.abs(q.y - h.y) <= 4 && q.x < h.x && h.x - q.x < 35; })\n          .sort(function (a, b) { return a.x - b.x; }).map(function (q) { return q.t; }).join('');\n        tat = Number(dg) || 0;\n      }\n      out.push({ page: page, m: c.t, om: om.t.match(/AO-[A-Z0-9]+/)[0], kw: Number(k[2]) / 10, s: k[1] + 'シリーズ',\n                 pw: pw ? pw.t.match(/^単相(100|200)V/)[0] : (k[5] === '2' ? '単相200V' : '単相100V'),\n                 tat: tat, y: pr ? yen(pr.t) : 0, open: !pr, year: k[3] });\n    });\n    return out;\n  }\n\n  function genRoomFinish(list) {\n    // 同じ形名が何か所にもあるときは、畳数の読めたもの → 値段のあるもの の順に採る\n    var by = {}, order = [];\n    list.forEach(function (x) {\n      var v = by[x.m];\n      if (!v) { by[x.m] = x; order.push(x.m); return; }\n      var score = function (e) { return (e.tat ? 2 : 0) + (e.y ? 1 : 0); };\n      if (score(x) > score(v)) by[x.m] = x;\n    });\n    var rows = [], pages = {};\n    order.forEach(function (m) {\n      var x = by[m];\n      pages[x.page] = 1;\n      var cold = x.s === 'ZNシリーズ' || x.s === 'DNシリーズ';\n      var cap = x.kw.toFixed(1) + 'kW' + (x.tat ? '（おもに' + x.tat + '畳）' : '');\n      var opt = [x.open ? 'オープン価格' : '', cold ? '寒冷地仕様（暖房強化型）' : '', x.year === '5' ? '2025年モデル' : ''].filter(Boolean).join('／');\n      rows.push({ m: m, hp: cap, y: x.y, u: String(x.page), s: x.s, i: '壁掛形', ab: cap, pw: x.pw, rc: 'ワイヤレス', tp: 'シングル',\n                  opt: opt, om: x.om, im: m, pm: '', rm: '' });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: 'ゼネラル',\n        brand: 'ルームエアコン（住宅設備用）',\n        source: '住宅設備用カタログ 2026（No.A674A・公開PDF）',\n        note: '希望小売価格（税別。事業者向けの積算見積価格）。配管/据付工事費は含まず。オープン価格の機種は値段0。ZN・DNは寒冷地仕様（暖房強化型）の2025年モデル。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['Xシリーズ', 'Zシリーズ', 'NHシリーズ', 'Wシリーズ', 'Lシリーズ', 'VHシリーズ', 'RHシリーズ', 'CHシリーズ', 'ZNシリーズ', 'DNシリーズ'],\n        typeOrder: ['シングル']\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     ゼネラル ルームエアコンの別売品（住宅設備用カタログ 2026・紙面37ページ＝PDFの20ページ ほか）\n\n     ①「別売品について」の表：列＝品番（［OP-J02A］…）、上に品名、下に「希望小売価格（税込）」の値段、\n       行＝シリーズ、マス＝「●」「ー」「2.2・2.5kWクラス」「2.8～9.0kWクラス」\n     ②寒冷地仕様（暖房強化型）の防雪フード：前面・側面・背面の3列。品番の横か下に「（ZNシリーズ2.8～7.1kWクラス）」\n     ③日晴金属の架台・金具（2段架台・平地架台・防雪屋根・壁面用金具・壁面用防雪屋根）\n     ④シリーズのページの「別売品」の欄の抗菌空清フィルター（APS-…）。この欄は字が化けて品名が読めないので、\n       紙面の絵で確かめた品名「抗菌空清フィルター」を APS- の品番に付ける。付くのはそのページの機種のシリーズ（2026-09-12）\n     値段は税込だけ → 1.1で割る\n     -------------------------------------------------------------------- */\n  function genRoomOptReadPage(items, page) {\n    // 見えない字はすき間に、全角の数字は半角に（「ZNシリーズ２.８～7.1kWクラス」）\n    function tidy(t) {\n      return String(t).split('').map(function (ch) {\n        var n = ch.charCodeAt(0);\n        return n < 32 ? ' ' : (n >= 0xFF10 && n <= 0xFF19) ? String.fromCharCode(n - 0xFEE0) : ch;\n      }).join('').replace(/\\s+/g, '');\n    }\n    var its = [];\n    items.forEach(function (o) { var t = tidy(o.s); if (t) its.push({ t: t, x: o.x, y: o.y, w: o.w || 0 }); });\n    var out = [];\n    function taxIn(t) { var m = String(t).match(/([\\d,]+)円/); return m ? Math.round(yen(m[1]) / 1.1) : 0; }\n\n    /* ① 別売品の表 */\n    var heads = its.filter(function (o) { return /^［OP-[A-Z0-9]+］$/.test(o.t); }).sort(function (a, b) { return a.x - b.x; });\n    if (heads.length >= 3) {\n      var colX = heads.map(function (h) { return h.x; });\n      var colOf = function (x) {\n        var ci = -1, bd = 46;\n        colX.forEach(function (cx, i) { var d = Math.abs(x - cx); if (d < bd) { bd = d; ci = i; } });\n        return ci;\n      };\n      var hy = Math.min.apply(null, heads.map(function (h) { return h.y; }));\n      // 品名は品番のすぐ上（「風向ガイド」「（室外機用）」、「リモコンホルダー」「（部品扱い）」）。注の印（※・☆）は除く\n      var names = heads.map(function (h) {\n        return its.filter(function (q) { var dy = q.y - h.y; return dy > 1 && dy <= 20 && q.x >= h.x - 12 && q.x < h.x + 70 && !/^[※☆＊]/.test(q.t); })\n          .sort(function (a, b) { return b.y - a.y || a.x - b.x; }).map(function (q) { return q.t; }).join('');\n      });\n      // 値段の行（「希望小売価格」「（税込）」「17,600円」…「オープン価格」）\n      // リモコンホルダーの品番だけ9下にあるので、いちばん下の品番から80下まで探す（値段の行はそこから50下）\n      var pl = its.filter(function (o) { return /（税込）/.test(o.t) && o.y < hy && hy - o.y < 80; })[0];\n      var vals = heads.map(function () { return ''; });\n      if (pl) {\n        its.forEach(function (o) {\n          if (Math.abs(o.y - pl.y) > 2 || !(/円$/.test(o.t) || /オープン価格/.test(o.t))) return;\n          var ci = colOf(o.x);\n          if (ci >= 0 && !vals[ci]) vals[ci] = o.t;\n        });\n        its.forEach(function (r) {\n          if (!/^[A-Z]{1,2}シリーズ$/.test(r.t) || r.x > colX[0] - 20 || r.y >= pl.y || pl.y - r.y > 120) return;\n          var cells = colX.map(function () { return ''; });\n          its.forEach(function (q) {\n            if (Math.abs(q.y - r.y) > 2 || q.x <= r.x + 20) return;\n            var ci = colOf(q.x);\n            if (ci >= 0) cells[ci] += q.t;\n          });\n          cells.forEach(function (t, ci) {\n            if (!t || /^[ー―－-]+$/.test(t)) return;\n            var fits = /●/.test(t) ? [{ rs: { s: r.t } }] : panaKwFits([r.t], t.replace(/クラス/g, ''));\n            if (!fits.length) return;\n            var open = /オープン/.test(vals[ci]);\n            out.push({ page: page, code: heads[ci].t.replace(/[［］]/g, ''), y: open ? 0 : taxIn(vals[ci]), fits: fits,\n                       name: names[ci] + (open ? '（オープン価格）' : '') });\n          });\n        });\n      }\n    }\n\n    /* ② 寒冷地仕様の防雪フード\n       前面・側面・背面の3列。列の見出し（「前面防雪フード」）は3つが同じ高さに並ぶ。\n       注記（「＊室外機直付け。前面・背面防雪フード」）も「防雪フード」で終わるので、\n       いちばん近いものを取ると側面の列が「背面」になってしまう（2026-09-12に判明）。\n       だから「前面・側面・背面が1つずつ同じ高さに並ぶ行」＝見出しの行だけを見出しとして使う。 */\n    var hoodHeads = (function () {\n      var cand = its.filter(function (q) { return /^(前面|側面|背面)防雪フード$/.test(q.t); });\n      var best = [];\n      cand.forEach(function (q) {\n        var row = cand.filter(function (r) { return Math.abs(r.y - q.y) <= 2; });\n        var kinds = {};\n        row.forEach(function (r) { kinds[r.t.slice(0, 2)] = (kinds[r.t.slice(0, 2)] || 0) + 1; });\n        if (kinds['前面'] === 1 && kinds['側面'] === 1 && kinds['背面'] === 1 && row.length > best.length) best = row;\n      });\n      return best;\n    })();\n    its.forEach(function (c) {\n      if (!/^OP-J07[A-Z]{2}$/.test(c.t)) return;\n      var pr = its.filter(function (q) { var dy = c.y - q.y; return /（税込）[\\d,]+円/.test(q.t) && dy > 0 && dy <= 32 && q.x >= c.x && q.x - c.x < 60; })\n        .sort(function (a, b) { return (c.y - a.y) - (c.y - b.y); })[0];\n      if (!pr) return;\n      var txt = its.filter(function (q) { return q.y <= c.y + 2 && q.y > pr.y + 1 && q.x > c.x + 10 && q.x < c.x + 128; })\n        .sort(function (a, b) { return b.y - a.y || a.x - b.x; }).map(function (q) { return q.t; }).join('');\n      var fits = [], re = /(ZN|DN)シリーズ([\\d.・～〜]+)kWクラス/g, m;\n      while ((m = re.exec(txt))) fits = fits.concat(panaKwFits([m[1] + 'シリーズ'], m[2] + 'kW'));\n      if (!fits.length) return;\n      var lab = hoodHeads.filter(function (q) { return q.y > c.y && Math.abs(q.x - c.x - 40) < 60; })\n        .sort(function (a, b) { return Math.abs(a.x - c.x - 40) - Math.abs(b.x - c.x - 40); })[0];\n      // 見出しの行が見つからないとき（紙面の作りが変わった）だけ、いちばん近いものを使う\n      if (!lab) lab = its.filter(function (q) { return /防雪フード$/.test(q.t) && q.y > c.y && q.y - c.y < 100 && Math.abs(q.x - c.x - 40) < 40; })\n        .sort(function (a, b) { return a.y - b.y; })[0];\n      out.push({ page: page, code: c.t, y: taxIn(pr.t), fits: fits, name: '室外機用 防雪部材 ' + (lab ? lab.t : '防雪フード') + '（寒冷地仕様用）' });\n    });\n\n    /* ③ 日晴金属の架台・金具（どの機種にも） */\n    its.forEach(function (c) {\n      if (!/^C-[A-Z0-9]+(-[A-Z0-9]+)?$/.test(c.t)) return;\n      var pr = its.filter(function (q) { var dy = c.y - q.y; return /（税込）[\\d,]+円/.test(q.t) && dy > 0 && dy <= 14 && q.x >= c.x && q.x - c.x < 60; })[0];\n      if (!pr) return;\n      var lab = its.filter(function (q) { var dy = q.y - c.y; return dy > 30 && dy < 80 && Math.abs(q.x - c.x) <= 10 && /[ぁ-んァ-ヶ一-龥]/.test(q.t) && !/荷重/.test(q.t); })\n        .sort(function (a, b) { return b.y - a.y; }).map(function (q) { return q.t; }).join('');\n      var tail = its.filter(function (q) { return Math.abs(q.y - c.y) <= 2 && q.x > c.x && q.x - c.x < 60 && /^（/.test(q.t); }).map(function (q) { return q.t; }).join('');\n      out.push({ page: page, code: c.t, y: taxIn(pr.t), fits: [{ all: true }], name: '室外機用 日晴金属製 ' + (lab || '架台') + tail });\n    });\n\n    /* ④ シリーズのページの抗菌空清フィルター（品名の字が化けている） */\n    var pageSeries = null;\n    its.forEach(function (c) {\n      if (!/^APS-[A-Z0-9]+$/.test(c.t)) return;\n      var pr = its.filter(function (q) { var dy = c.y - q.y; return /^\\d{1,3},\\d{3}$/.test(q.t) && dy > 3 && dy <= 12 && q.x - c.x > 20 && q.x - c.x < 60; })[0];\n      if (!pr) return;\n      if (!pageSeries) {\n        var seen = {};\n        pageSeries = genRoomReadPage(items, page).map(function (r) { return r.s; }).filter(function (s) { if (seen[s]) return false; seen[s] = 1; return true; });\n      }\n      if (!pageSeries.length) return;\n      var pc = its.filter(function (q) { return /^\\d{10}$/.test(q.t) && Math.abs(q.y - c.y) <= 2 && q.x > c.x; }).map(function (q) { return q.t; })[0];\n      out.push({ page: page, code: c.t, y: Math.round(yen(pr.t) / 1.1), fits: pageSeries.map(function (s) { return { rs: { s: s } }; }),\n                 name: '抗菌空清フィルター' + (pc ? '（部品コード ' + pc + '）' : '') });\n    });\n    return out;\n  }\n\n  function genRoomOptFinish(list) {\n    return optResult(list.filter(function (o) { return o.code; }), 'ゼネラル', 'ルームエアコン（住宅設備用） 別売品');\n  }\n\n  /* --------------------------------------------------------------------\n     日立 ルームエアコン（住宅設備用エアコン 2026-3・88ページ）\n\n     1台ぶんは列のかたまり（1ページに3列）：\n       室内機の形名（＝セットの形名）「RAS-XJ2226S（W）」／「室外機 RAC-XJ2226S」／「6 畳程度」／「室内 単相100V15A」\n       その左下にセットの値段「430,000 円（税別）」。オープン価格の機種（AJ・XK・RK・FD）は「オープン価格」\n     天井カセット（RAP-K28SD）・壁埋込（RAJ-A25SD）は「本体」「別売化粧パネル」（「別売前面グリル」「別売据付木枠」）と「合計」。\n     マルチ：室内機（RAM-SE22S・RAM-PS25S・RAM-JA25S…）と室外機（RAC-45M2SD：M2＝2部屋用）。\n     耐塩害仕様（末尾E）・耐重塩害仕様（末尾J）は70・71ページの一覧（中身は元のセットと同じで、値段と室外機が違う）。\n     値段は紙面が税別なので、そのまま入れる\n     -------------------------------------------------------------------- */\n  function hiKind(code) {\n    var m;\n    if ((m = code.match(/^RAS-([A-Z]{2})(\\d{2})\\d{2}[SD]$/))) return { s: m[1] + 'シリーズ', i: '壁掛形', kw: Number(m[2]) / 10 };\n    if ((m = code.match(/^RAF-D(\\d{2})F\\d?$/))) return { s: 'FDシリーズ', i: '床置形', kw: Number(m[1]) / 10 };\n    if ((m = code.match(/^RAP-([KSA])(\\d{2})SD$/))) return { s: 'P' + m[1] + 'シリーズ', i: '天井カセット形（1方向）', kw: Number(m[2]) / 10 };\n    if ((m = code.match(/^RAJ-A(\\d{2})SD$/))) return { s: 'JAシリーズ', i: '壁埋込形', kw: Number(m[1]) / 10 };\n    if ((m = code.match(/^RAM-(SE|SA|PS|PA|JA)(\\d{2})S$/))) {\n      return { s: 'マルチ用室内機', multi: true, kw: Number(m[2]) / 10,\n               i: { SE: '壁掛形', SA: '壁掛形', PS: '天井カセット形（1方向）', PA: '天井カセット形（1方向）', JA: '壁埋込形' }[m[1]] };\n    }\n    return null;\n  }\n\n  function hiRoomReadPage(items, page) {\n    function clean(t) { return String(t).replace(/[\\u0000-\\u001f]/g, ' ').replace(/\\s+/g, ' ').trim(); }\n    var its = items.map(function (o) { return { s: clean(o.s), x: o.x, y: o.y, w: o.w || 0 }; }).filter(function (o) { return o.s; });\n    // 割れた形名をつなぐ（71ページ「RAS-AJ71」「26DE」、33ページ「RAS-BJ71」「26」「D」）。すき間なく右に続く字だけ\n    its.forEach(function (o) {\n      if (!o.s || !/^（?(RAS|RAC)-[A-Z]{2}\\d{2}$/.test(o.s)) return;\n      var px = o.x, pw = o.w;\n      its.filter(function (q) { return q !== o && q.s && Math.abs(q.y - o.y) < 1.5 && q.x > o.x && q.x - o.x < 70 && /^[\\dA-Z]{1,4}）?$/.test(q.s); })\n        .sort(function (p, q) { return p.x - q.x; })\n        .forEach(function (q) {\n          if (/\\d{4}[SD][EJ]?）?$/.test(o.s)) return;\n          var gap = pw ? q.x - (px + pw) : q.x - px - 30;\n          if (gap < -4 || gap > 8) return;\n          o.s += q.s; q.s = ''; px = q.x; pw = q.w;\n        });\n    });\n    its = its.filter(function (o) { return o.s; });\n    var all = its.map(function (o) { return o.s; }).join(' ');\n    if (!/円|オープン価格/.test(all)) return [];\n    function money(o) {\n      var m = o.s.match(/^([\\d,]{4,})\\s*円/);\n      if (m) return yen(m[1]);\n      if (/^[\\d,]{4,}$/.test(o.s) && its.some(function (q) { return /^円/.test(q.s) && Math.abs(q.y - o.y) < 2 && q.x > o.x && q.x - o.x < 60; })) return yen(o.s);\n      return 0;\n    }\n    function isCode(o) { return !!hiKind(o.s); }\n    var out = [];\n    // 耐塩害の一覧表（70・71ページ）は下でまとめて読む。ここの形名はセットとして読まない\n    var saltPage = /製品年度/.test(all) && /耐塩害/.test(all);\n\n    its.forEach(function (a) {\n      var kind = hiKind(a.s);\n      if (!kind || saltPage) return;\n      // 同じ列の、下の段の形名（そこより下はよその機種）\n      var nextY = its.filter(function (o) { return o !== a && Math.abs(o.x - a.x) < 20 && o.y < a.y - 20 && isCode(o); })\n        .map(function (o) { return o.y; }).sort(function (p, q) { return q - p; })[0];\n      function above(o) { return nextY == null || o.y > nextY + 2; }\n      var body = 0, total = 0, open = false, extras = [];\n\n      if (kind.multi) {\n        // マルチ用の室内機：形名の下60までに「203,000 円」、別売化粧パネル、合計\n        var mb = its.filter(function (o) { return o.x >= a.x - 45 && o.x <= a.x + 60 && a.y - o.y > 0 && a.y - o.y <= 60 && above(o); });\n        var mn = mb.filter(function (o) { return money(o) > 0 && o.x >= a.x - 5; }).sort(function (p, q) { return q.y - p.y; });\n        if (!mn.length) return;\n        body = money(mn[0]);\n        var g0 = mb.filter(function (o) { return /^合/.test(o.s); })[0];\n        if (g0) { var t0 = mn.filter(function (o) { return Math.abs(o.y - g0.y) < 3; })[0]; if (t0) total = money(t0); }\n        if (mb.some(function (o) { return /KPS$|別売化粧/.test(o.s); })) extras.push('化粧パネル');\n        if (mb.some(function (o) { return /RAJ-FGF|前面/.test(o.s); })) extras.push('前面グリル');\n        if (mb.some(function (o) { return /RAJ-WFF|木枠/.test(o.s); })) extras.push('据付木枠');\n        out.push({ page: page, m: a.s, kw: kind.kw, tat: 0, s: kind.s, i: kind.i, pw: '室外機から', tp: 'マルチ用室内機', rc: 'ワイヤレス',\n                   y: total || body, opt: extras.length ? extras.join('・') + '込み' : '', om: '', im: a.s, pm: '' });\n        return;\n      }\n\n      // セット：左下（形名の50〜150下、左78〜5）にセットの値段か「オープン価格」\n      var band = its.filter(function (o) { return o.x >= a.x - 78 && o.x <= a.x - 5 && a.y - o.y >= 50 && a.y - o.y <= 150 && above(o); });\n      open = band.some(function (o) { return /^オープン価格/.test(o.s); });\n      var nums = band.filter(function (o) { return money(o) > 0; }).sort(function (p, q) { return q.y - p.y; });\n      if (nums.length) {\n        body = money(nums[0]);\n        var gl = band.filter(function (o) { return /^合/.test(o.s); })[0];\n        if (gl) { var tn = nums.filter(function (o) { return Math.abs(o.y - gl.y) < 3; })[0]; if (tn) total = money(tn); }\n      } else if (!open) {\n        // セットの値段の字が無いときは、室内と室外の値段を足す（23ページ RAS-XJ9026D は 324,000＋486,000）\n        var io2 = its.filter(function (o) { return o.x >= a.x - 5 && o.x <= a.x + 40 && a.y - o.y >= 50 && a.y - o.y <= 150 && above(o) && money(o) > 0; })\n          .sort(function (p, q) { return q.y - p.y; });\n        if (io2.length < 2) return;\n        body = money(io2[0]) + money(io2[1]);\n      }\n      if (!body && !open) return;\n      band.forEach(function (o) {\n        if (/^別売化粧パネル/.test(o.s)) extras.push('化粧パネル');\n        if (/^別売前面グリル/.test(o.s)) extras.push('前面グリル');\n        if (/^別売据付木枠/.test(o.s)) extras.push('据付木枠');\n      });\n      // 室外機・畳数・電源は形名のすぐ下\n      var near = its.filter(function (o) { return o.x >= a.x - 60 && o.x <= a.x + 120 && a.y - o.y > 2 && a.y - o.y <= 25; });\n      var om = '';\n      near.forEach(function (o) { var m = o.s.match(/(RAC-[A-Z0-9]+)/); if (!om && m && o.x >= a.x - 5 && o.x <= a.x + 40) om = m[1]; });\n      // 室外機の形名が真下に無いのは、4ページのような機種のまとめ表（値段は別の機種のもの）\n      if (!om) return;\n      var pw = '';\n      near.forEach(function (o) { var m = o.s.match(/単相\\s*(100|200)V/); if (!pw && m) pw = '単相' + m[1] + 'V'; });\n      var tat = 0;\n      var tj = near.filter(function (o) { return /^畳程度/.test(o.s) && o.x >= a.x - 32 && o.x <= a.x - 8; })[0];\n      if (tj) {\n        var dg = near.filter(function (o) { return Math.abs(o.y - tj.y) < 2.5 && o.x >= a.x - 58 && o.x < tj.x && /^\\d{1,2}$/.test(o.s); })\n          .sort(function (p, q) { return p.x - q.x; }).map(function (o) { return o.s; }).join('');\n        if (dg) tat = Number(dg);\n      }\n      out.push({ page: page, m: a.s, kw: kind.kw, tat: tat, s: kind.s, i: kind.i, pw: pw || '単相200V', tp: 'シングル', rc: 'ワイヤレス',\n                 y: open ? 0 : (total || body),\n                 opt: [open ? 'オープン価格' : '', extras.length ? extras.join('・') + '込み' : ''].filter(Boolean).join('／'),\n                 om: om, im: a.s, pm: '' });\n    });\n\n    // マルチの室外機（50ページ「RAC-45M2SD  323,000 円」。M2＝2部屋用）\n    its.forEach(function (a) {\n      var m = a.s.match(/^RAC-(\\d{2})M(\\d)SD$/);\n      if (!m) return;\n      var pr = its.filter(function (o) { return Math.abs(o.y - a.y) < 3 && o.x > a.x && o.x - a.x < 230 && money(o) > 0; })\n        .sort(function (p, q) { return p.x - q.x; })[0];\n      if (!pr) return;\n      out.push({ page: page, m: a.s, kw: Number(m[1]) / 10, tat: 0, s: 'システムマルチ（室外機）', i: 'マルチ室外機', pw: '単相200V',\n                 tp: m[2] + '室用', rc: '', y: money(pr), opt: '', om: a.s, im: '', pm: '' });\n    });\n\n    // 耐塩害仕様（末尾E）・耐重塩害仕様（末尾J）の一覧（70・71ページ）。値段は形名の右、次の形名の手前の、すぐ下の行\n    if (/耐塩害仕様/.test(all)) {\n      var sc = its.filter(function (o) { return /^RAS-[A-Z]{2}\\d{4}[SD][EJ]$/.test(o.s); });\n      sc.forEach(function (a) {\n        var nx = 1e9;\n        its.forEach(function (o) { if (o !== a && Math.abs(o.y - a.y) < 1.5 && o.x > a.x && /^RAS-/.test(o.s) && o.x < nx) nx = o.x; });\n        var win = its.filter(function (o) { return o.x > a.x && o.x < nx && a.y - o.y >= 0 && a.y - o.y < 7; });\n        var pr = win.filter(function (o) { return money(o) > 0; }).sort(function (p, q) { return p.x - q.x; })[0];\n        var op = !pr && win.some(function (o) { return /^オープン価格/.test(o.s); });\n        if (!pr && !op) return;\n        var oc = its.filter(function (o) { return Math.abs(o.x - (a.x - 3)) < 8 && a.y - o.y > 2 && a.y - o.y < 10 && /RAC-/.test(o.s); })[0];\n        out.push({ page: page, salt: a.s.slice(-1), m: a.s, base: a.s.slice(0, -1), y: pr ? money(pr) : 0, open: !pr,\n                   om: oc ? oc.s.replace(/[（）()]/g, '') : '' });\n      });\n    }\n    return out;\n  }\n\n  function hiRoomFinish(sets) {\n    var rows = [], seen = {}, pages = {}, baseOf = {};\n    sets.forEach(function (x) { if (!x.salt && !baseOf[x.m]) baseOf[x.m] = x; });\n    sets.forEach(function (x) {\n      pages[x.page] = 1;\n      if (x.salt) {\n        // 元のセットの中身（能力・畳数・電源）を写す。元が紙面に無いとき（XJの2025年度）は、同じ能力の別年度から\n        var b = baseOf[x.base] || baseOf[x.base.replace(/(\\d{2})(2[56])([SD])$/, function (_, c, yr, t) { return c + (yr === '25' ? '26' : '25') + t; })];\n        if (!b) return;\n        x = { page: x.page, m: x.m, kw: b.kw, tat: b.tat, s: b.s, i: b.i, pw: b.pw, tp: b.tp, rc: b.rc, y: x.y,\n              opt: [x.salt === 'J' ? '耐重塩害仕様' : '耐塩害仕様', x.open ? 'オープン価格' : '',\n                    (b.opt || '').replace(/オープン価格／?/, '').replace(/／?2025年度モデル/, '')].filter(Boolean).join('／'),\n              om: x.om || (b.om ? b.om + x.salt : ''), im: x.m, pm: '' };\n      }\n      if (seen[x.m]) return;\n      seen[x.m] = 1;\n      // 前の年度の機種（形名の年が25）は見分けがつくように書いておく\n      if (/^RAS-[A-Z]{2}\\d{2}25[SD]/.test(x.m)) x.opt = [x.opt, '2025年度モデル'].filter(Boolean).join('／');\n      var cap = x.kw.toFixed(1) + 'kW' + (x.tat ? '（おもに' + x.tat + '畳）' : '');\n      rows.push({\n        m: x.m, hp: cap, y: x.y, u: String(x.page),\n        s: x.s, i: x.i, ab: cap, pw: x.pw,\n        rc: x.rc, tp: x.tp, opt: x.opt, om: x.om, im: x.im, pm: x.pm, rm: ''\n      });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: '日立',\n        brand: 'ルームエアコン（住宅設備用）',\n        source: '住宅設備用エアコン 2026-3（公開Webカタログ）',\n        note: '希望小売価格・税抜。配管/据付工事費は含まず。AJ・XK・RK・FDシリーズはオープン価格（値段0）。耐塩害仕様は形名の末尾E、耐重塩害仕様はJ。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['XJシリーズ', 'ZJシリーズ', 'VJシリーズ', 'VLシリーズ', 'MJシリーズ', 'AJシリーズ', 'BJシリーズ', 'XKシリーズ', 'RKシリーズ',\n                      'FDシリーズ', 'PKシリーズ', 'PSシリーズ', 'PAシリーズ', 'JAシリーズ', 'システムマルチ（室外機）', 'マルチ用室内機'],\n        typeOrder: ['シングル', '2室用', '3室用', '4室用', 'マルチ用室内機']\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     日立 ルームエアコンの別売品（住宅設備用エアコン 2026-3 の紙面71〜76ページ）\n\n     品物は「カード」（品名・形名・希望小売価格・税抜価格）。値段は\n       「（税抜価格2,000円）」…そのまま／「990 円（税込）」だけの品…÷1.1／「オープン価格」…0\n     付く機種の書かれ方は5つ\n       ① 71ページの「適用一覧表」……行＝シリーズ、列＝形名、●で付く\n       ② 76ページの「据付部品適用一覧表」……行＝機種の形名の並び、列＝形名（「SP-」「BT-2」と縦に割れる）\n       ③ 76ページの防雪フード……機種の並びに番号（1・2、3・4…）、フードの図にも番号。奇数＝標準、偶数＝ステンレス\n       ④ 文……「［適用機種］AJシリーズ・…」「本カタログ掲載のルームエアコンに使用できます」、表の行の左の「XJシリーズ・…」\n       ⑤ ドレンアップキットは「P.53-56のメリット一覧表で『ドレンアップキット対応』の機種」……53〜56ページの表の●\n     シリーズは形名の頭で当てる（{re}）。2025年度の機種は形名の年が25なので、「2025年度」と書いてあれば25で当てる\n     -------------------------------------------------------------------- */\n  var HI_OPT_CODE = /^((?:SP|HA|PSC)-[A-Z0-9]+(?:-[A-Z0-9]+)*|RAC-N\\d{2}S\\d{3})/;\n  var HI_WALL = ['XJ', 'ZJ', 'VJ', 'VL', 'MJ', 'AJ', 'BJ', 'XK', 'RK'];\n  var HI_SERIES_RE = {\n    FD: '^RAF-D', PK: '^RAP-K', PS: '^RAP-S', PA: '^RAP-A', JA: '^RAJ-A',\n    MSE: '^RAM-SE', MSA: '^RAM-SA', MPS: '^RAM-PS', MPA: '^RAM-PA', MJA: '^RAM-JA',\n    AJE: '^RAS-AJ\\\\d{4}[SD]E', AJJ: '^RAS-AJ\\\\d{4}[SD]J'\n  };\n\n  /** 「AJシリーズ・2025年度 AJシリーズ、一方向天井カセットタイプPKシリーズ…」→ [{re}]。\n      「2025年度VJ・VLシリーズ」の2025年度は次の「シリーズ」まで、「2025年度モデル：」は後ろ全部 */\n  function hiSeriesFits(text) {\n    // 形名（「（RAF-D36F」「SP-RC4用」）は先に消す。「D36FXJシリーズ」のようにくっつくと XJ が読めない\n    //（形名は「英字・数字・英字1つ・数字1つ」まで。「RAF-D36F」「XJ…」がくっついても XJ は残す）\n    var t = String(text).replace(/\\s+/g, '').replace(/[A-Z]{2,4}-[A-Z]*\\d+[A-Z]?\\d?/g, '・'), out = [], seen = {};\n    var re = /2025年度(モデル[：:])?|([A-Z]{2,3})(?=・|シリーズ)|シリーズ/g, m, once = false, rest = false;\n    while ((m = re.exec(t))) {\n      if (m[0].indexOf('2025年度') === 0) { if (m[1]) rest = true; else once = true; continue; }\n      if (m[0] === 'シリーズ') { once = false; continue; }\n      var r = HI_WALL.indexOf(m[2]) >= 0 ? '^RAS-' + m[2] + '\\\\d{2}' + ((rest || once) ? '25' : '26') : HI_SERIES_RE[m[2]];\n      if (r && !seen[r]) { seen[r] = 1; out.push({ re: r }); }\n    }\n    return out;\n  }\n\n  /** 「RAS-XJ2226S・XJ2526S…」「RAF-D36F・D40F2・D50F2、」→ 形名の並び。頭（RAS-）は前のものを受け継ぐ */\n  function hiCodeList(text, prefix, st) {\n    var out = [];\n    st = st || {};\n    if (st.p) prefix = st.p;\n    String(text).replace(/\\s+/g, '').replace(/[（）()、用]/g, '・').split('・').forEach(function (t) {\n      var m = t.match(/^((?:RAS|RAF|RAP|RAJ|RAM|RAC)-)?([A-Z0-9]*\\d[A-Z0-9]*)$/);\n      if (!m || !/[A-Z]/.test(m[2]) || m[2].length < 4) return;\n      if (m[1]) prefix = m[1];\n      if (prefix) out.push(prefix + m[2]);\n    });\n    st.p = prefix;\n    return out;\n  }\n\n  function hiNameLike(t) {\n    t = String(t || '');\n    if (t.length < 3 || !/[ぁ-んァ-ヶ一-龥]/.test(t)) return false;\n    if (/^[※＊（(〈●◎☆★・]/.test(t)) return false;\n    if (/\\d/.test(t.replace(/（[^）]*）/g, ''))) return false;\n    if (/(SP|HA|PSC|RA[A-Z])-/.test(t)) return false;\n    return !/シリーズ|据付例|寸法|希望小売価格|税抜|税込|部品番号|適用|ください|です|ます|。|、|について|価格|取扱店|イメージ|注意|付属品|用意|できます/.test(t);\n  }\n\n  function hiRoomOptReadPage(items, page) {\n    function clean(t) { return String(t).replace(/[\\u0000-\\u001f]/g, ' ').replace(/\\s+/g, ' ').trim(); }\n    function nos(t) { return String(t).replace(/\\s+/g, ''); }\n    var raw = items.map(function (o) { return { s: clean(o.s), x: o.x, y: o.y, w: o.w || 0 }; });\n    var blanks = raw.filter(function (o) { return !o.s; });     // 字の間のすき間（見えない字）\n    var its = [], seenIt = {};\n    raw.forEach(function (o) {                                   // 53〜56ページは同じ字が2回ずつ入っている\n      if (!o.s) return;\n      var key = o.s + '|' + o.x.toFixed(1) + '|' + o.y.toFixed(1);\n      if (seenIt[key]) return;\n      seenIt[key] = 1; its.push(o);\n    });\n    var all = nos(its.map(function (o) { return o.s; }).join(' '));\n    var out = [];\n    function endX(o) { return o.x + (o.w || nos(o.s).length * 7); }\n\n    // 行と、行の中ですき間なく続くかたまり（seg）。見えない字をはさんで3以上あいたら切る\n    var lines = [];\n    its.slice().sort(function (a, b) { return b.y - a.y || a.x - b.x; }).forEach(function (o) {\n      for (var i = 0; i < lines.length; i++) if (Math.abs(lines[i].y - o.y) <= 1.5) { lines[i].its.push(o); return; }\n      lines.push({ y: o.y, its: [o] });\n    });\n    var segs = [];\n    lines.forEach(function (L) {\n      L.its.sort(function (a, b) { return a.x - b.x; });\n      var cur = null;\n      L.its.forEach(function (o) {\n        var gap = cur ? o.x - cur.x2 : 99;\n        var sp = cur && gap >= 3 && blanks.some(function (b) { return Math.abs(b.y - L.y) <= 1.5 && b.x >= cur.x2 - 1 && b.x <= o.x + 1; });\n        if (cur && (gap < 3 || (gap <= 8 && !sp))) { cur.its.push(o); cur.x2 = Math.max(cur.x2, endX(o)); o.seg = cur; return; }\n        cur = { y: L.y, x: o.x, x2: endX(o), its: [o] }; o.seg = cur; segs.push(cur);\n      });\n    });\n    segs.forEach(function (g) { g.t = nos(g.its.map(function (o) { return o.s; }).join('')); });\n    function lineOf(o) { for (var i = 0; i < lines.length; i++) if (lines[i].its.indexOf(o) >= 0) return lines[i]; return { its: [o], y: o.y }; }\n\n    // 同じ x の縦のまとまり（段落）。文の段落（。・シリーズ…）の中の字は品名にしない\n    var paras = [];\n    segs.slice().sort(function (a, b) { return b.y - a.y; }).forEach(function (g) {\n      for (var i = 0; i < paras.length; i++) {\n        var P = paras[i];\n        if (Math.abs(P.x - g.x) <= 4 && P.yb - g.y > 0 && P.yb - g.y <= 10.5) { P.segs.push(g); P.yb = g.y; g.para = P; return; }\n      }\n      var NP = { x: g.x, yt: g.y, yb: g.y, segs: [g] }; g.para = NP; paras.push(NP);\n    });\n    paras.forEach(function (P) {\n      P.t = P.segs.map(function (g) { return g.t; }).join('');\n      P.stop = P.segs.some(function (g) { return /シリーズ|。|ください|です|ます|できます/.test(g.t); });\n    });\n\n    /* ---- ⑤ メリット一覧表（53〜56ページ）。「ドレン（アップキット対応）」の列の●と、行のシリーズ名 ---- */\n    if (/シリーズ名/.test(all)) {\n      var mrows = [];\n      lines.forEach(function (L) {\n        var t = nos(L.its.filter(function (o) { return o.x < 330; }).map(function (o) { return o.s; }).join(''));\n        var f = /シリーズ/.test(t) ? hiSeriesFits(t) : [];\n        if (f.length) mrows.push({ y: L.y, fits: f });\n      });\n      if (mrows.length) out.push({ page: page, meritRows: mrows });\n    }\n    its.forEach(function (h) {\n      if (!/^ドレン(アップ)?$/.test(nos(h.s)) || h.y < 600) return;\n      // 56ページは表が2つあり、見出しが下の表の上にある。同じ列の●は高さを問わず採る\n      var ys = its.filter(function (o) { return /^●/.test(o.s) && Math.abs(o.x - h.x) <= 14; }).map(function (o) { return o.y; });\n      if (ys.length >= 3) out.push({ page: page, meritMarks: ys });\n    });\n\n    /* ---- ① 適用一覧表（71ページ）：行＝シリーズ、列＝形名 ---- */\n    var tt = segs.filter(function (g) { return g.t === '適用一覧表'; })[0];\n    if (tt) {\n      var hl = lines.filter(function (L) { return L.y < tt.y && tt.y - L.y <= 20 && L.its.filter(function (o) { return /^SP-/.test(o.s); }).length >= 3; })[0];\n      if (hl) {\n        var hdr = [];\n        hl.its.forEach(function (o, i) {\n          if (!/^SP-/.test(o.s)) return;\n          var code = nos(o.s), nx = hl.its[i + 1];\n          if (nx && /^[A-Z0-9]{1,3}$/.test(nos(nx.s)) && nx.x - o.x < 30) code += nos(nx.s);   // 「SP-VCF1」「1W」\n          hdr.push({ code: code, x: o.x });\n        });\n        var minX = Math.min.apply(null, hdr.map(function (h) { return h.x; }));\n        var rows1 = [];\n        lines.forEach(function (L) {\n          if (L.y >= hl.y - 3) return;\n          var t = nos(L.its.filter(function (o) { return o.x < minX - 5; }).map(function (o) { return o.s; }).join(''));\n          var f = /シリーズ/.test(t) ? hiSeriesFits(t) : [];\n          if (f.length) rows1.push({ y: L.y, fits: f });\n        });\n        its.forEach(function (d) {\n          if (!/^●/.test(d.s) || d.y >= hl.y - 3) return;\n          var row = rows1.filter(function (r) { return Math.abs(r.y - d.y) <= 5; }).sort(function (a, b) { return Math.abs(a.y - d.y) - Math.abs(b.y - d.y); })[0];\n          var col = hdr.filter(function (h) { return h.x <= d.x + 5 && d.x - h.x <= 25; }).sort(function (a, b) { return b.x - a.x; })[0];\n          if (row && col) out.push({ page: page, code: col.code, fits: row.fits });\n        });\n      }\n    }\n\n    /* ---- ② 据付部品適用一覧表（76ページ上）と ③ 防雪フード（76ページ下） ---- */\n    var hood = its.filter(function (o) {\n      return /^■/.test(o.s) && /防雪フード/.test(nos(its.filter(function (q) { return Math.abs(q.y - o.y) <= 1.5 && q.x >= o.x && q.x - o.x < 90; }).map(function (q) { return q.s; }).join('')));\n    })[0];\n    var hoodY = hood ? hood.y : -1;\n    // 形名の並びの行（左はし x60〜75 から）。行末が「・」「、」なら次の行へつづく\n    function chains(yTop, yBot, xMax) {\n      var ls = lines.filter(function (L) { return L.y < yTop && L.y > yBot && L.its.some(function (o) { return o.x >= 60 && o.x <= 75; }); })\n        .map(function (L) {\n          var li = L.its.filter(function (o) { return o.x >= 60 && o.x < xMax && !/^●/.test(o.s); });\n          while (li.length && !/^[A-Z0-9]/.test(nos(li[0].s))) li.shift();   // 「天井カセッ」「ト」の「ト」\n          return { y: L.y, x: li.length ? li[0].x : 66, t: nos(li.map(function (o) { return o.s; }).join('')) };\n        })\n        .filter(function (l) { return l.t && hiCodeList(l.t, 'RAS-').length; });\n      var res = [], cur = null;\n      ls.forEach(function (l) {\n        if (cur && /[・、]$/.test(cur.last)) { cur.ls.push(l); cur.yb = l.y; cur.last = l.t; return; }\n        cur = { yt: l.y, yb: l.y, x: l.x, ls: [l], last: l.t }; res.push(cur);\n      });\n      res.forEach(function (c) { var st = {}; c.codes = []; c.ls.forEach(function (l) { c.codes = c.codes.concat(hiCodeList(l.t, 'RAS-', st)); }); });\n      return res.filter(function (c) { return c.codes.length; });\n    }\n    function ccFits(codes) { return codes.map(function (c) { return { cc: c }; }); }\n\n    var t2 = segs.filter(function (g) { return /据付部品適用一覧表/.test(g.t); })[0];\n    if (t2) {\n      var hdr2 = [];\n      its.forEach(function (o) {\n        if (o.y >= t2.y || t2.y - o.y > 60) return;\n        var t = nos(o.s);\n        if (/^(SP|RAC)-$/.test(t)) {\n          var code = t;\n          its.filter(function (q) { return q !== o && Math.abs(q.x - o.x) <= 4 && o.y - q.y >= 2 && o.y - q.y <= 12 && /^[A-Z0-9-]+$/.test(nos(q.s)); })\n            .sort(function (a, b) { return b.y - a.y; })\n            .forEach(function (q) {\n              code += nos(q.s);\n              its.filter(function (r) { return Math.abs(r.y - q.y) < 1.5 && r.x > q.x && r.x - q.x < 15 && /^[A-Z0-9]{1,3}$/.test(nos(r.s)); })\n                .forEach(function (r) { code += nos(r.s); });\n            });\n          if (HI_OPT_CODE.test(code)) hdr2.push({ code: code, x: o.x, y: o.y });\n        } else if ((t.match(HI_OPT_CODE) || [])[0] === t) {\n          hdr2.push({ code: t, x: o.x, y: o.y });\n        }\n      });\n      if (hdr2.length >= 3) {\n        var hy = Math.min.apply(null, hdr2.map(function (h) { return h.y; }));\n        var ch2 = chains(hy - 3, hoodY, 258);\n        its.forEach(function (d) {\n          if (!/^●/.test(d.s) || d.y >= hy - 3 || d.y <= hoodY) return;\n          var c = ch2.filter(function (k) { return d.y <= k.yt + 4 && d.y >= k.yb - 4; })\n            .sort(function (a, b) { return Math.abs((a.yt + a.yb) / 2 - d.y) - Math.abs((b.yt + b.yb) / 2 - d.y); })[0];\n          var col = hdr2.filter(function (h) { return h.x <= d.x + 5 && d.x - h.x <= 20; }).sort(function (a, b) { return b.x - a.x; })[0];\n          if (c && col) out.push({ page: page, code: col.code, fits: ccFits(c.codes) });\n        });\n      }\n    }\n\n    if (hood) {\n      // 番号（「1」「1」と割れる2けたもつなぐ）\n      var nums = [];\n      its.filter(function (o) { return /^\\d$/.test(o.s) && o.y < hoodY; }).sort(function (a, b) { return b.y - a.y || a.x - b.x; }).forEach(function (o) {\n        var last = nums[nums.length - 1];\n        // 右どなり（6まで）の数字だけつなぐ。同じ高さの右の図の番号（x379）をつながないように\n        if (last && Math.abs(last.y - o.y) < 1.5 && o.x > last.lx && o.x - last.lx <= 6) { last.n = last.n * 10 + Number(o.s); last.lx = o.x; return; }\n        nums.push({ x: o.x, y: o.y, lx: o.x, n: Number(o.s) });\n      });\n      var ch3 = chains(hoodY, -1, 195);   // 右のフードの図の番号（x198〜）はまぜない\n      ch3.forEach(function (c) {\n        var ns = nums.filter(function (n) { return n.x >= c.x - 20 && n.x <= c.x - 5 && n.y >= c.yb - 5 && n.y <= c.yt + 5; }).map(function (n) { return n.n; });\n        c.fig = ns.filter(function (n) { return n % 2 === 1; }).sort(function (a, b) { return a - b; })[0] || 0;\n      });\n      its.forEach(function (o) {\n        var m = nos(o.s).match(/^SP-BF-[A-Z0-9-]+/);\n        if (!m || o.y >= hoodY) return;\n        // 図の番号はフードの組のいちばん上の2つの間。形名より上（y が大きい）のうち、いちばん近いもの\n        var lab = nums.filter(function (n) { return n.x >= o.x - 20 && n.x <= o.x - 5 && n.y >= o.y - 6; }).sort(function (a, b) { return a.y - b.y; })[0];\n        if (!lab) return;\n        var f = lab.n % 2 === 1 ? lab.n : lab.n - 1;\n        ch3.filter(function (c) { return c.fig === f; }).forEach(function (c) { out.push({ page: page, code: m[0], fits: ccFits(c.codes) }); });\n      });\n    }\n\n    /* ---- カード（形名と値段）。値段の書き方のあるページだけ ---- */\n    if (!/税抜価格|（税込）/.test(all)) return out;\n    var anchors = [];\n    segs.forEach(function (g) {\n      var pos = [], acc = '';\n      g.its.forEach(function (o) { pos.push(acc.length); acc += nos(o.s); });\n      function itemAt(k) { var j = 0; for (var i = 0; i < pos.length; i++) if (pos[i] <= k) j = i; return g.its[j]; }\n      var m, re = /税抜価格([\\d,]+)円/g, got = false;\n      while ((m = re.exec(g.t))) { got = true; anchors.push({ x: itemAt(m.index).x, y: g.y, v: yen(m[1]) }); }\n      if (!got) { re = /([\\d,]{3,})円（税込）/g; while ((m = re.exec(g.t))) anchors.push({ x: itemAt(m.index).x, y: g.y, v: Math.round(yen(m[1]) / 1.1) }); }\n      var k = g.t.search(/オープン価格(?!商品)/);\n      if (k >= 0) anchors.push({ x: itemAt(k).x, y: g.y, v: 0, open: true });\n    });\n    var CIRC = /^[①-⑳]/;\n    function circTitle(g) {   // 「①空気清浄フィルター」→「空気清浄フィルター」。「⑥白くまくんアプリ用」は見出しではない\n      // 字間のあいた見出し（「④ナノチタン除菌・脱臭空清フィルター」）は、かたまりに割れても行の字でつなぐ\n      var ln = lineOf(g.its[0]).its, i0 = ln.indexOf(g.its[0]), t = '', rx = null;\n      for (var i = i0; i < ln.length; i++) {\n        var q = ln[i], qt = nos(q.s);\n        if (i > i0 && (q.x - rx > 12 || CIRC.test(qt) || HI_OPT_CODE.test(qt) || /価格|円/.test(qt))) break;\n        t += qt; rx = endX(q);\n      }\n      t = t.replace(CIRC, '');\n      var k = t.search(/(SP|HA|PSC|RAC)-/);\n      if (k >= 0) t = t.slice(0, k);\n      return /[ぁ-んァ-ヶ一-龥]/.test(t) && !/用$/.test(t) ? t : '';\n    }\n    function titleText(h) {\n      var t = nos(h.s).replace(/^■/, '').replace(/^別売/, '');\n      if (!t) {\n        var nx = its.filter(function (q) { return Math.abs(q.y - h.y) < 1.5 && q.x > h.x && q.x - h.x < 60; }).sort(function (a, b) { return a.x - b.x; })[0];\n        t = nx ? nos(nx.s) : '';\n      }\n      return t.replace(/の据付(工事)?について$|について$/, '');\n    }\n    function sectionOf(x, y) {\n      var best = null;\n      its.forEach(function (o) {\n        if (!/^■/.test(o.s) || o.y <= y + 1.5 || o.x > x + 15) return;\n        if (!best || o.y < best.y || (o.y === best.y && o.x > best.x)) best = o;\n      });\n      return best;\n    }\n    // 「適用機種」の文（その■見出しの中の品物に付ける）\n    var stmts = [];\n    its.forEach(function (g) {        // 「［適用機種］」の字そのものの位置から（同じかたまりの左の「ホテル、…」はまぜない）\n      if (!/適用機種/.test(nos(g.s))) return;\n      // その行から下へ、同じ左はしで続く行（10.5まで）。行の中の字はぜんぶ（かたまりに割れていても）。右の別の表の行はとばす\n      var ls = lines.filter(function (L) { return L.y <= g.y + 1; }).sort(function (a, b) { return b.y - a.y; });\n      var t = '', py = null;\n      for (var i = 0; i < ls.length; i++) {\n        var li = ls[i].its.filter(function (q) { return q.x >= g.x - 6; });\n        if (!li.length || Math.abs(li[0].x - g.x) > 6) continue;\n        if (py !== null && (py - ls[i].y > 10.5 || /^[●※]/.test(li[0].s))) break;\n        t += nos(li.map(function (q) { return q.s; }).join(''));\n        py = ls[i].y;\n      }\n      var f = /メリット一覧表/.test(t) && /ドレンアップ/.test(t) ? [{ merit: 'drain' }] : hiSeriesFits(t);\n      if (f.length) stmts.push({ h: sectionOf(g.x, g.y), fits: f });\n    });\n    var ownPower = /本体から電源供給|別電源不要/.test(all);\n\n    its.forEach(function (o) {\n      var t = nos(o.s), m = t.match(HI_OPT_CODE);\n      if (!m) return;\n      var code = m[1];\n      if (/^(\\(P\\.|（P\\.|用|を|は|の|など)/.test(t.slice(code.length))) return;\n      var c = { code: code, x: o.x, y: o.y };\n      // 値段：下に45まで・同じ行の右300まで。いちばん近いもの\n      var best = null;\n      anchors.forEach(function (a) {\n        var dy = c.y - a.y, dx = a.x - c.x;\n        if (dy < -6 || dy > 45 || dx < -90 || dx > 300) return;\n        var d = Math.abs(dy) + 0.3 * Math.abs(dx);\n        if (!best || d < best.d) best = { d: d, a: a };\n      });\n\n      // 同じ行の、形名のすぐ左の字（「別売延長コード（8m）」「HEMSアダプター」「⑥白くまくんアプリ用」）と右の字（「（吹出口フード）」「（W）（単相100V用）」）\n      // 同じ行の字（76ページの「SP-BF-EB」と「（背面吸込口フード）」は高さが2ずれる）\n      var ln = its.filter(function (q) { return Math.abs(q.y - o.y) <= 2.5; }).sort(function (a, b) { return a.x - b.x; }), i0 = ln.indexOf(o);\n      var left = '', lx = o.x, li = null;\n      for (var i = i0 - 1; i >= 0; i--) {\n        var q = ln[i], qt = nos(q.s);\n        if (lx - endX(q) > (left ? 12 : 30) || HI_OPT_CODE.test(qt) || /価格|円|（税/.test(qt)) break;\n        left = qt + left; lx = q.x; li = q;\n      }\n      left = left.replace(CIRC, '').replace(/^■/, '').replace(/^別売/, '');\n      if (!hiNameLike(left)) { left = ''; li = null; }\n      var rest = t.slice(code.length), rx = endX(o);\n      for (var j = i0 + 1; j < ln.length; j++) {\n        var r2 = ln[j], rt = nos(r2.s);\n        if (r2.x - rx > 12 || HI_OPT_CODE.test(rt) || /^(希望小売価格|[\\d,]+|オープン|（税|円)/.test(rt)) break;\n        rest += rt; rx = endX(r2);\n      }\n      rest = rest.split(/希望小売価格|[\\d,]{3,}円|オープン価格|（税/)[0].replace(/[（(]別売[)）]/, '');\n      if ((rest.match(/[（(]/g) || []).length > (rest.match(/[）)]/g) || []).length) rest += '）';\n      if (!/[ぁ-んァ-ヶ一-龥（(]/.test(rest)) rest = '';\n      if (!left) {     // 同じ行に無いときは、±7の高さの左の名前（「HA接続コード」は2つの形名のまん中の高さ）\n        var lg = segs.filter(function (g2) { return Math.abs(g2.y - c.y) <= 7 && g2.x < c.x && g2.x2 >= c.x - 40 && g2.x2 <= c.x + 3 && hiNameLike(g2.t); })\n          .sort(function (a, b) { return Math.abs(a.y - c.y) - Math.abs(b.y - c.y); })[0];\n        if (lg) { left = lg.t; li = lg.its[0]; }\n      }\n      if (left && li && !/用$/.test(left)) {\n        // 縦に割れた名前（「ドレンアップ」「キット」＝まん中ぞろえで左はしがずれる）。左はしがそろった並び（「別売延長コード」×3）はつながない\n        var colq = its.filter(function (q) {\n          var dx = Math.abs(q.x - li.x);\n          return q !== li && dx >= 3 && dx <= 15 && Math.abs(q.y - li.y) <= 12 && endX(q) <= c.x + 3 && hiNameLike(nos(q.s).replace(/^別売/, ''));\n        });\n        if (colq.length) left = colq.concat([li]).sort(function (a, b) { return b.y - a.y; }).map(function (q) { return nos(q.s); }).join('').replace(/^別売/, '');\n      }\n      var qual = [];\n      if (/用$/.test(left)) { qual.push('（' + left + '）'); left = ''; }\n\n      // 上の見出し（①… ■…）と、見出しの下の名前らしい字\n      var H = null, PL = null;\n      segs.forEach(function (g2) {\n        var dy = g2.y - c.y;\n        if (dy <= 1.5 || dy > 130 || g2.x > c.x + 15) return;\n        var ht = CIRC.test(g2.t) ? (dy >= 8 ? circTitle(g2) : '') : (/^■/.test(g2.t) ? titleText(g2.its[0]) : '');\n        if (ht && (!H || dy < H.dy || (dy === H.dy && g2.x > H.x))) H = { dy: dy, x: g2.x, y: g2.y, t: ht };\n      });\n      segs.forEach(function (g2) {\n        var dy = g2.y - c.y;\n        if (dy <= 1.5 || dy > 110 || g2.x < c.x - 120 || g2.x > c.x + 15) return;\n        if (H && g2.y >= H.y) return;\n        if (CIRC.test(g2.t) || /^■/.test(g2.t) || !hiNameLike(g2.t) || /ホワイト|ブラック|ベージュ|ブラウン|シルバー/.test(g2.t)) return;\n        if (g2.para && g2.para.stop) return;\n        if (!PL || dy < PL.dy || (dy === PL.dy && Math.abs(g2.x - c.x) < Math.abs(PL.x - c.x))) PL = { dy: dy, x: g2.x, y: g2.y, t: g2.t, g: g2 };\n      });\n      var name = '', ns = 2, fits = [];\n      if (left) { name = left; ns = 3; }     // 形名と同じ行の名前がいちばん確か\n      else if (PL && H && H.t.indexOf(PL.t) < 0 && PL.t.indexOf(H.t) < 0) name = H.t + '（' + PL.t + '）';\n      else if (H) name = H.t;\n      else if (PL) name = PL.t;\n      else { var h2 = sectionOf(c.x, c.y); if (h2) { name = titleText(h2); ns = 1; } else ns = 0; }\n      if (/-SS$/.test(code) && /ステンレス製/.test(all)) qual.push('（ステンレス製・受注生産品）');\n      name = (name + rest + qual.join('')).replace(/\\s+/g, '');\n      // 品名の段落にある「（RAF-D36F・D40F2・D50F2 用）」\n      if (!left && !H && PL && PL.g.para) PL.g.para.segs.forEach(function (g3) { if (/^（/.test(g3.t)) fits = fits.concat(ccFits(hiCodeList(g3.t, ''))); });\n\n      // 付く機種の文\n      //  a) すぐ左の列の、この高さを含む行の並び（表の行「XJシリーズ・ZJシリーズ…」）\n      var xs = [];\n      lines.forEach(function (L) { var f0 = L.its[0]; if (Math.abs(L.y - c.y) <= 6 && f0 && f0.x < c.x - 20) xs.push(f0.x); });\n      if (xs.length) {\n        var rx0 = Math.max.apply(null, xs);\n        var colL = lines.filter(function (L) { return L.its[0] && Math.abs(L.its[0].x - rx0) <= 4; }).sort(function (a, b) { return b.y - a.y; });\n        var k0 = -1;\n        colL.forEach(function (L, i) { if (k0 < 0 && Math.abs(L.y - c.y) <= 6) k0 = i; });\n        if (k0 >= 0) {\n          var a0 = k0, b0 = k0;\n          while (a0 > 0 && colL[a0 - 1].y - colL[a0].y <= 13) a0--;\n          while (b0 < colL.length - 1 && colL[b0].y - colL[b0 + 1].y <= 13) b0++;\n          var rt0 = colL.slice(a0, b0 + 1).map(function (L) { return nos(L.its.filter(function (q) { return q.x < c.x - 3; }).map(function (q) { return q.s; }).join('')); }).join('');\n          if (/シリーズ/.test(rt0) && !/^※/.test(rt0)) fits = fits.concat(hiSeriesFits(rt0));\n        }\n      }\n      if (best) {\n        //  b) 同じ列の上（110まで）・下（70まで）の文。列の幅は右どなりの形名の手前まで（60まで）。\n        //     75ページのリモコンホルダーは3つの列が5しか離れていないので、かたまりではなく字の位置で切る\n        var xr = c.x + 60;\n        its.forEach(function (q) { if (q !== o && Math.abs(q.y - c.y) <= 15 && q.x > c.x + 10 && q.x - 5 < xr && HI_OPT_CODE.test(nos(q.s))) xr = q.x - 5; });\n        var wl = lines.filter(function (L) { var dy = L.y - c.y; return (dy > 1.5 && dy <= 110) || (dy < -1.5 && dy >= -70); })\n          .map(function (L) { var li = L.its.filter(function (q) { return q.x >= c.x - 5 && q.x < xr; }); return { y: L.y, x0: li.length ? li[0].x : 0, t: nos(li.map(function (q) { return q.s; }).join('')) }; })\n          .filter(function (l) { return l.t; }).sort(function (a, b) { return b.y - a.y; });\n        var blocks = [], cb = null;\n        wl.forEach(function (l) {\n          var cl = /^((?:SP|HA|PSC)-|RAC-N)/.test(l.t);\n          if (cb && cb.yb - l.y <= 10.5 && (cb.yb - c.y) * (l.y - c.y) > 0) { cb.t += l.t; cb.yb = l.y; cb.code = cb.code || cl; return; }\n          cb = { yt: l.y, yb: l.y, t: l.t, code: cl, x0: l.x0 }; blocks.push(cb);\n        });\n        // 上は近い順に見て、形名の行・この品の名前の行に着いたら止める（その先は別の品の文）。下は「本カタログ掲載の…に使用できます」だけ\n        var nameY = PL ? PL.y : (H ? H.y : null);\n        var isAll = function (t) { return /本カタログ(に)?掲載の(ルームエアコン|機種)/.test(t) && /使用でき/.test(t) && !/除く/.test(t); };\n        var ups = blocks.filter(function (B) { return B.yb > c.y; }).sort(function (a, b) { return a.yb - b.yb; });\n        for (var u = 0; u < ups.length; u++) {\n          var B = ups[u];\n          if (isAll(B.t)) { fits.push({ all: true }); break; }\n          if (B.code) break;\n          if (/シリーズ/.test(B.t) && !/^※/.test(B.t)) { fits = fits.concat(hiSeriesFits(B.t)); break; }\n          if (nameY != null && nameY <= B.yt + 1 && nameY >= B.yb - 1) break;\n        }\n        blocks.forEach(function (B) { if (B.yt < c.y && Math.abs(B.x0 - c.x) <= 15 && isAll(B.t)) fits.push({ all: true }); });\n        //  c) 同じ■見出しの中の「適用機種」\n        var hh = sectionOf(c.x, c.y);\n        stmts.forEach(function (st) {\n          if (!hh || st.h !== hh) return;\n          st.fits.forEach(function (f) {\n            // 電源を本体からとる品（プラス換気ユニット）は、単相100V用＝形名の末尾S、200V用＝D\n            var pw = name.match(/単相(100|200)V用/);\n            if (f.re && ownPower && pw && /^\\^RAS-/.test(f.re)) fits.push({ re: f.re + (pw[1] === '100' ? 'S' : 'D') });\n            else fits.push(f);\n          });\n        });\n      }\n      out.push({ page: page, code: code, name: name, ns: ns, y: best ? best.a.v : null, d: best ? best.d : null, open: !!(best && best.a.open), fits: fits });\n    });\n    return out;\n  }\n\n  function hiRoomOptFinish(list) {\n    var labels = {}, drain = [];\n    list.forEach(function (o) { if (o.meritRows) labels[o.page] = o.meritRows; });\n    list.forEach(function (o) {\n      if (!o.meritMarks) return;\n      var rows = (labels[o.page] || []).concat(labels[o.page - 1] || []);\n      o.meritMarks.forEach(function (y) { rows.filter(function (r) { return Math.abs(r.y - y) < 3; }).forEach(function (r) { drain = drain.concat(r.fits); }); });\n    });\n    var by = {}, order = [];\n    list.forEach(function (o) {\n      if (!o.code) return;\n      var v = by[o.code];\n      if (!v) { v = by[o.code] = { es: [], fits: [] }; order.push(o.code); }\n      v.es.push(o);\n      (o.fits || []).forEach(function (f) { if (f.merit) v.fits = v.fits.concat(drain); else v.fits.push(f); });\n    });\n    var ok = [];\n    order.forEach(function (c) {\n      var v = by[c];\n      // 値段は形名にいちばん近く書いてあるもの。品名もそのカードから（表の中の同じ形名は付く機種だけ借りる）\n      var pr = v.es.filter(function (e) { return e.y != null; }).sort(function (a, b) { return a.d - b.d; })[0];\n      if (!pr) return;                       // 値段の載っていない形名（CS-NET機器 PSC-…）\n      // 品名は、値段のあるカード → 名前の確かさ（同じ行3・見出し2・■だけ1）→ 値段の近さ の順\n      var nm = (v.es.filter(function (e) { return e.name; }).sort(function (a, b) {\n        return ((b.y != null) - (a.y != null)) || ((b.ns || 0) - (a.ns || 0)) || ((a.d == null ? 1e9 : a.d) - (b.d == null ? 1e9 : b.d));\n      })[0] || {}).name;\n      ok.push({ page: pr.page, code: c, name: (nm || c) + (pr.open ? '（オープン価格）' : ''), y: pr.y, fits: v.fits.length ? v.fits : [{ all: true }] });\n    });\n    return optResult(ok, '日立', 'ルームエアコン（住宅設備用） 別売品');\n  }\n\n  /* --------------------------------------------------------------------\n     三菱電機 ルームエアコン（住宅設備用総合カタログ 2026-06・100ページ）\n\n     1台ぶんは列のかたまり（1ページに3列×2段）：\n       形名「MSZ-FZV4026S（W）」／畳数「とも主に 14 畳」／電源「単相 200V」\n       室内「:MSZ-FZV4026S-W-IN 232,000円（税別）」／室外「:MUZ-FZV4026S 348,000円（税別）」\n       本体価格「580,000 円（税別）」\n       天井カセット形は「化粧パネル（別売）43,200円」、壁埋込形は「前面グリル（別売）」「据付枠（別売）」と「合計価格」\n     GVシリーズは値段のかわりに「オープン価格」。値段は紙面が税別なので、そのまま入れる。\n     システムマルチは、室外機（63ページ「MXZ-4626AS」の下に「本体価格 412,000 円」）と、\n     室内機の一覧（64・65ページ「MSZ-2226ZXAS-W-IN 本体価格 247,000円」。セットと違って頭に「:」が無い）\n     -------------------------------------------------------------------- */\n  var ME_ROOM_WALL = { FZV: 'FZシリーズ', ZXV: 'Zシリーズ', VXV: 'VXVシリーズ', HXV: 'HXVシリーズ', JXV: 'JXVシリーズ',\n                       BXV: 'BXVシリーズ', AXV: 'AXVシリーズ', NXV: 'NXVシリーズ', KXV: 'KXVシリーズ', FLV: 'FLシリーズ', GV: 'GVシリーズ' };\n  var ME_ROOM_CODE = /^(MSZ|MLZ|MTZ|MBZ|MFZ)-([A-Z]*)(\\d{4,5})([A-Z]*)/;\n\n  /** 形名の頭からシリーズと室内機の形を決める。ルームエアコンでなければ null */\n  function meRoomKind(pre, lead, tail) {\n    if (pre === 'MSZ') {\n      if (!lead && /^(ZXAS|BXAS|GXAS)$/.test(tail)) return { s: tail.replace(/AS$/, '') + 'シリーズ', i: '壁掛形' };\n      return ME_ROOM_WALL[lead] ? { s: ME_ROOM_WALL[lead], i: '壁掛形' } : null;\n    }\n    if (pre === 'MLZ') {\n      if (/^(RX|GX|HX)$/.test(lead)) return { s: lead + 'シリーズ', i: '天井カセット形（1方向）' };\n      if (lead === 'M') return { s: 'Mシリーズ', i: '天井カセット形（1方向・小能力）' };\n      if (/^(W|HW)$/.test(lead)) return { s: lead + 'シリーズ', i: '天井カセット形（2方向）' };\n      return null;\n    }\n    if (pre === 'MTZ') return { s: '壁埋込形', i: '壁埋込形' };\n    if (pre === 'MBZ') return { s: 'フリービルトイン', i: 'フリービルトイン形' };\n    if (pre === 'MFZ') return /^(K|HK)$/.test(lead) ? { s: lead + 'シリーズ', i: '床置形' } : null;\n    return null;\n  }\n\n  function meRoomReadPage(items, page) {\n    function clean(t) { return String(t).replace(/[\\u0000-\\u001f]/g, ' ').trim(); }\n    var all = items.map(function (o) { return clean(o.s); }).join(' ');\n    if (!/本体価格|オープン価格/.test(all)) return [];\n    var out = [];\n    function num(t) { var m = clean(t).match(/^([\\d,]{4,})\\s*円?$/); return m ? yen(m[1]) : 0; }\n    function rowAt(band, y, tol) {\n      return band.filter(function (o) { return Math.abs(o.y - y) < (tol || 2.5); }).sort(function (a, b) { return a.x - b.x; });\n    }\n    function valueRight(band, label) {\n      var v = 0;\n      rowAt(band, label.y).forEach(function (o) { if (!v && o.x > label.x) v = num(o.s); });\n      return v;\n    }\n\n    // 1) 室内機の品番。セットは頭に「:」（室内の行）、マルチ用の室内機の一覧は「:」なし\n    items.forEach(function (a) {\n      var t = clean(a.s), code = t.replace(/^:+/, '');\n      if (!/-IN$/.test(code)) return;\n      // 室内の「:」は品番の頭に付いて来ることも、すぐ左の別の文字で来ることもある（28ページ FL）\n      var colon = /^:/.test(t) || items.some(function (o) {\n        return o !== a && clean(o.s) === ':' && Math.abs(o.y - a.y) < 1 && a.x - o.x >= 0 && a.x - o.x < 4;\n      });\n      var m = code.match(ME_ROOM_CODE);\n      if (!m) return;\n      var kind = meRoomKind(m[1], m[2], m[4]);\n      if (!kind) return;\n      // 色違い「-W,-T-IN」「-W,-R,-K-IN」は1つにまとめて書く（「-W-IN」）\n      var imAll = code.replace(/(-[A-Z])(,-[A-Z])+/, '$1');\n      // その品番の列だけを見る（セットは列の幅170、マルチ用の室内機の一覧は85）\n      var band = items.filter(function (o) { return o.x >= a.x - (colon ? 30 : 8) && o.x < a.x + (colon ? 150 : 82); });\n      // 本体価格の札は、セットは品番の11左、一覧は品番と同じ x。となりの列の札を取らない\n      var tx = colon ? a.x - 11 : a.x;\n      var bodyL = band.filter(function (o) { var dy = a.y - o.y; return dy > -1 && dy <= 30 && clean(o.s) === '本体価格'; })\n        .sort(function (p, q) { return Math.abs(p.x - tx) - Math.abs(q.x - tx); })[0];\n      if (bodyL && Math.abs(bodyL.x - tx) > 25) bodyL = null;\n      var open = !bodyL && band.some(function (o) { var dy = a.y - o.y; return dy > -1 && dy <= 30 && /オープン価格/.test(clean(o.s)); });\n      var body = bodyL ? valueRight(band, bodyL) : 0;\n      if (!body && !open) return;\n      // 化粧パネル・前面グリル・据付枠（別売）と合計価格（本体価格の下32まで、本体価格の札と同じ列）\n      var extras = [], total = 0;\n      if (bodyL) {\n        band.forEach(function (o) {\n          var dy = bodyL.y - o.y, tt = clean(o.s);\n          if (dy <= 1 || dy > 32 || Math.abs(o.x - bodyL.x) > 25) return;\n          if (/^化粧パネル/.test(tt)) extras.push('化粧パネル');\n          if (/^前面グ/.test(tt)) extras.push('前面グリル');\n          if (/^据付枠/.test(tt)) extras.push('据付枠');\n          if (tt === '合計価格') total = valueRight(band, o) || total;\n        });\n      }\n      // 室外機（セットだけ。すぐ下の「:MU…」）\n      var om = '';\n      if (colon) band.forEach(function (o) {\n        var dy = a.y - o.y, tt = clean(o.s);\n        if (!om && dy > 1 && dy < 12 && /^:?MU[A-Z]*-/.test(tt)) om = tt.replace(/^:+/, '');\n      });\n      // 畳数（「とも主に」の行の数）と電源（「単相」の行の「100V」「200V」）は品番より上140まで\n      var above = band.filter(function (o) { var dy = o.y - a.y; return dy > 0 && dy < 140; });\n      var tat = 0, pw = '';\n      above.forEach(function (o) {\n        if (!/とも|も主に/.test(clean(o.s))) return;\n        rowAt(band, o.y, 3).forEach(function (q) { var v = clean(q.s); if (!tat && /^\\d{1,2}$/.test(v)) tat = Number(v); });\n      });\n      above.forEach(function (o) {\n        if (clean(o.s) !== '単相') return;\n        rowAt(band, o.y).forEach(function (q) { var v = clean(q.s); if (!pw && /^(100|200)V$/.test(v)) pw = '単相' + v; });\n      });\n      var multi = !colon;\n      /* 「MSZ-JXV2826(S)」は100V（JXV2826）と200V（JXV2826S）の2機種で、値段は同じ（30ページ）。\n         紙面の電源の欄にも「JXV2826 100V」「JXV2826S 200V」と2行ある */\n      var vars = /\\(S\\)/.test(code) ? [{ sfx: '', pw: '単相100V' }, { sfx: 'S', pw: '単相200V' }] : [null];\n      vars.forEach(function (v) {\n        var tail = v ? v.sfx : m[4];\n        var base = m[1] + '-' + m[2] + m[3] + tail;\n        var im = v ? imAll.replace('(S)', v.sfx) : imAll;\n        var om1 = v ? om.replace('(S)', v.sfx) : om;\n        out.push({\n          page: page, m: multi ? im : base, kw: parseInt(m[3].slice(0, -2), 10) / 10, tat: tat,\n          s: multi ? 'マルチ用室内機' : kind.s, i: kind.i,\n          pw: multi ? '室外機から' : (v ? v.pw : (pw || (m[1] === 'MSZ' && !/S$/.test(m[4]) ? '単相100V' : '単相200V'))),\n          tp: multi ? 'マルチ用室内機' : 'シングル', rc: 'ワイヤレス',\n          y: open ? 0 : (total || body),\n          opt: [open ? 'オープン価格' : '', extras.length ? extras.join('・') + '込み' : ''].filter(Boolean).join('／'),\n          om: multi ? '' : om1, im: im, pm: ''\n        });\n      });\n    });\n\n    // 2) システムマルチの室外機（63ページ「MXZ-4626AS」の下に「本体価格」と値段）\n    items.forEach(function (a) {\n      var t = clean(a.s), m = t.match(/^MXZ-(\\d{4,5})AS$/);\n      if (!m) return;\n      var band = items.filter(function (o) { return o.x >= a.x - 8 && o.x < a.x + 90; });\n      var bodyL = band.filter(function (o) { var dy = a.y - o.y; return dy > 1 && dy < 14 && clean(o.s) === '本体価格'; })[0];\n      var body = bodyL ? valueRight(band, bodyL) : 0;\n      if (!body) return;\n      out.push({ page: page, m: t, kw: parseInt(m[1].slice(0, -2), 10) / 10, tat: 0, s: 'システムマルチ（室外機）', i: 'マルチ室外機',\n                 pw: '単相200V', tp: 'システムマルチ', rc: '', y: body, opt: '', om: t, im: '', pm: '' });\n    });\n\n    /* 3) 耐塩害仕様・耐重塩害仕様のセット（67ページの一覧「MSZ-FZV4026SE 595,000 円」「MSZ-GV2226EE オープン価格」）。\n       中身は元のセット（MSZ-FZV4026S）と同じで、値段と室外機が違う。元のセットの能力・畳数・電源はあとで写す。\n       室外機だけの一覧（「MULZ-RX2826AS-E」）は、セットではないので入れない */\n    if (/耐塩害仕様（セット）|耐重塩害仕様（セット）/.test(all)) {\n      items.forEach(function (a) {\n        var t = clean(a.s), m = t.match(/^(MSZ-[A-Z]+\\d{4}S?)(E{1,2})$/);\n        if (!m) return;\n        var pr = items.filter(function (o) {\n          var tt = clean(o.s);\n          return Math.abs(o.y - a.y) < 3 && o.x > a.x + 40 && o.x < a.x + 120 && (/^[\\d,]{5,}$/.test(tt) || /オープン価格/.test(tt));\n        }).sort(function (p, q) { return Math.abs(p.y - a.y) - Math.abs(q.y - a.y) || p.x - q.x; })[0];\n        if (!pr) return;\n        var open = /オープン価格/.test(clean(pr.s));\n        out.push({ page: page, salt: m[2], m: t, base: m[1], y: open ? 0 : yen(clean(pr.s)), open: open });\n      });\n    }\n    return out;\n  }\n\n  function meRoomFinish(sets) {\n    var rows = [], seen = {}, pages = {}, baseOf = {};\n    sets.forEach(function (x) { if (!x.salt && !baseOf[x.m]) baseOf[x.m] = x; });\n    sets.forEach(function (x) {\n      pages[x.page] = 1;\n      // 耐塩害仕様のセットは、元のセットの中身を写す（元が読めなかったものは入れない）\n      if (x.salt) {\n        var b = baseOf[x.base];\n        if (!b) return;\n        x = { page: x.page, m: x.m, kw: b.kw, tat: b.tat, s: b.s, i: b.i, pw: b.pw, tp: b.tp, rc: b.rc, y: x.y,\n              opt: [x.open ? 'オープン価格' : '', x.salt === 'EE' ? '耐重塩害仕様' : '耐塩害仕様',\n                    (b.opt || '').replace(/オープン価格／?/, '')].filter(Boolean).join('／'),\n              om: b.om ? b.om + '-' + x.salt : '', im: b.im, pm: '' };\n      }\n      if (seen[x.m]) return;\n      seen[x.m] = 1;\n      // 「馬力」の手順には、ルームエアコンでは能力の文字を入れる（画面はそのまま出す）\n      var cap = x.kw.toFixed(1) + 'kW' + (x.tat ? '（おもに' + x.tat + '畳）' : '');\n      rows.push({\n        m: x.m, hp: cap, y: x.y, u: String(x.page),\n        s: x.s, i: x.i, ab: cap, pw: x.pw,\n        rc: x.rc, tp: x.tp, opt: x.opt, om: x.om, im: x.im, pm: x.pm, rm: ''\n      });\n    });\n    return {\n      rows: rows,\n      pricePages: Object.keys(pages).length,\n      head: {\n        maker: '三菱電機',\n        brand: 'ルームエアコン（住宅設備用）',\n        source: '住宅設備用総合カタログ（公開Webカタログ）',\n        note: '希望小売価格・税抜。配管/据付工事費は含まず。GVシリーズはオープン価格（値段0）。耐塩害仕様・耐重塩害仕様のセットは形名の末尾E・EE。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['FZシリーズ', 'Zシリーズ', 'VXVシリーズ', 'HXVシリーズ', 'JXVシリーズ', 'BXVシリーズ', 'AXVシリーズ',\n                      'NXVシリーズ', 'KXVシリーズ', 'FLシリーズ', 'GVシリーズ',\n                      'RXシリーズ', 'GXシリーズ', 'HXシリーズ', 'Mシリーズ', 'Wシリーズ', 'HWシリーズ',\n                      '壁埋込形', 'フリービルトイン', 'Kシリーズ', 'HKシリーズ',\n                      'システムマルチ（室外機）', 'マルチ用室内機'],\n        typeOrder: ['シングル', 'システムマルチ', 'マルチ用室内機']\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     三菱電機 ルームエアコンの別売部品（住宅設備用総合カタログ 2026-06）\n     44ページ（防雪架台・防雪フード）・55〜57ページ（フリービルトイン形の必要部品）・68〜71ページ（別売部品）。\n     ダイキンのような●の表ではなく、1品ずつ「形名・価格・適用機種」が並び、適用機種は文で書いてある：\n       「本カタログ掲載の全機種」「壁掛形全機種」「FZ・Z・JXV…シリーズ」「MSZ- ZXV2226～ZXV2826」「室外機の高さ550mmの機種」\n     シリーズは形名の頭で見分ける（{cc:'MSZ-FZV'}。マルチ用の室内機「MSZ-2226ZXAS-W-IN」も「ZXAS」で当たる）。\n     室外機の高さや配管の太さのように、機種データに無いことで決まるものは、どの機種にも出して、品名にその条件を書く。\n     値段は税別のまま\n     -------------------------------------------------------------------- */\n  var ME_OPT_CODE = /^(MAC-[A-Z0-9]{3,}|PAC-[A-Z0-9]{3,}|MOKD[A-Z]*-[A-Z0-9-]+|MOPAC-[A-Z0-9-]+|C-[A-Z]{1,4}\\d?(?:-L)?)$/;\n  var ME_OPT_SERIES = {\n    FZ: 'MSZ-FZV', Z: 'MSZ-ZXV', FL: 'MSZ-FLV', JXV: 'MSZ-JXV', BXV: 'MSZ-BXV', AXV: 'MSZ-AXV', GV: 'MSZ-GV',\n    VXV: 'MSZ-VXV', HXV: 'MSZ-HXV', NXV: 'MSZ-NXV', KXV: 'MSZ-KXV', ZXAS: 'ZXAS', BXAS: 'BXAS', GXAS: 'GXAS',\n    RX: 'MLZ-RX', GX: 'MLZ-GX', HX: 'MLZ-HX', M: 'MLZ-M', W: 'MLZ-W', HW: 'MLZ-HW', K: 'MFZ-K', HK: 'MFZ-HK'\n  };\n\n  /** 適用機種の文から「付く機種」を作る。作れなかったら fits は空 */\n  function meOptFits(text) {\n    var t = text.replace(/[０-９]/g, function (c) { return String.fromCharCode(c.charCodeAt(0) - 0xFEE0); })\n      .replace(/([ぁ-んァ-ヶー一-龥])\\s+(?=[ぁ-んァ-ヶー一-龥])/g, '$1')\n      .replace(/シ\\s*リ\\s*ー\\s*ズ/g, 'シリーズ').replace(/\\s*・\\s*/g, '・').replace(/([A-Z])\\s+シリーズ/g, '$1シリーズ')\n      // 「ズバ暖シリーズ・MXZ-6026AS~10226ASを除く全機種」：除く部分は消して読む（残るのは「全機種」）\n      .replace(/[^、（(]*を除く/g, ' ');\n    var fits = [];\n    function add(f) { var k = JSON.stringify(f); if (!fits.some(function (g) { return JSON.stringify(g) === k; })) fits.push(f); }\n    if (/掲載の全機種|すべての室外機|(^|[^形\\s])\\s*全機種/.test(t)) add({ all: true });\n    if (/壁掛形(・床置形)?全機種/.test(t)) add({ type: '壁掛形' });\n    if (/床置形全機種/.test(t)) add({ type: '床置形' });\n    var m, re = /((?:[A-Z]+・)*[A-Z]+)シリーズ/g;\n    while ((m = re.exec(t))) m[1].split('・').forEach(function (k) { if (ME_OPT_SERIES[k]) add({ cc: ME_OPT_SERIES[k] }); });\n    if (/壁埋込形/.test(t)) add({ cc: 'MTZ-' });\n    if (/フリービルトイン形/.test(t)) add({ cc: 'MBZ-' });\n    // 形名の範囲（「MSZ- ZXV2226～ZXV2826」「MXZ -6826AS～10226AS」）と形名そのもの（「FLV2821」）\n    var pre = '', r2 = /(MSZ|MLZ|MFZ|MTZ|MBZ|MXZ|MUZ|MUFZ|MULZ|MUTZ)\\s*-\\s*|([A-Z]{0,3})(\\d{4,5})(S|AS)?(?:\\s*[～~〜]\\s*([A-Z]{0,3})(\\d{4,5})(S|AS)?)?/g;\n    while ((m = r2.exec(t))) {\n      if (m[1]) { pre = m[1] + '-'; continue; }\n      if (!pre || !m[3]) continue;\n      var lead = m[2], d1 = m[3], t1 = m[4] || '';\n      if (m[6]) {\n        var d2 = m[6], t2 = m[7] || '';\n        [t1, t2].filter(function (v, i, a) { return a.indexOf(v) === i; }).forEach(function (tail) {\n          add({ rm: { p: pre + lead, t: d1.slice(-2) + tail, r: true, c: [Number(d1.slice(0, -2)), Number(d2.slice(0, -2))] } });\n        });\n      } else {\n        add({ mc: pre + lead + d1 + t1 });\n      }\n    }\n    return fits;\n  }\n\n  function meRoomOptReadPage(items, page) {\n    // 取り消し線のような字（U+0336）がくっついて来る文字がある（68ページ「MAC-398KT̶」「円̶」）。消して読む\n    function clean(t) { return String(t).replace(/[\\u0000-\\u001f\\u0336]/g, ' ').replace(/\\s+/g, ' ').trim(); }\n    var its = items.map(function (o) { return { s: clean(o.s), x: o.x, y: o.y, w: o.w || 0 }; }).filter(function (o) { return o.s; });\n    // 2行に割れた形名（44ページ「MOKDNA-」「R01-G-K-02」）をつなぐ\n    its.forEach(function (o) {\n      if (!/^[A-Z]{3,}-$/.test(o.s)) return;\n      var nx = its.filter(function (q) { return q !== o && Math.abs(q.x - o.x) <= 4 && o.y - q.y > 2 && o.y - q.y < 8 && /^[A-Z0-9][A-Z0-9-]+$/.test(q.s); })[0];\n      if (nx) { o.s = o.s + nx.s; nx.s = ''; }\n    });\n    its = its.filter(function (o) { return o.s; });\n    var codes = its.filter(function (o) { return ME_OPT_CODE.test(o.s); });\n    if (!codes.length) return [];\n    var all = its.map(function (o) { return o.s; }).join(' ');\n    if (!/価格/.test(all)) return [];\n    /* 読むのは別売部品のページだけ。53ページ（壁埋込形の前面グリルの写真の説明）・66ページ（旧製品からのグリル対応表）・\n       73ページ（システムコントロールの一覧。PAR-48MA などの行の値段を MAC-333IF に付けた）は読まない */\n    if (!/防雪フード|防雪架台|必要部品価格|本カタログ掲載機種用|室内機用部品|床置形用部品|スライド金具|シリーズ共通|■Mシリーズ/.test(all)) return [];\n\n    function money(o) {\n      var m = o.s.match(/^([\\d,]{3,})\\s*円/);\n      if (m) return yen(m[1]);\n      if (/^[\\d,]{3,}$/.test(o.s) && its.some(function (q) { return /^円/.test(q.s) && Math.abs(q.y - o.y) < 2 && q.x > o.x && q.x - o.x < 45; })) return yen(o.s);\n      return 0;\n    }\n    function isOpen(o) { return /^オープン価格/.test(o.s); }\n    var prices = its.filter(function (o) { return money(o) > 0 || isOpen(o); });\n    var kaku = prices.filter(function (p) {\n      return its.some(function (q) { return q.s === '各' && Math.abs(q.y - p.y) < 2 && p.x > q.x && p.x - q.x < 20; });\n    });\n    // その値段の持ち主＝値段より左の品番のうち、いちばん近い行の、いちばん右のもの\n    function owner(p) {\n      var best = null, bd = 1e9, bx = 1e9;\n      codes.forEach(function (d) {\n        if (d.x >= p.x || p.x - d.x > 480) return;\n        var dd = Math.abs(d.y - p.y), dx = p.x - d.x;\n        if (dd < bd - 0.5 || (Math.abs(dd - bd) <= 0.5 && dx < bx)) { bd = dd; bx = dx; best = d; }\n      });\n      return best;\n    }\n    // 注記の行（左に「＊」「※」で始まる字がある行）の字\n    var noteHeads = its.filter(function (o) { return /^[＊●]|^※[^\\d]/.test(o.s); });\n    function onNoteLine(o) { return noteHeads.some(function (n) { return Math.abs(n.y - o.y) < 1.8 && n.x <= o.x; }); }\n    // 形名の頭だけの字（「MSZ-」「MLZ-」）。次の行の範囲に真上から付く\n    var pres = its.filter(function (o) { return /^(MSZ|MLZ|MFZ|MTZ|MBZ|MXZ|MUZ|MUFZ|MULZ|MUTZ)\\s*-$/.test(o.s); });\n    // 行ごとにそろえて、左から並べる（字の高さが少しずつ違うと「ーズ リ FLシ」の順になった）\n    function inLines(arr) {\n      var srt = arr.slice().sort(function (a, b) { return b.y - a.y; }), ls = [], cur = null;\n      srt.forEach(function (o) { if (!cur || Math.abs(cur.y - o.y) > 1.8) { cur = { y: o.y, it: [] }; ls.push(cur); } cur.it.push(o); });\n      return ls.map(function (L) { return L.it.sort(function (a, b) { return a.x - b.x; }).map(function (o) { return o.s; }).join(' '); }).join(' ');\n    }\n    function tidy(t) {\n      return t.replace(/※\\d*|注\\s*[\\d,]*/g, ' ').replace(/([ぁ-んァ-ヶー一-龥（）])\\s+(?=[ぁ-んァ-ヶー一-龥（）])/g, '$1')\n        .replace(/\\s+/g, ' ').trim();\n    }\n    // 44ページの防雪架台・防雪フードは、適用機種が右の欄にまとめて書いてある（①〜④の組）。ページの中の形名の範囲を全部まとめて使う\n    var snowFits = null;\n    if (/防雪フード|防雪架台/.test(all)) {\n      snowFits = meOptFits(its.filter(function (o) { return o.x >= 470; }).sort(function (a, b) { return b.y - a.y || a.x - b.x; })\n        .map(function (o) { return o.s; }).join(' ')).filter(function (f) { return f.rm || f.mc; });\n    }\n\n    var out = [];\n    codes.forEach(function (c) {\n      // 同じ行の、次の品番の手前まで\n      var nextX = 1e9, prevX = -1e9;\n      codes.forEach(function (d) {\n        if (d === c || Math.abs(d.y - c.y) >= 3) return;\n        if (d.x > c.x && d.x < nextX) nextX = d.x;\n        if (d.x < c.x && d.x > prevX) prevX = d.x;\n      });\n      // 右どなりの表の品番の列（上下30の中）。そこより右は、よその表\n      var rightX = 1e9;\n      codes.forEach(function (d) { if (d.x > c.x + 20 && Math.abs(d.y - c.y) < 30 && d.x < rightX) rightX = d.x; });\n      var edge = Math.min(nextX, rightX);\n      // 1) 同じ行の値段（その値段にいちばん近い品番が c のときだけ）\n      var pr = prices.filter(function (p) {\n        return p.x > c.x && p.x < edge && p.x - c.x < 480 && Math.abs(p.y - c.y) <= 5 && kaku.indexOf(p) < 0 && owner(p) === c;\n      }).sort(function (a, b) { return Math.abs(a.y - c.y) - Math.abs(b.y - c.y) || a.x - b.x; })[0];\n      // 2) 「各 15,000 円」：2行ぶんのまん中に1つ（69ページ 吹出ガイド）\n      if (!pr) pr = kaku.filter(function (p) { return p.x > c.x && p.x < edge && Math.abs(p.y - c.y) <= 14; })\n        .sort(function (a, b) { return Math.abs(a.y - c.y) - Math.abs(b.y - c.y); })[0];\n      // 3) 背の高いマス：品番の下、同じ列の次の品番の上にある値段（71ページ MAC-A20JP、68ページ MAC-645BH）\n      if (!pr) {\n        var below = codes.filter(function (d) { return d !== c && Math.abs(d.x - c.x) < 30 && d.y < c.y; }).sort(function (a, b) { return b.y - a.y; })[0];\n        var lo = below ? below.y + 2 : c.y - 60;\n        pr = prices.filter(function (p) {\n          return p.x > c.x + 100 && p.x < edge && p.y < c.y && p.y > lo && !codes.some(function (d) { return Math.abs(d.y - p.y) < 3.5; });\n        }).sort(function (a, b) { return b.y - a.y; })[0];\n      }\n      // 4) まとめて1つの「各」やオープン価格（68ページ 200V機種用、ヤモリガード、69ページ 無線LANアダプター）\n      if (!pr) pr = prices.filter(function (p) { return p.x > c.x && p.x < edge && Math.abs(p.y - c.y) <= 45 && (kaku.indexOf(p) >= 0 || isOpen(p)); })\n        .sort(function (a, b) { return Math.abs(a.y - c.y) - Math.abs(b.y - c.y); })[0];\n      if (!pr) return;\n      var y = isOpen(pr) ? 0 : money(pr);\n\n      // 品名：上の「■見出し」＋同じ行の左の名前\n      var head = its.filter(function (o) { return /^■/.test(o.s) && o.y > c.y && o.y - c.y < 300 && o.x <= c.x + 30 && c.x - o.x < 420; })\n        .sort(function (a, b) { return (a.y - c.y + (c.x - a.x > 240 ? 150 : 0)) - (b.y - c.y + (c.x - b.x > 240 ? 150 : 0)); })[0];\n      var hname = head ? head.s.replace(/^■\\s*/, '').replace(/（[^）]*ページ[^）]*）/, '').trim() : '';\n      /* 左の名前は、左どなりの表の品番（上下30の中）より自分に近い字だけ。\n         68ページのヤモリガードの右の表（MAC-760HK）が、左の表の適用機種「高さ550・630mmの機種」を品名にしていた */\n      var leftX = c.x - 260;\n      codes.forEach(function (d) { if (d.x < c.x - 20 && Math.abs(d.y - c.y) < 30) leftX = Math.max(leftX, (d.x + c.x) / 2); });\n      var left = its.filter(function (o) {\n        return o.x < c.x - 2 && o.x > leftX && Math.abs(o.y - c.y) <= 16 && !ME_OPT_CODE.test(o.s) && !onNoteLine(o) &&\n          !/でき$|できます|可能$|ます$|まで|最大|^[仕様]$|：|、$/.test(o.s) &&\n          !/^[Ⓐ-ⓩA-Z]$|^[\\d,.]+$|^各$|^NEW$|^在庫僅少$|円|税別|（MAC-|（PAC-|。|^注|^※|^＊|^■|^:|^【|シリーズ|^・$|[A-Z]シ$|^ーズ|機種|^［|φ|^[A-Z]{1,5}$|^[／/×]$/.test(o.s) &&\n          !/^(形|名|適|用|機|種|形名|品名|入り数|適用機種|価格|容量|構成部品|カラー|交換のめやす|吹出可能方向|仕様|後継形名|従来形名|耐荷重|用途|型式|品|名（枚数）)$/.test(o.s) &&\n          !(money(o) > 0);\n      });\n      var rname = '', md = 99;\n      if (left.length) {\n        md = Math.min.apply(null, left.map(function (o) { return Math.abs(o.y - c.y); }));\n        // いちばん近い字の側（上か下か）だけ\n        var near0 = left.filter(function (o) { return Math.abs(o.y - c.y) === md; })[0];\n        var side = near0.y >= c.y ? 1 : -1;\n        var line = left.filter(function (o) { return Math.abs(Math.abs(o.y - c.y) - md) <= 2.5 && (Math.abs(o.y - c.y) < 2 || (o.y >= c.y ? 1 : -1) === side); });\n        // シリーズの切れはし（「リーズ、」「リ」）は消す\n        rname = tidy(inLines(line)).replace(/シ?リーズ、?/g, '').replace(/\\s*リ$/, '').trim()\n          // 閉じていないかっこ（「（スマー」）と、開いていないかっこ（「2m）」の「）」）\n          .replace(/（[^）]*$/, '').replace(/^([^（]*)）/, '$1').trim();\n      }\n      var name = tidy([hname, rname].filter(Boolean).join(' ')) || '別売部品';\n      // 55〜57ページの一覧の見出し（「別売部品一覧」「必要部品価格」）は品名にしない\n      if (/^(別売部品一覧|必要部品価格)$/.test(hname)) name = rname ? 'フリービルトイン用 ' + rname : 'フリービルトイン用 別売部品';\n      // 44ページ：防雪フードは材質、防雪架台は地域で見分ける（形名の末尾・頭で決まる）\n      if (/^MOPAC-/.test(c.s)) {\n        var mat = /-BSG-\\d+$/.test(c.s) ? '鋼板製・耐重塩害仕様' : /-S-\\d+$/.test(c.s) ? 'ステンレス製' : '鋼板製・標準／耐塩害仕様';\n        name = 'ズバ暖霧ヶ峰室外機専用防雪フード（' + mat + '）' + (rname ? ' ' + rname : '');\n      }\n      if (/^MOKD/.test(c.s)) {\n        var area = { NA: '降雪量の少ない地域向け', WA: '降雪量の多い地域向け', SA: '壁面設置用' }[c.s.slice(4, 6)] || '';\n        name = 'ズバ暖霧ヶ峰室外機専用防雪架台' + (area ? '（' + area + '）' : '') + ' ' + c.s.replace(/^MOKD[A-Z]+-/, '').replace(/-.*$/, '');\n      }\n\n      // 適用機種：右の、この品番がいちばん近い行（同じ列の品番どうしで比べる）\n      var mine = its.filter(function (o) {\n        if (o.x <= c.x + 25 || o.x >= Math.min(edge, c.x + 480) || o === pr) return false;\n        if (money(o) > 0 || /^各$|^円|税別|価格|^オープン価格|。|同梱|^＊|^※|^■|^【|くださ|ご使用|お使い|できま|^NEW$/.test(o.s) || ME_OPT_CODE.test(o.s)) return false;\n        if (onNoteLine(o)) return false;\n        if (/^(形|名|適|用|機|種|形名|品名|入り数|適用機種|容量|構成部品|カラー|交換のめやす|吹出可能方向|仕様|後継形名|従来形名|耐荷重)$/.test(o.s)) return false;\n        // この品番がいちばん近い（2つの品番のまん中なら両方）\n        return true;\n      });\n      /* 背の高いマス（68ページのヒーターの表）：行を8.5より広いすき間でかたまりに分け、\n         かたまりの中に同じ列の品番が1つだけなら、その品番のもの。そうでなければ、いちばん近い品番（まん中なら両方） */\n      var colCodes = codes.filter(function (d) { return Math.abs(d.x - c.x) < 30; });\n      var ysDesc = mine.map(function (o) { return o.y; }).sort(function (a, b) { return b - a; });\n      var blocks = [];\n      ysDesc.forEach(function (y) { var b = blocks[blocks.length - 1]; if (b && b.lo - y <= 8.5) b.lo = y; else blocks.push({ hi: y, lo: y }); });\n      mine = mine.filter(function (o) {\n        var b = blocks.filter(function (k) { return o.y <= k.hi && o.y >= k.lo; })[0];\n        var inB = b ? colCodes.filter(function (d) { return d.y <= b.hi + 3 && d.y >= b.lo - 3; }) : [];\n        if (inB.length === 1) return inB[0] === c;\n        var nd = 1e9, dc = 1e9;\n        colCodes.forEach(function (d) { var dd = Math.abs(d.y - o.y); if (dd < nd) nd = dd; if (d === c) dc = dd; });\n        return dc <= nd + 1.5 && dc <= 40;\n      });\n      mine = mine.map(function (o) {\n        if (!/^[A-Z]{0,3}\\d{4}/.test(o.s)) return o;\n        var pf = pres.filter(function (q) { return Math.abs(q.x - o.x) <= 14 && q.y - o.y > 2 && q.y - o.y < 30; })\n          .sort(function (a, b) { return a.y - b.y; })[0];\n        return pf ? { s: pf.s.replace(/\\s/g, '') + o.s, x: o.x, y: o.y, w: o.w } : o;\n      });\n      var cond = tidy(inLines(mine));\n      var fits = snowFits && /^MO/.test(c.s) ? snowFits.slice() : meOptFits(cond);\n      // 「室外機の高さ550mmの機種」「液管φ6.35、ガス管φ9.52の機種」の言い方があれば、それだけを条件にする\n      // 条件の言い方（「室外機の高さ550mmの機種」「液管φ6.35、ガス管φ9.52の機種」「W840×H802×D320（mm）を除いた室外機」）\n      var kisyu = (cond.replace(/\\s+/g, '').match(/[^。：]*?(の機種|室外機(?!の))/g) || []).map(function (k) {\n        var m0 = k.match(/(室外機の高さ|下記以外の液管|液管|システムマルチ|すべて|高さ\\d|幅\\d|W\\d).*$/);\n        return m0 ? m0[0] : '';\n      }).filter(Boolean);\n      if (!fits.length && kisyu.length) cond = kisyu.join('・');\n      /* その形の機種だけに付くページの区切り（70ページ「床置形用部品」「壁埋込形用部品」「フリービルトイン形用部品」、\n         55〜57ページのフリービルトイン形の必要部品） */\n      var sec = its.filter(function (o) { return /^(床置形|壁埋込形|フリービルトイン形)用部品$/.test(o.s) && o.y > c.y && o.x < c.x + 10; })\n        .sort(function (a, b) { return a.y - b.y; })[0];\n      var secFit = sec ? { '床置形': 'MFZ-', '壁埋込形': 'MTZ-', 'フリービルトイン形': 'MBZ-' }[sec.s.replace(/用部品$/, '')] : (/必要部品価格/.test(all) ? 'MBZ-' : '');\n      if (!fits.length && secFit) { fits = [{ cc: secFit }]; cond = ''; }\n      /* 化粧パネルの表（49〜51・60・61ページ）は見出し「RX・GX・HXシリーズ共通」「Mシリーズ」が適用機種。\n         色は形名の末尾で決まる（PW ホワイト・PB ベージュ・PM 板目・PT 柾目） */\n      var headFits = /シリーズ共通$|^[A-Z・]+シリーズ$/.test(hname) ? meOptFits(hname) : [];\n      if (headFits.length) {\n        fits = headFits;\n        var col = { PW: 'ホワイト', PB: 'ベージュ', PM: '板目', PT: '柾目' }[c.s.slice(-2)] || '';\n        name = '化粧パネル' + (col ? '（' + col + '）' : '') + '　' + hname;\n        cond = '';\n      }\n      out.push({ page: page, code: c.s, name: name, y: y, fits: fits, named: !!rname, md: rname ? md : 99,\n                 cond: fits.length ? '' : cond.slice(0, 40), mates: codes.filter(function (d) { return d !== c && Math.abs(d.y - c.y) < 3; }).map(function (d) { return d.s; }) });\n    });\n    return out;\n  }\n\n  /** 付く機種が作れなかった品は、同じ行のほかの品（色違い・材質違い）から借りる。それでも無ければ、どの機種にも出して品名に条件を書く */\n  function meRoomOptFinish(list) {\n    var byCode = {}, byName = {};\n    list.forEach(function (o) {\n      if (!o.fits.length) return;\n      byCode[o.code] = (byCode[o.code] || []).concat(o.fits);\n      var k = o.page + '｜' + o.name;\n      byName[k] = (byName[k] || []).concat(o.fits);\n    });\n    list.forEach(function (o) {\n      if (o.fits.length) return;\n      /* 自分の条件（「室外機の高さ538mmの機種」「…φ9.52の機種」）がある品は借りない。\n         69ページの吹出ガイドが、同じ品名のMXZ用の品から付く機種を借りていた */\n      if (/の機種|室外機|φ/.test(o.cond || '')) { o.fits = [{ all: true }]; o.name += '（' + o.cond + '）'; return; }\n      (o.mates || []).forEach(function (m) { (byCode[m] || []).forEach(function (f) { o.fits.push(f); }); });\n      // 同じページの同じ品名（色違い：71ページ MAC-L11WS ホワイト／L12BS ベージュ）から借りる\n      if (!o.fits.length) (byName[o.page + '｜' + o.name] || []).forEach(function (f) { o.fits.push(f); });\n      if (o.fits.length) { o.cond = ''; return; }\n      o.fits = [{ all: true }];\n      if (o.cond) o.name += '（' + o.cond + '）';\n    });\n    // 同じ形名が何か所にもあるとき（55〜57ページの必要部品の表と品名の表）は、行に名前のある方を先に\n    var ok = list.slice().sort(function (a, b) { return (b.named ? 1 : 0) - (a.named ? 1 : 0) || a.md - b.md || a.name.length - b.name.length; })\n      .map(function (o) { return { page: o.page, code: o.code, name: o.name, y: o.y, fits: o.fits }; });\n    return optResult(ok, '三菱電機', 'ルームエアコン（住宅設備用） 別売品');\n  }\n\n  /* --------------------------------------------------------------------\n     三菱電機\n     Mr.SLIM（店舗・事務所用パッケージエアコン）\n\n     **このメーカーだけ、PDFではなくデータファイル（.json）を読む。**\n     三菱の総合カタログには価格が載っていない（2026-09-02 に紙面で確認済み）。\n     価格は、三菱の機種検索ページが読み込んでいるデータファイルの中にある。\n     ブラウザでそのリンクを開いて保存すれば、ただのファイルなので放り込める。\n\n     中身は1機種＝1つのかたまりで、品番・価格・シリーズ・室内機タイプまで\n     全部そろっている。紙面を読み解く必要がないぶん、5社の中でいちばん楽。\n     -------------------------------------------------------------------- */\n  function mitsuFinish(list) {\n    var rows = [], seen = {}, noPrice = 0;\n    var dash = function (v) { return (!v || v === '-') ? '' : String(v); };\n\n    list.forEach(function (x) {\n      var m = x && x.set_model ? String(x.set_model) : '';\n      if (!m || seen[m]) return;\n      seen[m] = 1;\n      // 「1,078,000 円(税別)」のように単位と注が付いている。数字のところだけ取る\n      var y = yen((String(x.price || '').match(/[\\d,]+/) || [])[0]);\n      if (!y) { noPrice++; return; }\n      var hp = Number((String(x.ability || '').match(/<\\s*([\\d.]+)\\s*馬力/) || [])[1]) || 0;\n      rows.push({\n        m: m, hp: hp, y: y,\n        u: String(x.url || '').replace(/^.*\\//, ''),\n        s: dash(x.out_name) || 'その他',\n        i: dash(x.in_name) || 'その他',\n        ab: dash(x.ability),\n        pw: dash(x.power) || '三相',\n        rc: dash(x.remocon) || 'リモコン別売',\n        tp: dash(x.type) || 'シングル',\n        opt: dash(x.wide),\n        om: dash(x.out_model), im: dash(x.in_model),\n        pm: dash(x.panel_model), rm: dash(x.rc_model)\n      });\n    });\n\n    return {\n      rows: rows,\n      pricePages: rows.length ? 1 : 0,   // 紙ではないので「ページ」は数えない\n      skippedNoPrice: noPrice,\n      head: {\n        maker: '三菱電機',\n        brand: 'Mr.SLIM（店舗・事務所用パッケージエアコン）',\n        source: '三菱電機 Mr.SLIM 機種検索のデータファイル',\n        note: '価格は税別のメーカー標準価格です。配管・据付工事費は含みません。社内利用限定（第三者提供不可）。',\n        seriesOrder: ['スリムZR', 'スリムER', 'ズバ暖スリムHシリーズ', 'ズバ暖スリムDHシリーズ'],\n        typeOrder: ['シングル', '同時ツイン', '同時トリプル', '同時フォー'],\n        urlBase: 'https://www.mitsubishielectric.co.jp/ldg/wink/qr/002/'\n      }\n    };\n  }\n\n  /* ====================================================================\n     別売品（オプション）を読む\n     --------------------------------------------------------------------\n     機種データとは別の表。紙面の作りが違うので読み方も別にしてある。\n\n     表の作り（ダイキン p.185〜240 で確認）\n     ・列の見出し＝**その別売品が付く室内機**（FHCP40〜71GA など）\n     ・マスに品番と価格があれば「付く」、**「―」なら付かない**\n       （「―」＝非対応は NotebookLM でも確認した）\n     ・品名は左側に縦に並ぶ。しかも表のマスが縦につながっているので、\n       品名の文字が**その行より下に置かれている**ことがある。\n       だから「上の行から引き継ぐ」では取れない。\n       x の帯ごとに、その行にいちばん近い文字を採る。\n\n     ここを間違えると「フレッシュホワイト」のような色だけの名前になる。\n     正しくは「センシング機能無しパネル 標準パネル フレッシュホワイト」。\n     ==================================================================== */\n  var OPT_MONEY = /^[¥￥]?\\s*((?:[1-9]\\d{0,2},)?\\d{1,3},\\d{3})\\s*円?/;\n  var OPT_CODE = /^[A-Z][A-Z0-9\\-]{3,}$/;\n  var OPT_DASH = /^[―—‐\\-–]$/;\n  // 室内機の見出し（FHCP40〜71GA、FHP224・280DB など）\n  var OPT_HEAD = /^F[A-Z]{1,3}\\d{2,3}\\s*[～~〜・,、]\\s*\\d{2,3}\\s*[A-Z]{1,2}$|^F[A-Z]{1,3}\\d{2,3}\\s*[A-Z]{1,2}$/;\n  var OPT_COMMON = '(各機種共通)';\n\n  function isJa(s) { return /[ぁ-ヿ一-鿿]/.test(s); }\n\n  /** 行にまとめる（マスは分けたまま） */\n  function optRows(items) {\n    var a = items.filter(function (i) { return i.s.trim(); })\n      .sort(function (p, q) { return q.y - p.y || p.x - q.x; });\n    var rows = [], cur = null;\n    a.forEach(function (o) {\n      if (!cur || Math.abs(cur.y - o.y) > 3) { cur = { y: o.y, cells: [] }; rows.push(cur); }\n      cur.cells.push(o);\n    });\n    rows.forEach(function (r) { r.cells.sort(function (p, q) { return p.x - q.x; }); });\n    return rows;\n  }\n\n  /* 品番の値段を探す。\n     まず同じ行の右どなり。無ければ**すぐ下の行**の、真下あたりを見る。\n     日立の表は「品番の行」と「金額の行」が上下に分かれていることがあり、\n     前は同じ行しか見ていなかったので、その表の品目がまるごと落ちていた\n     （2026-09-09、日立 p.88 てんつり BG-56NUP2／67,000円 で分かった）。 */\n  function optPriceRightOrBelow(rows, r, i, o) {\n    var money = false;\n    for (var j = i + 1; j < r.cells.length; j++) {\n      var t = r.cells[j].s.trim();\n      if (OPT_CODE.test(t)) break;\n      var mm = t.match(OPT_MONEY);\n      if (mm) return { y: yen(mm[1]), x: r.cells[j].x };\n    }\n    // その行のどこかに金額があるなら、値段は同じ行に書く表。下の行は見ない（となりの品番の値段を取ってしまう）\n    r.cells.forEach(function (c) { if (OPT_MONEY.test(c.s.trim())) money = true; });\n    if (money) return null;\n\n    var best = null, bd = 1e9;\n    rows.forEach(function (q) {\n      var dy = r.y - q.y;                     // y は下から上。dy>0 が「下の行」\n      if (dy <= 0 || dy > 14) return;\n      q.cells.forEach(function (c) {\n        var m = c.s.trim().match(OPT_MONEY);\n        if (!m) return;\n        var dx = Math.abs(c.x - o.x);\n        if (dx > 40) return;\n        var d = dy * 2 + dx;\n        if (d < bd) { bd = d; best = { y: yen(m[1]), x: c.x }; }\n      });\n    });\n    return best;\n  }\n\n  /** 列の見出し（＝付く室内機）を集める */\n  function optColumns(rows) {\n    var cols = [];\n    rows.forEach(function (r) {\n      r.cells.forEach(function (c) {\n        var s = c.s.replace(/\\s/g, '');\n        if (!OPT_HEAD.test(s)) return;\n        var col = null;\n        cols.forEach(function (k) { if (!col && Math.abs(k.x - c.x) < 40) col = k; });\n        if (!col) { col = { x: c.x, heads: [], y: c.y }; cols.push(col); }\n        if (col.heads.indexOf(s) < 0) col.heads.push(s);\n        col.y = Math.max(col.y, c.y);\n      });\n    });\n    return cols.sort(function (a, b) { return a.x - b.x; });\n  }\n\n  /* --------------------------------------------------------------------\n     品名を組み立てる道具（5社で共通に使う）\n\n     品名は左側に「大分類 ｜ 品名 ｜ 色」のように縦の列で並ぶ。\n     しかも表のマスが縦につながっていて、**品名の文字がその行より下に\n     置かれていることがある**（マスの中央に置かれるため）。\n     だから「上の行から引き継ぐ」では取れない。\n\n     ① 隙間で細かく切って、始まりの x を数え、列の位置を決める\n     ② 断片を列に割り当て、同じ列のものはつなげる\n     ③ 行ごとに、列（帯）ごとの「いちばん近い文字」を拾ってつなげる\n     -------------------------------------------------------------------- */\n  /* 品名として通してよい文字かどうか。\n\n     狭い欄に折り返して書かれた注意書きが、品名の列に紛れ込む。\n\n       右記以外のフレキ        ← 注意書きの1行目\n       シブルダクト（丸ダク    ← 2行目。ここだけ拾うと品名が「シブルダクト（丸ダク」になる\n       ト）関連部材につい\n       てはP.204以降を\n\n     途中で切れた行は**かっこの数が合わない**か、**助詞で終わる**。そこで見分ける。\n     （2026-09-09、ダイキン K-SGRS18A2FF とキヤリア TCB-PCNT31TL で分かった） */\n  function nameLike(s) {\n    var t = String(s || '').trim();\n    if (!t) return false;\n    if (/[■●▲◆]/.test(t)) return false;                 // 紙面の飾り記号\n    if (/P\\.\\s*\\d/.test(t)) return false;                // 「P.204以降を」のような参照\n    if (/[をにはがのてでと、]$/.test(t)) return false;     // 助詞で終わる＝文の途中\n    if (/^(右記|左記|上記|下記|前記|同左|同上|その他の)/.test(t)) return false;  // 注意書きの書き出し\n    var open = (t.match(/[（(]/g) || []).length;\n    var close = (t.match(/[）)]/g) || []).length;\n    if (open !== close) return false;                     // かっこが片方だけ＝行の途中で切れている\n    return true;\n  }\n\n  function nameReader(rows, leftEnd, headY, mode) {\n    function chops(cells) {\n      var seg = [], cur = null;\n      cells.filter(function (o) {\n        if (o.x >= leftEnd) return false;\n        // 紙面のいちばん端（x<25）に1文字ずつ縦に並んでいるのはページの見出し。\n        // 品名の列ではないので入れない（「カセット形」が品名に混ざっていた）\n        if (o.x < 25 && o.s.trim().length <= 1) return false;\n        return true;\n      })\n        .sort(function (p, q) { return p.x - q.x; })\n        .forEach(function (o) {\n          if (!cur || o.x > cur.right + 4) { cur = { x: o.x, right: o.x + o.w, s: o.s }; seg.push(cur); }\n          else { cur.s += o.s; cur.right = Math.max(cur.right, o.x + o.w); }\n        });\n      return seg;\n    }\n\n    var hist = {};\n    rows.forEach(function (r) {\n      if (r.y >= headY - 2) return;\n      chops(r.cells).forEach(function (s) { hist[s.x] = (hist[s.x] || 0) + 1; });\n    });\n    var colX = [];\n    Object.keys(hist).map(Number).sort(function (a, b) { return a - b; }).forEach(function (x) {\n      var n = hist[x], near = null;\n      colX.forEach(function (k) { if (!near && Math.abs(k.x - x) < 12) near = k; });\n      if (near) { if (n > near.n) { near.x = x; near.n = n; } }\n      else if (n >= 2) colX.push({ x: x, n: n });\n    });\n    colX.sort(function (a, b) { return a.x - b.x; });\n\n    function words(cells) {\n      var byCol = {};\n      chops(cells).forEach(function (s) {\n        var col = null, bd = 1e9;\n        colX.forEach(function (k) { var d = Math.abs(k.x - s.x); if (d < bd) { bd = d; col = k; } });\n        var key = col ? col.x : s.x;\n        byCol[key] = (byCol[key] || '') + s.s;\n      });\n      return Object.keys(byCol).map(Number).sort(function (a, b) { return a - b; })\n        .map(function (x) { return { x: x, s: byCol[x].replace(/注[\\d,]+/g, '').replace(/※\\d+/g, '').trim() }; })\n        .filter(function (o) { return isJa(o.s) && o.s.length >= 2; });\n    }\n\n    var bands = [];\n    rows.forEach(function (r) {\n      if (r.y >= headY - 2) return;\n      words(r.cells).forEach(function (w) {\n        var b = null;\n        bands.forEach(function (k) { if (!b && k.x === w.x) b = k; });\n        if (!b) { b = { x: w.x, at: [] }; bands.push(b); }\n        b.at.push({ y: r.y, s: w.s });\n      });\n    });\n    bands.sort(function (a, b) { return a.x - b.x; });\n\n    // mode が above のときは「その行より上」に置かれた見出しだけを見る。\n    // 品名をマスの上端に書く社（パナソニック）は、これでないと1行ずれる。\n    return function (y) {\n      var parts = [];\n      bands.forEach(function (b) {\n        var best = null, bd = 1e9;\n        b.at.forEach(function (l) {\n          if (!l.s || l.s.length < 2) return;\n          // 表の下や横に書かれた注記の文章は品名ではない\n          // （「『エアーフィルター』は室内ユニットに標準で…です。」が品名に付いていた）\n          if (l.s.length > 12 && /[。］」]/.test(l.s)) return;\n          if (!nameLike(l.s)) return;\n          if (mode === 'above' && l.y < y - 2) return;\n          var d = Math.abs(l.y - y);\n          if (d < bd) { bd = d; best = l; }\n        });\n        // vert（品番が縦に並ぶ表）は、品名がマスの先頭にしか書かれず離れているので広めに見る\n        var far = (mode === 'above' ? 60 : (mode === 'vert' ? 60 : 30));\n        if (best && bd <= far) parts.push(best.s);\n      });\n      var uniq = [];\n      parts.forEach(function (s) {\n        var dup = false;\n        uniq.forEach(function (u) { if (u.indexOf(s) >= 0) dup = true; });\n        if (!dup) uniq.push(s);\n      });\n      return uniq.join(' ')\n        .replace(/[①-⑳]/g, ' ')     // 紙面の丸数字（①②③…）は品名ではない\n        .replace(/[（(]\\s*注\\s*[\\d,\\s]+\\s*[）)]?/g, ' ')   // （注 2 ）のような注記番号\n        .replace(/^品名\\s*/, '')                          // 見出しの「品名」を拾うことがある\n        .replace(/(FIVE\\s*STAR\\s*ZEAS|Eco-?ZEAS|スゴ暖\\s*ZEAS)\\s*シリーズ(のみ)?に?適用?/g, ' ')\n        .replace(/(FIVE\\s*STAR\\s*ZEAS|Eco-?ZEAS)\\s*(および|シリーズ)?/g, ' ')\n        .replace(/[\\d,]{3,}\\s*円?/g, ' ')\n        .replace(/\\b[A-Z][A-Z0-9\\-]{3,}\\b/g, ' ')\n        .replace(/適用機種/g, ' ')\n        .replace(/[ -]/g, ' ')\n        .replace(/[（(]\\s*[）)]/g, ' ')      // 注記を消したあとに残る空の（）\n        .replace(/^[・、。\\s]+|[・、。\\s]+$/g, '')\n        .replace(/\\s+/g, ' ')\n        .trim();\n    };\n  }\n\n  function optPage(items, page, out) {\n    var rows = optRows(items);\n    var cols0 = optColumns(rows);\n    var common = !cols0.length;      // 列見出しの無いページは「各機種共通の別売品」\n    var cols = common ? [{ x: 300, heads: [OPT_COMMON], y: -1e9 }] : cols0;\n    var headY = common ? 1e9 : Math.max.apply(null, cols.map(function (k) { return k.y; }));\n    var leftEnd = common ? 300 : cols[0].x - 20;\n\n    // 品名は5社で同じ作りなので、共通の道具で組み立てる\n    var nameAt = nameReader(rows, leftEnd, headY);\n\n    rows.forEach(function (r) {\n      if (r.y >= headY - 2) return;\n      var name = nameAt(r.y);\n      var marks = [];\n      r.cells.forEach(function (o, i) {\n        var s = o.s.trim();\n        if (OPT_DASH.test(s)) { marks.push({ x: o.x, dash: true }); return; }\n        if (!OPT_CODE.test(s)) return;\n        var y2 = 0;\n        for (var j = i + 1; j < r.cells.length; j++) {\n          var t = r.cells[j].s.trim();\n          if (OPT_CODE.test(t) || OPT_DASH.test(t)) break;   // 次のマスに入ったら打ち切り\n          var mm = t.match(OPT_MONEY);\n          if (mm) { y2 = yen(mm[1]); break; }\n        }\n        marks.push({ x: o.x, code: s, yen: y2 });\n      });\n\n      marks.forEach(function (mk) {\n        if (mk.dash || !mk.code || !mk.yen) return;   // 「―」は付かない／値段の無いものは入れない\n        var col = null, bd = 1e9;\n        cols.forEach(function (k) { var d = Math.abs(k.x - mk.x); if (d < bd) { bd = d; col = k; } });\n        if (!col || bd > 120) return;\n        var fits = col.heads.map(function (h) { return h === OPT_COMMON ? { all: true } : { im: h }; });\n        out.push({ page: page, name: name, code: mk.code, y: mk.yen, fits: fits });\n      });\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     パナソニックの別売品\n     ダイキンと違い、**「適用室内ユニット」という列が1つ**ある。\n       品名 ｜ 適用室内ユニット ｜ 品番 ｜ 希望小売価格\n       天井パネル 標準パネル ホワイト ｜ 全機種 ｜ CZ-160KPU7C ｜ 71,000円\n       自然気化式加湿器            ｜ P40〜P80 ｜ CZ-07ASU7 ｜ 141,000円\n     どの機種タイプの表かは、ページの端に縦書きで書いてある（「４方向天井カセット形」）。\n     -------------------------------------------------------------------- */\n  function optPagePana(items, page, out, opt, ctx) {\n    opt = opt || {};\n    var codeWord = opt.codeWord || /品番|部品形名/;\n    var priceWord = opt.priceWord || /希望小売価格|価格/;\n    var types = opt.types || null;\n    var rows = optRows(items);\n\n    var head = null;\n    rows.forEach(function (r) {\n      if (head) return;\n      var t = r.cells.map(function (o) { return o.s; }).join('');\n      if (/適用室内ユニット/.test(t) && codeWord.test(t)) head = r;\n    });\n    if (!head) return;\n\n    var xOf = function (word) {\n      var x = null;\n      head.cells.forEach(function (c) { if (x === null && c.s.indexOf(word) >= 0) x = c.x; });\n      return x;\n    };\n    // 見出しのマスが「適用室内ユニッ」「ト」のように割れることがあるので、頭の数文字で探す\n    var xFit = xOf('適用室内');\n    var xCode = null, xPrice = null;\n    head.cells.forEach(function (c) {\n      if (xCode === null && codeWord.test(c.s)) xCode = c.x;\n      if (xPrice === null && priceWord.test(c.s) && c.s.indexOf('適用') < 0) xPrice = c.x;\n    });\n    if (xFit == null || xCode == null) return;\n    if (xPrice == null) xPrice = xCode + 80;\n\n    /* この表がどの機種タイプのものかを決める。\n\n       タイプの一覧をもらっているときは、**その名前が紙面にあるかどうか**で決める。\n       まずページのノド（右端）を見て、無ければページ全体から探す。\n       長い名前から先に見る（「高天井用1方向カセット形」を「1方向…」より先に当てる）。\n\n       前はページの端の縦書きを先に使っていて、\n       ・キヤリア……「70㎥/h以下、P112形」をタイプとして拾い、28品目がどこにも当たらない\n       ・パナソニック……1方向天井カセット形のページを天井ビルトインカセット形と取り違え、\n         18機種に別売品が出ない\n       という取り違えが起きていた（2026-09-09）。 */\n    var norm = function (t) {\n      return String(t).replace(/[\\s　]/g, '')\n        .replace(/[０-９]/g, function (c) { return String.fromCharCode(c.charCodeAt(0) - 0xFEE0); });\n    };\n    var type = '';\n    if (types) {\n      var sorted = types.slice().sort(function (a, b) { return b.length - a.length; });\n\n      /* ①「そのマスがタイプ名そのもの」を、ページのいちばん上から探す。\n         これが節の見出し。ページの中の参照（「天井ビルトインカセット形はP.81」など）に\n         引っかからないので、いちばん確か */\n      var top = -1e9;\n      items.forEach(function (o) {\n        var t = norm(o.s);\n        sorted.forEach(function (k) {\n          if (norm(k) !== t) return;\n          if (o.y > top) { top = o.y; type = k; }\n        });\n      });\n\n      // ② ページのノド（端）に入っている小さな見出し\n      if (!type) {\n        var side = norm(items.filter(function (o) { return o.x > xPrice + 30 || o.x < 40; })\n          .map(function (o) { return o.s; }).join(''));\n        sorted.forEach(function (t) { if (!type && side.indexOf(norm(t)) >= 0) type = t; });\n      }\n\n      // ③ それでも決まらなければページ全体から。長い名前を先に見る\n      if (!type) {\n        var whole = norm(items.map(function (o) { return o.s; }).join(''));\n        sorted.forEach(function (t) { if (!type && whole.indexOf(norm(t)) >= 0) type = t; });\n      }\n    }\n    // タイプの一覧が無い社は、ページの端の縦書きを使う\n    if (!type && !types) {\n      items.forEach(function (o) {\n        if (o.x > xPrice + 30 && /形$/.test(o.s) && o.s.length >= 4 && isJa(o.s)) type = o.s;\n      });\n    }\n    // 機種データのページ番号から決めるのがいちばん確か（紙面に書いていない社があるため）\n    if (ctx && ctx.pageTypes) {\n      var byPage = typeAtPage(ctx.pageTypes, page);\n      if (byPage) type = byPage;\n    }\n\n    // 品名は「大分類 ｜ 品名 ｜ 色」の縦の列。色だけの行でも品名を拾えるようにする\n    var nameAt = nameReader(rows, xFit - 25, head.y, 'above');\n\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 2) return;\n\n      var code = '', price = 0, fitTxt = '';\n      r.cells.forEach(function (c) {\n        var s = c.s.trim();\n        /* 品番のマスに注記がくっついていることがある。\n           「TCB-KBCN60（オプション出力）」はマス全体では品番の形にならず、\n           落としていた（キヤリア壁掛形の4品目のうち2つ）。頭から品番だけ取る */\n        if (!code && Math.abs(c.x - xCode) < 40) {\n          var mc = s.match(/^([A-Z][A-Z0-9]*-[A-Z0-9\\-]{2,})/);\n          if (mc) code = mc[1];\n        }\n        if (c.x >= xFit - 25 && c.x < xCode - 15) fitTxt += s;\n        if (c.x >= xPrice - 25) { var m = s.match(OPT_MONEY); if (m && !price) price = yen(m[1]); }\n      });\n      var nm = nameAt(r.y);\n      if (!code || !price) return;\n\n      var fit;\n      if (/全機種/.test(fitTxt)) fit = type ? { type: type } : { all: true };\n      else {\n        var cr = capRange(fitTxt);\n        fit = cr ? (type ? { type: type, cap: cr } : { cap: cr }) : (type ? { type: type } : { all: true });\n      }\n      out.push({ page: page, name: nm, code: code, y: price, fits: [fit] });\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     日本キヤリアの別売品\n     **列の見出しがシリーズ**で、マスに入っているのは「能力ランク」。\n       別売部品 ｜ ウルトラパワーエコ ｜ スーパーパワーエコゴールド ｜ … ｜ 部品形名 ｜ 希望小売価格\n       吹出ガイド ｜ P40形〜P50形 ｜ P40形〜P63形 ｜ … ｜ TCB-G50 ｜ ¥14,000\n     「―」はそのシリーズには付かない印。\n     シリーズ名は2行に割れて書かれている（「ウルトラ」＋「パワーエコ®」）ので、\n     見出しのまわり数行を x でまとめてから読む。\n     -------------------------------------------------------------------- */\n  /* --------------------------------------------------------------------\n     キヤリアの「別売部品一覧、および組合せ可否」のページ（p.57 など）\n\n       部品名                    部品形名           価格（税別）\n       天井パネル（標準）          RBC-U43PG★       ¥67,000\n       オートグリルパネル          RBC-UA43PG（W）   ¥100,000\n\n     右半分は組合せの○×がびっしり並ぶが、そこは読まない。\n     どの機種に付くかは、手前の価格ページから覚えたタイプで決める。\n\n     **室内機に付く別売品（パネル・フィルター・リモコン）はここにしか無い。**\n     前はこの表を読んでおらず、天井カセット形4方向の240機種には\n     室外機の架台しか出ていなかった（2026-09-09）。\n     -------------------------------------------------------------------- */\n  /* --------------------------------------------------------------------\n     キヤリアの別売品の品名（2026-09-10 作り直し）\n     ----------------------------------------------------------------------\n     前は nameReader（上下どちらでも近いものを採る・列ごとに拾ってつなぐ）で\n     読んでいて、166品目中49件の品名が崩れていた。BIGBOSSの画面で\n     「○1○2吹出ガイド」「御 ワイヤレスリモコン受信部 ワイヤードリモコン」と出た。\n\n     紙面を見て分かったこと（57・127〜129ページ）\n       ・丸数字は「①」ではなく「○」＋数字、または「12 高さ調整」の形で来る\n       ・「受」「在」は受注生産・在庫限りのマーク。■も同じ。品名ではない\n       ・「制御」「空質関連」は1文字ずつ縦に並んだ見出し。品名ではない\n       ・■ があるだけで品名を「品名らしくない」とはねていたので、\n         自分の行の品名が消え、となりの行の品名2つがつながっていた\n         （TCB-TCU41L は「リモートセンサー」なのに「ワイヤレスリモコン受信部」になった）\n\n     表の作りが2通りある。\n       一覧の表（室内機用）……品名は品番と**同じ行**。2行にまたがるマスだけ\n                              （「プラズマ空清」「ユニット」）はつなぐ\n       室外機の表          ……品名は背の高いマスの**いちばん上**に1回だけ。\n                              その下の品番は**上の見出し**を受け継ぐ\n     -------------------------------------------------------------------- */\n\n  /** 品名の列（leftEnd より左）の文字を、行ごとに「かたまり」にする */\n  function carSegs(rows, leftEnd, headY, leftStart) {\n    var out = [];\n    var from = (leftStart == null) ? -1e9 : leftStart;\n    rows.forEach(function (r) {\n      if (r.y >= headY - 2) return;\n      var cur = null;\n      r.cells.filter(function (o) { return o.x < leftEnd && o.x >= from; })   // 表の左にある図の文字は見ない\n        .sort(function (p, q) { return p.x - q.x; })\n        .forEach(function (o) {\n          /* 1文字のかけら（縦書き見出しの「制」、マークの「在」）は、となりの品名とつながない。\n             すき間が4ポイントより狭いと、前は「制 ワイヤレスリモコン」とひとかたまりにしていた。\n             1文字どうし（1文字ずつに割れて来た語）はつなぐ */\n          var one = o.s.trim().length <= 1 && /[一-龥ぁ-んァ-ヶ]/.test(o.s);\n          /* 1文字のかな・漢字は、ふつうは切り離す（縦書き見出しの「制」、マークの「在」）。\n             ただし、前の語とのすき間がほとんど無い（1.5以内）ときは語の続き。\n             pdf.js は「防護ネットセット」を「防護ネットセッ」＋「ト」に割ってよこすので、\n             切り離すと「防護ネットセッ」になっていた（2026-09-10） */\n          var tight = cur && (o.x - cur.right) <= 1.5;\n          if (!cur || o.x > cur.right + 4 || (one !== cur.one && !tight) || Math.abs(o.y - cur.oy) > 2.5) {\n            cur = { x: o.x, y: r.y, oy: o.y, right: o.x + (o.w || 0), s: o.s, one: one };\n            out.push(cur);\n          } else {\n            cur.s += o.s;\n            cur.right = Math.max(cur.right, o.x + (o.w || 0));\n          }\n        });\n    });\n    return out;\n  }\n\n  /** 丸数字・マーク・注記番号・くっついた品番を落とす */\n  function carClean(s) {\n    return String(s || '')\n      .replace(/[\\u2000-\\u200b\\u3000]/g, ' ')\n      .replace(/[\\u0600-\\u06FF]/g, '')                    // pdf.js が記号の字形をアラビア文字に読み違えたかけら\n      .replace(/[\\u2460-\\u24FF\\u2776-\\u2793\\u3251-\\u325F\\u32B1-\\u32BF]/g, ' ')\n      .replace(/○\\s*\\d{1,2}/g, ' ')\n      .replace(/○/g, ' ')                                  // 数字と離れて残った丸（「○壁取付架台」）\n      /* 区分の語（縦書きの見出し）。pdf.js は横書きの1語でよこし、品名の頭に付くことがある。\n         うしろにすき間か番号が続くときだけ外す（「吸込ハーフパネル」の「吸込」は品名なので外さない） */\n      .replace(/^\\s*(空質関連|施工関連|フィルターほか|リモコン・グリル|据\\s*付|吸\\s*込|吹\\s*出|制\\s*御|空\\s*質|その他)(?=\\s|\\d|[\\u2460-\\u24FF\\u2776-\\u2793])\\s*/, '')\n      .replace(/^\\s*\\d{1,2}[a-z]?\\s+(?=[^\\d\\s])/, '')    // 丸数字が「12 」「1a 」の形で来るとき\n      .replace(/^\\s*\\d{1,2}[a-z]?(?=[ぁ-んァ-ヶ一-龥（(Ａ-Ｚａ-ｚ])(?![方形個本枚台])/, '')   // 「10高湿度対応キット」「1a天井パネル」「2Ｌ字配管キット」（丸数字の下に小さい番号）。「2方向」は数なので残す\n      .replace(/^\\s*[a-z](?=[ぁ-んァ-ヶ一-龥])/, '')      // 「bオートグリルパネル」\n      .replace(/^\\s*[一-龥]\\s+[一-龥](?=[^\\s\\d])/, '')   // 「据 付ドレンアップキット」「吸 込下面吸込ボックス」（区分がくっついた）\n      .replace(/※\\s*\\d+/g, ' ')\n      .replace(/[■□◆◇●★☆]/g, ' ')\n      .replace(/[A-Z][A-Z0-9]*-(?=[A-Z0-9\\-]*\\d)[A-Z0-9\\-]{2,}/g, ' ')   // 品名にくっついた品番（品番には必ず数字がある。TCC-LINK は品名）\n      .replace(/\\s+/g, ' ').trim()\n      .replace(/([^\\s])\\s*[受在]$/, '$1')                  // うしろの「受」「在」マーク\n      .trim();\n  }\n\n  function carNumbered(raw) {\n    return /^[\\s\\u2000-\\u200b\\u3000]*([\\u2460-\\u24FF\\u2776-\\u2793\\u3251-\\u325F\\u32B1-\\u32BF]|○\\s*\\d|\\d{1,2}[\\s\\u2000-\\u200b])/.test(raw);\n  }\n\n  /** 一覧の表：品名は品番と同じ行 */\n  function carrierPartsNamer(rows, leftEnd, headY, codeYs, tableLeft) {\n    if (!codeYs.length) return function () { return ''; };\n    var lo = Math.min.apply(null, codeYs) - 8;\n    var onCode = function (y) { return codeYs.some(function (c) { return Math.abs(c - y) <= 3.5; }); };\n    var all = carSegs(rows, leftEnd, headY, tableLeft);\n    var segs = all.filter(function (g) {\n      if (g.y < lo) return false;                  // 表の下の注記\n      if (/。/.test(g.s)) return false;\n      var t = carClean(g.s);\n      return t.length >= 2 && isJa(t);            // 縦書き見出しの1文字・「在」マークはここで落ちる\n    });\n    if (!segs.length) return function () { return ''; };\n    /* 品名の列は、品番の行にいちばん多く出てくる列。\n       その左の列は区分の見出し（「据　付」「吸込・吹出」「制御」）なので落とす（74ページ）。\n       「P40形〜P160形」「全機種」のような適用の列は数えない。ほぼ全部の行にあるので、\n       数えると品名の列より多くなってしまう（103ページ） */\n    var CAPLIKE = /P\\d+形|全機種|筐体/;\n    var colsX = [];\n    segs.forEach(function (g) {\n      if (!onCode(g.y) || CAPLIKE.test(g.s)) return;\n      var c = null;\n      colsX.forEach(function (k) { if (!c && Math.abs(k.x - g.x) < 6) c = k; });\n      if (!c) { c = { x: g.x, n: 0 }; colsX.push(c); }\n      c.n++;\n    });\n    if (!colsX.length) return function () { return ''; };\n    var mainX = colsX.reduce(function (a, b) { return b.n > a.n ? b : a; }).x;\n    var subX0 = mainX + 35;\n    var hasName = function (y) {\n      return segs.some(function (h) { return Math.abs(h.y - y) <= 1 && h.x >= mainX - 6 && h.x < subX0; });\n    };\n    segs = segs.filter(function (g) {\n      if (g.x >= mainX - 6) return true;\n      return g.right > mainX + 4 && !hasName(g.y);\n    });\n    var subX = mainX + 35;                         // 右の列は「ハイタイプ」「P40形〜P71形」などの補足\n\n    /* 1つの品名のマスに品番が何行もあり、2行目から下に注意書きが並ぶ表がある（67ページ）\n         「交換の目安 2,500時間」「ご使用の際には必須です。」「オプションフィルター③〜⑤を」\n       これは品名ではない。上の品名を受け継ぐ */\n    var NOTE = /目安|必須|ご使用|使用時|突出|ご確|認ください|能力|条件|参照|します|です|加湿量|マンセル|\\d\\s*[〜～]\\s*\\d/;\n    var ys = codeYs.slice().sort(function (a, b) { return b - a; });   // 上の行から\n    var info = ys.map(function (y) {\n      var own = segs.filter(function (g) { return Math.abs(g.y - y) <= 3.5; });\n      var raw = own.filter(function (g) { return g.x < subX; }).map(function (g) { return g.s; }).join(' ');\n      var marks = all.filter(function (g) {\n        return Math.abs(g.y - y) <= 3.5 && g.x >= mainX - 12 && g.x < subX;\n      });\n      return {\n        y: y,\n        main: carClean(raw),\n        numbered: carNumbered(raw) || marks.some(function (g) { return carNumbered(g.s) || /^[\\u2460-\\u24FF\\u2776-\\u2793\\u3251-\\u325F\\u32B1-\\u32BF★]$/.test(g.s.trim()); }),\n        sub: carClean(own.filter(function (g) { return g.x >= subX; }).map(function (g) { return g.s; }).join(' '))\n               .replace(/^全機種$/, '')\n      };\n    });\n\n    info.forEach(function (it) {\n      if (it.main && NOTE.test(it.main)) { it.main = ''; it.note = true; }\n      else if (it.main && /^[（(]/.test(it.main)) { it.paren = it.main; it.main = ''; }\n    });\n    /* 自分の行に品名が無い＝品名が品番の行から少しずれて書いてある。\n         2行で1つの品番の行を上下からはさむ（「オートグリル操作専用」「ワイヤレスリモコン」）\n         2行ぶんのマスのまん中に1行（「高性能フィルター」）\n       はさんでいる行を上からつないで、その行の品名にする。\n       **つなぎ判定より先にやる。**57ページの「12 高さ調整」は品番の行から3.8ずれていて、\n       後回しにすると、つなぐかどうか決める時点で品名が空のままになり、\n       下の行の「スペーサー」とつながらなかった（「高さ調整」「スペーサー」に割れた）。\n       ほかの品番の行に「ほぼ乗っている」（2以内）ものは、その品番の品名なので採らない。\n       3.5 にすると、まん中より少し下にある品名を下の品番に取られていた（57ページ UFM1604UA） */\n    info.forEach(function (it) {\n      if (it.main || it.note || it.paren) return;\n      var near = segs.filter(function (g) {\n        if (g.x >= subX || Math.abs(g.y - it.y) > 7) return false;\n        if (NOTE.test(g.s)) return false;\n        return !codeYs.some(function (c) { return c !== it.y && Math.abs(c - g.y) <= 2; });\n      }).sort(function (a, b) { return b.y - a.y; });\n      if (!near.length) return;\n      it.main = carClean(near.map(function (g) { return g.s; }).join(''));\n      it.numbered = carNumbered(near[0].s);\n    });\n\n    // 2行にまたがるマス。補足のある行が続き、下の行の品名に番号が無ければ続き\n    info.forEach(function (it, i) {\n      var prev = info[i - 1];\n      if (prev && prev.group && prev.sub && it.sub && it.main && !it.numbered &&\n          /^[ァ-ヶー]{2,6}$/.test(it.main) && prev.main && prev.main.length <= 8) {\n        it.group = prev.group;\n        it.group.text += it.main;\n      } else if (it.main) {\n        it.group = { text: it.main };\n      }\n    });\n    // 自分の行に品名が無い＝2行ぶんのマスのまん中に1行で書いてある（「高性能フィルター」）\n    info.forEach(function (it, i) {\n      if (it.group) return;\n      /* 受け継ぐ先は、すぐ上にある品名らしい行。品番の無い行に書いた見出しもふくめる。\n         83ページの「⑩気化式加湿器」は品番の無い行にあり、その下の3行（品名の欄は\n         「加湿量（kg/h）…」という注記）が、品番のある行しか見ていなかったせいで\n         さらに上の「⑨丸ダクト用フランジ」を受け継いでいた（2026-09-10） */\n      var up = null;\n      segs.forEach(function (g) {\n        if (g.x >= subX || g.y <= it.y || g.y - it.y > 30) return;\n        if (NOTE.test(g.s) || /^[（(]/.test(g.s.trim()) || carClean(g.s).length < 2) return;\n        if (!up || g.y < up.y) up = g;\n      });\n      if (up) {\n        // その行が品番のある行なら、その行の品名（つないだ・かっこを足した形）をそのまま使う\n        var owner = null;\n        info.forEach(function (o) { if (o.group && Math.abs(o.y - up.y) <= 2) owner = o; });\n        var base = owner ? owner.group.text : carClean(up.s);\n        it.group = it.paren ? { text: base + it.paren } : (owner ? owner.group : { text: base });\n        return;\n      }\n      // マスの上端に品名を書く表では、すぐ上の行の品名を受け継ぐ。\n      // かっこ書き（「（ロングライフフィルター付）」）は、その品名の続きとして付ける\n      for (var k = i - 1; k >= 0; k--) {\n        if (info[k].group && info[k].y - it.y < 45) {\n          it.group = it.paren ? { text: info[k].group.text + it.paren } : info[k].group;\n          break;\n        }\n      }\n      if (!it.group && it.paren) it.group = { text: it.paren };\n    });\n\n    var by = {};\n    info.forEach(function (it) {\n      var name = it.group ? it.group.text : '';\n      if (it.sub) name = name ? name + ' ' + it.sub : it.sub;\n      by[it.y] = name;\n    });\n    return function (y) { return by[y] || ''; };\n  }\n\n  /** 「TCB-G50 ○」「TCB-G50 ①」のように、品番のうしろに丸数字が付いて来ても頭の品番を取る。\n      pdf.js はこの形でよこす（PyMuPDF は分けてよこしていたので、前は気づかなかった）。\n      「TCB-G802×2」（2台ぶんのセット）は本物の TCB-G802 とぶつかるので、品番とみなさない */\n  function carCodeHead(s) {\n    var m = String(s || '').trim().match(/^([A-Z][A-Z0-9]*-[A-Z0-9\\-]{2,})(.*)$/);\n    if (!m || /[×xX]\\s*\\d/.test(m[2])) return '';\n    return OPT_CODE.test(m[1]) ? m[1] : '';\n  }\n\n  /** 室外機の表：品名はマスのいちばん上。下の品番は上の見出しを受け継ぐ */\n  function carrierOutdoorNamer(rows, leftEnd, headY, codeYs) {\n    var segs = carSegs(rows, leftEnd, headY);\n    /* 見出しは列の左はしに立つ。絵の説明（「吸込フード（側面）」「①支柱」）は内側に寄っている。\n       その左はしは、**丸数字つきの見出し（「①② 吹出ガイド」）の x** で決める。\n       ページの端のつまみ（「室外」「別売部品」）は pdf.js では横書きの1語で来るので、\n       名前らしい文字のいちばん左で決めると、つまみを左はしと取り違え、\n       本物の見出しを内側の説明文として捨てていた（2026-09-10、128ページ。品名が全部空になった） */\n    var edge = 1e9, edgeAny = 1e9;\n    segs.forEach(function (g) {\n      var t = carClean(g.s);\n      if (t.length < 2 || !isJa(t)) return;\n      edgeAny = Math.min(edgeAny, g.x);\n      if (carNumbered(g.s)) edge = Math.min(edge, g.x);\n    });\n    if (edge === 1e9) edge = edgeAny;\n    var heads = [];\n    segs.forEach(function (g) {\n      if (g.x > edge + 8 || g.x < edge - 8) return;      // つまみ（左はしより外）も見出しにしない\n      var raw = g.s.replace(/[\\u2000-\\u200b\\u3000]/g, ' ').trim();\n      if (/^[（(〈<]|^[a-z]\\s|、|。/.test(raw)) return;   // 「（深形）」「a 防雪フード」「上・下吹き、…」\n      var t = carClean(raw);\n      if (t.length < 2 || !isJa(t)) return;\n      var onRow = codeYs.some(function (y) { return Math.abs(y - g.y) <= 3.5; });\n      if (!carNumbered(raw) && !onRow) return;\n      // すぐ下の「（深形）」「(ドレンパン付)」は品名の続き\n      segs.forEach(function (h) {\n        if (h.y < g.y - 14 || h.y > g.y - 4 || Math.abs(h.x - g.x) > 15) return;\n        var hs = h.s.replace(/[\\u2000-\\u200b\\u3000]/g, ' ').trim();\n        if (/^[（(]/.test(hs) && hs.length <= 16) t += carClean(hs);\n      });\n      heads.push({ y: g.y, s: t });\n    });\n    return function (y) {\n      var best = null;\n      heads.forEach(function (h) {\n        if (h.y >= y - 3.5 && (!best || h.y < best.y)) best = h;   // 上にあるもので、いちばん近いもの\n      });\n      return best ? best.s : '';\n    };\n  }\n\n  /** その行の品番。品番の列にあるもの、または品名のうしろにくっついて来たもの */\n  function carRowCode(r, xCode) {\n    var code = '';\n    r.cells.forEach(function (c) {\n      var t = c.s.trim();\n      if (!code && Math.abs(c.x - xCode) < 30) {\n        var m = t.match(/^([A-Z][A-Z0-9]*-[A-Z0-9\\-]{2,})/);\n        if (m) code = m[1];\n      }\n    });\n    // 「丸ダクト用フランジ（吹出し分ダクト用）TCB-FF151US」と品名にくっついて来る行がある\n    if (!code) r.cells.forEach(function (c) {\n      if (code || c.x >= xCode - 15) return;\n      var m = c.s.trim().match(/([A-Z][A-Z0-9]*-(?=[A-Z0-9\\-]*\\d)[A-Z0-9\\-]{2,})$/);\n      if (m && isJa(c.s)) code = m[1];\n    });\n    return code;\n  }\n\n  /** ページから「品番 → 品名」を引く（一覧の表の読み方で）。\n      103〜123ページの表は optPagePana（パナソニックと共用）が読むが、その品名は\n      となりの行の品名や縦書きの区分をつないでいた（「制 御 アダプター™受」）。\n      パナソニックには手を入れず、キヤリアだけ後から品名を付け直すのに使う */\n  function carrierPageNames(items) {\n    var rows = optRows(items);\n    var head = null;\n    rows.forEach(function (r) {\n      if (head) return;\n      var t = r.cells.map(function (o) { return o.s; }).join('');\n      if (/部品形名/.test(t) && /価格/.test(t)) head = r;\n    });\n    if (!head) return null;\n    /* 品番の列は、見出しの下で「品番の形をした文字」がいちばん多く並ぶ x。\n       見出しの「部品形名」はマスのまん中寄せなので、列の幅が広い表では左寄せの品番から\n       30以上離れ、品番が1つも見つからずに、品名を付け直さないままになっていた（83ページほか） */\n    var xs = {};\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 2) return;\n      r.cells.forEach(function (c) {\n        if (!carCodeHead(c.s)) return;\n        var k = Math.round(c.x / 4) * 4;\n        xs[k] = (xs[k] || 0) + 1;\n      });\n    });\n    var xCode = null;\n    Object.keys(xs).forEach(function (k) { if (xCode === null || xs[k] > xs[xCode]) xCode = k; });\n    xCode = (xCode === null) ? null : Number(xCode);\n    if (xCode === null) head.cells.forEach(function (c) { if (xCode === null && c.s.indexOf('部品形名') >= 0) xCode = c.x; });\n    if (xCode === null) return null;\n    var yOf = {}, codeYs = [];\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 2) return;\n      var code = carRowCode(r, xCode);\n      if (!code) return;\n      codeYs.push(r.y);\n      if (!(code in yOf)) yOf[code] = r.y;\n    });\n    if (!codeYs.length) return null;\n    var nameAt = carrierPartsNamer(rows, xCode - 15, head.y, codeYs);\n    return function (code) {\n      var y = yOf[code];\n      if (y == null) {\n        // 「RBC-US21PG（W）-1」のように後ろに付いた形でも引けるように\n        Object.keys(yOf).forEach(function (k) { if (y == null && String(code).indexOf(k) === 0) y = yOf[k]; });\n      }\n      return y == null ? '' : nameAt(y);\n    };\n  }\n\n  function optPageCarrierParts(items, page, out, ctx) {\n    var rows = optRows(items);\n    var head = null;\n    rows.forEach(function (r) {\n      if (head) return;\n      var t = r.cells.map(function (o) { return o.s; }).join('');\n      if (/部品形名/.test(t) && /価格/.test(t)) head = r;\n    });\n    if (!head) return;\n\n    var type = (ctx && ctx.pageTypes) ? typeAtPage(ctx.pageTypes, page) : '';\n    if (!type) return;\n\n    var xCode = null, xPrice = null;\n    head.cells.forEach(function (c) {\n      if (xCode === null && c.s.indexOf('部品形名') >= 0) xCode = c.x;\n      if (xPrice === null && c.s.indexOf('価格') >= 0) xPrice = c.x;\n    });\n    if (xCode === null) return;\n    if (xPrice === null) xPrice = xCode + 45;\n\n    // 品番のある行を先に集める（品名の読み方が、行どうしの並びを見るため）\n    function partsCode(r) { return carRowCode(r, xCode); }\n    var codeYs = [];\n    rows.forEach(function (r) { if (r.y < head.y - 2 && partsCode(r)) codeYs.push(r.y); });\n    var nameAt = carrierPartsNamer(rows, xCode - 15, head.y, codeYs);\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 2) return;\n      var code = partsCode(r), price = 0;\n      r.cells.forEach(function (c) {\n        var t = c.s.trim();\n        if (!price && c.x >= xPrice - 20 && c.x < xPrice + 60) {\n          var mm = t.match(/^[¥￥]?\\s*([\\d,]{4,})/);\n          if (mm) price = yen(mm[1]);\n        }\n      });\n      if (!code || !price) return;\n      var nm = nameAt(r.y);\n      if (!nm || nm.length < 2) return;\n      out.push({ page: page, name: nm, code: code, y: price, fits: [{ type: type }] });\n    });\n  }\n\n  function optPageCarrier(items, page, out) {\n    var rows = optRows(items);\n\n    // 見出し行（「部品形名」がある行）\n    var head = null;\n    rows.forEach(function (r) {\n      if (head) return;\n      var t = r.cells.map(function (o) { return o.s; }).join('');\n      if (/部品形名/.test(t)) head = r;\n    });\n    if (!head) return;\n\n    var xCode = null, xPrice = null;\n    head.cells.forEach(function (c) {\n      if (xCode === null && c.s.indexOf('部品形名') >= 0) xCode = c.x;\n      if (c.s.indexOf('希望小売価格') >= 0) xPrice = c.x;\n    });\n    if (xCode === null) return;\n    if (xPrice === null) xPrice = xCode + 70;\n\n    // シリーズの列。見出しの上下3行ぶんの日本語を x でまとめる\n    var band = {};\n    rows.forEach(function (r) {\n      if (Math.abs(r.y - head.y) > 16) return;\n      r.cells.forEach(function (c) {\n        if (c.x >= xCode - 20 || !isJa(c.s)) return;\n        if (/別売部品|適用|能力ランク|希望小売|税別|部品形名|標準|オプション|価格|形名|注|※/.test(c.s)) return;\n        var key = null;\n        Object.keys(band).forEach(function (k) { if (key === null && Math.abs(Number(k) - c.x) < 30) key = k; });\n        if (key === null) { band[c.x] = { x: c.x, s: c.s }; }\n        else { band[key].s += c.s; band[key].x = Math.min(band[key].x, c.x); }\n      });\n    });\n    /* 列の見出しは**シリーズ名**。キヤリアのシリーズは4つと決まっているので、\n       それに合うものだけを列と認める。\n       ゆるくすると注釈文（「音が大きく感じられる場合があります」など）を\n       シリーズとして拾ってしまう。 */\n    var known = [];\n    Object.keys(CARRIER_SERIES).forEach(function (k) { known.push(CARRIER_SERIES[k]); });\n    var cols = Object.keys(band).map(function (k) { return band[k]; })\n      .map(function (b) {\n        var raw = b.s.replace(/[®™\\s]/g, '').replace(/［.*?］|\\[.*?\\]/g, '');\n        var hit = '';\n        known.forEach(function (s) { if (!hit && looseSame(s, raw)) hit = s; });\n        return { x: b.x, series: hit };\n      })\n      .filter(function (b) { return b.series; })\n      .sort(function (a, b) { return a.x - b.x; });\n    if (!cols.length) return;\n\n    var leftEnd = cols[0].x - 20;\n    var codeYs = [];\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 20) return;\n      if (r.cells.some(function (c) {\n        return c.x >= xCode - 25 && c.x < xPrice - 20 && carCodeHead(c.s);\n      })) codeYs.push(r.y);\n    });\n    var nameAt = carrierOutdoorNamer(rows, leftEnd, head.y, codeYs);\n\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 20) return;\n\n      var code = '', price = 0;\n      r.cells.forEach(function (c) {\n        var s = c.s.trim();\n        if (c.x >= xCode - 25 && c.x < xPrice - 20 && !code && carCodeHead(s)) code = carCodeHead(s);\n        if (c.x >= xPrice - 30) { var m = s.match(OPT_MONEY); if (m && !price) price = yen(m[1]); }\n      });\n      if (!code || !price) return;\n\n      // シリーズごとに、そのマスの能力ランクを読む\n      var fits = [];\n      cols.forEach(function (col, i) {\n        var lo = col.x - 18;\n        var hi = (i === cols.length - 1) ? xCode - 25 : cols[i + 1].x - 18;\n        var txt = '', dash = false;\n        r.cells.forEach(function (c) {\n          if (c.x < lo || c.x >= hi) return;\n          if (OPT_DASH.test(c.s.trim())) { dash = true; return; }\n          txt += c.s;\n        });\n        if (dash || !txt) return;\n        var cr = capRange(txt);\n        if (cr) fits.push({ series: col.series, cap: cr });\n      });\n      if (!fits.length) return;\n\n      out.push({ page: page, name: nameAt(r.y), code: code, y: price, fits: fits });\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     日立の別売品（オプション一覧）\n       ■ オプション一覧（ てんかせ 4方向）        ← ページの機種タイプ\n       容量・型名（相当馬力）｜ 28型〜71型 ｜ 80型〜160型   ← 列＝容量の範囲\n       品名 ｜ 基本パネル ｜ デザインパネル ｜ …\n       高性能フィルター … F-71M-K3 24,300円 ｜ F-160M-K3 31,200円\n\n     列の容量範囲は「本体の筐体サイズ」で決まっている（NotebookLM でも確認）。\n     その列の品番は、その容量範囲の機種に付く。\n     -------------------------------------------------------------------- */\n  /* --------------------------------------------------------------------\n     日立の別売品の品名（2026-09-11 作り直し）\n     ----------------------------------------------------------------------\n     前は nameReader（列ごとに、上下どちらでも近いものを採ってつなぐ）で読んでいて、\n     201品目のほとんどの品名が崩れていた（「22注 NEW高湿度対応キット 分ダクトフランジ（φ ） 分ダクト 1m」）。\n     キヤリアと同じ病気。となりの行の品名、左はしの縦書きの区分（補助・ダクト）、\n     注記の番号がつながり、「φ150」の数字は値段と思って消していた。\n\n     日立の表は、左から「区分（縦書き）」→「まとめ書き」→「品名」→「補足」の入れ子のマス。\n       てんかせ4方向  F-71M-K3 ＝ ボックス方式 ＞ 抗菌加工高性能フィルター ＞ 比色法65%相当\n       ビルトイン     FD-1A1   ＝ 吹き出し ＞ フレキシブルダクト（φ200） ＞ 分ダクト 1m\n     読み方\n       ・品番の行と同じ高さにある文字を左から読む\n       ・いちばん右の文字より左で空いている欄は、そのすぐ右の欄で選んだ文字に\n         いちばん近い、その欄のまとめ書きを受け継ぐ\n       ・同じ欄で上下にくっついた2行（「ABS樹脂製」「グリル」）は1つのマス\n       ・区分は縦書き（文字数のわりに幅がとても狭い）なので、それで落とす\n     pdf.js は語を細かく割ってよこす（「ロ」「ングライ」「フ」「フ」「ィ」「ルター」）ので、\n     確かめはブラウザと同じ pdf.js の文字でやった\n     -------------------------------------------------------------------- */\n\n  function hitClean(s) {\n    return String(s || '')\n      .replace(/[\\u2000-\\u200b\\u3000]/g, ' ')\n      .replace(/[（(]\\s*注\\s*[\\d,\\s]+[）)]/g, ' ')       // （注 12 ）\n      .replace(/注\\s*\\d+/g, ' ')\n      .replace(/※\\s*\\d*/g, ' ')\n      .replace(/[（(［\\[]\\s*(受注対応品?|特注対応色?)\\s*[）)］\\]]/g, ' ')   // （受注対応品）［受注対応］\n      .replace(/[［\\[][^］\\]]*寸法[^］\\]]*[］\\]]/g, ' ')                  // ［外形横寸法（mm）］は品名の下の注記\n      .replace(/SEK|NEW|受注対応品?|特注対応色?|[★☆■●◆]/g, ' ')\n      .replace(/[［\\[]\\s*[］\\]]/g, ' ')\n      .replace(/（\\s+/g, '（').replace(/\\s+）/g, '）')\n      .replace(/［\\s+/g, '［').replace(/\\s+］/g, '］')\n      .replace(/[０-９]/g, function (d) { return String.fromCharCode(d.charCodeAt(0) - 0xFEE0); })\n      .replace(/[（(]\\s*[）)]/g, ' ')\n      .replace(/\\s+/g, ' ').trim();\n  }\n\n  // 日立の表の左はしの区分（縦書き）\n  var CATEGORY_WORD = /^(補助|ダクト|フィルター|リモコン|グリル|その他)$/;\n  var CATEGORY_HEAD = /^(補助|ダクト|フィルター|リモコン|グリル|その他)\\s+/;\n\n  function hitachiNamer(rows, nameRight, headY, endY, rowYs) {\n    /* 高さは、行にそろえる前の本当の高さで測る。行のまとめ（optRows）は3ポイントまで寄せるので、\n       88ページでは品番 678.5 の行が 681.0 になり、すぐ下の「脱臭フィルター」（674.8）が\n       その品番の品名だと気づけなかった。呼ぶ側は行の高さ（rowYs）で名前を聞くので、対応を持っておく */\n    var realOf = {};\n    rowYs.forEach(function (ry) {\n      var r = rows.filter(function (x) { return x.y === ry; })[0];\n      var ys = r ? r.cells.filter(function (c) { return OPT_CODE.test(c.s.trim()); }).map(function (c) { return c.y; }) : [];\n      realOf[ry] = ys.length ? ys.reduce(function (a, b) { return a + b; }, 0) / ys.length : ry;\n    });\n    var codeYs = rowYs.map(function (ry) { return realOf[ry]; });\n\n    // その高さに近い品番の行（品名の文字は品番より5ポイントほどずれることがある）\n    function codeYOf(y, tol) {\n      var best = null, bd = tol;\n      codeYs.forEach(function (c) { var d = Math.abs(c - y); if (d <= bd) { bd = d; best = c; } });\n      return best;\n    }\n    var isLatin = function (t) { return /^[A-Z]{2,4}$/.test(t); };   // 「ABS」樹脂製グリル\n\n    /* 注の番号「（注 11 ）」は、かたまりを作る前に抜いておく。\n       品名とのすき間が狭いと1つにつながり、注を消した後もかたまりの左はしが注の位置のまま残る\n       （60ページの「（注 11 ）ワイドパネル」が品名より左の欄になっていた）。\n       ただし、注だけの行はマスの2行目（「抗菌加工高性能フィルター」の下の「（注1）（注2）」）なので、\n       マスがどの行まで続くかを見るのに使う */\n    var NOTE = /^[（(]?\\s*注\\s*[\\d,\\s]*[）)]?$/;\n    var notes = [];\n    var rows2 = rows.map(function (r) {\n      return {\n        y: r.y,\n        cells: r.cells.filter(function (c) {\n          if (!NOTE.test(c.s.replace(/\\s/g, ''))) return true;\n          if (c.x < nameRight && r.y < headY - 2 && r.y > endY) notes.push({ x: c.x, y: c.y });\n          return false;\n        })\n      };\n    });\n\n    var segs = carSegs(rows2, nameRight, headY).filter(function (g) {\n      if (g.y <= endY) return false;\n      var raw = g.s.replace(/\\s/g, '');\n      /* 縦書き（区分・注記・ページのつまみ）は、文字数のわりに幅がとても狭い。\n         横書きのかな・漢字は1文字あたり5.5ほど（小さい「ッ」が入ると4くらい）。2文字の縦書き「補助」は3.4だった */\n      var ja = raw.replace(/[^ぁ-んァ-ヶー一-龥]/g, '');\n      if (ja.length >= 2 && ja.length === raw.length && (g.right - g.x) / raw.length < 3.7) return false;\n      if (raw.length >= 2 && (g.right - g.x) / raw.length < 3) return false;\n      // 表の下の注記の文章（「「ロングライフフィルター」は化粧パネルに…」）と、見出しの語\n      if (/[。「」]/.test(g.s)) return false;\n      if (/^(品名|容量・型名|容量|型名)/.test(raw)) return false;\n      g.t = hitClean(g.s);\n      // 長さは注を消した後で見る（「空気清浄ユニット…（注28）（注30）」を文章と思って消していた）\n      return g.t.length >= 2 && g.t.length <= 40 && (isJa(g.t) || isLatin(g.t));\n    });\n    segs.forEach(function (g) { g.y = g.oy; });\n\n    /* 区分の語（「補助」）は、横書きの幅で来ることもある（96ページ）。\n       品番の行に乗っている品名の、いちばん左より左にあるものは区分として落とす */\n    var nameLeft = 1e9;\n    segs.forEach(function (g) {\n      if (!isLatin(g.t) && !CATEGORY_WORD.test(g.t) && codeYOf(g.y, 4) != null) nameLeft = Math.min(nameLeft, g.x);\n    });\n    if (nameLeft < 1e9) segs = segs.filter(function (g) { return g.x >= nameLeft - 5; });\n\n    segs.forEach(function (g) {\n      g.hasRight = segs.some(function (h) { return Math.abs(h.y - g.y) <= 4 && h.x > g.x + 8; });\n    });\n\n    // 欄（x でまとめる）。注は、すでにある欄にだけ入れる\n    var cols = [];\n    segs.forEach(function (g) {\n      var c = null;\n      cols.forEach(function (k) { if (!c && Math.abs(k.x - g.x) < 8) c = k; });\n      if (!c) { c = { x: g.x, segs: [] }; cols.push(c); }\n      c.segs.push(g);\n    });\n    cols.sort(function (a, b) { return a.x - b.x; });\n    notes.forEach(function (n) {\n      if (segs.some(function (g) { return Math.abs(g.y - n.y) <= 1 && g.x < n.x && g.right >= n.x - 8; })) return;\n      var c = null;\n      cols.forEach(function (k) { if (!c && Math.abs(k.x - n.x) < 8) c = k; });\n      if (c) c.segs.push({ y: n.y, note: true });\n    });\n\n    /* 同じ欄で上下にくっついた2行は1つのマス（「化粧」「パネル用」）。\n       品番の行どうし（「比色法65%」「比色法90%」）と、同じ文字のくり返しはつながない。\n       ただし2行目がカタカナだけの切れはし（「抗菌加工高性能」＋「フィルター」）ならつなぐ。\n       注だけの行は、文字は足さずにマスの下はしだけ延ばす */\n    cols.forEach(function (c) {\n      c.segs.sort(function (a, b) { return b.y - a.y; });\n      var cells = [], cur = null;\n      c.segs.forEach(function (g) {\n        if (g.note) {\n          if (cur && cur.lo - g.y <= 11 && cur.tlo - g.y <= 22) cur.lo = Math.min(cur.lo, g.y);\n          return;\n        }\n        var cy = codeYOf(g.y, 2);\n        var head = cur && cur.n === 1 && isLatin(cur.t);\n        var frag = cur && /^[ァ-ヶー]{2,6}$/.test(g.t) && /[一-龥]$/.test(cur.t);\n        var two = cur && cur.toy - g.oy <= 9.3 && cur.lastRight && g.hasRight;\n        var nw = /^\\s*NEW/.test(g.s);\n        if (nw) cur = null;\n        if (cur && !cur.nw && (cur.n < 2 || head) && cur.tlo - g.y <= 10 && (!(cur.code && cy != null) || frag || head || two) && cur.t.indexOf(g.t) < 0) {\n          cur.t += (/用$/.test(cur.t) && /用$/.test(g.t) ? '・' : '') + g.t; cur.tlo = g.y; cur.toy = g.oy; cur.lo = Math.min(cur.lo, g.y); cur.lastRight = g.hasRight;\n          cur.right = Math.max(cur.right, g.right);\n          if (!head) cur.n++;\n          if (cy != null) cur.code = true;\n        } else {\n          cur = { t: g.t, hi: g.y, tlo: g.y, toy: g.oy, lo: g.y, n: 1, code: cy != null, right: g.right, lastRight: g.hasRight, nw: nw };\n          cells.push(cur);\n        }\n      });\n      c.cells = cells.filter(function (cell) { return !isLatin(cell.t); });\n    });\n\n    /* その品番自身の品名のマス（品番の行にあって、右に何も続かない）＝「葉」。\n       品番の行とは7ポイントまでずれてよい（「側面カバー」は4.9、「交換用フィルター（ろ材）」は6.3ずれている）。\n       その品番の行のすぐそば（7以内）に、自分の文字が1つも無い品番の行があるときは、葉にしない。\n       1つのマスに品番が2段で書いてある（「BPD-7WB+BPD-4WB」と「BPD-7WB」が6.3離れている）ところで、\n       まとめ書きの「分ダクト部材」「ブラック」を、その行の品名と取り違えていた（51ページ） */\n    cols.forEach(function (c) {\n      c.cells.forEach(function (cell) {\n        var cy = codeYOf(cell.hi, 7);\n        if (cy == null) cy = codeYOf(cell.tlo, 7);\n        cell.codeY = cy;\n        cell.leaf = cy != null &&\n          !codeYs.some(function (y2) {\n            return y2 !== cy && Math.abs(y2 - cy) <= 7 && !segs.some(function (g) { return Math.abs(g.y - y2) <= 4; });\n          }) &&\n          !segs.some(function (g) {\n            return g.x > c.x + 8 && g.y <= Math.max(cy, cell.hi) + 4 && g.y >= Math.min(cy, cell.tlo) - 4;\n          });\n        cell.mid = (cell.hi + cell.lo) / 2;\n      });\n    });\n\n    function dist(cell, y) {\n      if (y <= cell.hi && y >= cell.lo) return 0;\n      return Math.min(Math.abs(y - cell.hi), Math.abs(y - cell.lo));\n    }\n\n    /* ここから、まとめ書きの割り当て（2026-09-11 作り直し）\n       ----------------------------------------------------------------------\n       前は「近いまとめ書きを受け継ぐ」だった。まとめ書きは何行ぶんものマスのまん中に\n       書いてあるので、上の品物の名前のほうが近いことがある（51ページの\n       「分ダクトフランジ」に、すぐ上の「高湿度対応キット」が付いていた）。\n       いまは欄ごとに、左の欄から順に\n         ・その行に自分の文字がある → それを採る。葉ならそこで区切り\n         ・左で採った文字が、この欄の位置まで横に伸びている → この欄は空（区切り）\n         ・その行の品名がこの欄より左で終わっている → 区切り\n         ・それ以外 → 区切りと区切りの間の行を、間にあるまとめ書きに分ける。\n           まとめ書きはマスのまん中にあるので、「分けた行のまん中」と\n           「まとめ書きの高さ」がいちばん合う分け方を選ぶ（80ページの紙面で確かめた。\n           「吹き出し」は角ダクトフランジの2行目から分ダクト 5m までのまん中にある）\n       まとめ書きが無い区切りの間は、すぐ上の葉を受け継ぐ（上寄せのマス。\n       「オイルガードフィルター」＞「交換用フィルター（ろ材）」、「ブラック」の2段目） */\n    var R = rowYs.slice().sort(function (a, b) { return realOf[b] - realOf[a]; }).map(function (key) {\n      var y = realOf[key];\n      var own = cols.map(function (c) {\n        var best = null, bd = 1e9;\n        c.cells.forEach(function (cell) {\n          var d = cell.codeY === y ? 0 : dist(cell, y);\n          if (d <= 4 && d < bd) { bd = d; best = cell; }\n        });\n        return best;\n      });\n      var last = -1;\n      own.forEach(function (o, ci) { if (o && o.leaf) last = ci; });\n      return { y: y, key: key, own: own, last: last < 0 ? cols.length : last, pick: [], right: -1e9 };\n    });\n\n    function split(run, labels) {\n      // run の行を labels（上から順）に、上から続けて分ける。空のまとめ書きは 8 の罰\n      var n = run.length, k = labels.length, INF = 1e12;\n      var f = [], from = [];\n      for (var i = 0; i <= n; i++) { f.push([]); from.push([]); for (var j = 0; j <= k; j++) { f[i].push(INF); from[i].push(-1); } }\n      f[0][0] = 0;\n      function cost(a, b, L) {           // 行 a..b-1 を L に\n        var sum = 0, bad = 0;\n        for (var t = a; t < b; t++) { sum += run[t].y; if (run[t].lab && run[t].lab !== L) bad++; }\n        return Math.abs(sum / (b - a) - L.mid) + bad * 1000;\n      }\n      for (var j2 = 1; j2 <= k; j2++) {\n        for (var i2 = 0; i2 <= n; i2++) {\n          if (f[i2][j2 - 1] + 8 < f[i2][j2]) { f[i2][j2] = f[i2][j2 - 1] + 8; from[i2][j2] = i2; }\n          for (var p = 0; p < i2; p++) {\n            if (f[p][j2 - 1] >= INF) continue;\n            var v = f[p][j2 - 1] + cost(p, i2, labels[j2 - 1]);\n            if (v < f[i2][j2]) { f[i2][j2] = v; from[i2][j2] = p; }\n          }\n        }\n      }\n      var out = [], i3 = n;\n      for (var j3 = k; j3 >= 1; j3--) {\n        var p3 = from[i3][j3];\n        for (var t = p3; t < i3; t++) out[t] = labels[j3 - 1];\n        i3 = p3;\n      }\n      return out;\n    }\n\n    cols.forEach(function (c, ci) {\n      var st = R.map(function (r) {\n        var o = r.own[ci];\n        if (o) { r.pick[ci] = o; r.right = Math.max(r.right, o.right); return o.leaf ? 'bar' : 'lab'; }\n        if (ci > r.last || r.right >= c.x - 2) return 'bar';\n        return 'free';\n      });\n      var i = 0;\n      while (i < R.length) {\n        if (st[i] === 'bar') { i++; continue; }\n        var a = i;\n        while (i < R.length && st[i] !== 'bar') i++;\n        var yA = a > 0 ? R[a - 1].y : 1e9, yB = i < R.length ? R[i].y : -1e9;\n        var run = [];\n        for (var t = a; t < i; t++) run.push({ y: R[t].y, lab: st[t] === 'lab' ? R[t].own[ci] : null, r: R[t] });\n        var labels = c.cells.filter(function (cell) { return !cell.leaf && cell.mid < yA && cell.mid > yB; })\n          .sort(function (p, q) { return q.mid - p.mid; });\n        if (labels.length) {\n          var got = split(run, labels);\n          run.forEach(function (x, t2) {\n            if (x.lab || !got[t2]) return;\n            x.r.pick[ci] = got[t2]; x.r.right = Math.max(x.r.right, got[t2].right);\n          });\n        } else if (a > 0) {\n          var up = R[a - 1].own[ci];\n          if (up && up.leaf) run.forEach(function (x) {\n            if (up.lo - x.y <= 20) { x.r.pick[ci] = up; x.r.right = Math.max(x.r.right, up.right); }\n          });\n        }\n      }\n    });\n\n    var names = {};\n    R.forEach(function (r) {\n      var parts = [];\n      r.pick.forEach(function (p) { if (p && parts.indexOf(p.t) < 0) parts.push(p.t); });\n      names[r.key] = parts.join(' ').replace(CATEGORY_HEAD, '');\n    });\n    return function (y) { return names[y] || ''; };\n  }\n\n  function optPageHitachi(items, page, out) {\n    var rows = optRows(items);\n\n    var type = '', titleY = 0;\n    rows.forEach(function (r) {\n      if (type) return;\n      var t = r.cells.map(function (o) { return o.s; }).join('').replace(/\\s/g, '');\n      var m = t.match(/オプション一覧[（(]([^）)]{2,20})[）)]/);\n      if (m) { type = m[1]; titleY = r.y; }\n    });\n    if (!type) return;\n\n    /* 列の見出し（容量の並び）は、「■ オプション一覧（…）」の**すぐ下**にある。\n\n       ここを間違えていた。前は\n       ・ページの中でいちばん最初の「容量・型名」の行を見出しにしていた\n       ・「容量・型名（相当馬力）」だけが1行に置かれ、容量は次の行にあるページを読めなかった\n       この2つで、10タイプ中6タイプがまるごと落ちていた\n       （2026-09-09、BIGBOSSの「天吊型にもでないぞ」で分かった。\n         てんかせ1方向のページは、先に「化粧パネル」の表が載っている） */\n    /* 見出しの容量は**3行に割れていることがある**（てんうめのページ）。\n\n         22型（0.8）〜        45型（1.8）〜\n       品名                63型（2.5）〜90型（3.3）  112型（4.0）〜…\n         40型（1.5）          56型（2.3）\n\n       なので「見出しの行を1つ選ぶ」のではなく、\n       見出しの帯にある容量の字をぜんぶ集めて、x（横の位置）でまとめる。\n       「40型（1.5）」の形に書いてあるものだけを容量と見なす。数字だけで見ると\n       「20,000円」の20や「F-160LB1」の160まで容量に見えてしまう。 */\n    var hcells = [];\n    rows.forEach(function (r) {\n      if (r.y >= titleY || r.y < titleY - 40) return;\n      r.cells.forEach(function (c) {\n        if (/\\d{2,3}\\s*[型形]/.test(c.s) && capRange(c.s)) hcells.push(c);\n      });\n    });\n    if (!hcells.length) return;\n    var headY = 1e9;\n    hcells.forEach(function (c) { if (c.y < headY) headY = c.y; });\n    var head = { y: headY };\n\n    // 表の終わりは、見出しより下にある次の「■」（次の節の始まり）\n    var endY = -1e9;\n    rows.forEach(function (r) {\n      if (r.y >= head.y) return;\n      var t = r.cells.map(function (o) { return o.s; }).join('').trim();\n      if (t.charAt(0) === '■' && r.y > endY) endY = r.y;\n    });\n\n    // 集めた容量の字を x でまとめて、列（容量の範囲）にする\n    hcells.sort(function (a, b) { return a.x - b.x || b.y - a.y; });\n    var cols = [], cur = null;\n    hcells.forEach(function (c) {\n      if (!cur || c.x - cur.x > 30) { cur = { x: c.x, right: c.x, s: c.s }; cols.push(cur); }\n      else { cur.s += c.s; cur.right = Math.max(cur.right, c.x); }\n    });\n    cols = cols.map(function (c) { return { x: c.x, cap: capRange(c.s) }; })\n      .filter(function (c) { return c.cap; });\n    if (!cols.length) return;\n\n    // 品番のある行（品名の読み方が、行どうしの並びを見るため）\n    var codeYs = [];\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 2 || r.y <= endY) return;\n      if (r.cells.some(function (c) { return OPT_CODE.test(c.s.trim()); })) codeYs.push(r.y);\n    });\n    var nameAt = hitachiNamer(rows, cols[0].x - 20, head.y, endY, codeYs);\n\n    // 表ぜんぶにかかる容量（1行に品番が1つだけのときに使う）\n    var wide = [1e9, 0];\n    cols.forEach(function (k) {\n      if (k.cap[0] < wide[0]) wide[0] = k.cap[0];\n      if (k.cap[1] > wide[1]) wide[1] = k.cap[1];\n    });\n\n    rows.forEach(function (r) {\n      if (r.y >= head.y - 2 || r.y <= endY) return;\n      var name = nameAt(r.y);\n\n      /* リモコンやドレンアップメカのように、**容量に関係なく1つだけ**の品番は、\n         マスを何列もまたいで真ん中あたりに書かれている。\n         前はいちばん近い列に押し込んでいたので、\n         「かべかけ4馬力に多機能デザインリモコンが出ない」ことになっていた\n         （2026-09-09、BIGBOSSの指摘で分かった）。1行に品番が1つなら表ぜんぶに付くとみなす */\n      var nCode = 0;\n      r.cells.forEach(function (c) { if (OPT_CODE.test(c.s.trim())) nCode++; });\n\n      r.cells.forEach(function (o, i) {\n        var s = o.s.trim();\n        if (!OPT_CODE.test(s)) return;\n        var price = optPriceRightOrBelow(rows, r, i, o);\n        if (!price) return;\n        // どの列かは**金額の位置**で決める。品番は長さがまちまちで、\n        // 長い品番ほどマスの中で左に伸び、1つ左の列に取られてしまう\n        // （2026-09-09、F-112LPK2-AGV が50〜63型の列に入っていた）\n        var atX = price.x;\n        var cap = wide;\n        if (nCode > 1) {\n          var col = null, bd = 1e9;\n          cols.forEach(function (k) { var d = Math.abs(k.x - atX); if (d < bd) { bd = d; col = k; } });\n          if (!col || bd > 110) return;\n          cap = col.cap;\n        }\n        out.push({ page: page, name: name, code: s, y: price.y, fits: [{ type: type, cap: cap }] });\n      });\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     三菱電機の別売品\n     ほかの4社と違い、**別売品だけの表が無い**。\n     セット価格のページに、その機種の構成品として書かれている。\n\n       1方向天井カセット形                    ← ページの機種タイプ\n       P40形（1.5馬力） ｜ P45形 ｜ P50形 ｜ P56形   ← 4段組み\n       セット価格 1,064,000円\n       室内：PM-RP40FA22 352,000円\n       室外：PUZ-ERMP40SKA16 599,000円\n       ワイヤードリモコン：PAR-48MA 60,000円\n       ムーブアイセンサーパネル：PMP-P80FWF11 53,000円   ← これが別売品\n\n     「◯◯：品番 金額円」の形なので、ラベルがそのまま品名になる。\n     室内機・室外機は本体なので別売品には入れない。\n     -------------------------------------------------------------------- */\n  function optPageMitsu(items, page, out) {\n    var rows = optRows(items);\n\n    // ページの機種タイプ（いちばん上の日本語）\n    var type = '';\n    var top = rows.slice(0, 3);\n    top.forEach(function (r) {\n      r.cells.forEach(function (c) {\n        if (!type && isJa(c.s) && /形$/.test(c.s.trim()) && c.s.length >= 4) type = c.s.trim();\n      });\n    });\n    if (!type) return;\n\n    // 「セット価格」の x で段を割る\n    var xs = [];\n    items.forEach(function (i) { if (/セット/.test(i.s)) xs.push(i.x); });\n    if (xs.length < 2) return;\n    xs.sort(function (a, b) { return a - b; });\n    var st = [];\n    xs.forEach(function (x) { if (!st.length || x - st[st.length - 1] > 60) st.push(x); });\n\n    st.forEach(function (s, i) {\n      var lo = s - 62;\n      var hi = (i === st.length - 1) ? 1e9 : st[i + 1] - 62;\n\n      // その段の容量（P40形 など）\n      var cap = 0;\n      rows.forEach(function (r) {\n        if (cap) return;\n        var t = r.cells.filter(function (c) { return c.x >= lo && c.x < hi; })\n          .map(function (c) { return c.s; }).join('');\n        var m = t.match(/P(\\d{2,3})\\s*形/);\n        if (m) cap = Number(m[1]);\n      });\n      if (!cap) return;\n\n      rows.forEach(function (r) {\n        // **文字をつなげない。** つなげると「PLP-P160HWF」＋「74,000」が\n        // 「PLP-P160HWF7」＋「4,000」に化ける（ダイキンで踏んだのと同じ落とし穴）\n        var cells = r.cells.filter(function (c) { return c.x >= lo && c.x < hi; });\n        // 別売品の行は必ず「ラベル：品番 金額円」の形。\n        // 「：」が無い行を通すと、室外機の品番の切れ端（KA16 など）を拾ってしまう\n        var hasColon = false;\n        cells.forEach(function (c) { if (/[：:]/.test(c.s)) hasColon = true; });\n        if (!hasColon) return;\n\n        // 三菱の別売品の品番は必ず「英字3〜4文字＋ハイフン」で始まる（25種すべてで確認）。\n        // ハイフンを求めないと、室外機の品番の切れ端（KA16 など）を拾う\n        var MITSU_CODE = /^[A-Z]{2,4}-[A-Z0-9]{3,}$/;\n        var parts = [], code = '', price = 0;\n        cells.forEach(function (c) {\n          var s = c.s.trim();\n          if (!code) {\n            if (MITSU_CODE.test(s)) { code = s; return; }\n            if (isJa(s)) parts.push(s);\n            return;\n          }\n          if (!price) {\n            var m = s.match(OPT_MONEY);\n            if (m) price = yen(m[1]);\n          }\n        });\n        /* 品名になる文字だけ残す。\n           ・1文字だけのかけら … 段の左端に縦に並んだページの見出し。\n             これを入れていたので「ワイヤードリモコン」が「ネワイヤードリモコン」になっていた\n           ・数字の入ったもの … となりの段の金額。\n             「21,000円ワイヤレスリモコン」になっていた\n           （2026-09-09、BIGBOSSが画面を見て気づかせてくれた） */\n        var label = parts.filter(function (t) {\n          return t.length >= 2 && !/[0-9０-９]/.test(t);\n        }).join('');\n        label = label.replace(/[：:].*$/, '').replace(/\\s/g, '');\n        if (!code || !price || label.length < 2) return;\n        if (/室内|室外|セット価格|合計/.test(label)) return;   // 本体は別売品ではない\n        out.push({\n          page: page, name: label, code: code, y: price,\n          fits: [{ type: type, cap: [cap, cap] }]\n        });\n      });\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     三菱電機の共通別売部品（分配管など・カタログ144ページ）\n\n       共通別売部品オプション\n       分配管（マルチディストリビュータ）\n       ■同時ツイン用（適応機種：P80〜P280形）\n       形名   SDD-50SR9    SDD-50WR9\n       価格   22,000円     27,000円\n       ＊SDD-50SR9（P80〜P160形用）・SDD-50WR9（P224・P280形用）\n\n     **縦並びの表**（形名の行と価格の行が別）なので、x で対応づける。\n     どの台数用か（同時ツイン／トリプル／フォー）は「■◯◯用」の見出しから、\n     容量は注釈の「（P80〜P160形用）」から取る。\n     分配管は同時マルチのときに必ず要る（BIGBOSS 2026-09-05 確認）。\n     -------------------------------------------------------------------- */\n  /* --------------------------------------------------------------------\n     三菱の「オプション構成図」のページ（p.132〜142）\n\n       室内ユニット　オプション\n       4方向天井カセット形〈i-スクエアタイプ〉\n       部品名                          形　名          価格\n       ムーブアイセンサーパネル          PLP-P160HWF     74,000円\n\n     **これが三菱でいちばん品数の多い別売品の表**。\n     前はセット価格ページの構成品しか読んでいなかったので、\n     いちばん台数の多い「4方向天井カセット形」のパネルが1つも入っていなかった\n     （2026-09-09、361機種／1,097機種）。\n     -------------------------------------------------------------------- */\n  function optPageMitsuList(items, page, out) {\n    var rows = optRows(items);\n    if (rows.length < 6) return;\n\n    // 上のほうに「オプション」と「部品名」が両方あるページだけ\n    var top = rows.slice(0, 8).map(function (r) { return r.cells.map(function (c) { return c.s; }).join(''); }).join('');\n    if (top.indexOf('オプション') < 0) return;\n    var all = rows.map(function (r) { return r.cells.map(function (c) { return c.s; }).join(''); }).join('');\n    if (all.indexOf('部品名') < 0) return;\n\n    /* 室内機のタイプ。「4方向天井カセット形〈i-スクエアタイプ〉」のように\n       〈〉の中がタイプ名のこともあれば、〈PL-RP・LA22〉のように品番のこともある。\n       品番のほうは捨てる（機種データのタイプ名は品番を持っていない） */\n    /* 1ページに表が2つ3つ載っていることがある。\n       142ページは「壁掛形」「床置形」「厨房用天吊形」の3つが縦に並ぶ。\n       前は最初に見つけた1つ（壁掛形）をページ全体に付けていたので、\n       床置形の14機種には別売品が1つも出なかった（2026-09-09） */\n    var heads = [];\n    rows.forEach(function (r) {\n      r.cells.forEach(function (c) {\n        var t = c.s.trim();\n        if (!/形[〈（(]|形$/.test(t)) return;\n        if (!/カセット|天吊|壁掛|床置|ビルトイン|埋込|厨房/.test(t)) return;\n        if (!/タイプ/.test(t)) t = t.replace(/[〈（(].*$/, '');\n        heads.push({ y: r.y, type: t });\n      });\n    });\n    if (!heads.length) return;\n    heads.sort(function (a, b) { return b.y - a.y; });   // 紙面の上から順\n    var headY = heads[0].y;\n\n    /** その行がどの見出しの下にあるか（y は下から上なので、行より大きいいちばん近い見出し） */\n    function typeAt(y) {\n      var best = '', bd = 1e9;\n      heads.forEach(function (h) {\n        var d = h.y - y;\n        if (d <= 0 || d >= bd) return;\n        bd = d; best = h.type;\n      });\n      return best;\n    }\n\n    // 品名はいちばん左の列。形名と価格はその右\n    var nameEnd = 250;\n    rows.forEach(function (r) {\n      if (r.y >= headY) return;\n      var name = [], code = '', price = 0, at = -1;\n      r.cells.forEach(function (c, i) {\n        var t = c.s.trim();\n        if (c.x < nameEnd && !/^[0-9]/.test(t)) { name.push(t); return; }\n        if (!code) {\n          var m = t.match(/^([A-Z][A-Z0-9]*-[A-Z0-9\\-]{2,})/);\n          if (m) { code = m[1]; at = i; return; }\n        }\n        if (code && !price && i > at) {\n          var mm = t.match(OPT_MONEY);\n          if (mm) price = yen(mm[1]);\n        }\n      });\n      if (!code || !price) return;\n      /* 1文字だけのかけら（縦書きの見出しの1文字）と、\n         品名の列に入り込んだ品番は品名ではない */\n      var nm = name.filter(function (t) {\n        return t.length >= 2 && !/^[A-Z][A-Z0-9]*-[A-Z0-9\\-]{2,}$/.test(t);\n      }).join(' ')\n        .replace(/[①-⓿]/g, ' ')          // 丸数字（①②③…）は品名ではない\n        .replace(/[※注][\\d,\\s]*/g, ' ')\n        .replace(/\\s+/g, ' ').trim();\n      if (nm.length < 2) return;\n      var type = typeAt(r.y);\n      if (!type) return;\n      out.push({ page: page, name: nm, code: code, y: price, fits: [{ type: type }] });\n    });\n  }\n\n  function optPageMitsuCommon(items, page, out) {\n    var rows = optRows(items);\n    var flat = items.map(function (o) { return o.s; }).join('').replace(/\\s/g, '');\n    if (flat.indexOf('共通別売部品') < 0) return;\n\n    // 「＊SDD-50SR9（P80〜P160形用）」のような注釈から、品番ごとの容量を拾う\n    var capOf = {};\n    var re = /([A-Z]{2,4}-[A-Z0-9]{3,})\\s*[（(]\\s*P?(\\d{2,3})\\s*[形型]?\\s*[～~〜・]\\s*P?(\\d{2,3})?\\s*[形型]/g, m;\n    var joined = items.map(function (o) { return o.s; }).join('');\n    while ((m = re.exec(joined)) !== null) {\n      capOf[m[1]] = [Number(m[2]), Number(m[3] || m[2])];\n    }\n\n    var tp = '';\n    rows.forEach(function (r) {\n      var t = r.cells.map(function (c) { return c.s; }).join('').replace(/\\s/g, '');\n\n      // 「■同時ツイン用（適応機種：P80〜P280形）」で、ここから下の表の台数が決まる\n      var h = t.match(/[■●]?(同時[ツトリプルフォーン]{2,6})用/);\n      if (h) tp = h[1];\n\n      // 「形名」で始まる行の品番を拾い、次の「価格」の行と x で突き合わせる\n      if (!/^形\\s*名/.test(t.replace(/\\s/g, '形名').slice(0, 4)) && t.indexOf('形名') !== 0) return;\n      var codes = r.cells.filter(function (c) { return /^[A-Z]{2,4}-[A-Z0-9]{3,}$/.test(c.s.trim()); });\n      if (!codes.length) return;\n\n      // すぐ下の「価格」の行\n      var idx = rows.indexOf(r), price = null;\n      for (var k = idx + 1; k < Math.min(rows.length, idx + 4); k++) {\n        var tt = rows[k].cells.map(function (c) { return c.s; }).join('');\n        if (tt.indexOf('価') >= 0 && /[\\d,]{5,}/.test(tt)) { price = rows[k]; break; }\n      }\n      if (!price) return;\n\n      codes.forEach(function (c) {\n        var best = null, bd = 1e9;\n        price.cells.forEach(function (pc) {\n          if (!OPT_MONEY.test(pc.s.trim())) return;\n          var d = Math.abs(pc.x - c.x);\n          if (d < bd) { bd = d; best = pc; }\n        });\n        // 同じ列（真下）の金額だけを採る。ゆるくすると隣の表の金額を拾う\n        if (!best || bd > 40) return;\n        var code = c.s.trim();\n        var fit = { tp: tp || '同時ツイン' };\n        if (capOf[code]) fit.cap = capOf[code];\n        out.push({ page: page, name: '分配管（マルチディストリビュータ）', code: code, y: yen(best.s), fits: [fit] });\n      });\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     ダイキンの「縦並び」の表（吹出ユニットなど・204〜206ページ）\n\n       品名        ｜ 2.5〜4.0 ｜ 4.0〜6.0 ｜ …      ← 列＝推奨風量\n       フレッシュホワイト ｜ K-DGS4EFF ｜ K-DGS5EFF ｜ …\n       ホワイト        ｜ K-DGS4EWW ｜ K-DGS5EWW ｜ …   ← 色ちがいで品番が5行\n       接続ダクト径     ｜ φ150 ｜ φ150 ｜ …\n       価格          ｜ 31,700円 ｜ 35,700円 ｜ …      ← 価格は1行だけ\n\n     **同じ列の品番は全部同じ価格**。行の中に金額が無いので、\n     ふつうの読み方では1件も取れない（292件を取りこぼしていた）。\n     「価格」の行を見つけて、その上の品番の行と x で突き合わせる。\n\n     2026-09-05、私は「このページには価格が載っていない」と判断した。\n     LMに聞いたら「載っている」と返ってきて、間違いに気づいた。\n     -------------------------------------------------------------------- */\n  function optPageDaikinVert(items, page, out) {\n    var rows = optRows(items);\n\n    // 品名が品番と同じ行に無いことがある（表のマスが縦につながっているため）。\n    // 品番の左端より左を品名の場所として、共通の道具で組み立てる\n    var minCodeX = 1e9;\n    rows.forEach(function (r) {\n      r.cells.forEach(function (c) {\n        if (OPT_CODE.test(c.s.trim()) && c.x < minCodeX) minCodeX = c.x;\n      });\n    });\n    var nameAt = (minCodeX < 1e9) ? nameReader(rows, minCodeX - 20, 1e9, 'vert') : null;\n\n    rows.forEach(function (r, idx) {\n      var t = r.cells.map(function (c) { return c.s; }).join('').replace(/\\s/g, '');\n      if (t.indexOf('価格') !== 0) return;\n      var prices = r.cells.filter(function (c) { return OPT_MONEY.test(c.s.trim()); });\n      if (prices.length < 2) return;\n\n      // その上にある品番の行をさかのぼって集める（色ちがいで何行もある）\n      for (var k = idx - 1; k >= 0 && k >= idx - 8; k--) {\n        var codes = rows[k].cells.filter(function (c) { return OPT_CODE.test(c.s.trim()); });\n        if (!codes.length) continue;\n        var left = codes[0].x - 20;\n        var name = rows[k].cells.filter(function (c) { return c.x < left && isJa(c.s); })\n          .map(function (c) { return c.s; }).join('').replace(/注\\d+/g, '').trim();\n        /* 「ホワイト」「ブラウン」だけの行は、色であって品名ではない。\n           品名は上の行（縦につながったマス）にあるので、上下から拾い直す */\n        var colorOnly = /^[（(]?(フレッシュ)?(ホワイト|ブラック|ブラウン|ベージュ|グレー|シルバー|アイボリー|ホワイ|ブラ)[）)]?(（単色）)?$/;\n        if ((name.length < 2 || colorOnly.test(name.replace(/\\s/g, ''))) && nameAt) {\n          var n2 = nameAt(rows[k].y);\n          if (n2 && n2.length > name.length) name = n2;\n        }\n\n        codes.forEach(function (c) {\n          var best = null, bd = 1e9;\n          prices.forEach(function (p) { var d = Math.abs(p.x - c.x); if (d < bd) { bd = d; best = p; } });\n          if (!best || bd > 60) return;\n          out.push({\n            page: page, name: name, code: c.s.trim(), y: yen(best.s),\n            fits: [{ all: true }]     // 吹出ユニットは風量で選ぶ共通部材（機種を選ばない）\n          });\n        });\n      }\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     三菱電機の室外ユニットオプション（143ページ前後）\n\n       室外ユニット オプション\n       形名 ｜ スリムZR ｜ ズバ暖スリム            ← 大きい区分（シリーズ）\n            ｜ P28〜P63形 ｜ P80形 ｜ P112〜P160形 …  ← その下が容量\n       部品名\n       エアガイド ｜ PAC-SJ06AG 24,000円 ｜ PAC-SJ03AG 24,000円 ｜ …\n\n     室外機に付くものなので、室内機のタイプでは決まらない。\n     **シリーズと室外機の容量**で決まる（NotebookLM でも確認。スリムERにも付く）。\n     -------------------------------------------------------------------- */\n  function optPageMitsuOutdoor(items, page, out) {\n    var flat = items.map(function (o) { return o.s; }).join('').replace(/\\s/g, '');\n    if (flat.indexOf('室外ユニット') < 0 || flat.indexOf('オプション') < 0) return;\n    if (flat.indexOf('共通別売部品') >= 0) return;   // 分配管のページは別で読む\n\n    var rows = optRows(items);\n    var head = null;\n    rows.forEach(function (r) {\n      if (head) return;\n      r.cells.forEach(function (c) { if (!head && c.s.indexOf('部品名') >= 0) head = r; });\n    });\n    if (!head) return;\n\n    // 見出しの上（＝y が大きい）から、容量の列を拾う。\n    // 「P224」「・」「P280形」のようにマスが割れるので、隣どうしはつなげる\n    var caps = [];\n    rows.forEach(function (r) {\n      if (r.y <= head.y) return;\n      var cur = null;\n      r.cells.forEach(function (c) {\n        var s = c.s.replace(/\\s/g, '');\n        if (/^[P\\d～~〜・、,形型]+$/.test(s) && /\\d/.test(s)) {\n          if (cur && c.x - cur.right < 30) { cur.s += s; cur.right = c.x + (c.w || 10); }\n          else { cur = { x: c.x, right: c.x + (c.w || 10), s: s }; caps.push(cur); }\n        } else cur = null;\n      });\n    });\n    var cols = caps.map(function (c) { return { x: c.x, cap: capRange(c.s) }; })\n      .filter(function (c) { return c.cap; })\n      .sort(function (a, b) { return a.x - b.x; });\n    if (!cols.length) return;\n\n    // シリーズの区分（大きい見出し）\n    var sers = [];\n    rows.forEach(function (r) {\n      if (r.y <= head.y) return;\n      r.cells.forEach(function (c) {\n        var s = c.s.replace(/\\s/g, '');\n        if (/スリムZR|ズバ暖スリム|スリムER/.test(s)) sers.push({ x: c.x, s: s });\n      });\n    });\n    sers.sort(function (a, b) { return a.x - b.x; });\n\n    var nameAt = nameReader(rows, cols[0].x - 20, head.y + 1);\n\n    rows.forEach(function (r) {\n      if (r.y >= head.y) return;\n      var name = nameAt(r.y);\n\n      r.cells.forEach(function (o, i) {\n        var s = o.s.trim();\n        if (!/^[A-Z]{2,4}-[A-Z0-9]{3,}$/.test(s)) return;\n        var price = 0;\n        for (var j = i + 1; j < r.cells.length; j++) {\n          var t = r.cells[j].s.trim();\n          if (/^[A-Z]{2,4}-[A-Z0-9]{3,}$/.test(t)) break;\n          var mm = t.match(OPT_MONEY);\n          if (mm) { price = yen(mm[1]); break; }\n        }\n        if (!price) return;\n\n        var col = null, bd = 1e9;\n        cols.forEach(function (k) { var d = Math.abs(k.x - o.x); if (d < bd) { bd = d; col = k; } });\n        if (!col || bd > 70) return;\n\n        // その品番より左にある、いちばん近いシリーズ見出し\n        var ser = '';\n        sers.forEach(function (k) { if (k.x <= o.x + 40) ser = k.s; });\n\n        var fit = { cap: col.cap };\n        if (ser) fit.series = ser;\n        out.push({ page: page, name: name, code: s, y: price, fits: [fit] });\n      });\n    });\n  }\n\n  /** 同じ品番をまとめ、付く機種を足し合わせる */\n  function optFinish(list) {\n    var map = {}, order = [];\n    list.forEach(function (o) {\n      if (!map[o.code]) { map[o.code] = { code: o.code, name: o.name, y: o.y, fits: [] }; order.push(o.code); }\n      var v = map[o.code];\n      if (!v.name && o.name) v.name = o.name;\n      o.fits.forEach(function (f) {\n        var key = JSON.stringify(f), dup = false;\n        v.fits.forEach(function (g) { if (JSON.stringify(g) === key) dup = true; });\n        if (!dup) v.fits.push(f);\n      });\n    });\n    return order.map(function (c) { return map[c]; });\n  }\n\n  /** 別売品の読み取り結果を、空調王に渡す形にそろえる */\n  function optResult(list, maker, brand) {\n    return {\n      rows: optFinish(list),\n      pricePages: 0,\n      head: {\n        maker: maker,\n        brand: brand,\n        note: '希望小売価格・税抜。社内利用限定（第三者提供不可）。'\n      }\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     データファイル（.json）を読む\n     -------------------------------------------------------------------- */\n  function readJsonFile(file) {\n    return file.text().then(function (t) {\n      var data = null;\n      try { data = JSON.parse(t); } catch (e) {\n        throw new Error('データファイルとして読めませんでした。\\n' +\n                        'ページを「名前を付けて保存」で保存し直してから、もう一度選んでください。');\n      }\n      var list = Array.isArray(data) ? data : null;\n      if (!list && data && typeof data === 'object') {\n        // 入れ物の中に一覧が入っている形も受ける\n        Object.keys(data).forEach(function (k) {\n          if (!list && Array.isArray(data[k]) && data[k].length > 50) list = data[k];\n        });\n      }\n      if (!list || !list.length) {\n        throw new Error('中身が空でした。ファイルが途中までしか保存されていないかもしれません。');\n      }\n      return list;\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     メーカーの一覧\n     min＝これを下回ったら「読めていない」とみなす件数。\n     2026-09-04 の実績（キヤリア870件・日立626件・パナ946件）の8割を目安にしてある。\n     -------------------------------------------------------------------- */\n  // パナソニックの室内機タイプ（機種データの呼び方にそろえてある）\n  var PANA_TYPES = [\n    '天吊形厨房用エアコン（高温吸込み対応）', '高温吸込み天吊形厨房用エアコン', '天吊形厨房用エアコン',\n    '高天井用1方向カセット形', 'ビルトインオールダクト形', '天井ビルトインカセット形',\n    '4方向天井カセット形', '2方向天井カセット形', '1方向天井カセット形',\n    '天井吊形', '壁掛形', '床置形', '天井埋込形'\n  ];\n\n  /* ======================================================================\n     材料メーカー（部材）\n     ----------------------------------------------------------------------\n     機器の5社とは入れ物が違う。読み取った中身は機種データでも別売品でもなく、\n     **単価マスタの行**になる。だから run() は kind:'parts' のときだけ\n     { parts: … } を返し、app.js 側でCSVと同じ道に流し込む。\n     ====================================================================== */\n\n  /* --------------------------------------------------------------------\n     因幡電工\n     ----------------------------------------------------------------------\n     読むのは総合カタログではなく「価格改定表」。\n     総合カタログは1,000ページ近くあって値段が紙面の絵の中に散っているが、\n     価格改定表は品番と値段だけが並んだ表で、値上げのたびに必ず出る。\n     しかも2種類とも公式サイトから直にPDFで落とせる。\n\n     紙面は2段組。左の帯と右の帯を、別々に上から読む。\n\n       ● スリムダクトＬＤ           ← シリーズ（分類になる）\n       ・ウォールコーナー             ← 品名\n       コード 型番 新標準単価 掲載      ← 見出し\n       2302  LDW-70  ¥920  P 6      ← ここが1行\n\n     **同じ行でも y が 0.1 ずれることがある。**行に分けたあと、\n     必ず x の順に並べ直す。これをしないと、ずれた1語だけが行の末尾に回り、\n     型番が空のまま「値段だけの行」ができる（2026-09-10、20件で分かった）。\n     -------------------------------------------------------------------- */\n\n  var INABA_HEAD = {\n    'コード': 1, '型番': 1, '新標準単価': 1, '掲載': 1, '新標準価格': 1, '品番': 1\n  };\n\n  /** 位置つきの文字を「行」にまとめる。pdf.js の y は下から上なので、上の行＝y が大きい */\n  function inabaLines(items, chain) {\n    var live = items.filter(function (i) { return String(i.s).trim() !== ''; });\n    live.sort(function (a, b) { return b.y - a.y; });\n    var lines = [], cur = [], y = null;\n    live.forEach(function (i) {\n      if (y === null || Math.abs(i.y - y) < 2.5) {\n        cur.push(i);\n        // chain のときは、直前の文字と比べて次へつなぐ。\n        // 1行の中で y が少しずつ下がっていく紙面（オーケー器材）は、\n        // 行の頭とだけ比べていると、行の後ろのほうが2.5を超えて別の行にされる。\n        // 品番と値段が別々の行になって丸ごと落ちる（2026-09-10、17件で分かった）\n        if (y === null || chain) y = i.y;\n      } else {\n        lines.push(cur); cur = [i]; y = i.y;\n      }\n    });\n    if (cur.length) lines.push(cur);\n    lines.forEach(function (ln) { ln.sort(function (a, b) { return a.x - b.x; }); });\n    return lines;\n  }\n\n  /** 「¥」と数字が別々に来ることがあるので、くっつけてから見る */\n  function inabaTokens(line) {\n    var raw = line.map(function (i) { return String(i.s).trim(); })\n                  .filter(function (t) { return t !== ''; });\n    var out = [];\n    for (var i = 0; i < raw.length; i++) {\n      if ((raw[i] === '¥' || raw[i] === '￥') && i + 1 < raw.length && /^[\\d,]+$/.test(raw[i + 1])) {\n        out.push('¥' + raw[i + 1]); i++;\n      } else out.push(raw[i]);\n    }\n    return out;\n  }\n\n  /* 段組みの数はページによって違う（最後のほうは1段）。\n     そこで「段に切ってから読む」のをやめた。\n     **1行の中に ¥ が出てくるたびに1件**と数える。これなら1段でも2段でも同じ手で読める。\n     左右の端から真ん中を割る手では、1段のページで1つの段を2つに割ってしまい、\n     品番と値段が別々になって丸ごと落ちた（2026-09-10、5件）。 */\n  function inabaRecords(toks) {\n    var isCode = function (t) { return /^\\d{3,7}$/.test(t); };\n    var recs = [], start = 0;\n    for (var i = 0; i < toks.length; i++) {\n      var hit = toks[i].match(/^[¥￥]([\\d,]+)$/);\n      if (!hit) continue;\n      // 値段のついていない件（「オープン」価格など）が手前にあると、\n      // その語がそのまま次の件の型番にくっつく。**いちばん近いコードから拾う**\n      var from = start;\n      for (var k = i - 1; k >= start; k--) { if (isCode(toks[k])) { from = k; break; } }\n      var seg = toks.slice(from, i);\n      // 値段のうしろは掲載ページ（「P 6」「オープン P32」など）。\n      // **次の件の頭＝コードに当たるまで読み飛ばす。**\n      // 「P とその次の1語」だけ飛ばす作りだと、「オープン」のような\n      // 余分な語が次の件の型番にくっつく（2026-09-10）\n      var j = i + 1, tail = [];\n      while (j < toks.length && !isCode(toks[j])) { tail.push(toks[j]); j++; }\n      var pm = tail.join('').match(/[PＰ]\\s*([\\dA-Za-z\\-]+)/);\n      recs.push({ seg: seg, price: Number(hit[1].replace(/,/g, '')), ref: pm ? 'P' + pm[1] : '' });\n      i = j - 1;\n      start = j;\n    }\n    return recs;\n  }\n\n  /** 品名の見出しは段の左端に立っている。件も同じ段の左端から始まる。\n      だから x がいちばん近い見出しが、その件の品名。 */\n  function inabaNearest(list, x, y) {\n    var best = null, bestDx = 1e9;\n    list.forEach(function (h) {\n      if (h.y < y - 0.5) return;              // pdf.js の y は下から上。見出しは件より上（y が大きい）\n      var dx = Math.abs(h.x - x);\n      if (dx > 120) return;                   // となりの段の見出しは拾わない\n      if (dx < bestDx || (dx === bestDx && h.y < best.y)) { best = h; bestDx = dx; }\n    });\n    return best ? best.name : '';\n  }\n\n  function inabaReadPage(items, pageNo, ctx) {\n    if (!items.length) return [];\n    ctx = ctx || {};\n    var out = [], names = [];\n\n    inabaLines(items).forEach(function (ln) {\n      var toks = inabaTokens(ln);\n      if (!toks.length) return;\n\n      // 見出しは行の途中にも混じる（左の段が見出し、右の段が明細、という行がある）。\n      // ● は紙面の大見出しなので、段にも行にも縛られず、そのまま次の件に効かせる\n      ln.forEach(function (it, k) {\n        var t = String(it.s).trim();\n        if (t.charAt(0) === '●') {\n          // 「●」だけで1つの文字として来ることがある。そのときは行の残りが見出し\n          var nm = t.slice(1).trim();\n          if (!nm) {\n            // 行の残りをつなぐ。ただし**となりの段に入る手前で止める**。\n            // 止めないと「スリムダクトＬＤ・ひねり90°エルボ」のように\n            // 右の段の品名まで見出しに入ってしまう\n            for (var q = k + 1; q < ln.length; q++) {\n              var v = String(ln[q].s).trim();\n              if (!v || INABA_HEAD[v]) continue;\n              if (v.charAt(0) === '・' || v.charAt(0) === '●' || /^\\d{3,7}$/.test(v)) break;\n              nm += v;\n            }\n          }\n          if (nm) ctx.inabaSeries = nm;\n        } else if (t.charAt(0) === '・') {\n          names.push({ x: it.x, y: it.y, name: t.slice(1).trim() });\n        }\n      });\n\n      // 見出しと表の見出し語は、明細の並びから外す。\n      // 外さないと「・スリムダクトＬＤ2314LDN-70」のように型番にくっつく\n      var body = toks.filter(function (t) {\n        return t.charAt(0) !== '●' && t.charAt(0) !== '・' && t.charAt(0) !== '（' && !INABA_HEAD[t];\n      });\n\n      // 件が紙面のどのあたりから始まるかは、品番の文字を行の中から探して x を取る\n      inabaRecords(body).forEach(function (rec) {\n        if (!rec.price || !rec.seg.length) return;\n        var code = /^\\d{3,7}$/.test(rec.seg[0]) ? rec.seg[0] : '';\n        var model = rec.seg.slice(code ? 1 : 0).join('').replace(/\\s+/g, '');\n        if (!model) return;\n        var x = 0;\n        for (var k = 0; k < ln.length; k++) {\n          if (String(ln[k].s).trim() === rec.seg[0]) { x = ln[k].x; break; }\n        }\n        var nm = inabaNearest(names, x, ln[0].y);\n        var sr = ctx.inabaSeries || '';\n        out.push({\n          m: model, y: rec.price, code: code,\n          name: nm || sr || model, series: sr, ref: rec.ref, page: pageNo\n        });\n      });\n    });\n    return out;\n  }\n\n  function inabaFinish(sets) {\n    var rows = [], seen = {};\n    sets.forEach(function (r) {\n      // 総合とエアコン配管部材の2冊に同じ品番が出る。先に読んだほうを残す\n      if (seen[r.m]) return;\n      seen[r.m] = 1;\n      rows.push(r);\n    });\n    return {\n      head: {\n        maker: '因幡電工',\n        brand: '価格改定表',\n        note: '新標準単価・税抜。工事費は含まず。カタログの価格改定表から読み取ったもの。'\n      },\n      rows: rows,\n      pricePages: sets.length ? 1 : 0\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     ユーシー産業（エバック）\n     ----------------------------------------------------------------------\n     総合カタログ76ページの、うしろのほうにある「単価表」だけを読む。\n     1段の素直な表で、列がきれいにそろっている。\n\n       品　名          品　番     ホース呼び径 ホース長 梱　包  単　価\n       エバフリーAFP型  AFP-20     φ20         400mm   30本   ¥1,120\n\n     **列の x は、紙面の見出し（品　番／単　価）から覚える。**\n     見出しの無いページは単価表ではないので、まるごと読み飛ばす。\n     こうしておくと、カタログの前半（写真と説明のページ）を\n     値段だと読み違えることがない。\n     -------------------------------------------------------------------- */\n\n  function ucFlat(t) {\n    return String(t == null ? '' : t).replace(/[\\s\\u3000]/g, '');\n  }\n\n  /** 全角の英数字を半角にそろえる。品番が「ＢＦＰ-30-1000Ｌ」で来ることがある */\n  function ucHalf(t) {\n    return String(t).replace(/[Ａ-Ｚａ-ｚ０-９－]/g, function (c) {\n      if (c === '－') return '-';\n      return String.fromCharCode(c.charCodeAt(0) - 0xFEE0);\n    });\n  }\n\n  /* pdf.js は見出しを1文字ずつに割ってよこす（「品」「 」「番」）。\n     文字の幅（w）を見て、となりとくっついているものをつなぎ直す。\n     これをしないと「品番」という見出しが永遠に見つからず、0件で終わる。 */\n  function ucWords(ln) {\n    var out = [], cur = null, GAP = 12;   // 文字1つぶんの幅。列と列のすき間は70以上ある\n    ln.forEach(function (it) {\n      var t = String(it.s);\n      var blank = !t.trim();\n      // **つなぐのは1文字ずつ来たものだけ。**\n      // 見出しは「品」「 」「番」と1文字ずつ来るのでつなぐ必要があるが、\n      // 明細の文字（「エバフリーAFP型」「LJH-25」）はまとまって来る。\n      // 何でもつなぐと、品名の右はしと品番がくっついて品番が見つからなくなる\n      var single = blank || t.length === 1;\n      if (cur && single && cur.single && it.x - (cur.x + cur.w) < GAP) {\n        if (!blank) cur.s += t;\n        cur.w = (it.x + (it.w || 0)) - cur.x;\n        return;\n      }\n      if (blank) { cur = null; return; }\n      cur = { s: t, x: it.x, y: it.y, w: it.w || 0, single: single };\n      out.push(cur);\n    });\n    return out;\n  }\n\n  /** 品名は数行ぶんの高さの真ん中に置かれている。上でも下でもなく、いちばん近いものを採る */\n  function ucNearest(list, y) {\n    var best = null, bestDy = 1e9;\n    list.forEach(function (h) {\n      var dy = Math.abs(h.y - y);\n      if (dy < bestDy) { bestDy = dy; best = h; }\n    });\n    return best && bestDy < 90 ? best.name : '';\n  }\n\n  function ucReadPage(items, pageNo) {\n    var cx = {}, near = 60;\n    var lines = inabaLines(items).map(ucWords);\n\n    // 1回目：紙面の見出しから、列の x を覚える\n    lines.forEach(function (ws) {\n      ws.forEach(function (it) {\n        var t = ucFlat(it.s);\n        if (t === '品番' && cx.code == null) { cx.code = it.x; cx.headY = it.y; }\n        if (t === '単価' && cx.price == null) cx.price = it.x;\n        if (t === '品名' && cx.name == null) cx.name = it.x;\n      });\n    });\n    if (cx.code == null || cx.price == null) return [];   // 単価表ではないページ\n\n    // 2回目：品名を先に全部ひろう。品名は行の真ん中に置かれていて、\n    // 上から順に読んでいる途中では「まだ出てきていない」ことがある\n    var names = [];\n    if (cx.name != null) {\n      lines.forEach(function (ws) {\n        ws.forEach(function (it) {\n          var t = ucFlat(it.s);\n          if (!t || t === '品名') return;\n          if (Math.abs(it.x - cx.name) > near) return;\n          if (t.charAt(0) === '→') return;             // 「→P.05」は参照ページ\n          if (t.charAt(0) === '■') return;             // 「■排水部材・産業資材」は章の見出し\n          if (t.length < 3) return;                    // 割れて残った「・」などは品名ではない\n          // 表の見出しより上にあるものは、章の題（「■排水部材・産業資材」）であって品名ではない\n          if (cx.headY != null && it.y > cx.headY - 2) return;\n          names.push({ x: it.x, y: it.y, name: t });\n        });\n      });\n    }\n\n    // 3回目：明細\n    var out = [];\n    lines.forEach(function (ws) {\n      if (!ws.length) return;\n      var price = 0, model = '';\n      ws.forEach(function (it) {\n        var t = ucFlat(it.s);\n        var m = t.match(/^[¥￥]([\\d,]+)$/);\n        if (m && Math.abs(it.x - cx.price) < near) { price = Number(m[1].replace(/,/g, '')); return; }\n        if (!model && Math.abs(it.x - cx.code) < near && /[A-Za-zＡ-Ｚａ-ｚ]/.test(t)) model = ucHalf(t);\n      });\n      if (!price || !model) return;\n\n      out.push({\n        m: model, y: price, code: '',\n        name: ucNearest(names, ws[0].y) || model,\n        series: 'ユーシー産業', ref: 'P' + pageNo, page: pageNo\n      });\n    });\n    return out;\n  }\n\n  function ucFinish(sets) {\n    // 同じ品番が「品番表」と「単価表」の両方に出る。値段は同じだが、\n    // 品名が入っているのは単価表のほうだけ。**品名のあるほうを残す**\n    var rows = [], at = {};\n    sets.forEach(function (r) {\n      var i = at[r.m];\n      if (i == null) { at[r.m] = rows.length; rows.push(r); return; }\n      if (rows[i].name === rows[i].m && r.name !== r.m) rows[i] = r;\n    });\n    return {\n      head: {\n        maker: 'ユーシー産業',\n        brand: '単価表',\n        note: '定価・税抜。総合カタログの単価表から読み取ったもの。'\n      },\n      rows: rows,\n      pricePages: sets.length ? 1 : 0\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     オーケー器材（ダイキン系の部材）\n     ----------------------------------------------------------------------\n     空調工事部材カタログ。612ページ・163MBある大物で、値段は製品の表の\n     あちこちに「12,100円/本」の形で散っている。¥ は使わない。\n\n     紙面の作り\n       ・1ページに表が1〜4つ。縦に並ぶことも、左右に並ぶこともある\n       ・列の位置は見出し「品　番」「希望小売価格」が教えてくれる\n       ・**見出しの y は「品番」と「希望小売価格」で3ポイントほどずれる**（同じ行ではない）\n       ・**品番と値段の y も1ポイントほどずれる**\n\n     だから「行にまとめてから読む」をやめた。紙面のへり（縦書きの見出し）に\n     引っぱられて、品番と値段が別の行に割れる。\n     **値段のほうを起点にして、同じ列でいちばん y の近い品番を組にする。**\n     -------------------------------------------------------------------- */\n\n  var OK_PRICE = /^([\\d,]+)円(?:\\/(\\S+))?$/;\n  var OK_CODE  = /^[A-Z][A-Za-z0-9\\-]{2,}$/;\n  var OK_NEAR  = 45;      // 値段の列のずれ\n  var OK_WIDE  = 90;      // 品番の列は見出しがまん中ぞろえで、実物より右に出る\n  var OK_BAND  = 10;      // 見出しの y のずれ\n  // 品番の形をしているが品番ではないもの。BIMは「BIMデータあり」の印\n  var OK_NOT_CODE = { BIM: 1, NEW: 1, PDF: 1, CAD: 1 };\n\n  /** 見出しを y でまとめ、品番と希望小売価格を左から順に組にする */\n  function okBands(ws) {\n    var marks = [];\n    ws.forEach(function (w) {\n      var t = ucFlat(w.s);\n      if (t === '品番') marks.push({ kind: 'code', x: w.x, y: w.y });\n      else if (t === '希望小売価格') marks.push({ kind: 'price', x: w.x, y: w.y });\n    });\n    marks.sort(function (a, b) { return b.y - a.y; });    // pdf.js の y は下から上。上の見出しから\n\n    var bands = [], cur = [], y = null;\n    marks.forEach(function (m) {\n      if (y === null || Math.abs(m.y - y) < OK_BAND) {\n        cur.push(m);\n        if (y === null) y = m.y;\n      } else { bands.push({ y: y, marks: cur }); cur = [m]; y = m.y; }\n    });\n    if (cur.length) bands.push({ y: y, marks: cur });\n\n    var out = [];\n    bands.forEach(function (b) {\n      b.marks.sort(function (a, c) { return a.x - c.x; });\n      var pairs = [];\n      b.marks.forEach(function (m, i) {\n        if (m.kind !== 'code') return;\n        for (var j = i + 1; j < b.marks.length; j++) {\n          if (b.marks[j].kind === 'price') { pairs.push({ cx: m.x, px: b.marks[j].x }); break; }\n        }\n      });\n      if (pairs.length) out.push({ y: b.y, pairs: pairs });\n    });\n    return out;\n  }\n\n  var OK_GLUED = /^([A-Z][A-Za-z0-9\\-]{2,})[\\s　]+([\\d,]+)$/;\n\n  /** 値段の文字の来かたが3通りある。どれも1つの語にそろえる。\n        「12,100円/本」……そのまま\n        「13,700」＋「円/本」……数字のうしろに円が来る\n        「KHR58S211 21,100」＋「円」……**品番と値段が1つの文字になっている**（分岐管のページ）\n      3つ目は品番と値段に切り分ける。値段の x は、うしろに来る「円」の位置を使う */\n  function okWords(ln) {\n    var ws = ucWords(ln), out = [];\n    for (var i = 0; i < ws.length; i++) {\n      var w = ws[i], nx = ws[i + 1];\n      var yen = nx && nx.s.charAt(0) === '円' && nx.x - (w.x + w.w) < 6;\n      var glued = w.s.match(OK_GLUED);\n      if (glued && yen) {\n        out.push({ s: glued[1], x: w.x, y: w.y, w: w.w / 2 });\n        out.push({ s: glued[2] + nx.s, x: nx.x - 1, y: w.y, w: nx.w });\n        i++;\n      } else if (yen && /^[\\d,]+$/.test(w.s)) {\n        out.push({ s: w.s + nx.s, x: w.x, y: w.y, w: (nx.x + nx.w) - w.x });\n        i++;\n      } else out.push(w);\n    }\n    return out;\n  }\n\n  function okReadPage(items, pageNo) {\n    if (!items.length) return [];\n    // 見出しは1文字ずつ、値段は数字と「円/本」に割れて来る。つなぎ直してから見る\n    var ws = [], byLine = [];\n    inabaLines(items, true).forEach(function (ln) {\n      var w = okWords(ln);\n      byLine.push(w);\n      ws = ws.concat(w);\n    });\n\n    var bands = okBands(ws);\n    if (!bands.length) return [];\n\n    /* ページのいちばん上の柱（「クイックパイパー」など）を、そのページの品名にする。\n       **1語だけ採ると尻切れになる。**「スカイダクト」が「スカイダク」＋「ト」に\n       割れて来るので、いちばん上の行をまるごとつなぐ（2026-09-10） */\n    var top = '';\n    for (var li = 0; li < byLine.length && !top; li++) {\n      var t = byLine[li].map(function (w) { return ucFlat(w.s); })\n                        .filter(function (v) { return v && !/^[\\d,.\\-]+$/.test(v); })\n                        .join('');\n      if (t.length >= 3) top = t;\n    }\n\n    var prices = [], codes = [];\n    ws.forEach(function (w) {\n      var t = ucFlat(w.s);\n      var m = t.match(OK_PRICE);\n      if (m) prices.push({ x: w.x, y: w.y, val: Number(m[1].replace(/,/g, '')), unit: m[2] || '' });\n      else if (OK_CODE.test(t) && !OK_NOT_CODE[t]) codes.push({ x: w.x, y: w.y, s: t });\n    });\n\n    var out = [];\n    prices.forEach(function (p) {\n      // その値段より上にある見出しのうち、**値段の列が合うもので**いちばん近いもの。\n      // 左右2つの表が縦にずれて並ぶページがあり、「いちばん近い見出し」だけで選ぶと\n      // 右の表の見出しで左の表を読もうとして、列が合わず丸ごと落ちる\n      var pair = null, all = null;\n      bands.forEach(function (b) {\n        if (b.y < p.y - 1) return;                 // 上＝y が大きい\n        b.pairs.forEach(function (q) {\n          if (Math.abs(p.x - q.px) < OK_NEAR) { pair = q; all = b.pairs; }\n        });\n      });\n      if (!pair) return;\n\n      var best = null, bestDy = 99;\n      codes.forEach(function (c) {\n        if (Math.abs(c.x - pair.cx) >= OK_WIDE) return;\n        // 品番の列がいくつもあるページでは、いちばん近い列のものだけを採る\n        var own = all[0];\n        all.forEach(function (q) { if (Math.abs(c.x - q.cx) < Math.abs(c.x - own.cx)) own = q; });\n        if (own.cx !== pair.cx) return;\n        var dy = Math.abs(c.y - p.y);\n        if (dy < bestDy) { bestDy = dy; best = c.s; }\n      });\n      if (best && bestDy < 4) {\n        out.push({ m: best, y: p.val, code: '', name: top || best, series: top || '部材',\n                   ref: 'P' + pageNo, unit: p.unit, page: pageNo });\n      }\n    });\n    return out;\n  }\n\n  function okFinish(sets) {\n    var rows = [], seen = {};\n    sets.forEach(function (r) { if (!seen[r.m]) { seen[r.m] = 1; rows.push(r); } });\n    return {\n      head: {\n        maker: 'オーケー器材',\n        brand: '空調工事部材カタログ',\n        note: '希望小売価格・税抜。カタログの表から読み取ったもの。'\n      },\n      rows: rows,\n      pricePages: sets.length ? 1 : 0\n    };\n  }\n\n  var MAKERS = [\n    {\n      id: 'carrier',\n      name: '日本キヤリア（旧東芝）',\n      catalog: '店舗・オフィス用カスタムエアコン',\n      size: '212ページ・59MBほど。読み取りに1分ほどかかります。',\n      howto: [\n        '下のリンクからカタログを開く',\n        'カタログの画面から、PDFを丸ごと保存する',\n        '保存したPDFを「カタログPDFを選ぶ」で選ぶ'\n      ],\n      url: 'https://cjc.icata.net/iportal/oc.do?v=CJC00001&d=CJCD01&c=090_90_9999_1&p=1',\n      min: 700,\n      isPricePage: carrierIsPricePage,\n      build: buildCarrier\n    },\n    {\n      id: 'hitachi',\n      name: '日立',\n      catalog: '店舗・オフィス用パッケージエアコン総合カタログ',\n      size: '286ページ・380MBほど。読み取りに6分ほどかかり、そのあいだパソコンが重くなります。',\n      howto: [\n        '下のリンクを押すと、カタログ1冊ぶんのPDFが落ちてくる（380MBあるので数分かかります）',\n        '落ちてきたPDFを「カタログPDFを選ぶ」で選ぶ'\n      ],\n      url: 'https://www.hitachi-gls.co.jp/catalog/office/book/data/Target.pdf',\n      min: 500,\n      isPricePage: hitachiIsPricePage,\n      build: buildHitachi\n    },\n    {\n      id: 'panasonic',\n      name: 'パナソニック',\n      catalog: 'オフィス・店舗用エアコン総合カタログ',\n      size: '236ページ・53MBほど。読み取りに1分ほどかかります。',\n      howto: [\n        '下のリンクからカタログの一覧を開く',\n        '「オフィス・店舗用エアコン総合カタログ」を開いて、PDFを丸ごと保存する',\n        '保存したPDFを「カタログPDFを選ぶ」で選ぶ'\n      ],\n      url: 'https://panasonic.icata.net/iportal/CatalogSearch.do?method=catalogSearchByAnyCategories&volumeID=PEWJ0001&categoryID=353090000',\n      min: 750,\n      layout: true,            // 3段組なので、文字を位置つきで読む\n      readPage: panaReadPage,\n      finish: panaFinish\n    },\n    {\n      id: 'daikin',\n      name: 'ダイキン',\n      catalog: '店舗・オフィスエアコン スカイエア',\n      size: '価格ページだけで130MBほど。読み取りに2分ほどかかります。',\n      howto: [\n        '下のリンクを押すと「52〜123ページ（価格の載っているところ）だけのPDF」を作る画面が出る',\n        'その画面のボタンでPDFを保存する',\n        '保存したPDFを「カタログPDFを選ぶ」で選ぶ'\n      ],\n      url: 'https://ec.daikinaircon.com/cgi-bin/ecatalog/bindPDF.cgi?C=CP26016AXX&S=52&E=123&CT=1&CV=1',\n      min: 700,\n      layout: true,            // 4段組。しかも行の文字をつなげてはいけない\n      readPage: daikinReadPage,\n      finish: daikinFinish\n    },\n    {\n      id: 'daikin-room',\n      name: 'ダイキン（ルームエアコン）',\n      catalog: '住宅設備用カタログ（ルームエアコン・ハウジングエアコン）',\n      size: '100ページ・96MBほど。読み取りに2分ほどかかります。',\n      howto: [\n        '下のリンクを押すと「全ページのPDF」を作る画面が出る',\n        'その画面の［ダウンロード開始］でPDFを保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://ec.daikinaircon.com/cgi-bin/ecatalog/bindPDF.cgi?C=CR25227BXX&S=0&E=99&CT=1&CV=1',\n      urlNote: '壁掛形・天井埋込カセット・床置形・壁埋込形・ビルトイン形・マルチエアコン（マルチパック・ココタス・システムマルチの室外機と室内機）を読みます。パネル別売の形は「パネル込みの合計価格」で入ります。Eシリーズはオープン価格なので値段0で入ります。',\n      min: 60,\n      layout: true,\n      readPage: dkRoomReadPage,\n      finish: dkRoomFinish\n    },\n    {\n      id: 'daikin-room-opt',\n      name: 'ダイキン（ルームエアコン別売品）',\n      catalog: '住宅設備用カタログ の別売品（69〜77ページ）',\n      size: '機種と同じPDFでかまいません。100ページ・96MBほど。読み取りに2分ほどかかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種と同じPDFでかまいません',\n        '下のリンクを押すと「全ページのPDF」を作る画面が出る。［ダウンロード開始］で保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://ec.daikinaircon.com/cgi-bin/ecatalog/bindPDF.cgi?C=CR25227BXX&S=0&E=99&CT=1&CV=1',\n      urlNote: '値段は紙面の税込価格を1.1で割った税抜きで入ります。業務用（スカイエア）の別売品とは別に入ります。',\n      min: 150,\n      readPage: dkRoomOptReadPage,\n      finish: dkRoomOptFinish\n    },\n    {\n      id: 'mitsubishi-room',\n      name: '三菱電機（ルームエアコン）',\n      catalog: '住宅設備用総合カタログ（ルームエアコン・ハウジングエアコン）',\n      size: '100ページ・79MBほど。読み取りに2分ほどかかります。',\n      howto: [\n        '下のリンクを押すとカタログのPDFが開く（大きいので開くまで少しかかります）',\n        '開いたPDFを保存する（右上の保存ボタン、または右クリック →「名前を付けて保存」）',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://dl.mitsubishielectric.co.jp/dl/ldg/wink/wink_doc/contents/doc/WEB_CATA/S1795CB073D/data/target.pdf',\n      urlNote: '壁掛形（FZ・Z・VXV・HXV・JXV・BXV・AXV・NXV・KXV・FL・GV）・天井カセット形・壁埋込形・フリービルトイン形・床置形・システムマルチ（室外機と室内機）を読みます。パネルやグリルが別売の形は「合計価格」で入ります。GVシリーズはオープン価格なので値段0で入ります。',\n      min: 60,\n      layout: true,\n      readPage: meRoomReadPage,\n      finish: meRoomFinish\n    },\n    {\n      id: 'hitachi-room',\n      name: '日立（ルームエアコン）',\n      catalog: '住宅設備用エアコン（白くまくん・ハウジングエアコン）',\n      size: '88ページ・175MBほど。大きいので読み取りに3〜5分かかります。',\n      howto: [\n        '下のリンクを押すとカタログのPDFが開く（とても大きいので開くまで時間がかかります）',\n        '開いたPDFを保存する（右上の保存ボタン、または右クリック →「名前を付けて保存」）',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://kadenfan.hitachi.co.jp/catalog/raj/book/data/Target.pdf',\n      urlNote: '壁掛形（XJ・ZJ・VJ・VL・MJ・AJ・BJ・XK・RK）・床置形（FD）・天井カセット形（PK・PS・PA）・壁埋込形（JA）・システムマルチ（室外機と室内機）・耐塩害仕様を読みます。天井カセット・壁埋込は「合計」（化粧パネル・前面グリル込み）で入ります。AJ・XK・RK・FDはオープン価格なので値段0で入ります。',\n      min: 60,\n      layout: true,\n      readPage: hiRoomReadPage,\n      finish: hiRoomFinish\n    },\n    {\n      id: 'panasonic-room',\n      name: 'パナソニック（ルームエアコン）',\n      catalog: '住宅設備エアコン総合カタログ（エオリア・住宅設備用）',\n      size: '128ページ・65MBほど。読み取りに2〜3分かかります。',\n      howto: [\n        '下のリンクを押すとカタログのページが開く',\n        '「ダウンロード」を押す（「ご利用条件」が出たら、読んで同意する）',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://panasonic.icata.net/iportal/CatalogDetail.do?method=initial_screen&type=clcsr&volumeID=PEWJ0001&catalogID=7901270000&designID=standard_sp',\n      urlNote: '壁掛け（HX・EX・GX・J・F・EL・N・C）・寒冷地（UX・TX・K・UB・UY）・加湿換気（LV）・三相モデル・床置き（Y）・天井ビルトイン（BC・BW）・壁ビルトイン（BK）・フリービルトイン（BA）・マルチ（室外機と室内機）・耐塩害仕様を読みます。値段は税抜（カタログは税込で、（ ）内が税抜）。ビルトインは別売の化粧グリル等込みの合計です。オープン価格の機種は値段0で入ります。',\n      min: 100,\n      layout: true,\n      readPage: panaRoomReadPage,\n      finish: panaRoomFinish\n    },\n    {\n      id: 'hitachi-room-opt',\n      name: '日立（ルームエアコン別売品）',\n      catalog: '住宅設備用エアコン の別売部品（71〜76ページ）',\n      size: '機種と同じPDFでかまいません。88ページ・175MBほど。読み取りに3〜5分かかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種と同じPDFでかまいません',\n        '下のリンクを押すとカタログのPDFが開く。保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://kadenfan.hitachi.co.jp/catalog/raj/book/data/Target.pdf',\n      urlNote: '値段は紙面の税抜価格で入ります（税込だけ書いてある品は税抜に直します）。付く機種は紙面の適用一覧表と「適用機種」の文から決めます。業務用の日立の別売品とは別に入ります。',\n      min: 50,\n      readPage: hiRoomOptReadPage,\n      finish: hiRoomOptFinish\n    },\n    {\n      id: 'mitsubishi-room-opt',\n      name: '三菱電機（ルームエアコン別売品）',\n      catalog: '住宅設備用総合カタログ の別売部品（44・55〜57・67〜70ページ）',\n      size: '機種と同じPDFでかまいません。100ページ・79MBほど。読み取りに2分ほどかかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種と同じPDFでかまいません',\n        '下のリンクを押すとカタログのPDFが開く。保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://dl.mitsubishielectric.co.jp/dl/ldg/wink/wink_doc/contents/doc/WEB_CATA/S1795CB073D/data/target.pdf',\n      urlNote: '値段は紙面の税別のまま入ります。室外機の高さや配管の太さで決まる部品は、どの機種にも出して品名に条件を書きます。業務用（Mr.SLIM）の別売品とは別に入ります。',\n      min: 80,\n      readPage: meRoomOptReadPage,\n      finish: meRoomOptFinish\n    },\n    {\n      id: 'panasonic-room-opt',\n      name: 'パナソニック（ルームエアコン別売品）',\n      catalog: '住宅設備エアコン総合カタログ のアクセサリー／別売品（114〜118ページ）',\n      size: '機種と同じPDFでかまいません。128ページ・65MBほど。読み取りに2〜3分かかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種と同じPDFでかまいません',\n        '下のリンクを押すとカタログのページが開く。「ダウンロード」で保存する（「ご利用条件」が出たら、読んで同意する）',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://panasonic.icata.net/iportal/CatalogDetail.do?method=initial_screen&type=clcsr&volumeID=PEWJ0001&catalogID=7901270000&designID=standard_sp',\n      urlNote: '値段は税抜（カタログは税込で、（ ）内が税抜）。フィルター・配管化粧カバー（スッキリダクト）・室外機の置台と金具・日よけ屋根・吹出グリル・リモコンホルダー・室外機背面金網・集中コントロールを読みます。付く機種は紙面の適用一覧表（シリーズと能力）から決めます。業務用のパナソニックの別売品とは別に入ります。',\n      min: 80,\n      readPage: panaRoomOptReadPage,\n      finish: panaRoomOptFinish\n    },\n    {\n      id: 'general-room',\n      name: 'ゼネラル（ルームエアコン）',\n      catalog: '住宅設備用カタログ 2026（ノクリア）',\n      size: '20ページ・13MBほど。読み取りは1分かかりません。',\n      howto: [\n        '下のリンクを押すとカタログのPDFが開く',\n        '開いたPDFを保存する（右上の保存ボタン、または右クリック →「名前を付けて保存」）',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://www.generalww.com/jp/resources/pdf/support/downloads/aircon/catalog/2026/ctlg-a674-all-01.pdf',\n      urlNote: '壁掛形（X・Z・NH・W・L・VH・RH・CH と、寒冷地仕様の ZN・DN）を読みます。値段は紙面の税別のまま入ります。オープン価格の機種（X・Z・W・L・ZN・DN）は値段0で入ります。',\n      min: 50,\n      layout: true,\n      readPage: genRoomReadPage,\n      finish: genRoomFinish\n    },\n    {\n      id: 'general-room-opt',\n      name: 'ゼネラル（ルームエアコン別売品）',\n      catalog: '住宅設備用カタログ 2026 の別売品（37ページ）とシリーズのページの別売品',\n      size: '機種と同じPDFでかまいません。20ページ・13MBほど。読み取りは1分かかりません。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種と同じPDFでかまいません',\n        '下のリンクを押すとカタログのPDFが開く。保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://www.generalww.com/jp/resources/pdf/support/downloads/aircon/catalog/2026/ctlg-a674-all-01.pdf',\n      urlNote: '値段は紙面の税込を1.1で割った税抜きで入ります。風向ガイド・無線LANアダプター・かんたんリモコン・リモコンホルダー（付く機種は別売品の表）、寒冷地仕様の防雪フード、日晴金属の架台・金具、抗菌空清フィルターを読みます。',\n      min: 15,\n      readPage: genRoomOptReadPage,\n      finish: genRoomOptFinish\n    },\n    {\n      id: 'mitsubishi',\n      name: '三菱電機',\n      catalog: 'Mr.SLIM の機種データ（価格つき）',\n      size: '1MBほど。読み取りはすぐ終わります。',\n      kind: 'json',            // PDFではなくデータファイルを読む\n      howto: [\n        '下のリンクを押すと、文字がびっしり並んだ画面が出る（これで合っています）',\n        'その画面で右クリック →「名前を付けて保存」で保存する',\n        '保存したファイルを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://www.mitsubishielectric.co.jp/ldm/slim/search/data.json',\n      min: 900,\n      finish: mitsuFinish\n    },\n    {\n      id: 'daikin-opt',\n      name: 'ダイキン（別売品）',\n      catalog: '店舗・オフィスエアコン スカイエア の別売品',\n      size: '別売品のページだけで60MBほど。読み取りに2分ほどかかります。',\n      kind: 'options',         // 機種データではなく別売品として入る\n      layout: true,\n      howto: [\n        '下のリンクを押すと「180〜243ページ（別売品のところ）だけのPDF」を作る画面が出る',\n        'その画面のボタンでPDFを保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://ec.daikinaircon.com/cgi-bin/ecatalog/bindPDF.cgi?C=CP26016AXX&S=180&E=243&CT=1&CV=1',\n      min: 500,\n      readPage: function (items, page) {\n        var txt = items.map(function (i) { return i.s; }).join('').replace(/\\s/g, '');\n        /* 耐塩害・耐重塩害の「機種の選び方」ページは別売品ではない。\n           室外機の品番と本体価格が並んでいるので、放っておくと\n           機種そのものが「どの機種にも付く別売品」として72件入ってしまう\n           （2026-09-09、RZRP40CV 581,000円 などで分かった） */\n        if (/耐重塩害仕様/.test(txt)) return [];\n        var out = [];\n        optPage(items, page, out);            // 列の見出しが室内機の表\n        optPageDaikinVert(items, page, out); // 品番が縦に並び、価格が下に1行だけの表\n        return out;\n      },\n      finish: function (list) { return optResult(list, 'ダイキン', 'スカイエア 別売品'); }\n    },\n    {\n      id: 'panasonic-opt',\n      name: 'パナソニック（別売品）',\n      catalog: 'オフィス・店舗用エアコン総合カタログ の別売品',\n      size: '236ページ・53MBほど。読み取りに5分ほどかかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種データと同じPDFでかまいません',\n        'カタログを開いてPDFを保存し、「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://panasonic.icata.net/iportal/CatalogSearch.do?method=catalogSearchByAnyCategories&volumeID=PEWJ0001&categoryID=353090000',\n      min: 160,\n      readPage: function (items, page, ctx) {\n        var out = [];\n        /* 別売品一覧表のページには機種タイプが書いていない。\n           紙面のノドに入っている「54 ４方向天井カセット形」を手がかりにする。\n           これが無いと、壁掛形にも天井パネルが出てしまう（2026-09-09） */\n        optPagePana(items, page, out, { types: PANA_TYPES }, ctx);\n        return out;\n      },\n      finish: function (list) { return optResult(list, 'パナソニック', 'オフィス・店舗用エアコン 別売品'); }\n    },\n    {\n      id: 'carrier-opt',\n      name: '日本キヤリア（別売品）',\n      catalog: '店舗・オフィス用カスタムエアコン の別売品',\n      size: '212ページ・59MBほど。読み取りに4分ほどかかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種データと同じPDFでかまいません',\n        'カタログを開いてPDFを保存し、「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://cjc.icata.net/iportal/oc.do?v=CJC00001&d=CJCD01&c=090_90_9999_1&p=1',\n      min: 80,\n      readPage: function (items, page, ctx) {\n        var out = [];\n        /* 別売品のページには機種タイプが書いていない（章立てで決まっているため）。\n           手前の**価格ページの形名**からタイプを割り出して覚えておき、\n           別売品のページはその続きとみなす。\n           この覚え書き（ctx.pageTypes）は前から使うつもりで書いてあったのに、\n           入れるほうを作っていなかった。そのため化粧パネルもリモコンも\n           どの機種にも当たらず、室外機の架台しか出ていなかった（2026-09-09） */\n        var flat = items.map(function (i) { return i.s; }).join(' ');\n        if (ctx && carrierIsPricePageLoose(flat)) {\n          var cnt = {}, re = /\\b([A-Z]{4}\\d{5}[A-Z0-9]*)\\b/g, m;\n          while ((m = re.exec(flat)) !== null) {\n            var t = CARRIER_TYPE[m[1].charAt(1)];\n            if (t) cnt[t] = (cnt[t] || 0) + 1;\n          }\n          var best = '', bn = 0;\n          Object.keys(cnt).forEach(function (k) { if (cnt[k] > bn) { bn = cnt[k]; best = k; } });\n          if (best) { ctx.pageTypes = ctx.pageTypes || {}; ctx.pageTypes[page] = best; }\n        }\n        // キヤリアは表が2種類ある。室内機用（適用室内ユニット列）と室外機用（列＝シリーズ）\n        optPagePana(items, page, out, {\n          codeWord: /部品形名/, priceWord: /価格/,\n          types: ['天井カセット形4方向', '天井カセット形2方向', '天井カセット形1方向',\n                  '天井吊形', '壁掛形', 'ビルトイン', 'ダクト', '床置形', '厨房用天井吊形']\n        }, ctx);\n        // 品名は一覧の表の読み方で付け直す（上の carrierPageNames の説明）\n        if (out.length) {\n          var carName = carrierPageNames(items);\n          if (carName) out.forEach(function (o) { var nm = carName(o.code); if (nm && nm.length >= 2) o.name = nm; });\n        }\n        // 室内機用の表が無いページは、室外機用（列＝シリーズ）として読み直す\n        if (!out.length) optPageCarrier(items, page, out);\n        // 「別売部品一覧」の表（室内機に付くパネル・フィルターはここにしか無い）\n        if (!out.length) optPageCarrierParts(items, page, out, ctx);\n        return out;\n      },\n      finish: function (list) { return optResult(list, '日本キヤリア（旧東芝）', '店舗・オフィス用カスタムエアコン 別売品'); }\n    },\n    {\n      id: 'hitachi-opt',\n      name: '日立（別売品）',\n      catalog: '店舗・オフィス用パッケージエアコン総合カタログ の別売品',\n      size: '286ページ・380MBほど。読み取りに6分ほどかかり、そのあいだパソコンが重くなります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '機種データと同じPDFでかまいません',\n        '下のリンクからPDFを保存し、「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://www.hitachi-gls.co.jp/catalog/office/book/data/Target.pdf',\n      min: 75,\n      readPage: function (items, page) { var out = []; optPageHitachi(items, page, out); return out; },\n      finish: function (list) { return optResult(list, '日立', '店舗・オフィス用パッケージエアコン 別売品'); }\n    },\n    {\n      id: 'mitsubishi-opt',\n      name: '三菱電機（別売品）',\n      catalog: 'Mr.SLIM 総合カタログ の別売品',\n      size: '136MBほど。読み取りに4分ほどかかります。',\n      kind: 'options',\n      layout: true,\n      howto: [\n        '下のリンクからカタログのPDFを保存する',\n        '保存したPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://dl.mitsubishielectric.co.jp/dl/ldg/wink/wink_doc/contents/doc/WEB_CATA/S1794CB020E/data/target.pdf',\n      min: 10,\n      readPage: function (items, page) {\n        var out = [];\n        optPageMitsuList(items, page, out);      // オプション構成図のページ（132〜142ページ）\n        optPageMitsu(items, page, out);          // 機種ページの構成品（パネル・リモコン）\n        optPageMitsuCommon(items, page, out);   // 共通別売部品（分配管など・144ページ）\n        optPageMitsuOutdoor(items, page, out);  // 室外ユニットオプション（143ページ）\n        return out;\n      },\n      finish: function (list) { return optResult(list, '三菱電機', 'Mr.SLIM 別売品'); }\n    },\n    {\n      id: 'inaba-parts',\n      name: '因幡電工（部材）',\n      catalog: '価格改定表（総合カタログ／エアコン配管部材カタログ）',\n      size: '2冊で2MBほど。読み取りはすぐ終わります。',\n      kind: 'parts',           // 機種データでも別売品でもなく、単価マスタに入る\n      layout: true,\n      howto: [\n        '下のリンクを押すと「価格改定表」のPDFが落ちてくる（軽いのですぐ終わります）',\n        'エアコンの配管部材だけでよければ、下のもう1本のほうが小さい',\n        '落ちてきたPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://www.inaba-denko.com/storage/files/202606_price.pdf',\n      url2: 'https://www.inaba-denko.com/common/broadcast/feature/img/202606_aircon_price.pdf',\n      urlNote: '因幡電工のデジタルカタログのページ（https://www.inaba-denko.com/ja/catalog）に、いつも最新の「価格改定表」が並んでいます。値上げがあったら、そこから取り直してください。',\n      min: 200,\n      readPage: inabaReadPage,\n      finish: inabaFinish\n    },\n    {\n      id: 'uc-parts',\n      name: 'ユーシー産業（部材）',\n      catalog: 'エバック 総合カタログ（うしろの単価表を読みます）',\n      size: '76ページ・24MBほど。読み取りに20秒ほどかかります。',\n      kind: 'parts',\n      layout: true,\n      howto: [\n        '下のリンクを押すと、総合カタログのPDFが落ちてくる',\n        '落ちてきたPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://microbecms-microbe-cms-evuc-uploads.s3.ap-northeast-1.amazonaws.com/uploads/2026/05/1779965937194-70c95f460c37d093.pdf',\n      urlNote: 'リンクが切れていたら、ユーシー産業のサイト（https://www.evuc.co.jp/e-catalog/）のWEBカタログから取り直してください。',\n      min: 30,\n      readPage: ucReadPage,\n      finish: ucFinish\n    },\n    {\n      id: 'okkizai-parts',\n      name: 'オーケー器材（部材）',\n      catalog: '空調工事部材カタログ 2026〜2027',\n      size: '612ページ・163MBほど。読み取りに5〜8分かかり、そのあいだパソコンが重くなります。ほかのアプリを閉じてから始めてください。',\n      kind: 'parts',\n      layout: true,\n      howto: [\n        '下のリンクを押すと、カタログ1冊ぶんのPDFが落ちてくる（163MBあるので数分かかります）',\n        '落ちてきたPDFを「カタログのファイルを選ぶ」で選ぶ'\n      ],\n      url: 'https://dcs.gamedios.com/webapi/v4.7/stream/get/file/data?appkey=JOSGCVAITEAWL&volumeid=OKK10001&id=22045240000&dg=30F7CC7E890BF5C045EDC2D9126960E5967CE365&age=orig',\n      urlNote: 'リンクが切れていたら、オーケー器材のデジタルカタログ（https://ok-kizai.co.jp/system/catalog/index）の「空調工事部材カタログ」から取り直してください。',\n      min: 800,\n      readPage: okReadPage,\n      finish: okFinish\n    }\n  ];\n\n  /* --------------------------------------------------------------------\n     PDFを読む\n     pdf-parse（ブラウザ版）を使う。中身は pdf.js だが、紙面の段組みを\n     復元してくれるところが値打ち。自前で並べ替えると 46個中6件しか\n     取れない（2026-09-02 実測）。真似しようとしないこと。\n\n     日本語のPDFは cMapUrl / cMapPacked を渡さないと文字が丸ごと消える。\n     消えても例外は出ないので、渡し忘れると「価格が載っていない」と\n     見誤る。ここは絶対に外さない。\n     -------------------------------------------------------------------- */\n  var CHUNK = 8;   // 何ページずつ読むか（進み具合を出すため小分けにする）\n\n  function readPages(file, maker, onProgress) {\n    var PP = window.PdfParse && window.PdfParse.PDFParse;\n    if (!PP) return Promise.reject(new Error('PDFを読む部品が読み込めませんでした'));\n    PP.setWorker('vendor/pdfparse/pdf.worker.mjs?v=' + (window.KUCHOO_APP_VERSION || ''));\n\n    // 読み方は大きさで変える。\n    // ・ふつうの大きさ（キヤリア59MB）は丸ごとメモリに載せる。212ページを41秒。\n    // ・大きいもの（日立は1冊380MB）を丸ごと載せると端末が落ちる。\n    //   blobのURLを渡すと、要るところだけ読みに行くのでメモリは要らない。\n    //   ただし読み込みは10倍ほど遅い（実測。だから小さいものには使わない）。\n    var big = file.size > 150 * 1024 * 1024;\n    var blobUrl = big ? URL.createObjectURL(file) : '';\n\n    return (big ? Promise.resolve(null) : file.arrayBuffer()).then(function (buf) {\n      var parser = new PP(big\n        ? { url: blobUrl, cMapUrl: 'vendor/pdfparse/cmaps/', cMapPacked: true,\n            disableAutoFetch: true, verbosity: 0 }\n        : { data: new Uint8Array(buf), cMapUrl: 'vendor/pdfparse/cmaps/', cMapPacked: true,\n            verbosity: 0 });\n      return parser.getInfo().then(function (info) {\n        var total = info.total || info.numpages || 0;\n        if (!total) throw new Error('ページ数が読み取れませんでした。PDFが壊れていないか確かめてください');\n        var pages = {}, at = 1;\n\n        function step() {\n          if (at > total) return Promise.resolve();\n          var part = [];\n          for (var n = at; n < at + CHUNK && n <= total; n++) part.push(n);\n          at += CHUNK;\n          return parser.getText({ partial: part }).then(function (r) {\n            (r.pages || []).forEach(function (pg, i) {\n              var num = pg.num != null ? pg.num : part[i];\n              var text = pg.text || '';\n              // 値段の載っていないページの文字は、その場で捨てる。\n              // 日立は286ページある。全部ためておくとメモリを無駄に食う。\n              // （ページ番号は残すので「何ページ読んだか」は分かる）\n              pages[num] = (!maker.isPricePage || maker.isPricePage(text)) ? text : '';\n            });\n            if (onProgress) onProgress(Math.min(at - 1, total), total);\n            // 画面を固まらせない\n            return new Promise(function (ok) { setTimeout(ok, 0); }).then(step);\n          });\n        }\n\n        return step().then(function () {\n          return parser.destroy().then(function () { return pages; },\n                                       function () { return pages; });\n        });\n      });\n    }).then(function (pages) {\n      if (blobUrl) URL.revokeObjectURL(blobUrl);\n      return pages;\n    }, function (e) {\n      if (blobUrl) URL.revokeObjectURL(blobUrl);\n      throw e;\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     位置つきで読む（段組みのある紙面用）\n\n     pdf-parse の部品を読み込むと、その中の pdf.js 本体が\n     window.pdfjsLib として一緒に出てくる。だから追加の部品は要らない。\n\n     1ページ読むごとに、その場で機種の塊にしてしまう。\n     ページの文字は残さない（286ページぶん残すとメモリを食う）。\n     -------------------------------------------------------------------- */\n  function readLayout(file, maker, onProgress, ctx) {\n    var PP = window.PdfParse && window.PdfParse.PDFParse;\n    if (!PP) return Promise.reject(new Error('PDFを読む部品が読み込めませんでした'));\n    PP.setWorker('vendor/pdfparse/pdf.worker.mjs?v=' + (window.KUCHOO_APP_VERSION || ''));\n\n    var pdfjs = window.pdfjsLib;\n    if (!pdfjs || !pdfjs.getDocument) {\n      return Promise.reject(new Error('PDFを読む部品（pdf.js）が見つかりませんでした'));\n    }\n\n    var big = file.size > 150 * 1024 * 1024;\n    var blobUrl = big ? URL.createObjectURL(file) : '';\n\n    return (big ? Promise.resolve(null) : file.arrayBuffer()).then(function (buf) {\n      var opt = big ? { url: blobUrl, disableAutoFetch: true } : { data: new Uint8Array(buf) };\n      opt.cMapUrl = 'vendor/pdfparse/cmaps/';   // 渡さないと日本語が丸ごと消える\n      opt.cMapPacked = true;\n      opt.verbosity = 0;\n\n      return pdfjs.getDocument(opt).promise.then(function (doc) {\n        var total = doc.numPages, sets = [], at = 1;\n        if (!total) throw new Error('ページ数が読み取れませんでした。PDFが壊れていないか確かめてください');\n\n        function step() {\n          if (at > total) return Promise.resolve();\n          var n = at++;\n          return doc.getPage(n).then(function (pg) {\n            return pg.getTextContent().then(function (c) {\n              var items = [];\n              (c.items || []).forEach(function (i) {\n                // w（文字の幅）は別売品の品名を組み立てるときに使う\n                if (i.str) items.push({ s: i.str, x: i.transform[4], y: i.transform[5], w: i.width || 0 });\n              });\n              var got = maker.readPage(items, n, ctx);\n              if (got && got.length) sets.push.apply(sets, got);\n              pg.cleanup();\n              if (onProgress) onProgress(n, total);\n              // 画面を固まらせない\n              return new Promise(function (ok) { setTimeout(ok, 0); }).then(step);\n            });\n          });\n        }\n\n        return step().then(function () {\n          return doc.destroy().then(function () { return sets; }, function () { return sets; });\n        });\n      });\n    }).then(function (sets) {\n      if (blobUrl) URL.revokeObjectURL(blobUrl);\n      return sets;\n    }, function (e) {\n      if (blobUrl) URL.revokeObjectURL(blobUrl);\n      throw e;\n    });\n  }\n\n  /* --------------------------------------------------------------------\n     読み取った行を、空調王が読む形（辞書＋番号）に詰め直す\n     -------------------------------------------------------------------- */\n  var FIELDS = ['m', 'hp', 'y', 'u', 's', 'i', 'ab', 'pw', 'rc', 'tp', 'opt', 'om', 'im', 'pm', 'rm'];\n  var DICT_FIELDS = ['s', 'i', 'ab', 'pw', 'rc', 'tp', 'opt', 'om', 'im', 'pm', 'rm'];\n\n  function pack(head, rows) {\n    var dict = {}, idx = {};\n    DICT_FIELDS.forEach(function (f) {\n      var vals = [], seen = {};\n      rows.forEach(function (r) {\n        var v = r[f];\n        if (!(v in seen)) { seen[v] = vals.length; vals.push(v); }\n      });\n      dict[f] = vals; idx[f] = seen;\n    });\n    var d = new Date(), p2 = function (x) { return String(x).length < 2 ? '0' + x : String(x); };\n    return {\n      maker: head.maker, brand: head.brand, source: head.source, note: head.note,\n      fetched: d.getFullYear() + '-' + p2(d.getMonth() + 1) + '-' + p2(d.getDate()),\n      seriesOrder: head.seriesOrder, typeOrder: head.typeOrder, urlBase: head.urlBase,\n      fields: FIELDS, dictFields: DICT_FIELDS, dict: dict,\n      rows: rows.map(function (r) {\n        return FIELDS.map(function (f) {\n          return DICT_FIELDS.indexOf(f) >= 0 ? idx[f][r[f]] : r[f];\n        });\n      })\n    };\n  }\n\n  /* --------------------------------------------------------------------\n     読み取りの点検\n     少なすぎ・空っぽを黙って通さない。何を疑えばいいかまで出す。\n     -------------------------------------------------------------------- */\n  function inspect(maker, res) {\n    var rows = res.rows;\n    if (!rows.length) {\n      var bad = ['1件も読み取れませんでした。'];\n      if (!res.pricePages) bad.push('価格の載ったページが1枚も見つかりませんでした。');\n      bad.push('確かめること：① そのメーカーのカタログか ② 値段の載っている総合カタログか（技術資料ではないか） ③ カタログが新しくなって書き方が変わっていないか');\n      return { ok: false, msg: bad.join('\\n') };\n    }\n    var noPrice = rows.filter(function (r) { return !r.y; }).length;\n    if (noPrice === rows.length) {\n      return { ok: false, msg: '品番は読めましたが、金額が1件も取れませんでした。金額の書き方（¥・円・全角）が変わった可能性があります。' };\n    }\n    if (rows.length < maker.min) {\n      return {\n        ok: false, warn: true,\n        msg: '読み取れたのは ' + rows.length + ' 件でした。ふだんは ' + maker.min + ' 件以上あります。\\n' +\n             'カタログが新しくなって書き方が変わったのかもしれません。このまま使うと機種が抜けます。'\n      };\n    }\n    return { ok: true, noPrice: noPrice };\n  }\n\n  /* --------------------------------------------------------------------\n     入口\n     run(file, makerId, onProgress) → { pack, count, … }\n     -------------------------------------------------------------------- */\n  function run(file, makerId, onProgress, ctx) {\n    ctx = ctx || {};          // ページ→機種タイプの覚え書きを入れる場所\n    var maker = null;\n    MAKERS.forEach(function (mk) { if (mk.id === makerId) maker = mk; });\n    if (!maker) return Promise.reject(new Error('メーカーが選ばれていません'));\n\n    // 読み方は3通り。\n    //  ・データファイル（三菱）……そのまま読む\n    //  ・段組みのある紙面（パナ・ダイキン）……文字を位置つきで読む\n    //  ・ふつうの紙面（キヤリア・日立）……つないだ文字で読む\n    var job = maker.kind === 'json'\n      ? readJsonFile(file).then(function (list) { return maker.finish(list); })\n      : (maker.layout\n          ? readLayout(file, maker, onProgress, ctx).then(function (sets) { return maker.finish(sets); })\n          : readPages(file, maker, onProgress).then(function (pages) { return maker.build(pages); }));\n\n    return job.then(function (res) {\n      var chk = inspect(maker, res);\n      if (!chk.ok) {\n        var e = new Error(chk.msg);\n        e.soft = !!chk.warn;\n        e.rows = res.rows.length;\n        // 「少なすぎる」で止めた場合だけは、中身も渡す。\n        // 本当に機種が減った年かもしれないので、人が見て決められるようにする。\n        if (chk.warn && res.rows.length) e.pack = pack(res.head, res.rows);\n        throw e;\n      }\n      // 部材は単価マスタに入る。機種データの形（pack）には通さない\n      if (maker.kind === 'parts') {\n        return {\n          parts: { maker: res.head.maker, brand: res.head.brand, note: res.head.note, rows: res.rows },\n          count: res.rows.length,\n          pricePages: res.pricePages,\n          noPrice: chk.noPrice\n        };\n      }\n\n      // 別売品は機種データとは別の入れ物にする\n      if (maker.kind === 'options') {\n        var d = new Date(), p2 = function (x) { return String(x).length < 2 ? '0' + x : String(x); };\n        return {\n          options: {\n            maker: res.head.maker,\n            brand: res.head.brand,\n            note: res.head.note,\n            fetched: d.getFullYear() + '-' + p2(d.getMonth() + 1) + '-' + p2(d.getDate()),\n            items: res.rows\n          },\n          count: res.rows.length,\n          pricePages: res.pricePages,\n          noPrice: chk.noPrice\n        };\n      }\n\n      return {\n        pack: pack(res.head, res.rows),\n        count: res.rows.length,\n        pricePages: res.pricePages,\n        noPrice: chk.noPrice\n      };\n    });\n  }\n\n  /* ====================================================================\n     別売品が、その機種に付くかどうかを見分ける\n     --------------------------------------------------------------------\n     **適用機種の書かれ方は5社で全部ちがう。** 紙面を見て確かめた。\n\n       ダイキン    列の見出しが室内機の品番（FHCP40〜71GA）\n       パナソニック「適用室内ユニット」列（「全機種」「P40〜P80」）＋ページの機種タイプ\n       日本キヤリア列の見出しがシリーズ、マスが能力ランク（P40形〜P50形）\n       日立        ページの機種タイプ（てんかせ4方向）＋列の見出しが容量（28型〜71型）\n\n     そこで、どの社の書き方も次の形に直してから比べる。\n       { all:true }                        …… 全機種に付く\n       { im:'FHCP40～71GA' }               …… 室内機の品番の範囲\n       { type:'てんかせ4方向', cap:[28,71] } …… 機種タイプ＋容量の範囲\n       { series:'ウルトラパワーエコ', cap:[40,50] } …… シリーズ＋容量の範囲\n     ==================================================================== */\n  // 日立には28型・32型など小さいものがある。ここに無い数字は容量とみなさないので、\n  // 抜けていると「28型〜71型」が「71型だけ」になってしまう\n  var CAPS = [20, 22, 25, 28, 32, 36, 40, 45, 50, 56, 63, 71, 80, 90, 100, 112, 125, 140, 160,\n              180, 200, 224, 250, 280, 335, 400, 450, 500, 560];\n\n  /** 機種データの1行から容量（40・112 など）を取る。\n      **室内機の品番を先に見る。**\n      別売品（パネル・フィルター・グリル）は室内機に付くもので、\n      表の列も室内機の容量で並んでいる。\n      ab（224型＝8馬力相当）はシステム全体＝室外機の容量なので、\n      ツイン・トリプルのときに実物より何倍も大きくなり、\n      **どの列にも当たらず別売品が1つも出なくなる**\n      （2026-09-09、RAS-GP224RGH2／RPK-GP56KA×4 で分かった。室内機は56型） */\n  function capOfModel(x) {\n    if (!x) return 0;\n    var im = String(x.im || '').replace(/×\\s*\\d+\\s*$/, '');\n    var m = im.match(/(\\d{2,3})/);\n    if (m && CAPS.indexOf(Number(m[1])) >= 0) return Number(m[1]);\n    m = String(x.ab || '').match(/(\\d{2,3})\\s*[形型]/);\n    if (m) return Number(m[1]);\n    m = String(x.m || '').match(/(\\d{2,3})/);\n    return m ? Number(m[1]) : 0;\n  }\n\n  /** ゆるく比べる（「てんかせ4方向」と「てんかせ4方向（Jr.除く）」を同じとみなす） */\n  function looseSame(a, b) {\n    if (!a || !b) return false;\n    var s = function (t) {\n      return String(t).replace(/[\\s　・（）()［］\\[\\]【】〈〉<>《》]/g, '')\n        .replace(/[０-９]/g, function (c) { return String.fromCharCode(c.charCodeAt(0) - 0xFEE0); })\n        // 「天井ビルトインカセット（Eco）」は「天井ビルトインカセット形」と同じ形。\n        // 後ろのグレード名だけを外す（形そのものは外さない。\n        // 外すと「天井埋込形」と「天井埋込ダクト形」が同じになってしまう）\n        .replace(/(Eco|Premium|沖縄専用|沖縄)$/i, '');\n    };\n    var x = s(a), y = s(b);\n    return x.indexOf(y) >= 0 || y.indexOf(x) >= 0;\n  }\n\n  /** 室内機の品番のまとめ書き（FHCP40〜71GA）に、その品番が入るか */\n  function imInRange(head, imCode) {\n    var h = String(head).replace(/\\s/g, '');\n    var im = String(imCode || '').replace(/\\s/g, '').replace(/×\\d+$/, '');\n    var mh = h.match(/^([A-Z]+)([\\d～~〜・,、]+)([A-Z]+)$/);\n    var mi = im.match(/^([A-Z]+)(\\d{2,3})([A-Z]+)$/);\n    if (!mh || !mi) return false;\n    if (mh[1] !== mi[1] || mh[3] !== mi[3]) return false;\n    var cap = Number(mi[2]), mid = mh[2];\n    if (/[～~〜]/.test(mid)) {\n      var ab = mid.split(/[～~〜]/);\n      return cap >= Number(ab[0]) && cap <= Number(ab[1]) && CAPS.indexOf(cap) >= 0;\n    }\n    return mid.split(/[・,、]/).map(Number).indexOf(cap) >= 0;\n  }\n\n  /* ルームエアコンの別売品の「付く機種」。紙面の見出しは「S22～406ATRS」「S40・566ATRP」「C22～563ATSV」「MP40・453AV」。\n     頭（S・C・2M・MP）＋能力＋うしろ（6ATRS）に分けて持つ。機種の品番・室外機・室内機のどれかが入れば付く。\n     うしろの字のあとに色の W・K が付く品番（C223ATSVW）も同じ機種として見る */\n  function roomCodeIn(f, code) {\n    code = String(code || '').replace(/×\\d+$/, '');\n    if (!f.p || code.indexOf(f.p) !== 0) return false;\n    var rest = code.slice(f.p.length);\n    if (rest.slice(-f.t.length) !== f.t) {\n      if (/[WK]$/.test(rest) && rest.slice(0, -1).slice(-f.t.length) === f.t) rest = rest.slice(0, -1);\n      else return false;\n    }\n    var cap = rest.slice(0, rest.length - f.t.length);\n    if (!/^\\d{2,3}$/.test(cap)) return false;\n    cap = Number(cap);\n    return f.r ? (cap >= f.c[0] && cap <= f.c[1]) : f.c.indexOf(cap) >= 0;\n  }\n  function roomCodeIs(mc, code) {\n    code = String(code || '').replace(/×\\d+$/, '');\n    return code === mc || code === mc + 'W' || code === mc + 'K';\n  }\n\n  /* パナソニックのルームエアコンの別売品の「付く機種」。紙面の表は「シリーズ×能力」（116ページ「HX（200V）4.0～9.0kW」）。\n     s＝シリーズ、kr＝能力の範囲、k＝能力の並び、v＝電源、re＝形名の形（年式や在庫限定の型の見分け）。\n     能力は機種データの馬力の欄（「2.2kW（おもに6畳）」）の頭の数 */\n  function rsFits(f, model) {\n    // シリーズはぴったり同じ名前で見る。looseSame は片方がもう片方を含めば同じとみなすので、\n    // 「Kシリーズ」が「BKシリーズ」、「Cシリーズ」が「BCシリーズ」、「Yシリーズ」が「UYシリーズ」に当たってしまう\n    if (String(f.s) !== String(model.s || '')) return false;\n    if (f.re) {\n      var rx = new RegExp(f.re);\n      if (![model.m, model.im].some(function (c) { return rx.test(String(c || '')); })) return false;\n    }\n    if (f.v && String(model.pw || '').indexOf(f.v) < 0) return false;\n    if (f.k || f.kr) {\n      var m = String(model.hp || model.ab || '').match(/^(\\d+(?:\\.\\d+)?)kW/);\n      var kw = m ? Number(m[1]) : 0;\n      if (!kw) return false;\n      if (f.kr && (kw < f.kr[0] - 0.01 || kw > f.kr[1] + 0.01)) return false;\n      if (f.k && !f.k.some(function (k) { return Math.abs(k - kw) < 0.01; })) return false;\n    }\n    return true;\n  }\n\n  function optFits(fit, model) {\n    if (!fit || !model) return false;\n    if (fit.all) return true;\n    if (fit.rs) return rsFits(fit.rs, model);\n    if (fit.rm) return [model.m, model.om, model.im].some(function (c) { return roomCodeIn(fit.rm, c); });\n    if (fit.mc) return [model.m, model.om, model.im].some(function (c) { return roomCodeIs(fit.mc, c); });\n    // 形名の頭（三菱のルームエアコン「MSZ-FZV」「MLZ-RX」、マルチ用室内機の「ZXAS」）\n    if (fit.cc) return [model.m, model.im].some(function (c) { return String(c || '').indexOf(fit.cc) >= 0; });\n    // 形名の形（日立のルームエアコン「^RAS-XJ\\d{2}26」＝XJシリーズの2026年度）\n    if (fit.re) { var rx = new RegExp(fit.re); return [model.m, model.im].some(function (c) { return rx.test(String(c || '')); }); }\n    if (fit.im) return imInRange(fit.im, model.im);\n    if (fit.type && !looseSame(fit.type, model.i)) return false;\n    if (fit.series && !looseSame(fit.series, model.s)) return false;\n    if (fit.tp && !looseSame(fit.tp, model.tp)) return false;   // 同時ツイン用の分配管など\n    if (fit.cap) {\n      var c = capOfModel(model);\n      if (!c || c < fit.cap[0] || c > fit.cap[1]) return false;\n    }\n    return !!(fit.type || fit.series || fit.cap || fit.tp);\n  }\n\n  /* --------------------------------------------------------------------\n     別売品の仲間分け\n     1台の機種に700品目も出ると、現場では選べない。品名で仲間に分ける。\n     上から順に見て、最初に当たったものをその品目の仲間とする\n     （「化粧パネル用フィルター」はフィルターではなくパネルに入れたくないので、\n       フィルターを先に置いてある）\n     -------------------------------------------------------------------- */\n  var OPT_CATS = [\n    // パナソニックのルームエアコン：室外機の置台・金具・吹出グリル（「グリル」でパネルに入れない）と、\n    // 配管化粧カバー スッキリダクト（「エルボ」「ダクト」でダクト・吹出に入れない）\n    ['屋外・防雪', /^室外機用 /],\n    ['取付・工事部材', /^配管化粧カバー/],\n    ['ダクト・吹出', /^吹出グリル用チャンバー|^吸込グリル用チャンバー/],   // フリービルトインのチャンバー（「グリル」でパネルに入れない）\n    ['フィルター', /フィルタ|ろ材|集塵|エレメント/],\n    ['パネル・グリル', /パネル|グリル|化粧|ルーバー|吹出口閉鎖/],\n    ['リモコン・制御', /リモコン|コントロ|アダプタ|基板|センサ|集中|タイマ|通信|端子|遠方|ケーブル/],\n    ['ドレン', /ドレン/],\n    ['ダクト・吹出', /ダクト|吹出|チャンバ|フランジ|ガイド|パンカ|ノズル|ホース|エルボ/],\n    ['屋外・防雪', /防雪|架台|防振|フード|屋根|network|安全ネット|凍結|ヒータ|散水|背面/],\n    ['空気清浄・加湿', /ストリーマ|除菌|脱臭|加湿|空気清浄|UV|イオン|クリーン/],\n    ['分岐・分配', /分岐|分配|ディストリビュータ/],\n    ['取付・工事部材', /取付|キット|金具|バンド|カバー|スペーサ|延長|接続/]\n  ];\n\n  /* 画面に並べる順。仕事でよく使うものから出す。\n     （上の OPT_CATS は「どれに入れるか」を決める順で、こちらは「どう並べるか」） */\n  var OPT_CAT_ORDER = ['パネル・グリル', 'リモコン・制御', 'フィルター', 'ドレン', '分岐・分配',\n                       '空気清浄・加湿', 'ダクト・吹出', '屋外・防雪', '取付・工事部材', 'その他'];\n\n  function optCategory(name) {\n    var s = String(name || '');\n    for (var i = 0; i < OPT_CATS.length; i++) {\n      if (OPT_CATS[i][1].test(s)) return OPT_CATS[i][0];\n    }\n    return 'その他';\n  }\n\n  /** その機種に付く別売品を返す。model は［機器を選ぶ］で選んだ1行 */\n  function optionsFor(store, model) {\n    var out = [];\n    ((store && store.items) || []).forEach(function (o) {\n      var ok = false;\n      (o.fits || []).forEach(function (f) { if (!ok && optFits(f, model)) ok = true; });\n      if (ok) out.push(o);\n    });\n    return out;\n  }\n\n  /* --------------------------------------------------------------------\n     別売品のページが「どの機種タイプの別売品か」を決める\n\n     キヤリアのように、別売品のページに機種タイプが書いていない社がある\n     （章立てで決まっているので、紙面には書く必要がない）。\n     そこで**機種データのページ番号**を使う。機種データはどの価格ページから\n     読んだかを持っているので、別売品のページの手前にある価格ページの\n     機種タイプが、その別売品の相手になる。\n\n     2026-09-05、キヤリア p.67 の別売品（RBC-UW283PG など）が\n     機種データでは天井カセット形2方向の機種にしか使われていないことで確かめた。\n     （ページの端の文字から拾う方法は「ダクト」を誤って拾って失敗した）\n     -------------------------------------------------------------------- */\n  function typeAtPage(pageTypes, page) {\n    if (!pageTypes) return '';\n    var best = 0, type = '';\n    Object.keys(pageTypes).forEach(function (k) {\n      var n = Number(k);\n      if (n <= page && n > best) { best = n; type = pageTypes[k]; }\n    });\n    return type;\n  }\n\n  /** 「P40形〜P80形」「28型（1.0）〜160型（6.0）」から容量の範囲を取る */\n  function capRange(s) {\n    var t = String(s || '').replace(/（[^）]*）|\\([^)]*\\)/g, '').replace(/\\s/g, '');\n    var nums = (t.match(/\\d{2,3}/g) || []).map(Number).filter(function (n) { return CAPS.indexOf(n) >= 0; });\n    if (!nums.length) return null;\n    if (/[～~〜]/.test(t)) return [Math.min.apply(null, nums), Math.max.apply(null, nums)];\n    return [Math.min.apply(null, nums), Math.max.apply(null, nums)];\n  }\n\n  window.KUCHOO_CATALOG = { makers: MAKERS, run: run, yen: yen, optionsFor: optionsFor, optFits: optFits, typeAtPage: typeAtPage, optCategory: optCategory, catOrder: OPT_CAT_ORDER,\n                            _readPage: { ok: okReadPage, inaba: inabaReadPage, uc: ucReadPage } };\n})();\n"],["app.js","/* ==========================================================================\n   50Airtec 見積作成ツール（社内用）\n   - 単価マスタ・保存した見積は、この端末のブラウザ（localStorage）に保存されます\n   - 連動（sync.js）を設定した場合だけ、暗号にしてクラウドにも預けます\n   ========================================================================== */\n(function () {\n  'use strict';\n\n  /* ---------- 保存キー ---------- */\n  /* この画面がいつの版か。\n     ★ 手で直さないこと。version.txt を書き換えて `node build.mjs` を走らせれば、\n       ここも入口（index.html）も、build.mjs が機械的にそろえる。\n       人が何か所も手で合わせると、必ずどこかがずれる。 */\n  var APP_VERSION = '202609122100';\n\n  var KEY_PB    = 'airtec_pricebook_v1';\n  var KEY_EST   = 'airtec_estimates_v1';\n  var KEY_DRAFT = 'airtec_draft_v1';\n  var KEY_MDL   = 'airtec_models_v1';\n  var KEY_OPT   = 'airtec_options_v1';    // 別売品（どの室内機に付くかの情報つき）\n  var KEY_SITE  = 'airtec_sites_v1';\n  var KEY_INV   = 'airtec_invoices_v1';\n  var KEY_COST  = 'airtec_showcost_v1';   // 原価を画面に出すかどうか（端末ごと）\n\n  /* ---------- 便利関数 ---------- */\n  function $(sel, root) { return (root || document).querySelector(sel); }\n  function $$(sel, root) { return Array.prototype.slice.call((root || document).querySelectorAll(sel)); }\n  function el(tag, cls, text) {\n    var n = document.createElement(tag);\n    if (cls) n.className = cls;\n    if (text != null) n.textContent = text;\n    return n;\n  }\n  function num(v) { var n = parseFloat(v); return isFinite(n) ? n : 0; }\n  function yen(n) { return '¥' + Math.round(n).toLocaleString('ja-JP'); }\n\n  /**\n   * 製品ページのURLとして安全なものだけを通す。\n   * CSVは外から持ってくるファイルなので、http/https 以外（javascript: など）は捨てる。\n   */\n  function safeUrl(v) {\n    var s = String(v == null ? '' : v).trim();\n    if (!s) return '';\n    if (!/^https?:\\/\\//i.test(s)) return '';\n    return s;\n  }\n\n  /**\n   * 社判・ロゴの画像。見積書のHTMLに src= として差し込むので、\n   * 画像そのものの形（data:image の base64）以外は受け付けない。\n   * 外から持ってきたバックアップに細工した文字列が入っていても、ここで落とす。\n   * SVGは中に命令を書けてしまうので通さない。\n   */\n  function safeImage(v) {\n    var s = String(v == null ? '' : v).trim();\n    if (!s) return '';\n    return /^data:image\\/(png|jpe?g|gif|webp);base64,[A-Za-z0-9+/=]+$/i.test(s) ? s : '';\n  }\n\n  /* 見積書の見た目。中身の組み立ては1つで、変わるのはCSSだけ。\n     だから紙に出る金額・明細はどれを選んでも同じになる。 */\n  var SHEET_SKINS = [\n    { id: 'standard', name: '標準',\n      hint: '罫線はふつうの太さ。迷ったらこれ' },\n    { id: 'frame', name: 'きっちり枠',\n      hint: '枠と罫線を濃く、表題を囲む。白黒コピーでもはっきり出る' },\n    { id: 'plain', name: 'すっきり',\n      hint: '横線だけで縦の罫線なし。余白広め。読みやすい今風' },\n    { id: 'band', name: '色帯',\n      hint: '表題と見出しを色のベタに。カラーで渡すとき' },\n    { id: 'logo', name: 'ロゴ大きく左',\n      hint: 'ロゴを左上に大きく。便箋のような形。ロゴが無いときは会社名が大きく出る' }\n  ];\n  function skinId(v) {\n    for (var i = 0; i < SHEET_SKINS.length; i++) if (SHEET_SKINS[i].id === v) return v;\n    return 'standard';\n  }\n\n  /* 見積書の色。CSS変数 --sheet-accent に入れて、各見た目がそれを使う。\n     「標準」は色を使わない（白黒のままが正解なので）。\n     「きっちり枠」「すっきり」は線だけ、「色帯」「ロゴ大きく左」はしっかり出る。 */\n  var SHEET_COLORS = [\n    { id: 'charcoal', name: '炭',   hex: '#1A1D21', note: '名刺と同じ色' },\n    { id: 'navy',     name: '紺',   hex: '#1F3A63', note: '' },\n    { id: 'forest',   name: '深緑', hex: '#1E4D3B', note: '' },\n    { id: 'wine',     name: 'えんじ', hex: '#7A2230', note: '' },\n    { id: 'brown',    name: '茶',   hex: '#5A3E2B', note: '' }\n  ];\n  function colorHex(v) {\n    for (var i = 0; i < SHEET_COLORS.length; i++) if (SHEET_COLORS[i].id === v) return SHEET_COLORS[i].hex;\n    return SHEET_COLORS[0].hex;\n  }\n  function colorId(v) {\n    for (var i = 0; i < SHEET_COLORS.length; i++) if (SHEET_COLORS[i].id === v) return v;\n    return 'charcoal';\n  }\n\n  /** 取り込んだ自社情報を整える。足りない項目は初期値で埋め、画像は上の判定を通す */\n  function adoptCompany(src) {\n    var c = Object.assign({}, DEFAULT_PRICEBOOK.company, src || {});\n    c.sealImage = safeImage(c.sealImage);\n    c.logoImage = safeImage(c.logoImage);\n    c.sheetSkin = skinId(c.sheetSkin);\n    c.sheetColor = colorId(c.sheetColor);\n    return c;\n  }\n\n  /**\n   * 単価表の項目につける文字色。\n   * シリーズごとに色を変えて、単価表から選ぶときに見分けやすくするためのもの。\n   * CSVは外から持ってくるファイルなので、決めた色の名前と #rrggbb 以外は受け付けない。\n   */\n  var ITEM_COLORS = {\n    '青': '#1565c0', '緑': '#2e7d32', '橙': '#e65100', '赤': '#c62828',\n    '紫': '#6a1b9a', '茶': '#5d4037', '水': '#0277bd', '桃': '#ad1457',\n    '灰': '#546e7a', '黒': ''\n  };\n  /**\n   * 品名から色を見当てる規則。上から順に見て、最初に当たったものを使う。\n   * 手で打った項目にも効くよう、完全一致ではなく品名に含まれる言葉で判断する。\n   * 順番が大事：「室外機 天吊り設置」は室外機の据付（紫）であって天吊形（赤）ではない。\n   * 「室外機」で始まる行を先に拾っておかないと、あとの機種の形の規則に取られる。\n   */\n  var COLOR_RULES = [\n    [/^室外機/, '紫'],\n    [/金具|据付ブロック|防振|立ち下ろし|屋根置き|二段置き|高所|クレーン|ユニック|足場/, '紫'],\n    [/取外し|撤去|処分|リサイクル|収集運搬|フロン/, '灰'],\n    [/天吊|天井吊/, '赤'],\n    [/天カセ|天井カセット|カセット/, '青'],\n    [/床置/, '緑'],\n    [/壁掛/, '橙'],\n    [/ビルトイン|天井埋込|ダクト形/, '桃'],\n    [/冷媒配管|ドレン|配管|化粧カバー/, '水'],\n    [/電源|コンセント|電圧|ブレーカー/, '桃'],\n    [/穴|貫通|開口|点検口|下地|補修/, '茶'],\n    [/標準取付|入替|移設/, '緑']\n  ];\n\n  /** その品名なら何色か。当てはまらなければ空（色なし） */\n  function autoColorFor(name) {\n    var s = String(name || '');\n    for (var i = 0; i < COLOR_RULES.length; i++) {\n      if (COLOR_RULES[i][0].test(s)) return COLOR_RULES[i][1];\n    }\n    return '';\n  }\n\n  /**\n   * まだ色のついていない項目に、規則で色をつける。\n   * すでに色を決めてある項目には触らない。\n   */\n  function autoColorAll() {\n    var n = 0;\n    pb.categories.forEach(function (c) {\n      c.items.forEach(function (it) {\n        if (it.color) return;\n        var col = autoColorFor(it.name);\n        if (col) { it.color = col; n++; }\n      });\n    });\n    return n;\n  }\n\n  function itemColor(v) {\n    var s = String(v == null ? '' : v).trim();\n    if (!s) return '';\n    if (ITEM_COLORS.hasOwnProperty(s)) return ITEM_COLORS[s];\n    if (/^#[0-9a-f]{6}$/i.test(s)) return s;\n    return '';\n  }\n\n  /** 製品ページを新しいタブで開く小さなボタン。URLが無いときは null を返す */\n  function refButton(url, label) {\n    var u = safeUrl(url);\n    if (!u) return null;\n    var b = el('button', 'icon-btn icon-ref', '🔍');\n    b.type = 'button';\n    b.title = (label ? label + 'の' : '') + '製品ページを見る\\n' + u;\n    b.addEventListener('click', function (ev) {\n      ev.preventDefault();\n      ev.stopPropagation();\n      window.open(u, '_blank', 'noopener,noreferrer');\n    });\n    return b;\n  }\n  function clone(o) { return JSON.parse(JSON.stringify(o)); }\n  function esc(s) {\n    return String(s == null ? '' : s)\n      .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')\n      .replace(/\"/g, '&quot;');\n  }\n  function todayISO() {\n    var d = new Date(), p = function (n) { return ('0' + n).slice(-2); };\n    return d.getFullYear() + '-' + p(d.getMonth() + 1) + '-' + p(d.getDate());\n  }\n  function jpDate(iso) {\n    if (!iso) return '';\n    var p = iso.split('-');\n    if (p.length !== 3) return iso;\n    return p[0] + '年' + Number(p[1]) + '月' + Number(p[2]) + '日';\n  }\n  function addDays(iso, days) {\n    if (!iso) return '';\n    var d = new Date(iso + 'T00:00:00');\n    if (isNaN(d)) return '';\n    d.setDate(d.getDate() + Number(days || 0));\n    var p = function (n) { return ('0' + n).slice(-2); };\n    return d.getFullYear() + '-' + p(d.getMonth() + 1) + '-' + p(d.getDate());\n  }\n  function load(key, fallback) {\n    try {\n      var raw = localStorage.getItem(key);\n      return raw ? JSON.parse(raw) : fallback;\n    } catch (e) { return fallback; }\n  }\n  function save(key, val) {\n    try {\n      localStorage.setItem(key, JSON.stringify(val));\n      syncTouch(key);\n      return true;\n    }\n    catch (e) { toast('保存できませんでした（ブラウザの容量不足かもしれません）'); return false; }\n  }\n  /** 保存キーをまるごと消す（消したことも連動先に伝える） */\n  function removeKey(key) {\n    localStorage.removeItem(key);\n    syncTouch(key);\n  }\n  /** 連動（sync.js）に「ここが変わった」と知らせる。連動していなければ何も起きない */\n  function syncTouch(key) {\n    if (key === KEY_DRAFT) return;                 // 下書きは端末ごとのものなので送らない\n    if (window.AirtecSync) window.AirtecSync.changed(key);\n  }\n  var toastTimer;\n  function toast(msg) {\n    var t = $('#toast');\n    t.textContent = msg;\n    t.classList.add('is-show');\n    clearTimeout(toastTimer);\n    toastTimer = setTimeout(function () { t.classList.remove('is-show'); }, 2200);\n  }\n  function download(filename, text) {\n    var blob = new Blob([text], { type: 'application/json;charset=utf-8' });\n    var url = URL.createObjectURL(blob);\n    var a = document.createElement('a');\n    a.href = url; a.download = filename;\n    document.body.appendChild(a); a.click();\n    document.body.removeChild(a);\n    setTimeout(function () { URL.revokeObjectURL(url); }, 1000);\n  }\n\n  /* ======================================================================\n     単価マスタ\n     ====================================================================== */\n  var pb = load(KEY_PB, null) || clone(DEFAULT_PRICEBOOK);\n  // 古い保存データに新しい項目が無い場合の補完\n  pb.company  = adoptCompany(pb.company);\n  pb.defaults = Object.assign({}, DEFAULT_PRICEBOOK.defaults, pb.defaults || {});\n  if (!Array.isArray(pb.categories)) pb.categories = clone(DEFAULT_PRICEBOOK.categories);\n  ensureCostRates();\n  migratePB();\n\n  /** 掛率の表は、初期値の配列をそのまま使うと、直したときに初期値まで書き換わる。必ず自分のものにする */\n  function ensureCostRates() {\n    pb.defaults.costRates = Array.isArray(pb.defaults.costRates) ? pb.defaults.costRates.slice() : [];\n  }\n\n  function savePB() { return save(KEY_PB, pb); }\n\n  /**\n   * 前に使っていた単価マスタを、いまのツールの決まりごとに合わせる。\n   * 端末のブラウザに保存してある単価は消さず、足りない印と項目だけを足す。\n   */\n  function migratePB() {\n    migrateTo8();\n    migrateTo9();\n    migrateTo10();\n    migrateTo11();\n    migrateTo12();\n  }\n\n  /** 分類に、その品名＋規格の項目が無ければ足す。すでにある行の金額には触らない */\n  function addItemIfMissing(catId, item) {\n    var cat = null;\n    pb.categories.forEach(function (c) { if (c.id === catId) cat = c; });\n    if (!cat || !Array.isArray(cat.items)) return false;\n    var exists = false;\n    cat.items.forEach(function (it) {\n      if (it.name === item.name && (it.spec || '') === (item.spec || '')) exists = true;\n    });\n    if (exists) return false;\n    cat.items.push(clone(item));\n    return true;\n  }\n\n  /**\n   * 2026-09-02 以降に足した項目を、すでに使っている単価マスタにも届ける。\n   * 足りない行を入れるだけで、書き換えてある金額には一切触らない。\n   */\n  function migrateTo9() {\n    if (num(pb.version) >= 9) return;\n    var added = 0;\n    if (addItemIfMissing('biz', {\n      name: 'オートグリル 組み込み', spec: '',\n      unit: '台', price: 20000, color: '青'\n    })) added++;\n    pb.version = 9;\n    save(KEY_PB, pb);\n    if (added) console.log('単価マスタに ' + added + ' 項目を足しました');\n  }\n\n  /**\n   * 版9で入れた「オートグリル 組み込み」に規格を書いてしまっていたので、空に戻す。\n   * 天カセだけでなく天吊形にも付くため、形を決め打ちにしない。\n   * 自分で規格を書き足した行は、そのまま残す。\n   */\n  function migrateTo10() {\n    if (num(pb.version) >= 10) return;\n    pb.categories.forEach(function (c) {\n      (c.items || []).forEach(function (it) {\n        if (it.name === 'オートグリル 組み込み' && it.spec === '天カセ4方向 昇降パネル') it.spec = '';\n      });\n    });\n    pb.version = 10;\n    save(KEY_PB, pb);\n  }\n\n  /**\n   * 単価マスタの並びを、現場の段取りの順にそろえる（2026-09-02）。\n   * 初期値の並びを手本にして、同じ品名・規格の行をその順に置き直すだけ。\n   * 金額も規格も色も触らない。自分で足した項目は、順番を保ったまま分類の最後に残す。\n   */\n  /**\n   * 2026-09-06。2つのことをする。\n   *\n   * 1. 初期値のまま使っている行に「目安」の印をつける。\n   *    空調王が最初から入れている金額を、自社の相場だと思い込ませないため。\n   *    金額を直してある行にはつけない（＝もうその人の金額なので）。\n   *\n   * 2. 標準取付工事に入っていた 0.1人工 を外す。\n   *    0.1人工＝1人が48分。標準取付が48分で終わるはずがなく、入れ間違い。\n   *    金額はいまのまま残して、打てる状態にするだけにする。\n   *    勝手に金額を上げると、出したばかりの見積と食い違う。\n   */\n  function migrateTo12() {\n    if (num(pb.version) >= 12) return;\n\n    var defBy = {};\n    DEFAULT_PRICEBOOK.categories.forEach(function (dc) {\n      (dc.items || []).forEach(function (di) {\n        defBy[dc.id + '｜' + di.name + '｜' + (di.spec || '')] = di;\n      });\n    });\n\n    pb.categories.forEach(function (c) {\n      (c.items || []).forEach(function (it) {\n        var di = defBy[c.id + '｜' + it.name + '｜' + (it.spec || '')];\n\n        // 1. 金額が初期値のままなら「目安」\n        if (di && di.est && num(it.price) === num(di.price)) it.est = 1;\n\n        /* 2. 入れ間違いの人工を外す。金額はいまのまま残す。\n              ただし、その金額は人工の掛け算で出ただけで、\n              本人が決めた額ではない。だから「目安」の札は付ける */\n        if (it.name === '標準取付工事' && num(it.manDay) && num(it.manDay) < 0.25) {\n          delete it.manDay;\n          it.est = 1;\n        }\n      });\n    });\n\n    pb.version = 12;\n    savePB();\n  }\n\n  function migrateTo11() {\n    if (num(pb.version) >= 11) return;\n\n    DEFAULT_PRICEBOOK.categories.forEach(function (dc) {\n      var cat = null;\n      pb.categories.forEach(function (c) { if (c.id === dc.id) cat = c; });\n      if (!cat || !Array.isArray(cat.items)) return;\n\n      var key = function (it) { return it.name + '｜' + (it.spec || ''); };\n      var rest = cat.items.slice();      // まだ置いていない行\n      var sorted = [];\n\n      // 手本の順に、同じ品名・規格の行を拾っていく\n      (dc.items || []).forEach(function (di) {\n        for (var i = 0; i < rest.length; i++) {\n          if (key(rest[i]) === key(di)) { sorted.push(rest.splice(i, 1)[0]); return; }\n        }\n      });\n      // 手本に無い行（自分で足したもの）は、元の順のまま後ろに付ける\n      cat.items = sorted.concat(rest);\n    });\n\n    pb.version = 11;\n    save(KEY_PB, pb);\n  }\n\n  function migrateTo8() {\n    if (num(pb.version) >= 8) return;\n\n    DEFAULT_PRICEBOOK.categories.forEach(function (dc) {\n      pb.categories.forEach(function (c) {\n        if (c.id !== dc.id) return;\n        // どの分類を「作業費」として数えるかの印\n        if (dc.work && !c.work) c.work = true;\n        // 自動で単価が決まる項目（消耗品雑費・諸経費など）。\n        // 同じ品名がすでにあればその行を自動計算に変え、無ければ足す。\n        (dc.items || []).forEach(function (di) {\n          if (!di.autoPercent) return;\n          var found = null;\n          c.items.forEach(function (it) { if (it.name === di.name) found = it; });\n          if (found) {\n            if (!found.autoPercent) {\n              found.autoPercent = di.autoPercent;\n              found.autoBase = di.autoBase;\n              found.price = 0;\n            }\n          } else {\n            c.items.push(clone(di));\n          }\n        });\n\n        // 色分けの初期設定を配る。すでに色を付けてある行はそのまま残す。\n        (dc.items || []).forEach(function (di) {\n          if (!di.color) return;\n          c.items.forEach(function (it) {\n            if (it.color) return;\n            if (it.name === di.name && (it.spec || '') === (di.spec || '')) it.color = di.color;\n          });\n        });\n      });\n    });\n\n    // 手で足した項目にも色がつくよう、品名の規則でも配る\n    autoColorAll();\n\n    pb.version = 8;\n    save(KEY_PB, pb);\n  }\n\n  /* ======================================================================\n     見積データ（state）\n     ====================================================================== */\n  function newState() {\n    return {\n      id: 'e' + Date.now(),\n      no: nextNo(),\n      date: todayISO(),\n      customer: '',\n      honorific: '御中',\n      subject: '',\n      site: '',\n      validDays: pb.defaults.validDays,\n      delivery: pb.defaults.deliveryTerms,\n      payment: pb.defaults.paymentTerms,\n      overhead: pb.defaults.overheadPercent,\n      unitRound: pb.defaults.unitRoundYen,\n      manDayYen: pb.defaults.manDayYen,\n      discount: 0,\n      tax: pb.defaults.taxRatePercent,\n      rounding: 'floor',\n      note: pb.defaults.footerNote,\n      lines: []\n    };\n  }\n\n  function nextNo() {\n    var d = todayISO().replace(/-/g, '');\n    var list = load(KEY_EST, []);\n    var n = 1;\n    list.forEach(function (e) {\n      var m = String(e.no || '').match(new RegExp('^' + d + '-(\\\\d+)$'));\n      if (m) n = Math.max(n, Number(m[1]) + 1);\n    });\n    return d + '-' + ('0' + n).slice(-2);\n  }\n\n  var st = load(KEY_DRAFT, null) || newState();\n  if (st.unitRound == null) st.unitRound = 0;   // この設定より前に作った見積\n  if (st.manDayYen == null) st.manDayYen = num(pb.defaults.manDayYen);\n  applyManDayToMaster();   // 1人工の金額を変えたあとに開き直したときのため\n\n  /* ======================================================================\n     計算\n     ====================================================================== */\n  function calc() { return calcOf(st); }\n\n  /* ----------------------------------------------------------------------\n     単価が自動で決まる行（消耗品雑費・諸経費）の計算。\n\n     作業費＝家庭用・業務用・移設のように work: true をつけた分類から入れた行。\n     機器本体・材料（因幡などのCSV）・その他値引きは作業費に数えない。\n\n       作業費の合計         ×  5%  →  消耗品雑費\n       作業費＋消耗品雑費   × 15%  →  諸経費\n\n     という順番なので、'work' の行を先に計算してから 'work+auto' の行を計算する。\n     ---------------------------------------------------------------------- */\n\n  // 何の合計をもとにするか。画面に出す呼び名も一緒に持たせる\n  var AUTO_BASES = {\n    'work':      { name: '作業費', short: '作業費' },\n    'work+auto': { name: '作業費＋消耗品雑費', short: '作業費＋雑費' }\n  };\n\n  /** その項目（または行）が、何の合計をもとにするか */\n  function autoBaseOf(x) {\n    return (x && AUTO_BASES[x.autoBase]) ? x.autoBase : 'work';\n  }\n\n  /** 「作業費の5%」のような、割合の呼び名 */\n  function autoLabel(x) {\n    return AUTO_BASES[autoBaseOf(x)].name + 'の' + num(x.autoPercent) + '%';\n  }\n\n  /**\n   * 単価マスタから同じ品名の項目を探す。\n   * 明細の「仕様」は［品番　規格］をつなげたものなので、同じ形にして見比べる。\n   */\n  function findMasterItem(name, spec) {\n    var nm = String(name || '').trim();\n    if (!nm) return null;\n    var loose = null;\n    for (var i = 0; i < pb.categories.length; i++) {\n      var c = pb.categories[i];\n      for (var j = 0; j < c.items.length; j++) {\n        var it = c.items[j];\n        if (String(it.name || '').trim() !== nm) continue;\n        var full = [it.code || '', it.spec || ''].filter(Boolean).join('　');\n        if (full === String(spec || '').trim()) return { cat: c, item: it };\n        if (!loose) loose = { cat: c, item: it };   // 品名だけ合う候補は控えにしておく\n      }\n    }\n    return loose;\n  }\n\n  /**\n   * この機能をつける前に作った見積の行は、どの分類から入れたかを持っていない。\n   * そのままだと作業費が0円になってしまうので、単価マスタと品名を突き合わせて補う。\n   */\n  function backfillLineCats(doc) {\n    ((doc || {}).lines || []).forEach(function (l) {\n      if (l.cat || num(l.autoPercent)) return;\n      var hit = findMasterItem(l.name, l.spec);\n      if (!hit) return;\n      l.cat = hit.cat.id;\n      // 単価0の「消耗品雑費」「諸経費」は、自動計算の行に切り替える\n      if (num(l.price) === 0 && num(hit.item.autoPercent)) {\n        l.autoPercent = num(hit.item.autoPercent);\n        l.autoBase = hit.item.autoBase || 'work';\n      }\n    });\n  }\n\n  /**\n   * その行が「作業費」かどうか。\n   * ふだんは入れたときの分類で決まるが、行の「工」ボタンで手動で決めることもできる。\n   */\n  function isWorkLine(l) {\n    if (!l || num(l.autoPercent)) return false;   // 自動計算の行どうしは数えない\n    if (l.work === true) return true;             // 手で「数える」にした行\n    if (l.work === false) return false;           // 手で「数えない」にした行\n    var work = false;\n    pb.categories.forEach(function (c) { if (c.id === l.cat && c.work) work = true; });\n    return work;\n  }\n\n  /** 自動計算の行のもとになる合計（base ごと） */\n  function autoBaseTotal(doc, base) {\n    var lines = ((doc || {}).lines || []);\n    var sum = 0;\n    lines.forEach(function (l) {\n      if (isWorkLine(l)) sum += lineAmount(l, (doc || {}).unitRound);\n    });\n    if (base === 'work+auto') {\n      // 諸経費は、先に決まった消耗品雑費も含めた金額に掛ける\n      lines.forEach(function (l) {\n        if (num(l.autoPercent) && autoBaseOf(l) === 'work') sum += lineAmount(l, (doc || {}).unitRound);\n      });\n    }\n    return sum;\n  }\n\n  // 自動計算の行の画面表示を書き換える関数（renderLines が行ごとに入れる）\n  var autoRowUpdaters = [];\n\n  /** 自動計算の行の単価を計算し直して、画面にも反映する */\n  function refreshAutoLines() {\n    backfillLineCats(st);\n    ['work', 'work+auto'].forEach(function (base) {\n      var total = autoBaseTotal(st, base);\n      st.lines.forEach(function (l) {\n        if (!num(l.autoPercent) || autoBaseOf(l) !== base) return;\n        l.price = ceilYen(total * num(l.autoPercent) / 100, st.unitRound);\n        l.base  = l.price;   // 掛率は使わない行なので、元値も同じにしておく\n        l.rate  = 100;\n      });\n    });\n    autoRowUpdaters.forEach(function (fn) { fn(); });\n  }\n\n  /**\n   * 単価の端数を繰り上げる。step が 10 なら10円未満を、100 なら100円未満を切り上げる。\n   * 6,205円 → 10円繰り上げで 6,210円 ／ 100円繰り上げで 6,300円。\n   */\n  function ceilYen(v, step) {\n    var n = num(v);\n    var st10 = num(step);\n    if (st10 <= 1) return Math.round(n);\n    return Math.ceil(n / st10) * st10;\n  }\n\n  /**\n   * 人工（にんく）から単価を出す。\n   * 公共工事の考え方で、作業ごとに「この工事は0.5人工」と決めておき、\n   * 1人工の金額（配管工でおおむね3万〜4万）を掛けて単価にする。\n   * 1人工の金額は変わっても作業人工は変わらないので、掛け算だけで済む。\n   */\n  function manDayPrice(md) {\n    return Math.round(num(md) * num(st.manDayYen));\n  }\n\n  /**\n   * 見積を切り替えたときに呼ぶ。見積ごとに1人工の金額が違うので、\n   * そろえ直さないと選ぶ画面が前の見積の金額を出したままになる。\n   */\n  function syncManDayToQuote() {\n    if (applyManDayToMaster()) renderPicker();\n  }\n\n  /** 単価マスタの人工つきの項目の単価を、いまの1人工の金額で出し直す */\n  function applyManDayToMaster() {\n    var changed = false;\n    pb.categories.forEach(function (c) {\n      c.items.forEach(function (it) {\n        if (!num(it.manDay)) return;\n        var v = manDayPrice(it.manDay);\n        if (num(it.price) !== v) { it.price = v; changed = true; }\n      });\n    });\n    if (changed) savePBQuiet();\n    return changed;\n  }\n\n  /* ----------------------------------------------------------------------\n     原価と粗利（社内用）。見積書には一切出さない。\n\n     1単位あたりの原価の決め方は、上から順に見て最初に当たったもの。\n       1. 項目・行に原価が入っていれば、その額\n       2. 人工の作業なら　人工 × 原価の1人工\n       3. 材料の仕入掛率が決めてあれば　定価 × 仕入掛率\n       4. どれも無ければ「原価未入力」。粗利は多めに見えるので、その旨を画面に出す\n     ---------------------------------------------------------------------- */\n\n  /** 原価を画面に出すかどうか（端末ごとの見た目の設定。見積には保存しない） */\n  var showCost = load(KEY_COST, false) === true;\n\n  /**\n   * その品名・仕様に当たる仕入掛率（%）を返す。当たらなければ 0。\n   * メーカーだけの指定より、シリーズまで書いてある指定のほうを優先する。\n   * （例：「日立 23%」と「日立の寒さ知らず 24%」があれば、寒冷地モデルには24%）\n   */\n  function costRateFor(text) {\n    var list = pb.defaults.costRates || [];\n    var best = 0, bestScore = -1;\n    for (var i = 0; i < list.length; i++) {\n      var maker  = String(list[i].maker  || '').trim();\n      var series = String(list[i].series || '').trim();\n      if (!maker && !series) continue;\n      if (maker  && text.indexOf(maker)  < 0) continue;\n      if (series && text.indexOf(series) < 0) continue;\n      var score = (maker ? 1 : 0) + (series ? 2 : 0);\n      if (score > bestScore) { bestScore = score; best = num(list[i].percent); }\n    }\n    return best;\n  }\n\n  /* ======================================================================\n     仕入先の見積を読ませる\n     ----------------------------------------------------------------------\n     商社（西方商店など）の見積には［定価］と［仕切＝原価］が並んでいる。\n     この2つがあれば掛率が出る。掛率が分かれば、見積に載っていない\n     何千行の原価まで出せる。1本の見積が、単価マスタ全体を照らす。\n     ====================================================================== */\n  var COST_MAKERS = ['ダイキン', '三菱電機', '三菱', '日立', 'パナソニック', '東芝',\n                     '日本キヤリア', 'キヤリア', '因幡電工', '因幡', 'オーケー器材',\n                     'ユーシー産業', '日晴金属', 'キヤッチャー'];\n\n  /** 品名・規格・カテゴリ名から、どのメーカーの品かを見当てる */\n  function makerOf(text) {\n    var t = String(text || '');\n    for (var i = 0; i < COST_MAKERS.length; i++) {\n      if (t.indexOf(COST_MAKERS[i]) >= 0) return COST_MAKERS[i];\n    }\n    return '';\n  }\n\n  /**\n   * 読み取った行から掛率をまとめる。\n   * 同じメーカーの中で掛率がそろっていれば「日立は23%」と言えるが、\n   * ばらついていたら言えない（シリーズごとに違うため）。\n   * だから黙って平均を出さず、そろっているものだけを提案する。\n   */\n  function summarizeRates(pairs) {\n    var byMaker = {};\n    pairs.forEach(function (p) {\n      if (!(p.price > 0 && p.cost > 0)) return;\n      var mk = makerOf(p.text) || '（メーカー不明）';\n      (byMaker[mk] = byMaker[mk] || []).push(Math.round(p.cost / p.price * 1000) / 10);\n    });\n    var out = [];\n    Object.keys(byMaker).forEach(function (mk) {\n      var pcts = byMaker[mk];\n      var lo = Math.min.apply(null, pcts), hi = Math.max.apply(null, pcts);\n      out.push({\n        maker: mk,\n        count: pcts.length,\n        steady: (hi - lo) <= 1,                 // 1ポイント以内なら「そろっている」\n        pct: Math.round(((lo + hi) / 2) * 10) / 10,\n        kinds: pcts.filter(function (v, i, a) { return a.indexOf(v) === i; }).sort(function (a, b) { return a - b; })\n      });\n    });\n    return out.sort(function (a, b) { return b.count - a.count; });\n  }\n\n  /** 単価マスタの項目から、1単位あたりの原価を見積もる */\n  function itemCost(item, cat) {\n    if (!item) return 0;\n    if (num(item.cost) > 0) return num(item.cost);\n    if (num(item.manDay) > 0) return Math.round(num(item.manDay) * num(pb.defaults.manDayCostYen));\n    /* 掛率は品名と規格で当てていたが、部材はカテゴリにしかメーカー名が無い\n       （品名は「ペアコイル」だけで、「因幡電工」はカテゴリ名のほう）。\n       カテゴリ名も見ないと、メーカーごとの掛率が当たらない */\n    var pct = costRateFor((item.name || '') + ' ' + (item.spec || '') +\n                          ' ' + ((cat && cat.name) || '')) ||\n              num(pb.defaults.materialCostPercent);\n    if (pct > 0 && (!cat || !cat.work)) return Math.round(num(item.price) * pct / 100);\n    return 0;\n  }\n\n  /**\n   * その行が入っているカテゴリの名前。\n   * 行が覚えているのは id（c1787811020342324 のような文字）で、\n   * メーカー名は名前のほう（「因幡電工｜配管化粧カバー」）にしかない。\n   */\n  function lineCatName(l) {\n    var id = l && l.cat;\n    if (!id) return '';\n    var nm = '';\n    pb.categories.forEach(function (c) { if (c.id === id) nm = c.name || ''; });\n    return nm;\n  }\n\n  /**\n   * 明細1行の原価（1単位あたり）を、いまの設定から出し直す。\n   * 単価マスタに実額の原価（仕入見積から取り込んだ値など）が入っていれば、\n   * それが最優先。ここを見落とすと、仕入掛率の推定値で本物の仕入値を潰してしまう。\n   */\n  function lineCostFromSettings(l) {\n    if (num(l.autoPercent) > 0) return 0;      // 消耗品雑費・諸経費は原価を持たない\n\n    var hit = findMasterItem(l.name, l.spec);\n    if (hit && num(hit.item.cost) > 0) return num(hit.item.cost);\n\n    if (num(l.manDay) > 0) return Math.round(num(l.manDay) * num(pb.defaults.manDayCostYen));\n\n    /* 仕入掛率は材料の話。「工」ボタンで作業費に数えている行には当てない。\n       カテゴリ名も見る。部材は品名が「ペアコイル」だけで、\n       「因幡電工」はカテゴリ名にしか無いため（2026-09-06、３８８の見積で気づいた） */\n    var pct = costRateFor((l.name || '') + ' ' + (l.spec || '') +\n                          ' ' + lineCatName(l)) ||\n              num(pb.defaults.materialCostPercent);\n    if (pct > 0 && !isWorkLine(l)) return Math.round(num(l.base) * pct / 100);\n    return 0;\n  }\n\n  /** その見積の原価と粗利 */\n  function profitOf(doc) {\n    var t = calcOf(doc);\n    var cost = 0, blank = 0;\n    (doc.lines || []).forEach(function (l) {\n      var c = num(l.cost);\n      cost += num(l.qty) * c;\n      // 金額があるのに原価が入っていない行は、粗利を多く見せてしまう。\n      // 消耗品雑費と諸経費はもともと原価を持たない行なので、数に入れない。\n      if (c <= 0 && !num(l.autoPercent) && num(l.qty) * num(l.price) > 0) blank++;\n    });\n    var sales = t.taxable;                       // 税抜の受取額（諸経費・値引き込み）\n    var gross = sales - cost;\n    return {\n      cost: cost,\n      gross: gross,\n      rate: sales > 0 ? (gross / sales) * 100 : 0,\n      blank: blank\n    };\n  }\n\n  /**\n   * 機種1台ぶんの掛率（定価の何%で出すか）を、原価と「乗せる利益」の設定から出す。\n   *     定価 × 仕入掛率 ＝ 原価　→　原価 ÷ modelSellDivisor ＝ 見積に出す金額\n   * 会社によって乗せる利益が違うので、割る数は［自社情報］で決める。\n   * 仕入掛率が未設定で原価が出せないときは 100%＝定価のまま（勝手に安くしない）。\n   */\n  function modelRateFor(l) {\n    var list = num(l.listPrice), div = num(pb.defaults.modelSellDivisor);\n    var cost = num(l.cost) || lineCostFromSettings(l);\n    if (!list || !div || cost <= 0) return 100;\n    return (cost / div) / list * 100;\n  }\n\n  /**\n   * 機種の行の単価を、いまの設定で出し直す。\n   * 掛率や単価を手で触った行（rateFixed）は動かさない。\n   */\n  function applyModelPrices() {\n    st.lines.forEach(function (l) {\n      if (!num(l.listPrice) || l.rateFixed) return;\n      l.rate = modelRateFor(l);\n      l.price = priceFromBase(l);\n    });\n  }\n\n  /** 「原価 ÷ ○○」の下に、それが粗利率いくつになるかを言葉で出す */\n  function showModelDivNote() {\n    var note = $('#c-model-div-note');\n    if (!note) return;\n    var d = num($('#c-model-div').value);\n    if (d > 0 && d < 1) {\n      note.textContent = '粗利率 ' + Math.round((1 - d) * 1000) / 10 + '%。' +\n        '例：原価10万円 → 見積に出す金額 ' + yen(100000 / d) + '。' +\n        '仕入掛率を入れていない機種は、定価のまま出ます。';\n    } else if (d >= 1) {\n      note.textContent = '1以上だと利益が乗りません。0.65 なら粗利率35%です。';\n    } else {\n      note.textContent = '空か0のときは、機種は定価のまま出ます。';\n    }\n  }\n\n  /** 掛率の表示。半端な数字になるので、小数第1位まで見せる */\n  function fmtRate(r) { return (Math.round(num(r) * 10) / 10) + '%'; }\n\n  /** 元値（定価）と掛率から、その行の単価を出す。端数の繰り上げもここでかける */\n  function priceFromBase(l) {\n    return ceilYen(num(l.base) * num(l.rate) / 100, st.unitRound);\n  }\n\n  /** 1人工の金額や端数の設定を変えたときに、すべての行の単価を計算し直す */\n  function applyLinePrices() {\n    st.lines.forEach(function (l) {\n      if (num(l.autoPercent)) return;   // 自動計算の行は refreshAutoLines がやる\n      if (l.base == null) l.base = num(l.price);\n      if (l.rate == null) l.rate = 100;\n      if (num(l.manDay)) l.base = manDayPrice(l.manDay);   // 人工の行は掛け算し直す\n      l.price = priceFromBase(l);\n    });\n  }\n\n  /**\n   * まだ原価の入っていない行だけ、いまの設定から埋める。\n   *\n   * 原価は行を足した瞬間にしか出していなかったので、\n   * あとから仕入掛率を入れても、すでに保存してある見積は「未入力」のままだった。\n   * （2026-09-06、３８８の見積で分かった。掛率を入れても粗利が出てこない）\n   *\n   * 入っている原価には触らない。仕入見積から取り込んだ実額や、\n   * 手で打った額を、あとからの推定値で潰さないため。\n   */\n  function fillMissingLineCosts() {\n    var filled = 0;\n    (st.lines || []).forEach(function (l) {\n      if (num(l.cost) > 0) return;\n      var c = lineCostFromSettings(l);\n      if (c > 0) { l.cost = c; filled++; }\n    });\n    return filled;\n  }\n\n  /** 原価の設定を変えたときに、手で入れていない行の原価を出し直す */\n  function applyLineCosts() {\n    st.lines.forEach(function (l) {\n      if (l.costFixed) return;            // 手で打った原価は動かさない\n      l.cost = lineCostFromSettings(l);\n    });\n  }\n\n  /**\n   * その行の金額。ふつうは「数量 × 単価」だが、数量に小数がある行\n   * （フロン破壊処理費 3.2kg など）は、それでも端数が残ってしまう。\n   * そこで金額にも同じ繰り上げをかける。数量が整数の行では何も変わらない。\n   */\n  function lineAmount(l, step) {\n    return ceilYen(num(l.qty) * num(l.price), step);\n  }\n\n  /** 見積でも請求書でも使えるように、対象の書類を受け取って計算する */\n  function calcOf(st) {\n    var subtotal = 0;\n    (st.lines || []).forEach(function (l) { subtotal += lineAmount(l, st.unitRound); });\n\n    var overhead = subtotal * num(st.overhead) / 100;\n    var discount = num(st.discount);\n    var taxable  = subtotal + overhead - discount;\n    var taxRaw   = taxable * num(st.tax) / 100;\n\n    var fn = st.rounding === 'ceil' ? Math.ceil : (st.rounding === 'round' ? Math.round : Math.floor);\n    overhead = fn(overhead);\n    var tax  = fn(taxRaw);\n    taxable  = subtotal + overhead - discount;\n\n    return {\n      subtotal: subtotal,\n      overhead: overhead,\n      discount: discount,\n      taxable: taxable,\n      tax: tax,\n      total: taxable + tax\n    };\n  }\n\n  /* ======================================================================\n     画面切り替え\n     ====================================================================== */\n  $$('.tab').forEach(function (btn) {\n    btn.addEventListener('click', function () {\n      $$('.tab').forEach(function (b) { b.classList.remove('is-active'); });\n      $$('.view').forEach(function (v) { v.classList.remove('is-active'); });\n      btn.classList.add('is-active');\n      $('#view-' + btn.dataset.view).classList.add('is-active');\n      if (btn.dataset.view === 'list')     renderList();\n      if (btn.dataset.view === 'master')   { renderCsvTargets(); renderMaster(); }\n      if (btn.dataset.view === 'settings') fillCompany();\n      window.scrollTo(0, 0);\n    });\n  });\n\n  /* ======================================================================\n     基本情報フォーム\n     ====================================================================== */\n  var metaMap = {\n    '#m-no': 'no', '#m-date': 'date', '#m-customer': 'customer', '#m-honorific': 'honorific',\n    '#m-subject': 'subject', '#m-site': 'site', '#m-valid': 'validDays',\n    '#m-delivery': 'delivery', '#m-payment': 'payment', '#m-overhead': 'overhead',\n    '#m-discount': 'discount', '#m-tax': 'tax', '#m-rounding': 'rounding', '#m-note': 'note',\n    '#m-unit-round': 'unitRound', '#m-manday': 'manDayYen'\n  };\n  var numericFields = { validDays: 1, overhead: 1, discount: 1, tax: 1, unitRound: 1, manDayYen: 1 };\n\n  function fillMeta() {\n    Object.keys(metaMap).forEach(function (sel) {\n      var node = $(sel);\n      if (node) node.value = st[metaMap[sel]];\n    });\n  }\n\n  Object.keys(metaMap).forEach(function (sel) {\n    var node = $(sel);\n    if (!node) return;\n    node.addEventListener('input', function () {\n      var key = metaMap[sel];\n      st[key] = numericFields[key] ? num(node.value) : node.value;\n      if (key === 'unitRound' || key === 'manDayYen') {\n        if (key === 'manDayYen') { applyManDayToMaster(); renderPicker(); }\n        applyLinePrices();\n        renderLines();\n      }\n      else if (key === 'overhead' || key === 'discount' || key === 'tax' || key === 'rounding') renderTotals();\n      persistDraft();\n    });\n    node.addEventListener('change', function () {\n      var key = metaMap[sel];\n      st[key] = numericFields[key] ? num(node.value) : node.value;\n      if (key === 'unitRound' || key === 'manDayYen') {\n        if (key === 'manDayYen') { applyManDayToMaster(); renderPicker(); }\n        applyLinePrices();\n        renderLines();\n      }\n      else renderTotals();\n      persistDraft();\n    });\n  });\n\n  var draftTimer;\n  function persistDraft() {\n    clearTimeout(draftTimer);\n    draftTimer = setTimeout(function () { save(KEY_DRAFT, st); }, 400);\n  }\n\n  /* ======================================================================\n     単価ピッカー\n     ====================================================================== */\n  var activeCat = pb.categories.length ? pb.categories[0].id : null;\n\n  var FREQ = '__freq__';       // 「よく使う」は本物のカテゴリではなく、使用回数から作る仮のまとまり\n  var FREQ_MAX = 40;\n\n  function frequentItems() {\n    var out = [];\n    pb.categories.forEach(function (c) {\n      c.items.forEach(function (it) { if (it.used) out.push({ cat: c, item: it }); });\n    });\n    out.sort(function (a, b) { return (b.item.used || 0) - (a.item.used || 0); });\n    return out.slice(0, FREQ_MAX);\n  }\n\n  function renderPicker() {\n    var cats = $('#picker-cats');\n    cats.innerHTML = '';\n\n    var freq = frequentItems();\n    if (freq.length) {\n      var fb = el('button', 'cat-btn cat-freq' + (activeCat === FREQ ? ' is-active' : ''), '★ よく使う');\n      fb.type = 'button';\n      fb.addEventListener('click', function () {\n        activeCat = FREQ;\n        $('#picker-search').value = '';\n        renderPicker();\n      });\n      cats.appendChild(fb);\n    } else if (activeCat === FREQ) {\n      activeCat = pb.categories.length ? pb.categories[0].id : null;\n    }\n\n    pb.categories.forEach(function (c) {\n      var b = el('button', 'cat-btn' + (c.id === activeCat ? ' is-active' : ''), c.name);\n      b.type = 'button';\n      b.addEventListener('click', function () {\n        activeCat = c.id;\n        $('#picker-search').value = '';\n        renderPicker();\n      });\n      cats.appendChild(b);\n    });\n    renderPickerItems();\n  }\n\n  function renderPickerItems() {\n    var box = $('#picker-items');\n    var q = $('#picker-search').value.trim().toLowerCase();\n    box.innerHTML = '';\n\n    var results = [];\n    if (!q && activeCat === FREQ) {\n      results = frequentItems();\n    } else {\n      pb.categories.forEach(function (c) {\n        if (!q && c.id !== activeCat) return;\n        c.items.forEach(function (it) {\n          // 品番でも探せるようにする（例：「LD-70」と打てば出る）\n          var hay = ((it.code || '') + ' ' + it.name + ' ' + (it.spec || '') + ' ' + c.name).toLowerCase();\n          if (!q || hay.indexOf(q) >= 0) results.push({ cat: c, item: it });\n        });\n      });\n    }\n\n    if (!results.length) {\n      box.appendChild(el('p', 'picker-empty', '該当する項目がありません。'));\n      return;\n    }\n\n    // 検索時は件数が多くなりうるので上限をつける\n    var LIMIT = 300;\n    var shown = results.slice(0, LIMIT);\n\n    shown.forEach(function (r) {\n      var b = el('button', 'item-btn');\n      b.type = 'button';\n      var col = itemColor(r.item.color);\n      if (col) b.style.color = col;      // シリーズごとの色分け\n      if (r.item.code) b.appendChild(el('i', 'item-code', r.item.code));\n      b.appendChild(el('b', null, r.item.name));\n      if (r.item.spec) b.appendChild(el('em', null, r.item.spec));\n      b.appendChild(el('span', null, num(r.item.autoPercent)\n        ? autoLabel(r.item)\n        : (num(r.item.manDay) ? r.item.manDay + '人工　' : '') + yen(r.item.price) + ' / ' + r.item.unit));\n      b.addEventListener('click', function () {\n        // 自動計算の項目（消耗品雑費・諸経費）は、2回入れると二重に乗ってしまう。\n        // 家庭用と業務用の両方に消耗品雑費を置いてあるので、うっかり両方押せてしまう\n        if (num(r.item.autoPercent)) {\n          var already = false;\n          st.lines.forEach(function (l) {\n            if (num(l.autoPercent) && l.name === r.item.name) already = true;\n          });\n          if (already) { toast('「' + r.item.name + '」はもう明細に入っています'); return; }\n        }\n        addLine({\n          name: r.item.name,\n          // 仕様欄はそのまま見積書に印刷される。単価マスタの規格には\n          // 「20m巻（1巻 ¥124,100）」のような仕入れの都合が入っているので、\n          // 品番のある材料は品番だけにする。\n          // 品番の無い工事（「3馬力」など）は規格がそのまま意味を持つので残す。\n          spec: r.item.code ? String(r.item.code).trim() : (r.item.spec || ''),\n          qty: 1,\n          unit: r.item.unit,\n          price: r.item.price,\n          // どの分類から入れたか。作業費の合計（消耗品雑費の計算のもと）に使う\n          cat: r.cat.id,\n          // 単価が自動で決まる項目は、割合と「何の合計をもとにするか」も持たせる\n          autoPercent: num(r.item.autoPercent) || 0,\n          autoBase: r.item.autoBase || '',\n          // 人工つきの作業は、1人工の金額を変えたときに計算し直せるよう覚えておく\n          manDay: num(r.item.manDay) || 0,\n          // 原価（社内用）。見積書には出ない\n          cost: itemCost(r.item, r.cat),\n          // 見積を作りながら「これどんな材料だっけ」を確かめられるよう、製品ページも持たせる\n          url: r.item.url || ''\n        });\n        // 選んだ回数を覚えておき、「★よく使う」に出す。\n        // ここで画面を作り直すとボタンの位置が動いて押しにくいので、保存だけする。\n        r.item.used = (r.item.used || 0) + 1;\n        savePBQuiet();\n        toast('「' + r.item.name + '」を追加しました');\n      });\n\n      // 選ぶ前に「これどんな材料だっけ」を確かめられるように、\n      // ボタンの右上に製品ページへのリンクを重ねておく。\n      // button の中に button は置けないので、外側の箱で包む。\n      var cell = el('div', 'item-cell');\n      cell.appendChild(b);\n      var pref = refButton(r.item.url, r.item.name);\n      if (pref) { pref.classList.add('item-ref'); b.classList.add('has-ref'); cell.appendChild(pref); }\n      box.appendChild(cell);\n    });\n\n    if (results.length > LIMIT) {\n      box.appendChild(el('p', 'picker-empty',\n        results.length + '件見つかりました。' + LIMIT + '件まで表示しています。品番や品名でもう少し絞り込んでください。'));\n    }\n  }\n\n  $('#picker-search').addEventListener('input', renderPickerItems);\n\n  /* ======================================================================\n     明細\n     ====================================================================== */\n  function addLine(line) {\n    var l = Object.assign({ name: '', spec: '', qty: 1, unit: '式', price: 0, url: '', cat: '', autoPercent: 0, autoBase: '', manDay: 0, cost: 0, listPrice: 0 }, line || {});\n    // base は掛率をかける前の元値（単価マスタの定価）。rate は「定価の何%で出すか」\n    if (l.base == null) l.base = num(l.price);\n    if (l.rate == null) l.rate = 100;\n    if (num(l.manDay)) l.base = manDayPrice(l.manDay);\n    l.price = priceFromBase(l);   // 端数の設定にしたがって繰り上げる\n    if (!num(l.cost)) l.cost = lineCostFromSettings(l);\n    st.lines.push(l);\n    renderLines();\n    persistDraft();\n  }\n\n  /* 明細行の「掛率」プルダウンの選択肢（定価の何%で出すか） */\n  var RATES = [100, 95, 90, 85, 80, 75, 70, 65, 60, 55, 50];\n\n  /* ------------------------------------------------------------------\n     明細行を「つかんで動かす」並び替え。\n     マウスでも指でも同じように動くよう pointer イベントで扱う。\n     動かしている間は見た目（DOM）だけ入れ替え、指を離したときに\n     はじめてデータの順番を入れ替えて、行を作り直す。\n     ------------------------------------------------------------------ */\n  var drag = null;\n\n  function startDrag(ev, tr, from) {\n    if (ev.button != null && ev.button !== 0) return;   // 左ボタンと指だけ\n    ev.preventDefault();\n    var handle = ev.currentTarget, tb = $('#lines-body');\n    drag = { tb: tb, tr: tr, from: from, handle: handle, pid: ev.pointerId };\n    tr.classList.add('is-dragging');\n    tb.classList.add('is-reordering');\n    // 監視は書類全体に付ける。\n    // つまみは動かす行の中にあるので、行を入れ替えた瞬間につまみも\n    // いったん外れてしまい、つまみに付けた監視では「指を離した」を\n    // 取り逃す。書類全体で見ていればそれが起きない。\n    document.addEventListener('pointermove', onDragMove);\n    document.addEventListener('pointerup', endDrag);\n    document.addEventListener('pointercancel', endDrag);\n    window.addEventListener('blur', endDrag);\n  }\n\n  function onDragMove(ev) {\n    if (!drag) return;\n    ev.preventDefault();\n    var rows = Array.prototype.slice.call(drag.tb.children);\n    var cur = rows.indexOf(drag.tr);\n    if (cur < 0 || !rows.length) return;\n\n    var y = ev.clientY, target = null;\n    for (var k = 0; k < rows.length; k++) {\n      var r = rows[k].getBoundingClientRect();\n      if (y >= r.top && y <= r.bottom) { target = k; break; }\n    }\n    if (target === null) {\n      // 表からはみ出したら、いちばん上／いちばん下に寄せる\n      if (y < rows[0].getBoundingClientRect().top) target = 0;\n      else if (y > rows[rows.length - 1].getBoundingClientRect().bottom) target = rows.length - 1;\n      else return;\n    }\n    if (target === cur) return;\n\n    var ref = rows[target];\n    if (target < cur) drag.tb.insertBefore(drag.tr, ref);\n    else drag.tb.insertBefore(drag.tr, ref.nextSibling);\n  }\n\n  function endDrag() {\n    if (!drag) return;\n    var d = drag; drag = null;\n\n    document.removeEventListener('pointermove', onDragMove);\n    document.removeEventListener('pointerup', endDrag);\n    document.removeEventListener('pointercancel', endDrag);\n    window.removeEventListener('blur', endDrag);\n    d.tr.classList.remove('is-dragging');\n    d.tb.classList.remove('is-reordering');\n\n    var to = Array.prototype.indexOf.call(d.tb.children, d.tr);\n    if (to >= 0 && to !== d.from) {\n      st.lines.splice(to, 0, st.lines.splice(d.from, 1)[0]);\n      persistDraft();\n    }\n    // 各行が覚えている「自分は何行目か」がずれるので、必ず作り直す\n    renderLines();\n  }\n\n  function renderLines() {\n    // 古い見積の行に分類の印を補う。行を組み立てる前にやらないと、\n    // 自動計算の行なのに「ふつうの行」として描いてしまう\n    backfillLineCats(st);\n\n    var tb = $('#lines-body');\n    tb.innerHTML = '';\n    autoRowUpdaters = [];\n    $('#lines-empty').style.display = st.lines.length ? 'none' : 'block';\n    $('#lines-table').style.display = st.lines.length ? 'table' : 'none';\n\n    st.lines.forEach(function (l, i) {\n      var tr = el('tr');\n      // 消耗品雑費のように「作業費の◯%」で単価が決まる行\n      var isAuto = !!num(l.autoPercent);\n      if (isAuto) tr.classList.add('line-auto');\n\n      // 並び替え：つまみをつかんで動かす。矢印でも1つずつ動かせる\n      var tdMove = el('td', 'c-move');\n      var mvWrap = el('div', 'move-wrap');\n\n      var grip = el('div', 'row-grip', '⠿');\n      grip.title = 'つかんで上下に動かすと、行の順番を入れ替えられます';\n      grip.tabIndex = 0;\n      grip.setAttribute('role', 'button');\n      grip.setAttribute('aria-label', (i + 1) + '行目をつかんで並び替える');\n      grip.addEventListener('pointerdown', function (ev) { startDrag(ev, tr, i); });\n      grip.addEventListener('keydown', function (ev) {\n        // マウスが使いにくいときのために、上下キーでも動かせるようにしておく\n        if (ev.key === 'ArrowUp') { ev.preventDefault(); moveLine(i, -1, true); }\n        else if (ev.key === 'ArrowDown') { ev.preventDefault(); moveLine(i, 1, true); }\n      });\n      mvWrap.appendChild(grip);\n\n      var mv = el('div', 'move-btns');\n      var up = el('button', 'icon-btn', '▲'); up.type = 'button'; up.title = '上へ';\n      var dn = el('button', 'icon-btn', '▼'); dn.type = 'button'; dn.title = '下へ';\n      up.addEventListener('click', function () { moveLine(i, -1); });\n      dn.addEventListener('click', function () { moveLine(i, 1); });\n      mv.appendChild(up); mv.appendChild(dn);\n      mvWrap.appendChild(mv);\n      tdMove.appendChild(mvWrap);\n      tr.appendChild(tdMove);\n\n      // 品名・仕様\n      var tdName = el('td', 'c-name');\n      var wrap = el('div', 'line-name');\n      var iName = el('input'); iName.type = 'text'; iName.value = l.name; iName.placeholder = '品名';\n      var iSpec = el('input', 'spec'); iSpec.type = 'text'; iSpec.value = l.spec || ''; iSpec.placeholder = '仕様・型番など（任意）';\n      iName.addEventListener('input', function () { l.name = iName.value; persistDraft(); });\n      iSpec.addEventListener('input', function () { l.spec = iSpec.value; persistDraft(); });\n      wrap.appendChild(iName); wrap.appendChild(iSpec);\n      tdName.appendChild(wrap);\n      // 見積書に出る「定価 → 売値」を、画面でも見えるようにしておく\n      var listHint = el('div', 'line-list-hint');\n      tdName.appendChild(listHint);\n      tr.appendChild(tdName);\n\n      // 数量\n      var tdQty = el('td', 'c-qty');\n      var iQty = el('input'); iQty.type = 'number'; iQty.step = '0.1'; iQty.value = l.qty;\n      tdQty.appendChild(iQty);\n      tr.appendChild(tdQty);\n\n      // 単位\n      var tdUnit = el('td', 'c-unit');\n      var iUnit = el('input'); iUnit.type = 'text'; iUnit.value = l.unit;\n      iUnit.addEventListener('input', function () { l.unit = iUnit.value; persistDraft(); });\n      tdUnit.appendChild(iUnit);\n      tr.appendChild(tdUnit);\n\n      // 掛率（定価の何%で出すか）\n      // 古い見積を開いたときは base / rate が無いので、ここで今の単価を元値とみなす\n      if (l.base == null) l.base = num(l.price);\n      if (l.rate == null) l.rate = 100;\n\n      var tdRate = el('td', 'c-rate');\n      var sRate = el('select', 'rate-sel');\n      RATES.forEach(function (r) {\n        var o = el('option', null, r + '%');\n        o.value = r;\n        if (num(l.rate) === r) o.selected = true;\n        sRate.appendChild(o);\n      });\n      // 選択肢に無い掛率（手入力の結果など）も残せるようにしておく\n      if (RATES.indexOf(num(l.rate)) < 0) {\n        var oX = el('option', null, fmtRate(l.rate));\n        oX.value = l.rate; oX.selected = true;\n        sRate.insertBefore(oX, sRate.firstChild);\n      }\n      if (isAuto) {\n        sRate.disabled = true;\n        sRate.title = 'この行の単価は自動で決まるので、掛率は使いません';\n      }\n      tdRate.appendChild(sRate);\n      tr.appendChild(tdRate);\n\n      // 単価\n      var tdPrice = el('td', 'c-price');\n      var iPrice = el('input'); iPrice.type = 'number'; iPrice.step = '1'; iPrice.value = l.price;\n      if (isAuto) iPrice.readOnly = true;\n      tdPrice.appendChild(iPrice);\n      /* この行の単価が人工から出ていることを、画面に出す。\n         打ち替えは前からできたが、なぜその金額なのかが見えなかった */\n      var mdNote = el('small', 'line-mdnote');\n      function showMdNote() {\n        mdNote.textContent = num(l.manDay)\n          ? num(l.manDay) + '人工 × ' + yen(num(st.manDayYen))\n          : '';\n      }\n      showMdNote();\n      tdPrice.appendChild(mdNote);\n      tr.appendChild(tdPrice);\n\n      function showBaseHint() {\n        // 掛率が100%でないときだけ、元の定価が分かるようにしておく\n        iPrice.title = num(l.rate) === 100 ? '' : '定価 ' + yen(num(l.base)) + ' の ' + l.rate + '%';\n        tdRate.classList.toggle('is-off', num(l.rate) !== 100);\n        var lp = num(l.listPrice);\n        if (lp && num(l.price) && lp > num(l.price)) {\n          listHint.textContent = '見積書に出ます　定価 ' + yen(lp) + ' → ' + yen(num(l.price));\n          listHint.classList.remove('is-warn');\n        } else if (lp && num(l.cost) <= 0) {\n          // 原価が出せないと利益の乗せようがない。黙って定価のままにせず、理由を出す\n          listHint.textContent = '仕入掛率が未設定なので、定価のまま出ます（［自社情報］で設定できます）';\n          listHint.classList.add('is-warn');\n        } else {\n          listHint.textContent = '';\n          listHint.classList.remove('is-warn');\n        }\n      }\n      showBaseHint();\n\n      sRate.addEventListener('change', function () {\n        l.rate = num(sRate.value);\n        l.rateFixed = true;          // 手で選んだ掛率は、設定を変えても動かさない\n        l.price = priceFromBase(l);\n        iPrice.value = l.price;\n        showBaseHint();\n        recalc();\n      });\n\n      // 金額\n      var tdAmt = el('td', 'c-amount', yen(lineAmount(l, st.unitRound)));\n      tr.appendChild(tdAmt);\n\n      // 自動計算の行は、ほかの行をいじるたびに単価と金額を書き直す\n      if (isAuto) {\n        autoRowUpdaters.push(function () {\n          var bs = autoBaseOf(l);\n          iPrice.value = l.price;\n          iPrice.title = AUTO_BASES[bs].name + 'の合計 ' + yen(autoBaseTotal(st, bs)) +\n                         ' の ' + num(l.autoPercent) + '%';\n          tdAmt.textContent = yen(lineAmount(l, st.unitRound));\n          showMargin();\n        });\n      }\n\n      function recalc() {\n        l.qty = num(iQty.value);\n        if (!isAuto) l.price = num(iPrice.value);\n        tdAmt.textContent = yen(lineAmount(l, st.unitRound));\n        showMargin();\n        refreshAutoLines();\n        renderTotals();\n        persistDraft();\n      }\n      iQty.addEventListener('input', recalc);\n      iPrice.addEventListener('input', function () {\n        if (isAuto) return;   // 作業費から自動で決まる行は書き換えさせない\n        // 単価を手で書き換えたら、その金額が新しい元値。掛率は100%に戻す\n        l.base = num(iPrice.value);\n        l.rate = 100;\n        l.rateFixed = true;          // 手で入れた単価は、設定を変えても動かさない\n        sRate.value = '100';\n        /* この見積のこの行だけ、人工から外す。単価マスタには触らない。\n           次に作る見積では、また人工から出た金額で入ってくる */\n        if (num(l.manDay)) { l.manDay = 0; showMdNote(); }\n        showBaseHint();\n        recalc();\n      });\n\n      // 打っている途中で数字が飛ぶと打ちにくいので、\n      // 端数の繰り上げは入力を終えたとき（欄から離れたとき）にかける\n      iPrice.addEventListener('change', function () {\n        if (isAuto) return;\n        var rounded = priceFromBase(l);\n        if (rounded === num(iPrice.value)) return;\n        iPrice.value = rounded;\n        recalc();\n      });\n\n      // 原価と粗利率（社内用）。見積書には出ない\n      var tdCost = el('td', 'c-cost');\n      var tdMargin = el('td', 'c-margin');\n      /* 欄に入れるのは**その行ぶんの原価**（数量を掛けた額）。\n         となりの「金額」も数量を掛けた額なので、これで左右がそろう。\n         9m×¥1,425 の行なら、金額 ¥27,000 に対して原価 ¥12,825 と出る。\n\n         中では1つあたりで持っている（単価マスタも人工も1つあたりのため）。\n         入れられた額は数量で割って l.cost に入れる\n         （2026-09-09、BIGBOSSの「欄に数量かけた金額」） */\n      var iCost = el('input'); iCost.type = 'number'; iCost.step = '1';\n      iCost.placeholder = '—';\n      iCost.title = 'この行ぶんの原価（数量を掛けた額）。空のままだと粗利を多めに見せてしまいます';\n      tdCost.appendChild(iCost);\n      var costEach = el('div', 'cost-sum');\n      tdCost.appendChild(costEach);\n      tr.appendChild(tdCost);\n      tr.appendChild(tdMargin);\n\n      function showMargin() {\n        var amt = lineAmount(l, st.unitRound);\n        var cst = num(l.qty) * num(l.cost);\n        // 打っている最中の欄は書き換えない\n        if (document.activeElement !== iCost) iCost.value = num(l.cost) ? Math.round(cst) : '';\n        costEach.textContent = (num(l.cost) && num(l.qty) !== 1)\n          ? '1' + (l.unit || '個') + 'あたり ' + yen(Math.round(num(l.cost))) : '';\n        if (!amt || !num(l.cost)) {\n          tdMargin.textContent = num(l.cost) ? '—' : '未入力';\n          tdMargin.classList.remove('is-thin');\n          return;\n        }\n        var r = ((amt - cst) / amt) * 100;\n        tdMargin.textContent = r.toFixed(1) + '%';\n        tdMargin.classList.toggle('is-thin', r < 15);\n      }\n      showMargin();\n\n      iCost.addEventListener('input', function () {\n        // 入れられたのは行ぶんの額。1つあたりに戻して持つ\n        var q = num(l.qty) || 1;\n        l.cost = num(iCost.value) / q;\n        l.costFixed = num(iCost.value) > 0;   // 手で入れた原価は設定変更で上書きしない\n        showMargin();\n        renderTotals();\n        persistDraft();\n      });\n\n      // 製品ページ ＋ 単価表に登録 ＋ 削除\n      var tdDel = el('td', 'c-del');\n\n      var ref = refButton(l.url, l.name);\n      if (ref) tdDel.appendChild(ref);\n\n      if (!isAuto) {\n        // この行を作業費（消耗品雑費・諸経費のもと）に数えるかどうか\n        var wk = el('button', 'icon-btn icon-work', '工'); wk.type = 'button';\n        if (isWorkLine(l)) wk.classList.add('is-on');\n        wk.title = isWorkLine(l)\n          ? 'この行は作業費に数えています（押すと数えなくなります）'\n          : 'この行は作業費に数えていません（押すと数えます）';\n        wk.addEventListener('click', function () {\n          l.work = !isWorkLine(l);\n          renderLines();\n          persistDraft();\n        });\n        tdDel.appendChild(wk);\n\n        var reg = el('button', 'icon-btn icon-reg', '＋表'); reg.type = 'button';\n        reg.title = 'この行を単価表に登録して、次から選べるようにする';\n        reg.addEventListener('click', function () {\n          registerLineToMaster(l);\n        });\n        tdDel.appendChild(reg);\n      }\n\n      var del = el('button', 'icon-btn', '✕'); del.type = 'button'; del.title = 'この行を削除';\n      del.addEventListener('click', function () {\n        st.lines.splice(i, 1);\n        renderLines();\n        persistDraft();\n      });\n      tdDel.appendChild(del);\n      tr.appendChild(tdDel);\n\n      showMargin();\n      tb.appendChild(tr);\n    });\n\n    applyCostVisibility();\n    refreshAutoLines();\n    renderTotals();\n  }\n\n  /**\n   * 見積の1行を、そのまま単価表に登録する。\n   * 現場で「この材料、まだ単価表に入れてなかった」となったときに、\n   * 画面を行き来せず登録できるようにするためのもの。\n   */\n  function registerLineToMaster(line) {\n    if (!(line.name || '').trim()) { toast('品名を入れてから登録してください'); return; }\n\n    var cat = null;\n    pb.categories.forEach(function (c) { if (c.id === activeCat) cat = c; });\n    if (!cat) cat = pb.categories[0];\n    if (!cat) { toast('先に単価マスタでカテゴリを作ってください'); return; }\n\n    // 「LD-70　ダクト70」のように品番と規格をまとめてある場合は、先頭を品番として切り出す\n    var spec = (line.spec || '').trim();\n    var code = '';\n    var m = spec.match(/^([0-9A-Za-z][0-9A-Za-z\\-_/.]{1,23})(?:[　\\s]+(.*))?$/);\n    if (m) { code = m[1]; spec = (m[2] || '').trim(); }\n\n    var dup = null;\n    cat.items.forEach(function (it) {\n      if (it.name === line.name && (it.code || '') === code && (it.spec || '') === spec) dup = it;\n    });\n\n    if (dup) {\n      if (dup.price === num(line.price)) { toast('「' + cat.name + '」にすでに同じ内容で登録されています'); return; }\n      if (!confirm('「' + cat.name + '」に同じ項目があります。単価を ' +\n        yen(dup.price) + ' → ' + yen(num(line.price)) + ' に更新しますか？')) return;\n      dup.price = num(line.price);\n      dup.unit = line.unit || dup.unit;\n      if (num(line.cost)) dup.cost = num(line.cost);\n    } else {\n      if (!confirm('この内容で「' + cat.name + '」に登録します。よろしいですか？\\n\\n' +\n        '　品番：' + (code || '（なし）') + '\\n' +\n        '　品名：' + line.name + '\\n' +\n        '　規格：' + (spec || '（なし）') + '\\n' +\n        '　単位：' + (line.unit || '個') + '\\n' +\n        '　単価：' + yen(num(line.price)))) return;\n      var reg = {\n        code: code, name: line.name, spec: spec,\n        unit: line.unit || '個', price: num(line.price),\n        url: safeUrl(line.url)\n      };\n      // 行が持っている原価・人工・色も一緒に持っていく。\n      // ここで落とすと、次に選んだとき原価が空のまま出てくる。\n      if (num(line.cost)) reg.cost = num(line.cost);\n      if (num(line.manDay)) reg.manDay = num(line.manDay);\n      if (line.color) reg.color = line.color;\n      cat.items.push(reg);\n    }\n\n    if (savePB() === false) return;\n    renderPicker();\n    toast('「' + cat.name + '」に登録しました');\n  }\n\n  /** 仕様欄が「品番　つづき」の形になっているか */\n  var CODE_HEAD_RE = /^([0-9A-Za-z][0-9A-Za-z\\-_\\/.]{1,23})[　\\s]+\\S/;\n\n  /**\n   * 明細の1行の「仕様」を、品番だけに詰める。\n   * 「PC-3520-10H　20m巻（1巻 ¥124,100）」→「PC-3520-10H」\n   * 品番で始まっていない行（工事の「3馬力」など）は触らない。\n   */\n  function stripSpecToCode(l) {\n    var spec = String(l.spec || '').trim();\n    if (!spec) return false;\n\n    // まず単価マスタで品番を調べる。見つからないときは先頭のかたまりを品番とみなす\n    var hit = findMasterItem(l.name, l.spec);\n    var code = (hit && hit.item.code) ? String(hit.item.code).trim() : '';\n    if (!code) {\n      var m = spec.match(CODE_HEAD_RE);\n      code = m ? m[1] : '';\n    }\n    if (!code || spec === code) return false;\n    if (spec.indexOf(code) !== 0) return false;   // 品番で始まっていない行は触らない\n\n    l.spec = code;\n    return true;\n  }\n\n  /** 詰めたらどうなるかを、実際には書き換えずに調べる */\n  function strippedSpec(l) {\n    var probe = { name: l.name, spec: l.spec };\n    return stripSpecToCode(probe) ? probe.spec : null;\n  }\n\n  function moveLine(i, dir, keepFocus) {\n    var j = i + dir;\n    if (j < 0 || j >= st.lines.length) return;\n    var tmp = st.lines[i];\n    st.lines[i] = st.lines[j];\n    st.lines[j] = tmp;\n    renderLines();\n    persistDraft();\n    // 上下キーで動かしたときは、動いた行のつまみに焦点を戻して続けて押せるようにする\n    if (keepFocus) {\n      var row = $('#lines-body').children[j];\n      var g = row && row.querySelector('.row-grip');\n      if (g) g.focus();\n    }\n  }\n\n  /**\n   * 原価の列を出す／隠す。\n   * body にクラスを付けてCSSに任せる。そうしないと、あとから作られる\n   * 単価マスタの行や、足したばかりの明細の行に効かない。\n   */\n  function applyCostVisibility() {\n    document.body.classList.toggle('show-cost', showCost);\n    // 見積作成と単価マスタ、両方のチェックをそろえる\n    ['#chk-cost', '#chk-cost-m'].forEach(function (sel) {\n      var chk = $(sel);\n      if (chk) chk.checked = showCost;\n    });\n  }\n\n  ['#chk-cost', '#chk-cost-m'].forEach(function (sel) {\n    var chk = $(sel);\n    if (!chk) return;\n    chk.addEventListener('change', function () {\n      showCost = chk.checked;\n      save(KEY_COST, showCost);\n      applyCostVisibility();\n      renderTotals();\n    });\n  });\n\n  function renderTotals() {\n    var t = calc();\n    var box = $('#totals');\n    var rows = [];\n\n    // 消耗品雑費・諸経費を使っているときは、そのもとになる作業費も見えるようにしておく。\n    // これは画面だけの表示で、見積書には印刷されない。\n    var hasAuto = false;\n    st.lines.forEach(function (l) { if (num(l.autoPercent)) hasAuto = true; });\n    if (hasAuto) rows.push(['作業費（画面のみ）', yen(autoBaseTotal(st, 'work'))]);\n\n    rows.push(['小計', yen(t.subtotal)]);\n    if (t.overhead) rows.push(['諸経費（' + st.overhead + '%）', yen(t.overhead)]);\n    if (t.discount) rows.push(['値引き', '-' + yen(t.discount)]);\n    rows.push(['課税対象額', yen(t.taxable)]);\n    rows.push(['消費税（' + st.tax + '%）', yen(t.tax)]);\n\n    var html = '<dl>';\n    rows.forEach(function (r) { html += '<dt>' + esc(r[0]) + '</dt><dd>' + esc(r[1]) + '</dd>'; });\n    html += '<dt class=\"row-total\">御見積金額（税込）</dt><dd class=\"row-total\">' + esc(yen(t.total)) + '</dd>';\n\n    // 原価と粗利（社内用）。画面だけで、見積書には印刷されない\n    if (showCost) {\n      var pf = profitOf(st);\n      html += '<dt class=\"row-cost\">原価（画面のみ）</dt><dd class=\"row-cost\">' + esc(yen(pf.cost)) + '</dd>';\n      html += '<dt class=\"row-cost\">粗利（税抜）</dt><dd class=\"row-cost\">' + esc(yen(pf.gross)) + '</dd>';\n      html += '<dt class=\"row-cost\">粗利率</dt><dd class=\"row-cost\">' + esc(pf.rate.toFixed(1) + '%') + '</dd>';\n      if (pf.blank) {\n        html += '<dt class=\"row-cost\">原価未入力</dt><dd class=\"row-cost\">' + pf.blank +\n                '行（粗利は多めに出ています）</dd>';\n      }\n    }\n    html += '</dl>';\n    box.innerHTML = html;\n  }\n\n  $('#btn-strip-spec').addEventListener('click', function () {\n    var targets = st.lines.filter(function (l) { return strippedSpec(l) !== null; });\n    if (!targets.length) { toast('品番だけに詰められる行はありませんでした'); return; }\n\n    var sample = targets.slice(0, 5).map(function (l) {\n      return '　' + l.spec + '\\n　　→ ' + strippedSpec(l);\n    }).join('\\n');\n\n    if (!confirm(targets.length + '行の「仕様」を品番だけにします。よろしいですか？\\n' +\n      '（見積書に梱包数や仕入れ値が出ないようにするためのものです）\\n\\n' + sample +\n      (targets.length > 5 ? '\\n　…ほか ' + (targets.length - 5) + '行' : ''))) return;\n\n    var n = 0;\n    st.lines.forEach(function (l) { if (stripSpecToCode(l)) n++; });\n    renderLines();\n    persistDraft();\n    toast(n + '行の仕様を品番だけにしました');\n  });\n\n  $('#btn-add-blank').addEventListener('click', function () { addLine(); });\n  $('#btn-clear-lines').addEventListener('click', function () {\n    if (!st.lines.length) return;\n    if (!confirm('明細をすべて消します。よろしいですか？')) return;\n    st.lines = [];\n    renderLines();\n    persistDraft();\n  });\n\n  /* ======================================================================\n     新規 / 保存\n     ====================================================================== */\n  $('#btn-new').addEventListener('click', function () {\n    if (!confirm('新しい見積を作ります。今の内容は（保存していなければ）消えます。よろしいですか？')) return;\n    st = newState();\n    syncManDayToQuote();\n    fillMeta();\n    renderLines();\n    save(KEY_DRAFT, st);\n    toast('新規作成しました');\n  });\n\n  $('#btn-save').addEventListener('click', function () {\n    var list = load(KEY_EST, []);\n    var t = calc();\n\n    // どの現場のものか決まっていなければ、件名と宛名から現場を作る\n    if (!st.siteId) {\n      var nm = (st.subject || '').trim() || (st.site || '').trim() || (st.customer || '').trim();\n      if (!nm) { toast('先に件名かお客様名を入れてください（現場の名前になります）'); return; }\n      var sites = loadSites();\n      var found = null;\n      sites.forEach(function (x) {\n        if (x.name === nm && (x.customer || '') === (st.customer || '').trim()) found = x;\n      });\n      if (!found) {\n        found = {\n          id: 's' + Date.now() + Math.floor(Math.random() * 1000),\n          name: nm,\n          customer: (st.customer || '').trim(),\n          honorific: st.honorific || '御中',\n          address: st.site || '',\n          tel: '',\n          memo: '',\n          createdAt: new Date().toISOString()\n        };\n        sites.push(found);\n        if (saveSites(sites) === false) return;\n      }\n      st.siteId = found.id;\n    }\n\n    var rec = clone(st);\n    rec.total = t.total;\n    rec.savedAt = new Date().toISOString();\n    var idx = -1;\n    for (var i = 0; i < list.length; i++) { if (list[i].id === rec.id) { idx = i; break; } }\n    if (idx >= 0) list[idx] = rec; else list.unshift(rec);\n    save(KEY_EST, list);\n    save(KEY_DRAFT, st);\n    toast('保存しました（' + rec.no + '）');\n  });\n\n  /* ======================================================================\n     保存済み一覧\n     ====================================================================== */\n  /* ======================================================================\n     現場（案件）\n     ----------------------------------------------------------------------\n     1つの現場に、見積・請求書・現調シートがぶら下がる構造。\n     見積は siteId で現場に結びつく。\n     ====================================================================== */\n  var openSiteId = null;      // いま開いている現場（null なら一覧を表示）\n\n  function loadSites() { return load(KEY_SITE, []); }\n  function saveSites(v) { return save(KEY_SITE, v); }\n  function findSite(id) {\n    var hit = null;\n    loadSites().forEach(function (s) { if (s.id === id) hit = s; });\n    return hit;\n  }\n  function estimatesOf(siteId) {\n    return load(KEY_EST, []).filter(function (e) { return e.siteId === siteId; });\n  }\n\n  /**\n   * 現場という考え方が無かった頃に保存した見積を、現場にふり分ける。\n   * 宛名＋件名が同じものは1つの現場にまとめる。中身は書き換えず siteId を足すだけ。\n   */\n  function migrateEstimatesToSites() {\n    var list = load(KEY_EST, []);\n    var orphans = list.filter(function (e) { return !e.siteId; });\n    if (!orphans.length) return 0;\n\n    var sites = loadSites();\n    var byKey = {};\n    sites.forEach(function (s) { byKey[(s.customer || '') + ' :: ' + (s.name || '')] = s; });\n\n    orphans.forEach(function (e) {\n      var name = (e.subject || '').trim() || (e.site || '').trim() || '（件名なし）';\n      var cust = (e.customer || '').trim();\n      var key = cust + ' :: ' + name;\n      var s = byKey[key];\n      if (!s) {\n        s = {\n          id: 's' + Date.now() + Math.floor(Math.random() * 1000),\n          name: name,\n          customer: cust,\n          honorific: e.honorific || '御中',\n          address: e.site || '',\n          tel: '',\n          memo: '',\n          createdAt: e.savedAt || new Date().toISOString()\n        };\n        sites.push(s);\n        byKey[key] = s;\n      }\n      e.siteId = s.id;\n    });\n    saveSites(sites);\n    save(KEY_EST, list);\n    return orphans.length;\n  }\n\n  function siteDialog(site) {\n    var isNew = !site;\n    var s = site || { name: '', customer: '', honorific: '御中', address: '', tel: '', memo: '' };\n    var name = prompt(isNew ? '現場名（件名）を入れてください\\n例：〇〇商店 事務所エアコン更新' : '現場名（件名）', s.name);\n    if (name === null) return null;\n    name = name.trim();\n    if (!name) { toast('現場名を入れてください'); return null; }\n    var cust = prompt('お客様名', s.customer);\n    if (cust === null) return null;\n    var addr = prompt('工事場所（住所）', s.address);\n    if (addr === null) return null;\n    var tel = prompt('連絡先（任意）', s.tel);\n    if (tel === null) return null;\n    s.name = name; s.customer = cust.trim(); s.address = addr.trim(); s.tel = tel.trim();\n    return s;\n  }\n\n  $('#btn-new-site').addEventListener('click', function () {\n    var s = siteDialog(null);\n    if (!s) return;\n    s.id = 's' + Date.now() + Math.floor(Math.random() * 1000);\n    s.createdAt = new Date().toISOString();\n    var sites = loadSites();\n    sites.push(s);\n    if (saveSites(sites) === false) return;\n    openSiteId = s.id;\n    renderList();\n    toast('現場を作りました');\n  });\n\n  $('#btn-site-back').addEventListener('click', function () { openSiteId = null; renderList(); });\n\n  $('#btn-site-edit').addEventListener('click', function () {\n    var sites = loadSites();\n    var s = null;\n    sites.forEach(function (x) { if (x.id === openSiteId) s = x; });\n    if (!s) return;\n    if (!siteDialog(s)) return;\n    saveSites(sites);\n    renderList();\n    toast('現場情報を更新しました');\n  });\n\n  $('#btn-site-del').addEventListener('click', function () {\n    var s = findSite(openSiteId);\n    if (!s) return;\n    var n = estimatesOf(s.id).length;\n    // 写真は別の入れ物にあるので、数えるのも消すのもあとから（待たせないため先に数だけ聞く）\n    photosOf(s.id).then(function (photos) { askDeleteSite(s, n, photos || []); },\n                        function () { askDeleteSite(s, n, []); });\n  });\n\n  function askDeleteSite(s, n, photos) {\n    if (!confirm('現場「' + s.name + '」を削除します。\\n' +\n      (n ? 'この現場の見積 ' + n + '件も一緒に消えます。\\n' : '') +\n      (photos.length ? 'この現場の写真 ' + photos.length + '枚も一緒に消えます。\\n' : '') +\n      'よろしいですか？')) return;\n    saveSites(loadSites().filter(function (x) { return x.id !== s.id; }));\n    save(KEY_EST, load(KEY_EST, []).filter(function (e) { return e.siteId !== s.id; }));\n    // 現場が消えたあとに写真だけ残ると、二度と出せないゴミになる\n    photos.forEach(function (p) { photoDel(p.id); });\n    if (photos.length) photosChanged();          // もう一方の端末からも消す\n    openSiteId = null;\n    renderList();\n    toast('削除しました');\n  }\n\n  $('#site-search').addEventListener('input', function () { renderSiteList(); });\n\n  function renderList() {\n    migrateEstimatesToSites();\n    freePhotoUrls();                 // 前に出していた写真の一時アドレスを返す\n    var showDetail = !!(openSiteId && findSite(openSiteId));\n    $('#site-list-card').style.display = showDetail ? 'none' : '';\n    $('#site-detail-card').style.display = showDetail ? '' : 'none';\n    if (showDetail) renderSiteDetail(); else renderSiteList();\n  }\n\n  function renderSiteList() {\n    var box = $('#site-list');\n    box.innerHTML = '';\n    var q = ($('#site-search').value || '').trim().toLowerCase();\n    var sites = loadSites();\n    var ests = load(KEY_EST, []);\n\n    if (!sites.length) {\n      box.appendChild(el('p', 'empty-note',\n        'まだ現場がありません。「＋ 現場を追加」で作るか、見積を保存すると自動で作られます。'));\n      return;\n    }\n    var shown = sites.filter(function (s) {\n      if (!q) return true;\n      return ((s.name || '') + ' ' + (s.customer || '') + ' ' + (s.address || '')).toLowerCase().indexOf(q) >= 0;\n    });\n    if (!shown.length) { box.appendChild(el('p', 'empty-note', '見つかりませんでした。')); return; }\n\n    // 更新が新しい現場を上に\n    var lastOf = {};\n    ests.forEach(function (e) {\n      if (!e.siteId) return;\n      if (!lastOf[e.siteId] || String(e.savedAt) > lastOf[e.siteId]) lastOf[e.siteId] = String(e.savedAt);\n    });\n    shown.sort(function (a, b) {\n      return String(lastOf[b.id] || b.createdAt || '').localeCompare(String(lastOf[a.id] || a.createdAt || ''));\n    });\n\n    shown.forEach(function (s) {\n      var mine = ests.filter(function (e) { return e.siteId === s.id; });\n      var sum = mine.reduce(function (a, e) { return a + num(e.total); }, 0);\n      var row = el('button', 'site-row'); row.type = 'button';\n      var main = el('div', 'est-main');\n      main.appendChild(el('b', null, s.name));\n      main.appendChild(el('small', null,\n        (s.customer || '（お客様名なし）') + (s.address ? '　/　' + s.address : '')));\n      row.appendChild(main);\n      var right = el('div', 'site-right');\n      right.appendChild(el('div', 'est-amount', mine.length ? yen(sum) : '—'));\n      right.appendChild(el('small', null, '見積 ' + mine.length + '件'));\n      row.appendChild(right);\n      row.addEventListener('click', function () { openSiteId = s.id; renderList(); });\n      box.appendChild(row);\n    });\n  }\n\n  function renderSiteDetail() {\n    var s = findSite(openSiteId);\n    var box = $('#site-detail');\n    box.innerHTML = '';\n\n    var head = el('div', 'site-head');\n    head.appendChild(el('h2', 'card-title', s.name));\n    var sub = [s.customer, s.address, s.tel].filter(Boolean).join('　/　');\n    if (sub) head.appendChild(el('p', 'hint', sub));\n    box.appendChild(head);\n\n    var acts = el('div', 'card-actions site-acts');\n    var mk = el('button', 'btn btn-primary', '＋ この現場で見積を作る'); mk.type = 'button';\n    mk.addEventListener('click', function () { newEstimateForSite(s); });\n    acts.appendChild(mk);\n    box.appendChild(acts);\n\n    box.appendChild(renderSurveyBlock(s));\n    box.appendChild(renderPhotoBlock(s));\n\n    var mine = estimatesOf(s.id).sort(function (a, b) {\n      return String(b.savedAt).localeCompare(String(a.savedAt));\n    });\n    box.appendChild(el('div', 'site-sec-label', '見積（' + mine.length + '件）'));\n    if (!mine.length) {\n      box.appendChild(el('p', 'empty-note', 'まだ見積がありません。'));\n      return;\n    }\n    mine.forEach(function (e) {\n      var row = el('div', 'est-row');\n      var main = el('div', 'est-main');\n      main.appendChild(el('b', null, e.no + '　' + (e.subject || s.name)));\n      main.appendChild(el('small', null, jpDate(e.date) + '　/　' + (e.customer || '')));\n      row.appendChild(main);\n      row.appendChild(el('div', 'est-amount', yen(e.total || 0)));\n\n      var open = el('button', 'btn btn-ghost', '開く'); open.type = 'button';\n      open.addEventListener('click', function () { openEstimate(e); });\n\n      var dup = el('button', 'btn btn-ghost', '複製'); dup.type = 'button';\n      dup.addEventListener('click', function () {\n        var c = clone(e);\n        delete c.total; delete c.savedAt;\n        c.id = 'e' + Date.now();\n        c.no = nextNo();\n        c.date = todayISO();\n        openEstimate(c, '複製しました');\n      });\n\n      var del = el('button', 'btn btn-ghost btn-danger', '削除'); del.type = 'button';\n      del.addEventListener('click', function () {\n        if (!confirm('この見積を削除します。よろしいですか？\\n' + e.no + '　' + (e.customer || ''))) return;\n        save(KEY_EST, load(KEY_EST, []).filter(function (x) { return x.id !== e.id; }));\n        renderList();\n        toast('削除しました');\n      });\n\n      var bill = el('button', 'btn btn-ghost', '請求書'); bill.type = 'button';\n      bill.title = 'この見積の金額で請求書を作ります';\n      bill.addEventListener('click', function () { makeInvoice(e, s); });\n\n      row.appendChild(open); row.appendChild(dup); row.appendChild(bill); row.appendChild(del);\n      box.appendChild(row);\n    });\n\n    box.appendChild(renderInvoiceBlock(s));\n  }\n\n  /* ======================================================================\n     請求書\n     ----------------------------------------------------------------------\n     見積の金額をそのまま引き継いで作る。見積は書き換えないので、\n     あとから見積を直しても、出した請求書はそのまま残る。\n     ====================================================================== */\n  function loadInvoices() { return load(KEY_INV, []); }\n  function invoicesOf(siteId) {\n    return loadInvoices().filter(function (v) { return v.siteId === siteId; });\n  }\n\n  function nextInvoiceNo() {\n    var d = todayISO().replace(/-/g, '');\n    var n = 1;\n    loadInvoices().forEach(function (v) {\n      var m = String(v.no || '').match(new RegExp('^' + d + '-(\\\\d+)$'));\n      if (m) n = Math.max(n, Number(m[1]) + 1);\n    });\n    return d + '-' + ('0' + n).slice(-2);\n  }\n\n  function makeInvoice(est, site) {\n    if (!(pb.company.name || '').trim()) {\n      toast('先に［自社情報］で会社名を登録してください');\n      $('.tab[data-view=\"settings\"]').click();\n      return;\n    }\n    var days = prompt('お支払期限を、今日から何日後にしますか？\\n（空欄なら期限なし）', '30');\n    if (days === null) return;\n\n    var v = clone(est);\n    delete v.savedAt;\n    v.id = 'v' + Date.now();\n    v.no = nextInvoiceNo();\n    v.date = todayISO();\n    v.doneDate = todayISO();\n    v.dueDate = String(days).trim() ? addDays(todayISO(), num(days)) : '';\n    v.estimateId = est.id;\n    v.estimateNo = est.no;\n    // 見積の「※本見積は…」という但し書きは請求書には合わないので引き継がない。\n    // 振込先は自社情報から自動で入るので、備考は空でよい。\n    v.note = '';\n    v.siteId = site.id;\n    v.total = calcOf(v).total;\n    v.savedAt = new Date().toISOString();\n\n    var list = loadInvoices();\n    list.push(v);\n    if (save(KEY_INV, list) === false) return;\n    renderList();\n    toast('請求書 ' + v.no + ' を作りました');\n  }\n\n  function renderInvoiceBlock(site) {\n    var wrap = el('div', 'inv-block');\n    var mine = invoicesOf(site.id).sort(function (a, b) {\n      return String(b.savedAt).localeCompare(String(a.savedAt));\n    });\n    wrap.appendChild(el('div', 'site-sec-label', '請求書（' + mine.length + '件）'));\n    if (!mine.length) {\n      wrap.appendChild(el('p', 'empty-note', 'まだ請求書はありません。上の見積の［請求書］から作れます。'));\n      return wrap;\n    }\n    mine.forEach(function (v) {\n      var row = el('div', 'est-row');\n      var main = el('div', 'est-main');\n      main.appendChild(el('b', null, v.no + '　' + (v.subject || site.name)));\n      main.appendChild(el('small', null,\n        '請求日 ' + jpDate(v.date) +\n        (v.dueDate ? '　/　支払期限 ' + jpDate(v.dueDate) : '') +\n        (v.estimateNo ? '　/　見積 ' + v.estimateNo + ' より' : '')));\n      row.appendChild(main);\n      row.appendChild(el('div', 'est-amount', yen(v.total || 0)));\n\n      var pr = el('button', 'btn btn-primary', '印刷 / PDF'); pr.type = 'button';\n      pr.addEventListener('click', function () { printInvoice(v); });\n\n      var ed = el('button', 'btn btn-ghost', '日付を直す'); ed.type = 'button';\n      ed.addEventListener('click', function () { editInvoiceDates(v); });\n\n      var del = el('button', 'btn btn-ghost btn-danger', '削除'); del.type = 'button';\n      del.addEventListener('click', function () {\n        if (!confirm('請求書 ' + v.no + ' を削除します。よろしいですか？')) return;\n        save(KEY_INV, loadInvoices().filter(function (x) { return x.id !== v.id; }));\n        renderList();\n        toast('削除しました');\n      });\n\n      row.appendChild(pr); row.appendChild(ed); row.appendChild(del);\n      wrap.appendChild(row);\n    });\n    return wrap;\n  }\n\n  function editInvoiceDates(v) {\n    var d1 = prompt('請求日（YYYY-MM-DD）', v.date);\n    if (d1 === null) return;\n    var d2 = prompt('工事完了日（YYYY-MM-DD／空欄可）', v.doneDate || '');\n    if (d2 === null) return;\n    var d3 = prompt('お支払期限（YYYY-MM-DD／空欄可）', v.dueDate || '');\n    if (d3 === null) return;\n    var list = loadInvoices();\n    list.forEach(function (x) {\n      if (x.id !== v.id) return;\n      x.date = d1.trim(); x.doneDate = d2.trim(); x.dueDate = d3.trim();\n    });\n    save(KEY_INV, list);\n    renderList();\n    toast('日付を直しました');\n  }\n\n  /* ======================================================================\n     全データのバックアップ（端末を移すとき用）\n     ====================================================================== */\n  /**\n   * バックアップの中の機種／別売品を数える（読み込む前の確認画面用）。\n   * どちらもメーカーごとの束だが、中身を入れている名前が違う。\n   *   機種   … packs[].rows   （読み込んだ生のまま）\n   *   別売品 … stores[].items\n   * 片方だけ見ていると、本物の5,681機種を「0件」と出して人を驚かせる。\n   * 2026-09-06、実際にそう出た。どちらの名前も見る。\n   */\n  function countPackItems(list, legacy) {\n    if (Array.isArray(list)) {\n      return list.reduce(function (a, p) {\n        if (!p) return a;\n        if (Array.isArray(p.rows))  return a + p.rows.length;\n        if (Array.isArray(p.items)) return a + p.items.length;\n        return a;\n      }, 0);\n    }\n    if (legacy && Array.isArray(legacy.rows)) return legacy.rows.length;\n    return 0;\n  }\n\n  function buildAllData() {\n    return {\n      type: 'airtec-all',\n      version: 1,\n      exportedAt: new Date().toISOString(),\n      pricebook: pb,\n      models: load(KEY_MDL, null),\n      /* 別売品は機種データとは別の引き出し（KEY_OPT）に入っている。\n         ここに入れ忘れると、書き出して読み込み直したときだけ\n         5社1,565品目が黙って消える。2026-09-06 に気づいて足した */\n      options: load(KEY_OPT, null),\n      sites: loadSites(),\n      estimates: load(KEY_EST, []),\n      invoices: loadInvoices()\n    };\n  }\n\n  $('#btn-export-all').addEventListener('click', function () {\n    download('空調王-全データ-' + todayISO() + '.json', JSON.stringify(buildAllData()));\n    markBackedUp('（ダウンロード）');\n    toast('書き出しました');\n  });\n\n  /* ---------- かんたんバックアップ ----------\n     保存先のファイルを1回決めておくと、次からは上書き保存できる。\n     OneDrive の中に置けば、そのままクラウドにも残る。\n     （この機能はパソコンのChrome/Edge向け。スマホでは通常のダウンロードになる） */\n  var KEY_BK = 'airtec_backup_meta_v1';\n  /* 何日空いたら知らせるか。\n     iPhoneのSafariは「7日そのサイトを開かないと保存した中身を消す」ので、\n     14日待っていては手遅れになる。7日に合わせる。 */\n  var BK_DAYS = 7;\n  var canPickFile = (typeof window.showSaveFilePicker === 'function');\n  var bkHandle = null;\n\n  /* ---------- 端末の側で内容が消える道と、その塞ぎ方 ----------\n     1. ブラウザの閲覧データ削除\n     2. iPhoneのSafariが7日で消す（Appleの決まり。ホーム画面のアプリは対象外）\n     3. 端末の故障・紛失・買い替え\n     連動を入れておけばクラウドに写しが残るので、1と3は塞げる。\n     2だけはホーム画面に追加してもらうしかない。 */\n  var KEY_A2HS = 'airtec_a2hs_hidden_v1';   // 案内を「あとで」した日\n\n  /** ホーム画面から開いているか（Safariのアプリ扱いか） */\n  function isStandalone() {\n    if (window.navigator.standalone === true) return true;               // iOS\n    try { return window.matchMedia('(display-mode: standalone)').matches; } catch (e) { return false; }\n  }\n  function isIOS() {\n    var ua = navigator.userAgent || '';\n    // iPadOSはMacintoshと名乗るので、指で触れるかどうかで見分ける\n    return /iP(hone|od|ad)/.test(ua) ||\n           (/Macintosh/.test(ua) && typeof document.ontouchend !== 'undefined');\n  }\n  /** 連動が入っているか（入っていればクラウドに写しがある） */\n  function syncOn() {\n    return !!(window.AirtecSync && window.AirtecSync.isOn && window.AirtecSync.isOn());\n  }\n\n  var A2HS_STEPS =\n    'iPhone・iPadでの追加のしかた\\n\\n' +\n    '１. Safariの下（または上）にある「共有」ボタンを押す\\n' +\n    '　　　□ に ↑ の矢印が刺さった絵のボタンです\\n' +\n    '２. 出てきた一覧を下にたどって「ホーム画面に追加」を押す\\n' +\n    '３. 右上の「追加」を押す\\n\\n' +\n    'ホーム画面に空調王のアイコンができます。\\n' +\n    '次からは、そのアイコンから開いてください。\\n\\n' +\n    '※ 大事なこと\\n' +\n    'ホーム画面のアプリは、Safariとは別の入れ物になることがあります。\\n' +\n    '空だったときは、先にSafariのほうで［自社情報］→ 連動を始め、\\n' +\n    'ホーム画面のほうで同じあいことばを入れてください。中身が流れ込みます。';\n\n  function showA2HS() { alert(A2HS_STEPS); }\n\n  function renderA2HS() {\n    var bar = $('#a2hs-warn');\n    if (!bar) return;\n    var hide = load(KEY_A2HS, 0);\n    var shownRecently = hide && (Date.now() - hide) < 30 * 86400000;   // 「あとで」は30日黙る\n    // iPhoneで、Safariから開いていて、まだ言われていない人にだけ出す\n    bar.style.display = (isIOS() && !isStandalone() && !shownRecently) ? '' : 'none';\n  }\n\n  /* --------------------------------------------------------------------\n     IndexedDB（この端末の中の、大きいものを置ける入れ物）\n\n     localStorage には文字しか入らず、容量も5MBほどしかない。\n     そこに入らないものを、ここに置く。棚は2つ。\n       kv     … 保存先のファイルハンドル\n       photos … 現場写真（写真そのもの＝Blob）\n     -------------------------------------------------------------------- */\n  var IDB_VER = 2;                 // 棚を増やしたら1つ上げる（増えた棚だけ作られる）\n  var idbConn = null;              // 開いた入れ物は使い回す（写真を何十枚も入れるときのため）\n  function idbOpen() {\n    if (idbConn) return idbConn;\n    idbConn = new Promise(function (res, rej) {\n      var q = indexedDB.open('airtec', IDB_VER);\n      q.onupgradeneeded = function () {\n        // すでにある棚は作り直さない（作ろうとするとその場で止まる）\n        var db = q.result;\n        if (!db.objectStoreNames.contains('kv')) db.createObjectStore('kv');\n        if (!db.objectStoreNames.contains('photos')) {\n          db.createObjectStore('photos', { keyPath: 'id' })\n            .createIndex('siteId', 'siteId', { unique: false });\n        }\n      };\n      q.onerror   = function () { rej(q.error); };\n      q.onblocked = function () { rej(new Error('別のタブが開いています')); };\n      q.onsuccess = function () {\n        var db = q.result;\n        // 別のタブが棚を増やそうとしたら、こちらは手を離す（次に使うとき開き直す）\n        db.onversionchange = function () { db.close(); idbConn = null; };\n        db.onclose = function () { idbConn = null; };\n        res(db);\n      };\n    });\n    idbConn.catch(function () { idbConn = null; });   // 開けなかったら、次でやり直せるようにする\n    return idbConn;\n  }\n  /** 棚を1つ開いて、その中で1回だけ操作する */\n  function idb(store, mode, fn) {\n    return idbOpen().then(function (db) {\n      return new Promise(function (res, rej) {\n        // put() などはその場でエラーを投げることがある。\n        // ここで受け止めないと、待っている側が永久に止まってしまう。\n        try {\n          var tx = db.transaction(store, mode);\n          var r = fn(tx.objectStore(store));\n          r.onsuccess = function () { res(r.result); };\n          r.onerror = function () { rej(r.error); };\n          tx.onabort = function () { rej(tx.error); };\n        } catch (e) { rej(e); }\n      });\n    });\n  }\n  function idbSet(k, v) { return idb('kv', 'readwrite', function (s) { return s.put(v, k); }); }\n  function idbGet(k) { return idb('kv', 'readonly', function (s) { return s.get(k); }); }\n\n  function bkMeta() { return load(KEY_BK, { lastAt: '', name: '' }); }\n  function markBackedUp(name) {\n    var m = bkMeta();\n    m.lastAt = new Date().toISOString();\n    if (name) m.name = name;\n    save(KEY_BK, m);\n    renderBackupState();\n  }\n  function daysSinceBackup() {\n    var m = bkMeta();\n    if (!m.lastAt) return Infinity;\n    return (Date.now() - new Date(m.lastAt).getTime()) / 86400000;\n  }\n\n  function renderBackupState() {\n    var m = bkMeta();\n    var t = $('#bk-target'), l = $('#bk-last');\n    if (!t) return;\n    t.textContent = bkHandle ? (bkHandle.name || m.name || '設定済み')\n      : (canPickFile ? 'まだ決めていません' : 'この端末ではダウンロード保存になります');\n    l.textContent = m.lastAt\n      ? jpDate(m.lastAt.slice(0, 10)) + '（' + Math.floor(daysSinceBackup()) + '日前）' + (m.name ? '　' + m.name : '')\n      : 'まだ';\n    $('#btn-bk-pick').style.display = canPickFile ? '' : 'none';\n\n    var d = daysSinceBackup();\n    var warn = $('#bk-warn');\n\n    /* 連動が入っていれば、書いたそばからクラウドに写しが残る。\n       それでも急かすと「またこれか」で読まれなくなるので、黙る。\n       守りが1つも無いときだけ、はっきり言う。 */\n    if (syncOn()) { warn.style.display = 'none'; renderA2HS(); return; }\n\n    if (d === Infinity || d >= BK_DAYS) {\n      $('#bk-warn-text').textContent = (d === Infinity)\n        ? 'まだ一度もバックアップしていません。連動も入っていません。' +\n          'いまブラウザの閲覧データを消すと、入れた単価も見積もすべて消えます。'\n        : Math.floor(d) + '日バックアップしていません（連動も入っていません）。';\n      warn.style.display = '';\n    } else warn.style.display = 'none';\n\n    renderA2HS();\n  }\n\n  async function pickBackupFile() {\n    var h;\n    try {\n      h = await window.showSaveFilePicker({\n        suggestedName: '空調王-全データ.json',\n        types: [{ description: '空調王のバックアップ', accept: { 'application/json': ['.json'] } }]\n      });\n    } catch (e) { return; }          // 選ぶのをやめた場合。何もしない\n    if (!h) return;\n\n    bkHandle = h;\n    var m = bkMeta(); m.name = h.name; save(KEY_BK, m);\n    // 保存先の記憶に失敗しても、今回の保存は続ける（次回また選んでもらえばよい）\n    var remembered = true;\n    try { await idbSet('backupHandle', h); } catch (e) { remembered = false; }\n    renderBackupState();\n    if (await writeBackup(true)) {\n      toast(remembered\n        ? '保存先を決めました。次からは「いますぐ保存」だけでOKです'\n        : '保存しました（保存先を覚えられなかったので、次回もう一度選んでください）');\n    }\n  }\n\n  async function writeBackup(silent) {\n    if (!bkHandle) {\n      // 保存先が無いときは、いつものダウンロードで保存する\n      download('空調王-全データ-' + todayISO() + '.json', JSON.stringify(buildAllData()));\n      markBackedUp('（ダウンロード）');\n      if (!silent) toast('書き出しました');\n      return true;\n    }\n    try {\n      var p = await bkHandle.queryPermission({ mode: 'readwrite' });\n      if (p !== 'granted') p = await bkHandle.requestPermission({ mode: 'readwrite' });\n      if (p !== 'granted') { toast('保存先への書き込みが許可されませんでした'); return false; }\n      var w = await bkHandle.createWritable();\n      await w.write(JSON.stringify(buildAllData()));\n      await w.close();\n      markBackedUp(bkHandle.name);\n      if (!silent) toast('保存しました（' + bkHandle.name + '）');\n      return true;\n    } catch (e) {\n      toast('保存できませんでした。「保存先を決める」からやり直してください');\n      return false;\n    }\n  }\n\n  /* 連動（sync.js）は app.js のあとに読み込まれる。\n     先に警告を描いてしまうと「連動していない」と誤って判断したまま残るので、\n     全部そろったところで一度描き直す。\n\n     ★ 入口と本体に割ってから（[[project-kuchoo-lock]]）、app.js はページが\n       出来上がったあとに流し込まれるようになった。そのときは load がとっくに\n       済んでいて、この待ち伏せは永久に当たらない。だから、もう済んでいたら\n       待たずに描く。待つのは、昔ながらの読み込まれ方をしたときだけ。 */\n  if (document.readyState === 'complete') setTimeout(renderBackupState, 0);\n  else window.addEventListener('load', function () { renderBackupState(); });\n\n  $('#btn-a2hs-how').addEventListener('click', showA2HS);\n  $('#btn-a2hs-how2').addEventListener('click', showA2HS);\n  $('#btn-a2hs-hide').addEventListener('click', function () {\n    save(KEY_A2HS, Date.now());\n    $('#a2hs-warn').style.display = 'none';\n  });\n\n  $('#btn-bk-pick').addEventListener('click', function () { pickBackupFile(); });\n  $('#btn-bk-save').addEventListener('click', function () { writeBackup(false); });\n  $('#btn-bk-warn-save').addEventListener('click', function () { writeBackup(false); });\n  $('#btn-bk-warn-hide').addEventListener('click', function () { $('#bk-warn').style.display = 'none'; });\n\n  /* --------------------------------------------------------------------\n     新しい版が出ていないか、見に行く。\n\n     版番号は app.js?v=… のように「中のファイル」にしか付いていない。\n     だから入口の index.html が端末に残っていると、その端末はいつまでも\n     古い番号のファイルを取りに行く。スマホでこれが起きると、\n     直したものが永久に届かない。気づく手立てが無いのがいちばん困る。\n\n     そこで version.txt（毎回サーバーから取り直す）と見比べて、ちがっていたら\n     知らせる。押すとURLに新しい番号を付けて開き直すので、入口ごと新しくなる。\n     入れた内容はURLではなくドメインごとに置いてあるので、消えない。\n     -------------------------------------------------------------------- */\n  var newVersion = '';\n\n  var lastVerCheck = 0;\n  function checkNewVersion() {\n    if (location.protocol === 'file:') return;\n    if (Date.now() - lastVerCheck < 60000) return;      // 1分に1回まで\n    lastVerCheck = Date.now();\n    fetch('version.txt?t=' + Date.now(), { cache: 'no-store' })\n      .then(function (r) { return r.ok ? r.text() : ''; })\n      .then(function (t) {\n        var v = String(t || '').trim();\n        if (!v || v === APP_VERSION) return;\n        newVersion = v;\n        $('#new-warn').style.display = '';\n      })\n      .catch(function () { /* 圏外などで取れなくても、いまの画面はそのまま使える */ });\n  }\n  checkNewVersion();\n  /* ホーム画面のアプリは、閉じたつもりでも裏で眠っているだけのことが多い。\n     もう一度開いても読み込み直さないので、起動したときの1回だけでは新しい版に気づかない。\n     2026-09-10、BIGBOSSの「スマホ版があたらしくならない」で分かった。\n     画面に戻ってきたときにも見に行く。 */\n  document.addEventListener('visibilitychange', function () {\n    if (document.visibilityState === 'visible') checkNewVersion();\n  });\n  window.addEventListener('pageshow', function (e) { if (e.persisted) checkNewVersion(); });\n\n  $('#btn-new-reload').addEventListener('click', function () {\n    location.replace(location.pathname + '?v=' + encodeURIComponent(newVersion || Date.now()));\n  });\n\n  /* 起動時：保存先が生きていて、しばらく保存していなければ静かに保存しておく */\n  async function initBackup() {\n    if (canPickFile) {\n      try {\n        var h = await idbGet('backupHandle');\n        if (h) {\n          bkHandle = h;\n          var p = await h.queryPermission({ mode: 'readwrite' });\n          if (p === 'granted' && daysSinceBackup() >= 1) {\n            if (await writeBackup(true)) toast('バックアップを保存しました');\n          }\n        }\n      } catch (e) { /* 使えなければ手動保存にまかせる */ }\n    }\n    renderBackupState();\n  }\n\n  $('#file-import-all').addEventListener('change', function (ev) {\n    readJSON(ev.target, function (data) {\n      if (!data || data.type !== 'airtec-all' || !data.pricebook) {\n        toast('「全部まとめて書き出す」で作ったファイルを選んでください');\n        return;\n      }\n      var n = {\n        単価: (data.pricebook.categories || []).reduce(function (a, c) { return a + (c.items || []).length; }, 0),\n        機種: countPackItems(data.models && data.models.packs, data.models),\n        別売品: countPackItems(data.options && data.options.stores, null),\n        現場: (data.sites || []).length,\n        見積: (data.estimates || []).length,\n        請求書: (data.invoices || []).length\n      };\n      if (!confirm('この端末の内容を、読み込んだファイルで置き換えます。\\n\\n' +\n        '　単価　：' + n.単価 + '件\\n' +\n        '　機種　：' + n.機種 + '件\\n' +\n        '　別売品：' + n.別売品 + '件\\n' +\n        '　現場　：' + n.現場 + '件\\n' +\n        '　見積　：' + n.見積 + '件\\n' +\n        '　請求書：' + n.請求書 + '件\\n\\n' +\n        'いまの内容は消えます。よろしいですか？')) return;\n\n      pb = data.pricebook;\n      pb.company  = adoptCompany(pb.company);\n      pb.defaults = Object.assign({}, DEFAULT_PRICEBOOK.defaults, pb.defaults || {});\n      if (!Array.isArray(pb.categories)) pb.categories = [];\n      activeCat = pb.categories.length ? pb.categories[0].id : null;\n      if (savePB() === false) return;\n\n      if (data.models) save(KEY_MDL, data.models); else removeKey(KEY_MDL);\n      /* 別売品を入れ忘れていた頃のバックアップには options が無い。\n         そのときは端末に入っている別売品をそのまま残す。\n         「無いから消す」にすると、古いファイルを読んだ人の別売品が巻き添えで消える */\n      if (data.options) save(KEY_OPT, data.options);\n      saveSites(data.sites || []);\n      save(KEY_EST, data.estimates || []);\n      save(KEY_INV, data.invoices || []);\n\n      openSiteId = null;\n      chooserSel = {};\n      loadModels();\n      renderMaster(); renderPicker(); fillCompany(); renderList();\n      renderBackupState();\n      toast('読み込みました');\n    });\n  });\n\n  function printInvoice(v) {\n    buildSheet('invoice', v);\n    document.title = '請求書_' + (v.customer || '無題') + '_' + v.no;\n    setTimeout(function () { window.print(); }, 60);\n  }\n\n  /* ======================================================================\n     現地調査チェックシート\n     ----------------------------------------------------------------------\n     項目の中身は survey.js の SURVEY / SURVEY_HEAD。\n     入力した内容は、その現場の中（site.survey）に保存する。\n     ====================================================================== */\n\n  /* ----------------------------------------------------------------------\n     現調シートの記入から、機種選定の条件を読み取る\n     ----------------------------------------------------------------------\n     現場で書いた紙の内容がそのまま選定に効くようにする。\n     ・用途 …… 店舗→一般商店、事務所→一般事務所、飲食は発熱負荷の「厨房」で見る\n     ・対象面積・天井高 …… 「120㎡ / 天井高2.7m」のような自由記入から数字を拾う\n     ・構造 …… 木造/軽量鉄骨は断熱が弱いほう、RCは強いほうに寄せる\n     ・発熱負荷・外気日射 …… チェックをそのまま上乗せに使う\n     読み取れなかったものは、選定の画面で人が直せる。\n     ---------------------------------------------------------------------- */\n  function szFromSurvey(data) {\n    data = data || {};\n    var arr = function (k) { var v = data[k]; return Array.isArray(v) ? v : (v ? [v] : []); };\n    var txt = function (k) { return String(data[k] || ''); };\n\n    // 用途\n    var uses = arr('用途').join(' ') + ' ' + txt('建物用途');\n    var heat = arr('発熱負荷').join(' ');\n    var use = 0;\n    if (/厨房|窯/.test(heat) || /飲食|レストラン|食堂/.test(uses)) use = 3;\n    else if (/理美容|美容|サロン|喫茶|カフェ/.test(uses)) use = 2;\n    else if (/店舗|店/.test(uses)) use = 1;\n    else use = 0;\n\n    // 面積と天井高（「120㎡ / 天井高2.7m」など）\n    var at = txt('対象面積・天井高');\n    var area = szAreaFromText(at);\n    var height = 0;\n    var mh = at.match(/(?:天井高|天高|高さ)\\s*([\\d.]+)/);\n    if (!mh && !SZ_X.test(at)) mh = at.match(/([\\d.]+)\\s*m(?!\\s*[²2])/i);\n    if (mh) height = Number(mh[1]);\n\n    // 構造から断熱を推し量る（あくまで初期値。現場を見て直す）\n    var st = arr('構造').join(' ');\n    var insul = 0;\n    if (/RC|鉄筋/.test(st)) insul = 1;          // 標準的な断熱ありに寄せる\n    else if (/木造|軽量鉄骨/.test(st)) insul = 0;\n    var age = txt('階数・築年数');\n    if (/新築|築\\s*[0-9]\\s*年|築\\s*1[0-4]\\s*年/.test(age)) insul = Math.max(insul, 1);\n\n    // 上乗せ\n    var sun = arr('外気・日射').join(' ');\n    var adds = [];\n    if (/西日/.test(sun)) adds.push('west');\n    if (/吹抜|吹き抜/.test(sun)) adds.push('glass');\n    if (/厨房|窯/.test(heat)) adds.push('kitchen');\n    if (/換気量多い|出入口多い/.test(sun)) adds.push('air');\n    var people = txt('使用人数').match(/(\\d+)/g);\n    if (people && area && Math.max.apply(null, people.map(Number)) / area > 0.15) adds.push('people');\n\n    return { use: use, area: area, height: height || 2.7, insul: insul, adds: adds };\n  }\n\n  /** 現場に保存されている調査内容を取り出す（無ければ空） */\n  function surveyOf(site) { return site.survey || {}; }\n\n  /** 調査内容を書き戻して保存する */\n  function saveSurvey(siteId, data) {\n    var sites = loadSites();\n    var hit = null;\n    sites.forEach(function (s) { if (s.id === siteId) hit = s; });\n    if (!hit) return;\n    hit.survey = data;\n    hit.surveyAt = new Date().toISOString();\n    saveSites(sites);\n  }\n\n  /** 入力済みの項目数を数える（進み具合の表示用） */\n  function surveyFilled(data) {\n    var n = 0;\n    Object.keys(data || {}).forEach(function (k) {\n      var v = data[k];\n      if (Array.isArray(v)) { if (v.length) n++; }\n      else if (String(v || '').trim()) n++;\n    });\n    return n;\n  }\n\n  function surveyTotalFields() {\n    var n = SURVEY_HEAD.length;\n    SURVEY.forEach(function (s) { s.pairs.forEach(function (p) { n += p.length; }); });\n    return n;\n  }\n\n  function renderSurveyBlock(site) {\n    var wrap = el('div', 'survey-block');\n    var data = surveyOf(site);\n\n    // 現場に入っている情報は、調査シートにも先に入れておく（数える前にやる）\n    var pre = { '案件名': site.name, 'お客様名': site.customer, '現場住所': site.address, '連絡先': site.tel };\n    var filledFromSite = false;\n    Object.keys(pre).forEach(function (k) {\n      if (!String(data[k] || '').trim() && String(pre[k] || '').trim()) { data[k] = pre[k]; filledFromSite = true; }\n    });\n    if (filledFromSite) saveSurvey(site.id, data);\n\n    var head = el('div', 'survey-head');\n    head.appendChild(el('span', 'site-sec-label', '現地調査チェックシート'));\n    var cnt = el('span', 'survey-count', surveyFilled(data) + ' / ' + surveyTotalFields() + ' 項目');\n    head.appendChild(cnt);\n\n    var cp = el('button', 'btn btn-ghost btn-sm', 'コピー'); cp.type = 'button';\n    cp.title = '現場で入力した内容を文字にします。LINEなどで事務所に送ってください';\n    cp.addEventListener('click', function () { copySurvey(site); });\n    var ps = el('button', 'btn btn-ghost btn-sm', '貼り付け'); ps.type = 'button';\n    ps.title = '送られてきた文字を貼り付けて、このシートに取り込みます';\n    ps.addEventListener('click', function () { pasteSurvey(site); });\n    head.appendChild(cp); head.appendChild(ps);\n\n    var pr = el('button', 'btn btn-ghost btn-sm', '印刷 / PDF'); pr.type = 'button';\n    pr.addEventListener('click', function () { printSurvey(site, false); });\n    var bl = el('button', 'btn btn-ghost btn-sm', '白紙で印刷'); bl.type = 'button';\n    bl.title = '現場に持っていく用。何も記入されていない状態で印刷します';\n    bl.addEventListener('click', function () { printSurvey(site, true); });\n    head.appendChild(pr); head.appendChild(bl);\n    wrap.appendChild(head);\n\n    function touch() {\n      saveSurvey(site.id, data);\n      cnt.textContent = surveyFilled(data) + ' / ' + surveyTotalFields() + ' 項目';\n    }\n\n    var hd = el('details', 'survey-sec');\n    var hs = el('summary', null, '基本情報');\n    hd.appendChild(hs);\n    var hgrid = el('div', 'survey-grid');\n    SURVEY_HEAD.forEach(function (f) {\n      hgrid.appendChild(surveyField(f, data, touch));\n    });\n    hd.appendChild(hgrid);\n    wrap.appendChild(hd);\n\n    SURVEY.forEach(function (sec) {\n      var d = el('details', 'survey-sec');\n      d.appendChild(el('summary', null, sec.sec));\n      var grid = el('div', 'survey-grid');\n      sec.pairs.forEach(function (pair) {\n        pair.forEach(function (f) { grid.appendChild(surveyField(f, data, touch)); });\n      });\n      d.appendChild(grid);\n      wrap.appendChild(d);\n    });\n\n    // 現調シートの記入から、必要な能力を出す\n    wrap.appendChild(renderSurveySizer(site, data));\n    return wrap;\n  }\n\n  /* ----------------------------------------------------------------------\n     現調シートの中の「必要な能力を出す」\n     ----------------------------------------------------------------------\n     現場でシートを埋めると、そのまま馬力の目安が出る。\n     読み取った値は人が直せる。直したものは現場に覚えておく（site.sizing）。\n     出した馬力は［見積を作る］から見積作成へ持っていける。\n     ---------------------------------------------------------------------- */\n  function renderSurveySizer(site, data) {\n    var box = el('details', 'survey-sec sz-sec');\n    box.open = true;\n    box.appendChild(el('summary', null, '必要な能力を出す（機種選定）'));\n\n    var body = el('div', 'sz-body');\n    box.appendChild(body);\n\n    var p = site.sizing || szFromSurvey(data);\n\n    function save() {\n      var sites = loadSites();\n      sites.forEach(function (s) { if (s.id === site.id) { s.sizing = p; site.sizing = p; } });\n      saveSites(sites);\n    }\n\n    function draw() {\n      body.innerHTML = '';\n\n      var form = el('div', 'sizer-form');\n      // 用途\n      var fu = el('label', 'f'); fu.appendChild(el('span', null, '用途'));\n      var su = document.createElement('select');\n      SZ_USES.forEach(function (u, i) {\n        var o = document.createElement('option');\n        o.value = String(i); o.textContent = u.name + '（' + u.lo + '〜' + u.hi + ' W/m²）';\n        su.appendChild(o);\n      });\n      su.value = String(p.use || 0);\n      su.addEventListener('change', function () { p.use = Number(su.value); save(); draw(); });\n      fu.appendChild(su); form.appendChild(fu);\n\n      // 広さ\n      var fa = el('label', 'f'); fa.appendChild(el('span', null, '広さ（m²）'));\n      var ia = document.createElement('input');\n      ia.type = 'number'; ia.min = '0'; ia.step = '0.1'; ia.placeholder = '例 120';\n      ia.value = p.area ? String(Math.round(p.area * 10) / 10) : '';\n      ia.addEventListener('input', function () { p.area = num(ia.value); save(); drawResult(); });\n      fa.appendChild(ia); form.appendChild(fa);\n\n      // 天井高\n      var fh = el('label', 'f'); fh.appendChild(el('span', null, '天井の高さ（m）'));\n      var ih = document.createElement('input');\n      ih.type = 'number'; ih.min = '1.8'; ih.max = '8'; ih.step = '0.1';\n      ih.value = String(p.height || 2.7);\n      ih.addEventListener('input', function () { p.height = num(ih.value); save(); drawResult(); });\n      fh.appendChild(ih); form.appendChild(fh);\n\n      // 断熱\n      var fi = el('label', 'f'); fi.appendChild(el('span', null, '建物・断熱'));\n      var si = document.createElement('select');\n      SZ_INSUL.forEach(function (v, i) {\n        var o = document.createElement('option');\n        o.value = String(i); o.textContent = v.name;\n        si.appendChild(o);\n      });\n      si.value = String(p.insul || 0);\n      si.addEventListener('change', function () { p.insul = Number(si.value); save(); drawResult(); });\n      fi.appendChild(si); form.appendChild(fi);\n      body.appendChild(form);\n\n      /* 寸法から広さを出す\n         現場ではメジャーで測るだけ。横と縦を入れれば掛け算も足し算もここでやる。\n         L字の部屋は「大きい四角 ＋ 出っ張り」か「大きい四角 － 欠け」で入れる。 */\n      if (!Array.isArray(p.rooms)) p.rooms = [];\n      if (!p.rooms.length) p.rooms.push({ n: '', w: 0, d: 0, minus: false });\n      if (p.unit !== 'cm') p.unit = 'm';\n\n      var meas = el('div', 'sz-meas');\n      var mhd = el('div', 'sz-meas-head');\n      mhd.appendChild(el('b', null, '寸法から広さを出す'));\n      mhd.appendChild(el('span', 'sz-note', '測った横と縦を入れるだけ。掛け算も足し算もこちらでやります'));\n      var sunit = document.createElement('select');\n      sunit.className = 'sz-unit';\n      [['m', 'メートルで入れる'], ['cm', 'センチで入れる']].forEach(function (u) {\n        var o = document.createElement('option');\n        o.value = u[0]; o.textContent = u[1];\n        sunit.appendChild(o);\n      });\n      sunit.value = p.unit;\n      sunit.addEventListener('change', function () {\n        // 入れ直さなくていいように、いま入っている数字も単位に合わせて直す\n        var k = (p.unit === 'm' && sunit.value === 'cm') ? 100\n              : (p.unit === 'cm' && sunit.value === 'm') ? 0.01 : 1;\n        if (k !== 1) {\n          p.rooms.forEach(function (r) {\n            if (num(r.w)) r.w = Math.round(num(r.w) * k * 100) / 100;\n            if (num(r.d)) r.d = Math.round(num(r.d) * k * 100) / 100;\n          });\n        }\n        p.unit = sunit.value; save(); drawRows(); syncArea();\n      });\n      mhd.appendChild(sunit);\n      meas.appendChild(mhd);\n\n      var rowsBox = el('div', 'sz-rows');\n      meas.appendChild(rowsBox);\n\n      var mfoot = el('div', 'sz-meas-foot');\n      var addBtn = el('button', 'btn btn-ghost btn-sm', '＋ 場所を足す');\n      addBtn.type = 'button';\n      addBtn.title = '部屋がいくつかあるとき、L字の部屋のときに足してください';\n      addBtn.addEventListener('click', function () {\n        p.rooms.push({ n: '', w: 0, d: 0, minus: false });\n        save(); drawRows(); syncArea();\n        var last = rowsBox.querySelector('.sz-row:last-child .sz-d');\n        if (last) last.focus();\n      });\n      mfoot.appendChild(addBtn);\n      var tot = el('div', 'sz-meas-total');\n      mfoot.appendChild(tot);\n      var wr = el('button', 'btn btn-ghost btn-sm', 'この広さをシートに書く');\n      wr.type = 'button';\n      wr.title = '上の「対象面積・天井高」の欄に書き込みます';\n      wr.addEventListener('click', function () {\n        if (!p.area) return;\n        var line = szFmt(p.area) + '㎡ / 天井高' + (p.height || 2.7) + 'm';\n        data['対象面積・天井高'] = line;\n        saveSurvey(site.id, data);\n        var host = box.parentNode || document;\n        var inp = host.querySelector('.survey-f[data-k=\"対象面積・天井高\"] input');\n        if (inp) inp.value = line;\n        wr.textContent = 'シートに書きました';\n        setTimeout(function () { wr.textContent = 'この広さをシートに書く'; }, 1600);\n      });\n      mfoot.appendChild(wr);\n      meas.appendChild(mfoot);\n      body.appendChild(meas);\n\n      /** 1行ぶんの数字の入れ物。打っている間に作り直すと文字が消えるので、\n          入力中は行の中の答えと合計だけを書き換える */\n      function measInput(r, key, eq) {\n        var i = document.createElement('input');\n        i.type = 'number'; i.min = '0'; i.step = '0.01';\n        i.className = 'sz-d'; i.inputMode = 'decimal';\n        i.placeholder = (p.unit === 'cm') ? '例 820' : '例 8.2';\n        i.value = r[key] ? String(r[key]) : '';\n        i.addEventListener('input', function () {\n          r[key] = num(i.value);\n          eq.textContent = '＝ ' + szFmt(szRoomArea(r, p.unit)) + ' m²';\n          syncArea();\n        });\n        return i;\n      }\n\n      function drawRows() {\n        rowsBox.innerHTML = '';\n        p.rooms.forEach(function (r, idx) {\n          var row = el('div', 'sz-row' + (r.minus ? ' sz-row-minus' : ''));\n\n          var nm = document.createElement('input');\n          nm.type = 'text'; nm.className = 'sz-nm';\n          nm.placeholder = '場所（例 事務所）';\n          nm.value = r.n || '';\n          nm.addEventListener('input', function () { r.n = nm.value; save(); });\n          row.appendChild(nm);\n\n          var eq = el('span', 'sz-eq', '＝ ' + szFmt(szRoomArea(r, p.unit)) + ' m²');\n          row.appendChild(el('span', 'sz-lab', '横'));\n          row.appendChild(measInput(r, 'w', eq));\n          row.appendChild(el('span', 'sz-x', '×'));\n          row.appendChild(el('span', 'sz-lab', '縦'));\n          row.appendChild(measInput(r, 'd', eq));\n          row.appendChild(eq);\n\n          var pm = el('button', 'btn btn-ghost btn-sm sz-pm', r.minus ? 'ひく' : 'たす');\n          pm.type = 'button';\n          pm.title = '出っ張りは「たす」、欠けている所は「ひく」にしてください';\n          pm.addEventListener('click', function () {\n            r.minus = !r.minus; save(); drawRows(); syncArea();\n          });\n          row.appendChild(pm);\n\n          if (p.rooms.length > 1) {\n            var del = el('button', 'btn btn-ghost btn-sm sz-del', '✕');\n            del.type = 'button'; del.title = 'この行を消す';\n            del.setAttribute('aria-label', (idx + 1) + '行目を消す');\n            del.addEventListener('click', function () {\n              p.rooms.splice(idx, 1); save(); drawRows(); syncArea();\n            });\n            row.appendChild(del);\n          }\n          rowsBox.appendChild(row);\n        });\n      }\n\n      /** 行の合計を「広さ」の欄に流し込む。1行でも数字が入っていれば、そちらが正。\n          quiet は最初に見た目をそろえるだけのとき（まだ結果の欄を作っていない） */\n      function syncArea(quiet) {\n        var any = p.rooms.some(function (r) { return num(r.w) > 0 && num(r.d) > 0; });\n        var sum = any ? szRoomsArea(p.rooms, p.unit) : 0;\n        if (any && sum > 0) {\n          p.area = sum;\n          ia.value = szFmt(sum);\n          tot.textContent = '合計 ' + szFmt(sum) + ' m²（約 ' + szTsubo(sum) + ' 坪）';\n          tot.classList.remove('sz-warn');\n        } else if (any) {\n          tot.textContent = 'ひく方が大きくなっています。「たす／ひく」を見直してください';\n          tot.classList.add('sz-warn');\n        } else {\n          tot.textContent = '';\n          tot.classList.remove('sz-warn');\n        }\n        wr.style.display = (any && sum > 0) ? '' : 'none';\n        if (!quiet) { save(); drawResult(); }\n      }\n\n      drawRows();\n      syncArea(true);\n\n      // 上乗せ\n      var checks = el('div', 'sizer-checks');\n      SZ_ADDS.forEach(function (a) {\n        var lab = el('label', 'sz-check');\n        var cb = document.createElement('input');\n        cb.type = 'checkbox';\n        cb.checked = (p.adds || []).indexOf(a.key) >= 0;\n        cb.addEventListener('change', function () {\n          p.adds = (p.adds || []).filter(function (k) { return k !== a.key; });\n          if (cb.checked) p.adds.push(a.key);\n          save(); drawResult();\n        });\n        lab.appendChild(cb);\n        lab.appendChild(el('span', null, a.name));\n        checks.appendChild(lab);\n      });\n      body.appendChild(checks);\n\n      var re = el('button', 'btn btn-ghost btn-sm', 'シートから読み直す');\n      re.type = 'button';\n      re.title = '上のチェックシートの記入内容から、もう一度読み取ります';\n      re.addEventListener('click', function () { p = szFromSurvey(data); save(); draw(); });\n      body.appendChild(re);\n\n      var res = el('div', 'sizer-result');\n      body.appendChild(res);\n\n      function drawResult() {\n        res.innerHTML = '';\n        var r = szCalc(p);\n        if (!r.ok) {\n          res.appendChild(el('div', 'sz-empty', '広さを入れると、必要な能力の目安が出ます。'));\n          return;\n        }\n        var head = el('div', 'sz-head');\n        head.appendChild(el('b', null, '必要な能力の目安　' + r.mid.toFixed(1) + ' kW'));\n        head.appendChild(el('span', 'sz-range', '（' + r.lo.toFixed(1) + '〜' + r.hi.toFixed(1) + ' kW の幅）'));\n        res.appendChild(head);\n\n        var rec = el('div', 'sz-pick');\n        rec.appendChild(el('b', null, 'P' + r.form[0] + '形（' + r.form[1] + '馬力・' + (r.form[0] / 10).toFixed(1) + 'kW）'));\n        if (r.formHi[0] !== r.form[0]) {\n          rec.appendChild(el('span', null, '　※負荷が重いほうに振れるなら P' + r.formHi[0] + '形（' + r.formHi[1] + '馬力）'));\n        }\n        res.appendChild(rec);\n\n        var why = el('div', 'sz-why');\n        why.appendChild(el('span', null, '広さ ' + r.area.toFixed(1) + ' m² × ' + r.use.lo + '〜' + r.use.hi + ' W/m²'));\n        if (r.insul.k !== 1) why.appendChild(el('span', null, '／断熱 ×' + r.insul.k.toFixed(2)));\n        if (r.hk > 1) why.appendChild(el('span', null, '／天井 ' + r.height + 'm ×' + r.hk.toFixed(2)));\n        if (r.addNames.length) why.appendChild(el('span', null, '／' + r.addNames.join('・') + ' ×' + r.addK.toFixed(2)));\n        res.appendChild(why);\n\n        var found = szFindModels(r.form);\n        if (found) {\n          if (found.moved) {\n            res.appendChild(el('div', 'sz-why', 'P' + r.form[0] + '形は入っている機種データに無いので、1つ上の P' + found.form[0] + '形（' + found.form[1] + '馬力）で探します。'));\n          }\n          var go = el('button', 'btn btn-primary btn-sm', 'この馬力（' + found.form[1] + '馬力）で見積を作る　' + found.count + '件');\n          go.type = 'button';\n          go.addEventListener('click', function () {\n            // この現場の情報を引き継いで見積を始め、その馬力で機器を選べる状態にする\n            newEstimateForSite(site);\n            chooserSel = { hp: found.form[1] };\n            renderChooser();\n            var c = $('#chooser');\n            if (c) setTimeout(function () { c.scrollIntoView({ block: 'start' }); }, 150);\n            toast(found.form[1] + '馬力で機器を選べます');\n          });\n          res.appendChild(go);\n        } else if (models) {\n          res.appendChild(el('div', 'sz-why', 'この馬力に合う機種が、入っている機種データにありません。'));\n        }\n\n        var note = el('div', 'sz-note');\n        note.appendChild(el('span', null,\n          '用途の負荷と断熱の倍率はカタログの数字です。天井の高さと上のチェックは、' +\n          'カタログに数字が無いので設計の考え方から置いた目安です。最後は現場を見て決めてください。'));\n        res.appendChild(note);\n      }\n\n      drawResult();\n    }\n\n    draw();\n    return box;\n  }\n\n  /** 1項目分の入力欄を作る */\n  function surveyField(f, data, touch) {\n    var box = el('div', 'survey-f' + (f.t === 'memo' ? ' survey-f-wide' : ''));\n    box.setAttribute('data-k', f.k);   // 下の機種選定から書き戻すときの目印\n    box.appendChild(el('span', 'survey-k', f.k));\n\n    if (f.t === 'check' || f.t === 'checktext') {\n      if (!Array.isArray(data[f.k])) data[f.k] = [];\n      var opts = el('div', 'survey-opts');\n      f.o.forEach(function (o) {\n        var lab = el('label', 'survey-chk');\n        var cb = el('input'); cb.type = 'checkbox';\n        cb.checked = data[f.k].indexOf(o) >= 0;\n        cb.addEventListener('change', function () {\n          var i = data[f.k].indexOf(o);\n          if (cb.checked && i < 0) data[f.k].push(o);\n          if (!cb.checked && i >= 0) data[f.k].splice(i, 1);\n          touch();\n        });\n        lab.appendChild(cb);\n        lab.appendChild(el('span', null, o));\n        opts.appendChild(lab);\n      });\n      box.appendChild(opts);\n      if (f.t === 'checktext') {\n        var k2 = f.k + '_memo';\n        var ti = el('input', 'survey-sub'); ti.type = 'text';\n        ti.placeholder = f.ph || ''; ti.value = data[k2] || '';\n        ti.addEventListener('input', function () { data[k2] = ti.value; touch(); });\n        box.appendChild(ti);\n      }\n      return box;\n    }\n\n    if (f.t === 'memo') {\n      var ta = el('textarea'); ta.rows = 2; ta.value = data[f.k] || '';\n      ta.addEventListener('input', function () { data[f.k] = ta.value; touch(); });\n      box.appendChild(ta);\n      return box;\n    }\n\n    var inp = el('input'); inp.type = (f.t === 'date' ? 'date' : 'text');\n    inp.placeholder = f.ph || ''; inp.value = data[f.k] || '';\n    inp.addEventListener('input', function () { data[f.k] = inp.value; touch(); });\n    box.appendChild(inp);\n    return box;\n  }\n\n  /* ---------- 現調シートの受け渡し（現場のスマホ → 事務所のPC） ----------\n     LINEなどに貼れるよう、読んで分かる文字にする。\n     同じ形式をそのまま読み戻せるので、ファイルのやりとりが要らない。 */\n\n  /** 項目名から定義を引けるようにした一覧を作る */\n  function surveyFieldMap() {\n    var map = {};\n    SURVEY_HEAD.forEach(function (f) { map[f.k] = f; });\n    SURVEY.forEach(function (s) {\n      s.pairs.forEach(function (p) { p.forEach(function (f) { map[f.k] = f; }); });\n    });\n    return map;\n  }\n\n  var SURVEY_MARK = '【空調王】現調シート';\n\n  function surveyToText(site) {\n    var live = findSite(site.id) || site;\n    var data = surveyOf(live);\n    var out = [SURVEY_MARK, '現場: ' + (live.name || ''), ''];\n\n    function push(f) {\n      var v = data[f.k];\n      if (f.t === 'check' || f.t === 'checktext') {\n        if (Array.isArray(v) && v.length) out.push(f.k + ': ' + v.join(', '));\n        var m = data[f.k + '_memo'];\n        if (String(m || '').trim()) out.push(f.k + '(記入): ' + m);\n      } else if (String(v || '').trim()) {\n        out.push(f.k + ': ' + v);\n      }\n    }\n    out.push('■ 基本情報');\n    SURVEY_HEAD.forEach(push);\n    SURVEY.forEach(function (s) {\n      var before = out.length;\n      out.push('', '■ ' + s.sec);\n      s.pairs.forEach(function (p) { p.forEach(push); });\n      if (out.length === before + 2) out.length = before;   // 何も無いセクションは出さない\n    });\n    return out.join('\\n');\n  }\n\n  function textToSurvey(text) {\n    if (String(text).indexOf(SURVEY_MARK) < 0) return null;\n    var map = surveyFieldMap();\n    var data = {};\n    var lastKey = null;\n    String(text).split(/\\r?\\n/).forEach(function (line) {\n      var m = line.match(/^([^:：]+)[:：][ 　]?(.*)$/);\n      var key = m ? m[1].trim() : null;\n      var memo = key && /\\(記入\\)$/.test(key);\n      var base = memo ? key.replace(/\\(記入\\)$/, '') : key;\n\n      if (key && map[base]) {\n        var f = map[base];\n        if (memo) { data[base + '_memo'] = m[2]; lastKey = base + '_memo'; }\n        else if (f.t === 'check' || f.t === 'checktext') {\n          data[base] = m[2].split(/[,、]/).map(function (x) { return x.trim(); })\n            .filter(function (x) { return f.o.indexOf(x) >= 0; });\n          lastKey = null;\n        } else { data[base] = m[2]; lastKey = base; }\n        return;\n      }\n      // 「見出し: 値」の形でない行は、直前の自由記入の続きとみなす（複数行メモ用）\n      if (lastKey && line.trim() && !/^■/.test(line) && !/^現場: /.test(line) && line !== SURVEY_MARK) {\n        data[lastKey] = (data[lastKey] || '') + '\\n' + line;\n      }\n    });\n    return data;\n  }\n\n  function copySurvey(site) {\n    var txt = surveyToText(site);\n    function done() { toast('コピーしました。LINEなどに貼り付けて送ってください'); }\n    if (navigator.clipboard && navigator.clipboard.writeText) {\n      navigator.clipboard.writeText(txt).then(done, function () { fallbackCopy(txt, done); });\n    } else fallbackCopy(txt, done);\n  }\n\n  function fallbackCopy(txt, done) {\n    var ta = document.createElement('textarea');\n    ta.value = txt;\n    ta.style.position = 'fixed'; ta.style.opacity = '0';\n    document.body.appendChild(ta);\n    ta.select();\n    try { document.execCommand('copy'); done(); }\n    catch (e) { prompt('この文字をコピーして送ってください', txt); }\n    document.body.removeChild(ta);\n  }\n\n  /* 現場一覧から貼り付けたとき。文字の中の「現場: ○○」を見て、\n     同じ名前の現場が無ければ作ってから取り込む（スマホで作った現場をPCに持ってくる用） */\n  $('#btn-paste-survey').addEventListener('click', function () {\n    var txt = prompt('現場から送られてきた文字を貼り付けて OK を押してください。\\n' +\n      '同じ名前の現場が無ければ、新しく作ります。');\n    if (txt === null) return;\n    var data = textToSurvey(txt);\n    if (!data) { toast('現調シートの文字ではないようです'); return; }\n    var n = surveyFilled(data);\n    if (!n) { toast('中身が読み取れませんでした'); return; }\n\n    var m = String(txt).match(/^現場:[ 　]?(.+)$/m);\n    var name = (m ? m[1] : '').trim() || String(data['案件名'] || '').trim();\n    if (!name) { toast('現場名が読み取れませんでした'); return; }\n\n    var sites = loadSites();\n    var hit = null;\n    sites.forEach(function (s) { if (s.name === name) hit = s; });\n\n    if (!confirm(hit\n      ? '現場「' + name + '」の現調シートを、' + n + '項目の内容で置き換えます。よろしいですか？'\n      : '現場「' + name + '」を新しく作って、' + n + '項目の現調シートを入れます。よろしいですか？')) return;\n\n    if (!hit) {\n      hit = {\n        id: 's' + Date.now() + Math.floor(Math.random() * 1000),\n        name: name,\n        customer: String(data['お客様名'] || '').trim(),\n        honorific: '御中',\n        address: String(data['現場住所'] || '').trim(),\n        tel: String(data['連絡先'] || '').trim(),\n        memo: '',\n        createdAt: new Date().toISOString()\n      };\n      sites.push(hit);\n      if (saveSites(sites) === false) return;\n    }\n    saveSurvey(hit.id, data);\n    openSiteId = hit.id;\n    renderList();\n    toast(n + '項目を取り込みました（' + name + '）');\n  });\n\n  function pasteSurvey(site) {\n    var txt = prompt('送られてきた文字を貼り付けて OK を押してください。\\n' +\n      '（このシートの内容は置き換わります）');\n    if (txt === null) return;\n    var data = textToSurvey(txt);\n    if (!data) { toast('現調シートの文字ではないようです'); return; }\n    var n = surveyFilled(data);\n    if (!n) { toast('中身が読み取れませんでした'); return; }\n    if (!confirm(n + '項目を読み取りました。\\nこの現場の現調シートを置き換えます。よろしいですか？')) return;\n    saveSurvey(site.id, data);\n    renderList();\n    toast(n + '項目を取り込みました');\n  }\n\n  /* ======================================================================\n     現場写真\n     ----------------------------------------------------------------------\n     撮った写真を、その現場にぶら下げて残す。\n\n     写真は1枚が数MBある。localStorage（5MB）にも、連動の1MBにも入らない。\n     そこで写真そのものは IndexedDB（この端末の中）に置き、\n     「何を撮ったか」の印だけを現調シートの［写真］の欄に書き戻す。\n     印は現場の中身なので、連動しているもう一方の端末にも届く。\n\n     撮った写真はそのままでは大きすぎるので、長いほうの辺を1600pxに縮めて\n     JPEGにし直してから入れる。紙に印刷するにはこれで十分足りる。\n     ====================================================================== */\n  var PHOTO_KINDS = ['全景', '銘板', '配管', '電源', '搬入経路', 'その他'];\n  var PHOTO_MAX = 1600;      // 長いほうの辺（これより大きい写真は縮める）\n  var PHOTO_Q   = 0.72;      // JPEGの画質\n  /* 1枚の大きさの上限。連動は1件あたり約1MBまでしか預けられず、\n     暗号にすると3割ほど太る。420KBに収めておけば必ず入る。 */\n  var PHOTO_BYTES = 420000;\n  /* 上限に入らなかったときに、順にゆるめていく組み合わせ */\n  var PHOTO_STEPS = [\n    { max: PHOTO_MAX, q: PHOTO_Q },\n    { max: PHOTO_MAX, q: 0.60 },\n    { max: 1280,      q: 0.60 },\n    { max: 1024,      q: 0.55 }\n  ];\n\n  function photoPut(rec)   { return idb('photos', 'readwrite', function (s) { return s.put(rec); }); }\n  function photoDel(id)    { return idb('photos', 'readwrite', function (s) { return s.delete(id); }); }\n  function photoAll()      { return idb('photos', 'readonly',  function (s) { return s.getAll(); }); }\n  function photoGet(id)    { return idb('photos', 'readonly',  function (s) { return s.get(id); }); }\n\n  /**\n   * 「この端末で写真をいじった」と連動に知らせる。\n   * 連動から届いた写真を入れるときは呼ばない（呼ぶと送り返してしまう）。\n   */\n  function photosChanged() {\n    if (window.AirtecSync) window.AirtecSync.changed('photos');\n  }\n  function photosOf(siteId) {\n    return idb('photos', 'readonly', function (s) { return s.index('siteId').getAll(siteId); })\n      .then(function (list) {\n        return (list || []).sort(function (a, b) { return String(a.at).localeCompare(String(b.at)); });\n      });\n  }\n\n  /**\n   * 写真を読み込む。\n   * iPhoneで撮った写真は「横向きのまま記録して、正しい向きは別に書いてある」ことがある。\n   * from-image と伝えないと、canvas に描いたとたん横倒しになる。\n   */\n  function loadBitmap(src) {\n    if (window.createImageBitmap) {\n      return createImageBitmap(src, { imageOrientation: 'from-image' })\n        .catch(function () { return createImageBitmap(src); })\n        .catch(function () { return loadViaImg(src); });\n    }\n    return loadViaImg(src);\n  }\n  function loadViaImg(src) {\n    return new Promise(function (res, rej) {\n      var url = URL.createObjectURL(src);\n      var im = new Image();\n      im.onload  = function () { URL.revokeObjectURL(url); res(im); };\n      im.onerror = function () { URL.revokeObjectURL(url); rej(new Error('画像として読めません')); };\n      im.src = url;\n    });\n  }\n\n  /** 決めた寸法・画質で1枚のJPEGを作る。turn を渡すとその角度だけ回してから描く */\n  function encodeAt(img, turn, maxSide, q) {\n    var scale = Math.min(1, maxSide / Math.max(img.width, img.height));\n    var dw = Math.round(img.width * scale), dh = Math.round(img.height * scale);\n    turn = ((turn || 0) % 360 + 360) % 360;\n    var sideways = (turn === 90 || turn === 270);\n    var cv = document.createElement('canvas');\n    cv.width  = sideways ? dh : dw;\n    cv.height = sideways ? dw : dh;\n    var cx = cv.getContext('2d');\n    cx.translate(cv.width / 2, cv.height / 2);\n    if (turn) cx.rotate(turn * Math.PI / 180);\n    cx.drawImage(img, -dw / 2, -dh / 2, dw, dh);\n    return new Promise(function (res, rej) {\n      cv.toBlob(function (b) {\n        if (b) res({ blob: b, w: cv.width, h: cv.height });\n        else rej(new Error('画像を作れませんでした'));\n      }, 'image/jpeg', q);\n    });\n  }\n\n  /**\n   * 写真を縮めてJPEGにする。turn を渡すとその角度だけ回してから作り直す。\n   * 上限（PHOTO_BYTES）に入るまで、画質→寸法の順にゆるめる。\n   * ここで必ず収めておかないと、連動に乗せられない1枚ができてしまう。\n   */\n  function shrinkImage(src, turn, steps) {\n    steps = steps || PHOTO_STEPS;\n    return loadBitmap(src).then(function (img) {\n      function attempt(i) {\n        return encodeAt(img, turn, steps[i].max, steps[i].q).then(function (r) {\n          if (r.blob.size <= PHOTO_BYTES || i >= steps.length - 1) return r;\n          return attempt(i + 1);\n        });\n      }\n      return attempt(0).then(function (r) {\n        if (img.close) img.close();\n        return r;\n      }, function (e) {\n        if (img.close) img.close();\n        throw e;\n      });\n    });\n  }\n\n  function photoSizeText(list) {\n    var mb = list.reduce(function (a, p) { return a + num(p.size); }, 0) / 1048576;\n    return mb < 0.1 ? '' : mb.toFixed(1) + 'MB';\n  }\n\n  /* 画面に出している写真の一時アドレス。作り直すたびに古いものを返しておく */\n  var photoUrls = [];\n  function freePhotoUrls() {\n    photoUrls.forEach(function (u) { URL.revokeObjectURL(u); });\n    photoUrls = [];\n  }\n  function photoUrl(blob) {\n    var u = URL.createObjectURL(blob);\n    photoUrls.push(u);\n    return u;\n  }\n\n  /** 選んだ（撮った）写真を、この現場に入れる */\n  function addPhotos(site, kind, files, after) {\n    var list = Array.prototype.slice.call(files || []);\n    if (!list.length) return;\n    toast(list.length + '枚を取り込んでいます…');\n\n    var ok = 0, ng = 0, full = false;\n    var chain = Promise.resolve();\n    list.forEach(function (f) {\n      chain = chain.then(function () {\n        if (!/^image\\//.test(f.type)) { ng++; return; }\n        return shrinkImage(f, 0).then(function (r) {\n          return photoPut({\n            id: 'ph' + Date.now() + Math.floor(Math.random() * 100000),\n            siteId: site.id,\n            kind: kind,\n            memo: '',\n            at: new Date().toISOString(),\n            w: r.w, h: r.h, size: r.blob.size,\n            blob: r.blob\n          });\n        }).then(function () { ok++; }, function (e) {\n          ng++;\n          if (e && (e.name === 'QuotaExceededError' || /quota/i.test(String(e.message || '')))) full = true;\n        });\n      });\n    });\n\n    chain.then(function () {\n      photosChanged();\n      if (after) after();\n      if (full) toast('端末の空きが足りません。いらない写真を消してからやり直してください');\n      else if (ng) toast(ok + '枚を入れました（' + ng + '枚は取り込めませんでした）');\n      else toast(ok + '枚を入れました');\n    });\n  }\n\n  /**\n   * 撮った種類を、現調シートの［写真］の欄に印として書き戻す。\n   * 写真そのものは端末に残るが、何を撮ったかは連動でもう一方の端末にも届く。\n   * 1枚も入れていないうちは触らない（紙だけで使っている人の印を消さないため）。\n   */\n  function syncPhotoCheck(siteId, list) {\n    if (!list || !list.length) return;\n    var live = findSite(siteId);\n    if (!live) return;\n    var f = surveyFieldMap()['写真'];\n    var allow = (f && f.o) ? f.o : [];\n    var has = {};\n    list.forEach(function (p) { has[p.kind] = true; });\n    var kinds = allow.filter(function (k) { return has[k]; });\n    var data = surveyOf(live);\n    var cur = Array.isArray(data['写真']) ? data['写真'] : [];\n    if (cur.join('|') === kinds.join('|')) return;      // 変わっていなければ保存しない\n    data['写真'] = kinds;\n    saveSurvey(live.id, data);\n  }\n\n  function renderPhotoBlock(site) {\n    var wrap = el('div', 'photo-block');\n\n    var head = el('div', 'survey-head');\n    head.appendChild(el('span', 'site-sec-label', '現場写真'));\n    var cnt = el('span', 'survey-count', '');\n    head.appendChild(cnt);\n    wrap.appendChild(head);\n\n    /* --------------------------------------------------------------------\n       2026-09-06、実機で確かめた。\n       iPhoneのSafariで［写真を撮る］を選んで撮った写真は、\n       iPhoneの写真アプリには残らない。空調王の中の1枚きりになる。\n       いつものカメラで撮ってから［フォトライブラリ］で選べば、両方に残る。\n       押す場所のすぐそばで知らせないと、気づくのは無くしたあとになる。\n       -------------------------------------------------------------------- */\n    if (window.matchMedia && window.matchMedia('(pointer: coarse)').matches) {\n      wrap.appendChild(el('p', 'photo-warn',\n        'その場で［写真を撮る］と、その写真はスマホの写真アプリには残りません（空調王の中だけになります）。' +\n        'あとで見返したい写真は、先にいつものカメラで撮ってから［フォトライブラリ］を選んでください。'));\n    }\n\n    var pick = el('div', 'photo-pick');\n    PHOTO_KINDS.forEach(function (k) {\n      var lab = el('label', 'btn btn-ghost btn-sm file-btn', '＋ ' + k);\n      lab.title = k + 'の写真を撮るか、写真から選びます';\n      var inp = el('input');\n      inp.type = 'file'; inp.accept = 'image/*'; inp.multiple = true;\n      inp.addEventListener('change', function () {\n        // 先に写しを取る。inp.value を空にすると inp.files も同時に空になるので、\n        // 参照のまま渡すと1枚も入らない\n        var files = Array.prototype.slice.call(inp.files || []);\n        inp.value = '';                    // 同じ写真をもう一度選べるようにする\n        addPhotos(site, k, files, function () { drawPhotos(site, grid, cnt); });\n      });\n      lab.appendChild(inp);\n      pick.appendChild(lab);\n    });\n    wrap.appendChild(pick);\n\n    var grid = el('div', 'photo-grid');\n    wrap.appendChild(grid);\n\n    wrap.appendChild(el('p', 'hint photo-note',\n      '写真はこの端末の中だけに残ります（見積の連動には乗りません）。' +\n      '端末を替えるときは［設定］の「写真を書き出す」で持ち出してください。' +\n      '［印刷 / PDF］を押すと、現調シートのうしろに写真のページが付きます。'));\n\n    drawPhotos(site, grid, cnt);\n    return wrap;\n  }\n\n  function drawPhotos(site, grid, cnt) {\n    photosOf(site.id).then(function (list) {\n      freePhotoUrls();\n      grid.innerHTML = '';\n      var sz = photoSizeText(list);\n      cnt.textContent = list.length + '枚' + (sz ? '　' + sz : '');\n      syncPhotoCheck(site.id, list);\n\n      if (!list.length) {\n        grid.appendChild(el('p', 'empty-note',\n          'まだ写真がありません。上のボタンを押すと、その場で撮るか、写真から選べます。'));\n        return;\n      }\n\n      list.forEach(function (p, i) {\n        var cell = el('div', 'photo-cell');\n\n        var im = el('img');\n        im.src = photoUrl(p.blob);\n        im.alt = p.kind || '写真';\n        // loading=\"lazy\" は付けない。写真は端末の中にあるので待つ必要がなく、\n        // 「画面に入るまで読まない」ぶん、かえって白いままになることがある\n        im.addEventListener('click', function () { openPhotoView(list, i); });\n        cell.appendChild(im);\n\n        var bar = el('div', 'photo-bar');\n        var sel = el('select', 'photo-kind');\n        PHOTO_KINDS.forEach(function (k) {\n          var o = el('option', null, k);\n          o.value = k;\n          if (k === p.kind) o.selected = true;\n          sel.appendChild(o);\n        });\n        sel.addEventListener('change', function () {\n          p.kind = sel.value;\n          photoPut(p).then(function () { photosChanged(); drawPhotos(site, grid, cnt); });\n        });\n        bar.appendChild(sel);\n\n        var rot = el('button', 'btn btn-ghost btn-sm', '回転'); rot.type = 'button';\n        rot.title = '横向きに写っているときに押してください';\n        rot.addEventListener('click', function () {\n          rot.disabled = true;\n          shrinkImage(p.blob, 90).then(function (r) {\n            p.blob = r.blob; p.w = r.w; p.h = r.h; p.size = r.blob.size;\n            return photoPut(p);\n          }).then(function () { photosChanged(); drawPhotos(site, grid, cnt); },\n                  function () { rot.disabled = false; toast('回転できませんでした'); });\n        });\n        bar.appendChild(rot);\n\n        var del = el('button', 'btn btn-ghost btn-sm btn-danger', '削除'); del.type = 'button';\n        del.addEventListener('click', function () {\n          if (!confirm('この写真を削除します。よろしいですか？')) return;\n          photoDel(p.id).then(function () {\n            photosChanged();\n            drawPhotos(site, grid, cnt);\n            toast('削除しました');\n          });\n        });\n        bar.appendChild(del);\n        cell.appendChild(bar);\n\n        var memo = el('input', 'photo-memo');\n        memo.type = 'text';\n        memo.value = p.memo || '';\n        memo.placeholder = '覚え書き';\n        memo.title = '写真の説明。印刷したときに写真の下に出ます（例：室外機の裏、サビあり）';\n        memo.addEventListener('change', function () {\n          p.memo = memo.value;\n          photoPut(p).then(photosChanged);\n        });\n        cell.appendChild(memo);\n\n        grid.appendChild(cell);\n      });\n    }, function () {\n      grid.innerHTML = '';\n      grid.appendChild(el('p', 'empty-note', '写真を読み出せませんでした。'));\n    });\n  }\n\n  /** 写真を大きく見る */\n  function openPhotoView(list, idx) {\n    var box = el('div', 'photo-view');\n    var im = el('img');\n    var cap = el('div', 'photo-view-cap');\n    var url = '';\n\n    function show(i) {\n      idx = (i + list.length) % list.length;\n      if (url) URL.revokeObjectURL(url);\n      url = URL.createObjectURL(list[idx].blob);\n      im.src = url;\n      var p = list[idx];\n      cap.textContent = (idx + 1) + ' / ' + list.length + '　' +\n        (p.kind || '') + (p.memo ? '　' + p.memo : '');\n    }\n    function close() {\n      if (url) URL.revokeObjectURL(url);\n      document.removeEventListener('keydown', onKey);\n      if (box.parentNode) box.parentNode.removeChild(box);\n    }\n    function onKey(e) {\n      if (e.key === 'Escape') close();\n      else if (e.key === 'ArrowRight') show(idx + 1);\n      else if (e.key === 'ArrowLeft')  show(idx - 1);\n    }\n\n    var prev = el('button', 'photo-nav photo-prev', '‹'); prev.type = 'button';\n    var next = el('button', 'photo-nav photo-next', '›'); next.type = 'button';\n    var shut = el('button', 'photo-close', '閉じる');     shut.type = 'button';\n    prev.addEventListener('click', function (e) { e.stopPropagation(); show(idx - 1); });\n    next.addEventListener('click', function (e) { e.stopPropagation(); show(idx + 1); });\n    shut.addEventListener('click', close);\n    box.addEventListener('click', function (e) { if (e.target === box || e.target === im) close(); });\n    document.addEventListener('keydown', onKey);\n\n    box.appendChild(im); box.appendChild(cap);\n    if (list.length > 1) { box.appendChild(prev); box.appendChild(next); }\n    box.appendChild(shut);\n    document.body.appendChild(box);\n    show(idx);\n  }\n\n  /* ---------- 写真の持ち出し・持ち込み ----------\n     写真は全データのバックアップには入れない（毎日の上書き保存が重くなるため）。\n     端末を替えるときだけ、この専用のファイルで運ぶ。 */\n  function blobToDataUrl(b) {\n    return new Promise(function (res, rej) {\n      var r = new FileReader();\n      r.onload  = function () { res(String(r.result)); };\n      r.onerror = function () { rej(r.error); };\n      r.readAsDataURL(b);\n    });\n  }\n  function dataUrlToBlob(d) {\n    var m = String(d || '').match(/^data:(image\\/[a-z+]+);base64,([A-Za-z0-9+/=]+)$/i);\n    if (!m) return null;\n    var bin = atob(m[2]), arr = new Uint8Array(bin.length);\n    for (var i = 0; i < bin.length; i++) arr[i] = bin.charCodeAt(i);\n    return new Blob([arr], { type: m[1] });\n  }\n\n  $('#btn-export-photos').addEventListener('click', function () {\n    photoAll().then(function (list) {\n      list = list || [];\n      if (!list.length) { toast('写真がまだありません'); return; }\n      var names = {};\n      loadSites().forEach(function (s) { names[s.id] = s.name; });\n      return Promise.all(list.map(function (p) {\n        return blobToDataUrl(p.blob).then(function (d) {\n          return {\n            id: p.id, siteId: p.siteId, siteName: names[p.siteId] || '',\n            kind: p.kind, memo: p.memo, at: p.at, w: p.w, h: p.h, data: d\n          };\n        });\n      })).then(function (rows) {\n        download('空調王-現場写真-' + todayISO() + '.json', JSON.stringify({\n          type: 'airtec-photos', version: 1,\n          exportedAt: new Date().toISOString(),\n          photos: rows\n        }));\n        toast(rows.length + '枚を書き出しました');\n      });\n    }).catch(function () { toast('書き出せませんでした'); });\n  });\n\n  $('#file-import-photos').addEventListener('change', function (ev) {\n    readJSON(ev.target, function (data) {\n      if (!data || data.type !== 'airtec-photos' || !Array.isArray(data.photos)) {\n        toast('「写真を書き出す」で作ったファイルを選んでください');\n        return;\n      }\n      var sites = loadSites();\n      var byId = {}, byName = {};\n      sites.forEach(function (s) { byId[s.id] = s; byName[s.name] = s; });\n\n      var rows = [], lost = 0;\n      data.photos.forEach(function (p) {\n        var site = byId[p.siteId] || byName[p.siteName];      // 現場が作り直されていても名前で拾う\n        var blob = dataUrlToBlob(p.data);\n        if (!site || !blob) { lost++; return; }\n        rows.push({\n          id: p.id, siteId: site.id, kind: p.kind || 'その他', memo: p.memo || '',\n          at: p.at || new Date().toISOString(), w: p.w, h: p.h, size: blob.size, blob: blob\n        });\n      });\n      if (!rows.length) { toast('入れられる写真がありませんでした（先に現場を読み込んでください）'); return; }\n      if (!confirm(rows.length + '枚をこの端末に入れます。\\n' +\n        (lost ? '（' + lost + '枚は、行き先の現場が無いので入れません）\\n' : '') +\n        '同じ写真がすでにあれば上書きします。よろしいですか？')) return;\n\n      var chain = Promise.resolve(), ok = 0, ng = 0;\n      rows.forEach(function (r) {\n        chain = chain.then(function () {\n          return photoPut(r).then(function () { ok++; }, function () { ng++; });\n        });\n      });\n      chain.then(function () {\n        photosChanged();\n        renderList();\n        toast(ok + '枚を入れました' + (ng ? '（' + ng + '枚は入りませんでした）' : ''));\n      });\n    });\n  });\n\n  /* ----------------------------------------------------------------------\n     連動（sync.js）から写真を出し入れするための窓口\n     ----------------------------------------------------------------------\n     写真は IndexedDB の中にあって sync.js からは触れない。\n     暗号のカギは sync.js の中にあって app.js からは触れない。\n     そこで「写真そのもの」だけをここでやり取りする。\n     持ち運ぶ形は data:image/jpeg;base64,… の文字列（JSONに入れられる形）。\n\n     大事なきまり：ここから入れた写真では photosChanged() を呼ばない。\n     呼ぶと「この端末で撮った」ことになって、もらった写真を送り返してしまう。\n     ---------------------------------------------------------------------- */\n  function photoMeta(p) {\n    return {\n      id: p.id, siteId: p.siteId, kind: p.kind, memo: p.memo || '',\n      at: p.at, w: p.w, h: p.h, size: p.size\n    };\n  }\n\n  window.AirtecPhotos = {\n    /** 写真そのものは付けずに、一覧だけ返す */\n    list: function () {\n      return photoAll().then(function (rows) { return (rows || []).map(photoMeta); });\n    },\n\n    /** 1枚を、持ち運べる形にして返す */\n    get: function (id) {\n      return photoGet(id).then(function (p) {\n        if (!p) return null;\n        return blobToDataUrl(p.blob).then(function (d) {\n          var m = photoMeta(p);\n          m.data = d;\n          return m;\n        });\n      });\n    },\n\n    /** もらった1枚を入れる（同じidがあれば上書き） */\n    put: function (rec) {\n      var blob = dataUrlToBlob(rec && rec.data);\n      if (!blob) return Promise.reject(new Error('画像として読めません'));\n      return photoPut({\n        id: rec.id,\n        siteId: rec.siteId,\n        kind: PHOTO_KINDS.indexOf(rec.kind) >= 0 ? rec.kind : 'その他',\n        memo: String(rec.memo || ''),\n        at: rec.at || new Date().toISOString(),\n        w: rec.w, h: rec.h, size: blob.size,\n        blob: blob\n      });\n    },\n\n    /** 相手の端末で直された種類・覚え書きを、こちらにも反映する */\n    setMeta: function (id, m) {\n      return photoGet(id).then(function (p) {\n        if (!p) return false;\n        if (PHOTO_KINDS.indexOf(m.kind) >= 0) p.kind = m.kind;\n        p.memo = String(m.memo || '');\n        if (m.siteId) p.siteId = m.siteId;\n        return photoPut(p).then(function () { return true; });\n      });\n    },\n\n    del: function (id) { return photoDel(id); },\n\n    /** 預けるには大きすぎた1枚を、もう一段小さくして入れ直す */\n    reshrink: function (id) {\n      return photoGet(id).then(function (p) {\n        if (!p) return false;\n        return shrinkImage(p.blob, 0, [{ max: 1024, q: 0.5 }]).then(function (r) {\n          p.blob = r.blob; p.w = r.w; p.h = r.h; p.size = r.blob.size;\n          return photoPut(p).then(function () { return true; });\n        }, function () { return false; });\n      });\n    },\n\n    /** 連動で写真が入れ替わったら、開いている現場を描き直す */\n    refresh: function () { if (openSiteId) renderList(); }\n  };\n\n  /** 調査シートを紙と同じ形で印刷する。blank=true なら何も記入しない状態で出す */\n  function printSurvey(site, blank) {\n    // 画面が持っている現場は入力前の写しのことがあるので、保存済みを読み直す\n    var live = findSite(site.id) || site;\n    var data = blank ? {} : surveyOf(live);\n    site = live;\n    var esc2 = function (v) { return esc(String(v == null ? '' : v)); };\n\n    function cell(f) {\n      if (!f) return '<th class=\"sv-k\"></th><td class=\"sv-v\"></td>';\n      var v = '';\n      if (f.t === 'check' || f.t === 'checktext') {\n        var picked = Array.isArray(data[f.k]) ? data[f.k] : [];\n        v = f.o.map(function (o) {\n          return '<span class=\"sv-o\">' + (picked.indexOf(o) >= 0 ? '☑' : '☐') + esc2(o) + '</span>';\n        }).join('');\n        if (f.t === 'checktext') v += '<span class=\"sv-sub\">' + esc2(data[f.k + '_memo'] || (blank ? f.ph : '')) + '</span>';\n      } else {\n        var raw = data[f.k];\n        v = raw ? esc2(raw).replace(/\\n/g, '<br>') : (blank && f.ph ? '<span class=\"sv-ph\">' + esc2(f.ph) + '</span>' : '');\n      }\n      return '<th class=\"sv-k\">' + esc2(f.k) + '</th><td class=\"sv-v\">' + v + '</td>';\n    }\n\n    var html = '<div class=\"sheet-page sv-page\">';\n    html += '<div class=\"sv-title\">店舗・事務所・工場用　空調設備現場調査確認表</div>';\n\n    html += '<table class=\"sv-tbl sv-head\">';\n    for (var i = 0; i < SURVEY_HEAD.length; i += 2) {\n      html += '<tr>' + cell(SURVEY_HEAD[i]) + cell(SURVEY_HEAD[i + 1]) + '</tr>';\n    }\n    html += '</table>';\n\n    SURVEY.forEach(function (sec) {\n      html += '<div class=\"sv-sec\">' + esc2(sec.sec) + '</div>';\n      html += '<table class=\"sv-tbl\">';\n      sec.pairs.forEach(function (p) {\n        html += '<tr>' + cell(p[0]) + cell(p[1]) + '</tr>';\n      });\n      html += '</table>';\n    });\n    html += '</div>';\n\n    document.title = '現場調査確認表_' + (site.name || '');\n    freePrintUrls();\n    if (blank) { showSheetAndPrint(html); return; }\n\n    // 記入済みで出すときは、うしろに写真のページを足す\n    photosOf(site.id).then(function (list) {\n      showSheetAndPrint(html + photoPagesHtml(list, site));\n    }, function () { showSheetAndPrint(html); });\n  }\n\n  /* 印刷に出している写真の一時アドレス。次に印刷するときに返す */\n  var printUrls = [];\n  function freePrintUrls() {\n    printUrls.forEach(function (u) { URL.revokeObjectURL(u); });\n    printUrls = [];\n  }\n\n  /** 写真のページを組み立てる（1枚の紙に6枚） */\n  function photoPagesHtml(list, site) {\n    if (!list || !list.length) return '';\n    var out = '', per = 6;\n    for (var i = 0; i < list.length; i += per) {\n      out += '<div class=\"sheet-page sv-page ph-page\">';\n      out += '<div class=\"sv-title\">現場写真　' + esc(site.name || '') + '</div>';\n      out += '<div class=\"ph-grid\">';\n      list.slice(i, i + per).forEach(function (p, j) {\n        var u = URL.createObjectURL(p.blob);\n        printUrls.push(u);\n        out += '<figure class=\"ph-cell\"><img src=\"' + esc(u) + '\" alt=\"\">' +\n          '<figcaption>' + (i + j + 1) + '. ' + esc(p.kind || '') +\n          (p.memo ? '　' + esc(p.memo) : '') + '</figcaption></figure>';\n      });\n      out += '</div></div>';\n    }\n    return out;\n  }\n\n  /**\n   * 紙面を画面に組んでから印刷する。\n   * 写真は読み込みが終わる前に印刷を始めると、白いままの紙が出てしまう。\n   * だから全部の画像が出そろうのを待ってから window.print() を呼ぶ。\n   */\n  function showSheetAndPrint(html) {\n    // 現調シートは見積書ではないので、見積書の見た目は外す\n    $('#sheet').className = '';\n    $('#sheet').innerHTML = html;\n    var imgs = $$('#sheet img');\n    var waits = imgs.map(function (im) {\n      if (im.complete) return Promise.resolve();\n      return new Promise(function (res) { im.onload = im.onerror = res; });\n    });\n    Promise.all(waits).then(function () {\n      setTimeout(function () { window.print(); }, 60);\n    });\n  }\n\n  function openEstimate(e, msg) {\n    st = clone(e);\n    delete st.total; delete st.savedAt;\n    if (st.unitRound == null) st.unitRound = 0;   // この設定より前に保存した見積\n    if (st.manDayYen == null) st.manDayYen = num(pb.defaults.manDayYen);\n    syncManDayToQuote();\n    fillMissingLineCosts();      // 保存したあとに入れた仕入掛率を、この見積にも効かせる\n    fillMeta(); renderLines(); save(KEY_DRAFT, st);\n    $('.tab[data-view=\"edit\"]').click();\n    toast(msg || '読み込みました');\n  }\n\n  /** その現場の情報を引き継いで、新しい見積を始める */\n  function newEstimateForSite(s) {\n    st = newState();\n    syncManDayToQuote();\n    st.siteId = s.id;\n    st.customer = s.customer;\n    st.honorific = s.honorific || '御中';\n    st.subject = s.name;\n    st.site = s.address;\n    fillMeta(); renderLines(); save(KEY_DRAFT, st);\n    $('.tab[data-view=\"edit\"]').click();\n    toast('「' + s.name + '」の見積を作ります');\n  }\n\n  $('#btn-export-estimates').addEventListener('click', function () {\n    // 現場と見積はセットでないと意味がないので、1つのファイルにまとめて出す\n    var bundle = { type: 'airtec-sites', sites: loadSites(), estimates: load(KEY_EST, []), invoices: loadInvoices() };\n    download('50airtec-現場データ-' + todayISO() + '.json', JSON.stringify(bundle, null, 2));\n  });\n  $('#file-import-estimates').addEventListener('change', function (ev) {\n    readJSON(ev.target, function (data) {\n      // 昔のバックアップは見積だけの配列。どちらでも読めるようにしておく\n      var sites = null, ests = null, invs = null;\n      if (Array.isArray(data)) { ests = data; }\n      else if (data && Array.isArray(data.estimates)) { ests = data.estimates; sites = data.sites || []; invs = data.invoices || []; }\n      if (!ests) { toast('見積データの形式が違います'); return; }\n\n      if (!confirm('保存済みの現場と見積を、読み込んだファイルの内容で置き換えます。よろしいですか？\\n\\n' +\n        '　現場：' + (sites ? sites.length + '件' : '（入っていません。件名から作り直します）') + '\\n' +\n        '　見積：' + ests.length + '件\\n' +\n        '　請求書：' + (invs ? invs.length + '件' : '（入っていません）'))) return;\n\n      save(KEY_EST, ests);\n      if (sites) saveSites(sites);\n      if (invs) save(KEY_INV, invs);\n      openSiteId = null;\n      renderList();              // 現場が無い古いデータは、ここで自動的にふり分けられる\n      toast('読み込みました');\n    });\n  });\n\n  function readJSON(input, cb) {\n    var f = input.files && input.files[0];\n    if (!f) return;\n    var r = new FileReader();\n    r.onload = function () {\n      try { cb(JSON.parse(r.result)); }\n      catch (e) { toast('ファイルを読めませんでした'); }\n      input.value = '';\n    };\n    r.readAsText(f, 'utf-8');\n  }\n\n  /* ======================================================================\n     単価マスタ編集\n     ====================================================================== */\n  var MASTER_PAGE = 200;   // 一度に描く行数の上限（多いと画面が重くなるため）\n\n  function renderMaster() {\n    renderCsvTargets();\n    var box = $('#master-editor');\n    box.innerHTML = '';\n\n    pb.categories.forEach(function (cat, ci) {\n      var d = el('details', 'cat-block');\n      var sm = el('summary');\n      var count = el('span', 'cat-count');\n      sm.appendChild(document.createTextNode(cat.name));\n      sm.appendChild(count);\n      d.appendChild(sm);\n\n      var body = el('div', 'cat-body');\n      d.appendChild(body);\n\n      var refresh = function () { count.textContent = '（' + cat.items.length + '項目）'; };\n      refresh();\n\n      // 開いたときに初めて中身を作る（閉じたままの分類は描かない）\n      var built = false;\n      var build = function () { built = true; buildCatBody(cat, ci, body, refresh); };\n      d.addEventListener('toggle', function () { if (d.open && !built) build(); });\n      if (ci === 0) { d.open = true; build(); }\n\n      box.appendChild(d);\n    });\n  }\n\n  /** ひとつの分類の中身（検索欄・行・操作ボタン）を組み立てる */\n  function buildCatBody(cat, ci, body, refreshCount) {\n    body.innerHTML = '';\n\n    var rowsBox = el('div', 'cat-rows');\n    var filter = '';\n    var onAddBlank = null;   // 「＋項目を追加」と同じ動きを行から呼べるようにする\n\n    // 項目が多い分類には、その中を探すための検索欄を出す\n    if (cat.items.length > 30) {\n      var fwrap = el('div', 'cat-filter');\n      var fi = el('input');\n      fi.type = 'search';\n      fi.placeholder = 'この分類の中を品番・品名で探す';\n      fi.addEventListener('input', function () { filter = fi.value; drawRows(); });\n      fwrap.appendChild(fi);\n      body.appendChild(fwrap);\n    }\n\n    var head = el('div', 'mrow mrow-head');\n    // 末尾の2つは「製品ページ」ボタンと「削除」ボタンの列（見出しは無し）\n    ['品番', '品名', '規格・仕様', '単位', '人工', '原価', '単価', '色', '', ''].forEach(function (h, i) {\n      head.appendChild(el('div', i === 5 ? 'mcol-cost' : null, h));\n    });\n    body.appendChild(head);\n    body.appendChild(rowsBox);\n\n    function drawRows() {\n      rowsBox.innerHTML = '';\n      var q = filter.trim().toLowerCase();\n      var matched = [];\n      cat.items.forEach(function (item, ii) {\n        if (!q) { matched.push([item, ii]); return; }\n        var hay = ((item.code || '') + ' ' + item.name + ' ' + (item.spec || '')).toLowerCase();\n        if (hay.indexOf(q) >= 0) matched.push([item, ii]);\n      });\n\n      matched.slice(0, MASTER_PAGE).forEach(function (m) {\n        rowsBox.appendChild(masterRow(cat, m[0], m[1], function () {\n          if (refreshCount) refreshCount();\n          drawRows();\n        }, function () { if (onAddBlank) onAddBlank(); }));\n      });\n\n      if (!matched.length) {\n        rowsBox.appendChild(el('p', 'picker-empty', '該当する項目がありません。'));\n      } else if (matched.length > MASTER_PAGE) {\n        rowsBox.appendChild(el('p', 'picker-empty',\n          matched.length + '件のうち ' + MASTER_PAGE + '件を表示しています。上の検索でしぼり込んでください。'));\n      }\n    }\n    drawRows();\n\n    var actions = el('div', 'card-actions');\n    actions.style.marginTop = '10px';\n    actions.style.marginLeft = '0';\n\n    var addItem = el('button', 'btn btn-ghost', '＋ 項目を追加'); addItem.type = 'button';\n    addItem.addEventListener('click', function () { addBlankItem(); });\n\n    // 空の行を足して、すぐ品番から打ち始められるようにする\n    function addBlankItem() {\n      cat.items.push({ code: '', name: '', spec: '', unit: '', manDay: 0, price: 0 });\n      savePB(); renderPicker();\n      if (refreshCount) refreshCount();\n      filter = '';\n      var f = body.querySelector('.cat-filter input');\n      if (f) f.value = '';\n      drawRows();\n      var rows = rowsBox.querySelectorAll('.mrow');\n      var last = rows[rows.length - 1];\n      if (last) {\n        last.scrollIntoView({ block: 'center' });\n        var first = last.querySelector('input');\n        if (first) first.focus();\n      }\n    }\n    onAddBlank = addBlankItem;\n\n    var renCat = el('button', 'btn btn-ghost', 'カテゴリ名を変更'); renCat.type = 'button';\n    renCat.addEventListener('click', function () {\n      var v = prompt('カテゴリ名', cat.name);\n      if (v == null) return;\n      cat.name = v.trim() || cat.name;\n      savePB(); renderMaster(); renderPicker();\n    });\n\n    var delCat = el('button', 'btn btn-ghost btn-danger', 'カテゴリを削除'); delCat.type = 'button';\n    delCat.addEventListener('click', function () {\n      if (!confirm('カテゴリ「' + cat.name + '」を中の項目ごと削除します。よろしいですか？')) return;\n      pb.categories.splice(ci, 1);\n      if (activeCat === cat.id) activeCat = pb.categories.length ? pb.categories[0].id : null;\n      savePB(); renderMaster(); renderPicker();\n    });\n\n    actions.appendChild(addItem);\n    actions.appendChild(renCat);\n    actions.appendChild(delCat);\n    body.appendChild(actions);\n  }\n\n  function masterRow(cat, item, ii, onDelete, onEnterAtEnd) {\n    var row = el('div', 'mrow');\n\n    function inp(val, cls, type, onchange, placeholder) {\n      var i = el('input', cls);\n      i.type = type || 'text';\n      i.value = val;\n      if (placeholder) i.placeholder = placeholder;\n      i.addEventListener('input', function () { onchange(i.value); savePBDebounced(); });\n      return i;\n    }\n\n    row.appendChild(inp(item.code || '', 'm-code', 'text', function (v) { item.code = v; }, '品番'));\n    row.appendChild(inp(item.name, null, 'text', function (v) { item.name = v; }, '品名'));\n    row.appendChild(inp(item.spec || '', null, 'text', function (v) { item.spec = v; }, '規格・仕様'));\n    row.appendChild(inp(item.unit, null, 'text', function (v) { item.unit = v; }, '個'));\n\n    // 消耗品雑費のような「作業費の◯%」の項目は、金額ではなく割合を入れてもらう\n    var isAuto = !!num(item.autoPercent);\n\n    // 人工（にんく）。0.1きざみで入れると、単価は［人工 × 1人工の金額］で決まる\n    var mdCell = el('span', 'm-manday');\n    var mdInput = null;\n    if (isAuto) {\n      mdCell.appendChild(el('i', null, '—'));\n    } else {\n      mdInput = inp(num(item.manDay) || '', 'm-md', 'number', function (v) {\n        item.manDay = num(v);\n        if (!item.manDay) delete item.manDay;\n        syncPrice();\n      }, '0.0');\n      mdInput.step = '0.1';\n      mdInput.min = '0';\n      mdInput.title = 'この作業が何人工かを0.1きざみで入れます。空ならふつうの単価として扱います';\n      mdCell.appendChild(mdInput);\n    }\n    row.appendChild(mdCell);\n    // 「0.1人工 × ¥30,000」を画面に出す札（マウスを乗せなくても読める）\n    var mdNote = el('small', 'm-mdnote');\n\n    /* 空調王が最初から持っている金額は「目安」。\n       買った人が自社の金額だと思い込まないよう、直すまで札を出しておく。\n       単価を打った時点で外れる（＝その人の金額になった） */\n    var estNote = el('small', 'm-estnote', item.est ? '目安' : '');\n    if (item.est) estNote.title = '空調王が最初から入れている目安の金額です。自社の金額に直してください';\n\n    // 原価（社内用）。空なら人工や仕入掛率から見当をつける\n    var costCell = el('span', 'm-manday mcol-cost');\n    var costInput = inp(num(item.cost) || '', 'm-cost', 'number', function (v) {\n      item.cost = num(v);\n      if (!item.cost) delete item.cost;\n    }, String(itemCost(item, cat) || '—'));\n    costInput.title = '仕入れ値・原価。空なら人工や仕入掛率から見当をつけます';\n    costCell.appendChild(costInput);\n    row.appendChild(costCell);\n    var priceInput = isAuto\n      ? inp(item.autoPercent, 'm-price', 'number', function (v) { item.autoPercent = num(v); })\n      : inp(item.price, 'm-price', 'number', function (v) {\n          item.price = num(v);\n          // 手で打った金額が、次に1人工の金額を変えたときに上書きされないよう、\n          // ここで人工を外す。人工で出したいなら人工の欄に入れ直せばよい\n          if (num(item.manDay)) {\n            delete item.manDay;\n            if (mdInput) mdInput.value = '';\n            mdNote.textContent = '';\n          }\n          delete item.est;          // 目安の札を外す（自分の金額になったので）\n          if (estNote) estNote.textContent = '';\n        });\n    // 単価まで打ったら Enter で次の行へ。続けて打ち込めるようにする\n    priceInput.addEventListener('keydown', function (ev) {\n      if (ev.key !== 'Enter') return;\n      ev.preventDefault();\n      savePB();\n      if (onEnterAtEnd) onEnterAtEnd();\n    });\n    /**\n     * 人工が入っている項目の単価は［人工 × 1人工の金額］で決まる。\n     * ただし打てなくはしない。前は readOnly にしていたが、\n     * スマホでは「なぜ打てないのか」を出す方法（マウスを乗せる）が無く、\n     * 理由の分からない数字がそこにあるだけになっていた。\n     * いまは打てる。打った時点で、その項目は人工から離れる。\n     */\n    function syncPrice() {\n      if (isAuto) return;\n      var md = num(item.manDay);\n      if (md > 0) {\n        item.price = manDayPrice(md);\n        priceInput.value = item.price;\n      }\n      priceInput.title = md > 0\n        ? md + '人工 × ' + yen(num(st.manDayYen)) + ' ＝ ' + yen(item.price) + '（単価を打つと人工から外れます）'\n        : '';\n      // マウスの無い端末でも分かるように、画面にも出す\n      mdNote.textContent = md > 0\n        ? md + '人工 × ' + yen(num(st.manDayYen))\n        : '';\n      // 原価の見当（うすい文字）も、人工に合わせて出し直す\n      costInput.placeholder = String(itemCost(item, cat) || '—');\n    }\n\n    if (isAuto) {\n      priceInput.title = autoBaseOf(item) === 'work'\n        ? '作業費（家庭用・業務用・移設から入れた行）の合計にかける割合です'\n        : '作業費と消耗品雑費を足した金額にかける割合です';\n      var pcell = el('span', 'm-auto');\n      pcell.appendChild(priceInput);\n      pcell.appendChild(el('i', null, '% ' + AUTO_BASES[autoBaseOf(item)].short));\n      row.appendChild(pcell);\n    } else {\n      var pcell2 = el('span', 'm-pricecell');\n      pcell2.appendChild(priceInput);\n      pcell2.appendChild(mdNote);\n      pcell2.appendChild(estNote);\n      row.appendChild(pcell2);\n      syncPrice();\n    }\n\n    // 色分け。取付の形など、ぱっと見分けたいものに使う\n    var colSel = el('select', 'm-color');\n    colSel.title = 'この項目の文字色。取付の形などを見分けるのに使います';\n    var COLOR_ORDER = ['', '青', '水', '緑', '橙', '赤', '桃', '紫', '茶', '灰'];\n    COLOR_ORDER.forEach(function (name) {\n      var op = el('option', null, name || '—');\n      op.value = name;\n      var c = itemColor(name);\n      if (c) op.style.color = c;\n      if ((item.color || '') === name) op.selected = true;\n      colSel.appendChild(op);\n    });\n    // 選択肢に無い色（CSVで色コードを入れた場合など）も残す\n    if (item.color && COLOR_ORDER.indexOf(item.color) < 0) {\n      var opX = el('option', null, item.color);\n      opX.value = item.color; opX.selected = true;\n      colSel.insertBefore(opX, colSel.firstChild);\n    }\n    function paintRow() {\n      var c = itemColor(item.color);\n      row.style.color = c || '';\n      colSel.style.color = c || '';\n    }\n    colSel.addEventListener('change', function () {\n      item.color = colSel.value;\n      if (!item.color) delete item.color;\n      paintRow();\n      savePBDebounced();\n    });\n    paintRow();\n    row.appendChild(colSel);\n\n    // URLが無い行でも列がずれないよう、入れ物は必ず置く\n    var refCell = el('span', 'm-ref');\n    var ref = refButton(item.url, item.name);\n    if (ref) refCell.appendChild(ref);\n    row.appendChild(refCell);\n\n    var del = el('button', 'icon-btn', '✕'); del.type = 'button'; del.title = 'この項目を削除';\n    del.addEventListener('click', function () {\n      cat.items.splice(ii, 1);\n      savePB(); renderPicker();\n      if (onDelete) onDelete();\n    });\n    row.appendChild(del);\n    return row;\n  }\n  /* ======================================================================\n     CSV取り込み（メーカーの価格表・自作の単価表をまとめて読み込む）\n     ====================================================================== */\n\n  /** 日本のExcelが書き出すCSVは Shift_JIS のことが多いので、文字化けしたら読み直す */\n  function decodeCSV(buffer) {\n    var bytes = new Uint8Array(buffer);\n    // BOM付きUTF-8\n    if (bytes[0] === 0xEF && bytes[1] === 0xBB && bytes[2] === 0xBF) {\n      return new TextDecoder('utf-8').decode(bytes.subarray(3));\n    }\n    var utf8 = new TextDecoder('utf-8').decode(bytes);\n    if (utf8.indexOf('�') < 0) return utf8;      // 文字化けなし＝UTF-8\n    try { return new TextDecoder('shift_jis').decode(bytes); }\n    catch (e) { return utf8; }\n  }\n\n  /** カンマ区切りを1行ずつ配列に。ダブルクォートで囲まれたカンマ・改行にも対応 */\n  function parseCSV(text) {\n    var rows = [], row = [], cur = '', inQ = false;\n    for (var i = 0; i < text.length; i++) {\n      var ch = text[i];\n      if (inQ) {\n        if (ch === '\"') {\n          if (text[i + 1] === '\"') { cur += '\"'; i++; }\n          else inQ = false;\n        } else cur += ch;\n      } else if (ch === '\"') {\n        inQ = true;\n      } else if (ch === ',' || ch === '\\t') {\n        row.push(cur); cur = '';\n      } else if (ch === '\\n') {\n        row.push(cur); rows.push(row); row = []; cur = '';\n      } else if (ch !== '\\r') {\n        cur += ch;\n      }\n    }\n    if (cur !== '' || row.length) { row.push(cur); rows.push(row); }\n    // 空行を落とす\n    return rows.filter(function (r) {\n      return r.some(function (c) { return String(c).trim() !== ''; });\n    });\n  }\n\n  // 見出しの言い方はバラバラなので、よくある呼び方をまとめて受け取る\n  var CSV_ALIASES = {\n    category: ['カテゴリ', 'カテゴリー', '分類', '大分類', 'category'],\n    code:     ['品番', '型番', '品番／型番', '品番/型番', '商品コード', 'コード', 'code', '品目コード'],\n    name:     ['品名', '名称', '商品名', '製品名', '品名・仕様', 'name'],\n    spec:     ['規格', '仕様', 'サイズ', '規格・仕様', 'spec'],\n    url:      ['URL', 'ＵＲＬ', 'リンク', '製品ページ', '参考URL', 'ページ', 'url', 'link'],\n    color:    ['色', '色分け', '文字色', 'カラー', 'color'],\n    unit:     ['単位', 'unit'],\n    price:    ['定価', '単価', '価格', '金額', '希望小売価格', '標準価格', 'price'],\n    manDay:   ['人工', '人工数', '作業人工', '歩掛', '歩掛り', 'manday'],\n    cost:     ['原価', '仕入', '仕入値', '仕入単価', '仕入価格', '仕切', '仕切価格', 'cost']\n  };\n\n  function normalizeHeader(s) {\n    return String(s || '').trim().replace(/\\s+/g, '').replace(/[（(].*?[）)]/g, '').toLowerCase();\n  }\n\n  /** 1行目が見出しなら列の位置を返す。見出しでなければ null */\n  function detectColumns(headerRow) {\n    var map = {}, hit = 0;\n    headerRow.forEach(function (cell, idx) {\n      var h = normalizeHeader(cell);\n      Object.keys(CSV_ALIASES).forEach(function (key) {\n        if (map[key] != null) return;\n        if (CSV_ALIASES[key].some(function (a) { return normalizeHeader(a) === h; })) {\n          map[key] = idx; hit++;\n        }\n      });\n    });\n    // 品名か品番、どちらかと価格が見つかれば見出し行とみなす\n    if (hit >= 2 && (map.name != null || map.code != null)) return map;\n    return null;\n  }\n\n  function toPrice(v) {\n    // 「¥1,200」「1,200円」なども数値にする\n    var s = String(v == null ? '' : v).replace(/[¥￥,，\\s円]/g, '');\n    s = s.replace(/[０-９．－]/g, function (c) {\n      return String.fromCharCode(c.charCodeAt(0) - 0xFEE0);\n    });\n    var n = parseFloat(s);\n    return isFinite(n) ? n : 0;\n  }\n\n  function findOrCreateCategory(name) {\n    var target = null;\n    pb.categories.forEach(function (c) { if (c.name === name) target = c; });\n    if (!target) {\n      target = { id: 'c' + Date.now() + Math.floor(Math.random() * 1000), name: name, items: [] };\n      pb.categories.push(target);\n    }\n    return target;\n  }\n\n  function importCSV(text) {\n    var rows = parseCSV(text);\n    if (!rows.length) { toast('CSVが空でした'); return; }\n\n    var cols = detectColumns(rows[0]);\n    var body;\n    if (cols) {\n      body = rows.slice(1);\n    } else {\n      // 見出しが無いときは、列の数から並びを推測する\n      var n = 0;\n      rows.forEach(function (r) { n = Math.max(n, r.length); });\n      if (n <= 2)      cols = { name: 0, price: 1 };\n      else if (n === 3) cols = { name: 0, unit: 1, price: 2 };\n      else if (n === 4) cols = { code: 0, name: 1, unit: 2, price: 3 };\n      else              cols = { code: 0, name: 1, spec: 2, unit: 3, price: 4 };\n      body = rows;\n    }\n\n    var fallbackName = $('#csv-target').value;\n    var replace = $('#csv-replace').checked;\n    var costOnly = $('#csv-costonly') && $('#csv-costonly').checked;\n    var touched = {};   // カテゴリ名 → 追加件数\n    var staged = [];\n    var skipped = 0;\n\n    body.forEach(function (r) {\n      function cell(key) { return cols[key] != null ? String(r[cols[key]] == null ? '' : r[cols[key]]).trim() : ''; }\n      var name = cell('name');\n      var code = cell('code');\n      if (!name && !code) { skipped++; return; }\n      var catName = cell('category') || fallbackName;\n      var md = toPrice(cell('manDay'));\n      var it = {\n        code: code,\n        name: name || code,\n        spec: cell('spec'),\n        url: safeUrl(cell('url')),\n        color: cell('color'),\n        unit: cell('unit') || '個',\n        price: toPrice(cell('price'))\n      };\n      // 人工の列があれば、単価は［人工 × 1人工の金額］で出す\n      if (md > 0) { it.manDay = md; it.price = manDayPrice(md); }\n      // 仕入値（原価）の列があれば入れる。社内用なので見積書には出ない\n      var cst = toPrice(cell('cost'));\n      if (cst > 0) it.cost = cst;\n      staged.push({ catName: catName, item: it });\n      touched[catName] = (touched[catName] || 0) + 1;\n    });\n\n    if (!staged.length) { toast('取り込める行が見つかりませんでした'); return; }\n\n    // 仕入先の見積を読ませるとき。行は増やさず、原価だけを書き込む\n    if (costOnly) { importCostsOnly(staged); return; }\n\n    /* 原価の列があるのにチェックが入っていない、という取り違えを止める。\n       原価しか入っていない表は、まず間違いなく仕入先の見積であって、\n       新しい品物の一覧ではない。黙って151件足してしまったことがある\n       （2026-09-06）。人が覚えておく話ではないので、道具の側で聞く。 */\n    var withCost = staged.filter(function (x) { return num(x.item.cost) > 0; }).length;\n    var withPrice = staged.filter(function (x) { return num(x.item.price) > 0; }).length;\n    if (withCost > 0 && withCost >= staged.length / 2 && withPrice < staged.length / 2) {\n      var ans = confirm(\n        '原価の列が入っていて、定価がほとんど入っていません。\\n' +\n        '仕入先の見積を読ませようとしていませんか？\\n\\n' +\n        '　このまま進むと … 新しい項目を ' + staged.length + '件 追加します\\n' +\n        '　やめて直すと … いまある項目の原価だけを書き込みます（増えません）\\n\\n' +\n        '［OK］いまある項目の原価だけを書き込む（おすすめ）\\n' +\n        '［キャンセル］このまま ' + staged.length + '件を新しく追加する');\n      if (ans) {\n        var box = $('#csv-costonly');\n        if (box) box.checked = true;         // 次からは押し忘れないよう、印も入れておく\n        importCostsOnly(staged);\n        return;\n      }\n    }\n\n    // カテゴリが多いとダイアログが長くなり、ブラウザに途中で切られてしまう。\n    // 内訳は先頭だけ出して、残りは件数でまとめる。\n    var catNames = Object.keys(touched);\n    var SUMMARY_MAX = 5;\n    var summary = catNames.slice(0, SUMMARY_MAX).map(function (k) {\n      return '・' + k + '：' + touched[k] + '件';\n    }).join('\\n');\n    if (catNames.length > SUMMARY_MAX) {\n      summary += '\\n・ほか ' + (catNames.length - SUMMARY_MAX) + ' カテゴリ';\n    }\n\n    // 読み違えていないか目で確かめてもらうため、最初の数件を見せる\n    var preview = staged.slice(0, 3).map(function (s) {\n      var i = s.item;\n      return '　' + [i.code || '（品番なし）', i.name, i.spec || '—', i.unit,\n                     (num(i.manDay) ? i.manDay + '人工 ' : '') + yen(i.price) +\n                     (num(i.cost) ? '（原価 ' + yen(i.cost) + '）' : '')].join(' ／ ');\n    }).join('\\n');\n\n    // 読み取り結果を先に出す。長くてブラウザに切られても、ここだけは必ず見えるようにする。\n    var msg = staged.length + '件を取り込みます。\\n\\n' +\n      '【読み取り結果の確認（最初の' + Math.min(3, staged.length) + '件）】\\n' +\n      '　品番 ／ 品名 ／ 規格 ／ 単位 ／ 単価\\n' + preview +\n      '\\n\\nこの並びで合っていますか？\\n\\n' +\n      '【入れ先カテゴリ（' + catNames.length + '件）】\\n' + summary +\n      (skipped ? '\\n\\n（品名も品番も空の ' + skipped + ' 行はとばします）' : '') +\n      (replace ? '\\n\\n※取り込み先カテゴリの中身は、いったん空にしてから入れ直します。' : '\\n\\n※いまある項目はそのまま残し、後ろに追加します。');\n    if (!confirm(msg)) return;\n\n    if (replace) {\n      Object.keys(touched).forEach(function (k) { findOrCreateCategory(k).items = []; });\n    }\n    staged.forEach(function (s) { findOrCreateCategory(s.catName).items.push(s.item); });\n\n    if (savePB() === false) {\n      // 保存できなかったときは、取り込む前の状態に戻す\n      toast('件数が多すぎて保存できませんでした。分けて取り込んでください');\n      pb = load(KEY_PB, null) || clone(DEFAULT_PRICEBOOK);\n      pb.company  = adoptCompany(pb.company);\n      pb.defaults = Object.assign({}, DEFAULT_PRICEBOOK.defaults, pb.defaults || {});\n      if (!Array.isArray(pb.categories)) pb.categories = clone(DEFAULT_PRICEBOOK.categories);\n      if (!pb.categories.some(function (c) { return c.id === activeCat; })) {\n        activeCat = pb.categories.length ? pb.categories[0].id : null;\n      }\n    }\n    renderMaster(); renderPicker();\n    toast(staged.length + '件を取り込みました');\n  }\n\n  /**\n   * 仕入先の見積から、原価だけを既存の項目に書き込む。\n   *\n   * 突き合わせは品番が第一。品番が無い行は品名＋規格で探す。\n   * 見つからない行は足さずに数えるだけ。勝手に増やすと、\n   * 商社の見積にしか無い品が単価マスタに紛れ込む。\n   */\n  function importCostsOnly(staged) {\n    // 4,000行を毎回なめないよう、先に索引を作る\n    var byCode = {}, byName = {};\n    pb.categories.forEach(function (c) {\n      (c.items || []).forEach(function (it) {\n        var cd = String(it.code || '').trim().toUpperCase();\n        if (cd && !byCode[cd]) byCode[cd] = { item: it, cat: c };\n        var k = (it.name || '') + '｜' + (it.spec || '');\n        if (!byName[k]) byName[k] = { item: it, cat: c };\n      });\n    });\n\n    var hit = [], miss = [], pairs = [];\n    staged.forEach(function (sg) {\n      var r = sg.item;\n      var cost = num(r.cost);\n      if (cost <= 0) return;                       // 原価が無い行は用が無い\n      var cd = String(r.code || '').trim().toUpperCase();\n      var found = (cd && byCode[cd]) || byName[(r.name || '') + '｜' + (r.spec || '')];\n      // 掛率は見積の定価と原価から出す。当たったかどうかとは関係なく使える\n      if (num(r.price) > 0) {\n        pairs.push({ price: num(r.price), cost: cost,\n                     text: (r.name || '') + ' ' + (r.spec || '') + ' ' + (sg.catName || '') });\n      }\n      if (found) hit.push({ ref: found, cost: cost, label: (r.code || r.name) });\n      else miss.push(r.code || r.name);\n    });\n\n    if (!hit.length && !pairs.length) {\n      alert('原価の入った行が見つかりませんでした。\\n\\n' +\n            '見出しに「原価」「仕切」「仕入」のどれかの列が要ります。');\n      return;\n    }\n\n    var sample = hit.slice(0, 3).map(function (h) {\n      return '　' + h.label + ' … ' + yen(num(h.ref.item.price)) + ' → 原価 ' + yen(h.cost);\n    }).join('\\n');\n\n    var msg = '仕入先の見積として読みました。\\n\\n' +\n      '　いまの項目に当てはまった：' + hit.length + '件\\n' +\n      '　見つからなかった：' + miss.length + '件（足しません）\\n\\n' +\n      (sample ? '【当てはまった例】\\n' + sample + '\\n\\n' : '') +\n      (miss.length ? '【見つからなかった品番】\\n　' + miss.slice(0, 5).join('、') +\n        (miss.length > 5 ? ' ほか' + (miss.length - 5) + '件' : '') + '\\n\\n' : '') +\n      '原価だけを書き込みます。定価と項目の数は変わりません。よろしいですか？';\n    if (!confirm(msg)) return;\n\n    hit.forEach(function (h) { h.ref.item.cost = h.cost; });\n    savePB();\n    renderMaster(); renderPicker(); renderLines();\n    toast(hit.length + '件に原価を入れました');\n\n    offerCostRates(pairs);\n  }\n\n  /** 読み取った掛率を、仕入掛率の表に入れるか聞く */\n  function offerCostRates(pairs) {\n    var sum = summarizeRates(pairs);\n    if (!sum.length) return;\n\n    var lines = sum.map(function (r) {\n      return r.steady\n        ? '　' + r.maker + '　' + r.pct + '%　（' + r.count + '件そろっています）'\n        : '　' + r.maker + '　' + r.kinds.join('% / ') + '%　（' + r.count + '件・ばらついています）';\n    }).join('\\n');\n\n    var steady = sum.filter(function (r) { return r.steady && r.maker !== '（メーカー不明）'; });\n    if (!steady.length) {\n      alert('掛率はこう読めました。\\n\\n' + lines + '\\n\\n' +\n        'ばらついているので、自動では入れません。\\n' +\n        'シリーズごとに違う掛率なら、［自社情報］の仕入掛率で\\n' +\n        'シリーズ名を書いた行を足してください。');\n      return;\n    }\n\n    if (!confirm('掛率はこう読めました。\\n\\n' + lines + '\\n\\n' +\n      'そろっている ' + steady.length + '件を［自社情報］の仕入掛率に入れますか？\\n\\n' +\n      'ここに入れると、見積に載っていない同じメーカーの項目にも\\n' +\n      '原価が出るようになります（1本の見積で数千行が埋まります）。\\n' +\n      '※ すでに同じメーカーだけの行があれば、上書きします')) return;\n\n    ensureCostRates();\n    steady.forEach(function (r) {\n      var at = -1;\n      pb.defaults.costRates.forEach(function (x, i) {\n        if (String(x.maker || '').trim() === r.maker && !String(x.series || '').trim()) at = i;\n      });\n      var row = { maker: r.maker, series: '', percent: r.pct };\n      if (at >= 0) pb.defaults.costRates[at] = row;\n      else pb.defaults.costRates.push(row);\n    });\n    savePB();\n    renderCostRates();\n    renderMaster(); renderPicker(); renderLines();\n    toast(steady.length + '件の掛率を入れました');\n  }\n\n  function renderCsvTargets() {\n    var sel = $('#csv-target');\n    if (!sel) return;\n    var keep = sel.value;\n    sel.innerHTML = '';\n    pb.categories.forEach(function (c) {\n      var o = el('option', null, c.name);\n      o.value = c.name;\n      sel.appendChild(o);\n    });\n    var has = function (v) {\n      return Array.prototype.some.call(sel.options, function (o) { return o.value === v; });\n    };\n    // 前に選んでいたカテゴリが残っていればそれを保つ。無ければ先頭にする。\n    // （この欄は、カテゴリ列の無いCSVを取り込むときの入れ先を決めるためのもの）\n    if (keep && has(keep)) sel.value = keep;\n    else if (sel.options.length) sel.selectedIndex = 0;\n  }\n\n  $('#file-csv').addEventListener('change', function (ev) {\n    var f = ev.target.files && ev.target.files[0];\n    ev.target.value = '';\n    if (!f) return;\n    var r = new FileReader();\n    r.onerror = function () { toast('ファイルを読み込めませんでした'); };\n    r.onload = function () {\n      try { importCSV(decodeCSV(r.result)); }\n      catch (e) { toast('CSVを読み込めませんでした：' + e.message); }\n    };\n    r.readAsArrayBuffer(f);\n  });\n\n  $('#btn-paste-import').addEventListener('click', function () {\n    var text = $('#paste-area').value;\n    if (!text.trim()) { toast('貼り付け欄が空です'); return; }\n    try {\n      importCSV(text);\n      $('#paste-area').value = '';\n    } catch (e) { toast('読み込めませんでした：' + e.message); }\n  });\n\n  $('#btn-paste-clear').addEventListener('click', function () {\n    $('#paste-area').value = '';\n  });\n\n  $('#btn-csv-template').addEventListener('click', function () {\n    var lines = [\n      'カテゴリ,品番,品名,規格,単位,定価,URL',\n      '材料,LD-70,スリムダクト LD ダクト,ダクト 70,本,0,https://www.inaba-denko.com/ja/product/detail/1540000',\n      '材料,,ここに実際の品番・品名・定価を入れてください,,個,0,'\n    ].join('\\r\\n');\n    // Excelでそのまま開けるよう BOM 付き UTF-8 で書き出す\n    var blob = new Blob(['﻿' + lines], { type: 'text/csv;charset=utf-8' });\n    var url = URL.createObjectURL(blob);\n    var a = document.createElement('a');\n    a.href = url; a.download = '単価取り込みテンプレート.csv';\n    document.body.appendChild(a); a.click(); document.body.removeChild(a);\n    setTimeout(function () { URL.revokeObjectURL(url); }, 1000);\n  });\n\n  var pbTimer;\n  function savePBDebounced() {\n    clearTimeout(pbTimer);\n    pbTimer = setTimeout(function () { savePB(); renderPicker(); }, 500);\n  }\n\n  var pbQuietTimer;\n  function savePBQuiet() {          // 画面を作り直さずに保存だけする\n    clearTimeout(pbQuietTimer);\n    pbQuietTimer = setTimeout(function () { savePB(); }, 600);\n  }\n\n  $('#btn-autocolor').addEventListener('click', function () {\n    // 何色になるかを先に見せる。すでに色のある項目には触らない\n    var plan = [];\n    pb.categories.forEach(function (c) {\n      c.items.forEach(function (it) {\n        if (it.color) return;\n        var col = autoColorFor(it.name);\n        if (col) plan.push({ name: it.name, spec: it.spec || '', color: col });\n      });\n    });\n    if (!plan.length) { toast('色をつけられる項目はありませんでした'); return; }\n\n    var NL = String.fromCharCode(10);\n    var sample = plan.slice(0, 6).map(function (x) {\n      return '　' + x.color + '　' + x.name + (x.spec ? '（' + x.spec + '）' : '');\n    }).join(NL);\n\n    if (!confirm(plan.length + '件に色をつけます。よろしいですか？' + NL +\n      '（すでに色をつけてある項目は、そのまま残します）' + NL + NL + sample +\n      (plan.length > 6 ? NL + '　…ほか ' + (plan.length - 6) + '件' : ''))) return;\n\n    var n = autoColorAll();\n    if (savePB() === false) return;\n    renderPicker();\n    renderMaster();\n    toast(n + '件に色をつけました');\n  });\n\n  $('#btn-add-cat').addEventListener('click', function () {\n    var v = prompt('新しいカテゴリ名');\n    if (!v) return;\n    pb.categories.push({ id: 'c' + Date.now(), name: v.trim(), items: [] });\n    savePB(); renderMaster(); renderPicker();\n  });\n\n  $('#btn-export-pb').addEventListener('click', function () {\n    // 機種データも一緒に入れておく。端末を移すとき、これ1つで全部運べるようにするため。\n    var bundle = Object.assign({}, pb, { _models: load(KEY_MDL, null) });\n    download('50airtec-単価マスタ-' + todayISO() + '.json', JSON.stringify(bundle, null, 2));\n  });\n  $('#file-import-pb').addEventListener('change', function (ev) {\n    readJSON(ev.target, function (data) {\n      if (!data || !Array.isArray(data.categories)) { toast('単価マスタの形式が違います'); return; }\n      var mdl = data._models || null;      // 古いバックアップには入っていないので、無ければ何もしない\n      var msg = '今の単価マスタを、読み込んだファイルの内容で置き換えます。よろしいですか？';\n      if (mdl) msg += '\\n\\n（機種データも一緒に入っています）';\n      if (!confirm(msg)) return;\n      delete data._models;\n      pb = data;\n      pb.company  = adoptCompany(pb.company);\n      pb.defaults = Object.assign({}, DEFAULT_PRICEBOOK.defaults, pb.defaults || {});\n      activeCat = pb.categories.length ? pb.categories[0].id : null;\n      savePB(); renderMaster(); renderPicker(); fillCompany();\n      if (mdl) { save(KEY_MDL, mdl); chooserSel = {}; loadModels(); }\n      /* 単価マスタが入れ替わったら、開いている見積の原価も出し直す。\n         ここを呼ばないと、原価を入れた単価マスタを読み込んでも\n         画面の見積は「未入力」のまま。見積を開き直すまで気づけない\n         （2026-09-06、３８８の見積で分かった） */\n      var n = fillMissingLineCosts();\n      if (n) { persistDraft(); renderLines(); }\n      toast('読み込みました' + (n ? '（開いている見積の ' + n + '行にも原価が入りました）' : ''));\n    });\n  });\n  $('#btn-reset-pb').addEventListener('click', function () {\n    if (!confirm('単価マスタを初期値に戻します。今の金額は消えます。よろしいですか？')) return;\n    pb = clone(DEFAULT_PRICEBOOK);\n    activeCat = pb.categories[0].id;\n    savePB(); renderMaster(); renderPicker(); fillCompany();\n    toast('初期値に戻しました');\n  });\n\n  /* ======================================================================\n     自社情報\n     ====================================================================== */\n  var companyMap = {\n    '#c-name': 'name', '#c-owner': 'owner', '#c-zip': 'zip', '#c-address': 'address',\n    '#c-tel': 'tel', '#c-email': 'email', '#c-web': 'web', '#c-invoice': 'invoiceNo', '#c-bank': 'bank'\n  };\n\n  function fillCompany() {\n    Object.keys(companyMap).forEach(function (sel) {\n      var n = $(sel);\n      if (n) n.value = pb.company[companyMap[sel]] || '';\n    });\n    $('#c-footer').value = pb.defaults.footerNote || '';\n    $('#c-unit-round').value = num(pb.defaults.unitRoundYen) || 0;\n    $('#c-manday').value = num(pb.defaults.manDayYen) || 0;\n    $('#c-manday-cost').value = num(pb.defaults.manDayCostYen) || 0;\n    $('#c-material-cost').value = num(pb.defaults.materialCostPercent) || 0;\n    $('#c-model-div').value = num(pb.defaults.modelSellDivisor) || '';\n    showModelDivNote();\n    // うまく動かないときに「どの版を使っているか」を言えるようにしておく\n    $('#app-ver').textContent = '空調王　版 ' + APP_VERSION;\n    renderCostRates();\n    $('#seal-size').value = pb.company.sealSizeMm || 18;\n    $('#logo-size').value = pb.company.logoHeightMm || 12;\n    renderSkinPick();\n    renderColorPick();\n    renderSealPreview();\n    renderLogoPreview();\n    renderPresets();\n    updateBrand();\n  }\n\n  /* ----------------------------------------------------------------------\n     メーカー・シリーズごとの仕入掛率\n     商社は「メーカーごと、寒冷地モデルかどうか」で掛率を変える。\n     （西方商店：日立の省エネの達人 23%／寒さ知らず 24%）\n     ---------------------------------------------------------------------- */\n  function renderCostRates() {\n    var box = $('#cost-rates');\n    if (!box) return;\n    box.innerHTML = '';\n    var list = pb.defaults.costRates || [];\n    if (!list.length) {\n      box.appendChild(el('p', 'picker-empty',\n        'まだありません。「＋ 行を足す」で、メーカーごとの掛率を決められます。'));\n      return;\n    }\n    list.forEach(function (r, i) { box.appendChild(costRateRow(r, i)); });\n  }\n\n  function costRateRow(r, i) {\n    var row = el('div', 'rate-row');\n\n    var mk = el('input');\n    mk.type = 'text'; mk.placeholder = '日立'; mk.value = r.maker || '';\n\n    var sr = el('input');\n    sr.type = 'text'; sr.placeholder = '空ならメーカー全部'; sr.value = r.series || '';\n\n    var pc = el('input');\n    pc.type = 'number'; pc.min = 0; pc.max = 100; pc.step = 1;\n    pc.placeholder = '%'; pc.value = num(r.percent) || '';\n\n    mk.addEventListener('input', function () { r.maker = mk.value; });\n    sr.addEventListener('input', function () { r.series = sr.value; });\n    pc.addEventListener('input', function () { r.percent = num(pc.value); });\n\n    var del = el('button', 'icon-btn', '✕');\n    del.type = 'button';\n    del.title = 'この行を消す';\n    del.addEventListener('click', function () {\n      pb.defaults.costRates.splice(i, 1);\n      renderCostRates();\n    });\n\n    row.appendChild(mk); row.appendChild(sr); row.appendChild(pc); row.appendChild(del);\n    return row;\n  }\n\n  function updateBrand() {\n    var name = (pb.company.name || '').trim();\n    $('#brand-company').textContent = name || '自社情報が未設定です';\n    var mark = name ? name.replace(/[（(].*$/, '').trim().slice(0, 2) : '空調';\n    $('#brand-mark').textContent = mark || '空調';\n  }\n\n  // 割る数を打ち替えている途中でも、粗利率がいくつになるか見えるようにする\n  $('#c-model-div').addEventListener('input', showModelDivNote);\n\n  $('#btn-rate-add').addEventListener('click', function () {\n    ensureCostRates();\n    pb.defaults.costRates.push({ maker: '', series: '', percent: 0 });\n    renderCostRates();\n  });\n\n  $('#btn-save-company').addEventListener('click', function () {\n    Object.keys(companyMap).forEach(function (sel) {\n      pb.company[companyMap[sel]] = $(sel).value;\n    });\n    pb.defaults.footerNote = $('#c-footer').value;\n    pb.defaults.unitRoundYen = num($('#c-unit-round').value);\n    pb.defaults.manDayYen = num($('#c-manday').value);\n    pb.defaults.manDayCostYen = num($('#c-manday-cost').value);\n    pb.defaults.materialCostPercent = num($('#c-material-cost').value);\n    pb.defaults.modelSellDivisor = num($('#c-model-div').value);\n    // メーカー名もシリーズ名も空、あるいは掛率0の行は捨てる\n    pb.defaults.costRates = (pb.defaults.costRates || []).filter(function (r) {\n      return (String(r.maker || '').trim() || String(r.series || '').trim()) && num(r.percent) > 0;\n    });\n    renderCostRates();\n    applyLineCosts();\n    applyModelPrices();   // 乗せる利益や仕入掛率を変えたら、機種の金額も出し直す\n    renderLines();\n    savePB();\n    updateBrand();\n    toast('保存しました');\n  });\n\n  /* ---------- 会社情報のひな形 ---------- */\n  function renderPresets() {\n    var box = $('#preset-actions');\n    box.innerHTML = '';\n    var presets = (pb.companyPresets && pb.companyPresets.length)\n      ? pb.companyPresets\n      : (DEFAULT_PRICEBOOK.companyPresets || []);\n    presets.forEach(function (p) {\n      var b = el('button', 'btn btn-ghost', p.label + 'の情報を入れる');\n      b.type = 'button';\n      b.addEventListener('click', function () {\n        if (!confirm('入力欄を「' + p.label + '」の内容で上書きします。よろしいですか？')) return;\n        Object.keys(companyMap).forEach(function (sel) {\n          var key = companyMap[sel];\n          if (p[key] != null) $(sel).value = p[key];\n        });\n        toast('入力しました。内容を確認して「保存」を押してください');\n      });\n      box.appendChild(b);\n    });\n  }\n\n  /* ======================================================================\n     社判\n     ====================================================================== */\n  var SEAL_MAX_PX = 600;   // 保存サイズ（大きすぎるとブラウザの保存容量を圧迫するため）\n\n  function renderSealPreview() {\n    var box = $('#seal-preview');\n    box.innerHTML = '';\n    if (pb.company.sealImage) {\n      var img = el('img', 'seal-img-preview');\n      img.src = pb.company.sealImage;\n      img.alt = '社判';\n      box.appendChild(img);\n      box.classList.add('has-seal');\n    } else {\n      var ph = el('span', 'seal-placeholder');\n      ph.innerHTML = '社判なし<br><small>㊞ と印字</small>';\n      box.appendChild(ph);\n      box.classList.remove('has-seal');\n    }\n  }\n\n  /** 画像のどこかに透けている部分があるか（＝すでに背景が抜いてある電子印鑑かどうか） */\n  function hasTransparency(px) {\n    // 全画素見ると重いので、間引いて調べる\n    var step = 4 * Math.max(1, Math.floor(px.length / 4 / 20000));\n    for (var i = 3; i < px.length; i += step * 4) {\n      if (px[i] < 240) return true;\n    }\n    return false;\n  }\n\n  /**\n   * 社判の画像を取り込み用に整えて data URL（PNG）で返す。\n   *\n   * - 写真やスキャン（白い紙が写っている）→ 明るい部分ほど透明にして印影だけ残す\n   * - はじめから背景が透明な電子印鑑データ → 何もせずそのまま使う\n   * - SVG（ベクター）→ 大きめに描き直してから取り込む\n   *\n   * cb(dataUrl, info) の info.mode に 'kept'（そのまま）/'removed'（背景を抜いた）が入る。\n   */\n  function processSeal(file, makeTransparent, cb) {\n    var isSVG = /svg/i.test(file.type) || /\\.svg$/i.test(file.name || '');\n\n    var reader = new FileReader();\n    reader.onerror = function () { toast('ファイルを読み込めませんでした'); };\n    reader.onload = function () {\n      var img = new Image();\n      img.onerror = function () {\n        toast('この形式は読み込めませんでした。PNG か JPEG で書き出してお試しください');\n      };\n      img.onload = function () {\n        var iw = img.naturalWidth || img.width || 0;\n        var ih = img.naturalHeight || img.height || 0;\n        if (!iw || !ih) { iw = 512; ih = 512; }          // SVGで寸法が取れない場合の保険\n\n        // SVGはベクターなので、粗くならないよう大きめに描き直す\n        var scale = isSVG\n          ? SEAL_MAX_PX / Math.max(iw, ih)\n          : Math.min(1, SEAL_MAX_PX / Math.max(iw, ih));\n        var w = Math.max(1, Math.round(iw * scale));\n        var h = Math.max(1, Math.round(ih * scale));\n\n        var cv = document.createElement('canvas');\n        cv.width = w; cv.height = h;\n        var ctx = cv.getContext('2d');\n        ctx.drawImage(img, 0, 0, w, h);\n\n        var d;\n        try { d = ctx.getImageData(0, 0, w, h); }\n        catch (e) { cb(cv.toDataURL('image/png'), { mode: 'kept' }); return; }\n        var px = d.data;\n\n        // すでに背景が抜けているデータは、絶対に触らない\n        if (hasTransparency(px)) {\n          cb(cv.toDataURL('image/png'), { mode: 'kept' });\n          return;\n        }\n        if (!makeTransparent) {\n          cb(cv.toDataURL('image/png'), { mode: 'kept' });\n          return;\n        }\n\n        for (var i = 0; i < px.length; i += 4) {\n          // 明るい（＝紙の白い部分）ほど透明に、濃い（＝印影）ほどそのまま残す。\n          // -22 は、少し灰色がかった紙もきちんと抜けるようにするための下駄。\n          var lum = 0.299 * px[i] + 0.587 * px[i + 1] + 0.114 * px[i + 2];\n          var a = (255 - lum) * 1.7 - 22;\n          if (a < 10) a = 0; else if (a > 255) a = 255;\n          if (a < px[i + 3]) px[i + 3] = a;              // 元の透明度は残す\n        }\n        ctx.putImageData(d, 0, 0);\n        cb(cv.toDataURL('image/png'), { mode: 'removed' });\n      };\n      img.src = reader.result;\n    };\n    reader.readAsDataURL(file);\n  }\n\n  $('#file-seal').addEventListener('change', function (ev) {\n    var f = ev.target.files && ev.target.files[0];\n    ev.target.value = '';\n    if (!f) return;\n\n    var okType = /^image\\//.test(f.type) || /\\.(png|jpe?g|gif|webp|bmp|svg)$/i.test(f.name || '');\n    if (!okType) {\n      toast(/pdf$/i.test(f.name || '')\n        ? 'PDFは読み込めません。PNGかJPEGで書き出してからお試しください'\n        : '画像ファイル（PNG・JPEG・SVGなど）を選んでください');\n      return;\n    }\n\n    processSeal(f, $('#seal-transparent').checked, function (dataUrl, info) {\n      var prev = pb.company.sealImage;\n      pb.company.sealImage = dataUrl;\n      if (savePB() === false) {\n        pb.company.sealImage = prev;\n        toast('社判が大きすぎて保存できませんでした。もう少し小さい画像でお試しください');\n        return;\n      }\n      renderSealPreview();\n      toast(info.mode === 'kept'\n        ? '透過データだったので、そのまま取り込みました'\n        : '社判を取り込みました（白い背景を透明にしました）');\n    });\n  });\n\n  $('#btn-seal-clear').addEventListener('click', function () {\n    if (!pb.company.sealImage) return;\n    if (!confirm('社判を削除します。よろしいですか？')) return;\n    pb.company.sealImage = '';\n    savePB();\n    renderSealPreview();\n    toast('社判を削除しました');\n  });\n\n  /* ---------- 見積書の見た目 ----------\n     押すとその場で切り替える。小さな紙の絵を並べて、\n     印刷しなくても違いが見えるようにしてある（絵のCSSは style.css の .skin-demo）。 */\n  function renderSkinPick() {\n    var box = $('#skin-pick');\n    if (!box) return;\n    box.innerHTML = '';\n    var now = skinId(pb.company.sheetSkin);\n    SHEET_SKINS.forEach(function (sk) {\n      var b = el('button', 'skin-card' + (sk.id === now ? ' is-on' : ''));\n      b.type = 'button';\n      b.setAttribute('aria-pressed', sk.id === now ? 'true' : 'false');\n\n      var demo = el('span', 'skin-demo skin-demo-' + sk.id);\n      demo.appendChild(el('span', 'sd-title', '御見積書'));\n      var tb = el('span', 'sd-table');\n      for (var i = 0; i < 3; i++) {\n        var r = el('span', 'sd-row' + (i ? '' : ' sd-head'));\n        r.appendChild(el('span', 'sd-c1'));\n        r.appendChild(el('span', 'sd-c2'));\n        r.appendChild(el('span', 'sd-c3'));\n        tb.appendChild(r);\n      }\n      demo.appendChild(tb);\n      demo.appendChild(el('span', 'sd-sum'));\n      b.appendChild(demo);\n\n      b.appendChild(el('span', 'skin-name', sk.name));\n      b.appendChild(el('span', 'skin-hint', sk.hint));\n      b.addEventListener('click', function () {\n        pb.company.sheetSkin = sk.id;\n        savePB();\n        renderSkinPick();\n        renderColorPick();\n        toast('見積書の見た目を「' + sk.name + '」にしました');\n      });\n      box.appendChild(b);\n    });\n    // 見本の紙の絵も、選んだ色で見せる\n    box.style.setProperty('--sheet-accent', colorHex(pb.company.sheetColor));\n  }\n\n  function renderColorPick() {\n    var box = $('#color-pick');\n    if (!box) return;\n    box.innerHTML = '';\n    var now = colorId(pb.company.sheetColor);\n    var skin = skinId(pb.company.sheetSkin);\n    SHEET_COLORS.forEach(function (co) {\n      var b = el('button', 'color-card' + (co.id === now ? ' is-on' : ''));\n      b.type = 'button';\n      b.setAttribute('aria-pressed', co.id === now ? 'true' : 'false');\n      var sw = el('span', 'color-swatch');\n      sw.style.background = co.hex;\n      b.appendChild(sw);\n      b.appendChild(el('span', 'color-name', co.name + (co.note ? '（' + co.note + '）' : '')));\n      b.addEventListener('click', function () {\n        pb.company.sheetColor = co.id;\n        savePB();\n        renderColorPick();\n        renderSkinPick();\n        toast('見積書の色を「' + co.name + '」にしました');\n      });\n      box.appendChild(b);\n    });\n    // 「標準」は白黒のままが正解なので、色が効かないことを言っておく\n    var note = $('#color-note');\n    if (note) {\n      note.textContent = skin === 'standard'\n        ? '※いま選んでいる「標準」は白黒のままです（色は使いません）。色を出したいときは「色帯」か「ロゴ大きく左」を選んでください。'\n        : (skin === 'band' || skin === 'logo'\n            ? '※いまの見た目では、表題・見出し・合計にこの色が出ます。'\n            : '※いまの見た目では、線と表題だけにこの色が出ます（控えめに入ります）。');\n    }\n  }\n\n  $('#seal-size').addEventListener('input', function () {\n    var v = num($('#seal-size').value);\n    pb.company.sealSizeMm = Math.min(45, Math.max(8, v || 18));\n    savePBDebounced();\n  });\n\n  /* ---------- ロゴ ----------\n     社判とちがい、ロゴは「見せたい形」がそのまま正解なので、\n     白い背景を抜くような加工はせず、大きさだけ整えて取り込む。 */\n  var LOGO_MAX_PX = 600;\n\n  function renderLogoPreview() {\n    var box = $('#logo-preview');\n    box.innerHTML = '';\n    if (pb.company.logoImage) {\n      var img = el('img', 'logo-img-preview');\n      img.src = pb.company.logoImage;\n      img.alt = 'ロゴ';\n      box.appendChild(img);\n      box.classList.add('has-logo');\n    } else {\n      var ph = el('span', 'seal-placeholder');\n      ph.innerHTML = 'ロゴなし<br><small>会社名だけ印字</small>';\n      box.appendChild(ph);\n      box.classList.remove('has-logo');\n    }\n  }\n\n  /** ロゴ画像を取り込み用に整えて data URL（PNG）で返す */\n  function processLogo(file, cb) {\n    var isSVG = /svg/i.test(file.type) || /\\.svg$/i.test(file.name || '');\n    var reader = new FileReader();\n    reader.onerror = function () { toast('ファイルを読み込めませんでした'); };\n    reader.onload = function () {\n      var img = new Image();\n      img.onerror = function () {\n        toast('この形式は読み込めませんでした。PNG か JPEG で書き出してお試しください');\n      };\n      img.onload = function () {\n        var iw = img.naturalWidth || img.width || 0;\n        var ih = img.naturalHeight || img.height || 0;\n        if (!iw || !ih) { iw = 512; ih = 512; }\n\n        // SVGはベクターなので、粗くならないよう大きめに描き直す\n        var scale = isSVG\n          ? LOGO_MAX_PX / Math.max(iw, ih)\n          : Math.min(1, LOGO_MAX_PX / Math.max(iw, ih));\n        var w = Math.max(1, Math.round(iw * scale));\n        var h = Math.max(1, Math.round(ih * scale));\n\n        var cv = document.createElement('canvas');\n        cv.width = w; cv.height = h;\n        cv.getContext('2d').drawImage(img, 0, 0, w, h);\n        cb(cv.toDataURL('image/png'));\n      };\n      img.src = reader.result;\n    };\n    reader.readAsDataURL(file);\n  }\n\n  $('#file-logo').addEventListener('change', function (ev) {\n    var f = ev.target.files && ev.target.files[0];\n    ev.target.value = '';\n    if (!f) return;\n\n    var okType = /^image\\//.test(f.type) || /\\.(png|jpe?g|gif|webp|bmp|svg)$/i.test(f.name || '');\n    if (!okType) { toast('画像ファイル（PNG・JPEG・SVGなど）を選んでください'); return; }\n\n    processLogo(f, function (dataUrl) {\n      var prev = pb.company.logoImage;\n      pb.company.logoImage = dataUrl;\n      if (savePB() === false) {\n        pb.company.logoImage = prev;\n        toast('画像が大きすぎて保存できませんでした。小さめの画像でお試しください');\n        return;\n      }\n      renderLogoPreview();\n      toast('ロゴを登録しました');\n    });\n  });\n\n  $('#btn-logo-clear').addEventListener('click', function () {\n    if (!pb.company.logoImage) return;\n    if (!confirm('ロゴを削除します。よろしいですか？')) return;\n    pb.company.logoImage = '';\n    savePB();\n    renderLogoPreview();\n    toast('ロゴを削除しました');\n  });\n\n  $('#logo-size').addEventListener('input', function () {\n    var v = num($('#logo-size').value);\n    pb.company.logoHeightMm = Math.min(40, Math.max(5, v || 12));\n    savePBDebounced();\n  });\n\n  /* ======================================================================\n     機器を選ぶ（メーカーの機種データから絞り込んで明細に入れる）\n     ----------------------------------------------------------------------\n     機種データは容量節約のため「辞書＋番号」で保存されているので、\n     読み込むときに元の文字列に戻してから使う。\n     ====================================================================== */\n  // メーカーごとの「機種データ」をまとめて持つ。\n  // { packs:[各メーカー], items:[全メーカーぶんを1本にしたもの], seriesOrder, typeOrder }\n  var models = null;\n  var modelRaws = [];       // 保存されている生のデータ（削除するときに使う）\n\n  /** 保存されている機種データを、使える形（配列）にほどく */\n  function decodeModels(p) {\n    if (!p || !Array.isArray(p.rows) || !Array.isArray(p.fields)) return null;\n    var dictFields = p.dictFields || [];\n    var items = p.rows.map(function (r) {\n      var o = {};\n      p.fields.forEach(function (f, i) {\n        o[f] = dictFields.indexOf(f) >= 0 ? (p.dict[f] || [])[r[i]] : r[i];\n      });\n      if (o.u && !/^https?:\\/\\//.test(o.u)) o.u = (p.urlBase || '') + o.u;\n      return o;\n    });\n    return {\n      maker: p.maker || 'メーカー',\n      brand: p.brand || '',\n      note: p.note || '',\n      fetched: p.fetched || '',\n      seriesOrder: p.seriesOrder || [],\n      typeOrder: p.typeOrder || [],\n      items: items\n    };\n  }\n\n  /** 同じメーカー・同じシリーズ群かどうかを見分ける鍵 */\n  function packKey(p) { return (p && p.maker || '') + '｜' + (p && p.brand || ''); }\n\n  /** 保存されている中身を「メーカーごとの配列」にそろえる。\n      以前は1メーカーぶんだけを直に入れていたので、その形も読めるようにしておく。 */\n  function rawPacks(raw) {\n    if (!raw) return [];\n    if (Array.isArray(raw.packs)) return raw.packs;\n    return [raw];\n  }\n\n  function loadModels() {\n    var packs = [];\n    modelRaws = [];\n    rawPacks(load(KEY_MDL, null)).forEach(function (r) {\n      var d = decodeModels(r);\n      if (d && d.items.length) { packs.push(d); modelRaws.push(r); }\n    });\n\n    if (!packs.length) {\n      models = null;\n    } else {\n      // 全メーカーぶんを1本の配列にまとめる。どのメーカーの機種かは mk に持たせる\n      var items = [], so = [], to = [];\n      packs.forEach(function (p) {\n        // br（ブランド）は、別売品を業務用とルームエアコンで分けるのに使う\n        p.items.forEach(function (x) { x.mk = p.maker; x.br = p.brand; items.push(x); });\n        p.seriesOrder.forEach(function (v) { if (so.indexOf(v) < 0) so.push(v); });\n        p.typeOrder.forEach(function (v) { if (to.indexOf(v) < 0) to.push(v); });\n      });\n      models = { packs: packs, items: items, seriesOrder: so, typeOrder: to };\n    }\n    renderModelsStatus();\n    renderChooser();\n  }\n\n  function renderModelsStatus() {\n    var box = $('#models-status');\n    if (!box) return;\n    box.innerHTML = '';\n    if (!models) { box.textContent = 'まだ読み込まれていません。'; return; }\n\n    models.packs.forEach(function (p, i) {\n      var row = el('div', 'models-row');\n      row.appendChild(el('b', null, p.maker + '　' + p.brand));\n      row.appendChild(el('span', 'models-num', p.items.length + '機種'));\n      if (p.fetched) row.appendChild(el('span', null, '取得日 ' + p.fetched));\n      if (p.note) row.appendChild(el('span', 'models-note', p.note));\n\n      var del = el('button', 'icon-btn', '✕');\n      del.type = 'button';\n      del.title = p.maker + ' の機種データだけを削除';\n      del.addEventListener('click', function () { removePack(i); });\n      row.appendChild(del);\n      box.appendChild(row);\n    });\n  }\n\n  /** メーカー1つぶんだけ消す */\n  function removePack(i) {\n    var p = models && models.packs[i];\n    if (!p) return;\n    if (!confirm(p.maker + '　' + p.brand + ' の機種データを削除します。よろしいですか？\\n\\n' +\n                 '（ほかのメーカーは残ります。見積の明細に入れた機器もそのまま残ります）')) return;\n    var raws = modelRaws.slice();\n    raws.splice(i, 1);\n    if (raws.length) { if (save(KEY_MDL, { v: 2, packs: raws }) === false) return; }\n    else removeKey(KEY_MDL);\n    chooserSel = {};\n    loadModels();\n    toast(p.maker + ' の機種データを削除しました');\n  }\n\n  /** 電源の見せ方。業務用は「単相」「三相」だけで持っている（どちらも200V）。\n      ルームエアコンは100Vもあるので「単相100V」のように、そのまま見せる文字で持っている */\n  function pwLabel(v) { return v === '三相' ? '三相200V' : (v === '単相' || !v ? '単相200V' : v); }\n\n  /* 絞り込みの順番。ここの並びがそのまま画面の手順になる */\n  var STEPS = [\n    { k: 'mk', label: 'メーカー' },\n    { k: 's',  label: 'シリーズ' },\n    { k: 'i',  label: '室内機タイプ' },\n    // 業務用は馬力の数（3 → 3馬力）。ルームエアコンは能力の文字（2.8kW（おもに10畳））をそのまま出す\n    { k: 'hp', label: '馬力・能力', fmt: function (v) { return typeof v === 'string' && /kW/.test(v) ? v : v + '馬力'; } },\n    { k: 'tp', label: '台数' },\n    { k: 'pw', label: '電源', fmt: pwLabel },\n    { k: 'rc', label: 'リモコン' }\n  ];\n  var chooserSel = {};\n\n  /** いま選ばれている条件に合う機種を返す。\n      stopAt を指定すると、**そのステップの条件だけを外して**残り全部で絞る\n      （そのステップの選択肢を出すため）。\n      先の順番でも、すでに選ばれている条件は効かせる。\n      ＝「必要な能力を出す」から馬力を先に決めて来たとき、\n        メーカーやシリーズの件数もその馬力で絞られる。 */\n  function chooserMatches(stopAt) {\n    if (!models) return [];\n    return models.items.filter(function (x) {\n      for (var i = 0; i < STEPS.length; i++) {\n        if (stopAt != null && i === stopAt) continue;\n        var k = STEPS[i].k;\n        if (chooserSel[k] != null && String(x[k]) !== String(chooserSel[k])) return false;\n      }\n      return true;\n    });\n  }\n\n  /** 選択肢を、その並び順の指定があればそれに従って並べる */\n  function sortOptions(k, vals) {\n    // ルームエアコンは「2.8kW（おもに10畳）」の文字で持っているので、頭の数字で並べる\n    if (k === 'hp') return vals.slice().sort(function (a, b) { return parseFloat(a) - parseFloat(b); });\n    /* シリーズ・台数の並びは、選んだメーカーの並びで決める。全メーカーの並びを1本にすると、\n       先に入れたメーカー（ダイキンのルームエアコン）の「RXシリーズ」「壁埋込形」などが前に来て、\n       三菱の壁掛形（FZ・Z…）が後ろに回っていた */\n    var order = null;\n    if (k === 's' || k === 'tp') {\n      var key = k === 's' ? 'seriesOrder' : 'typeOrder';\n      var mine = chooserSel.mk != null ? models.packs.filter(function (p) { return p.maker === chooserSel.mk; }) : [];\n      if (mine.length) {\n        order = [];\n        mine.forEach(function (p) { (p[key] || []).forEach(function (v) { if (order.indexOf(v) < 0) order.push(v); }); });\n      } else order = models[key];\n    }\n    if (order && order.length) {\n      return vals.slice().sort(function (a, b) {\n        var ia = order.indexOf(a), ib = order.indexOf(b);\n        return (ia < 0 ? order.length : ia) - (ib < 0 ? order.length : ib);\n      });\n    }\n    return vals.slice().sort(function (a, b) { return String(a).localeCompare(String(b), 'ja'); });\n  }\n\n  function renderChooser() {\n    var box = $('#chooser-body');\n    var srcEl = $('#chooser-src');\n    if (!box) return;\n    box.innerHTML = '';\n\n    if (!models) {\n      srcEl.textContent = '';\n      box.appendChild(el('p', 'picker-empty',\n        '機種データがまだありません。［単価マスタ］タブの「機種データを選ぶ」から読み込むと、ここでシリーズ・馬力・電源などから機器を選べるようになります。'));\n      return;\n    }\n    srcEl.textContent = models.packs.map(function (p) { return p.maker + ' ' + p.brand; }).join('　／　');\n\n    // 手順を上から順に出す。前の手順が決まっていない段階では、その先は出さない。\n    for (var i = 0; i < STEPS.length; i++) {\n      var step = STEPS[i];\n      var pool = chooserMatches(i);\n      var vals = sortOptions(step.k, [...new Set(pool.map(function (x) { return x[step.k]; }))].filter(function (v) { return v !== '' && v != null; }));\n\n      // 選択肢が1つしかないなら、迷わせず自動で決めてしまう\n      if (vals.length === 1 && chooserSel[step.k] == null) chooserSel[step.k] = vals[0];\n\n      var row = el('div', 'chooser-step');\n      var head = el('div', 'chooser-step-head');\n      head.appendChild(el('span', 'chooser-step-label', (i + 1) + '. ' + step.label));\n      if (chooserSel[step.k] != null) {\n        var clr = el('button', 'icon-btn chooser-clear', '変更'); clr.type = 'button';\n        clr.addEventListener('click', (function (kk, ii) {\n          return function () {\n            // ここから下の選択はやり直しになるので消す\n            for (var j = ii; j < STEPS.length; j++) delete chooserSel[STEPS[j].k];\n            renderChooser();\n          };\n        })(step.k, i));\n        head.appendChild(clr);\n      }\n      row.appendChild(head);\n\n      if (chooserSel[step.k] != null) {\n        var fmtSel = step.fmt ? step.fmt(chooserSel[step.k]) : chooserSel[step.k];\n        row.appendChild(el('div', 'chooser-chosen', fmtSel));\n      } else {\n        var opts = el('div', 'chooser-opts');\n        vals.forEach(function (v) {\n          var n = pool.filter(function (x) { return String(x[step.k]) === String(v); }).length;\n          var b = el('button', 'chooser-opt'); b.type = 'button';\n          b.appendChild(el('b', null, step.fmt ? step.fmt(v) : String(v)));\n          b.appendChild(el('span', null, n + '件'));\n          b.addEventListener('click', (function (kk, vv, ii) {\n            return function () {\n              chooserSel[kk] = vv;\n              for (var j = ii + 1; j < STEPS.length; j++) delete chooserSel[STEPS[j].k];\n              renderChooser();\n            };\n          })(step.k, v, i));\n          opts.appendChild(b);\n        });\n        row.appendChild(opts);\n        box.appendChild(row);\n        return;   // この手順が未選択なら、ここで止める\n      }\n      box.appendChild(row);\n    }\n\n    // 全部選び終わったので候補を出す\n    var hits = chooserMatches().slice().sort(function (a, b) { return a.y - b.y; });\n    var res = el('div', 'chooser-result');\n    res.appendChild(el('div', 'chooser-step-label', '該当 ' + hits.length + ' 機種（安い順）'));\n    if (!hits.length) {\n      res.appendChild(el('p', 'picker-empty', 'この組み合わせに合う機種がありませんでした。上の「変更」で条件を戻してください。'));\n    }\n    hits.forEach(function (x) {\n      var b = el('button', 'model-btn'); b.type = 'button';\n      var top = el('div', 'model-btn-top');\n      top.appendChild(el('i', 'item-code', x.m));\n      top.appendChild(el('span', 'model-price', yen(x.y)));\n      b.appendChild(top);\n      if (x.opt) b.appendChild(el('em', null, x.opt));\n      // マルチエアコンは室外機だけ・室内機だけの1台があるので、空の欄は出さない\n      b.appendChild(el('small', null, [\n        x.om ? '室外機 ' + x.om : '', x.im ? '室内機 ' + x.im : '',\n        x.pm ? 'パネル ' + x.pm : '', x.rm ? 'リモコン ' + x.rm : ''\n      ].filter(Boolean).join('／')));\n      b.addEventListener('click', function () {\n        var line = {\n          name: (x.mk || '') + ' ' + x.s + ' ' + x.i,\n          spec: [x.m, x.ab, x.tp, pwLabel(x.pw), x.rc, x.opt].filter(Boolean).join('　'),\n          // 定価は仕様の文字に焼き付けず、行の持ち物として覚えておく。\n          // 売値は掛率でいつでも動くので、見せる文字は印刷のたびに作り直す。\n          listPrice: x.y,\n          qty: 1,\n          unit: '台',\n          price: x.y,\n          url: x.u || ''\n        };\n        // 定価 × 仕入掛率 ＝ 原価。その原価に利益を乗せた金額を、見積に出す金額にする\n        line.base = num(x.y);\n        line.cost = lineCostFromSettings(line);\n        line.rate = modelRateFor(line);\n        addLine(line);\n        toast('「' + x.m + '」を追加しました');\n        showOptionsFor(x);\n      });\n      res.appendChild(b);\n    });\n    box.appendChild(res);\n  }\n\n  /* ======================================================================\n     必要な能力を出す（用途と広さから馬力の目安）\n     ----------------------------------------------------------------------\n     土台はカタログの「算出基準冷房負荷（W/m²）」。5社のカタログに共通で載っている。\n     （2026-09-06、NotebookLM で5社ぶん確認）\n\n     必要能力[kW] ＝ 面積[m²] × 基準負荷[W/m²] ÷ 1000\n       × 天井の高さの分 × 断熱の分 × 現場の事情の分\n\n     **数字の出どころを分けてある。**\n     ・用途別の基準負荷、断熱の倍率 …… カタログに書いてある\n     ・天井の高さ、西日・厨房などの上乗せ …… カタログに数字が無い。\n       設計の考え方から置いた**目安**なので、最後は人が決める。\n     ====================================================================== */\n  // 5社のカタログに共通で載っている幅を使う（社によってもう少し広い幅を書いているところもある）\n  var SZ_USES = [\n    { name: '一般事務所', lo: 115, hi: 170 },\n    { name: '一般商店', lo: 155, hi: 230 },\n    { name: '喫茶店・理美容室', lo: 230, hi: 290 },\n    { name: 'レストラン・飲食店', lo: 230, hi: 370 }\n  ];\n\n  // カタログの適用面積の倍率をひっくり返したもの（面積が広がる＝能力は小さくてよい）\n  var SZ_INSUL = [\n    { name: '一重窓・断熱材なし（カタログの基準）', k: 1.00 },\n    { name: '標準的な断熱材あり', k: 1 / 1.2 },\n    { name: '高断熱＋二重窓・木造', k: 1 / 1.2 },\n    { name: '高断熱＋二重窓・コンクリート造', k: 1 / 1.5 }\n  ];\n\n  // カタログに数字が無いので、設計の考え方から置いた上乗せ（目安）\n  var SZ_ADDS = [\n    { key: 'west', name: '西日が強く入る', k: 0.10 },\n    { key: 'glass', name: 'ガラス面が大きい', k: 0.10 },\n    { key: 'kitchen', name: '厨房・熱を出す機器がある', k: 0.20 },\n    { key: 'people', name: '人が多い（ピーク時）', k: 0.10 },\n    { key: 'air', name: '外気をたくさん入れる', k: 0.10 }\n  ];\n\n  /* 「横×縦」の書き方を見つけるための目印。× ｘ x X * ✕ のどれでも通す */\n  var SZ_X = /[×✕xX＊*]/;\n\n  /** 文字から広さ（m²）を読む。\n      「8.2×6」「8.2m×6m ＋ 3×2.4」→ 掛けて足す（測ったまま書ける）\n      「120㎡」→ そのまま／「36坪」→ m² に直す */\n  function szAreaFromText(s) {\n    s = String(s || '');\n    var re = /([\\d.]+)\\s*(?:m|ｍ)?\\s*[×✕xX＊*]\\s*([\\d.]+)\\s*(?:m|ｍ)?/g;\n    var sum = 0, hit = false, m;\n    while ((m = re.exec(s))) {\n      var w = Number(m[1]), d = Number(m[2]);\n      if (w > 0 && d > 0) { sum += w * d; hit = true; }\n    }\n    if (hit) return Math.round(sum * 100) / 100;\n    var ma = s.match(/([\\d.]+)\\s*(?:㎡|m2|m²|平米)/i);\n    if (ma) return Number(ma[1]);\n    var mt = s.match(/([\\d.]+)\\s*坪/);\n    if (mt) return Math.round(Number(mt[1]) * 3.3058 * 100) / 100;\n    return 0;\n  }\n\n  /** 測った部屋の一覧から広さ（m²）を出す。単位が cm なら 100 で割ってから掛ける */\n  function szRoomArea(r, unit) {\n    var d = (unit === 'cm') ? 100 : 1;\n    var a = (num(r.w) / d) * (num(r.d) / d);\n    return Math.round(a * 100) / 100;\n  }\n  function szRoomsArea(rooms, unit) {\n    var sum = 0;\n    (rooms || []).forEach(function (r) {\n      var a = szRoomArea(r, unit);\n      sum += r.minus ? -a : a;\n    });\n    return Math.round(sum * 100) / 100;   // ひきすぎたらマイナスのまま返す（呼ぶ側で気づけるように）\n  }\n  /** m² を坪にする（1坪 = 3.3058 m²）。現場では坪で話すことが多いので併記する */\n  function szTsubo(m2) { return Math.round((m2 / 3.3058) * 10) / 10; }\n\n  /** 小数のうしろのゼロを出さない（56.40 → 56.4、100.00 → 100） */\n  function szFmt(n) { return String(Math.round(n * 100) / 100); }\n\n  // 形番号（P40 など）と馬力。能力[kW] は形番号 ÷ 10\n  var SZ_FORMS = [\n    [40, 1.5], [45, 1.8], [50, 2], [56, 2.3], [63, 2.5], [71, 2.8], [80, 3], [90, 3.2],\n    [112, 4], [140, 5], [160, 6], [180, 7], [224, 8], [280, 10], [335, 12], [400, 14],\n    [450, 16], [500, 18], [560, 20]\n  ];\n\n  function szFormFor(kw) {\n    for (var i = 0; i < SZ_FORMS.length; i++) {\n      if (SZ_FORMS[i][0] / 10 >= kw - 0.001) return SZ_FORMS[i];\n    }\n    return SZ_FORMS[SZ_FORMS.length - 1];\n  }\n\n  /** 計算だけをする。画面は見積作成でも現調シートでも同じこれを使う。\n      p = { use:番号, area:m², height:m, insul:番号, adds:[key,...] } */\n  function szCalc(p) {\n    var u = SZ_USES[p.use || 0] || SZ_USES[0];\n    var ins = SZ_INSUL[p.insul || 0] || SZ_INSUL[0];\n    var area = num(p.area) || 0;\n    var h = num(p.height) || 2.7;\n    var hk = Math.max(1, h / 2.7);          // 天井が高い分は容積の比で見る\n    var addK = 1, addNames = [];\n    (p.adds || []).forEach(function (key) {\n      SZ_ADDS.forEach(function (a) { if (a.key === key) { addK += a.k; addNames.push(a.name); } });\n    });\n    var lo = area * u.lo / 1000 * ins.k * hk * addK;\n    var hi = area * u.hi / 1000 * ins.k * hk * addK;\n    var mid = (lo + hi) / 2;\n    return {\n      ok: area > 0, use: u, insul: ins, area: area, height: h, hk: hk,\n      addK: addK, addNames: addNames,\n      lo: lo, hi: hi, mid: mid,\n      form: szFormFor(mid), formHi: szFormFor(hi)\n    };\n  }\n\n  /** その形の機種が機種データにあるか探す。無ければ1つ上の形へ */\n  function szFindModels(form) {\n    if (!models || !models.items || !models.items.length) return null;\n    var formOf = function (x) {\n      var m = String(x.ab || '').match(/(\\d{2,3})\\s*[形型]/);\n      return m ? Number(m[1]) : 0;\n    };\n    for (var i = 0; i < SZ_FORMS.length; i++) {\n      if (SZ_FORMS[i][0] < form[0]) continue;\n      var need = SZ_FORMS[i][0];\n      var hit = models.items.filter(function (x) { return formOf(x) === need; });\n      if (hit.length) return { form: SZ_FORMS[i], count: hit.length, moved: need !== form[0] };\n    }\n    return null;\n  }\n\n\n  /* ----------------------------------------------------------------------\n     選んだ機種に付けられる別売品を出す\n     ----------------------------------------------------------------------\n     カタログの表では、列の見出しが「その別売品の付く室内機」になっている。\n     見出しは「FHCP40〜71GA」のようなまとめ書きなので、\n     容量のところを開いて、選んだ機種の室内機品番と突き合わせる\n     （突き合わせは catalog.js の optionsFor がやる）。\n     ---------------------------------------------------------------------- */\n  /* 別売品の欄に一言だけ出す。\n     何も出ないと「壊れている」のか「まだ入れていない」のか分からない\n     （2026-09-09、BIGBOSSが「機種えらんでも別売品でないよ」で気づかせてくれた） */\n  function optNote(box, msg) {\n    box.appendChild(el('p', 'picker-empty', msg));\n  }\n\n  function showOptionsFor(x) {\n    var box = $('#chooser-options');\n    if (!box) return;\n    box.innerHTML = '';\n    if (!window.KUCHOO_CATALOG || !KUCHOO_CATALOG.optionsFor) return;\n    if (!optStores.length) {\n      optNote(box, 'パネル・リモコンなどの別売品は、まだ読み込まれていません。' +\n        '［単価マスタ］の「カタログPDFから機種データを作る」で、' +\n        'メーカー名のうしろに「（別売品）」と付いたものを選んで読ませると、ここに出ます。');\n      return;\n    }\n\n    /* そのメーカーの別売品だけを見る。\n       ダイキンの機種にパナソニックのパネルを出してはいけない。 */\n    var same = function (a, b) {\n      if (!a || !b) return false;\n      return String(a).indexOf(String(b)) >= 0 || String(b).indexOf(String(a)) >= 0;\n    };\n    /* 同じダイキンでも、業務用（スカイエア）とルームエアコンの別売品は別もの。\n       業務用の「どの機種にも付く」吹出口やチャンバが、ルームエアコンを選んだときに出てはいけない */\n    var room = function (t) { return /ルームエアコン/.test(t || ''); };\n    var list = [];\n    optStores.forEach(function (s) {\n      if (x.mk && s.maker && !same(x.mk, s.maker)) return;\n      if (room(x.br) !== room(s.brand)) return;\n      KUCHOO_CATALOG.optionsFor(s, x).forEach(function (o) {\n        /* 「どの機種にも付く」ものと、この機種のために選ばれたものを分ける。\n           ダイキンは吹出口やチャンバなど353品目が「どの機種にも」に入るので、\n           混ぜると、この機種のパネルやリモコンが埋もれてしまう */\n        var common = (o.fits || []).length > 0 &&\n          (o.fits || []).every(function (f) { return f && f.all; });\n        list.push({ code: o.code, name: o.name, y: o.y, fits: o.fits, maker: s.maker, common: common });\n      });\n    });\n    if (!list.length) {\n      var mine = false, have = [];\n      optStores.forEach(function (s) {\n        have.push(s.maker);\n        if (same(x.mk, s.maker)) mine = true;\n      });\n      optNote(box, mine\n        ? '読み込んである ' + have.join('・') + ' の別売品の中に、「' + x.m + '」に付くものは見つかりませんでした。'\n        : (x.mk || 'このメーカー') + ' の別売品はまだ読み込まれていません（いま入っているのは ' + have.join('・') + '）。');\n      return;\n    }\n\n    var head = el('div', 'opt-head');\n    head.appendChild(el('b', null, '「' + x.m + '」に付けられる別売品'));\n    head.appendChild(el('span', 'models-num', list.length + '品目'));\n    var close = el('button', 'icon-btn', '✕');\n    close.type = 'button';\n    close.title = '閉じる';\n    close.addEventListener('click', function () { box.innerHTML = ''; });\n    head.appendChild(close);\n    box.appendChild(head);\n\n    /* 1台に700品目も出ると現場では選べない。品名で仲間に分けて絞れるようにする */\n    var cats = {};\n    /* セット価格にすでに入っているものを見分ける。\n\n       リモコンを選んで機種を決めたのに、別売品にそのリモコンが出てきて、\n       押すと定価がまるごと足されていた。日立のかべかけなら\n       セットに PC-ARFG4 が入っているのに、別売品の\n       「多機能デザインリモコン PC-ARFG4 ¥42,000」を押すと二重取りになる。\n       機種データの rm（付属リモコン）と pm（付属パネル）が、その印になる\n       （2026-09-09、BIGBOSSの指摘） */\n    var INCLUDED = [\n      { has: x.rm, word: /リモコン/, skip: /ケース|ケーブル|カバー|受光部|ホルダ|架台/, nm: 'リモコン' },\n      // ビルトインの化粧グリル・壁埋込の前面グリルも、パネルと同じく「付いてくるもの」\n      // （パナソニックのルームエアコンの合計は CZ-BT10-W 込み。2026-09-12）\n      { has: x.pm, word: /パネル|化粧グリル|前面グリル/, skip: /フィルタ|スペーサ|カバー|ろ材/, nm: '化粧パネル・グリル' }\n    ];\n    /** 品番から、読み込んである別売品を探す（付いてくるものの定価を知るため） */\n    function optByCode(code) {\n      var hit = null;\n      optStores.forEach(function (st2) {\n        (st2.items || []).forEach(function (it) {\n          if (!hit && String(it.code) === String(code)) hit = it;\n        });\n      });\n      return hit;\n    }\n\n    list.forEach(function (o) {\n      var onm = o.name || '';\n      INCLUDED.forEach(function (k) {\n        if (o.dup || !k.has) return;\n        // 付いてくるものが2つのときは「・」でつないである（前面グリルと据付枠「KDG413C10・KKF411B10B」「CZ-BKG22A・CZ-BKF2」）。\n        // 入れ替えの相手は1つ目（グリル）\n        var hasList = String(k.has).split('・');\n        if (hasList.indexOf(String(o.code)) >= 0) {\n          o.dup = 'この機種に付いてくるもの';\n          o.dupMsg = 'この機種の値段には、この' + k.nm + '（' + o.code + '）がもう入っています。\\n' +\n                     'そのまま足すと二重取りになります。\\n\\n' +\n                     'それでも明細に入れますか？';\n          return;\n        }\n        if (k.word.test(onm) && !k.skip.test(onm)) {\n          o.dup = '入れ替え';\n          o.swapKind = k.nm;\n          var inc = optByCode(hasList[0]);\n          o.swapCode = hasList[0];\n          o.swapName = (inc && inc.name) || hasList[0];\n          o.swapY = inc ? num(inc.y) : 0;\n          if (o.swapY > 0) {\n            /* 付いてくるものの定価が分かるので、差し引く明細も一緒に入れられる。\n               「差額を入れてください」と人にやらせると、まず忘れる\n               （2026-09-09、BIGBOSSの案） */\n            o.dupMsg = 'この機種には ' + o.swapName + '（' + o.swapCode + '）が付いています。\\n' +\n                       'こちらに取り替えるので、付いてくるぶんを差し引きます。\\n\\n' +\n                       '　' + (o.name || o.code) + '　＋' + yen(o.y) + '\\n' +\n                       '　' + o.swapName + '　−' + yen(o.swapY) + '\\n' +\n                       '　差引　' + (o.y - o.swapY < 0 ? '−' + yen(o.swapY - o.y) : '＋' + yen(o.y - o.swapY)) + '\\n\\n' +\n                       'よろしいですか？';\n          } else {\n            o.dupMsg = 'この機種には ' + hasList[0] + ' が付いています。\\n' +\n                       'これに取り替えるなら、差額だけを入れてください。\\n' +\n                       '（' + hasList[0] + ' の定価が別売品データに無いので、差し引きは自動でできません）\\n\\n' +\n                       'それでも定価 ' + yen(o.y) + ' で入れますか？';\n          }\n        }\n      });\n    });\n\n    var COMMON_CAT = 'どの機種にも使えるもの';\n    list.forEach(function (o) {\n      o.cat = o.common ? COMMON_CAT\n                       : (KUCHOO_CATALOG.optCategory ? KUCHOO_CATALOG.optCategory(o.name) : 'その他');\n      cats[o.cat] = (cats[o.cat] || 0) + 1;\n    });\n    // 仕事でよく使う順に並べる（catalog.js が持っている順）\n    var fixed = (KUCHOO_CATALOG.catOrder || []);\n    var order = Object.keys(cats).sort(function (a, b) {\n      // 「どの機種にも」はいちばん後ろ。先に出すのはこの機種のためのもの\n      var ia = a === COMMON_CAT ? 999 : fixed.indexOf(a);\n      var ib = b === COMMON_CAT ? 999 : fixed.indexOf(b);\n      return (ia < 0 ? 99 : ia) - (ib < 0 ? 99 : ib);\n    });\n    var pick = order.length > 1 ? order[0] : '';   // 最初はいちばん多い仲間だけ出す\n\n    var catBox = el('div', 'picker-cats');\n    box.appendChild(catBox);\n    var wrap = el('div', 'picker-items');\n    box.appendChild(wrap);\n\n    function drawCats() {\n      catBox.innerHTML = '';\n      var all = el('button', 'cat-btn' + (pick ? '' : ' is-active'), 'すべて' + list.length);\n      all.type = 'button';\n      all.addEventListener('click', function () { pick = ''; drawCats(); drawItems(); });\n      catBox.appendChild(all);\n      order.forEach(function (c) {\n        var b = el('button', 'cat-btn' + (pick === c ? ' is-active' : ''), c + cats[c]);\n        b.type = 'button';\n        b.addEventListener('click', function () { pick = c; drawCats(); drawItems(); });\n        catBox.appendChild(b);\n      });\n    }\n\n    function drawItems() {\n      wrap.innerHTML = '';\n      list.filter(function (o) { return !pick || o.cat === pick; }).forEach(function (o) {\n      var b = el('button', 'item-btn' + (o.dup ? ' is-included' : ''));\n      b.type = 'button';\n      b.appendChild(el('b', null, o.name || o.code));\n      b.appendChild(el('span', 'item-code', o.code));\n      if (o.dup) b.appendChild(el('span', 'item-dup', o.dup));\n      b.appendChild(el('em', null, yen(o.y)));\n      b.addEventListener('click', function () {\n        if (o.dupMsg && !confirm(o.dupMsg)) return;\n        var line = {\n          name: (o.maker || '') + '　' + (o.name || o.code),\n          spec: o.code,\n          listPrice: o.y,\n          qty: 1,\n          unit: '個',\n          price: o.y\n        };\n        line.base = num(o.y);\n        line.cost = lineCostFromSettings(line);\n        line.rate = modelRateFor(line);\n        addLine(line);\n\n        // 取り替えるときは、機種の値段に入っているぶんをマイナスの行で戻す\n        if (o.swapY > 0) {\n          var back = {\n            name: (o.maker || '') + '　' + o.swapName + '（本体付属分を差引）',\n            spec: o.swapCode,\n            listPrice: -o.swapY,\n            qty: 1,\n            unit: '個',\n            price: -o.swapY\n          };\n          back.base = -o.swapY;\n          back.rate = modelRateFor(back);\n          back.cost = -Math.abs(num(lineCostFromSettings({ name: back.name, spec: back.spec, base: o.swapY, price: o.swapY })));\n          addLine(back);\n          toast('「' + (o.name || o.code) + '」を追加し、' + o.swapName + ' を差し引きました');\n          return;\n        }\n        toast('「' + (o.name || o.code) + '」を追加しました');\n      });\n      wrap.appendChild(b);\n      });\n    }\n\n    drawCats();\n    drawItems();\n  }\n\n  $('#btn-chooser-reset').addEventListener('click', function () {\n    chooserSel = {};\n    renderChooser();\n    var ob = $('#chooser-options');\n    if (ob) ob.innerHTML = '';\n  });\n\n  /**\n   * 機種データ1つぶんを取り込む。\n   * 同じメーカー・同じシリーズ群のものがあれば入れ替え、無ければ足す。\n   * 戻り値は画面に出す一行（取り込めなければ null）。\n   */\n  function adoptModelPack(data) {\n    var decoded = decodeModels(data);\n    if (!decoded || !decoded.items.length) return null;\n\n    var raws = rawPacks(load(KEY_MDL, null));\n    var at = -1;\n    raws.forEach(function (r, i) { if (packKey(r) === packKey(data)) at = i; });\n    if (at >= 0) raws[at] = data; else raws.push(data);\n\n    if (save(KEY_MDL, { v: 2, packs: raws }) === false) return null;\n    return decoded.maker + ' ' + decoded.items.length + '件' + (at >= 0 ? '（入れ替え）' : '');\n  }\n\n  /** ファイルを1つ読んでJSONにする。読めなければ null を渡す */\n  function readJsonFile(f, done) {\n    var r = new FileReader();\n    r.onerror = function () { done(null); };\n    r.onload = function () {\n      var data = null;\n      try { data = JSON.parse(r.result); } catch (e) { data = null; }\n      done(data);\n    };\n    r.readAsText(f);\n  }\n\n  /* ----------------------------------------------------------------------\n     ボタンの取り違いを止める\n     ----------------------------------------------------------------------\n     単価マスタの画面には、ファイルを選ぶボタンが3つ並んでいる。\n     機種データ／別売品データ／全データ。ファイルの名前は似ているし、\n     どれがどのボタンのものかは書いていない。2026-09-09、BIGBOSSが\n     4つのファイルを別売品のボタンで選び、別売品1つだけが通って\n     機種データ3つが黙って弾かれた。画面は「読めませんでした」としか\n     言わなかったので、入っていないことに丸一日気づかなかった。\n     だから、弾くときは「これは何のデータで、どのボタンから入れるのか」を言う。\n     ---------------------------------------------------------------------- */\n\n  /** 選んだファイルが何のデータかを見分ける。分からなければ null */\n  function fileKind(data) {\n    if (!data || typeof data !== 'object') return null;\n    if (data.type === 'airtec-all') return 'all';\n    if (Array.isArray(data.fields) && Array.isArray(data.rows)) return 'models';\n    if (Array.isArray(data.stores) || (data.maker && Array.isArray(data.items))) return 'options';\n    return null;\n  }\n\n  var KIND_NAME  = { models: '機種データ', options: '別売品データ', all: '全データのバックアップ' };\n  var KIND_WHERE = {\n    models:  '「機種データを選ぶ」',\n    options: '「別売品データを選ぶ」',\n    all:     '［設定］の「まとめて読み込む」'\n  };\n\n  /** ボタンの下に赤い案内を出す（空文字で消す） */\n  function setFileHint(id, msg) {\n    var box = $('#' + id);\n    if (!box) return;\n    box.textContent = msg || '';\n    box.hidden = !msg;\n  }\n\n  /** 取り違えたファイルの案内文を組み立てる */\n  function wrongFileHint(wrong) {\n    if (!wrong.length) return '';\n    return wrong.map(function (w) {\n      return '「' + w.name + '」は' + KIND_NAME[w.kind] + 'です。'\n           + KIND_WHERE[w.kind] + 'から入れてください。';\n    }).join('\\n');\n  }\n\n  $('#file-models').addEventListener('change', function (ev) {\n    var files = Array.prototype.slice.call(ev.target.files || []);\n    ev.target.value = '';\n    if (!files.length) return;\n    setFileHint('models-hint', '');\n\n    var done = [], failed = [], wrong = [], i = 0;\n\n    // 1つずつ順に読む（まとめて読むと、同じ保存先を取り合って上書きし合うため）\n    (function next() {\n      if (i >= files.length) { finish(); return; }\n      var f = files[i++];\n      readJsonFile(f, function (data) {\n        var line = data ? adoptModelPack(data) : null;\n        if (line) { done.push(line); next(); return; }\n        var kind = fileKind(data);\n        if (kind && kind !== 'models') wrong.push({ name: f.name, kind: kind });\n        else failed.push(f.name);\n        next();\n      });\n    })();\n\n    function finish() {\n      chooserSel = {};\n      loadModels();\n      setFileHint('models-hint', wrongFileHint(wrong));\n\n      var msg = [];\n      if (done.length) msg.push(done.length === 1 ? done[0] + ' を入れました'\n                                                  : done.length + 'メーカーを入れました（' + done.join('／') + '）');\n      if (wrong.length) msg.push('ボタンが違うものが' + wrong.length + '件（下の赤い字を見てください）');\n      if (failed.length) msg.push('読めなかった: ' + failed.join('、'));\n      toast(msg.join('　') || '機種データとして読めませんでした');\n    }\n  });\n\n  $('#btn-models-clear').addEventListener('click', function () {\n    if (!models) return;\n    if (!confirm('読み込んだ機種データを、すべてのメーカーぶん削除します。よろしいですか？\\n（見積の明細に入れた機器はそのまま残ります）')) return;\n    removeKey(KEY_MDL);\n    chooserSel = {};\n    loadModels();\n    toast('機種データを削除しました');\n  });\n\n  /* ======================================================================\n     カタログPDFから機種データを作る\n     ----------------------------------------------------------------------\n     メーカーのデジタルカタログからPDFを保存して、ここに入れると、\n     その端末の中だけで読み取って機種データができる。\n\n     ・PDFも、読み取った中身も、どこにも送らない\n     ・よそのサイトも叩かない（社内のPCでも動く）\n     ・読む部品（480KB＋ワーカー2MB）は、押したときだけ読み込む\n\n     どの文字を手がかりに読むかは catalog.js に書いてある。\n     カタログの作りが変わって読めなくなったら、直すのはそちら。\n     ====================================================================== */\n  var catalogLibReady = false;\n\n  function ensureCatalogLib() {\n    if (catalogLibReady) return Promise.resolve();\n    return loadScript('vendor/pdfparse/pdf-parse.umd.js?v=' + APP_VERSION)\n      .then(function () { catalogLibReady = true; });\n  }\n\n  /** 途中経過や結果を出す。改行はそのまま行に分ける */\n  function catalogNote(msg, kind) {\n    var box = $('#catalog-status');\n    if (!box) return;\n    box.className = 'csv-note' + (kind ? ' ' + kind : '');\n    box.textContent = '';\n    String(msg).split('\\n').forEach(function (line, i) {\n      if (i) box.appendChild(document.createElement('br'));\n      box.appendChild(document.createTextNode(line));\n    });\n  }\n\n  function catalogMakers() {\n    return (window.KUCHOO_CATALOG && KUCHOO_CATALOG.makers) || [];\n  }\n\n  function currentCatalogMaker() {\n    var sel = $('#catalog-maker');\n    var id = sel ? sel.value : '';\n    var found = null;\n    catalogMakers().forEach(function (m) { if (m.id === id) found = m; });\n    return found;\n  }\n\n  /** 選んだメーカーの「どこからPDFを取るか」を出す */\n  function renderCatalogHowto() {\n    var box = $('#catalog-howto');\n    if (!box) return;\n    box.innerHTML = '';\n    var mk = currentCatalogMaker();\n    if (!mk) return;\n\n    var ol = el('ol', 'catalog-howto');\n    mk.howto.forEach(function (s) { ol.appendChild(el('li', null, s)); });\n    box.appendChild(ol);\n\n    var a = el('a', 'catalog-link', mk.catalog + '（' + mk.name + '）のカタログを開く');\n    a.href = mk.url;\n    a.target = '_blank';\n    a.rel = 'noopener';\n    box.appendChild(a);\n\n    // 因幡のように、軽いほうの冊子がもう1本あるメーカー用\n    if (mk.url2) {\n      var b = el('a', 'catalog-link', 'エアコン配管部材だけの価格表を開く（軽いほう）');\n      b.href = mk.url2;\n      b.target = '_blank';\n      b.rel = 'noopener';\n      box.appendChild(b);\n    }\n    box.appendChild(el('div', 'catalog-size', mk.size));\n    if (mk.urlNote) box.appendChild(el('div', 'catalog-size', mk.urlNote));\n  }\n\n  /* ----------------------------------------------------------------------\n     別売品（どの室内機に付くかの情報つき）\n     ---------------------------------------------------------------------- */\n  // メーカーごとに持つ（機種データと同じ）。1つしか持てないと、\n  // 2社目を入れたとたん1社目が消える\n  var optStores = [];\n\n  function loadOptions() {\n    var raw = load(KEY_OPT, null);\n    if (raw && raw.stores) optStores = raw.stores;\n    else if (raw && raw.items) optStores = [raw];      // 前の形（1社だけ）も読めるように\n    else optStores = [];\n    renderOptionsStatus();\n  }\n\n  function renderOptionsStatus() {\n    var box = $('#options-status');\n    if (!box) return;\n    box.innerHTML = '';\n    if (!optStores.length) { box.textContent = 'まだ読み込まれていません。'; return; }\n\n    optStores.forEach(function (s, i) {\n      var row = el('div', 'models-row');\n      row.appendChild(el('b', null, s.maker + '　' + (s.brand || '別売品')));\n      row.appendChild(el('span', 'models-num', (s.items || []).length + '品目'));\n      if (s.fetched) row.appendChild(el('span', null, '取得日 ' + s.fetched));\n      var del = el('button', 'icon-btn', '✕');\n      del.type = 'button';\n      del.title = s.maker + ' の別売品だけを削除';\n      del.addEventListener('click', function () {\n        if (!confirm(s.maker + ' の別売品を削除します。よろしいですか？\\n（ほかのメーカーは残ります）')) return;\n        var rest = optStores.slice();\n        rest.splice(i, 1);\n        if (rest.length) save(KEY_OPT, { v: 1, stores: rest });\n        else removeKey(KEY_OPT);\n        loadOptions();\n        toast(s.maker + ' の別売品を削除しました');\n      });\n      row.appendChild(del);\n      box.appendChild(row);\n    });\n  }\n\n  /* 読み取った別売品を、ファイルにして持ち出す／別の端末で読み込む。\n     カタログPDFの読み取りは1社で数分かかる。1度読めばファイルにできるので、\n     2台目のパソコンやスマホでは、選ぶだけで済む。 */\n  $('#btn-options-save').addEventListener('click', function () {\n    if (!optStores.length) { toast('別売品がまだ読み込まれていません'); return; }\n    download('空調王-別売品-' + todayISO() + '.json', JSON.stringify({ v: 1, stores: optStores }));\n    toast('別売品データを書き出しました');\n  });\n\n  $('#file-options').addEventListener('change', function (ev) {\n    var files = Array.prototype.slice.call(ev.target.files || []);\n    ev.target.value = '';\n    if (!files.length) return;\n    setFileHint('options-hint', '');\n\n    var done = [], failed = [], wrong = [], i = 0;\n    // 1つずつ順に読む（まとめて読むと、同じ保存先を取り合って上書きし合う）\n    (function next() {\n      if (i >= files.length) { finish(); return; }\n      var f = files[i++];\n      readJsonFile(f, function (data) {\n        var packs = optPacksOf(data);\n        if (!packs.length) {\n          var kind = fileKind(data);\n          if (kind && kind !== 'options') wrong.push({ name: f.name, kind: kind });\n          else failed.push(f.name);\n          next();\n          return;\n        }\n        packs.forEach(function (s) { putOptStore(s); done.push(s.maker + ' ' + (s.items || []).length + '品目'); });\n        next();\n      });\n    })();\n\n    function finish() {\n      loadOptions();\n      setFileHint('options-hint', wrongFileHint(wrong));\n\n      var msg = [];\n      if (done.length) msg.push('別売品を入れました（' + done.join('／') + '）');\n      if (wrong.length) msg.push('ボタンが違うものが' + wrong.length + '件（下の赤い字を見てください）');\n      if (failed.length) msg.push('読めなかった: ' + failed.join('、'));\n      toast(msg.join('　') || '別売品データとして読めませんでした');\n    }\n  });\n\n  /** ファイルの中から別売品の束を取り出す。書き出したファイルでも、全データでも読めるように */\n  function optPacksOf(data) {\n    if (!data) return [];\n    var raw = data.options || data;          // 全データの中の options でも、別売品だけのファイルでも\n    var list = raw.stores || (raw.items ? [raw] : []);\n    return list.filter(function (s) { return s && s.maker && (s.items || []).length; });\n  }\n\n  /* 別売品の束を見分ける鍵。メーカー名だけで見ると、ダイキンのルームエアコンの別売品を入れたとたんに\n     業務用（スカイエア）の別売品が消える。機種データと同じく「メーカー｜ブランド」で見る */\n  function optKey(s) { return (s && s.maker || '') + '｜' + (s && s.brand || ''); }\n\n  /** 1社ぶんを入れる（同じメーカー・同じブランドがあれば入れ替え）。保存はまとめて1回 */\n  function putOptStore(store) {\n    var stores = optStores.slice(), at = -1;\n    stores.forEach(function (s, i) { if (optKey(s) === optKey(store)) at = i; });\n    if (at >= 0) stores[at] = store; else stores.push(store);\n    save(KEY_OPT, { v: 1, stores: stores });\n    optStores = stores;\n  }\n\n  function adoptOptions(store, extra) {\n    var stores = optStores.slice();\n    var at = -1;\n    stores.forEach(function (s, i) { if (optKey(s) === optKey(store)) at = i; });\n    if (at >= 0) stores[at] = store; else stores.push(store);\n\n    if (save(KEY_OPT, { v: 1, stores: stores }) === false) {\n      catalogNote('読み取れましたが、保存できませんでした。端末の空きが足りないかもしれません。', 'ng');\n      return;\n    }\n    loadOptions();\n    catalogNote(store.items.length + '品目の別売品を読み取りました。' + (extra || '') +\n                (at >= 0 ? '（入れ替え）' : '') +\n                '\\n機器を選ぶと、その機種に付く別売品が下に出ます。', 'ok');\n    toast(store.items.length + '品目を入れました');\n  }\n\n  /** 読み取れた機種データを入れる。r は catalog.js が返したもの */\n  function adoptCatalogPack(pack, count, extra) {\n    var line = adoptModelPack(pack);\n    if (!line) {\n      catalogNote('読み取れましたが、保存できませんでした。端末の空きが足りないかもしれません。', 'ng');\n      return;\n    }\n    chooserSel = {};\n    loadModels();\n    catalogNote(count + '機種を読み取って、機種データに入れました。' + (extra || '') +\n                '\\n「見積を作る」の［機器を選ぶ］から使えます。', 'ok');\n    toast(count + '機種を入れました');\n  }\n\n  /**\n   * 材料メーカーのカタログ（価格改定表）から読み取った部材を、単価マスタに入れる。\n   *\n   * 機器のカタログと違って、これは**入れ替えではなく値段の更新**が本筋。\n   * 部材はすでに単価マスタに入っていて、値上げのたびに同じ品番の値段だけが変わる。\n   * だからCSVの取り込み（後ろに足す）をそのまま使うと、2回目で行が倍になる。\n   *\n   *   すでにある品番 → 値段を書き替える（いくらから いくらに、を見せてから）\n   *   無い品番       → 新しく足す\n   *   値段が同じ     → 触らない\n   */\n  function adoptCatalogParts(parts, extra) {\n    var rows = (parts && parts.rows) || [];\n    if (!rows.length) { catalogNote('部材が1件も読み取れませんでした。', 'ng'); return; }\n\n    // いまの単価マスタを品番で引けるようにする\n    var byCode = {};\n    pb.categories.forEach(function (c) {\n      c.items.forEach(function (it) {\n        var k = String(it.code || '').trim().toUpperCase();\n        if (k && !byCode[k]) byCode[k] = { cat: c, item: it };\n      });\n    });\n\n    var changed = [], added = [], odd = [], same = 0;\n    rows.forEach(function (r) {\n      var code = String(r.m || '').trim();\n      var price = num(r.y);\n      if (!code || !price) return;\n      var hit = byCode[code.toUpperCase()];\n      if (hit) {\n        var old = num(hit.item.price);\n        if (old === price) { same++; return; }\n        /* けたが違うものは、値上げではなく**単位の違い**。\n           ペアコイルは紙面が「1巻 ¥71,800（20m）」で、単価マスタは1mあたり ¥3,590。\n           そのまま書き替えると、20mの配管が20倍の値段になる。\n           2026-09-10、BIGBOSSの実データで15件（すべて単位が m）がこれだった。\n           勝手に直さず、別に数えて見せるだけにする。 */\n        // 値段が空（¥0）のものは、けた違いではなく「まだ値段が入っていない」だけ。そのまま入れる。\n        // 8月にお配りしたユーシー産業のCSVは、メーカーが値段を出していなかったので全部¥0だった。\n        // これを「けた違い」で止めると、せっかくカタログに値段があっても1件も入らない\n        if (!old) { changed.push({ item: hit.item, from: 0, to: price, code: code }); return; }\n        var ratio = price / old;\n        if (ratio > 3 || ratio < 1 / 3) {\n          odd.push({ item: hit.item, from: old, to: price, code: code, unit: hit.item.unit || '' });\n          return;\n        }\n        changed.push({ item: hit.item, from: old, to: price, code: code });\n      } else {\n        added.push({\n          catName: parts.maker + '｜' + (r.series || 'その他'),\n          item: {\n            code: code, name: r.name || code, spec: r.ref || '',\n            url: '', color: '', unit: '個', price: price\n          }\n        });\n      }\n    });\n\n    if (!changed.length && !added.length && !odd.length) {\n      catalogNote(rows.length + '件を読み取りました。' + (extra || '') +\n                  '\\n値段はすべて、いま入っているものと同じでした。直すところはありません。', 'ok');\n      toast('値段の変わったものはありませんでした');\n      return;\n    }\n\n    // 何がどう変わるのかを、数字で見せてから聞く\n    var sample = changed.slice(0, 5).map(function (c) {\n      return '　' + c.code + '　' + yen(c.from) + ' → ' + yen(c.to);\n    }).join('\\n');\n    var oddSample = odd.slice(0, 3).map(function (c) {\n      return '　' + c.code + '　' + yen(c.from) + '（' + (c.unit || '単位なし') + '） ↔ 紙面 ' + yen(c.to);\n    }).join('\\n');\n    var msg = parts.maker + ' の価格表から ' + rows.length + '件を読み取りました。\\n\\n' +\n      '　値段が変わるもの　' + changed.length + '件\\n' +\n      '　新しく増えるもの　' + added.length + '件\\n' +\n      '　そのままのもの　　' + same + '件\\n' +\n      (odd.length ? '　けた違い（さわりません）　' + odd.length + '件\\n' : '') +\n      (changed.length ? '\\n【値段が変わるもの（最初の' + Math.min(5, changed.length) + '件）】\\n' + sample + '\\n' : '') +\n      (odd.length ? '\\n【けた違い＝単位が違うと思われるもの（最初の' + Math.min(3, odd.length) + '件）】\\n' + oddSample +\n                    '\\nペアコイルのように、紙面は「1巻いくら」で単価マスタは「1mいくら」のものです。\\n' +\n                    'ここは直しません。必要なら手で直してください。\\n' : '') +\n      '\\nこの内容で入れますか？';\n    if (!confirm(msg)) { catalogNote('やめました。単価マスタは何も変えていません。'); return; }\n\n    changed.forEach(function (c) { c.item.price = c.to; });\n    added.forEach(function (a) { findOrCreateCategory(a.catName).items.push(a.item); });\n\n    if (savePB() === false) {\n      catalogNote('読み取れましたが、保存できませんでした。端末の空きが足りないかもしれません。', 'ng');\n      return;\n    }\n    renderMaster(); renderPicker();\n    catalogNote(changed.length + '件の値段を直し、' + added.length + '件を足しました。' + (extra || '') +\n                (odd.length ? '\\nけた違いの ' + odd.length + '件は、単位が違うのでさわっていません。' : '') +\n                '\\n［単価マスタ］の一覧で確かめられます。', 'ok');\n    toast('値段' + changed.length + '件・追加' + added.length + '件');\n  }\n\n  function initCatalogBox() {\n    var sel = $('#catalog-maker');\n    var file = $('#file-catalog');\n    if (!sel || !file) return;\n\n    var list = catalogMakers();\n    if (!list.length) {\n      catalogNote('この版では、まだカタログの読み取りが使えません。');\n      return;\n    }\n    list.forEach(function (m) {\n      var o = document.createElement('option');\n      o.value = m.id;\n      o.textContent = m.name;\n      sel.appendChild(o);\n    });\n    sel.addEventListener('change', renderCatalogHowto);\n    renderCatalogHowto();\n    loadOptions();\n\n    file.addEventListener('change', function (ev) {\n      var f = (ev.target.files || [])[0];\n      ev.target.value = '';\n      if (!f) return;\n      var mk = currentCatalogMaker();\n      if (!mk) { catalogNote('先にメーカーを選んでください。', 'ng'); return; }\n      // 三菱だけはPDFではなくデータファイル（.json）を読む\n      var wantJson = (mk.kind === 'json');\n      if (!(wantJson ? /\\.(json|txt)$/i.test(f.name) : /\\.pdf$/i.test(f.name))) {\n        catalogNote(wantJson\n          ? '保存したデータファイル（.json）を選んでください。'\n          : 'カタログのPDFを選んでください。', 'ng');\n        return;\n      }\n\n      // 大きいカタログは読み取りに数分かかり、そのあいだパソコン全体が重くなる。\n      // 黙って始めると「固まった」と思われるので、先に断る。\n      var mb = Math.round(f.size / 1024 / 1024);\n      if (f.size > 200 * 1024 * 1024) {\n        if (!confirm('このカタログは ' + mb + 'MB あります。\\n\\n' +\n                     '読み取りに数分かかり、そのあいだパソコンが重くなります。\\n' +\n                     'ほかのアプリ（LINE・Discord・動画など）を先に閉じておくと安全です。\\n\\n' +\n                     '始めますか？')) {\n          catalogNote('やめました。ほかのアプリを閉じてから、もう一度選んでください。');\n          return;\n        }\n      }\n\n      var t0 = Date.now();\n      file.disabled = true;\n      catalogNote('読み取りの部品を用意しています…');\n\n      ensureCatalogLib().then(function () {\n        catalogNote(mk.name + ' のカタログを読んでいます…');\n        return KUCHOO_CATALOG.run(f, mk.id, function (done, total) {\n          catalogNote(mk.name + ' のカタログを読んでいます…　' + done + ' / ' + total + 'ページ' +\n                      '\\nこの画面は閉じないでください。');\n        });\n      }).then(function (r) {\n        var sec = Math.round((Date.now() - t0) / 1000);\n        var time = (sec >= 60 ? Math.floor(sec / 60) + '分' + (sec % 60) + '秒' : sec + '秒');\n        // 紙のカタログのときだけ「価格ページ○枚」を出す（三菱はデータファイルなので枚数が無い）\n        var extra = (wantJson || !r.pricePages) ? '（' + time + '）'\n                                                : '（価格ページ ' + r.pricePages + '枚／' + time + '）';\n        if (r.parts) adoptCatalogParts(r.parts, extra);\n        else if (r.options) adoptOptions(r.options, extra);\n        else adoptCatalogPack(r.pack, r.count, extra);\n      }).catch(function (e) {\n        var msg = (e && e.message) || 'うまく読み取れませんでした';\n        catalogNote(msg, 'ng');\n        // 「いつもより少ない」で止めたときだけ、人が見て入れられるようにする\n        if (e && e.soft && e.pack) {\n          var box = $('#catalog-status');\n          var btn = el('button', 'btn btn-ghost', 'それでも ' + e.rows + '件を入れる');\n          btn.type = 'button';\n          btn.style.marginTop = '8px';\n          btn.addEventListener('click', function () { adoptCatalogPack(e.pack, e.rows, ''); });\n          box.appendChild(document.createElement('br'));\n          box.appendChild(btn);\n        }\n      }).then(function () {\n        file.disabled = false;\n      });\n    });\n  }\n\n  initCatalogBox();\n\n  /* ======================================================================\n     見積書の印刷\n     ====================================================================== */\n  /**\n   * 機種の行に「定価 ¥○○ → ¥△△」を出すための文字を作る。\n   * ・定価より安くなっているときだけ出す（同じ額なら見せる意味がない）\n   * ・売値は掛率でいつでも動くので、印刷のたびにここで作り直す\n   */\n  function listPriceHTML(l) {\n    var list = num(l.listPrice), now = num(l.price);\n    if (!list || !now || list <= now) return '';\n    return '<span class=\"l-list\">定価 <s>' + yen(list) + '</s> → <b>' + yen(now) + '</b></span>';\n  }\n\n  function buildSheet(mode, doc) {\n    var d = doc || st;\n    mode = mode || 'estimate';\n    /* 見た目を先に着せる。paginateSheet は実際の高さを測って紙を割るので、\n       あとから着せると線の太さの分だけ割り方がずれる */\n    $('#sheet').className = 'skin-' + skinId(pb.company.sheetSkin);\n    $('#sheet').style.setProperty('--sheet-accent', colorHex(pb.company.sheetColor));\n    var inv = (mode === 'invoice');\n    var t = calcOf(d);\n    var c = pb.company;\n    var to = (d.customer || '').trim();\n    var hon = d.honorific === '（なし）' ? '' : ('　' + d.honorific);\n    var validUntil = d.validDays ? jpDate(addDays(d.date, d.validDays)) + 'まで' : '';\n\n    var rowList = [];\n    d.lines.forEach(function (l, i) {\n      rowList.push(\n        '<tr>' +\n          '<td class=\"t-no\">' + (i + 1) + '</td>' +\n          '<td>' + esc(l.name) + (l.spec ? '<span class=\"l-spec\">' + esc(l.spec) + '</span>' : '') + listPriceHTML(l) + '</td>' +\n          '<td class=\"t-qty\">' + (num(l.qty) % 1 === 0 ? num(l.qty) : num(l.qty).toFixed(1)) + '</td>' +\n          '<td class=\"t-unit\">' + esc(l.unit) + '</td>' +\n          '<td class=\"t-price\">' + Math.round(num(l.price)).toLocaleString('ja-JP') + '</td>' +\n          '<td class=\"t-amount\">' + lineAmount(l, d.unitRound).toLocaleString('ja-JP') + '</td>' +\n        '</tr>');\n    });\n\n    var sumHTML = '<tr><th>小計</th><td>' + Math.round(t.subtotal).toLocaleString('ja-JP') + '</td></tr>';\n    if (t.overhead) sumHTML += '<tr><th>諸経費</th><td>' + t.overhead.toLocaleString('ja-JP') + '</td></tr>';\n    if (t.discount) sumHTML += '<tr><th>値引き</th><td>-' + t.discount.toLocaleString('ja-JP') + '</td></tr>';\n    sumHTML += '<tr><th>消費税（' + d.tax + '%）</th><td>' + t.tax.toLocaleString('ja-JP') + '</td></tr>';\n    sumHTML += '<tr class=\"grand\"><th>合計</th><td>' + Math.round(t.total).toLocaleString('ja-JP') + '</td></tr>';\n    // 適格請求書は「税率ごとに区分した対価の額と消費税額」を書くことが決まっている。\n    // 空調工事は軽減税率の対象外なので、区分は1つ（標準税率）だけになる。\n    if (inv) {\n      sumHTML += '<tr class=\"tax-break\"><th>' + d.tax + '%対象</th><td>' +\n        Math.round(t.taxable).toLocaleString('ja-JP') + '</td></tr>';\n      sumHTML += '<tr class=\"tax-break\"><th>　うち消費税</th><td>' +\n        t.tax.toLocaleString('ja-JP') + '</td></tr>';\n    }\n\n    var termsHTML = '';\n    function term(k, v) {\n      if (!v) return '';\n      return '<div><span class=\"k\">' + esc(k) + '</span><span class=\"v\">' + esc(v) + '</span></div>';\n    }\n    termsHTML += term('件　　名', d.subject);\n    termsHTML += term('工事場所', d.site);\n    if (inv) {\n      termsHTML += term('工事完了日', d.doneDate ? jpDate(d.doneDate) : '');\n      termsHTML += term('お支払期限', d.dueDate ? jpDate(d.dueDate) : '');\n      termsHTML += term('お支払条件', d.payment);\n    } else {\n      termsHTML += term('工　　期', d.delivery);\n      termsHTML += term('お支払条件', d.payment);\n      termsHTML += term('有効期限', validUntil);\n    }\n\n    var sealMm = Math.min(45, Math.max(8, num(c.sealSizeMm) || 18));\n    var sealSrc = safeImage(c.sealImage);\n    var sealHTML = sealSrc\n      ? '<img class=\"seal-img\" src=\"' + esc(sealSrc) + '\" alt=\"\" style=\"width:' + sealMm + 'mm\">'\n      : '<span class=\"seal-fallback\">㊞</span>';\n\n    var logoMm = Math.min(40, Math.max(5, num(c.logoHeightMm) || 12));\n    var logoSrc = safeImage(c.logoImage);\n    var logoHTML = logoSrc\n      ? '<img class=\"sheet-logo\" src=\"' + esc(logoSrc) + '\" alt=\"\" style=\"height:' + logoMm + 'mm\">'\n      : '';\n\n    var companyHTML =\n      '<div class=\"sheet-company\">' +\n        logoHTML +\n        '<b>' + esc(c.name) + '</b><br>' +\n        (c.zip ? esc(c.zip) + '　' : '') + esc(c.address) + '<br>' +\n        (c.tel ? 'TEL：' + esc(c.tel) + '<br>' : '') +\n        (c.email ? 'Mail：' + esc(c.email) + '<br>' : '') +\n        (c.web ? esc(c.web) + '<br>' : '') +\n        (c.invoiceNo ? '登録番号：' + esc(c.invoiceNo) + '<br>' : '') +\n        '<div class=\"seal-area\" style=\"min-height:' + sealMm + 'mm\">' +\n          '<span class=\"owner\">' + esc(c.owner || '') + '</span>' +\n          sealHTML +\n        '</div>' +\n      '</div>';\n\n    var remarks = (d.note || '') + (c.bank ? '\\n\\n【お振込先】' + c.bank : '');\n\n    /* 「ロゴ大きく左」用の置き場所。ほかの見た目では CSS で隠している。\n       いつも書き出しておけば、見た目の切り替えは CSS だけで済む。\n       ロゴが無い会社は会社名が大きく出る（空白の帯にならないように） */\n    // ロゴが無い会社は、左上に会社名が大きく出る。右の欄にも会社名があると2回出るので、\n    // そのときだけ右の会社名を隠す印を付ける（CSSの #sheet.skin-logo.no-logo）\n    $('#sheet').classList.toggle('no-logo', !logoSrc);\n    /* 左上のロゴは、［自社情報］の「ロゴの高さ」の2倍にする（16〜34mmの範囲）。\n       つまみを1つ増やさずに大きさを変えられる。\n       ロゴのPNGは周りに余白があることが多く、小さく見えがちなので下限を16mmにした */\n    var brandMm = Math.min(34, Math.max(16, logoMm * 2));\n    var brandHTML =\n        '<div class=\"sheet-brand\">' +\n          (logoSrc ? '<img class=\"brand-logo\" src=\"' + esc(logoSrc) + '\" alt=\"\" style=\"height:' + brandMm + 'mm\">'\n                   : '<span class=\"brand-name\">' + esc(c.name) + '</span>') +\n        '</div>';\n\n    var headHTML =\n        brandHTML +\n        '<div class=\"sheet-title\">' + (inv ? '御請求書' : '御見積書') + '</div>' +\n        '<div class=\"sheet-meta\">' +\n          (inv ? '請求番号：' : '見積番号：') + esc(d.no) + '<br>' +\n          (inv ? '請求日：' : '発行日：') + esc(jpDate(d.date)) +\n        '</div>' +\n        '<div class=\"sheet-head\">' +\n          '<div class=\"sheet-head-left\">' +\n            '<div class=\"sheet-to\">' + esc(to || '　') + esc(hon) + '</div>' +\n            '<p class=\"sheet-lead\">' +\n              (inv ? '下記の通りご請求申し上げます。' : '下記の通りお見積り申し上げます。') +\n            '</p>' +\n            '<div class=\"sheet-total-box\">' +\n              '<span class=\"label\">' + (inv ? 'ご請求金額' : '御見積金額') + '</span>' +\n              '<span class=\"value\">' + Math.round(t.total).toLocaleString('ja-JP') + ' 円</span>' +\n              '<span class=\"tax-note\">（消費税込）</span>' +\n            '</div>' +\n            '<div class=\"sheet-terms\">' + termsHTML + '</div>' +\n          '</div>' +\n          '<div class=\"sheet-head-right\">' + companyHTML + '</div>' +\n        '</div>';\n\n    var theadHTML =\n        '<thead><tr>' +\n          '<th class=\"t-no\">No</th><th>品名・仕様</th><th class=\"t-qty\">数量</th>' +\n          '<th class=\"t-unit\">単位</th><th class=\"t-price\">単価</th><th class=\"t-amount\">金額</th>' +\n        '</tr></thead>';\n\n    var footHTML =\n        '<div class=\"sheet-foot\">' +\n          '<div class=\"sheet-remarks\"><span class=\"rk\">備考</span>' + esc(remarks) + '</div>' +\n          '<table class=\"sheet-sum\">' + sumHTML + '</table>' +\n        '</div>';\n\n    paginateSheet(headHTML, theadHTML, rowList, footHTML);\n  }\n\n  var EMPTY_ROW = '<tr><td class=\"t-no\">&nbsp;</td><td></td><td class=\"t-qty\"></td>' +\n                  '<td class=\"t-unit\"></td><td class=\"t-price\"></td><td class=\"t-amount\"></td></tr>';\n\n  /**\n   * 紙に割る。\n   *\n   * ブラウザ任せにすると、iPhoneでは「行の途中で切るな」も「見出しを繰り返せ」も\n   * 効かない。品名だけ前の紙・仕様は次の紙、という見積書ができてしまう。\n   * （2026-09-02、実機で確認した）\n   *\n   * そこで、いったん1枚に全部入れて高さを実際に測り、こちらで紙を分ける。\n   * どのブラウザでも同じ紙になるし、プレビューも1枚ずつ見えるようになる。\n   */\n  function paginateSheet(headHTML, theadHTML, rows, footHTML) {\n    var box = $('#sheet');\n    var tableOf = function (inner) {\n      return '<table class=\"sheet-lines\">' + theadHTML + '<tbody>' + inner + '</tbody></table>';\n    };\n\n    // 隠れたままだと高さが全部0になるので、測るあいだだけ見えない形で開く\n    var pv = $('#preview'), wasHidden = pv.hidden;\n    if (wasHidden) { pv.style.visibility = 'hidden'; pv.hidden = false; }\n\n    box.innerHTML =\n      '<div class=\"sheet-page\">' +\n        '<div id=\"m-head\">' + headHTML + '</div>' +\n        tableOf(rows.join('') + EMPTY_ROW) +\n        '<div id=\"m-foot\">' + footHTML + '</div>' +\n      '</div>';\n\n    // 1mm が何ピクセルか、実物で測る（端末ごとにちがう）\n    var ruler = el('div');\n    ruler.style.cssText = 'position:absolute;width:100mm;height:0';\n    box.appendChild(ruler);\n    var pxPerMm = ruler.offsetWidth / 100;\n    box.removeChild(ruler);\n\n    var table  = box.querySelector('table.sheet-lines');\n    var headH  = $('#m-head').offsetHeight;\n    var theadH = table.tHead ? table.tHead.offsetHeight : 0;\n    var footH  = $('#m-foot').offsetHeight;\n    var trs    = table.tBodies[0] ? table.tBodies[0].rows : [];\n    var rowH   = [];\n    for (var i = 0; i < trs.length; i++) rowH.push(trs[i].offsetHeight);\n    var emptyH = rowH.pop() || 0;   // いちばん最後は、高さを測るために足した空行\n\n    if (wasHidden) { pv.hidden = true; pv.style.visibility = ''; }\n\n    // A4 297mm から上の余白14mm・下の余白12mm を引くと271mm。\n    // ただし iPhone の Safari は、その中にURLと日付とページ番号を自分で刷る。\n    // そのぶん使える高さが減るので、271mm いっぱいまで詰めると\n    // ほんの数mmはみ出して、真っ白な紙が1枚できてしまう。\n    // （2026-09-02、実機で確認）そこで 245mm までしか詰めない。\n    var avail = 245 * pxPerMm;\n    if (!pxPerMm || avail <= 0 || !rowH.length) return;   // 測れないときは1枚のまま\n\n    var pages = [], cur = [], space = avail - headH - theadH;\n    for (i = 0; i < rows.length; i++) {\n      if (cur.length && rowH[i] > space) {\n        pages.push(cur);\n        cur = []; space = avail - theadH;\n      }\n      cur.push(i);\n      space -= rowH[i];\n    }\n    pages.push(cur);\n\n    // 備考と合計は絶対に割らない。最後の紙に入らなければ、次の紙にまとめて置く\n    var footOwnPage = space < footH;\n\n    // 短い見積は表がすかすかに見えるので空行で埋める。\n    // ただし入るぶんだけ。数だけ見て足すと紙からあふれて、白紙が1枚増える。\n    var restSpace = space - (footOwnPage ? 0 : footH);\n    var filler = '';\n    for (var k = rows.length; k < 12 && emptyH > 0 && restSpace >= emptyH; k++) {\n      filler += EMPTY_ROW;\n      restSpace -= emptyH;\n    }\n\n    var html = '';\n    for (var p = 0; p < pages.length; p++) {\n      var last = (p === pages.length - 1);\n      var inner = '';\n      for (var j = 0; j < pages[p].length; j++) inner += rows[pages[p][j]];\n      if (last) inner += filler;\n      html += '<div class=\"sheet-page\">' +\n                (p === 0 ? headHTML : '') +\n                tableOf(inner) +\n                (last && !footOwnPage ? footHTML : '') +\n              '</div>';\n    }\n    if (footOwnPage) html += '<div class=\"sheet-page\">' + footHTML + '</div>';\n\n    box.innerHTML = html;\n  }\n\n  /* ---------- プレビュー ----------\n     印刷用に組んだ #sheet を、そのまま画面にかぶせて見せる。\n     別に組み直しているわけではないので、\n     プレビューで見た形と印刷された形がずれることはない。 */\n\n  var pvDocTitle = '';   // 印刷するときの見出し（PDF保存のファイル名になる）\n\n  /** 会社名が未登録だと見積書の体裁にならないので、そこだけ先に確かめる */\n  function readyToPrint() {\n    if ((pb.company.name || '').trim()) return true;\n    toast('先に［自社情報］で会社名を登録してください');\n    $('.tab[data-view=\"settings\"]').click();\n    return false;\n  }\n\n  function openPreview(headline, docTitle) {\n    pvDocTitle = docTitle || '';\n    $('#pv-title').textContent = headline || 'プレビュー';\n    $('#preview').hidden = false;\n    document.body.style.overflow = 'hidden';   // 後ろの画面が一緒に動かないようにする\n    fitPreview();\n  }\n\n  function closePreview() {\n    $('#preview').hidden = true;\n    document.body.style.overflow = '';\n  }\n\n  /** 画面の幅に合わせてA4を縮める（スマホでも紙1枚まるごと見えるように）。\n      縮めるのは見た目だけなので、印刷される中身は変わらない。 */\n  var fitRetry = 0;\n\n  function fitPreview() {\n    if ($('#preview').hidden) { fitRetry = 0; return; }\n    var scroll = $('#pv-scroll'), fit = $('#pv-fit'), stage = $('#pv-stage');\n    stage.style.transform = 'none';\n    fit.style.width = '';\n    fit.style.height = '';\n    var pageW = stage.offsetWidth;\n    if (!pageW) return;\n    var avail = scroll.clientWidth - 24;       // .preview-scroll の左右パディングぶん\n    // 開いた直後は入れ物の幅がまだ決まっていないことがある（スマホで起きやすい）。\n    // 幅を0のまま計算すると倍率がマイナスになり、紙が裏返って画面から消える。\n    // 次の描画まで待ってから測り直す。待ちすぎないよう回数を区切る。\n    if (avail <= 0) {\n      if (fitRetry < 30) { fitRetry++; requestAnimationFrame(fitPreview); }\n      return;\n    }\n    fitRetry = 0;\n    var scale = Math.min(1, avail / pageW);\n    stage.style.transform = 'scale(' + scale + ')';\n    // 縮小しても余白が空きっぱなしにならないよう、入れ物の大きさも合わせる\n    fit.style.width = (pageW * scale) + 'px';\n    fit.style.height = (stage.offsetHeight * scale) + 'px';\n  }\n\n  window.addEventListener('resize', fitPreview);\n\n  $('#btn-preview').addEventListener('click', function () {\n    if (!readyToPrint()) return;\n    buildSheet();\n    openPreview('見積書　' + st.no + (st.customer ? '　' + st.customer : ''),\n                '見積書_' + (st.customer || '無題') + '_' + st.no);\n  });\n\n  $('#pv-close').addEventListener('click', closePreview);\n\n  /* --------------------------------------------------------------------\n     PDFを、こちらで作る。\n\n     iPhoneの印刷は、紙の幅ではなく画面の幅で組み立ててから紙に合わせて\n     拡大するので、こちらで割った1枚とずれる。ずれた結果、ブラウザが\n     もう一度切って、品名と仕様が別の紙に離れてしまう。\n     （2026-09-02、実機のPDFで確認。同じ見積がPCで3枚、iPhoneで4枚）\n\n     そこで、印刷そのものに頼らない。画面に出ている紙をそのまま絵にして、\n     A4のPDFに1枚ずつ貼る。どの端末でも、画面で見えているとおりの紙になる。\n\n     部品（html2canvas と jsPDF）は vendor/ に置いてある。\n     よそのサイトから読まないので、電波が悪いところでも動く。\n     押したときだけ読み込むので、ふだんの起動は重くならない。\n     -------------------------------------------------------------------- */\n  var pdfLibsReady = false;\n\n  function loadScript(src) {\n    return new Promise(function (ok, ng) {\n      var s = document.createElement('script');\n      s.src = src;\n      s.onload = function () { ok(); };\n      s.onerror = function () { ng(new Error('部品が読み込めませんでした')); };\n      document.head.appendChild(s);\n    });\n  }\n\n  function ensurePdfLibs() {\n    if (pdfLibsReady) return Promise.resolve();\n    return loadScript('vendor/html2canvas.min.js?v=' + APP_VERSION)\n      .then(function () { return loadScript('vendor/jspdf.umd.min.js?v=' + APP_VERSION); })\n      .then(function () { pdfLibsReady = true; });\n  }\n\n  function makePdf(win, btn) {\n    btn = btn || $('#pv-pdf');\n    var label = btn.textContent;\n    // 新しい画面は「押した流れの中」でしか開けない。あとから開くと止められる\n    if (win === undefined) win = window.open('', '_blank');\n    btn.disabled = true;\n    btn.textContent = '作っています…';\n\n    var stage = $('#pv-stage'), fit = $('#pv-fit');\n    var keepTransform = stage.style.transform;\n    var keepW = fit.style.width, keepH = fit.style.height;\n\n    function restore() {\n      stage.style.transform = keepTransform;\n      fit.style.width = keepW;\n      fit.style.height = keepH;\n      btn.disabled = false;\n      btn.textContent = label;\n      fitPreview();\n    }\n\n    ensurePdfLibs().then(function () {\n      // 縮めたまま絵にすると粗くなるので、いったん原寸に戻す\n      stage.style.transform = 'none';\n      fit.style.width = '';\n      fit.style.height = '';\n\n      var pages = [].slice.call(document.querySelectorAll('#sheet .sheet-page'));\n      if (!pages.length) throw new Error('紙がありません');\n      var pdf = new window.jspdf.jsPDF({ unit: 'mm', format: 'a4', orientation: 'portrait' });\n\n      var chain = Promise.resolve();\n      pages.forEach(function (page, i) {\n        chain = chain.then(function () {\n          // 何枚目をやっているか出す。止まったように見せない\n          btn.textContent = (i + 1) + ' / ' + pages.length + ' 枚目…';\n          return window.html2canvas(page, {\n            scale: 2, backgroundColor: '#ffffff', useCORS: true, logging: false\n          });\n        }).then(function (canvas) {\n          if (i) pdf.addPage();\n          pdf.addImage(canvas.toDataURL('image/jpeg', 0.92), 'JPEG', 0, 0, 210, 297);\n        });\n      });\n\n      return chain.then(function () { return pdf; });\n    }).then(function (pdf) {\n      var name = (pvDocTitle || '見積書') + '.pdf';\n      if (win) {\n        // その場でPDFを開く。iPhoneはここから共有・印刷ができる\n        win.location.href = URL.createObjectURL(pdf.output('blob'));\n      } else {\n        pdf.save(name);   // 新しい画面が開けなかったときは、ファイルとして保存\n      }\n      restore();\n      toast('PDFにしました（' + document.querySelectorAll('#sheet .sheet-page').length + '枚）');\n    }).catch(function (e) {\n      if (win) win.close();\n      restore();\n      toast('PDFが作れませんでした：' + (e && e.message ? e.message : ''));\n    });\n  }\n\n  $('#pv-pdf').addEventListener('click', function () { makePdf(undefined, $('#pv-pdf')); });\n\n  // 下のバーからも一発で作れるようにする。\n  // プレビューを開いてからでないと紙が組み上がらないので、ここで開いてから渡す。\n  $('#btn-pdf').addEventListener('click', function () {\n    if (!readyToPrint()) return;\n    var win = window.open('', '_blank');   // 押した流れの中で開く\n    buildSheet();\n    openPreview('見積書　' + st.no + (st.customer ? '　' + st.customer : ''),\n                '見積書_' + (st.customer || '無題') + '_' + st.no);\n    makePdf(win, $('#pv-pdf'));\n  });\n\n  $('#pv-print').addEventListener('click', function () {\n    if (pvDocTitle) document.title = pvDocTitle;\n    setTimeout(function () { window.print(); }, 60);\n  });\n\n  document.addEventListener('keydown', function (e) {\n    if (e.key === 'Escape' && !$('#preview').hidden) closePreview();\n  });\n\n  $('#btn-print').addEventListener('click', function () {\n    if (!readyToPrint()) return;\n    if (!st.lines.length) {\n      if (!confirm('明細が1行もありません。このまま印刷しますか？')) return;\n    }\n    buildSheet();\n    document.title = '見積書_' + (st.customer || '無題') + '_' + st.no;\n    setTimeout(function () { window.print(); }, 60);\n  });\n\n  window.addEventListener('afterprint', function () {\n    document.title = '空調王';\n  });\n\n  /* ======================================================================\n     初期化\n     ====================================================================== */\n  fillMeta();\n  renderPicker();\n  renderLines();\n  fillCompany();\n  loadModels();\n  initBackup();\n\n  // はじめて開いたときは、自社情報の登録から始めてもらう\n  if (!(pb.company.name || '').trim()) {\n    $('.tab[data-view=\"settings\"]').click();\n    toast('まず会社名などの自社情報を登録してください');\n  }\n})();\n"],["sync.js","/* ==========================================================================\n   空調王：スマホ ⇄ パソコンの連動（同期）\n   --------------------------------------------------------------------------\n   考え方\n   - データは「この端末の中で暗号化してから」クラウド(Firestore)に置く。\n     クラウド側には意味の分からない文字列しか残らないので、\n     万一のぞかれても中身（お客様名・金額）は読めない。\n   - 暗号のカギは「あいことば」。あいことばを知っている端末だけが読める。\n     あいことばはクラウドには絶対に送らない。\n   - 置き場所の名前（doc id）も、あいことばから作った当てられない64桁。\n   - Firebase の SDK は読み込まない。REST（ただの fetch）だけで済ませる。\n     外から読み込むスクリプトを増やさないため。\n\n   同期のしかた\n   - 現場・見積・請求書（小さい・よく変わる）→ 1件ずつ突き合わせて合体。\n     消したものは「墓標」を残して、相手側でも消えるようにする。\n   - 単価マスタ・機種データ（大きい・たまにしか変わらない）\n     → まるごと1つ。あとから直したほうが勝ち。\n   ========================================================================== */\n(function () {\n  'use strict';\n\n  var CFG = window.AIRTEC_FIREBASE || null;\n  var READY = !!(CFG && CFG.apiKey && CFG.projectId);\n\n  /* ---------- 保存キー ---------- */\n  var K_SYNC  = 'airtec_sync_v1';        // 連動の設定と覚え書き\n  var K_AUTH  = 'airtec_sync_auth_v1';   // ログインの引換券\n  var KEY_PB  = 'airtec_pricebook_v1';\n  var KEY_MDL = 'airtec_models_v1';\n  /* 別売品。前は連動で運んでいなかったので、パソコンで入れてもスマホに出なかった\n     （2026-09-10、BIGBOSSの「スマホ版で別売品でない」で分かった）。\n     置き場所を増やすとクラウドの許可設定を貼り直してもらう必要があるので、\n     単価マスタ・機種データと同じ _master に同乗させる（大きさは圧縮して25KBほど）。 */\n  var KEY_OPT = 'airtec_options_v1';\n  var LISTS = [\n    { name: 'sites',     key: 'airtec_sites_v1',     label: '現場' },\n    { name: 'estimates', key: 'airtec_estimates_v1', label: '見積' },\n    { name: 'invoices',  key: 'airtec_invoices_v1',  label: '請求書' }\n  ];\n\n  var PUSH_WAIT_WORK   = 3000;    // 変更してから送るまで（小さいデータ）\n  var PUSH_WAIT_MASTER = 15000;   // 同上（大きいデータ）\n  var POLL_MS          = 45000;   // 相手の変更を見に行く間隔\n  var MAX_BLOB         = 900000;  // 1回に置ける上限（Firestore は約1MB）\n  var TOMB_DAYS        = 120;     // 墓標を残しておく日数\n\n  /* 現場写真は1枚ずつ別の入れ物に預ける。\n     まとめて送ると1MBの上限に一発で当たるし、1枚ごとなら\n     途中で電波が切れても、送れたぶんは残る。\n     1周でまとめて何十枚も動かすと、その間ずっと待たされるので枚数を区切る。 */\n  var PH_UP_PER_ROUND   = 6;      // 1周で預ける枚数\n  var PH_DOWN_PER_ROUND = 10;     // 1周でもらう枚数\n  var PH_SOON_MS        = 4000;   // まだ残っているとき、次の周までの間\n\n  /* ---------- ちいさな道具 ---------- */\n  function lsGet(k, fb) {\n    try { var r = localStorage.getItem(k); return r ? JSON.parse(r) : fb; }\n    catch (e) { return fb; }\n  }\n  function lsSet(k, v) {\n    try { localStorage.setItem(k, JSON.stringify(v)); return true; } catch (e) { return false; }\n  }\n  function now() { return Date.now(); }\n  function strHash(s) {                     // 中身が変わったかを見るだけの簡易ハッシュ\n    var h = 5381;\n    for (var i = 0; i < s.length; i++) h = ((h << 5) + h + s.charCodeAt(i)) | 0;\n    return h.toString(36);\n  }\n  function b64enc(bytes) {\n    var s = '', CH = 0x8000;\n    for (var i = 0; i < bytes.length; i += CH) {\n      s += String.fromCharCode.apply(null, bytes.subarray(i, i + CH));\n    }\n    return btoa(s);\n  }\n  function b64dec(str) {\n    var bin = atob(str), out = new Uint8Array(bin.length);\n    for (var i = 0; i < bin.length; i++) out[i] = bin.charCodeAt(i);\n    return out;\n  }\n\n  /* ---------- 連動の状態 ---------- */\n  var S = null;\n  function defState() {\n    return {\n      on: false,\n      code: '',            // あいことば（この端末の中だけ）\n      vault: '',           // 置き場所の名前（64桁）\n      lastSync: 0,\n      stamps: { sites: {}, estimates: {}, invoices: {} },   // 1件ごとの最終更新\n      tomb:   { sites: {}, estimates: {}, invoices: {} },   // 消した印\n      shadow: { sites: {}, estimates: {}, invoices: {} },   // 前回の中身（変更検知用）\n      masterAt: 0,         // この端末で単価マスタを最後に直した時刻\n      remoteMasterAt: 0,   // 取り込み済みのクラウド側マスタの時刻\n      /* 現場写真。1枚ごとに見る。\n         up は「その写真そのものを預け終えたか」。回転すると中身が変わるので、\n         印には寸法と大きさ（見た目の指紋）を入れておき、違っていたら預け直す。 */\n      ph: { stamps: {}, tomb: {}, shadow: {}, up: {}, big: {}, count: 0, bytes: 0 }\n    };\n  }\n  function loadState() {\n    S = lsGet(K_SYNC, null) || defState();\n    var d = defState();\n    ['stamps', 'tomb', 'shadow'].forEach(function (g) {\n      S[g] = Object.assign({}, d[g], S[g] || {});\n    });\n    S.ph = Object.assign({}, d.ph, S.ph || {});\n    ['stamps', 'tomb', 'shadow', 'up', 'big'].forEach(function (g) {\n      if (!S.ph[g] || typeof S.ph[g] !== 'object') S.ph[g] = {};\n    });\n    if (typeof S.masterAt !== 'number') S.masterAt = 0;\n    if (typeof S.remoteMasterAt !== 'number') S.remoteMasterAt = 0;\n    return S;\n  }\n  function saveState() { lsSet(K_SYNC, S); }\n\n  /* ======================================================================\n     あいことば と 暗号\n     ====================================================================== */\n  var ALPHABET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';   // 紛らわしい I O 0 1 は使わない\n\n  function makeCode() {\n    var r = new Uint8Array(20), out = '';\n    crypto.getRandomValues(r);\n    for (var i = 0; i < 20; i++) {\n      out += ALPHABET[r[i] % ALPHABET.length];\n      if (i % 5 === 4 && i !== 19) out += '-';\n    }\n    return out;                                        // 例 ABCDE-FGHJK-LMNPQ-RSTUV\n  }\n  function normCode(s) {\n    return String(s || '').toUpperCase().replace(/[^A-Z0-9]/g, '')\n      .replace(/O/g, '0').replace(/I/g, '1');          // 打ち間違いをある程度救う\n  }\n  function fmtCode(s) {\n    var n = normCode(s), out = [];\n    for (var i = 0; i < n.length; i += 5) out.push(n.slice(i, i + 5));\n    return out.join('-');\n  }\n\n  async function sha256hex(str) {\n    var buf = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(str));\n    return Array.prototype.map.call(new Uint8Array(buf), function (b) {\n      return ('0' + b.toString(16)).slice(-2);\n    }).join('');\n  }\n  async function vaultIdOf(code) { return sha256hex('airtec-vault-v1:' + normCode(code)); }\n\n  var keyCache = null;\n  async function aesKey(code) {\n    if (keyCache && keyCache.code === code) return keyCache.key;\n    var base = await crypto.subtle.importKey('raw', new TextEncoder().encode(normCode(code)),\n      'PBKDF2', false, ['deriveKey']);\n    var saltSrc = await sha256hex('airtec-salt-v1:' + normCode(code));\n    var key = await crypto.subtle.deriveKey(\n      { name: 'PBKDF2', salt: new TextEncoder().encode(saltSrc), iterations: 200000, hash: 'SHA-256' },\n      base, { name: 'AES-GCM', length: 256 }, false, ['encrypt', 'decrypt']);\n    keyCache = { code: code, key: key };\n    return key;\n  }\n\n  async function gzip(bytes) {\n    if (typeof CompressionStream !== 'function') return null;\n    var st = new Blob([bytes]).stream().pipeThrough(new CompressionStream('gzip'));\n    return new Uint8Array(await new Response(st).arrayBuffer());\n  }\n  async function gunzip(bytes) {\n    var st = new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));\n    return new Uint8Array(await new Response(st).arrayBuffer());\n  }\n\n  /** オブジェクト → 暗号化した文字列 */\n  async function seal(obj) {\n    var raw = new TextEncoder().encode(JSON.stringify(obj));\n    var z = 0, body = raw;\n    var packed = await gzip(raw);\n    if (packed && packed.length < raw.length) { z = 1; body = packed; }\n    var iv = crypto.getRandomValues(new Uint8Array(12));\n    var key = await aesKey(S.code);\n    var ct = new Uint8Array(await crypto.subtle.encrypt({ name: 'AES-GCM', iv: iv }, key, body));\n    return JSON.stringify({ v: 1, z: z, iv: b64enc(iv), ct: b64enc(ct) });\n  }\n  /** 暗号化した文字列 → オブジェクト（あいことばが違えば例外になる） */\n  async function unseal(str) {\n    var env = JSON.parse(str);\n    var key = await aesKey(S.code);\n    var plain = new Uint8Array(await crypto.subtle.decrypt(\n      { name: 'AES-GCM', iv: b64dec(env.iv) }, key, b64dec(env.ct)));\n    if (env.z) plain = await gunzip(plain);\n    return JSON.parse(new TextDecoder().decode(plain));\n  }\n\n  /* ======================================================================\n     クラウド（Firestore）とのやり取り　※ふつうの fetch だけ\n     ====================================================================== */\n  async function idToken() {\n    var a = lsGet(K_AUTH, null);\n    if (a && a.idToken && a.exp - 60000 > now()) return a.idToken;\n\n    var res, j;\n    if (a && a.refreshToken) {                       // 期限切れ → 更新\n      res = await fetch('https://securetoken.googleapis.com/v1/token?key=' + CFG.apiKey, {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },\n        body: 'grant_type=refresh_token&refresh_token=' + encodeURIComponent(a.refreshToken)\n      });\n      if (res.ok) {\n        j = await res.json();\n        var t1 = { idToken: j.id_token, refreshToken: j.refresh_token, exp: now() + Number(j.expires_in) * 1000 };\n        lsSet(K_AUTH, t1);\n        return t1.idToken;\n      }\n    }\n    // 匿名でログイン（メールもパスワードも使わない）\n    res = await fetch('https://identitytoolkit.googleapis.com/v1/accounts:signUp?key=' + CFG.apiKey, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ returnSecureToken: true })\n    });\n    if (!res.ok) throw new Error('login:' + res.status + ':' + (await res.text()).slice(0, 200));\n    j = await res.json();\n    var t2 = { idToken: j.idToken, refreshToken: j.refreshToken, exp: now() + Number(j.expiresIn) * 1000 };\n    lsSet(K_AUTH, t2);\n    return t2.idToken;\n  }\n\n  function docUrl(part) {\n    return 'https://firestore.googleapis.com/v1/projects/' + CFG.projectId +\n           '/databases/(default)/documents/airtec/' + S.vault + '_' + part;\n  }\n  /** クラウドから読む。無ければ null */\n  async function docGet(part) {\n    var res = await fetch(docUrl(part), { headers: { Authorization: 'Bearer ' + (await idToken()) } });\n    if (res.status === 404) return null;\n    if (!res.ok) throw new Error('get:' + res.status + ':' + (await res.text()).slice(0, 200));\n    var j = await res.json();\n    var f = j.fields || {};\n    return { blob: f.blob ? f.blob.stringValue : '', at: f.at ? Number(f.at.integerValue) : 0 };\n  }\n  /** クラウドから消す。もう無ければそれでよし */\n  async function docDel(part) {\n    var res = await fetch(docUrl(part), {\n      method: 'DELETE',\n      headers: { Authorization: 'Bearer ' + (await idToken()) }\n    });\n    if (!res.ok && res.status !== 404) throw new Error('del:' + res.status);\n  }\n  /** クラウドに書く（無ければ作られる） */\n  async function docPut(part, blob, at) {\n    if (blob.length > MAX_BLOB) throw new Error('too-big');\n    var res = await fetch(docUrl(part), {\n      method: 'PATCH',\n      headers: { Authorization: 'Bearer ' + (await idToken()), 'Content-Type': 'application/json' },\n      body: JSON.stringify({ fields: { blob: { stringValue: blob }, at: { integerValue: String(at) } } })\n    });\n    if (!res.ok) throw new Error('put:' + res.status + ':' + (await res.text()).slice(0, 200));\n  }\n\n  /* ======================================================================\n     変更の見つけかた（1件ごとの更新時刻をこちらで付ける）\n     ----------------------------------------------------------------------\n     見積などのデータ自体には手を入れたくないので、\n     「前回の中身」を覚えておいて、変わった件だけ時刻を打ち直す。\n     ====================================================================== */\n  function listOf(name) {\n    var d = null;\n    LISTS.forEach(function (l) { if (l.name === name) d = l; });\n    return d;\n  }\n  function readList(name) {\n    var v = lsGet(listOf(name).key, []);\n    return Array.isArray(v) ? v : [];\n  }\n\n  /** ローカルの変更を見て、stamps と tomb を最新にする */\n  function scanLocal(t) {\n    t = t || now();\n    var touched = false;\n    LISTS.forEach(function (l) {\n      var arr = readList(l.name);\n      var shadow = S.shadow[l.name] || {};\n      var next = {};\n      arr.forEach(function (it) {\n        if (!it || !it.id) return;\n        var h = strHash(JSON.stringify(it));\n        next[it.id] = h;\n        if (shadow[it.id] !== h) {                 // 新しい or 直した\n          S.stamps[l.name][it.id] = t;\n          delete S.tomb[l.name][it.id];\n          touched = true;\n        }\n      });\n      Object.keys(shadow).forEach(function (id) {  // 消えた\n        if (!next[id]) {\n          S.tomb[l.name][id] = t;\n          delete S.stamps[l.name][id];\n          touched = true;\n        }\n      });\n      S.shadow[l.name] = next;\n    });\n    return touched;\n  }\n\n  /** 古い墓標を捨てる（増えつづけないように） */\n  function pruneTombs() {\n    var limit = now() - TOMB_DAYS * 86400000;\n    LISTS.forEach(function (l) {\n      Object.keys(S.tomb[l.name]).forEach(function (id) {\n        if (S.tomb[l.name][id] < limit) delete S.tomb[l.name][id];\n      });\n    });\n  }\n\n  /* ======================================================================\n     合体（マージ）\n     ====================================================================== */\n  function mergeList(name, remote) {\n    var local = readList(name);\n    var lStamp = S.stamps[name] || {};\n    var rStamp = (remote && remote.stamps && remote.stamps[name]) || {};\n    var lTomb  = S.tomb[name] || {};\n    var rTomb  = (remote && remote.tomb && remote.tomb[name]) || {};\n    var rItems = (remote && remote.data && remote.data[name]) || [];\n\n    var byId = {}, order = [];\n    function put(it, from) {\n      if (!it || !it.id) return;\n      if (!byId[it.id]) order.push(it.id);\n      byId[it.id] = byId[it.id] || {};\n      byId[it.id][from] = it;\n    }\n    local.forEach(function (it) { put(it, 'l'); });\n    rItems.forEach(function (it) { put(it, 'r'); });\n\n    var out = [], stamps = {}, tomb = {};\n    Object.keys(lTomb).forEach(function (id) { tomb[id] = lTomb[id]; });\n    Object.keys(rTomb).forEach(function (id) {\n      if (!tomb[id] || rTomb[id] > tomb[id]) tomb[id] = rTomb[id];\n    });\n\n    /* 並び順は id 順に固定する。\n       端末ごとに順番が違うと「相手と違う」と判断して送り合いが止まらなくなるため。\n       画面に出すときは、アプリ側が日付などで並べ直すので見た目には影響しない。 */\n    order.sort();\n\n    order.forEach(function (id) {\n      var pair = byId[id];\n      var ls = lStamp[id] || 0, rs = rStamp[id] || 0;\n      var pick, stamp;\n      if (pair.l && pair.r) { pick = (rs > ls) ? pair.r : pair.l; stamp = Math.max(ls, rs); }\n      else if (pair.l)      { pick = pair.l; stamp = ls; }\n      else                  { pick = pair.r; stamp = rs; }\n      if (tomb[id] && tomb[id] >= stamp) return;        // 消されたほうが新しい → 出さない\n      out.push(pick);\n      stamps[id] = stamp || 0;\n    });\n\n    /* クラウド側が「合体後」と同じかどうか。違うなら送り直す必要がある。\n       （2台がほぼ同時に書くと、片方の書き込みがもう片方を上書きしてしまう。\n         そのとき、こちらが持っている分をもう一度送らないと消えたままになる） */\n    var remoteSame = sameList(rItems, out) && sameMap(rStamp, stamps) && sameMap(rTomb, tomb);\n\n    return { items: out, stamps: stamps, tomb: tomb, remoteSame: remoteSame };\n  }\n\n  function sameList(a, b) { return JSON.stringify(a) === JSON.stringify(b); }\n  function sameMap(a, b) {\n    a = a || {}; b = b || {};\n    var ka = Object.keys(a).sort(), kb = Object.keys(b).sort();\n    if (ka.length !== kb.length) return false;\n    for (var i = 0; i < ka.length; i++) {\n      if (ka[i] !== kb[i] || a[ka[i]] !== b[ka[i]]) return false;\n    }\n    return true;\n  }\n\n  /* ======================================================================\n     同期の本体\n     ====================================================================== */\n  var busy = false, pushTimer = null, pushMaster = false, lastErr = '';\n  /* 写真の不調は別に持つ。見積や現場は通っているのに\n     「つながりません」と出ると、直すべき場所を見誤る */\n  var photoErr = '';\n  var pendingReload = false;\n\n  function markChanged(key) {\n    if (!S || !S.on) return;\n    if (key === KEY_PB || key === KEY_MDL || key === KEY_OPT) {\n      S.masterAt = now();\n      pushMaster = true;\n      saveState();\n      schedule(PUSH_WAIT_MASTER);\n    } else {\n      schedule(PUSH_WAIT_WORK);\n    }\n  }\n  function schedule(ms) {\n    clearTimeout(pushTimer);\n    pushTimer = setTimeout(function () { syncNow(); }, ms);\n  }\n\n  /* ======================================================================\n     現場写真の同期\n     ----------------------------------------------------------------------\n     置きかた\n       <置き場所>_plist   … 何が在るかの一覧（覚え書き・時刻・消した印）。小さい\n       <置き場所>_p_<id>  … 写真そのもの1枚。1件1枚ずつ\n\n     写真そのものは app.js の中（IndexedDB）にあって、ここからは触れない。\n     window.AirtecPhotos を通して受け渡す。暗号にするのはここの仕事。\n\n     1周でやること\n       ① この端末で増えた・直した・消したを見つける\n       ② 一覧をもらって突き合わせる（新しいほうが勝つ）\n       ③ 消えたものをこの端末からも消す\n       ④ 足りないものをもらう\n       ⑤ まだ預けていないものを預ける\n       ⑥ 一覧を書き戻す\n     ④⑤は枚数を区切る。残っていれば「まだある」と返して、すぐ次の周を回す。\n     ====================================================================== */\n  /** 写真の中身が入れ替わったかを見分ける印（回転すると変わる） */\n  function phMark(m) { return (m.w || 0) + 'x' + (m.h || 0) + ':' + (m.size || 0); }\n\n  function prunePhTombs() {\n    var limit = now() - TOMB_DAYS * 86400000;\n    Object.keys(S.ph.tomb).forEach(function (id) {\n      if (S.ph.tomb[id] < limit) {\n        delete S.ph.tomb[id];\n        delete S.ph.up[id];\n        delete S.ph.big[id];\n      }\n    });\n  }\n\n  /** 1周ぶんの写真の同期。まだ残っていれば true を返す */\n  async function syncPhotos(t) {\n    var api = window.AirtecPhotos;\n    if (!api) return false;\n\n    var localList = await api.list();\n    var localById = {};\n    localList.forEach(function (p) { localById[p.id] = p; });\n\n    /* ① この端末での増減 */\n    var touched = false;\n    Object.keys(localById).forEach(function (id) {\n      var h = strHash(JSON.stringify(localById[id]));\n      if (S.ph.shadow[id] !== h) {\n        S.ph.stamps[id] = t;\n        delete S.ph.tomb[id];\n        touched = true;\n      }\n    });\n    Object.keys(S.ph.shadow).forEach(function (id) {\n      if (!localById[id]) {\n        S.ph.tomb[id] = t;\n        delete S.ph.stamps[id];\n        delete S.ph.big[id];\n        // up はわざと残す。「クラウドにも1枚ある」という覚えで、\n        // これを頼りに下で本体を片付ける（消したら up も消える）\n        touched = true;\n      }\n    });\n\n    /* ② クラウドの一覧 */\n    var doc = await docGet('plist');\n    var remote = (doc && doc.blob) ? await unseal(doc.blob) : null;\n    var rMeta  = (remote && remote.meta)   || {};\n    var rStamp = (remote && remote.stamps) || {};\n    var rTomb  = (remote && remote.tomb)   || {};\n\n    var ids = {};\n    [localById, rMeta, S.ph.stamps, S.ph.tomb, rStamp, rTomb].forEach(function (o) {\n      Object.keys(o).forEach(function (id) { ids[id] = 1; });\n    });\n\n    var meta = {}, stamps = {}, tomb = {};\n    var want = [], drop = [], fix = [];\n    Object.keys(ids).forEach(function (id) {\n      var lS = S.ph.stamps[id] || 0, lT = S.ph.tomb[id] || 0;\n      var rS = rStamp[id] || 0,      rT = rTomb[id] || 0;\n      var st = Math.max(lS, rS), tb = Math.max(lT, rT);\n\n      if (tb && tb >= st) {                       // 消えている\n        tomb[id] = tb;\n        if (localById[id]) drop.push(id);\n        return;\n      }\n      if (!st) return;                            // どちらにも印が無い＝知らない\n      stamps[id] = st;\n\n      var useRemote = (rS > lS) && rMeta[id];\n      meta[id] = useRemote ? rMeta[id] : (localById[id] || rMeta[id]);\n      if (!meta[id]) { delete stamps[id]; return; }\n\n      if (!localById[id]) {\n        want.push(id);                            // この端末に無い → もらう\n      } else if (useRemote) {\n        // 相手のほうが新しい。写真そのものが入れ替わっている（回した）なら\n        // もらい直す。覚え書きだけの違いなら、その文字だけ入れ替える\n        if (phMark(rMeta[id]) !== phMark(localById[id])) want.push(id);\n        else fix.push(id);\n      }\n    });\n\n    var changed = false;\n\n    /* ③ 消えたもの */\n    for (var a = 0; a < drop.length; a++) {\n      await api.del(drop[a]);\n      changed = true;          // up はそのまま。クラウド側の本体は下で片付ける\n    }\n\n    /* 覚え書き・種類だけ直されたもの */\n    for (var b = 0; b < fix.length; b++) {\n      await api.setMeta(fix[b], meta[fix[b]]);\n      changed = true;\n    }\n\n    /* ④ 足りないものをもらう */\n    var pending = 0, got = {};\n    for (var i = 0; i < want.length; i++) {\n      if (i >= PH_DOWN_PER_ROUND) { pending += want.length - i; break; }\n      var id2 = want[i];\n      var d2 = await docGet('p_' + id2);\n      if (!d2 || !d2.blob) {                      // 一覧にはあるが本体がまだ無い\n        delete stamps[id2]; delete meta[id2];\n        continue;\n      }\n      var body = await unseal(d2.blob);\n      var m2 = meta[id2];\n      await api.put({\n        id: id2, siteId: m2.siteId, kind: m2.kind, memo: m2.memo,\n        at: m2.at, w: m2.w, h: m2.h, data: body.data\n      });\n      S.ph.up[id2] = phMark(m2);                  // もらった＝すでに預かってある\n      got[id2] = 1;\n      changed = true;\n    }\n\n    /* ⑤ まだ預けていないものを預ける */\n    var mine = Object.keys(meta).filter(function (id) {\n      if (got[id]) return false;                  // いまもらったばかり。送り返さない\n      if (!localById[id] || S.ph.big[id]) return false;\n      return S.ph.up[id] !== phMark(localById[id]);\n    });\n    var uploaded = 0;\n    for (var j = 0; j < mine.length; j++) {\n      if (j >= PH_UP_PER_ROUND) { pending += mine.length - j; break; }\n      var id3 = mine[j];\n      var full = await api.get(id3);\n      if (!full) continue;\n      var blob3 = await seal({ v: 1, data: full.data });\n      if (blob3.length > MAX_BLOB) {\n        // 大きすぎる → もう一段小さくして、もう一度だけ試す\n        if (await api.reshrink(id3)) {\n          full = await api.get(id3);\n          blob3 = full ? await seal({ v: 1, data: full.data }) : blob3;\n        }\n        if (!full || blob3.length > MAX_BLOB) { S.ph.big[id3] = 1; continue; }\n        meta[id3] = { id: id3, siteId: full.siteId, kind: full.kind, memo: full.memo,\n                      at: full.at, w: full.w, h: full.h, size: full.size };\n        changed = true;                           // 画面の枚数表示を直すため\n      }\n      await docPut('p_' + id3, blob3, t);\n      S.ph.up[id3] = phMark(full);      // いま預けた中身そのものの印を控える\n      uploaded++;\n    }\n\n    /* 消した写真の本体を、クラウドからも片付ける。\n       一覧から消えるだけでは置き場所（1GBまで）を食いつぶしてしまう。\n       預けたことを覚えている端末が片付ける。すでに無ければ何も起きない。 */\n    var swept = 0;\n    var junk = Object.keys(tomb).filter(function (id) { return S.ph.up[id]; });\n    for (var c = 0; c < junk.length && c < PH_UP_PER_ROUND; c++) {\n      try { await docDel('p_' + junk[c]); } catch (e3) { /* 次の周でまた試す */ continue; }\n      delete S.ph.up[junk[c]];\n      swept++;\n    }\n    if (junk.length > swept) pending += junk.length - swept;\n\n    /* ⑥ 一覧を書き戻す */\n    if (touched || uploaded || drop.length || fix.length || !doc ||\n        !sameMap(stamps, rStamp) || !sameMap(tomb, rTomb)) {\n      await docPut('plist', await seal({ v: 1, meta: meta, stamps: stamps, tomb: tomb }), t);\n    }\n\n    /* 覚え書きを取り直す（もらった・消したぶんを織り込む） */\n    var shadow = {};\n    (await api.list()).forEach(function (p) { shadow[p.id] = strHash(JSON.stringify(p)); });\n    S.ph.shadow = shadow;\n    S.ph.stamps = stamps;\n    S.ph.tomb   = tomb;\n    prunePhTombs();\n\n    S.ph.count = Object.keys(meta).length;\n    S.ph.bytes = Object.keys(meta).reduce(function (a2, id) {\n      return a2 + Number((meta[id] && meta[id].size) || 0);\n    }, 0);\n\n    if (changed && api.refresh) api.refresh();\n    return pending > 0;\n  }\n\n  async function pushWork(t) {\n    var payload = { v: 1, data: {}, stamps: {}, tomb: {} };\n    LISTS.forEach(function (l) {\n      payload.data[l.name]   = readList(l.name);\n      payload.stamps[l.name] = S.stamps[l.name];\n      payload.tomb[l.name]   = S.tomb[l.name];\n    });\n    await docPut('work', await seal(payload), t);\n  }\n\n  /** 1周ぶんの同期。取り込み → 送り出し */\n  async function syncNow(opts) {\n    opts = opts || {};\n    if (!READY || !S || !S.on || busy) return false;\n    if (!navigator.onLine) return false;\n    busy = true;\n    var changed = false;\n    try {\n      /* ---- 小さいデータ（現場・見積・請求書） ---- */\n      var t = now();\n      var localTouched = scanLocal(t);\n      pruneTombs();\n\n      var remoteDoc = await docGet('work');\n      var remote = null;\n      if (remoteDoc && remoteDoc.blob) remote = await unseal(remoteDoc.blob);\n\n      var needPush = localTouched || !remoteDoc || opts.force;\n      LISTS.forEach(function (l) {\n        var m = mergeList(l.name, remote);\n        var cur = readList(l.name);\n        S.stamps[l.name] = m.stamps;\n        S.tomb[l.name]   = m.tomb;\n        if (!sameList(cur, m.items)) {\n          lsSet(l.key, m.items);\n          var sh = {};\n          m.items.forEach(function (it) { sh[it.id] = strHash(JSON.stringify(it)); });\n          S.shadow[l.name] = sh;\n          changed = true;                       // 相手の変更が入った\n        }\n        if (!m.remoteSame) needPush = true;     // クラウド側に足りない分がある\n      });\n      if (needPush) await pushWork(t);\n\n      /* ---- 大きいデータ（単価マスタ・機種データ） ---- */\n      var mDoc = await docGet('master');\n      if (mDoc && mDoc.at > S.remoteMasterAt && mDoc.at > S.masterAt) {\n        var m2 = await unseal(mDoc.blob);       // 相手のほうが新しい → もらう\n        if (m2.pricebook) lsSet(KEY_PB, m2.pricebook);\n        if (m2.models) lsSet(KEY_MDL, m2.models); else localStorage.removeItem(KEY_MDL);\n        /* 別売品は「届いたときだけ」入れる。無いからといって消さない。\n           古い版の端末や、別売品を持っていない端末が送ってきた _master には入っていないので、\n           それで消すと、パソコンの別売品がスマホからの送信で消えてしまう */\n        if (m2.options) { lsSet(KEY_OPT, m2.options); S.optSent = true; }\n        S.remoteMasterAt = mDoc.at;\n        S.masterAt = mDoc.at;\n        changed = true;\n      } else if (pushMaster || !mDoc || (opts.force && S.masterAt > mDoc.at) ||\n                 (!S.optSent && lsGet(KEY_OPT, null))) {\n        /* 最後の条件：別売品を持っているのに、まだ一度も送っていない端末。\n           この版に上がった最初の1回だけ、変更が無くても送ってスマホに届ける */\n        var at = Math.max(S.masterAt, (mDoc ? mDoc.at : 0) + 1);\n        var master = { v: 1, pricebook: lsGet(KEY_PB, null), models: lsGet(KEY_MDL, null) };\n        var opt = lsGet(KEY_OPT, null);\n        if (opt) master.options = opt;          // 持っている端末だけが送る（上の「消さない」と対）\n        await docPut('master', await seal(master), at);\n        S.masterAt = at;\n        S.remoteMasterAt = at;\n        if (opt) S.optSent = true;\n        pushMaster = false;\n      }\n\n      /* ---- 現場写真（1枚ずつ） ----\n         写真は数が多いので1周ぶんずつ動かす。まだ残っていたら、\n         45秒待たずにすぐ次の周を回す。 */\n      var phMore = false, phErr = '';\n      try { phMore = await syncPhotos(t); }\n      catch (e2) { phErr = String(e2 && e2.message || e2); }\n\n      S.lastSync = now();\n      lastErr = '';             // 現場・見積・単価マスタはここまでで通っている\n      photoErr = phErr;         // 写真だけの不調は、写真の行に出す\n      saveState();\n      if (changed) onRemoteChange();\n      render();\n      if (phMore) schedule(PH_SOON_MS);\n      return true;\n    } catch (e) {\n      lastErr = String(e && e.message || e);\n      return false;\n    } finally {\n      /* 表示を直すのは必ず最後。busy を下ろす前に描くと\n         「同期中…」のまま止まって見えてしまう */\n      busy = false;\n      render();\n    }\n  }\n\n  /* ---- 相手の変更が入ったときの見せ方 ----\n     画面が持っている情報と食い違うので、読み込み直すのがいちばん安全。\n     入力の途中かもしれないので、編集画面にいるときは声をかけるだけにする。 */\n  function onRemoteChange() {\n    var editing = document.querySelector('#view-edit.is-active') ||\n                  document.querySelector('#view-master.is-active');\n    if (!editing) { location.reload(); return; }\n    pendingReload = true;\n    var bar = document.getElementById('sync-warn');\n    if (bar) bar.style.display = '';\n  }\n\n  /* ======================================================================\n     画面まわり\n     ====================================================================== */\n  function fmtTime(ms) {\n    if (!ms) return 'まだ';\n    var d = new Date(ms), p = function (n) { return ('0' + n).slice(-2); };\n    var today = new Date();\n    var same = d.toDateString() === today.toDateString();\n    return (same ? '' : (d.getMonth() + 1) + '/' + d.getDate() + ' ') + p(d.getHours()) + ':' + p(d.getMinutes());\n  }\n  function $(id) { return document.getElementById(id); }\n\n  function render() {\n    var st = $('sync-state'); if (!st) return;\n    var on = !!(S && S.on);\n    if (!READY) {\n      st.textContent = '準備中（クラウドの設定がまだ）';\n      st.className = 'sync-badge is-off';\n    } else if (!on) {\n      st.textContent = '連動していません';\n      st.className = 'sync-badge is-off';\n    } else if (lastErr) {\n      st.textContent = 'つながりません';\n      st.className = 'sync-badge is-err';\n    } else {\n      st.textContent = busy ? '同期中…' : '連動中';\n      st.className = 'sync-badge is-on';\n    }\n    $('sync-last').textContent = S ? fmtTime(S.lastSync) : 'まだ';\n    $('sync-err').textContent = lastErr ? ('（' + errText(lastErr) + '）') : '';\n\n    /* 写真は預けられる量に上限（1GB）がある。近づいたら分かるように数を出す */\n    var ph = $('sync-photos');\n    if (ph) {\n      if (!on) ph.textContent = '—';\n      else if (photoErr) ph.textContent = '預けられません（' + errText(photoErr) + '）';\n      else if (!S.ph || !S.ph.count) ph.textContent = '0枚';\n      else {\n        var mb = S.ph.bytes / 1048576;\n        var size = mb < 1 ? Math.round(S.ph.bytes / 1024) + 'KB'\n                 : (mb < 10 ? mb.toFixed(1) : Math.round(mb)) + 'MB';\n        ph.textContent = S.ph.count + '枚（' + size + ' / 1,000MBまで）';\n      }\n      ph.className = photoErr ? 'sync-err' : '';\n    }\n    $('sync-off-btns').style.display = on ? 'none' : '';\n    $('sync-on-btns').style.display  = on ? '' : 'none';\n    $('sync-code-view').textContent  = on ? fmtCode(S.code) : '';\n  }\n  function errText(e) {\n    if (/too-big/.test(e))      return '単価マスタが大きすぎます。手動バックアップをお使いください';\n    if (/^put:40[13]/.test(e))  return 'クラウド側の許可設定（ルール）を見直してください。写真を足したときは貼り直しが必要です';\n    if (/^get:40[13]/.test(e)) return 'クラウド側の許可設定を見直してください';\n    if (/login:/.test(e))       return 'ログインできません。設定を見直してください';\n    if (/OperationError|decrypt/i.test(e)) return 'あいことばが違うようです';\n    return e.slice(0, 60);\n  }\n\n  function toast(msg) {\n    var t = $('toast');\n    if (!t) { alert(msg); return; }\n    t.textContent = msg;\n    t.classList.add('is-show');\n    setTimeout(function () { t.classList.remove('is-show'); }, 2600);\n  }\n\n  async function startNew() {\n    if (!READY) { alert('クラウドの設定（firebase-config.js）がまだです。'); return; }\n    var code = makeCode();\n    S.code = code;\n    S.vault = await vaultIdOf(code);\n    S.on = true;\n    S.masterAt = now();\n    pushMaster = true;\n    saveState();\n    var okc = await syncNow({ force: true });\n    if (!okc) { S.on = false; saveState(); render(); alert('つながりませんでした：\\n' + errText(lastErr)); return; }\n    render();\n    showCode('この端末のデータをクラウドに預けました。\\nもう一方の端末で「あいことばを入れる」を押して、下のあいことばを入力してください。');\n  }\n\n  function showCode(msg) {\n    var box = $('sync-code-box');\n    if (!box) return;\n    $('sync-code-msg').textContent = msg || '';\n    $('sync-code-big').textContent = fmtCode(S.code);\n    box.style.display = '';\n    box.scrollIntoView({ behavior: 'smooth', block: 'center' });\n  }\n\n  async function joinExisting() {\n    if (!READY) { alert('クラウドの設定（firebase-config.js）がまだです。'); return; }\n    var input = prompt('もう一方の端末に出ている「あいことば」を入力してください。\\n（小文字・ハイフンなしでも大丈夫です）');\n    if (!input) return;\n    var code = normCode(input);\n    if (code.length !== 20) { alert('あいことばは20文字です。もう一度確かめてください。'); return; }\n\n    var hasLocal = LISTS.some(function (l) { return readList(l.name).length > 0; });\n    if (hasLocal && !confirm('この端末にもデータがあります。\\n\\n' +\n      '・現場／見積／請求書 … 両方を合体します（同じものは新しいほうが残ります）\\n' +\n      '・単価マスタ・機種データ … もう一方の端末のものになります\\n\\n' +\n      '進めますか？')) return;\n\n    return joinWith(code);\n  }\n\n  async function joinWith(input) {\n    var code = normCode(input);\n    S.code = code;\n    S.vault = await vaultIdOf(code);\n    keyCache = null;\n\n    /* 打ち間違い対策：そのあいことばの置き場所が本当にあるか、先に確かめる。\n       確かめずに始めると、誰ともつながらない“自分だけの置き場所”が\n       静かにできてしまい、あとで「同期されない」と悩むことになる。 */\n    var probe = null;\n    try { probe = await docGet('work'); }\n    catch (e) { lastErr = String(e.message || e); }\n    if (!probe) {\n      S.code = ''; S.vault = ''; S.on = false; saveState(); render();\n      alert(lastErr ? ('つながりませんでした：\\n' + errText(lastErr))\n                    : 'そのあいことばの預け先が見つかりませんでした。\\n打ち間違いがないか確かめてください。');\n      lastErr = '';\n      return false;\n    }\n    try { await unseal(probe.blob); }\n    catch (e) {\n      S.code = ''; S.vault = ''; S.on = false; keyCache = null; saveState(); render();\n      alert('あいことばが違うようです。もう一度確かめてください。');\n      return false;\n    }\n\n    /* 単価マスタは「すでに連動しているほう」を正とする。\n       あとから入る端末の古いマスタで上書きしないため。 */\n    S.masterAt = 0;\n    S.remoteMasterAt = 0;\n\n    S.on = true;\n    saveState();\n    scanLocal(now());          // 手持ちのデータを「今できたもの」として扱う\n    saveState();\n    var okj = await syncNow({ force: true });\n    if (!okj) {\n      S.on = false; S.code = ''; S.vault = ''; saveState(); render();\n      alert('つながりませんでした：\\n' + errText(lastErr));\n      return false;\n    }\n    toast('連動しました');\n    setTimeout(function () { location.reload(); }, 800);\n    return true;\n  }\n\n  function stopSync() {\n    if (!confirm('この端末の連動をやめます。\\n\\nこの端末のデータは残ります。クラウドに預けた分もそのまま残ります。\\nよろしいですか？')) return;\n    S.on = false;\n    S.code = '';\n    S.vault = '';\n    keyCache = null;\n    photoErr = '';\n    saveState();\n    localStorage.removeItem(K_AUTH);\n    render();\n    toast('連動をやめました');\n  }\n\n  /* ======================================================================\n     組み立て\n     ====================================================================== */\n  function wire() {\n    if (!$('sync-state')) return;\n    $('btn-sync-start').addEventListener('click', function () { startNew(); });\n    $('btn-sync-join').addEventListener('click', function () { joinExisting(); });\n    $('btn-sync-now').addEventListener('click', function () {\n      toast('同期しています…');\n      syncNow({ force: true }).then(function (ok) { toast(ok ? '同期しました' : '同期できませんでした'); });\n    });\n    $('btn-sync-show').addEventListener('click', function () { showCode('このあいことばを、もう一方の端末で入力してください。'); });\n    $('btn-sync-stop').addEventListener('click', function () { stopSync(); });\n    $('btn-sync-copy').addEventListener('click', function () {\n      navigator.clipboard.writeText(fmtCode(S.code)).then(function () { toast('コピーしました'); },\n        function () { toast('コピーできませんでした'); });\n    });\n    var warnBtn = $('btn-sync-reload');\n    if (warnBtn) warnBtn.addEventListener('click', function () { location.reload(); });\n\n    if (!READY) {\n      var note = $('sync-setup-note');\n      if (note) note.style.display = '';\n    }\n    render();\n  }\n\n  function boot() {\n    loadState();\n    wire();\n    if (!READY || !S.on) return;\n\n    scanLocal(now());\n    saveState();\n    syncNow();\n    setInterval(function () { if (!document.hidden) syncNow(); }, POLL_MS);\n    window.addEventListener('focus', function () { syncNow(); });\n    document.addEventListener('visibilitychange', function () { if (!document.hidden) syncNow(); });\n    window.addEventListener('online', function () { syncNow(); });\n  }\n\n  /* app.js から「保存したよ」と教えてもらう窓口 */\n  window.AirtecSync = {\n    changed: function (key) { markChanged(key); },\n    isOn: function () { return !!(S && S.on); }\n  };\n\n  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot);\n  else boot();\n})();\n"]]});
